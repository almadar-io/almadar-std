name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ${{ vars.ALMADAR_RUNNER || 'ubuntu-latest' }}
    strategy:
      matrix:
        node-version: [22, 24]

    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          # setup-node@v5 turns package-manager-cache ON by default, and this
          # repo declares `packageManager: pnpm@...`, so it shells out to pnpm
          # for the cache path — before the step below has installed pnpm. That
          # failed every CI run from 2026-08-29 onward with
          # "Unable to locate executable file: pnpm". Publishing was unaffected
          # (publish.yml installs pnpm first), which is why it went unnoticed.
          package-manager-cache: false

      # pnpm/action-setup@v6 ignores the `version:` spec on some minor
      # versions (observed installing 11.0.0-beta.4-1 when asked for 10.30.3).
      # Install pnpm via npm with an exact pin instead.
      - name: Install pnpm 10.30.3 via npm
        run: npm install -g pnpm@10.30.3
      - run: pnpm --version
      - run: pnpm install --no-frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Test
        run: |
          if node -e "process.exit(require('./package.json').scripts?.test ? 0 : 1)"; then
            pnpm test
          else
            echo "no test script; skipping"
          fi

      - name: Lint
        run: pnpm exec eslint --no-warn-ignored --max-warnings 0 .

  # validate-service disabled (service .orb schemas need fixing)
  # validate-service:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Validate service schema
  #       run: echo "Disabled"
