app std-agent-conversation "1.0.0"
"std-agent-conversation as a Function"

orbital AgentConversationOrbital {
  entity AgentConversation [persistent: agentconversations] {
    id : string
    name : string
    description : string
    status : "active" | "inactive" | "pending" = "active"
    createdAt : string
    messages : [string] = []
    turnCount : number = 0
    lastMessage : string = ""
    tokenCount : number = 0
    role : string = "user"
    content : string = ""
    timestamp : string = ""
    toolName : string = ""
  }

  type AgentConversationLoaded     = Event AgentConversation                  "Fired when AgentConversation finishes loading"
  type AgentConversationLoadFailed = Event { message : string } "Fired when AgentConversation fails to load"
  type AgentConversationDeleted = Event { id : string }
  type AgentConversationDeleteFailed = Event { error : string, code : string }
  type AgentConversationSaved = Event { id : string }
  type AgentConversationSaveFailed = Event { error : string, code : string }
  trait AgentConversationThread -> AgentConversation [interaction, collection] {
    initial: idle
    state idle {
      INIT -> idle
        (fetch AgentConversation { emit: { success: "AgentConversationLoaded", failure: "AgentConversationLoadFailed" } })
        (render-ui main { type: "stack", direction: "vertical", gap: "lg", children: [{ type: "stack", direction: "horizontal", gap: "sm", justify: "between", align: "center", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "icon", name: "message-circle"}, { type: "typography", content: "AgentConversation Thread", variant: "h2" }] }, { type: "button", label: "New Message", action: "COMPOSE", variant: "primary", icon: "plus" }] }, { type: "divider" }, { type: "data-list", entity: "AgentConversation", fields: [], renderItem: ["fn", "item", { type: "stack", direction: "vertical", gap: "xs", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "badge", label: "@item.role" }, { type: "badge", label: "@item.status", variant: "outline" }, { type: "typography", variant: "caption", color: "muted", content: "@item.timestamp" }] }, { type: "typography", variant: "body", content: "@item.content" }] }] }] })
      COMPOSE -> composing
        (render-ui main { type: "stack", direction: "vertical", gap: "lg", children: [{ type: "stack", direction: "horizontal", gap: "sm", justify: "between", align: "center", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "icon", name: "message-circle"}, { type: "typography", content: "AgentConversation Thread", variant: "h2" }] }, { type: "button", label: "New Message", action: "COMPOSE", variant: "primary", icon: "plus"}] }, { type: "divider" }, { type: "data-list", entity: "AgentConversation", fields: [], renderItem: ["fn", "item", { type: "stack", direction: "vertical", gap: "xs", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "badge", label: "@item.role" }, { type: "badge", label: "@item.status", variant: "outline" }, { type: "typography", variant: "caption", color: "muted", content: "@item.timestamp" }] }, { type: "typography", variant: "body", content: "@item.content" }] }] }, { type: "form-section", fields: [], title: "Compose Message", children: [{ type: "textarea", placeholder: "Type your message..." }, { type: "stack", direction: "horizontal", gap: "sm", children: [{ type: "button", label: "Send", action: "SEND", variant: "primary", icon: "send" }] }] }] })
      CLEAR -> idle
        (persist delete AgentConversation  { emit: { success: "AgentConversationDeleted", failure: "AgentConversationDeleteFailed" } })
        (fetch AgentConversation { emit: { success: "AgentConversationLoaded", failure: "AgentConversationLoadFailed" } })
        (render-ui main { type: "stack", direction: "vertical", gap: "lg", children: [{ type: "stack", direction: "horizontal", gap: "sm", justify: "between", align: "center", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "icon", name: "message-circle"}, { type: "typography", content: "AgentConversation Thread", variant: "h2" }] }, { type: "button", label: "New Message", action: "COMPOSE", variant: "primary", icon: "plus" }] }, { type: "divider" }, { type: "data-list", entity: "AgentConversation", fields: [], renderItem: ["fn", "item", { type: "stack", direction: "vertical", gap: "xs", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "badge", label: "@item.role" }, { type: "badge", label: "@item.status", variant: "outline" }, { type: "typography", variant: "caption", color: "muted", content: "@item.timestamp" }] }, { type: "typography", variant: "body", content: "@item.content" }] }] }] })
    }
    state composing {
      SEND -> idle
        (set @entity.content @payload.content)
        (set @entity.role user)
        (set @entity.timestamp @now)
        (set @entity.status sent)
        (persist create AgentConversation { role: "user", content: "@payload.content", timestamp: "@now", status: "sent" } { emit: { success: "AgentConversationSaved", failure: "AgentConversationSaveFailed" } })
        (emit SEND_MESSAGE)
        (fetch AgentConversation { emit: { success: "AgentConversationLoaded", failure: "AgentConversationLoadFailed" } })
        (render-ui main { type: "stack", direction: "vertical", gap: "lg", children: [{ type: "stack", direction: "horizontal", gap: "sm", justify: "between", align: "center", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "icon", name: "message-circle"}, { type: "typography", content: "AgentConversation Thread", variant: "h2" }] }, { type: "button", label: "New Message", action: "COMPOSE", variant: "primary", icon: "plus" }] }, { type: "divider" }, { type: "data-list", entity: "AgentConversation", fields: [], renderItem: ["fn", "item", { type: "stack", direction: "vertical", gap: "xs", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "badge", label: "@item.role" }, { type: "badge", label: "@item.status", variant: "outline" }, { type: "typography", variant: "caption", color: "muted", content: "@item.timestamp" }] }, { type: "typography", variant: "body", content: "@item.content" }] }] }] })
      CLEAR -> idle
        (persist delete AgentConversation  { emit: { success: "AgentConversationDeleted", failure: "AgentConversationDeleteFailed" } })
        (fetch AgentConversation { emit: { success: "AgentConversationLoaded", failure: "AgentConversationLoadFailed" } })
        (render-ui main { type: "stack", direction: "vertical", gap: "lg", children: [{ type: "stack", direction: "horizontal", gap: "sm", justify: "between", align: "center", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "icon", name: "message-circle"}, { type: "typography", content: "AgentConversation Thread", variant: "h2" }] }, { type: "button", label: "New Message", action: "COMPOSE", variant: "primary", icon: "plus" }] }, { type: "divider" }, { type: "data-list", entity: "AgentConversation", fields: [], renderItem: ["fn", "item", { type: "stack", direction: "vertical", gap: "xs", children: [{ type: "stack", direction: "horizontal", gap: "sm", align: "center", children: [{ type: "badge", label: "@item.role" }, { type: "badge", label: "@item.status", variant: "outline" }, { type: "typography", variant: "caption", color: "muted", content: "@item.timestamp" }] }, { type: "typography", variant: "body", content: "@item.content" }] }] }] })
    }
    emits {
      SEND_MESSAGE internal { content: string }
    }
    listens {
      SEND { content : string! }
          AgentConversationLoadFailed { message : string }
    
      AgentConversationDeleteFailed { error : string, code : string }
      AgentConversationSaveFailed { error : string, code : string }
    }
    emits: [AgentConversationLoaded, AgentConversationLoadFailed, AgentConversationDeleted, AgentConversationDeleteFailed, AgentConversationSaved, AgentConversationSaveFailed]
  }
  trait AgentConversationAgent -> AgentConversation [interaction, collection] {
    initial: idle
    state idle {
      INIT -> idle
        (fetch AgentConversation { emit: { success: "AgentConversationLoaded", failure: "AgentConversationLoadFailed" } })
        (render-ui main { type: "empty-state", icon: "message-circle", title: "Conversation", description: "Conversation is ready" })
      SEND_MESSAGE -> active
        (set @entity.lastMessage @payload.content)
        (set @entity.turnCount (+ @entity.turnCount 1))
        (agent/generate @payload.content)
        (emit TOKEN_UPDATE)
    }
    state active {
      SEND_MESSAGE -> active
        (set @entity.lastMessage @payload.content)
        (set @entity.turnCount (+ @entity.turnCount 1))
        (agent/generate @payload.content)
        (emit TOKEN_UPDATE)
      PAUSE -> paused
      CLEAR -> idle
        (set @entity.messages [])
        (set @entity.turnCount 0)
        (set @entity.lastMessage "")
        (set @entity.tokenCount 0)
    }
    state paused {
      RESUME -> active
      CLEAR -> idle
        (set @entity.messages [])
        (set @entity.turnCount 0)
        (set @entity.lastMessage "")
        (set @entity.tokenCount 0)
    }
    emits {
      TOKEN_UPDATE internal { tokenCount: number }
    }
    listens {
      SEND_MESSAGE { content : string! }
          AgentConversationLoadFailed { message : string }
    }
    emits: [AgentConversationLoaded, AgentConversationLoadFailed]
  }
  page "/agentconversations" as AgentConversationPage -> AgentConversationThread, AgentConversationAgent
}