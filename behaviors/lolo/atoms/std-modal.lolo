app std-modal "1.0.0"
"std-modal as a Function"
;;
;; Modal-only trait: renders ONLY to the `modal` slot. The parent molecule's
;; layout/browse trait is responsible for the `main` slot (the list, the
;; trigger buttons, etc.). Consumers configure icon/title/fields/mode at
;; the trait-ref call site via `config: { ... }`. Side effects (persist,
;; notify) belong in a sibling coordinator trait that
;; `listens { ModalTrait SAVE -> DO_PERSIST }`.

orbital ModalRecordOrbital {
  entity ModalRecord [runtime] {
    id : string
    name : string
    description : string
    status : "active" | "inactive" | "pending" = "active"
    createdAt : string
  }

  trait ModalRecordModal -> ModalRecord [interaction, instance] {
    config {
      icon : string = "layout-panel-top"
      title : string = "Details"
      fields : [string] = []
      mode : string = "create"
    }
    initial: closed
    state closed {
      INIT -> closed
        (fetch ModalRecord)
      OPEN -> open
        (render-ui modal { type: "stack", direction: "vertical", gap: "md", children: [{ type: "stack", direction: "horizontal", gap: "sm", children: [{ type: "icon", name: "@config.icon", size: "md" }, { type: "typography", content: "@config.title", variant: "h3" }] }, { type: "divider" }, { type: "form-section", entity: "ModalRecord", mode: "@config.mode", submitEvent: "SAVE", cancelEvent: "CLOSE", fields: "@config.fields" }] })
    }
    state open {
      CLOSE -> closed
        (render-ui modal null)
        (notify Cancelled info)
      SAVE -> closed
        (render-ui modal null)
    }
    ;; consumer: a sibling coordinator trait listens for SAVE to run (persist create ...)
    listens {
      OPEN { id : string }
      SAVE { data : object! }
    }
  }
  page "/modalrecords/modal" as ModalRecordModalPage -> ModalRecordModal
}
