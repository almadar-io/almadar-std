/**
 * RatingReviewBoard Event Handlers
 *
 * Server-side event handlers with guard enforcement.
 * All data operations flow through these handlers.
 *
 * @generated by Orbital Compiler
 * @packageDocumentation
 */

import type { Request, Response, NextFunction } from 'express';
import { getDataService, logger, createLogger as createServerLogger, getServerEventBus, getRuntimeEntity } from '@almadar/server';
const effectsLog = createServerLogger('almadar:server:effects');
import type { EntityRow, EventPayload, EventPayloadValue, FieldValue } from '@almadar/core';
import type { EventRequest, EventResponse, EffectResult, EmittedEvent, UserContext } from '@app/shared';
import type { ReviewView } from '@app/shared';

const __ORBITAL_SOURCE = { orbital: 'RatingReviewOrbital', trait: 'RatingReviewBoard' } as const;

type RatingReviewBoardState = 'loading' | 'viewing' | 'composing' | 'submitting' | 'submitted' | 'error';

type RatingReviewBoardEvent = 'INIT' | 'ReviewsLoaded' | 'ReviewsLoadFailed' | 'ReviewSaved' | 'ReviewSaveFailed' | 'WRITE_REVIEW' | 'CHANGE_SORT' | 'MARK_HELPFUL' | 'RATE_DRAFT' | 'SUBMIT_REVIEW' | 'CANCEL_REVIEW' | 'RESTART';

/**
 * Handle events for RatingReviewBoard trait
 */
export async function handleEvent(
  req: Request,
  res: Response,
  _next: NextFunction
): Promise<void> {
  const { event, payload, currentState } = req.body as EventRequest;
  const user: UserContext | undefined = req.firebaseUser
    ? { id: req.firebaseUser.uid, email: req.firebaseUser.email ?? undefined }
    : undefined;

  const state: RatingReviewBoardState = (currentState as RatingReviewBoardState) || 'loading';

  try {
    let response: EventResponse;

    switch (event) {
      case 'INIT':
        response = await handleInit(state, payload, user);
        break;

      case 'ReviewsLoaded':
        response = await handleReviewsloaded(state, payload as { data: EntityRow[] } | undefined, user);
        break;

      case 'ReviewsLoadFailed':
        response = await handleReviewsloadfailed(state, payload as { error: string; code: string } | undefined, user);
        break;

      case 'ReviewSaved':
        response = await handleReviewsaved(state, payload as { row: Record<string, FieldValue | undefined> } | undefined, user);
        break;

      case 'ReviewSaveFailed':
        response = await handleReviewsavefailed(state, payload as { error: string; code: string } | undefined, user);
        break;

      case 'WRITE_REVIEW':
        response = await handleWriteReview(state, payload, user);
        break;

      case 'CHANGE_SORT':
        response = await handleChangeSort(state, payload as { tabId: string } | undefined, user);
        break;

      case 'MARK_HELPFUL':
        response = await handleMarkHelpful(state, payload as { id: string } | undefined, user);
        break;

      case 'RATE_DRAFT':
        response = await handleRateDraft(state, payload as { rating: number } | undefined, user);
        break;

      case 'SUBMIT_REVIEW':
        response = await handleSubmitReview(state, payload as { data: Record<string, FieldValue | undefined> } | undefined, user);
        break;

      case 'CANCEL_REVIEW':
        response = await handleCancelReview(state, payload, user);
        break;

      case 'RESTART':
        response = await handleRestart(state, payload, user);
        break;

      default:
        response = {
          success: false,
          error: `Unknown event: ${event}`,
        };
    }

    res.json(response);
  } catch (error) {
    logger.error('[RatingReviewBoard] Event handler error:', { error: error instanceof Error ? error.message : String(error) });
    res.status(500).json({
      success: false,
      error: error instanceof Error ? error.message : 'Internal server error',
    });
  }
}

/**
 * Handle INIT event
 */
async function handleInit(
  state: RatingReviewBoardState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Fetch ReviewView entities
            {
      const items = await getDataService().list<ReviewView>('review_view');
        data['ReviewView'] = items;
        const __totalCount_ReviewView = data['ReviewView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'ReviewView',
        success: true,
        data: data['ReviewView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['ReviewView'], totalCount: __totalCount_ReviewView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'ReviewsLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('ReviewsLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'ReviewsLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    case 'error':
    {
      // Execute server-side effects
            // Fetch ReviewView entities
            {
      const items = await getDataService().list<ReviewView>('review_view');
        data['ReviewView'] = items;
        const __totalCount_ReviewView = data['ReviewView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'ReviewView',
        success: true,
        data: data['ReviewView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['ReviewView'], totalCount: __totalCount_ReviewView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'ReviewsLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('ReviewsLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'ReviewsLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process INIT from state ${state}`,
      };
  }
}

/**
 * Handle ReviewsLoaded event
 */
async function handleReviewsloaded(
  state: RatingReviewBoardState,
  payload: { data: EntityRow[] } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.reviewsSource = (((payload as EventPayload | undefined)?.data ?? []) as readonly EntityRow[]).map(((row: EntityRow) => ({ helpful: (("helpful" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 0), rating: (("rating" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 5), authorName: (("authorName" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "Guest"), id: (("id" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? ""), createdAt: (("createdAt" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? ""), avatarIcon: (("avatarIcon" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "user"), comment: (("comment" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "") })))
            if (entity) {
      entity.reviewsSource = ((((payload as EventPayload | undefined)?.data ?? []) as readonly EntityRow[]).map(((row: EntityRow) => ({ helpful: (("helpful" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 0), rating: (("rating" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 5), authorName: (("authorName" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "Guest"), id: (("id" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? ""), createdAt: (("createdAt" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? ""), avatarIcon: (("avatarIcon" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "user"), comment: (("comment" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? "") })))) as typeof entity['reviewsSource'];
            }

            // Set entity?.reviews = entity?.reviewsSource
            if (entity) {
      entity.reviews = (entity?.reviewsSource) as typeof entity['reviews'];
            }

            // Set entity?.totalReviews = (((payload as EventPayload | undefined)?.data ?? []) as { readonly length: number }).length
            if (entity) {
      entity.totalReviews = ((((payload as EventPayload | undefined)?.data ?? []) as { readonly length: number }).length) as typeof entity['totalReviews'];
            }

            // Set entity?.averageRating = ((arr: readonly EntityRow[]) => arr.length ? arr.reduce((s: number, v: EntityRow) => s + (Number(v) || 0), 0) / arr.length : 0)((((((payload as EventPayload | undefined)?.data ?? []) as readonly EntityRow[]).map(((row: EntityRow) => (("rating" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 5))) ?? []) as readonly EntityRow[]))
            if (entity) {
      entity.averageRating = (((arr: readonly EntityRow[]) => arr.length ? arr.reduce((s: number, v: EntityRow) => s + (Number(v) || 0), 0) / arr.length : 0)((((((payload as EventPayload | undefined)?.data ?? []) as readonly EntityRow[]).map(((row: EntityRow) => (("rating" as string).split('.').reduce((o: any, k: string) => o?.[k], row) ?? 5))) ?? []) as readonly EntityRow[]))) as typeof entity['averageRating'];
            }

            // Set entity?.starDistribution = [{ percentage: 74, label: "5 stars" }, { percentage: 18, label: "4 stars" }, { label: "3 stars", percentage: 5 }, { percentage: 2, label: "2 stars" }, { percentage: 1, label: "1 star" }]
            if (entity) {
      entity.starDistribution = ([{ percentage: 74, label: "5 stars" }, { percentage: 18, label: "4 stars" }, { label: "3 stars", percentage: 5 }, { percentage: 2, label: "2 stars" }, { percentage: 1, label: "1 star" }]) as typeof entity['starDistribution'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'viewing',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process ReviewsLoaded from state ${state}`,
      };
  }
}

/**
 * Handle ReviewsLoadFailed event
 */
async function handleReviewsloadfailed(
  state: RatingReviewBoardState,
  payload: { error: string; code: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.errorMessage = (payload as EventPayload | undefined)?.error
            if (entity) {
      entity.errorMessage = ((payload as EventPayload | undefined)?.error) as typeof entity['errorMessage'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'error',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process ReviewsLoadFailed from state ${state}`,
      };
  }
}

/**
 * Handle ReviewSaved event
 */
async function handleReviewsaved(
  state: RatingReviewBoardState,
  payload: { row: Record<string, FieldValue | undefined> } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Fetch ReviewView entities
            {
      const items = await getDataService().list<ReviewView>('review_view');
        data['ReviewView'] = items;
        const __totalCount_ReviewView = data['ReviewView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'ReviewView',
        success: true,
        data: data['ReviewView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['ReviewView'], totalCount: __totalCount_ReviewView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'ReviewsLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('ReviewsLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'ReviewsLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    case 'submitting':
    {
      // Execute server-side effects

      return {
        success: true,
        newState: 'submitted',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process ReviewSaved from state ${state}`,
      };
  }
}

/**
 * Handle ReviewSaveFailed event
 */
async function handleReviewsavefailed(
  state: RatingReviewBoardState,
  payload: { error: string; code: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.errorMessage = (payload as EventPayload | undefined)?.error
            if (entity) {
      entity.errorMessage = ((payload as EventPayload | undefined)?.error) as typeof entity['errorMessage'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'error',
        data,
        effectResults,
        emittedEvents,
      };
    }

    case 'submitting':
    {
      // Execute server-side effects
            // Set entity?.errorMessage = (payload as EventPayload | undefined)?.error
            if (entity) {
      entity.errorMessage = ((payload as EventPayload | undefined)?.error) as typeof entity['errorMessage'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'error',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process ReviewSaveFailed from state ${state}`,
      };
  }
}

/**
 * Handle WRITE_REVIEW event
 */
async function handleWriteReview(
  state: RatingReviewBoardState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'viewing':
    {
      // Execute server-side effects
            // Set entity?.draftRating = 0
            if (entity) {
      entity.draftRating = (0) as typeof entity['draftRating'];
            }

            // Set entity?.draftComment = ""
            if (entity) {
      entity.draftComment = ("") as typeof entity['draftComment'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'composing',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process WRITE_REVIEW from state ${state}`,
      };
  }
}

/**
 * Handle CHANGE_SORT event
 */
async function handleChangeSort(
  state: RatingReviewBoardState,
  payload: { tabId: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'viewing':
    {
      if (((payload as EventPayload | undefined)?.tabId === 'highest')) {
        // Execute server-side effects
              // Set entity?.currentSort = "highest"
              if (entity) {
        entity.currentSort = ("highest") as typeof entity['currentSort'];
              }

              // Set entity?.reviews = [...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["rating"], bv = (b as EntityRow)?.["rating"]; const dir = (("desc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })
              if (entity) {
        entity.reviews = ([...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["rating"], bv = (b as EntityRow)?.["rating"]; const dir = (("desc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })) as typeof entity['reviews'];
              }


        // Persist entity mutations (auto-generated from set effects)
        if (entity && entityId) {
          await getDataService().update<ReviewView>('review_view', entityId, entity);
          effectResults.push({
            effect: 'persist',
            action: 'update',
            entityType: 'ReviewView',
            success: true,
            data: entity,
          });
        }

        return {
          success: true,
          newState: 'viewing',
          data,
          effectResults,
          emittedEvents,
        };
      } else if (((payload as EventPayload | undefined)?.tabId === 'lowest')) {
        // Execute server-side effects
              // Set entity?.currentSort = "lowest"
              if (entity) {
        entity.currentSort = ("lowest") as typeof entity['currentSort'];
              }

              // Set entity?.reviews = [...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["rating"], bv = (b as EntityRow)?.["rating"]; const dir = (("asc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })
              if (entity) {
        entity.reviews = ([...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["rating"], bv = (b as EntityRow)?.["rating"]; const dir = (("asc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })) as typeof entity['reviews'];
              }


        // Persist entity mutations (auto-generated from set effects)
        if (entity && entityId) {
          await getDataService().update<ReviewView>('review_view', entityId, entity);
          effectResults.push({
            effect: 'persist',
            action: 'update',
            entityType: 'ReviewView',
            success: true,
            data: entity,
          });
        }

        return {
          success: true,
          newState: 'viewing',
          data,
          effectResults,
          emittedEvents,
        };
      } else if (((payload as EventPayload | undefined)?.tabId === 'recent')) {
        // Execute server-side effects
              // Set entity?.currentSort = "recent"
              if (entity) {
        entity.currentSort = ("recent") as typeof entity['currentSort'];
              }

              // Set entity?.reviews = [...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["createdAt"], bv = (b as EntityRow)?.["createdAt"]; const dir = (("desc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })
              if (entity) {
        entity.reviews = ([...(entity?.reviewsSource ?? [])].sort((a: EntityRow, b: EntityRow) => { const av = (a as EntityRow)?.["createdAt"], bv = (b as EntityRow)?.["createdAt"]; const dir = (("desc") as string) === 'desc' ? -1 : 1; if ((av as number) < (bv as number)) return -1 * dir; if ((av as number) > (bv as number)) return 1 * dir; return 0; })) as typeof entity['reviews'];
              }


        // Persist entity mutations (auto-generated from set effects)
        if (entity && entityId) {
          await getDataService().update<ReviewView>('review_view', entityId, entity);
          effectResults.push({
            effect: 'persist',
            action: 'update',
            entityType: 'ReviewView',
            success: true,
            data: entity,
          });
        }

        return {
          success: true,
          newState: 'viewing',
          data,
          effectResults,
          emittedEvents,
        };
      } else {
        return {
          success: false,
          error: 'Access denied: guard condition not met',
        };
      }
    }

    default:
      return {
        success: false,
        error: `Cannot process CHANGE_SORT from state ${state}`,
      };
  }
}

/**
 * Handle MARK_HELPFUL event
 */
async function handleMarkHelpful(
  state: RatingReviewBoardState,
  payload: { id: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'viewing':
    {
      // Execute server-side effects

      return {
        success: true,
        newState: 'viewing',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process MARK_HELPFUL from state ${state}`,
      };
  }
}

/**
 * Handle RATE_DRAFT event
 */
async function handleRateDraft(
  state: RatingReviewBoardState,
  payload: { rating: number } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'composing':
    {
      // Execute server-side effects
            // Set entity?.draftRating = (payload as EventPayload | undefined)?.rating
            if (entity) {
      entity.draftRating = ((payload as EventPayload | undefined)?.rating) as typeof entity['draftRating'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'composing',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process RATE_DRAFT from state ${state}`,
      };
  }
}

/**
 * Handle SUBMIT_REVIEW event
 */
async function handleSubmitReview(
  state: RatingReviewBoardState,
  payload: { data: Record<string, FieldValue | undefined> } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'composing':
    {
      // Execute server-side effects
            // Persist ReviewView (create)
            {
      effectsLog.debug('persist:dispatch', { action: 'create', entityType: 'ReviewView', payloadKeys: payload && typeof payload === 'object' ? Object.keys(payload).join(',') : null });
      const createData = ({ rating: entity?.draftRating, comment: entity?.draftComment }) as Record<string, FieldValue | undefined> as Partial<ReviewView> | undefined;
      const created = createData ? await getDataService().create<ReviewView>('review_view', createData) : null;
      effectsLog.debug('persist:store-mutate', { action: 'create', entityType: 'ReviewView', resultId: created?.id, success: !!created });
      effectResults.push({
        effect: 'persist',
        action: 'create',
        entityType: 'ReviewView',
        success: !!created,
        data: created,
      });
      { const __evV: EventPayloadValue | undefined = (created) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'ReviewSaved', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('ReviewSaved', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'ReviewSaved', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'submitting',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process SUBMIT_REVIEW from state ${state}`,
      };
  }
}

/**
 * Handle CANCEL_REVIEW event
 */
async function handleCancelReview(
  state: RatingReviewBoardState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'composing':
    {
      // Execute server-side effects
            // Set entity?.draftRating = 0
            if (entity) {
      entity.draftRating = (0) as typeof entity['draftRating'];
            }

            // Set entity?.draftComment = ""
            if (entity) {
      entity.draftComment = ("") as typeof entity['draftComment'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<ReviewView>('review_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'ReviewView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'viewing',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process CANCEL_REVIEW from state ${state}`,
      };
  }
}

/**
 * Handle RESTART event
 */
async function handleRestart(
  state: RatingReviewBoardState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-rating-review', 'RatingReviewBoard', entityId) as ReviewView;

  switch (state) {
    case 'submitted':
    {
      // Execute server-side effects
            // Fetch ReviewView entities
            {
      const items = await getDataService().list<ReviewView>('review_view');
        data['ReviewView'] = items;
        const __totalCount_ReviewView = data['ReviewView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'ReviewView',
        success: true,
        data: data['ReviewView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['ReviewView'], totalCount: __totalCount_ReviewView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'ReviewsLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('ReviewsLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'ReviewsLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process RESTART from state ${state}`,
      };
  }
}

