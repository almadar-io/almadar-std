/**
 * WizardForm Event Handlers
 *
 * Server-side event handlers with guard enforcement.
 * All data operations flow through these handlers.
 *
 * @generated by Orbital Compiler
 * @packageDocumentation
 */

import type { Request, Response, NextFunction } from 'express';
import { getDataService, logger, createLogger as createServerLogger, getServerEventBus, getRuntimeEntity } from '@almadar/server';
const effectsLog = createServerLogger('almadar:server:effects');
import type { EntityRow, EventPayload, EventPayloadValue, FieldValue } from '@almadar/core';
import type { EventRequest, EventResponse, EffectResult, EmittedEvent, UserContext } from '@app/shared';
import type { WizardView } from '@app/shared';

const __ORBITAL_SOURCE = { orbital: 'WizardOrbital', trait: 'WizardForm' } as const;

type WizardFormState = 'loading' | 'running' | 'completed' | 'cancelled' | 'error';

type WizardFormEvent = 'INIT' | 'WizardLoaded' | 'WizardLoadFailed' | 'WizardSaved' | 'WizardSaveFailed' | 'ADVANCE' | 'RETREAT' | 'CANCEL' | 'RESTART';

/**
 * Handle events for WizardForm trait
 */
export async function handleEvent(
  req: Request,
  res: Response,
  _next: NextFunction
): Promise<void> {
  const { event, payload, currentState } = req.body as EventRequest;
  const user: UserContext | undefined = req.firebaseUser
    ? { id: req.firebaseUser.uid, email: req.firebaseUser.email ?? undefined }
    : undefined;

  const state: WizardFormState = (currentState as WizardFormState) || 'loading';

  try {
    let response: EventResponse;

    switch (event) {
      case 'INIT':
        response = await handleInit(state, payload, user);
        break;

      case 'WizardLoaded':
        response = await handleWizardloaded(state, payload as { data: EntityRow[] } | undefined, user);
        break;

      case 'WizardLoadFailed':
        response = await handleWizardloadfailed(state, payload as { code: string; error: string } | undefined, user);
        break;

      case 'WizardSaved':
        response = await handleWizardsaved(state, payload as { row: Record<string, FieldValue | undefined> } | undefined, user);
        break;

      case 'WizardSaveFailed':
        response = await handleWizardsavefailed(state, payload as { code: string; error: string } | undefined, user);
        break;

      case 'ADVANCE':
        response = await handleAdvance(state, payload as { data: Record<string, FieldValue | undefined> } | undefined, user);
        break;

      case 'RETREAT':
        response = await handleRetreat(state, payload, user);
        break;

      case 'CANCEL':
        response = await handleCancel(state, payload as { reason: string } | undefined, user);
        break;

      case 'RESTART':
        response = await handleRestart(state, payload, user);
        break;

      default:
        response = {
          success: false,
          error: `Unknown event: ${event}`,
        };
    }

    res.json(response);
  } catch (error) {
    logger.error('[WizardForm] Event handler error:', { error: error instanceof Error ? error.message : String(error) });
    res.status(500).json({
      success: false,
      error: error instanceof Error ? error.message : 'Internal server error',
    });
  }
}

/**
 * Handle INIT event
 */
async function handleInit(
  state: WizardFormState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Fetch WizardView entities
            {
      const items = await getDataService().list<WizardView>('wizard_view');
        data['WizardView'] = items;
        const __totalCount_WizardView = data['WizardView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'WizardView',
        success: true,
        data: data['WizardView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['WizardView'], totalCount: __totalCount_WizardView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'WizardLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('WizardLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'WizardLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    case 'error':
    {
      // Execute server-side effects

      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process INIT from state ${state}`,
      };
  }
}

/**
 * Handle WizardLoaded event
 */
async function handleWizardloaded(
  state: WizardFormState,
  payload: { data: EntityRow[] } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.currentStepIndex = 0
            if (entity) {
      entity.currentStepIndex = (0) as typeof entity['currentStepIndex'];
            }

            // Set entity?.totalSteps = ([{ fields: ["title", "description"], label: "Details", key: "details", icon: "file-text", description: "Basic information" }, { icon: "settings", key: "options", fields: ["category", "priority"], description: "Configure preferences", label: "Options" }, { description: "Confirm and submit", fields: ["notes"], key: "review", label: "Review", icon: "check-circle" }]).length
            if (entity) {
      entity.totalSteps = (([{ fields: ["title", "description"], label: "Details", key: "details", icon: "file-text", description: "Basic information" }, { icon: "settings", key: "options", fields: ["category", "priority"], description: "Configure preferences", label: "Options" }, { description: "Confirm and submit", fields: ["notes"], key: "review", label: "Review", icon: "check-circle" }]).length) as typeof entity['totalSteps'];
            }

            // Set entity?.wizardSteps = [{ description: "Basic information", title: "Details", id: "details" }, { id: "options", title: "Options", description: "Configure preferences" }, { id: "review", description: "Confirm and submit", title: "Review" }]
            if (entity) {
      entity.wizardSteps = ([{ description: "Basic information", title: "Details", id: "details" }, { id: "options", title: "Options", description: "Configure preferences" }, { id: "review", description: "Confirm and submit", title: "Review" }]) as typeof entity['wizardSteps'];
            }

            // Set entity?.currentStepLabel = (("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ icon: "file-text", description: "Basic information", key: "details", label: "Details", fields: ["title", "description"] }, { label: "Options", fields: ["category", "priority"], key: "options", icon: "settings", description: "Configure preferences" }, { icon: "check-circle", fields: ["notes"], key: "review", description: "Confirm and submit", label: "Review" }], 0)))
            if (entity) {
      entity.currentStepLabel = ((("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ icon: "file-text", description: "Basic information", key: "details", label: "Details", fields: ["title", "description"] }, { label: "Options", fields: ["category", "priority"], key: "options", icon: "settings", description: "Configure preferences" }, { icon: "check-circle", fields: ["notes"], key: "review", description: "Confirm and submit", label: "Review" }], 0)))) as typeof entity['currentStepLabel'];
            }

            // Set entity?.currentStepDescription = (("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", icon: "file-text", key: "details", fields: ["title", "description"] }, { key: "options", label: "Options", icon: "settings", description: "Configure preferences", fields: ["category", "priority"] }, { icon: "check-circle", fields: ["notes"], label: "Review", key: "review", description: "Confirm and submit" }], 0)) ?? "")
            if (entity) {
      entity.currentStepDescription = ((("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", icon: "file-text", key: "details", fields: ["title", "description"] }, { key: "options", label: "Options", icon: "settings", description: "Configure preferences", fields: ["category", "priority"] }, { icon: "check-circle", fields: ["notes"], label: "Review", key: "review", description: "Confirm and submit" }], 0)) ?? "")) as typeof entity['currentStepDescription'];
            }

            // Set entity?.currentStepIcon = (("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ fields: ["title", "description"], key: "details", description: "Basic information", label: "Details", icon: "file-text" }, { key: "options", label: "Options", fields: ["category", "priority"], icon: "settings", description: "Configure preferences" }, { label: "Review", description: "Confirm and submit", icon: "check-circle", fields: ["notes"], key: "review" }], 0)) ?? "circle")
            if (entity) {
      entity.currentStepIcon = ((("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ fields: ["title", "description"], key: "details", description: "Basic information", label: "Details", icon: "file-text" }, { key: "options", label: "Options", fields: ["category", "priority"], icon: "settings", description: "Configure preferences" }, { label: "Review", description: "Confirm and submit", icon: "check-circle", fields: ["notes"], key: "review" }], 0)) ?? "circle")) as typeof entity['currentStepIcon'];
            }

            // Set entity?.currentFields = (("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", key: "details", description: "Basic information", icon: "file-text", fields: ["title", "description"] }, { key: "options", fields: ["category", "priority"], icon: "settings", label: "Options", description: "Configure preferences" }, { icon: "check-circle", description: "Confirm and submit", key: "review", fields: ["notes"], label: "Review" }], 0)) ?? [])
            if (entity) {
      entity.currentFields = ((("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", key: "details", description: "Basic information", icon: "file-text", fields: ["title", "description"] }, { key: "options", fields: ["category", "priority"], icon: "settings", label: "Options", description: "Configure preferences" }, { icon: "check-circle", description: "Confirm and submit", key: "review", fields: ["notes"], label: "Review" }], 0)) ?? [])) as typeof entity['currentFields'];
            }

            // Set entity?.isFirstStep = true
            if (entity) {
      entity.isFirstStep = (true) as typeof entity['isFirstStep'];
            }

            // Set entity?.isLastStep = (([{ fields: ["title", "description"], key: "details", icon: "file-text", description: "Basic information", label: "Details" }, { fields: ["category", "priority"], icon: "settings", label: "Options", description: "Configure preferences", key: "options" }, { icon: "check-circle", description: "Confirm and submit", key: "review", fields: ["notes"], label: "Review" }]).length === 1)
            if (entity) {
      entity.isLastStep = ((([{ fields: ["title", "description"], key: "details", icon: "file-text", description: "Basic information", label: "Details" }, { fields: ["category", "priority"], icon: "settings", label: "Options", description: "Configure preferences", key: "options" }, { icon: "check-circle", description: "Confirm and submit", key: "review", fields: ["notes"], label: "Review" }]).length === 1)) as typeof entity['isLastStep'];
            }

            // Set entity?.primaryActionLabel = ((([{ description: "Basic information", label: "Details", fields: ["title", "description"], icon: "file-text", key: "details" }, { key: "options", label: "Options", description: "Configure preferences", fields: ["category", "priority"], icon: "settings" }, { fields: ["notes"], key: "review", icon: "check-circle", description: "Confirm and submit", label: "Review" }]).length === 1) ? "Submit" : "Next")
            if (entity) {
      entity.primaryActionLabel = (((([{ description: "Basic information", label: "Details", fields: ["title", "description"], icon: "file-text", key: "details" }, { key: "options", label: "Options", description: "Configure preferences", fields: ["category", "priority"], icon: "settings" }, { fields: ["notes"], key: "review", icon: "check-circle", description: "Confirm and submit", label: "Review" }]).length === 1) ? "Submit" : "Next")) as typeof entity['primaryActionLabel'];
            }

            // Set entity?.primaryActionIcon = ((([{ description: "Basic information", fields: ["title", "description"], icon: "file-text", key: "details", label: "Details" }, { key: "options", icon: "settings", fields: ["category", "priority"], description: "Configure preferences", label: "Options" }, { key: "review", description: "Confirm and submit", icon: "check-circle", label: "Review", fields: ["notes"] }]).length === 1) ? "check-circle" : "chevron-right")
            if (entity) {
      entity.primaryActionIcon = (((([{ description: "Basic information", fields: ["title", "description"], icon: "file-text", key: "details", label: "Details" }, { key: "options", icon: "settings", fields: ["category", "priority"], description: "Configure preferences", label: "Options" }, { key: "review", description: "Confirm and submit", icon: "check-circle", label: "Review", fields: ["notes"] }]).length === 1) ? "check-circle" : "chevron-right")) as typeof entity['primaryActionIcon'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'running',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process WizardLoaded from state ${state}`,
      };
  }
}

/**
 * Handle WizardLoadFailed event
 */
async function handleWizardloadfailed(
  state: WizardFormState,
  payload: { code: string; error: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.errorMessage = (payload as EventPayload | undefined)?.error
            if (entity) {
      entity.errorMessage = ((payload as EventPayload | undefined)?.error) as typeof entity['errorMessage'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'error',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process WizardLoadFailed from state ${state}`,
      };
  }
}

/**
 * Handle WizardSaved event
 */
async function handleWizardsaved(
  state: WizardFormState,
  payload: { row: Record<string, FieldValue | undefined> } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Fetch WizardView entities
            {
      const items = await getDataService().list<WizardView>('wizard_view');
        data['WizardView'] = items;
        const __totalCount_WizardView = data['WizardView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'WizardView',
        success: true,
        data: data['WizardView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['WizardView'], totalCount: __totalCount_WizardView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'WizardLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('WizardLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'WizardLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process WizardSaved from state ${state}`,
      };
  }
}

/**
 * Handle WizardSaveFailed event
 */
async function handleWizardsavefailed(
  state: WizardFormState,
  payload: { code: string; error: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'loading':
    {
      // Execute server-side effects
            // Set entity?.errorMessage = (payload as EventPayload | undefined)?.error
            if (entity) {
      entity.errorMessage = ((payload as EventPayload | undefined)?.error) as typeof entity['errorMessage'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'error',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process WizardSaveFailed from state ${state}`,
      };
  }
}

/**
 * Handle ADVANCE event
 */
async function handleAdvance(
  state: WizardFormState,
  payload: { data: Record<string, FieldValue | undefined> } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'running':
    {
      if (!(entity?.isLastStep)) {
        // Execute server-side effects
              // Set entity?.currentStepIndex = (+(entity?.currentStepIndex ?? 0) + 1)
              if (entity) {
        entity.currentStepIndex = ((+(entity?.currentStepIndex ?? 0) + 1)) as typeof entity['currentStepIndex'];
              }

              // Set entity?.currentStepLabel = (("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", label: "Details", fields: ["title", "description"], key: "details", icon: "file-text" }, { description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"], label: "Options" }, { label: "Review", icon: "check-circle", description: "Confirm and submit", fields: ["notes"], key: "review" }], entity?.currentStepIndex)))
              if (entity) {
        entity.currentStepLabel = ((("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", label: "Details", fields: ["title", "description"], key: "details", icon: "file-text" }, { description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"], label: "Options" }, { label: "Review", icon: "check-circle", description: "Confirm and submit", fields: ["notes"], key: "review" }], entity?.currentStepIndex)))) as typeof entity['currentStepLabel'];
              }

              // Set entity?.currentStepDescription = (("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", fields: ["title", "description"], label: "Details", key: "details", icon: "file-text" }, { key: "options", description: "Configure preferences", icon: "settings", label: "Options", fields: ["category", "priority"] }, { label: "Review", icon: "check-circle", key: "review", description: "Confirm and submit", fields: ["notes"] }], entity?.currentStepIndex)) ?? "")
              if (entity) {
        entity.currentStepDescription = ((("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", fields: ["title", "description"], label: "Details", key: "details", icon: "file-text" }, { key: "options", description: "Configure preferences", icon: "settings", label: "Options", fields: ["category", "priority"] }, { label: "Review", icon: "check-circle", key: "review", description: "Confirm and submit", fields: ["notes"] }], entity?.currentStepIndex)) ?? "")) as typeof entity['currentStepDescription'];
              }

              // Set entity?.currentStepIcon = (("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ key: "details", fields: ["title", "description"], icon: "file-text", description: "Basic information", label: "Details" }, { description: "Configure preferences", icon: "settings", fields: ["category", "priority"], label: "Options", key: "options" }, { fields: ["notes"], key: "review", icon: "check-circle", label: "Review", description: "Confirm and submit" }], entity?.currentStepIndex)) ?? "circle")
              if (entity) {
        entity.currentStepIcon = ((("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ key: "details", fields: ["title", "description"], icon: "file-text", description: "Basic information", label: "Details" }, { description: "Configure preferences", icon: "settings", fields: ["category", "priority"], label: "Options", key: "options" }, { fields: ["notes"], key: "review", icon: "check-circle", label: "Review", description: "Confirm and submit" }], entity?.currentStepIndex)) ?? "circle")) as typeof entity['currentStepIcon'];
              }

              // Set entity?.currentFields = (("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", fields: ["title", "description"], icon: "file-text", key: "details" }, { label: "Options", description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"] }, { fields: ["notes"], label: "Review", icon: "check-circle", description: "Confirm and submit", key: "review" }], entity?.currentStepIndex)) ?? [])
              if (entity) {
        entity.currentFields = ((("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", fields: ["title", "description"], icon: "file-text", key: "details" }, { label: "Options", description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"] }, { fields: ["notes"], label: "Review", icon: "check-circle", description: "Confirm and submit", key: "review" }], entity?.currentStepIndex)) ?? [])) as typeof entity['currentFields'];
              }

              // Set entity?.isFirstStep = (entity?.currentStepIndex === 0)
              if (entity) {
        entity.isFirstStep = ((entity?.currentStepIndex === 0)) as typeof entity['isFirstStep'];
              }

              // Set entity?.isLastStep = (entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1))
              if (entity) {
        entity.isLastStep = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1))) as typeof entity['isLastStep'];
              }

              // Set entity?.primaryActionLabel = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "Submit" : "Next")
              if (entity) {
        entity.primaryActionLabel = (((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "Submit" : "Next")) as typeof entity['primaryActionLabel'];
              }

              // Set entity?.primaryActionIcon = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "check-circle" : "chevron-right")
              if (entity) {
        entity.primaryActionIcon = (((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "check-circle" : "chevron-right")) as typeof entity['primaryActionIcon'];
              }


        // Persist entity mutations (auto-generated from set effects)
        if (entity && entityId) {
          await getDataService().update<WizardView>('wizard_view', entityId, entity);
          effectResults.push({
            effect: 'persist',
            action: 'update',
            entityType: 'WizardView',
            success: true,
            data: entity,
          });
        }

        return {
          success: true,
          newState: 'running',
          data,
          effectResults,
          emittedEvents,
        };
      } else if (entity?.isLastStep) {
        // Execute server-side effects
              // Set entity?.completionMessage = "All steps completed. Your submission has been recorded."
              if (entity) {
        entity.completionMessage = ("All steps completed. Your submission has been recorded.") as typeof entity['completionMessage'];
              }

              // Persist WizardView (update)
              {
        effectsLog.debug('persist:dispatch', { action: 'update', entityType: 'WizardView', payloadKeys: payload && typeof payload === 'object' ? Object.keys(payload).join(',') : null });
        const updateData = ((payload as EventPayload | undefined)?.data) as Record<string, FieldValue | undefined> as Partial<WizardView> | undefined;
        const __payloadWideUpd = payload as EventPayload | undefined;
        const __pEntityIdUpd = __payloadWideUpd?.entityId;
        const __pIdUpd = __payloadWideUpd?.id;
        const updateId: string | undefined = updateData?.id
          ?? entity?.id
          ?? (typeof __pEntityIdUpd === 'string' ? __pEntityIdUpd : undefined)
          ?? (typeof __pIdUpd === 'string' ? __pIdUpd : undefined);
        effectsLog.debug('persist:resolve-id', { action: 'update', entityType: 'WizardView', resolvedId: updateId, fromData: !!updateData?.id, fromEntity: !!entity?.id, fromPayloadEntityId: typeof __pEntityIdUpd === 'string', fromPayloadId: typeof __pIdUpd === 'string' });
        if (updateId) {
          const updated = await getDataService().update<WizardView>('wizard_view', updateId, updateData ?? {});
          effectsLog.debug('persist:store-mutate', { action: 'update', entityType: 'WizardView', resultId: updateId, success: true });
          effectResults.push({
            effect: 'persist',
            action: 'update',
            entityType: 'WizardView',
            success: true,
            data: updated,
          });
          { const __evV: EventPayloadValue | undefined = (updated) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'WizardSaved', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('WizardSaved', __evP, __ORBITAL_SOURCE); }
          effectsLog.debug('emit:push', { event: 'WizardSaved', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
        } else {
          effectsLog.warn('persist:noop-no-id', { action: 'update', entityType: 'WizardView', reason: 'updateId did not resolve from data/entity/payload' });
        }
              }


        return {
          success: true,
          newState: 'completed',
          data,
          effectResults,
          emittedEvents,
        };
      } else {
        return {
          success: false,
          error: 'Access denied: guard condition not met',
        };
      }
    }

    default:
      return {
        success: false,
        error: `Cannot process ADVANCE from state ${state}`,
      };
  }
}

/**
 * Handle RETREAT event
 */
async function handleRetreat(
  state: WizardFormState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'running':
    {
      // Execute server-side effects
            // Set entity?.currentStepIndex = (+(entity?.currentStepIndex ?? 0) - 1)
            if (entity) {
      entity.currentStepIndex = ((+(entity?.currentStepIndex ?? 0) - 1)) as typeof entity['currentStepIndex'];
            }

            // Set entity?.currentStepLabel = (("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", icon: "file-text", fields: ["title", "description"], key: "details" }, { label: "Options", description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"] }, { label: "Review", description: "Confirm and submit", fields: ["notes"], key: "review", icon: "check-circle" }], entity?.currentStepIndex)))
            if (entity) {
      entity.currentStepLabel = ((("label" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ label: "Details", description: "Basic information", icon: "file-text", fields: ["title", "description"], key: "details" }, { label: "Options", description: "Configure preferences", icon: "settings", key: "options", fields: ["category", "priority"] }, { label: "Review", description: "Confirm and submit", fields: ["notes"], key: "review", icon: "check-circle" }], entity?.currentStepIndex)))) as typeof entity['currentStepLabel'];
            }

            // Set entity?.currentStepDescription = (("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", fields: ["title", "description"], label: "Details", key: "details", icon: "file-text" }, { label: "Options", fields: ["category", "priority"], key: "options", description: "Configure preferences", icon: "settings" }, { icon: "check-circle", label: "Review", fields: ["notes"], key: "review", description: "Confirm and submit" }], entity?.currentStepIndex)) ?? "")
            if (entity) {
      entity.currentStepDescription = ((("description" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ description: "Basic information", fields: ["title", "description"], label: "Details", key: "details", icon: "file-text" }, { label: "Options", fields: ["category", "priority"], key: "options", description: "Configure preferences", icon: "settings" }, { icon: "check-circle", label: "Review", fields: ["notes"], key: "review", description: "Confirm and submit" }], entity?.currentStepIndex)) ?? "")) as typeof entity['currentStepDescription'];
            }

            // Set entity?.currentStepIcon = (("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ key: "details", description: "Basic information", icon: "file-text", fields: ["title", "description"], label: "Details" }, { icon: "settings", fields: ["category", "priority"], description: "Configure preferences", key: "options", label: "Options" }, { description: "Confirm and submit", label: "Review", key: "review", icon: "check-circle", fields: ["notes"] }], entity?.currentStepIndex)) ?? "circle")
            if (entity) {
      entity.currentStepIcon = ((("icon" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ key: "details", description: "Basic information", icon: "file-text", fields: ["title", "description"], label: "Details" }, { icon: "settings", fields: ["category", "priority"], description: "Configure preferences", key: "options", label: "Options" }, { description: "Confirm and submit", label: "Review", key: "review", icon: "check-circle", fields: ["notes"] }], entity?.currentStepIndex)) ?? "circle")) as typeof entity['currentStepIcon'];
            }

            // Set entity?.currentFields = (("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ fields: ["title", "description"], icon: "file-text", key: "details", description: "Basic information", label: "Details" }, { icon: "settings", label: "Options", description: "Configure preferences", key: "options", fields: ["category", "priority"] }, { description: "Confirm and submit", fields: ["notes"], label: "Review", key: "review", icon: "check-circle" }], entity?.currentStepIndex)) ?? [])
            if (entity) {
      entity.currentFields = ((("fields" as string).split('.').reduce((o: any, k: string) => o?.[k], ((__a: EventPayloadValue | undefined, __i: number | undefined) => Array.isArray(__a) ? __a[__i ?? 0] : undefined)([{ fields: ["title", "description"], icon: "file-text", key: "details", description: "Basic information", label: "Details" }, { icon: "settings", label: "Options", description: "Configure preferences", key: "options", fields: ["category", "priority"] }, { description: "Confirm and submit", fields: ["notes"], label: "Review", key: "review", icon: "check-circle" }], entity?.currentStepIndex)) ?? [])) as typeof entity['currentFields'];
            }

            // Set entity?.isFirstStep = (entity?.currentStepIndex === 0)
            if (entity) {
      entity.isFirstStep = ((entity?.currentStepIndex === 0)) as typeof entity['isFirstStep'];
            }

            // Set entity?.isLastStep = (entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1))
            if (entity) {
      entity.isLastStep = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1))) as typeof entity['isLastStep'];
            }

            // Set entity?.primaryActionLabel = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "Submit" : "Next")
            if (entity) {
      entity.primaryActionLabel = (((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "Submit" : "Next")) as typeof entity['primaryActionLabel'];
            }

            // Set entity?.primaryActionIcon = ((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "check-circle" : "chevron-right")
            if (entity) {
      entity.primaryActionIcon = (((entity?.currentStepIndex === (+(entity?.totalSteps ?? 0) - 1)) ? "check-circle" : "chevron-right")) as typeof entity['primaryActionIcon'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'running',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process RETREAT from state ${state}`,
      };
  }
}

/**
 * Handle CANCEL event
 */
async function handleCancel(
  state: WizardFormState,
  payload: { reason: string } | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'running':
    {
      // Execute server-side effects
            // Set entity?.cancelReason = (payload as EventPayload | undefined)?.reason
            if (entity) {
      entity.cancelReason = ((payload as EventPayload | undefined)?.reason) as typeof entity['cancelReason'];
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'cancelled',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process CANCEL from state ${state}`,
      };
  }
}

/**
 * Handle RESTART event
 */
async function handleRestart(
  state: WizardFormState,
  payload: EventPayload | undefined,
  user: UserContext | undefined
): Promise<EventResponse> {
  const data: Record<string, EntityRow[]> = {};
  const effectResults: EffectResult[] = [];
  const emittedEvents: EmittedEvent[] = [];

  // Resolve entity from linked entity collection. Accept both
  // `entityId` (system convention for config-injected ids) and `id`
  // (the .lolo emit-declaration field name surfaced by `actionPayload`).
  const __payloadWide = payload as EventPayload | undefined;
  const __payloadEntityId = __payloadWide?.entityId;
  const __payloadId = __payloadWide?.id;
  const entityId: string | undefined =
    typeof __payloadEntityId === 'string' ? __payloadEntityId
    : typeof __payloadId === 'string' ? __payloadId
    : undefined;
  const entity = getRuntimeEntity('std-wizard', 'WizardForm', entityId) as WizardView;

  switch (state) {
    case 'completed':
    {
      // Execute server-side effects
            // Set entity?.currentStepIndex = 0
            if (entity) {
      entity.currentStepIndex = (0) as typeof entity['currentStepIndex'];
            }

            // Set entity?.completionMessage = ""
            if (entity) {
      entity.completionMessage = ("") as typeof entity['completionMessage'];
            }

            // Fetch WizardView entities
            {
      const items = await getDataService().list<WizardView>('wizard_view');
        data['WizardView'] = items;
        const __totalCount_WizardView = data['WizardView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'WizardView',
        success: true,
        data: data['WizardView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['WizardView'], totalCount: __totalCount_WizardView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'WizardLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('WizardLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'WizardLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    case 'cancelled':
    {
      // Execute server-side effects
            // Set entity?.currentStepIndex = 0
            if (entity) {
      entity.currentStepIndex = (0) as typeof entity['currentStepIndex'];
            }

            // Set entity?.cancelReason = ""
            if (entity) {
      entity.cancelReason = ("") as typeof entity['cancelReason'];
            }

            // Fetch WizardView entities
            {
      const items = await getDataService().list<WizardView>('wizard_view');
        data['WizardView'] = items;
        const __totalCount_WizardView = data['WizardView'].length;
      effectResults.push({
        effect: 'fetch',
        entityType: 'WizardView',
        success: true,
        data: data['WizardView'],
      });
      { const __evV: EventPayloadValue | undefined = ({ data: data['WizardView'], totalCount: __totalCount_WizardView }) as EventPayloadValue | undefined; const __evP: EventPayload = (__evV !== null && __evV !== undefined && typeof __evV === 'object' && !Array.isArray(__evV) && !(__evV instanceof Date)) ? (__evV as EventPayload) : ({ result: __evV } as EventPayload); emittedEvents.push({ event: 'WizardLoaded', payload: __evP, source: __ORBITAL_SOURCE }); getServerEventBus().emit('WizardLoaded', __evP, __ORBITAL_SOURCE); }
      effectsLog.debug('emit:push', { event: 'WizardLoaded', cumulativeEmittedCount: emittedEvents.length, sourceTrait: __ORBITAL_SOURCE.trait, sourceOrbital: __ORBITAL_SOURCE.orbital });
            }


      // Persist entity mutations (auto-generated from set effects)
      if (entity && entityId) {
        await getDataService().update<WizardView>('wizard_view', entityId, entity);
        effectResults.push({
          effect: 'persist',
          action: 'update',
          entityType: 'WizardView',
          success: true,
          data: entity,
        });
      }

      return {
        success: true,
        newState: 'loading',
        data,
        effectResults,
        emittedEvents,
      };
    }

    default:
      return {
        success: false,
        error: `Cannot process RESTART from state ${state}`,
      };
  }
}

