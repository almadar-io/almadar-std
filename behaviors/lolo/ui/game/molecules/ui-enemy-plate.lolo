app ui-enemy-plate "1.0.0"
"EnemyPlate — UI factory (1:1 with the @almadar/ui enemy-plate pattern)."
orbital EnemyPlateOrbital {
  type EnemyPlateEffectsItem = {
    icon : icon!
    label : string
    variant : "buff" | "debuff" | "neutral"
  }

  entity EnemyPlateItem [runtime] {
    id : string!
  }

  trait EnemyPlateRender -> @rebindable EnemyPlateItem [interaction, instance] {
    initial: idle

    state idle {
      INIT -> idle
        (render-ui main {
          assetUrl: @config.assetUrl
          name: @config.name
          health: @config.health
          maxHealth: @config.maxHealth
          level: @config.level
          effects: @config.effects
          className: @config.className
          type: enemy-plate
        })
    }

    config {
      assetUrl : asset = "https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png"
        @label "Asset Url"
        @description "Portrait sprite URL — takes precedence over the default avatar slot"
        @tier "presentation"
      name : string = "Shadow Guard"
        @label "Name"
        @description "Enemy name"
        @tier "presentation"
      health : number = 80
        @label "Health"
        @description "Current health"
        @tier "presentation"
      maxHealth : number = 100
        @label "Max Health"
        @description "Maximum health"
        @tier "presentation"
      level : number = 5
        @label "Level"
        @description "Enemy level"
        @tier "presentation"
      effects : [EnemyPlateEffectsItem] = [{ icon: "circle", label: "Label", variant: buff }]
        @label "Effects"
        @description "Active status effects"
        @tier "presentation"
      className : string = ""
        @label "Class Name"
        @description "Additional CSS classes"
        @tier "presentation"
    }
  }

  page "/enemy-plate" as EnemyPlatePage -> EnemyPlateRender
}
