app ui-game-over-screen "1.0.0"
"GameOverScreen — UI factory (1:1 with the @almadar/ui game-over-screen pattern)."
orbital GameOverScreenOrbital {
  type GameOverScreenStatsItem = {
    label : string!
    value : string
    bind : string
    format : "number" | "time" | "text"
    icon : icon
  }
  type GameOverScreenActionsItem = {
    label : string!
    event : string
    navigatesTo : string
    variant : "primary" | "secondary" | "ghost"
  }
  type GameOverScreenMenuItemsItem = {
    label : string!
    event : string
    navigatesTo : string
    variant : "primary" | "secondary" | "ghost"
  }
  type GameOverScreenACTIONAction = {
    label : string!
    event : string
    navigatesTo : string
    variant : "primary" | "secondary" | "ghost"
  }

  entity GameOverScreenItem [runtime] {
    id : string!
  }

  trait GameOverScreenRender -> @rebindable GameOverScreenItem [interaction, instance] {
    initial: idle

    state idle {
      INIT -> idle
        (render-ui main {
          title: @config.title
          message: @config.message
          stats: @config.stats
          actions: @config.actions
          menuItems: @config.menuItems
          onAction: ACTION
          variant: @config.variant
          highScore: @config.highScore
          currentScore: @config.currentScore
          className: @config.className
          type: game-over-screen
        })
    }

    emits {
      ACTION -> external {
        action : GameOverScreenACTIONAction
      }
        @description "Called when an action is selected (legacy callback, prefer event bus)"
        @tier "essential"
    }

    config {
      title : string = "Game Over"
        @label "Title"
        @description "Screen title (e.g., 'Game Over', 'Victory!')"
        @tier "presentation"
      message : string = "Message"
        @label "Message"
        @description "Optional message"
        @tier "presentation"
      stats : [GameOverScreenStatsItem] = [{ label: "Label", value: "Value", bind: "Bind", format: number, icon: "circle" }]
        @label "Stats"
        @description "Stats to display"
        @tier "presentation"
      actions : [GameOverScreenActionsItem] = [{ label: "Label", navigatesTo: "Navigates To", variant: primary }]
        @label "Actions"
        @description "Action buttons"
        @tier "presentation"
      menuItems : [GameOverScreenMenuItemsItem] = [{ label: "Label", navigatesTo: "Navigates To", variant: primary }]
        @label "Menu Items"
        @description "Alias for actions (schema compatibility)"
        @tier "presentation"
      variant : "victory" | "defeat" | "neutral" = defeat
        @label "Variant"
        @description "Victory or defeat variant"
        @tier "presentation"
      highScore : string = "High Score"
        @label "High Score"
        @description "High score (optional, shows 'NEW HIGH SCORE!' if exceeded)"
        @tier "presentation"
      currentScore : string = "Current Score"
        @label "Current Score"
        @description "Current score for high score comparison (accepts string for schema bindings)"
        @tier "presentation"
      className : string = ""
        @label "Class Name"
        @description "Additional CSS classes"
        @tier "presentation"
    }
  }

  page "/game-over-screen" as GameOverScreenPage -> GameOverScreenRender
}
