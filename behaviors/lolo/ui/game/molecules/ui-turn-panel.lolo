app ui-turn-panel "1.0.0"
"TurnPanel — UI factory (1:1 with the @almadar/ui turn-panel pattern)."
orbital TurnPanelOrbital {
  type TurnPanelActionsItem = {
    label : string!
    assetUrl : TurnPanelActionsItemAssetUrl
    icon : icon
    event : string
    disabled : boolean
  }
  type TurnPanelActionsItemAssetUrl = {
    url : asset!
    role : string
    category : string
    animations : [string]
    style : string
    dimension : string
    aspect : string
    name : string
    thumbnailUrl : string
  }

  entity TurnPanelItem [runtime] {
    id : string!
  }

  trait TurnPanelRender -> @rebindable TurnPanelItem [interaction, instance] {
    initial: idle

    state idle {
      INIT -> idle
        (render-ui main {
          currentTurn: @config.currentTurn
          maxTurns: @config.maxTurns
          phase: @config.phase
          activeTeam: @config.activeTeam
          actions: @config.actions
          className: @config.className
          type: turn-panel
        })
    }

    config {
      currentTurn : number = 3
        @label "Current Turn"
        @description "Current turn number"
        @tier "presentation"
      maxTurns : number = 10
        @label "Max Turns"
        @description "Maximum number of turns"
        @tier "presentation"
      phase : string = Attack
        @label "Phase"
        @description "Current phase label"
        @tier "presentation"
      activeTeam : string = Heroes
        @label "Active Team"
        @description "Active team name"
        @tier "presentation"
      actions : [TurnPanelActionsItem] = [{ label: "Label", assetUrl: { url: "https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png", role: "decoration", category: "asset", animations: ["static"], style: "", dimension: "2d", aspect: "1:1", name: "asset", thumbnailUrl: "" }, icon: "circle", disabled: false }, { label: "Label 2", assetUrl: { url: "https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png", role: "decoration", category: "asset", animations: ["static"], style: "", dimension: "2d", aspect: "1:1", name: "asset", thumbnailUrl: "" }, icon: "circle", disabled: true }]
        @label "Actions"
        @description "Action buttons to display"
        @tier "presentation"
      className : string = ""
        @label "Class Name"
        @description "Additional CSS classes"
        @tier "presentation"
    }
  }

  page "/turn-panel" as TurnPanelPage -> TurnPanelRender
}
