app ui-resource-bar "1.0.0"
"ResourceBar — UI factory (1:1 with the @almadar/ui resource-bar pattern)."
orbital ResourceBarOrbital {
  type ResourceBarResourcesItem = {
    icon : icon
    label : string!
    value : number
    max : number
  }

  entity ResourceBarItem [runtime] {
    id : string!
  }

  trait ResourceBarRender -> @rebindable ResourceBarItem [interaction, instance] {
    initial: idle

    state idle {
      INIT -> idle
        (render-ui main {
          resources: @config.resources
          size: @config.size
          className: @config.className
          type: resource-bar
        })
    }

    config {
      resources : [ResourceBarResourcesItem] = [{ icon: "circle", label: "Label", value: 1, max: 1 }, { icon: "circle", label: "Label 2", value: 2, max: 2 }]
        @label "Resources"
        @description "Resources to display"
        @tier "presentation"
      size : "sm" | "md" | "lg" = md
        @label "Size"
        @description "Size variant"
        @tier "presentation"
      className : string = ""
        @label "Class Name"
        @description "Additional CSS classes"
        @tier "presentation"
    }
  }

  page "/resource-bar" as ResourceBarPage -> ResourceBarRender
}
