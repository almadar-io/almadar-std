/**
 * Organism Behaviors
 *
 * Full page experiences composed from molecules at runtime.
 * Organisms are built by the agent's compose tool, not pre-defined.
 * This module provides the compose/connect utilities.
 *
 * @level organism
 * @packageDocumentation
 */

// Organisms are not pre-defined. They are composed at runtime by:
// - compose(): combine multiple behaviors into a multi-page app
// - connect(): wire cross-orbital events between behaviors
// - pipeline(): chain behaviors in sequence

// See docs/Almadar_Std_Behaviors.md for composition model.

export {};
