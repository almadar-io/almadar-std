/**
 * std-cross-reference — std-cross-reference — join-table primitive for N:M relationships. Listens for LinkRequested/UnlinkRequested/FETCH_LINKS_FOR and manages the junction collection (default CrossRefLink [persistent: cross_ref_links]). Rebind via linkedEntity={YourJunction} to your consumer junction; if your junction's id fields differ from leftId/rightId, override them with fields{} renames at the call site. A rebind target must supply leftId, rightId, createdAt, filteredLinks. Emits LinkCreated, LinkRemoved, LinksLoaded as external broadcasts for downstream consumers.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-cross-reference.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-cross-reference
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-cross-reference';
const ALIAS = 'CrossReference';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCrossReferenceEventKey = 'FETCH_LINKS_FOR' | 'LinkCreated' | 'LinkOpFailed' | 'LinkRemoved' | 'LinkRequested' | 'LinksFetched' | 'LinksLoaded' | 'UnlinkRequested';

/**
 * Payload shape for the `LinkCreated` event.
 */
export interface StdCrossReferenceLinkCreatedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `LinkRemoved` event.
 */
export interface StdCrossReferenceLinkRemovedPayload {
  leftId?: string;
  rightId?: string;
}

/**
 * Payload shape for the `LinksLoaded` event.
 */
export interface StdCrossReferenceLinksLoadedPayload {
  leftId?: string;
  data?: EntityRow[];
}

/**
 * Payload shape for the `LinksFetched` event.
 */
export interface StdCrossReferenceLinksFetchedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `LinkOpFailed` event.
 */
export interface StdCrossReferenceLinkOpFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-cross-reference descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCrossReferenceParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCrossReferenceEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CrossReference.traits.CrossReferenceGate`. */
export function stdCrossReferenceTrait(params: StdCrossReferenceParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CrossReferenceGate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CrossReference.pages.CrossReferencePage`. */
export function stdCrossReferencePage(params: StdCrossReferenceParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CrossReferencePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCrossReference(params: StdCrossReferenceParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CrossReferenceOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCrossReferenceTrait(params),
    ],
    pages: [
      stdCrossReferencePage(params),
    ],
  });
}

type _StdCrossReferenceEntityName = 'CrossRefLink';
type _StdCrossReferenceListenTraitName = 'CrossReferenceGate';

/**
 * Tunable params for the CrossReferenceOrbital orbital.
 *
 * Canonical entity: CrossRefLink — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdCrossReferenceCrossReferenceOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CrossReferenceGate',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait CrossReferenceOrbital's `uses[]` exports. */
type _StdCrossReferenceCrossReferenceOrbitalUsesRef = never;

/** Per-orbital factory: builds the CrossReferenceOrbital orbital with consumer params. */
export function stdCrossReferenceCrossReferenceOrbital(params: StdCrossReferenceCrossReferenceOrbitalParams = {}): OrbitalDefinition {
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'cross_ref_links');
  const built = makeOrbitalWithUses({
    name: 'CrossReferenceOrbital',
    uses: [],
    entity: {
      name: 'CrossRefLink',
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': '',
            'description': 'Identifier for the related entity on the left side.',
            'name': 'leftId',
            'synonyms': 'sourceId, originId, parentId',
            'type': 'string',
          },
          {
            'default': '',
            'description': 'Identifier for the related entity on the right side.',
            'name': 'rightId',
            'synonyms': 'targetId, relatedId, counterpartId',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'createdAt',
            'type': 'string',
          },
          {
            'default': [],
            'description': 'A list of filtered link specifications.',
            'items': {
              'properties': {
                'href': {
                  'name': 'href',
                  'required': true,
                  'type': 'string',
                },
                'icon': {
                  'name': 'icon',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'name': 'filteredLinks',
            'synonyms': 'links, references, connections',
            'type': 'array',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'lifecycle',
        'emits': [
          {
            'event': 'LinkCreated',
            'payloadSchema': [
              {
                'name': 'row',
                'type': 'object',
              },
            ],
            'scope': 'external',
          },
          {
            'event': 'LinkRemoved',
            'payloadSchema': [
              {
                'name': 'leftId',
                'type': 'string',
              },
              {
                'name': 'rightId',
                'type': 'string',
              },
            ],
            'scope': 'external',
          },
          {
            'event': 'LinksLoaded',
            'payloadSchema': [
              {
                'name': 'leftId',
                'type': 'string',
              },
              {
                'name': 'data',
                'type': '[ObjectSpec]',
              },
            ],
            'scope': 'external',
          },
          {
            'event': 'LinksFetched',
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[ObjectSpec]',
              },
            ],
            'scope': 'external',
          },
          {
            'event': 'LinkOpFailed',
            'payloadSchema': [
              {
                'name': 'error',
                'type': 'string',
              },
              {
                'name': 'code',
                'type': 'string',
              },
            ],
            'scope': 'external',
          },
        ],
        'entityContract': {
          'provides': [
            'filteredLinks',
            'leftId',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'CrossRefLink',
        'name': 'CrossReferenceGate',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'LinkRequested',
              'name': 'Link requested',
              'payloadSchema': [
                {
                  'name': 'leftId',
                  'type': 'string',
                },
                {
                  'name': 'rightId',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'UnlinkRequested',
              'name': 'Unlink requested',
              'payloadSchema': [
                {
                  'name': 'leftId',
                  'type': 'string',
                },
                {
                  'name': 'rightId',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'FETCH_LINKS_FOR',
              'name': 'Fetch Links For',
              'payloadSchema': [
                {
                  'name': 'leftId',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'LinksFetched',
              'name': 'Links fetched',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[ObjectSpec]',
                },
              ],
            },
            {
              'key': 'LinkCreated',
              'name': 'Link created',
              'payloadSchema': [
                {
                  'name': 'row',
                  'type': 'object',
                },
              ],
            },
            {
              'key': 'LinkRemoved',
              'name': 'Link removed',
              'payloadSchema': [
                {
                  'name': 'leftId',
                  'type': 'string',
                },
                {
                  'name': 'rightId',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'LinksLoaded',
              'name': 'Links loaded',
              'payloadSchema': [
                {
                  'name': 'leftId',
                  'type': 'string',
                },
                {
                  'name': 'data',
                  'type': '[ObjectSpec]',
                },
              ],
            },
            {
              'key': 'LinkOpFailed',
              'name': 'Link op failed',
              'payloadSchema': [
                {
                  'name': 'error',
                  'type': 'string',
                },
                {
                  'name': 'code',
                  'type': 'string',
                },
              ],
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'persist',
                  'create',
                  ('CrossRefLink' satisfies _StdCrossReferenceEntityName),
                  {
                    'leftId': '@payload.leftId',
                    'rightId': '@payload.rightId',
                  },
                  {
                    'emit': {
                      'failure': 'LinkOpFailed',
                      'success': 'LinkCreated',
                    },
                  },
                ],
              ],
              'event': 'LinkRequested',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'persist',
                  'delete',
                  ('CrossRefLink' satisfies _StdCrossReferenceEntityName),
                  '@payload',
                  {
                    'emit': {
                      'failure': 'LinkOpFailed',
                      'success': 'LinkRemoved',
                    },
                  },
                ],
              ],
              'event': 'UnlinkRequested',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.leftId',
                  '@payload.leftId',
                ],
                [
                  'fetch',
                  ('CrossRefLink' satisfies _StdCrossReferenceEntityName),
                  {
                    'emit': {
                      'failure': 'LinkOpFailed',
                      'success': 'LinksFetched',
                    },
                  },
                ],
              ],
              'event': 'FETCH_LINKS_FOR',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.filteredLinks',
                  [
                    'array/filter',
                    '@payload.data',
                    [
                      'fn',
                      'row',
                      [
                        '=',
                        [
                          'object/get',
                          '@row',
                          'leftId',
                        ],
                        '@entity.leftId',
                      ],
                    ],
                  ],
                ],
                [
                  'emit',
                  'LinksLoaded',
                  {
                    'data': '@entity.filteredLinks',
                    'leftId': '@entity.leftId',
                  },
                ],
              ],
              'event': 'LinksFetched',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'CrossReferencePage',
        'path': '/cross-reference',
        'traits': [
          {
            'ref': 'CrossReferenceGate',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdCrossReferenceCrossReferenceOrbital. */
export const StdCrossReferenceCrossReferenceOrbitalManifest = {
  organism: 'std-cross-reference',
  orbitalName: 'CrossReferenceOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'CrossReferenceGate',
  ] as const,
};

/** Typed guard — runtime validates StdCrossReferenceCrossReferenceOrbitalParams keys. */
export function isStdCrossReferenceCrossReferenceOrbitalParams(p: object): p is StdCrossReferenceCrossReferenceOrbitalParams {
  type _OverrideRecord = NonNullable<StdCrossReferenceCrossReferenceOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdCrossReferenceCrossReferenceOrbitalManifest.traitNames,
      ...StdCrossReferenceCrossReferenceOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
