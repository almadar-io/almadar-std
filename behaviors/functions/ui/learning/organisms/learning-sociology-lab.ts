/**
 * learning-sociology-lab — Sociology lab organism — a social-stratification & mobility simulator (5 class strata redistribute across generations as the learner drives a mobility index) and a Sociological Imagination exercise (Mills: link each personal trouble to the public issue behind it), composed from the std-soc-stratification and std-soc-imagination instrument atoms behind a mode router.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-sociology-lab.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-sociology-lab
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-sociology-lab';
const ALIAS = 'LearningSociologyLab';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningSociologyLabEventKey = 'INIT' | 'MODE_SELECTED';

/**
 * Payload shape for the `MODE_SELECTED` event.
 */
export interface StdLearningSociologyLabModeSelectedPayload {
  mode?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningSociologyLabConfig {
  /** Default: `"stratification"` */
  mode?: 'stratification' | 'imagination';
}

type _StdLearningSociologyLabEntityName = 'SociologyLabItem' | 'StratificationScene' | 'ImaginationScene';
type _StdLearningSociologyLabListenTraitName = 'SociologyLabRouter' | 'StratificationSim' | 'ImaginationSim';

/**
 * Tunable params for the SociologyLabOrbital orbital.
 *
 * Canonical entity: SociologyLabItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningSociologyLabSociologyLabOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'StratificationSim' | 'ImaginationSim' | 'SociologyLabRouter',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait SociologyLabOrbital's `uses[]` exports. */
type _StdLearningSociologyLabSociologyLabOrbitalUsesRef = 'SocStratification.traits.SocStratSim' | 'SocImagination.traits.SocImaginationSim';

/** Per-orbital factory: builds the SociologyLabOrbital orbital with consumer params. */
export function stdLearningSociologyLabSociologyLabOrbital(params: StdLearningSociologyLabSociologyLabOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'SociologyLabOrbital',
    uses: [
      {
        'as': 'SocStratification',
        'from': 'std/behaviors/std-soc-stratification',
      },
      {
        'as': 'SocImagination',
        'from': 'std/behaviors/std-soc-imagination',
      },
    ],
    entity: {
      name: 'SociologyLabItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    auxiliaryEntities: [
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'mobility',
            'type': 'number',
          },
          {
            'name': 'generation',
            'type': 'number',
          },
          {
            'items': {
              'type': 'number',
            },
            'name': 'stratumShares',
            'type': 'array',
          },
          {
            'name': 'giniIndex',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'align': {
                  'name': 'align',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'left',
                    'center',
                    'right',
                  ],
                },
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'fill': {
                  'name': 'fill',
                  'required': false,
                  'type': 'string',
                },
                'fontSize': {
                  'name': 'fontSize',
                  'required': false,
                  'type': 'number',
                },
                'height': {
                  'name': 'height',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'text': {
                  'name': 'text',
                  'required': false,
                  'type': 'string',
                },
                'type': {
                  'name': 'type',
                  'required': true,
                  'type': 'string',
                  'values': [
                    'line',
                    'arrow',
                    'circle',
                    'rect',
                    'polygon',
                    'path',
                    'text',
                    'axis',
                    'grid',
                  ],
                },
                'width': {
                  'name': 'width',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': false,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': false,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'shapes',
            'type': 'array',
          },
        ],
        'name': 'StratificationScene',
        'persistence': 'runtime',
      },
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'from': {
                  'name': 'from',
                  'required': true,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'to': {
                  'name': 'to',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'name': 'edges',
            'type': 'array',
          },
          {
            'name': 'currentTroubleIndex',
            'type': 'number',
          },
          {
            'name': 'score',
            'type': 'number',
          },
          {
            'name': 'answeredCorrectly',
            'type': 'boolean',
          },
          {
            'name': 'feedbackText',
            'type': 'string',
          },
          {
            'name': 'explanationText',
            'type': 'string',
          },
        ],
        'name': 'ImaginationScene',
        'persistence': 'runtime',
      },
    ] as EntityRef[],
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'mode': {
            'default': 'stratification',
            'description': 'Which sociology simulation to render: social stratification and mobility across generations, or the Sociological Imagination exercise linking personal troubles to public issues.',
            'label': 'Mode',
            'synonyms': 'sociology, social stratification, class stratification, social class, class strata, class structure, upper class, middle class, working class, lower class, social mobility, intergenerational mobility, mobility index, income inequality, wealth inequality, Gini index, Gini coefficient, Lorenz curve, sociological imagination, C. Wright Mills, Mills, personal trouble, public issue, biography and history, structural unemployment, social structure',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'stratification',
              'imagination',
            ],
          },
        },
        'emits': [
          {
            'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
            'event': 'MODE_SELECTED',
            'payloadSchema': [
              {
                'name': 'mode',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'SociologyLabItem',
        'name': 'SociologyLabRouter',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
              'key': 'MODE_SELECTED',
              'name': 'Mode Selected',
              'payloadSchema': [
                {
                  'name': 'mode',
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'emit',
                  'MODE_SELECTED',
                  {
                    'mode': '@config.mode',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'stratification',
            'type': 'unknown',
          },
          'title': {
            'default': 'Sociology Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'StratificationScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'SociologyLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'StratificationSim',
        'ref': ('SocStratification.traits.SocStratSim' satisfies _StdLearningSociologyLabSociologyLabOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'imagination',
            'type': 'unknown',
          },
          'title': {
            'default': 'Sociology Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'ImaginationScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'SociologyLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'ImaginationSim',
        'ref': ('SocImagination.traits.SocImaginationSim' satisfies _StdLearningSociologyLabSociologyLabOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'SociologyLabPage',
        'path': '/learning/sociology-lab',
        'traits': [
          {
            'ref': 'SociologyLabRouter',
          },
          {
            'ref': 'StratificationSim',
          },
          {
            'ref': 'ImaginationSim',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningSociologyLabSociologyLabOrbital. */
export const StdLearningSociologyLabSociologyLabOrbitalManifest = {
  organism: 'learning-sociology-lab',
  orbitalName: 'SociologyLabOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'StratificationSim',
    'ImaginationSim',
  ] as const,
  inlineTraitNames: [
    'SociologyLabRouter',
  ] as const,
};

/** Typed guard — runtime validates StdLearningSociologyLabSociologyLabOrbitalParams keys. */
export function isStdLearningSociologyLabSociologyLabOrbitalParams(p: object): p is StdLearningSociologyLabSociologyLabOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningSociologyLabSociologyLabOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningSociologyLabSociologyLabOrbitalManifest.traitNames,
      ...StdLearningSociologyLabSociologyLabOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-sociology-lab — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningSociologyLabParams {
  SociologyLab?: StdLearningSociologyLabSociologyLabOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningSociologyLab(params: StdLearningSociologyLabParams = {}): OrbitalDefinition[] {
  return [
    stdLearningSociologyLabSociologyLabOrbital(params.SociologyLab ?? {}),
  ];
}
