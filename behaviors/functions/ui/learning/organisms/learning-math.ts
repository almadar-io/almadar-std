/**
 * learning-math — Math learning app — a fat organism whose orbitals are individual math topics, each composed via `uses` from a per-topic engine atom on the math-canvas.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-math.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-math
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-math';
const ALIAS = 'LearningMath';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningMathConfig {
  /** Default: `400` */
  height?: unknown;
  /** Default: `true` */
  running?: unknown;
  /** Default: `[{"color":"#2563eb","label":"A","vx":4,"vy":3,"x":0,"y":0},{"color":"#16a34a","label":"B","vx":3,"vy":2,"x":4,"y":3},{"color":"#dc2626","label":"A+B","vx":7,"vy":5,"x":0,"y":0}]` */
  vectors?: unknown;
  /** Default: `600` */
  width?: unknown;
}

type _StdLearningMathEntityName = 'VectorScene' | 'LinearScene' | 'NumberlineScene' | 'SequenceScene' | 'UnitCircleScene' | 'SineWaveScene';
type _StdLearningMathListenTraitName = 'VectorEngine' | 'LinearEngine' | 'NumberlineEngine' | 'SequenceEngine' | 'UnitCircleEngine' | 'SineWaveEngine';

/**
 * Tunable params for the VectorAdditionOrbital orbital.
 *
 * Canonical entity: VectorScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathVectorAdditionOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'VectorEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait VectorAdditionOrbital's `uses[]` exports. */
type _StdLearningMathVectorAdditionOrbitalUsesRef = 'MathVectors.traits.MathVectorsSim';

/** Per-orbital factory: builds the VectorAdditionOrbital orbital with consumer params. */
export function stdLearningMathVectorAdditionOrbital(params: StdLearningMathVectorAdditionOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'VectorAdditionOrbital',
    uses: [
      {
        'as': 'MathVectors',
        'from': 'std/behaviors/std-math-vectors',
      },
    ],
    entity: {
      name: 'VectorScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'vx': {
                  'name': 'vx',
                  'required': true,
                  'type': 'number',
                },
                'vy': {
                  'name': 'vy',
                  'required': true,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'vectors',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'vectors': {
            'default': [
              {
                'color': '#2563eb',
                'label': 'A',
                'vx': 4,
                'vy': 3,
                'x': 0,
                'y': 0,
              },
              {
                'color': '#16a34a',
                'label': 'B',
                'vx': 3,
                'vy': 2,
                'x': 4,
                'y': 3,
              },
              {
                'color': '#dc2626',
                'label': 'A+B',
                'vx': 7,
                'vy': 5,
                'x': 0,
                'y': 0,
              },
            ],
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'VectorScene',
        'name': 'VectorEngine',
        'ref': ('MathVectors.traits.MathVectorsSim' satisfies _StdLearningMathVectorAdditionOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'VectorAdditionPage',
        'path': '/math/vectors',
        'traits': [
          {
            'ref': 'VectorEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathVectorAdditionOrbital. */
export const StdLearningMathVectorAdditionOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'VectorAdditionOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'VectorEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathVectorAdditionOrbitalParams keys. */
export function isStdLearningMathVectorAdditionOrbitalParams(p: object): p is StdLearningMathVectorAdditionOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathVectorAdditionOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathVectorAdditionOrbitalManifest.traitNames,
      ...StdLearningMathVectorAdditionOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the LinearSystemsOrbital orbital.
 *
 * Canonical entity: LinearScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathLinearSystemsOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'LinearEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait LinearSystemsOrbital's `uses[]` exports. */
type _StdLearningMathLinearSystemsOrbitalUsesRef = 'MathLinear.traits.MathLinearSim';

/** Per-orbital factory: builds the LinearSystemsOrbital orbital with consumer params. */
export function stdLearningMathLinearSystemsOrbital(params: StdLearningMathLinearSystemsOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'LinearSystemsOrbital',
    uses: [
      {
        'as': 'MathLinear',
        'from': 'std/behaviors/std-math-linear',
      },
    ],
    entity: {
      name: 'LinearScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'curves': {
            'default': [
              {
                'color': '#2563eb',
                'label': 'A',
                'samples': [
                  {
                    'x': -9,
                    'y': -5,
                  },
                  {
                    'x': 9,
                    'y': 4,
                  },
                ],
              },
              {
                'color': '#16a34a',
                'label': 'B',
                'samples': [
                  {
                    'x': -9,
                    'y': 9.2,
                  },
                  {
                    'x': 9,
                    'y': -1.6,
                  },
                ],
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'mB': {
            'default': -0.6,
            'type': 'unknown',
          },
          'points': {
            'default': [
              {
                'color': '#dc2626',
                'label': 'solution',
                'radius': 6,
                'x': 3.9,
                'y': 1.45,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'LinearScene',
        'name': 'LinearEngine',
        'ref': ('MathLinear.traits.MathLinearSim' satisfies _StdLearningMathLinearSystemsOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'LinearSystemsPage',
        'path': '/math/linear',
        'traits': [
          {
            'ref': 'LinearEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathLinearSystemsOrbital. */
export const StdLearningMathLinearSystemsOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'LinearSystemsOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'LinearEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathLinearSystemsOrbitalParams keys. */
export function isStdLearningMathLinearSystemsOrbitalParams(p: object): p is StdLearningMathLinearSystemsOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathLinearSystemsOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathLinearSystemsOrbitalManifest.traitNames,
      ...StdLearningMathLinearSystemsOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the NumberLineOrbital orbital.
 *
 * Canonical entity: NumberlineScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathNumberLineOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'NumberlineEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait NumberLineOrbital's `uses[]` exports. */
type _StdLearningMathNumberLineOrbitalUsesRef = 'MathNumberline.traits.MathNumberlineSim';

/** Per-orbital factory: builds the NumberLineOrbital orbital with consumer params. */
export function stdLearningMathNumberLineOrbital(params: StdLearningMathNumberLineOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'NumberLineOrbital',
    uses: [
      {
        'as': 'MathNumberline',
        'from': 'std/behaviors/std-math-numberline',
      },
    ],
    entity: {
      name: 'NumberlineScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'curves': {
            'default': [
              {
                'color': '#94a3b8',
                'label': '',
                'samples': [
                  {
                    'x': -9,
                    'y': 0,
                  },
                  {
                    'x': 9,
                    'y': 0,
                  },
                ],
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'points': {
            'default': [
              {
                'color': '#94a3b8',
                'label': '-9',
                'radius': 2,
                'x': -9,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-8',
                'radius': 2,
                'x': -8,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-7',
                'radius': 2,
                'x': -7,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-6',
                'radius': 2,
                'x': -6,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-5',
                'radius': 2,
                'x': -5,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-4',
                'radius': 2,
                'x': -4,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-3',
                'radius': 2,
                'x': -3,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-2',
                'radius': 2,
                'x': -2,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '-1',
                'radius': 2,
                'x': -1,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '0',
                'radius': 2,
                'x': 0,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '1',
                'radius': 2,
                'x': 1,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '2',
                'radius': 2,
                'x': 2,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '3',
                'radius': 2,
                'x': 3,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '4',
                'radius': 2,
                'x': 4,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '5',
                'radius': 2,
                'x': 5,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '6',
                'radius': 2,
                'x': 6,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '7',
                'radius': 2,
                'x': 7,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '8',
                'radius': 2,
                'x': 8,
                'y': 0,
              },
              {
                'color': '#94a3b8',
                'label': '9',
                'radius': 2,
                'x': 9,
                'y': 0,
              },
              {
                'color': '#dc2626',
                'label': 'n',
                'radius': 6,
                'x': -9,
                'y': 0,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'NumberlineScene',
        'name': 'NumberlineEngine',
        'ref': ('MathNumberline.traits.MathNumberlineSim' satisfies _StdLearningMathNumberLineOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'NumberLinePage',
        'path': '/math/numberline',
        'traits': [
          {
            'ref': 'NumberlineEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathNumberLineOrbital. */
export const StdLearningMathNumberLineOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'NumberLineOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'NumberlineEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathNumberLineOrbitalParams keys. */
export function isStdLearningMathNumberLineOrbitalParams(p: object): p is StdLearningMathNumberLineOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathNumberLineOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathNumberLineOrbitalManifest.traitNames,
      ...StdLearningMathNumberLineOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the ArithmeticSequenceOrbital orbital.
 *
 * Canonical entity: SequenceScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathArithmeticSequenceOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SequenceEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait ArithmeticSequenceOrbital's `uses[]` exports. */
type _StdLearningMathArithmeticSequenceOrbitalUsesRef = 'MathSequence.traits.MathSequenceSim';

/** Per-orbital factory: builds the ArithmeticSequenceOrbital orbital with consumer params. */
export function stdLearningMathArithmeticSequenceOrbital(params: StdLearningMathArithmeticSequenceOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'ArithmeticSequenceOrbital',
    uses: [
      {
        'as': 'MathSequence',
        'from': 'std/behaviors/std-math-sequence',
      },
    ],
    entity: {
      name: 'SequenceScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'a0': {
            'default': -7,
            'type': 'unknown',
          },
          'curves': {
            'default': [
              {
                'color': '#94a3b8',
                'label': 'seq',
                'samples': [
                  {
                    'x': 0,
                    'y': -7,
                  },
                  {
                    'x': 1,
                    'y': -5.3,
                  },
                  {
                    'x': 2,
                    'y': -3.6,
                  },
                  {
                    'x': 3,
                    'y': -1.9,
                  },
                  {
                    'x': 4,
                    'y': -0.2,
                  },
                  {
                    'x': 5,
                    'y': 1.5,
                  },
                  {
                    'x': 6,
                    'y': 3.2,
                  },
                  {
                    'x': 7,
                    'y': 4.9,
                  },
                  {
                    'x': 8,
                    'y': 6.6,
                  },
                ],
              },
            ],
            'type': 'unknown',
          },
          'd': {
            'default': 1.7,
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'points': {
            'default': [
              {
                'color': '#2563eb',
                'label': 'a0',
                'radius': 5,
                'x': 0,
                'y': -7,
              },
              {
                'color': '#2563eb',
                'label': 'a1',
                'radius': 5,
                'x': 1,
                'y': -5.3,
              },
              {
                'color': '#2563eb',
                'label': 'a2',
                'radius': 5,
                'x': 2,
                'y': -3.6,
              },
              {
                'color': '#2563eb',
                'label': 'a3',
                'radius': 5,
                'x': 3,
                'y': -1.9,
              },
              {
                'color': '#2563eb',
                'label': 'a4',
                'radius': 5,
                'x': 4,
                'y': -0.2,
              },
              {
                'color': '#2563eb',
                'label': 'a5',
                'radius': 5,
                'x': 5,
                'y': 1.5,
              },
              {
                'color': '#2563eb',
                'label': 'a6',
                'radius': 5,
                'x': 6,
                'y': 3.2,
              },
              {
                'color': '#2563eb',
                'label': 'a7',
                'radius': 5,
                'x': 7,
                'y': 4.9,
              },
              {
                'color': '#2563eb',
                'label': 'a8',
                'radius': 5,
                'x': 8,
                'y': 6.6,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'SequenceScene',
        'name': 'SequenceEngine',
        'ref': ('MathSequence.traits.MathSequenceSim' satisfies _StdLearningMathArithmeticSequenceOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'ArithmeticSequencePage',
        'path': '/math/sequence',
        'traits': [
          {
            'ref': 'SequenceEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathArithmeticSequenceOrbital. */
export const StdLearningMathArithmeticSequenceOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'ArithmeticSequenceOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'SequenceEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathArithmeticSequenceOrbitalParams keys. */
export function isStdLearningMathArithmeticSequenceOrbitalParams(p: object): p is StdLearningMathArithmeticSequenceOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathArithmeticSequenceOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathArithmeticSequenceOrbitalManifest.traitNames,
      ...StdLearningMathArithmeticSequenceOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the UnitCircleOrbital orbital.
 *
 * Canonical entity: UnitCircleScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathUnitCircleOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'UnitCircleEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait UnitCircleOrbital's `uses[]` exports. */
type _StdLearningMathUnitCircleOrbitalUsesRef = 'MathUnitCircle.traits.MathUnitCircleSim';

/** Per-orbital factory: builds the UnitCircleOrbital orbital with consumer params. */
export function stdLearningMathUnitCircleOrbital(params: StdLearningMathUnitCircleOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'UnitCircleOrbital',
    uses: [
      {
        'as': 'MathUnitCircle',
        'from': 'std/behaviors/std-math-unitcircle',
      },
    ],
    entity: {
      name: 'UnitCircleScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'UnitCircleScene',
        'name': 'UnitCircleEngine',
        'ref': ('MathUnitCircle.traits.MathUnitCircleSim' satisfies _StdLearningMathUnitCircleOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'UnitCirclePage',
        'path': '/math/unitcircle',
        'traits': [
          {
            'ref': 'UnitCircleEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathUnitCircleOrbital. */
export const StdLearningMathUnitCircleOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'UnitCircleOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'UnitCircleEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathUnitCircleOrbitalParams keys. */
export function isStdLearningMathUnitCircleOrbitalParams(p: object): p is StdLearningMathUnitCircleOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathUnitCircleOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathUnitCircleOrbitalManifest.traitNames,
      ...StdLearningMathUnitCircleOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the SineWaveOrbital orbital.
 *
 * Canonical entity: SineWaveScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMathSineWaveOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SineWaveEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait SineWaveOrbital's `uses[]` exports. */
type _StdLearningMathSineWaveOrbitalUsesRef = 'MathSineWave.traits.MathSineWaveSim';

/** Per-orbital factory: builds the SineWaveOrbital orbital with consumer params. */
export function stdLearningMathSineWaveOrbital(params: StdLearningMathSineWaveOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'SineWaveOrbital',
    uses: [
      {
        'as': 'MathSineWave',
        'from': 'std/behaviors/std-math-sinewave',
      },
    ],
    entity: {
      name: 'SineWaveScene',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'SineWaveScene',
        'name': 'SineWaveEngine',
        'ref': ('MathSineWave.traits.MathSineWaveSim' satisfies _StdLearningMathSineWaveOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'SineWavePage',
        'path': '/math/sinewave',
        'traits': [
          {
            'ref': 'SineWaveEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMathSineWaveOrbital. */
export const StdLearningMathSineWaveOrbitalManifest = {
  organism: 'learning-math',
  orbitalName: 'SineWaveOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'SineWaveEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningMathSineWaveOrbitalParams keys. */
export function isStdLearningMathSineWaveOrbitalParams(p: object): p is StdLearningMathSineWaveOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMathSineWaveOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMathSineWaveOrbitalManifest.traitNames,
      ...StdLearningMathSineWaveOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-math — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningMathParams {
  VectorAddition?: StdLearningMathVectorAdditionOrbitalParams;
  LinearSystems?: StdLearningMathLinearSystemsOrbitalParams;
  NumberLine?: StdLearningMathNumberLineOrbitalParams;
  ArithmeticSequence?: StdLearningMathArithmeticSequenceOrbitalParams;
  UnitCircle?: StdLearningMathUnitCircleOrbitalParams;
  SineWave?: StdLearningMathSineWaveOrbitalParams;
}

/** Whole-organism descriptor (6 orbitals). Composes per-orbital factories. */
export function stdLearningMath(params: StdLearningMathParams = {}): OrbitalDefinition[] {
  return [
    stdLearningMathVectorAdditionOrbital(params.VectorAddition ?? {}),
    stdLearningMathLinearSystemsOrbital(params.LinearSystems ?? {}),
    stdLearningMathNumberLineOrbital(params.NumberLine ?? {}),
    stdLearningMathArithmeticSequenceOrbital(params.ArithmeticSequence ?? {}),
    stdLearningMathUnitCircleOrbital(params.UnitCircle ?? {}),
    stdLearningMathSineWaveOrbital(params.SineWave ?? {}),
  ];
}
