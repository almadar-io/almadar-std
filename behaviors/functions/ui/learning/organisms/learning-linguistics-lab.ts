/**
 * learning-linguistics-lab — Linguistics lab organism — a Universal Grammar syntax-tree builder (learner assembles a sentence's constituent structure bottom-up, one labelled bracket at a time) and a historical sound-change stepper (learner applies ordered Great Vowel Shift rules and watches every matching word's pronunciation shift at once), composed from the std-ling-syntaxtree and std-ling-soundchange instrument atoms behind a mode router.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-linguistics-lab.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-linguistics-lab
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-linguistics-lab';
const ALIAS = 'LearningLinguisticsLab';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningLinguisticsLabEventKey = 'INIT' | 'MODE_SELECTED';

/**
 * Payload shape for the `MODE_SELECTED` event.
 */
export interface StdLearningLinguisticsLabModeSelectedPayload {
  mode?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningLinguisticsLabConfig {
  /** Default: `"syntax-tree"` */
  mode?: 'syntax-tree' | 'sound-change';
}

type _StdLearningLinguisticsLabEntityName = 'LinguisticsLabItem' | 'SyntaxtreeScene' | 'SoundchangeScene' | 'LingSyntaxtreeScene' | 'LingSoundchangeScene';
type _StdLearningLinguisticsLabListenTraitName = 'LinguisticsLabRouter' | 'SyntaxtreeSim' | 'SoundchangeSim';

/**
 * Tunable params for the LinguisticsLabOrbital orbital.
 *
 * Canonical entity: LinguisticsLabItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningLinguisticsLabLinguisticsLabOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SyntaxtreeSim' | 'SoundchangeSim' | 'LinguisticsLabRouter',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait LinguisticsLabOrbital's `uses[]` exports. */
type _StdLearningLinguisticsLabLinguisticsLabOrbitalUsesRef = 'LingSyntaxtree.traits.LingSyntaxtreeSim' | 'LingSoundchange.traits.LingSoundchangeSim';

/** Per-orbital factory: builds the LinguisticsLabOrbital orbital with consumer params. */
export function stdLearningLinguisticsLabLinguisticsLabOrbital(params: StdLearningLinguisticsLabLinguisticsLabOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'LinguisticsLabOrbital',
    uses: [
      {
        'as': 'LingSyntaxtree',
        'from': 'std/behaviors/std-ling-syntaxtree',
      },
      {
        'as': 'LingSoundchange',
        'from': 'std/behaviors/std-ling-soundchange',
      },
    ],
    entity: {
      name: 'LinguisticsLabItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    auxiliaryEntities: [
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'align': {
                  'name': 'align',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'left',
                    'center',
                    'right',
                  ],
                },
                'axis': {
                  'name': 'axis',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'x',
                    'y',
                  ],
                },
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'fill': {
                  'name': 'fill',
                  'required': false,
                  'type': 'string',
                },
                'fontSize': {
                  'name': 'fontSize',
                  'required': false,
                  'type': 'number',
                },
                'height': {
                  'name': 'height',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'lineWidth': {
                  'name': 'lineWidth',
                  'required': false,
                  'type': 'number',
                },
                'max': {
                  'name': 'max',
                  'required': false,
                  'type': 'number',
                },
                'min': {
                  'name': 'min',
                  'required': false,
                  'type': 'number',
                },
                'opacity': {
                  'name': 'opacity',
                  'required': false,
                  'type': 'number',
                },
                'path': {
                  'name': 'path',
                  'required': false,
                  'type': 'string',
                },
                'points': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'points',
                  'required': false,
                  'type': 'array',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'step': {
                  'name': 'step',
                  'required': false,
                  'type': 'number',
                },
                'text': {
                  'name': 'text',
                  'required': false,
                  'type': 'string',
                },
                'type': {
                  'name': 'type',
                  'required': true,
                  'type': 'string',
                  'values': [
                    'line',
                    'arrow',
                    'circle',
                    'rect',
                    'polygon',
                    'path',
                    'text',
                    'axis',
                    'grid',
                  ],
                },
                'width': {
                  'name': 'width',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': false,
                  'type': 'number',
                },
                'x1': {
                  'name': 'x1',
                  'required': false,
                  'type': 'number',
                },
                'x2': {
                  'name': 'x2',
                  'required': false,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': false,
                  'type': 'number',
                },
                'y1': {
                  'name': 'y1',
                  'required': false,
                  'type': 'number',
                },
                'y2': {
                  'name': 'y2',
                  'required': false,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'shapes',
            'type': 'array',
          },
          {
            'name': 'sentenceIndex',
            'type': 'number',
          },
          {
            'name': 'stepIndex',
            'type': 'number',
          },
          {
            'name': 'syntaxFeedback',
            'type': 'string',
          },
        ],
        'name': 'SyntaxtreeScene',
        'persistence': 'runtime',
      },
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'align': {
                  'name': 'align',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'left',
                    'center',
                    'right',
                  ],
                },
                'axis': {
                  'name': 'axis',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'x',
                    'y',
                  ],
                },
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'fill': {
                  'name': 'fill',
                  'required': false,
                  'type': 'string',
                },
                'fontSize': {
                  'name': 'fontSize',
                  'required': false,
                  'type': 'number',
                },
                'height': {
                  'name': 'height',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'lineWidth': {
                  'name': 'lineWidth',
                  'required': false,
                  'type': 'number',
                },
                'max': {
                  'name': 'max',
                  'required': false,
                  'type': 'number',
                },
                'min': {
                  'name': 'min',
                  'required': false,
                  'type': 'number',
                },
                'opacity': {
                  'name': 'opacity',
                  'required': false,
                  'type': 'number',
                },
                'path': {
                  'name': 'path',
                  'required': false,
                  'type': 'string',
                },
                'points': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'points',
                  'required': false,
                  'type': 'array',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'step': {
                  'name': 'step',
                  'required': false,
                  'type': 'number',
                },
                'text': {
                  'name': 'text',
                  'required': false,
                  'type': 'string',
                },
                'type': {
                  'name': 'type',
                  'required': true,
                  'type': 'string',
                  'values': [
                    'line',
                    'arrow',
                    'circle',
                    'rect',
                    'polygon',
                    'path',
                    'text',
                    'axis',
                    'grid',
                  ],
                },
                'width': {
                  'name': 'width',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': false,
                  'type': 'number',
                },
                'x1': {
                  'name': 'x1',
                  'required': false,
                  'type': 'number',
                },
                'x2': {
                  'name': 'x2',
                  'required': false,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': false,
                  'type': 'number',
                },
                'y1': {
                  'name': 'y1',
                  'required': false,
                  'type': 'number',
                },
                'y2': {
                  'name': 'y2',
                  'required': false,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'shapes',
            'type': 'array',
          },
          {
            'name': 'ruleIndex',
            'type': 'number',
          },
          {
            'name': 'changedSummary',
            'type': 'string',
          },
          {
            'name': 'done',
            'type': 'boolean',
          },
        ],
        'name': 'SoundchangeScene',
        'persistence': 'runtime',
      },
    ] as EntityRef[],
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'mode': {
            'default': 'syntax-tree',
            'description': 'Which linguistics simulation to render: a Universal Grammar constituent-structure parse-tree builder, or a historical sound-change (vowel shift) stepper.',
            'label': 'Mode',
            'synonyms': 'linguistics, syntax, parse tree, constituent, constituent structure, phrase structure, Universal Grammar, grammar, sentence structure, X-bar theory, noun phrase, verb phrase, prepositional phrase, phonology, sound change, sound law, vowel shift, Great Vowel Shift, diphthongization, historical linguistics, phonetic change',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'syntax-tree',
              'sound-change',
            ],
          },
        },
        'emits': [
          {
            'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
            'event': 'MODE_SELECTED',
            'payloadSchema': [
              {
                'name': 'mode',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'LinguisticsLabItem',
        'name': 'LinguisticsLabRouter',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
              'key': 'MODE_SELECTED',
              'name': 'Mode Selected',
              'payloadSchema': [
                {
                  'name': 'mode',
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'emit',
                  'MODE_SELECTED',
                  {
                    'mode': '@config.mode',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'syntax-tree',
            'type': 'unknown',
          },
          'title': {
            'default': 'Linguistics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'SyntaxtreeScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'LinguisticsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'SyntaxtreeSim',
        'ref': ('LingSyntaxtree.traits.LingSyntaxtreeSim' satisfies _StdLearningLinguisticsLabLinguisticsLabOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'sound-change',
            'type': 'unknown',
          },
          'title': {
            'default': 'Linguistics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'SoundchangeScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'LinguisticsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'SoundchangeSim',
        'ref': ('LingSoundchange.traits.LingSoundchangeSim' satisfies _StdLearningLinguisticsLabLinguisticsLabOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'LinguisticsLabPage',
        'path': '/learning/linguistics-lab',
        'traits': [
          {
            'ref': 'LinguisticsLabRouter',
          },
          {
            'ref': 'SyntaxtreeSim',
          },
          {
            'ref': 'SoundchangeSim',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningLinguisticsLabLinguisticsLabOrbital. */
export const StdLearningLinguisticsLabLinguisticsLabOrbitalManifest = {
  organism: 'learning-linguistics-lab',
  orbitalName: 'LinguisticsLabOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'SyntaxtreeSim',
    'SoundchangeSim',
  ] as const,
  inlineTraitNames: [
    'LinguisticsLabRouter',
  ] as const,
};

/** Typed guard — runtime validates StdLearningLinguisticsLabLinguisticsLabOrbitalParams keys. */
export function isStdLearningLinguisticsLabLinguisticsLabOrbitalParams(p: object): p is StdLearningLinguisticsLabLinguisticsLabOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningLinguisticsLabLinguisticsLabOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningLinguisticsLabLinguisticsLabOrbitalManifest.traitNames,
      ...StdLearningLinguisticsLabLinguisticsLabOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-linguistics-lab — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningLinguisticsLabParams {
  LinguisticsLab?: StdLearningLinguisticsLabLinguisticsLabOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningLinguisticsLab(params: StdLearningLinguisticsLabParams = {}): OrbitalDefinition[] {
  return [
    stdLearningLinguisticsLabLinguisticsLabOrbital(params.LinguisticsLab ?? {}),
  ];
}
