/**
 * learning-molecule-viewer — Molecule viewer organism — a chemistry-canvas bound to entity data.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-molecule-viewer.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-molecule-viewer
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-molecule-viewer';
const ALIAS = 'LearningMoleculeViewer';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningMoleculeViewerEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningMoleculeViewerConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `[]` */
  arrows?: EntityRow[];
  /** Default: `[{"color":"#374151","element":"C","id":"C","radius":22,"x":300,"y":200},{"color":"#dc2626","element":"O","id":"O","radius":20,"x":380,"y":160},{"color":"#2563eb","element":"H","id":"H1","radius":14,"x":240,"y":160},{"color":"#2563eb","element":"H","id":"H2","radius":14,"x":260,"y":270},{"color":"#2563eb","element":"H","id":"H3","radius":14,"x":340,"y":270}]` */
  atoms?: EntityRow[];
  /** Default: `"#fafafa"` */
  backgroundColor?: string;
  /** Default: `[{"from":"C","to":"O","type":"double"},{"from":"C","to":"H1","type":"single"},{"from":"C","to":"H2","type":"single"},{"from":"C","to":"H3","type":"single"}]` */
  bonds?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `"Molecule Viewer"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Tunable params for the MoleculeViewerOrbital orbital.
 *
 * Canonical entity: MoleculeViewerItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningMoleculeViewerMoleculeViewerOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MoleculeViewerRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the MoleculeViewerOrbital orbital with consumer params. */
export function stdLearningMoleculeViewerMoleculeViewerOrbital(params: StdLearningMoleculeViewerMoleculeViewerOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'MoleculeViewerItem';
  const built = makeOrbitalWithUses({
    name: 'MoleculeViewerOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'backgroundColor',
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'element': {
                  'name': 'element',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'atoms',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'from': {
                  'name': 'from',
                  'required': true,
                  'type': 'string',
                },
                'to': {
                  'name': 'to',
                  'required': true,
                  'type': 'string',
                },
                'type': {
                  'name': 'type',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'single',
                    'double',
                    'triple',
                  ],
                },
              },
              'type': 'object',
            },
            'name': 'bonds',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'angle': {
                  'name': 'angle',
                  'required': false,
                  'type': 'number',
                },
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'length': {
                  'name': 'length',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'arrows',
            'type': 'array',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'animate': {
            'default': false,
            'description': 'Enable continuous redraw loop.',
            'label': 'Animate',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'arrows': {
            'default': [],
            'description': 'Reaction arrows.',
            'items': {
              'properties': {
                'angle': {
                  'name': 'angle',
                  'required': false,
                  'type': 'number',
                },
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'length': {
                  'name': 'length',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Arrows',
            'tier': 'domain',
            'type': '[MoleculeViewerArrow]',
          },
          'atoms': {
            'default': [
              {
                'color': '#374151',
                'element': 'C',
                'id': 'C',
                'radius': 22,
                'x': 300,
                'y': 200,
              },
              {
                'color': '#dc2626',
                'element': 'O',
                'id': 'O',
                'radius': 20,
                'x': 380,
                'y': 160,
              },
              {
                'color': '#2563eb',
                'element': 'H',
                'id': 'H1',
                'radius': 14,
                'x': 240,
                'y': 160,
              },
              {
                'color': '#2563eb',
                'element': 'H',
                'id': 'H2',
                'radius': 14,
                'x': 260,
                'y': 270,
              },
              {
                'color': '#2563eb',
                'element': 'H',
                'id': 'H3',
                'radius': 14,
                'x': 340,
                'y': 270,
              },
            ],
            'description': 'Atoms to render.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'element': {
                  'name': 'element',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Atoms',
            'tier': 'domain',
            'type': '[MoleculeViewerAtom]',
          },
          'backgroundColor': {
            'default': '#fafafa',
            'label': 'Background Color',
            'tier': 'presentation',
            'type': 'string',
          },
          'bonds': {
            'default': [
              {
                'from': 'C',
                'to': 'O',
                'type': 'double',
              },
              {
                'from': 'C',
                'to': 'H1',
                'type': 'single',
              },
              {
                'from': 'C',
                'to': 'H2',
                'type': 'single',
              },
              {
                'from': 'C',
                'to': 'H3',
                'type': 'single',
              },
            ],
            'description': 'Bonds between atoms.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'from': {
                  'name': 'from',
                  'required': true,
                  'type': 'string',
                },
                'to': {
                  'name': 'to',
                  'required': true,
                  'type': 'string',
                },
                'type': {
                  'name': 'type',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'single',
                    'double',
                    'triple',
                  ],
                },
              },
              'type': 'object',
            },
            'label': 'Bonds',
            'tier': 'domain',
            'type': '[MoleculeViewerBond]',
          },
          'height': {
            'default': 400,
            'label': 'Height',
            'tier': 'presentation',
            'type': 'number',
          },
          'interactive': {
            'default': false,
            'description': 'Enable pointer interaction on the canvas.',
            'label': 'Interactive',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'title': {
            'default': 'Molecule Viewer',
            'description': 'Title shown above the molecule.',
            'label': 'Title',
            'tier': 'domain',
            'type': 'string',
          },
          'width': {
            'default': 600,
            'label': 'Width',
            'tier': 'presentation',
            'type': 'number',
          },
        },
        'entityContract': {
          'provides': [
            'arrows',
            'atoms',
            'backgroundColor',
            'bonds',
            'title',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'MoleculeViewerItem',
        'name': 'MoleculeViewerRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.title',
                  '@config.title',
                ],
                [
                  'set',
                  '@entity.backgroundColor',
                  '@config.backgroundColor',
                ],
                [
                  'set',
                  '@entity.atoms',
                  '@config.atoms',
                ],
                [
                  'set',
                  '@entity.bonds',
                  '@config.bonds',
                ],
                [
                  'set',
                  '@entity.arrows',
                  '@config.arrows',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'content': '@entity.title',
                        'type': 'typography',
                        'variant': 'h4',
                      },
                      {
                        'animate': '@config.animate',
                        'arrows': '@entity.arrows',
                        'atoms': '@entity.atoms',
                        'backgroundColor': '@entity.backgroundColor',
                        'bonds': '@entity.bonds',
                        'height': '@config.height',
                        'interactive': '@config.interactive',
                        'type': 'chemistry-canvas',
                        'width': '@config.width',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'padding': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } as never, 'MoleculeViewerItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'MoleculeViewerPage',
        'path': '/learning/molecule-viewer',
        'traits': [
          {
            'ref': 'MoleculeViewerRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningMoleculeViewerMoleculeViewerOrbital. */
export const StdLearningMoleculeViewerMoleculeViewerOrbitalManifest = {
  organism: 'learning-molecule-viewer',
  orbitalName: 'MoleculeViewerOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'MoleculeViewerRender',
  ] as const,
};

/** Typed guard — runtime validates StdLearningMoleculeViewerMoleculeViewerOrbitalParams keys. */
export function isStdLearningMoleculeViewerMoleculeViewerOrbitalParams(p: object): p is StdLearningMoleculeViewerMoleculeViewerOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningMoleculeViewerMoleculeViewerOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningMoleculeViewerMoleculeViewerOrbitalManifest.traitNames,
      ...StdLearningMoleculeViewerMoleculeViewerOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-molecule-viewer — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningMoleculeViewerParams {
  MoleculeViewer?: StdLearningMoleculeViewerMoleculeViewerOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningMoleculeViewer(params: StdLearningMoleculeViewerParams = {}): OrbitalDefinition[] {
  return [
    stdLearningMoleculeViewerMoleculeViewerOrbital(params.MoleculeViewer ?? {}),
  ];
}
