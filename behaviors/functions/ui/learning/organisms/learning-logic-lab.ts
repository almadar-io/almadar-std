/**
 * learning-logic-lab — Propositional logic lab organism — toggle atomic proposition truth values and watch compound formulas evaluate live on a truth table, or apply inference rules (modus ponens, modus tollens) step by step to derive new propositions in a guided proof, composed from the std-logic-truthtable and std-logic-inference instrument atoms behind a mode router.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-logic-lab.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-logic-lab
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-logic-lab';
const ALIAS = 'LearningLogicLab';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningLogicLabEventKey = 'INIT' | 'MODE_SELECTED';

/**
 * Payload shape for the `MODE_SELECTED` event.
 */
export interface StdLearningLogicLabModeSelectedPayload {
  mode?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningLogicLabConfig {
  /** Default: `"truth-table"` */
  mode?: 'truth-table' | 'inference';
}

type _StdLearningLogicLabEntityName = 'LogicLabItem' | 'TruthTableScene' | 'InferenceScene';
type _StdLearningLogicLabListenTraitName = 'LogicLabRouter' | 'TruthTableSim' | 'InferenceSim';

/**
 * Tunable params for the LogicLabOrbital orbital.
 *
 * Canonical entity: LogicLabItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningLogicLabLogicLabOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'TruthTableSim' | 'InferenceSim' | 'LogicLabRouter',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait LogicLabOrbital's `uses[]` exports. */
type _StdLearningLogicLabLogicLabOrbitalUsesRef = 'LogicTruthtable.traits.LogicTruthTableSim' | 'LogicInference.traits.LogicInferenceSim';

/** Per-orbital factory: builds the LogicLabOrbital orbital with consumer params. */
export function stdLearningLogicLabLogicLabOrbital(params: StdLearningLogicLabLogicLabOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'LogicLabOrbital',
    uses: [
      {
        'as': 'LogicTruthtable',
        'from': 'std/behaviors/std-logic-truthtable',
      },
      {
        'as': 'LogicInference',
        'from': 'std/behaviors/std-logic-inference',
      },
    ],
    entity: {
      name: 'LogicLabItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    auxiliaryEntities: [
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'valP',
            'type': 'boolean',
          },
          {
            'name': 'valQ',
            'type': 'boolean',
          },
          {
            'name': 'valR',
            'type': 'boolean',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'tableRows',
            'type': 'array',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
        ],
        'name': 'TruthTableScene',
        'persistence': 'runtime',
      },
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'proofLines',
            'type': 'array',
          },
          {
            'name': 'feedback',
            'type': 'string',
          },
          {
            'name': 'goalReached',
            'type': 'boolean',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
        ],
        'name': 'InferenceScene',
        'persistence': 'runtime',
      },
    ] as EntityRef[],
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'mode': {
            'default': 'truth-table',
            'description': 'Whether the learner toggles proposition truth values against a live truth table, or applies inference rules to derive new lines in a proof.',
            'label': 'Mode',
            'synonyms': 'logic, boolean logic, propositional logic, truth table, truth value, proposition, atomic proposition, logical connective, conjunction, disjunction, negation, material implication, logical implication, AND gate, OR gate, NOT gate, inference rule, modus ponens, modus tollens, syllogism, logical deduction, logical proof, premise, logical argument, tautology, contradiction, logical equivalence',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'truth-table',
              'inference',
            ],
          },
        },
        'emits': [
          {
            'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
            'event': 'MODE_SELECTED',
            'payloadSchema': [
              {
                'name': 'mode',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'LogicLabItem',
        'name': 'LogicLabRouter',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
              'key': 'MODE_SELECTED',
              'name': 'Mode Selected',
              'payloadSchema': [
                {
                  'name': 'mode',
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'emit',
                  'MODE_SELECTED',
                  {
                    'mode': '@config.mode',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'truth-table',
            'type': 'unknown',
          },
          'title': {
            'default': 'Logic Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'TruthTableScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'LogicLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'TruthTableSim',
        'ref': ('LogicTruthtable.traits.LogicTruthTableSim' satisfies _StdLearningLogicLabLogicLabOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'inference',
            'type': 'unknown',
          },
          'title': {
            'default': 'Logic Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'InferenceScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'LogicLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'InferenceSim',
        'ref': ('LogicInference.traits.LogicInferenceSim' satisfies _StdLearningLogicLabLogicLabOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'LogicLabPage',
        'path': '/learning/logic-lab',
        'traits': [
          {
            'ref': 'LogicLabRouter',
          },
          {
            'ref': 'TruthTableSim',
          },
          {
            'ref': 'InferenceSim',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningLogicLabLogicLabOrbital. */
export const StdLearningLogicLabLogicLabOrbitalManifest = {
  organism: 'learning-logic-lab',
  orbitalName: 'LogicLabOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'TruthTableSim',
    'InferenceSim',
  ] as const,
  inlineTraitNames: [
    'LogicLabRouter',
  ] as const,
};

/** Typed guard — runtime validates StdLearningLogicLabLogicLabOrbitalParams keys. */
export function isStdLearningLogicLabLogicLabOrbitalParams(p: object): p is StdLearningLogicLabLogicLabOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningLogicLabLogicLabOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningLogicLabLogicLabOrbitalManifest.traitNames,
      ...StdLearningLogicLabLogicLabOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-logic-lab — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningLogicLabParams {
  LogicLab?: StdLearningLogicLabLogicLabOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningLogicLab(params: StdLearningLogicLabParams = {}): OrbitalDefinition[] {
  return [
    stdLearningLogicLabLogicLabOrbital(params.LogicLab ?? {}),
  ];
}
