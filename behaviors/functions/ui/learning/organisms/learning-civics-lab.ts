/**
 * learning-civics-lab — Civics lab organism — how a bill becomes law (a bill token travels Introduced -> House vote -> Senate vote -> President, with veto and a two-thirds override) and a checks-and-balances matrix (learner names which branch can check a given action, drawing the check arrow on a three-branch diagram), composed from the std-civics-billprocess and std-civics-checks instrument atoms behind a mode router.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-civics-lab.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-civics-lab
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-civics-lab';
const ALIAS = 'LearningCivicsLab';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningCivicsLabEventKey = 'INIT' | 'MODE_SELECTED';

/**
 * Payload shape for the `MODE_SELECTED` event.
 */
export interface StdLearningCivicsLabModeSelectedPayload {
  mode?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningCivicsLabConfig {
  /** Default: `"process"` */
  mode?: 'process' | 'checks-balances';
}

type _StdLearningCivicsLabEntityName = 'CivicsLabItem' | 'BillProcessScene' | 'ChecksScene' | 'CivicsBillProcessScene' | 'CivicsChecksScene';
type _StdLearningCivicsLabListenTraitName = 'CivicsLabRouter' | 'BillProcessSim' | 'ChecksSim';

/**
 * Tunable params for the CivicsLabOrbital orbital.
 *
 * Canonical entity: CivicsLabItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningCivicsLabCivicsLabOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'BillProcessSim' | 'ChecksSim' | 'CivicsLabRouter',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait CivicsLabOrbital's `uses[]` exports. */
type _StdLearningCivicsLabCivicsLabOrbitalUsesRef = 'CivicsBillProcess.traits.CivicsBillProcessSim' | 'CivicsChecks.traits.CivicsChecksSim';

/** Per-orbital factory: builds the CivicsLabOrbital orbital with consumer params. */
export function stdLearningCivicsLabCivicsLabOrbital(params: StdLearningCivicsLabCivicsLabOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'CivicsLabOrbital',
    uses: [
      {
        'as': 'CivicsBillProcess',
        'from': 'std/behaviors/std-civics-billprocess',
      },
      {
        'as': 'CivicsChecks',
        'from': 'std/behaviors/std-civics-checks',
      },
    ],
    entity: {
      name: 'CivicsLabItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    auxiliaryEntities: [
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'billIndex',
            'type': 'number',
          },
          {
            'name': 'billTitle',
            'type': 'string',
          },
          {
            'name': 'houseVotesLabel',
            'type': 'string',
          },
          {
            'name': 'senateVotesLabel',
            'type': 'string',
          },
          {
            'name': 'overrideVotesLabel',
            'type': 'string',
          },
          {
            'name': 'stationIndex',
            'type': 'number',
          },
          {
            'name': 'stationLabel',
            'type': 'string',
          },
          {
            'name': 'narration',
            'type': 'string',
          },
          {
            'name': 'vetoed',
            'type': 'boolean',
          },
          {
            'name': 'outcome',
            'type': 'string',
          },
          {
            'name': 'outcomeBadgeLabel',
            'type': 'string',
          },
          {
            'name': 'outcomeBadgeVariant',
            'type': 'string',
          },
          {
            'name': 'overrideReadout',
            'type': 'string',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
        ],
        'name': 'BillProcessScene',
        'persistence': 'runtime',
      },
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'actionIndex',
            'type': 'number',
          },
          {
            'name': 'actorBranch',
            'type': 'string',
          },
          {
            'name': 'actionText',
            'type': 'string',
          },
          {
            'name': 'hintText',
            'type': 'string',
          },
          {
            'name': 'resultState',
            'type': 'string',
          },
          {
            'name': 'feedback',
            'type': 'string',
          },
          {
            'name': 'checkedBranch',
            'type': 'string',
          },
          {
            'name': 'checkLabel',
            'type': 'string',
          },
          {
            'name': 'score',
            'type': 'number',
          },
          {
            'name': 'attempts',
            'type': 'number',
          },
          {
            'items': {
              'type': 'object',
            },
            'name': 'renderChildren',
            'type': 'array',
          },
        ],
        'name': 'ChecksScene',
        'persistence': 'runtime',
      },
    ] as EntityRef[],
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'mode': {
            'default': 'process',
            'description': 'Which civics simulation to render: how a bill becomes law (introduction, House and Senate votes, presidential signature or veto, and a two-thirds override), or a checks-and-balances matrix where the learner names which branch can check a given governmental action.',
            'label': 'Mode',
            'synonyms': 'civics, government, how a bill becomes law, legislative process, separation of powers, checks and balances, branches of government, legislative branch, executive branch, judicial branch, Congress, House of Representatives, Senate, president, veto, veto override, two-thirds vote, supermajority, judicial review, Marbury v. Madison, unconstitutional, constitutional check, bill, law, enact a law, override a veto, advice and consent, treaty ratification',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'process',
              'checks-balances',
            ],
          },
        },
        'emits': [
          {
            'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
            'event': 'MODE_SELECTED',
            'payloadSchema': [
              {
                'name': 'mode',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'CivicsLabItem',
        'name': 'CivicsLabRouter',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
              'key': 'MODE_SELECTED',
              'name': 'Mode Selected',
              'payloadSchema': [
                {
                  'name': 'mode',
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'emit',
                  'MODE_SELECTED',
                  {
                    'mode': '@config.mode',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'process',
            'type': 'unknown',
          },
          'title': {
            'default': 'Civics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'BillProcessScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'CivicsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'BillProcessSim',
        'ref': ('CivicsBillProcess.traits.CivicsBillProcessSim' satisfies _StdLearningCivicsLabCivicsLabOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'checks-balances',
            'type': 'unknown',
          },
          'title': {
            'default': 'Civics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'ChecksScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'CivicsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'ChecksSim',
        'ref': ('CivicsChecks.traits.CivicsChecksSim' satisfies _StdLearningCivicsLabCivicsLabOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'CivicsLabPage',
        'path': '/learning/civics-lab',
        'traits': [
          {
            'ref': 'CivicsLabRouter',
          },
          {
            'ref': 'BillProcessSim',
          },
          {
            'ref': 'ChecksSim',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningCivicsLabCivicsLabOrbital. */
export const StdLearningCivicsLabCivicsLabOrbitalManifest = {
  organism: 'learning-civics-lab',
  orbitalName: 'CivicsLabOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'BillProcessSim',
    'ChecksSim',
  ] as const,
  inlineTraitNames: [
    'CivicsLabRouter',
  ] as const,
};

/** Typed guard — runtime validates StdLearningCivicsLabCivicsLabOrbitalParams keys. */
export function isStdLearningCivicsLabCivicsLabOrbitalParams(p: object): p is StdLearningCivicsLabCivicsLabOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningCivicsLabCivicsLabOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningCivicsLabCivicsLabOrbitalManifest.traitNames,
      ...StdLearningCivicsLabCivicsLabOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-civics-lab — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningCivicsLabParams {
  CivicsLab?: StdLearningCivicsLabCivicsLabOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningCivicsLab(params: StdLearningCivicsLabParams = {}): OrbitalDefinition[] {
  return [
    stdLearningCivicsLabCivicsLabOrbital(params.CivicsLab ?? {}),
  ];
}
