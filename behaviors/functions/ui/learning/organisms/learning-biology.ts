/**
 * learning-biology — Biology learning app — a fat organism whose orbitals are individual biology topics, each composed via `uses` from a per-topic engine atom on the biology-canvas.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-biology.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-biology
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-biology';
const ALIAS = 'LearningBiology';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningBiologyConfig {
  /** Default: `[{"color":"#94a3b8","from":"predator","label":"eats","to":"prey"}]` */
  edges?: unknown;
  /** Default: `400` */
  height?: unknown;
  /** Default: `[{"color":"#16a34a","id":"prey","kind":"organism","label":"Prey","radius":20,"x":210,"y":200},{"color":"#dc2626","id":"predator","kind":"organism","label":"Predator","radius":20,"x":400,"y":200}]` */
  nodes?: unknown;
  /** Default: `true` */
  running?: unknown;
  /** Default: `600` */
  width?: unknown;
}

type _StdLearningBiologyEntityName = 'PopulationScene' | 'FoodwebScene' | 'MitosisScene' | 'CellScene';
type _StdLearningBiologyListenTraitName = 'PopulationEngine' | 'FoodwebEngine' | 'MitosisEngine' | 'CellEngine';

/**
 * Tunable params for the PredatorPreyOrbital orbital.
 *
 * Canonical entity: PopulationScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningBiologyPredatorPreyOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'PopulationEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait PredatorPreyOrbital's `uses[]` exports. */
type _StdLearningBiologyPredatorPreyOrbitalUsesRef = 'BioPopulation.traits.BioPopulationSim';

/** Per-orbital factory: builds the PredatorPreyOrbital orbital with consumer params. */
export function stdLearningBiologyPredatorPreyOrbital(params: StdLearningBiologyPredatorPreyOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'PopulationScene';
  const built = makeOrbitalWithUses({
    name: 'PredatorPreyOrbital',
    uses: [
      {
        'as': 'BioPopulation',
        'from': 'std/behaviors/std-bio-population',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'kind': {
                  'name': 'kind',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'cell',
                    'organelle',
                    'molecule',
                    'organism',
                  ],
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'edges': {
            'default': [
              {
                'color': '#94a3b8',
                'from': 'predator',
                'label': 'eats',
                'to': 'prey',
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'nodes': {
            'default': [
              {
                'color': '#16a34a',
                'id': 'prey',
                'kind': 'organism',
                'label': 'Prey',
                'radius': 20,
                'x': 210,
                'y': 200,
              },
              {
                'color': '#dc2626',
                'id': 'predator',
                'kind': 'organism',
                'label': 'Predator',
                'radius': 20,
                'x': 400,
                'y': 200,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'name': 'PopulationEngine',
        'ref': ('BioPopulation.traits.BioPopulationSim' satisfies _StdLearningBiologyPredatorPreyOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'PredatorPreyPage',
        'path': '/bio/population',
        'traits': [
          {
            'ref': 'PopulationEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningBiologyPredatorPreyOrbital. */
export const StdLearningBiologyPredatorPreyOrbitalManifest = {
  organism: 'learning-biology',
  orbitalName: 'PredatorPreyOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'PopulationEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningBiologyPredatorPreyOrbitalParams keys. */
export function isStdLearningBiologyPredatorPreyOrbitalParams(p: object): p is StdLearningBiologyPredatorPreyOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningBiologyPredatorPreyOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningBiologyPredatorPreyOrbitalManifest.traitNames,
      ...StdLearningBiologyPredatorPreyOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the FoodwebOrbital orbital.
 *
 * Canonical entity: FoodwebScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningBiologyFoodwebOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'FoodwebEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait FoodwebOrbital's `uses[]` exports. */
type _StdLearningBiologyFoodwebOrbitalUsesRef = 'BioFoodweb.traits.BioFoodwebSim';

/** Per-orbital factory: builds the FoodwebOrbital orbital with consumer params. */
export function stdLearningBiologyFoodwebOrbital(params: StdLearningBiologyFoodwebOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'FoodwebScene';
  const built = makeOrbitalWithUses({
    name: 'FoodwebOrbital',
    uses: [
      {
        'as': 'BioFoodweb',
        'from': 'std/behaviors/std-bio-foodweb',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'kind': {
                  'name': 'kind',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'cell',
                    'organelle',
                    'molecule',
                    'organism',
                  ],
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'edges': {
            'default': [
              {
                'color': '#94a3b8',
                'from': 'rabbit',
                'label': 'eats',
                'to': 'grass',
              },
              {
                'color': '#94a3b8',
                'from': 'insect',
                'label': 'eats',
                'to': 'grass',
              },
              {
                'color': '#94a3b8',
                'from': 'fox',
                'label': 'eats',
                'to': 'rabbit',
              },
              {
                'color': '#94a3b8',
                'from': 'fox',
                'label': 'eats',
                'to': 'insect',
              },
              {
                'color': '#94a3b8',
                'from': 'hawk',
                'label': 'eats',
                'to': 'fox',
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'nodes': {
            'default': [
              {
                'color': '#16a34a',
                'id': 'grass',
                'kind': 'organism',
                'label': 'Grass',
                'radius': 20,
                'x': 300,
                'y': 340,
              },
              {
                'color': '#a16207',
                'id': 'rabbit',
                'kind': 'organism',
                'label': 'Rabbit',
                'radius': 18,
                'x': 180,
                'y': 220,
              },
              {
                'color': '#0891b2',
                'id': 'insect',
                'kind': 'organism',
                'label': 'Insect',
                'radius': 14,
                'x': 420,
                'y': 220,
              },
              {
                'color': '#dc2626',
                'id': 'fox',
                'kind': 'organism',
                'label': 'Fox',
                'radius': 20,
                'x': 220,
                'y': 90,
              },
              {
                'color': '#7c3aed',
                'id': 'hawk',
                'kind': 'organism',
                'label': 'Hawk',
                'radius': 22,
                'x': 400,
                'y': 60,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'name': 'FoodwebEngine',
        'ref': ('BioFoodweb.traits.BioFoodwebSim' satisfies _StdLearningBiologyFoodwebOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'FoodwebPage',
        'path': '/bio/foodweb',
        'traits': [
          {
            'ref': 'FoodwebEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningBiologyFoodwebOrbital. */
export const StdLearningBiologyFoodwebOrbitalManifest = {
  organism: 'learning-biology',
  orbitalName: 'FoodwebOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'FoodwebEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningBiologyFoodwebOrbitalParams keys. */
export function isStdLearningBiologyFoodwebOrbitalParams(p: object): p is StdLearningBiologyFoodwebOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningBiologyFoodwebOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningBiologyFoodwebOrbitalManifest.traitNames,
      ...StdLearningBiologyFoodwebOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the MitosisOrbital orbital.
 *
 * Canonical entity: MitosisScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningBiologyMitosisOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MitosisEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait MitosisOrbital's `uses[]` exports. */
type _StdLearningBiologyMitosisOrbitalUsesRef = 'BioMitosis.traits.BioMitosisSim';

/** Per-orbital factory: builds the MitosisOrbital orbital with consumer params. */
export function stdLearningBiologyMitosisOrbital(params: StdLearningBiologyMitosisOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'MitosisScene';
  const built = makeOrbitalWithUses({
    name: 'MitosisOrbital',
    uses: [
      {
        'as': 'BioMitosis',
        'from': 'std/behaviors/std-bio-mitosis',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'kind': {
                  'name': 'kind',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'cell',
                    'organelle',
                    'molecule',
                    'organism',
                  ],
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'nodes': {
            'default': [
              {
                'color': '#2563eb',
                'id': 'cellA',
                'kind': 'cell',
                'label': 'Cell A',
                'radius': 24,
                'x': 300,
                'y': 200,
              },
              {
                'color': '#7c3aed',
                'id': 'cellB',
                'kind': 'cell',
                'label': 'Cell B',
                'radius': 24,
                'x': 300,
                'y': 200,
              },
            ],
            'type': 'unknown',
          },
          'period': {
            'default': 200,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'separation': {
            'default': 90,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'name': 'MitosisEngine',
        'ref': ('BioMitosis.traits.BioMitosisSim' satisfies _StdLearningBiologyMitosisOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'MitosisPage',
        'path': '/bio/mitosis',
        'traits': [
          {
            'ref': 'MitosisEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningBiologyMitosisOrbital. */
export const StdLearningBiologyMitosisOrbitalManifest = {
  organism: 'learning-biology',
  orbitalName: 'MitosisOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MitosisEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningBiologyMitosisOrbitalParams keys. */
export function isStdLearningBiologyMitosisOrbitalParams(p: object): p is StdLearningBiologyMitosisOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningBiologyMitosisOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningBiologyMitosisOrbitalManifest.traitNames,
      ...StdLearningBiologyMitosisOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the CellStructureOrbital orbital.
 *
 * Canonical entity: CellScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningBiologyCellStructureOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CellEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait CellStructureOrbital's `uses[]` exports. */
type _StdLearningBiologyCellStructureOrbitalUsesRef = 'BioCell.traits.BioCellSim';

/** Per-orbital factory: builds the CellStructureOrbital orbital with consumer params. */
export function stdLearningBiologyCellStructureOrbital(params: StdLearningBiologyCellStructureOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CellScene';
  const built = makeOrbitalWithUses({
    name: 'CellStructureOrbital',
    uses: [
      {
        'as': 'BioCell',
        'from': 'std/behaviors/std-bio-cell',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'kind': {
                  'name': 'kind',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'cell',
                    'organelle',
                    'molecule',
                    'organism',
                  ],
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'edges': {
            'default': [
              {
                'color': '#e2e8f0',
                'from': 'mitochondria',
                'label': '',
                'to': 'nucleus',
              },
              {
                'color': '#e2e8f0',
                'from': 'ribosome',
                'label': '',
                'to': 'nucleus',
              },
              {
                'color': '#e2e8f0',
                'from': 'golgi',
                'label': '',
                'to': 'nucleus',
              },
              {
                'color': '#e2e8f0',
                'from': 'vacuole',
                'label': '',
                'to': 'nucleus',
              },
              {
                'color': '#e2e8f0',
                'from': 'chloroplast',
                'label': '',
                'to': 'nucleus',
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'nodes': {
            'default': [
              {
                'color': '#2563eb',
                'id': 'nucleus',
                'kind': 'cell',
                'label': 'Nucleus',
                'radius': 34,
                'x': 300,
                'y': 200,
              },
              {
                'color': '#dc2626',
                'id': 'mitochondria',
                'kind': 'organelle',
                'label': 'Mitochondria',
                'radius': 14,
                'x': 170,
                'y': 120,
              },
              {
                'color': '#f59e0b',
                'id': 'ribosome',
                'kind': 'organelle',
                'label': 'Ribosome',
                'radius': 14,
                'x': 440,
                'y': 120,
              },
              {
                'color': '#7c3aed',
                'id': 'golgi',
                'kind': 'organelle',
                'label': 'Golgi',
                'radius': 14,
                'x': 150,
                'y': 290,
              },
              {
                'color': '#0891b2',
                'id': 'vacuole',
                'kind': 'organelle',
                'label': 'Vacuole',
                'radius': 14,
                'x': 450,
                'y': 290,
              },
              {
                'color': '#16a34a',
                'id': 'chloroplast',
                'kind': 'organelle',
                'label': 'Chloroplast',
                'radius': 14,
                'x': 300,
                'y': 330,
              },
            ],
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'name': 'CellEngine',
        'ref': ('BioCell.traits.BioCellSim' satisfies _StdLearningBiologyCellStructureOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'CellStructurePage',
        'path': '/bio/cell',
        'traits': [
          {
            'ref': 'CellEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningBiologyCellStructureOrbital. */
export const StdLearningBiologyCellStructureOrbitalManifest = {
  organism: 'learning-biology',
  orbitalName: 'CellStructureOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'CellEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningBiologyCellStructureOrbitalParams keys. */
export function isStdLearningBiologyCellStructureOrbitalParams(p: object): p is StdLearningBiologyCellStructureOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningBiologyCellStructureOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningBiologyCellStructureOrbitalManifest.traitNames,
      ...StdLearningBiologyCellStructureOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-biology — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningBiologyParams {
  PredatorPrey?: StdLearningBiologyPredatorPreyOrbitalParams;
  Foodweb?: StdLearningBiologyFoodwebOrbitalParams;
  Mitosis?: StdLearningBiologyMitosisOrbitalParams;
  CellStructure?: StdLearningBiologyCellStructureOrbitalParams;
}

/** Whole-organism descriptor (4 orbitals). Composes per-orbital factories. */
export function stdLearningBiology(params: StdLearningBiologyParams = {}): OrbitalDefinition[] {
  return [
    stdLearningBiologyPredatorPreyOrbital(params.PredatorPrey ?? {}),
    stdLearningBiologyFoodwebOrbital(params.Foodweb ?? {}),
    stdLearningBiologyMitosisOrbital(params.Mitosis ?? {}),
    stdLearningBiologyCellStructureOrbital(params.CellStructure ?? {}),
  ];
}
