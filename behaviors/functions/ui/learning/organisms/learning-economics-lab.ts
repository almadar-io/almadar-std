/**
 * learning-economics-lab — Economics lab organism — a supply-and-demand market simulator (learner shifts demand/supply curves and sets a price to see surplus or shortage) and a production-possibilities frontier simulator (learner allocates resources between two goods and sees opportunity cost, with an inefficiency demo), composed from the std-econ-market and std-econ-ppf instrument atoms behind a mode router.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-economics-lab.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-economics-lab
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-economics-lab';
const ALIAS = 'LearningEconomicsLab';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningEconomicsLabEventKey = 'INIT' | 'MODE_SELECTED';

/**
 * Payload shape for the `MODE_SELECTED` event.
 */
export interface StdLearningEconomicsLabModeSelectedPayload {
  mode?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningEconomicsLabConfig {
  /** Default: `"market"` */
  mode?: 'market' | 'allocation';
}

type _StdLearningEconomicsLabEntityName = 'EconomicsLabItem' | 'MarketScene' | 'PpfScene';
type _StdLearningEconomicsLabListenTraitName = 'EconomicsLabRouter' | 'MarketSim' | 'PpfSim';

/**
 * Tunable params for the EconomicsLabOrbital orbital.
 *
 * Canonical entity: EconomicsLabItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningEconomicsLabEconomicsLabOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MarketSim' | 'PpfSim' | 'EconomicsLabRouter',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait EconomicsLabOrbital's `uses[]` exports. */
type _StdLearningEconomicsLabEconomicsLabOrbitalUsesRef = 'EconMarket.traits.EconMarketSim' | 'EconPpf.traits.EconPpfSim';

/** Per-orbital factory: builds the EconomicsLabOrbital orbital with consumer params. */
export function stdLearningEconomicsLabEconomicsLabOrbital(params: StdLearningEconomicsLabEconomicsLabOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'EconomicsLabOrbital',
    uses: [
      {
        'as': 'EconMarket',
        'from': 'std/behaviors/std-econ-market',
      },
      {
        'as': 'EconPpf',
        'from': 'std/behaviors/std-econ-ppf',
      },
    ],
    entity: {
      name: 'EconomicsLabItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    auxiliaryEntities: [
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'demandIntercept',
            'type': 'number',
          },
          {
            'name': 'demandSlope',
            'type': 'number',
          },
          {
            'name': 'supplyIntercept',
            'type': 'number',
          },
          {
            'name': 'supplySlope',
            'type': 'number',
          },
          {
            'name': 'price',
            'type': 'number',
          },
          {
            'name': 'equilibriumPrice',
            'type': 'number',
          },
          {
            'name': 'equilibriumQuantity',
            'type': 'number',
          },
          {
            'name': 'qDemanded',
            'type': 'number',
          },
          {
            'name': 'qSupplied',
            'type': 'number',
          },
          {
            'name': 'marketStatusLabel',
            'type': 'string',
          },
          {
            'name': 'marketStatusMagnitude',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': true,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
        ],
        'name': 'MarketScene',
        'persistence': 'runtime',
      },
      {
        'fields': [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': false,
            'name': 'active',
            'type': 'boolean',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'goodALabel',
            'type': 'string',
          },
          {
            'name': 'goodBLabel',
            'type': 'string',
          },
          {
            'name': 'goodAMax',
            'type': 'number',
          },
          {
            'name': 'goodBMax',
            'type': 'number',
          },
          {
            'name': 'allocationShare',
            'type': 'number',
          },
          {
            'name': 'outputA',
            'type': 'number',
          },
          {
            'name': 'outputB',
            'type': 'number',
          },
          {
            'name': 'marginalOppCost',
            'type': 'number',
          },
          {
            'name': 'marginalOppCostLabel',
            'type': 'string',
          },
          {
            'name': 'wasteActive',
            'type': 'boolean',
          },
          {
            'name': 'actualOutputA',
            'type': 'number',
          },
          {
            'name': 'actualOutputB',
            'type': 'number',
          },
          {
            'name': 'allocationStatusLabel',
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': true,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
        ],
        'name': 'PpfScene',
        'persistence': 'runtime',
      },
    ] as EntityRef[],
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'mode': {
            'default': 'market',
            'description': 'Which economics simulation to render: a supply-and-demand market, or a production-possibilities (opportunity-cost) allocation.',
            'label': 'Mode',
            'synonyms': 'economics, supply, demand, supply and demand, market equilibrium, equilibrium price, equilibrium quantity, market price, surplus, shortage, price floor, price ceiling, opportunity cost, PPF, production possibilities frontier, production possibilities, scarcity, trade-off, marginal rate of transformation, resource allocation, guns and butter',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'market',
              'allocation',
            ],
          },
        },
        'emits': [
          {
            'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
            'event': 'MODE_SELECTED',
            'payloadSchema': [
              {
                'name': 'mode',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'EconomicsLabItem',
        'name': 'EconomicsLabRouter',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Broadcast once on mount — tells each composed instrument trait which mode the lab is in; the instrument whose matchMode equals it renders, the others render null.',
              'key': 'MODE_SELECTED',
              'name': 'Mode Selected',
              'payloadSchema': [
                {
                  'name': 'mode',
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'emit',
                  'MODE_SELECTED',
                  {
                    'mode': '@config.mode',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'market',
            'type': 'unknown',
          },
          'title': {
            'default': 'Economics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'MarketScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'EconomicsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'MarketSim',
        'ref': ('EconMarket.traits.EconMarketSim' satisfies _StdLearningEconomicsLabEconomicsLabOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'matchMode': {
            'default': 'allocation',
            'type': 'unknown',
          },
          'title': {
            'default': 'Economics Lab',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'PpfScene',
        'listens': [
          {
            'event': 'MODE_SELECTED',
            'source': {
              'kind': 'trait',
              'trait': 'EconomicsLabRouter',
            },
            'triggers': 'MODE_SELECTED',
          },
        ],
        'name': 'PpfSim',
        'ref': ('EconPpf.traits.EconPpfSim' satisfies _StdLearningEconomicsLabEconomicsLabOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'EconomicsLabPage',
        'path': '/learning/economics-lab',
        'traits': [
          {
            'ref': 'EconomicsLabRouter',
          },
          {
            'ref': 'MarketSim',
          },
          {
            'ref': 'PpfSim',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningEconomicsLabEconomicsLabOrbital. */
export const StdLearningEconomicsLabEconomicsLabOrbitalManifest = {
  organism: 'learning-economics-lab',
  orbitalName: 'EconomicsLabOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MarketSim',
    'PpfSim',
  ] as const,
  inlineTraitNames: [
    'EconomicsLabRouter',
  ] as const,
};

/** Typed guard — runtime validates StdLearningEconomicsLabEconomicsLabOrbitalParams keys. */
export function isStdLearningEconomicsLabEconomicsLabOrbitalParams(p: object): p is StdLearningEconomicsLabEconomicsLabOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningEconomicsLabEconomicsLabOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningEconomicsLabEconomicsLabOrbitalManifest.traitNames,
      ...StdLearningEconomicsLabEconomicsLabOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-economics-lab — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningEconomicsLabParams {
  EconomicsLab?: StdLearningEconomicsLabEconomicsLabOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningEconomicsLab(params: StdLearningEconomicsLabParams = {}): OrbitalDefinition[] {
  return [
    stdLearningEconomicsLabEconomicsLabOrbital(params.EconomicsLab ?? {}),
  ];
}
