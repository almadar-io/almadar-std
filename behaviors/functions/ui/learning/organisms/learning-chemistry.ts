/**
 * learning-chemistry — Chemistry learning app — a fat organism whose orbitals are individual chemistry topics, each composed via `uses` from a per-topic engine atom on the chemistry-canvas.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-chemistry.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-chemistry
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { asEntityId, asPageId } from '@almadar/core/types';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-chemistry';
const ALIAS = 'LearningChemistry';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningChemistryConfig {
  /** Default: `[{"color":"#dc2626","element":"A","id":"a1","radius":10,"x":60,"y":60},{"color":"#dc2626","element":"A","id":"a2","radius":10,"x":100,"y":100},{"color":"#dc2626","element":"A","id":"a3","radius":10,"x":60,"y":140},{"color":"#dc2626","element":"A","id":"a4","radius":10,"x":100,"y":180},{"color":"#dc2626","element":"A","id":"a5","radius":10,"x":60,"y":220},{"color":"#dc2626","element":"A","id":"a6","radius":10,"x":100,"y":260},{"color":"#dc2626","element":"A","id":"a7","radius":10,"x":60,"y":300},{"color":"#dc2626","element":"A","id":"a8","radius":10,"x":100,"y":340},{"color":"#2563eb","element":"B","id":"b1","radius":10,"x":540,"y":60},{"color":"#2563eb","element":"B","id":"b2","radius":10,"x":500,"y":100},{"color":"#2563eb","element":"B","id":"b3","radius":10,"x":540,"y":140},{"color":"#2563eb","element":"B","id":"b4","radius":10,"x":500,"y":180},{"color":"#2563eb","element":"B","id":"b5","radius":10,"x":540,"y":220},{"color":"#2563eb","element":"B","id":"b6","radius":10,"x":500,"y":260},{"color":"#2563eb","element":"B","id":"b7","radius":10,"x":540,"y":300},{"color":"#2563eb","element":"B","id":"b8","radius":10,"x":500,"y":340}]` */
  atoms?: unknown;
  /** Default: `400` */
  height?: unknown;
  /** Default: `true` */
  running?: unknown;
  /** Default: `600` */
  width?: unknown;
}

type _StdLearningChemistryEntityName = 'DiffusionScene' | 'ReactionScene' | 'OsmosisScene';
type _StdLearningChemistryListenTraitName = 'DiffusionEngine' | 'ReactionEngine' | 'OsmosisEngine';

/**
 * Tunable params for the DiffusionOrbital orbital.
 *
 * Canonical entity: DiffusionScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningChemistryDiffusionOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'DiffusionEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait DiffusionOrbital's `uses[]` exports. */
type _StdLearningChemistryDiffusionOrbitalUsesRef = 'ChemDiffusion.traits.ChemDiffusionSim';

/** Per-orbital factory: builds the DiffusionOrbital orbital with consumer params. */
export function stdLearningChemistryDiffusionOrbital(params: StdLearningChemistryDiffusionOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'DiffusionScene';
  const built = makeOrbitalWithUses({
    name: 'DiffusionOrbital',
    uses: [
      {
        'as': 'ChemDiffusion',
        'from': 'std/behaviors/std-chem-diffusion',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'element': {
                  'name': 'element',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'atoms',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'atoms': {
            'default': [
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a1',
                'radius': 10,
                'x': 60,
                'y': 60,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a2',
                'radius': 10,
                'x': 100,
                'y': 100,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a3',
                'radius': 10,
                'x': 60,
                'y': 140,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a4',
                'radius': 10,
                'x': 100,
                'y': 180,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a5',
                'radius': 10,
                'x': 60,
                'y': 220,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a6',
                'radius': 10,
                'x': 100,
                'y': 260,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a7',
                'radius': 10,
                'x': 60,
                'y': 300,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'id': 'a8',
                'radius': 10,
                'x': 100,
                'y': 340,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b1',
                'radius': 10,
                'x': 540,
                'y': 60,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b2',
                'radius': 10,
                'x': 500,
                'y': 100,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b3',
                'radius': 10,
                'x': 540,
                'y': 140,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b4',
                'radius': 10,
                'x': 500,
                'y': 180,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b5',
                'radius': 10,
                'x': 540,
                'y': 220,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b6',
                'radius': 10,
                'x': 500,
                'y': 260,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b7',
                'radius': 10,
                'x': 540,
                'y': 300,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'id': 'b8',
                'radius': 10,
                'x': 500,
                'y': 340,
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'linkedEntityId': asEntityId('ent_01KXG05376C1Z200AHHM67A6S6'),
        'name': 'DiffusionEngine',
        'ref': ('ChemDiffusion.traits.ChemDiffusionSim' satisfies _StdLearningChemistryDiffusionOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'id': asPageId('pag_01KXG05376ZGT0184RV86M8KAY'),
        'name': 'DiffusionPage',
        'path': '/chem/diffusion',
        'traits': [
          {
            'ref': 'DiffusionEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningChemistryDiffusionOrbital. */
export const StdLearningChemistryDiffusionOrbitalManifest = {
  organism: 'learning-chemistry',
  orbitalName: 'DiffusionOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'DiffusionEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningChemistryDiffusionOrbitalParams keys. */
export function isStdLearningChemistryDiffusionOrbitalParams(p: object): p is StdLearningChemistryDiffusionOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningChemistryDiffusionOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningChemistryDiffusionOrbitalManifest.traitNames,
      ...StdLearningChemistryDiffusionOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the ReactionOrbital orbital.
 *
 * Canonical entity: ReactionScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningChemistryReactionOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ReactionEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait ReactionOrbital's `uses[]` exports. */
type _StdLearningChemistryReactionOrbitalUsesRef = 'ChemReaction.traits.ChemReactionSim';

/** Per-orbital factory: builds the ReactionOrbital orbital with consumer params. */
export function stdLearningChemistryReactionOrbital(params: StdLearningChemistryReactionOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'ReactionScene';
  const built = makeOrbitalWithUses({
    name: 'ReactionOrbital',
    uses: [
      {
        'as': 'ChemReaction',
        'from': 'std/behaviors/std-chem-reaction',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'phase',
            'type': 'number',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'element': {
                  'name': 'element',
                  'required': false,
                  'type': 'string',
                },
                'home': {
                  'name': 'home',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'atoms',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'atoms': {
            'default': [
              {
                'color': '#dc2626',
                'element': 'A',
                'home': 150,
                'id': 'a1',
                'radius': 12,
                'x': 150,
                'y': 90,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'home': 450,
                'id': 'b1',
                'radius': 12,
                'x': 450,
                'y': 90,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'home': 150,
                'id': 'a2',
                'radius': 12,
                'x': 150,
                'y': 170,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'home': 450,
                'id': 'b2',
                'radius': 12,
                'x': 450,
                'y': 170,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'home': 150,
                'id': 'a3',
                'radius': 12,
                'x': 150,
                'y': 250,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'home': 450,
                'id': 'b3',
                'radius': 12,
                'x': 450,
                'y': 250,
              },
              {
                'color': '#dc2626',
                'element': 'A',
                'home': 150,
                'id': 'a4',
                'radius': 12,
                'x': 150,
                'y': 330,
              },
              {
                'color': '#2563eb',
                'element': 'B',
                'home': 450,
                'id': 'b4',
                'radius': 12,
                'x': 450,
                'y': 330,
              },
            ],
            'type': 'unknown',
          },
          'backgroundColor': {
            'default': '#f8fafc',
            'type': 'unknown',
          },
          'bonds': {
            'default': [
              {
                'color': '#94a3b8',
                'from': 'a1',
                'to': 'b1',
                'type': 'single',
              },
              {
                'color': '#94a3b8',
                'from': 'a2',
                'to': 'b2',
                'type': 'single',
              },
              {
                'color': '#94a3b8',
                'from': 'a3',
                'to': 'b3',
                'type': 'single',
              },
              {
                'color': '#94a3b8',
                'from': 'a4',
                'to': 'b4',
                'type': 'single',
              },
            ],
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'period': {
            'default': 200,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'linkedEntityId': asEntityId('ent_01KXG05376H82B3F5MFFH84Z6G'),
        'name': 'ReactionEngine',
        'ref': ('ChemReaction.traits.ChemReactionSim' satisfies _StdLearningChemistryReactionOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'id': asPageId('pag_01KXG053766N14BYV6EXSN38V2'),
        'name': 'ReactionPage',
        'path': '/chem/reaction',
        'traits': [
          {
            'ref': 'ReactionEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningChemistryReactionOrbital. */
export const StdLearningChemistryReactionOrbitalManifest = {
  organism: 'learning-chemistry',
  orbitalName: 'ReactionOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'ReactionEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningChemistryReactionOrbitalParams keys. */
export function isStdLearningChemistryReactionOrbitalParams(p: object): p is StdLearningChemistryReactionOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningChemistryReactionOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningChemistryReactionOrbitalManifest.traitNames,
      ...StdLearningChemistryReactionOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the OsmosisOrbital orbital.
 *
 * Canonical entity: OsmosisScene — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningChemistryOsmosisOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'OsmosisEngine',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait OsmosisOrbital's `uses[]` exports. */
type _StdLearningChemistryOsmosisOrbitalUsesRef = 'ChemOsmosis.traits.ChemOsmosisSim';

/** Per-orbital factory: builds the OsmosisOrbital orbital with consumer params. */
export function stdLearningChemistryOsmosisOrbital(params: StdLearningChemistryOsmosisOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'OsmosisScene';
  const built = makeOrbitalWithUses({
    name: 'OsmosisOrbital',
    uses: [
      {
        'as': 'ChemOsmosis',
        'from': 'std/behaviors/std-chem-osmosis',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'element': {
                  'name': 'element',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'atoms',
            'type': 'array',
          },
          {
            'default': false,
            'name': 'running',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'atoms': {
            'default': [
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's1',
                'radius': 13,
                'x': 360,
                'y': 80,
              },
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's2',
                'radius': 13,
                'x': 480,
                'y': 130,
              },
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's3',
                'radius': 13,
                'x': 400,
                'y': 200,
              },
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's4',
                'radius': 13,
                'x': 520,
                'y': 260,
              },
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's5',
                'radius': 13,
                'x': 360,
                'y': 320,
              },
              {
                'color': '#7c3aed',
                'element': 'S',
                'id': 's6',
                'radius': 13,
                'x': 470,
                'y': 360,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w1',
                'radius': 6,
                'x': 60,
                'y': 60,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w2',
                'radius': 6,
                'x': 140,
                'y': 120,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w3',
                'radius': 6,
                'x': 90,
                'y': 200,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w4',
                'radius': 6,
                'x': 200,
                'y': 280,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w5',
                'radius': 6,
                'x': 120,
                'y': 340,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w6',
                'radius': 6,
                'x': 240,
                'y': 80,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w7',
                'radius': 6,
                'x': 260,
                'y': 180,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w8',
                'radius': 6,
                'x': 180,
                'y': 360,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w9',
                'radius': 6,
                'x': 360,
                'y': 120,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w10',
                'radius': 6,
                'x': 440,
                'y': 300,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w11',
                'radius': 6,
                'x': 520,
                'y': 180,
              },
              {
                'color': '#2563eb',
                'element': 'W',
                'id': 'w12',
                'radius': 6,
                'x': 400,
                'y': 60,
              },
            ],
            'type': 'unknown',
          },
          'backgroundColor': {
            'default': '#f8fafc',
            'type': 'unknown',
          },
          'height': {
            'default': 400,
            'type': 'unknown',
          },
          'running': {
            'default': true,
            'type': 'unknown',
          },
          'shapes': {
            'default': [
              {
                'color': '#64748b',
                'id': 'membrane',
                'lineWidth': 4,
                'type': 'line',
                'x1': 300,
                'x2': 300,
                'y1': 20,
                'y2': 380,
              },
            ],
            'type': 'unknown',
          },
          'width': {
            'default': 600,
            'type': 'unknown',
          },
        },
        'linkedEntity': canonicalName,
        'linkedEntityId': asEntityId('ent_01KXG0537697E2FC5YG928PRN3'),
        'name': 'OsmosisEngine',
        'ref': ('ChemOsmosis.traits.ChemOsmosisSim' satisfies _StdLearningChemistryOsmosisOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'id': asPageId('pag_01KXG053769KJG4W72KN6QE047'),
        'name': 'OsmosisPage',
        'path': '/chem/osmosis',
        'traits': [
          {
            'ref': 'OsmosisEngine',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningChemistryOsmosisOrbital. */
export const StdLearningChemistryOsmosisOrbitalManifest = {
  organism: 'learning-chemistry',
  orbitalName: 'OsmosisOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'OsmosisEngine',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdLearningChemistryOsmosisOrbitalParams keys. */
export function isStdLearningChemistryOsmosisOrbitalParams(p: object): p is StdLearningChemistryOsmosisOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningChemistryOsmosisOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningChemistryOsmosisOrbitalManifest.traitNames,
      ...StdLearningChemistryOsmosisOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-chemistry — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningChemistryParams {
  Diffusion?: StdLearningChemistryDiffusionOrbitalParams;
  Reaction?: StdLearningChemistryReactionOrbitalParams;
  Osmosis?: StdLearningChemistryOsmosisOrbitalParams;
}

/** Whole-organism descriptor (3 orbitals). Composes per-orbital factories. */
export function stdLearningChemistry(params: StdLearningChemistryParams = {}): OrbitalDefinition[] {
  return [
    stdLearningChemistryDiffusionOrbital(params.Diffusion ?? {}),
    stdLearningChemistryReactionOrbital(params.Reaction ?? {}),
    stdLearningChemistryOsmosisOrbital(params.Osmosis ?? {}),
  ];
}
