/**
 * learning-charts — Statistical charts organism — shows quantities, trends, proportions, and comparisons as bar, line, pie, area, donut, scatter, or histogram charts from labeled values or multi-series data.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-charts.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-charts
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-charts';
const ALIAS = 'LearningCharts';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningChartsEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningChartsConfig {
  /** Default: `"bar"` */
  chartType?: 'bar' | 'line' | 'pie' | 'area' | 'donut' | 'scatter' | 'histogram';
  /** Default: `[{"label":"Spring","value":24},{"label":"Summer","value":38},{"label":"Fall","value":30},{"label":"Winter","value":18}]` */
  data?: EntityRow[];
  /** Default: `300` */
  height?: number;
  /** Default: `[]` */
  scatterData?: EntityRow[];
  /** Default: `[]` */
  series?: EntityRow[];
  /** Default: `true` */
  showLegend?: boolean;
  /** Default: `false` */
  showValues?: boolean;
  /** Default: `"Chart"` */
  title?: string;
}

type _StdLearningChartsEntityName = 'ChartsItem';
type _StdLearningChartsListenTraitName = 'ChartsRender';

/**
 * Tunable params for the ChartsOrbital orbital.
 *
 * Canonical entity: ChartsItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningChartsChartsOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ChartsRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait ChartsOrbital's `uses[]` exports. */
type _StdLearningChartsChartsOrbitalUsesRef = never;

/** Per-orbital factory: builds the ChartsOrbital orbital with consumer params. */
export function stdLearningChartsChartsOrbital(params: StdLearningChartsChartsOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'ChartsOrbital',
    uses: [],
    entity: {
      name: 'ChartsItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'title',
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'chartType': {
            'default': 'bar',
            'description': 'What kind of statistical chart to render.',
            'label': 'Chart Type',
            'synonyms': 'bar chart, line chart, pie chart, area chart, donut chart, scatter plot, trend over time, comparison of quantities, proportions, percentages, magnitudes, data series',
            'tier': 'domain',
            'type': 'string',
            'values': [
              'bar',
              'line',
              'pie',
              'area',
              'donut',
              'scatter',
              'histogram',
            ],
          },
          'data': {
            'default': [
              {
                'label': 'Spring',
                'value': 24,
              },
              {
                'label': 'Summer',
                'value': 38,
              },
              {
                'label': 'Fall',
                'value': 30,
              },
              {
                'label': 'Winter',
                'value': 18,
              },
            ],
            'description': 'Labeled values for a single-series chart.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
                'value': {
                  'name': 'value',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Data',
            'tier': 'domain',
            'type': '[ChartsDataItem]',
          },
          'height': {
            'default': 300,
            'description': 'Chart height in pixels.',
            'label': 'Height',
            'tier': 'presentation',
            'type': 'number',
          },
          'scatterData': {
            'default': [],
            'description': 'x/y points for scatter charts.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'size': {
                  'name': 'size',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Scatter Data',
            'tier': 'domain',
            'type': '[ChartsScatterItem]',
          },
          'series': {
            'default': [],
            'description': 'Multi-series line/area/bar data.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'data': {
                  'items': {
                    'properties': {
                      'color': {
                        'name': 'color',
                        'required': false,
                        'type': 'string',
                      },
                      'label': {
                        'name': 'label',
                        'required': true,
                        'type': 'string',
                      },
                      'value': {
                        'name': 'value',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'data',
                  'required': true,
                  'type': 'array',
                },
                'name': {
                  'name': 'name',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Series',
            'tier': 'domain',
            'type': '[ChartsSeriesItem]',
          },
          'showLegend': {
            'default': true,
            'description': 'Show the chart legend.',
            'label': 'Show Legend',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'showValues': {
            'default': false,
            'description': 'Show values on the chart.',
            'label': 'Show Values',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'title': {
            'default': 'Chart',
            'description': 'Title shown above the chart.',
            'label': 'Title',
            'tier': 'presentation',
            'type': 'string',
          },
        },
        'entityContract': {
          'provides': [
            'title',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'ChartsItem',
        'name': 'ChartsRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.title',
                  '@config.title',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'children': [
                          {
                            'content': '@entity.title',
                            'type': 'typography',
                            'variant': 'h4',
                          },
                          {
                            'chartType': '@config.chartType',
                            'data': '@config.data',
                            'height': '@config.height',
                            'scatterData': '@config.scatterData',
                            'series': '@config.series',
                            'showLegend': '@config.showLegend',
                            'showValues': '@config.showValues',
                            'type': 'chart',
                          },
                        ],
                        'direction': 'vertical',
                        'gap': 'md',
                        'type': 'stack',
                      },
                    ],
                    'padding': 'md',
                    'type': 'box',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'ChartsPage',
        'path': '/learning/charts',
        'traits': [
          {
            'ref': 'ChartsRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningChartsChartsOrbital. */
export const StdLearningChartsChartsOrbitalManifest = {
  organism: 'learning-charts',
  orbitalName: 'ChartsOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'ChartsRender',
  ] as const,
};

/** Typed guard — runtime validates StdLearningChartsChartsOrbitalParams keys. */
export function isStdLearningChartsChartsOrbitalParams(p: object): p is StdLearningChartsChartsOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningChartsChartsOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningChartsChartsOrbitalManifest.traitNames,
      ...StdLearningChartsChartsOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-charts — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningChartsParams {
  Charts?: StdLearningChartsChartsOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningCharts(params: StdLearningChartsParams = {}): OrbitalDefinition[] {
  return [
    stdLearningChartsChartsOrbital(params.Charts ?? {}),
  ];
}
