/**
 * learning-coordinate-plane — Coordinate plane organism — a math-canvas bound to entity curves and points.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-coordinate-plane.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-coordinate-plane
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-coordinate-plane';
const ALIAS = 'LearningCoordinatePlane';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningCoordinatePlaneEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningCoordinatePlaneConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `[{"color":"#2563eb","label":"y = x²","samples":[{"x":-3,"y":9},{"x":-2,"y":4},{"x":-1,"y":1},{"x":0,"y":0},{"x":1,"y":1},{"x":2,"y":4},{"x":3,"y":9}]}]` */
  curves?: EntityRow[];
  /** Default: `1` */
  gridStep?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `[{"color":"#dc2626","label":"A","radius":4,"x":-2,"y":4},{"color":"#16a34a","label":"B","radius":4,"x":1,"y":1}]` */
  points?: EntityRow[];
  /** Default: `true` */
  showAxes?: boolean;
  /** Default: `true` */
  showGrid?: boolean;
  /** Default: `"Coordinate Plane"` */
  title?: string;
  /** Default: `600` */
  width?: number;
  /** Default: `5` */
  xMax?: number;
  /** Default: `-5` */
  xMin?: number;
  /** Default: `10` */
  yMax?: number;
  /** Default: `-2` */
  yMin?: number;
}

/**
 * Tunable params for the CoordinatePlaneOrbital orbital.
 *
 * Canonical entity: CoordinatePlaneItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CoordinatePlaneRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the CoordinatePlaneOrbital orbital with consumer params. */
export function stdLearningCoordinatePlaneCoordinatePlaneOrbital(params: StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CoordinatePlaneItem';
  const built = makeOrbitalWithUses({
    name: 'CoordinatePlaneOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'color': {
                        'name': 'color',
                        'required': false,
                        'type': 'string',
                      },
                      'label': {
                        'name': 'label',
                        'required': false,
                        'type': 'string',
                      },
                      'radius': {
                        'name': 'radius',
                        'required': false,
                        'type': 'number',
                      },
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'curves',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'points',
            'type': 'array',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'animate': {
            'default': false,
            'description': 'Enable continuous redraw loop.',
            'label': 'Animate',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'curves': {
            'default': [
              {
                'color': '#2563eb',
                'label': 'y = x²',
                'samples': [
                  {
                    'x': -3,
                    'y': 9,
                  },
                  {
                    'x': -2,
                    'y': 4,
                  },
                  {
                    'x': -1,
                    'y': 1,
                  },
                  {
                    'x': 0,
                    'y': 0,
                  },
                  {
                    'x': 1,
                    'y': 1,
                  },
                  {
                    'x': 2,
                    'y': 4,
                  },
                  {
                    'x': 3,
                    'y': 9,
                  },
                ],
              },
            ],
            'description': 'Sampled curves to plot.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'samples': {
                  'items': {
                    'properties': {
                      'color': {
                        'name': 'color',
                        'required': false,
                        'type': 'string',
                      },
                      'label': {
                        'name': 'label',
                        'required': false,
                        'type': 'string',
                      },
                      'radius': {
                        'name': 'radius',
                        'required': false,
                        'type': 'number',
                      },
                      'x': {
                        'name': 'x',
                        'required': true,
                        'type': 'number',
                      },
                      'y': {
                        'name': 'y',
                        'required': true,
                        'type': 'number',
                      },
                    },
                    'type': 'object',
                  },
                  'name': 'samples',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'label': 'Curves',
            'tier': 'domain',
            'type': '[CoordinatePlaneCurve]',
          },
          'gridStep': {
            'default': 1,
            'label': 'Grid Step',
            'tier': 'presentation',
            'type': 'number',
          },
          'height': {
            'default': 400,
            'description': 'Canvas height in pixels.',
            'label': 'Height',
            'tier': 'presentation',
            'type': 'number',
          },
          'interactive': {
            'default': false,
            'description': 'Enable pointer interaction on the canvas.',
            'label': 'Interactive',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'points': {
            'default': [
              {
                'color': '#dc2626',
                'label': 'A',
                'radius': 4,
                'x': -2,
                'y': 4,
              },
              {
                'color': '#16a34a',
                'label': 'B',
                'radius': 4,
                'x': 1,
                'y': 1,
              },
            ],
            'description': 'Scatter points to highlight.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Points',
            'tier': 'domain',
            'type': '[CoordinatePlanePoint]',
          },
          'showAxes': {
            'default': true,
            'label': 'Show Axes',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'showGrid': {
            'default': true,
            'label': 'Show Grid',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'title': {
            'default': 'Coordinate Plane',
            'description': 'Title shown above the plane.',
            'label': 'Title',
            'tier': 'domain',
            'type': 'string',
          },
          'width': {
            'default': 600,
            'description': 'Canvas width in pixels.',
            'label': 'Width',
            'tier': 'presentation',
            'type': 'number',
          },
          'xMax': {
            'default': 5,
            'description': 'Maximum x value.',
            'label': 'X Max',
            'tier': 'domain',
            'type': 'number',
          },
          'xMin': {
            'default': -5,
            'description': 'Minimum x value.',
            'label': 'X Min',
            'tier': 'domain',
            'type': 'number',
          },
          'yMax': {
            'default': 10,
            'description': 'Maximum y value.',
            'label': 'Y Max',
            'tier': 'domain',
            'type': 'number',
          },
          'yMin': {
            'default': -2,
            'description': 'Minimum y value.',
            'label': 'Y Min',
            'tier': 'domain',
            'type': 'number',
          },
        },
        'entityContract': {
          'provides': [
            'curves',
            'points',
            'title',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'CoordinatePlaneItem',
        'name': 'CoordinatePlaneRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.title',
                  '@config.title',
                ],
                [
                  'set',
                  '@entity.curves',
                  '@config.curves',
                ],
                [
                  'set',
                  '@entity.points',
                  '@config.points',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'content': '@entity.title',
                        'type': 'typography',
                        'variant': 'h4',
                      },
                      {
                        'animate': '@config.animate',
                        'curves': '@entity.curves',
                        'gridStep': '@config.gridStep',
                        'height': '@config.height',
                        'interactive': '@config.interactive',
                        'points': '@entity.points',
                        'showAxes': '@config.showAxes',
                        'showGrid': '@config.showGrid',
                        'type': 'math-canvas',
                        'width': '@config.width',
                        'xMax': '@config.xMax',
                        'xMin': '@config.xMin',
                        'yMax': '@config.yMax',
                        'yMin': '@config.yMin',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'padding': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } as never, 'CoordinatePlaneItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'CoordinatePlanePage',
        'path': '/learning/coordinate-plane',
        'traits': [
          {
            'ref': 'CoordinatePlaneRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningCoordinatePlaneCoordinatePlaneOrbital. */
export const StdLearningCoordinatePlaneCoordinatePlaneOrbitalManifest = {
  organism: 'learning-coordinate-plane',
  orbitalName: 'CoordinatePlaneOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'CoordinatePlaneRender',
  ] as const,
};

/** Typed guard — runtime validates StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams keys. */
export function isStdLearningCoordinatePlaneCoordinatePlaneOrbitalParams(p: object): p is StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningCoordinatePlaneCoordinatePlaneOrbitalManifest.traitNames,
      ...StdLearningCoordinatePlaneCoordinatePlaneOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-coordinate-plane — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningCoordinatePlaneParams {
  CoordinatePlane?: StdLearningCoordinatePlaneCoordinatePlaneOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningCoordinatePlane(params: StdLearningCoordinatePlaneParams = {}): OrbitalDefinition[] {
  return [
    stdLearningCoordinatePlaneCoordinatePlaneOrbital(params.CoordinatePlane ?? {}),
  ];
}
