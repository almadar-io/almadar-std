/**
 * learning-field-canvas — Field canvas organism — a physics-canvas bound to entity bodies, constraints, and forces.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/learning-field-canvas.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=learning-field-canvas
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/learning-field-canvas';
const ALIAS = 'LearningFieldCanvas';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearningFieldCanvasEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearningFieldCanvasConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#2563eb","id":"b1","label":"A","radius":14,"vx":2,"vy":-1,"x":120,"y":200},{"color":"#dc2626","id":"b2","label":"B","radius":18,"vx":-1,"vy":1.5,"x":300,"y":150},{"color":"#16a34a","fx":-0.5,"fy":0,"id":"b3","label":"C","radius":12,"vx":0,"vy":0,"x":480,"y":220}]` */
  bodies?: EntityRow[];
  /** Default: `[]` */
  constraints?: EntityRow[];
  /** Default: `30` */
  forceScale?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `true` */
  showForces?: boolean;
  /** Default: `true` */
  showVelocity?: boolean;
  /** Default: `"Field Canvas"` */
  title?: string;
  /** Default: `25` */
  velocityScale?: number;
  /** Default: `600` */
  width?: number;
}

/**
 * Tunable params for the FieldCanvasOrbital orbital.
 *
 * Canonical entity: FieldCanvasItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLearningFieldCanvasFieldCanvasOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'FieldCanvasRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the FieldCanvasOrbital orbital with consumer params. */
export function stdLearningFieldCanvasFieldCanvasOrbital(params: StdLearningFieldCanvasFieldCanvasOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'FieldCanvasItem';
  const built = makeOrbitalWithUses({
    name: 'FieldCanvasOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'title',
            'type': 'string',
          },
          {
            'name': 'backgroundColor',
            'type': 'string',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'fx': {
                  'name': 'fx',
                  'required': false,
                  'type': 'number',
                },
                'fy': {
                  'name': 'fy',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'vx': {
                  'name': 'vx',
                  'required': false,
                  'type': 'number',
                },
                'vy': {
                  'name': 'vy',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'name': 'bodies',
            'type': 'array',
          },
          {
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'from': {
                  'name': 'from',
                  'required': true,
                  'type': 'string',
                },
                'to': {
                  'name': 'to',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'name': 'constraints',
            'type': 'array',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'animate': {
            'default': false,
            'description': 'Enable continuous redraw loop.',
            'label': 'Animate',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'backgroundColor': {
            'default': '#f8fafc',
            'label': 'Background Color',
            'tier': 'presentation',
            'type': 'string',
          },
          'bodies': {
            'default': [
              {
                'color': '#2563eb',
                'id': 'b1',
                'label': 'A',
                'radius': 14,
                'vx': 2,
                'vy': -1,
                'x': 120,
                'y': 200,
              },
              {
                'color': '#dc2626',
                'id': 'b2',
                'label': 'B',
                'radius': 18,
                'vx': -1,
                'vy': 1.5,
                'x': 300,
                'y': 150,
              },
              {
                'color': '#16a34a',
                'fx': -0.5,
                'fy': 0,
                'id': 'b3',
                'label': 'C',
                'radius': 12,
                'vx': 0,
                'vy': 0,
                'x': 480,
                'y': 220,
              },
            ],
            'description': 'Physics bodies to render.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'fx': {
                  'name': 'fx',
                  'required': false,
                  'type': 'number',
                },
                'fy': {
                  'name': 'fy',
                  'required': false,
                  'type': 'number',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': false,
                  'type': 'string',
                },
                'radius': {
                  'name': 'radius',
                  'required': false,
                  'type': 'number',
                },
                'vx': {
                  'name': 'vx',
                  'required': false,
                  'type': 'number',
                },
                'vy': {
                  'name': 'vy',
                  'required': false,
                  'type': 'number',
                },
                'x': {
                  'name': 'x',
                  'required': true,
                  'type': 'number',
                },
                'y': {
                  'name': 'y',
                  'required': true,
                  'type': 'number',
                },
              },
              'type': 'object',
            },
            'label': 'Bodies',
            'tier': 'domain',
            'type': '[FieldCanvasBody]',
          },
          'constraints': {
            'default': [],
            'description': 'Constraints linking bodies.',
            'items': {
              'properties': {
                'color': {
                  'name': 'color',
                  'required': false,
                  'type': 'string',
                },
                'from': {
                  'name': 'from',
                  'required': true,
                  'type': 'string',
                },
                'to': {
                  'name': 'to',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Constraints',
            'tier': 'domain',
            'type': '[FieldCanvasConstraint]',
          },
          'forceScale': {
            'default': 30,
            'label': 'Force Scale',
            'tier': 'presentation',
            'type': 'number',
          },
          'height': {
            'default': 400,
            'label': 'Height',
            'tier': 'presentation',
            'type': 'number',
          },
          'interactive': {
            'default': false,
            'description': 'Enable pointer interaction on the canvas.',
            'label': 'Interactive',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'showForces': {
            'default': true,
            'description': 'Render force arrows for each body.',
            'label': 'Show Forces',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'showVelocity': {
            'default': true,
            'description': 'Render velocity arrows for each body.',
            'label': 'Show Velocity',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'title': {
            'default': 'Field Canvas',
            'description': 'Title shown above the field.',
            'label': 'Title',
            'tier': 'domain',
            'type': 'string',
          },
          'velocityScale': {
            'default': 25,
            'label': 'Velocity Scale',
            'tier': 'presentation',
            'type': 'number',
          },
          'width': {
            'default': 600,
            'label': 'Width',
            'tier': 'presentation',
            'type': 'number',
          },
        },
        'entityContract': {
          'provides': [
            'backgroundColor',
            'bodies',
            'constraints',
            'title',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'FieldCanvasItem',
        'name': 'FieldCanvasRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.title',
                  '@config.title',
                ],
                [
                  'set',
                  '@entity.backgroundColor',
                  '@config.backgroundColor',
                ],
                [
                  'set',
                  '@entity.bodies',
                  '@config.bodies',
                ],
                [
                  'set',
                  '@entity.constraints',
                  '@config.constraints',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'content': '@entity.title',
                        'type': 'typography',
                        'variant': 'h4',
                      },
                      {
                        'animate': '@config.animate',
                        'backgroundColor': '@entity.backgroundColor',
                        'bodies': '@entity.bodies',
                        'constraints': '@entity.constraints',
                        'forceScale': '@config.forceScale',
                        'height': '@config.height',
                        'interactive': '@config.interactive',
                        'showForces': '@config.showForces',
                        'showVelocity': '@config.showVelocity',
                        'type': 'physics-canvas',
                        'velocityScale': '@config.velocityScale',
                        'width': '@config.width',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'padding': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } as never, 'FieldCanvasItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'FieldCanvasPage',
        'path': '/learning/field-canvas',
        'traits': [
          {
            'ref': 'FieldCanvasRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLearningFieldCanvasFieldCanvasOrbital. */
export const StdLearningFieldCanvasFieldCanvasOrbitalManifest = {
  organism: 'learning-field-canvas',
  orbitalName: 'FieldCanvasOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'FieldCanvasRender',
  ] as const,
};

/** Typed guard — runtime validates StdLearningFieldCanvasFieldCanvasOrbitalParams keys. */
export function isStdLearningFieldCanvasFieldCanvasOrbitalParams(p: object): p is StdLearningFieldCanvasFieldCanvasOrbitalParams {
  type _OverrideRecord = NonNullable<StdLearningFieldCanvasFieldCanvasOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLearningFieldCanvasFieldCanvasOrbitalManifest.traitNames,
      ...StdLearningFieldCanvasFieldCanvasOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for learning-field-canvas — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLearningFieldCanvasParams {
  FieldCanvas?: StdLearningFieldCanvasFieldCanvasOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdLearningFieldCanvas(params: StdLearningFieldCanvasParams = {}): OrbitalDefinition[] {
  return [
    stdLearningFieldCanvasFieldCanvasOrbital(params.FieldCanvas ?? {}),
  ];
}
