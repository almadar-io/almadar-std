/**
 * std-geo-urban — Urban land-use model engine — Burgess concentric, Hoyt sector, and Harris-Ullman multiple-nuclei overlays toggle on a config-declared city map; the learner cycles each zone's land-use tag and checks how many zones match the selected model's prediction, on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geo-urban.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geo-urban
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geo-urban';
const ALIAS = 'GeoUrban';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeoUrbanEventKey = 'CHECK' | 'INIT' | 'MODE_SELECTED' | 'TAG_ZONE' | 'TOGGLE_MODEL' | 'URBAN_UPDATED';

/**
 * Payload shape for the `URBAN_UPDATED` event.
 */
export interface StdGeoUrbanUrbanUpdatedPayload {
  selectedModel?: string;
  score?: number;
  attempts?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeoUrbanConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `[{"color":"#38bdf8","lineWidth":6,"opacity":0.35,"type":"line","x1":0,"x2":640,"y1":295,"y2":335},{"color":"#475569","lineWidth":3,"opacity":0.6,"type":"line","x1":60,"x2":580,"y1":220,"y2":220},{"color":"#334155","fill":"#334155","height":30,"opacity":0.3,"type":"rect","width":30,"x":305,"y":205}]` */
  cityLayout?: EntityRow[];
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"code":"burgess","color":"#8b5cf6","expectations":[{"tag":"commerce","zoneId":"z1"},{"tag":"industry","zoneId":"z2"},{"tag":"industry","zoneId":"z3"},{"tag":"industry","zoneId":"z4"},{"tag":"residential","zoneId":"z5"}],"label":"Burgess Concentric","overlays":[{"lineWidth":2,"radius":70,"type":"circle","x":320,"y":220},{"lineWidth":2,"radius":140,"type":"circle","x":320,"y":220},{"lineWidth":2,"radius":210,"type":"circle","x":320,"y":220}]},{"code":"hoyt","color":"#0ea5e9","expectations":[{"tag":"commerce","zoneId":"z1"},{"tag":"residential","zoneId":"z2"},{"tag":"industry","zoneId":"z3"},{"tag":"industry","zoneId":"z4"},{"tag":"residential","zoneId":"z5"}],"label":"Hoyt Sector","overlays":[{"lineWidth":2,"points":[{"x":320,"y":220},{"x":640,"y":170},{"x":640,"y":270}],"type":"polygon"},{"lineWidth":2,"points":[{"x":320,"y":220},{"x":0,"y":170},{"x":0,"y":270}],"type":"polygon"}]},{"code":"harris","color":"#e11d48","expectations":[{"tag":"commerce","zoneId":"z1"},{"tag":"residential","zoneId":"z2"},{"tag":"commerce","zoneId":"z3"},{"tag":"industry","zoneId":"z4"},{"tag":"residential","zoneId":"z5"}],"label":"Harris-Ullman Nuclei","overlays":[{"lineWidth":2,"radius":50,"type":"circle","x":320,"y":220},{"lineWidth":2,"radius":55,"type":"circle","x":460,"y":220},{"lineWidth":2,"radius":55,"type":"circle","x":180,"y":220}]}]` */
  models?: EntityRow[];
  /** Default: `[{"color":"#f59e0b","label":"Commerce","value":"commerce"},{"color":"#64748b","label":"Industry","value":"industry"},{"color":"#22c55e","label":"Residential","value":"residential"}]` */
  tags?: EntityRow[];
  /** Default: `"#1e293b"` */
  textColor?: string;
  /** Default: `"Urban Land-Use Models"` */
  title?: string;
  /** Default: `640` */
  width?: number;
  /** Default: `[{"id":"z1","label":"Central","radius":32,"x":320,"y":220},{"id":"z2","label":"North","radius":30,"x":320,"y":110},{"id":"z3","label":"East","radius":30,"x":470,"y":220},{"id":"z4","label":"West","radius":30,"x":170,"y":220},{"id":"z5","label":"South","radius":30,"x":320,"y":340}]` */
  zones?: EntityRow[];
}

/**
 * Params for the std-geo-urban descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeoUrbanParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeoUrbanEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeoUrbanConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GeoUrban.traits.GeoUrbanSim`. */
export function stdGeoUrbanTrait(params: StdGeoUrbanParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoUrbanSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GeoUrban.pages.GeoUrbanPage`. */
export function stdGeoUrbanPage(params: StdGeoUrbanParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoUrbanPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeoUrban(params: StdGeoUrbanParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoUrbanOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeoUrbanTrait(params),
    ],
    pages: [
      stdGeoUrbanPage(params),
    ],
  });
}
