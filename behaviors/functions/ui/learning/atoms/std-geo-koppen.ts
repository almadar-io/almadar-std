/**
 * std-geo-koppen — Simplified Köppen climate classifier engine — the learner sets a summer/winter temperature and average-monthly-precipitation profile and a 12-month climograph classifies the climate by first-match-wins rule, on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geo-koppen.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geo-koppen
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geo-koppen';
const ALIAS = 'GeoKoppen';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeoKoppenEventKey = 'INIT' | 'KOPPEN_UPDATED' | 'MODE_SELECTED' | 'SET_PRECIP' | 'SET_TEMP_SUMMER' | 'SET_TEMP_WINTER';

/**
 * Payload shape for the `KOPPEN_UPDATED` event.
 */
export interface StdGeoKoppenKoppenUpdatedPayload {
  climateClassCode?: string;
  climateClassName?: string;
  coldestMonthTemp?: number;
  warmestMonthTemp?: number;
  annualPrecipTotal?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeoKoppenConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#94a3b8"` */
  axisColor?: string;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"code":"B","comparator":"lt","metric":"annualPrecip","name":"Arid","ruleText":"annual precipitation < 500 mm","threshold":500},{"code":"A","comparator":"gte","metric":"coldestMonthTemp","name":"Tropical","ruleText":"coldest month >= 18°C","threshold":18},{"code":"E","comparator":"lt","metric":"warmestMonthTemp","name":"Polar","ruleText":"warmest month < 10°C","threshold":10},{"code":"D","comparator":"lt","metric":"coldestMonthTemp","name":"Continental","ruleText":"coldest month < -3°C","threshold":-3},{"code":"C","comparator":"gte","metric":"coldestMonthTemp","name":"Temperate","ruleText":"coldest month >= -3°C (mild-winter, sufficient precipitation, warm-enough summer)","threshold":-3}]` */
  climateClasses?: EntityRow[];
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"id":"jan","label":"Jan","order":0,"precipWeight":0.6,"tempWeight":0.05},{"id":"feb","label":"Feb","order":1,"precipWeight":0.6,"tempWeight":0.05},{"id":"mar","label":"Mar","order":2,"precipWeight":0.7,"tempWeight":0.25},{"id":"apr","label":"Apr","order":3,"precipWeight":0.8,"tempWeight":0.45},{"id":"may","label":"May","order":4,"precipWeight":1,"tempWeight":0.7},{"id":"jun","label":"Jun","order":5,"precipWeight":1.3,"tempWeight":0.9},{"id":"jul","label":"Jul","order":6,"precipWeight":1.5,"tempWeight":1},{"id":"aug","label":"Aug","order":7,"precipWeight":1.4,"tempWeight":0.95},{"id":"sep","label":"Sep","order":8,"precipWeight":1.1,"tempWeight":0.75},{"id":"oct","label":"Oct","order":9,"precipWeight":0.8,"tempWeight":0.5},{"id":"nov","label":"Nov","order":10,"precipWeight":0.6,"tempWeight":0.25},{"id":"dec","label":"Dec","order":11,"precipWeight":0.6,"tempWeight":0.1}]` */
  months?: EntityRow[];
  /** Default: `80` */
  precip?: number;
  /** Default: `400` */
  precipAxisMax?: number;
  /** Default: `"#38bdf8"` */
  precipColor?: string;
  /** Default: `40` */
  tempAxisMax?: number;
  /** Default: `-30` */
  tempAxisMin?: number;
  /** Default: `"#f97316"` */
  tempCurveColor?: string;
  /** Default: `24` */
  tempSummer?: number;
  /** Default: `4` */
  tempWinter?: number;
  /** Default: `"#1e293b"` */
  textColor?: string;
  /** Default: `"Geography Lab"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-geo-koppen descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeoKoppenParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeoKoppenEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeoKoppenConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GeoKoppen.traits.GeoKoppenSim`. */
export function stdGeoKoppenTrait(params: StdGeoKoppenParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoKoppenSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GeoKoppen.pages.GeoKoppenPage`. */
export function stdGeoKoppenPage(params: StdGeoKoppenParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoKoppenPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeoKoppen(params: StdGeoKoppenParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoKoppenOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeoKoppenTrait(params),
    ],
    pages: [
      stdGeoKoppenPage(params),
    ],
  });
}
