/**
 * std-logic-quantifier — Predicate-logic model evaluator — a finite domain of objects with visible properties; the learner marks which ∀/∃ statements they believe true, evaluates them against the model, and can toggle object properties to flip a statement's truth. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-logic-quantifier.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-logic-quantifier
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-logic-quantifier';
const ALIAS = 'LogicQuantifier';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLogicQuantifierEventKey = 'EVALUATE_STATEMENT' | 'INIT' | 'MODE_SELECTED' | 'PICK_TRUE_STATEMENTS' | 'QUANTIFIER_UPDATED' | 'TOGGLE_OBJECT';

/**
 * Payload shape for the `QUANTIFIER_UPDATED` event.
 */
export interface StdLogicQuantifierQuantifierUpdatedPayload {
  picks?: string[];
  feedback?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLogicQuantifierConfig {
  /** Default: `true` */
  allowEdit?: boolean;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"id":"a","label":"a","predicates":["G","S"],"x":140,"y":150},{"id":"b","label":"b","predicates":["G"],"x":300,"y":150},{"id":"c","label":"c","predicates":[],"x":460,"y":150}]` */
  domain?: EntityRow[];
  /** Default: `260` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"color":"#15803d","key":"G","label":"G"},{"color":"#7c3aed","key":"S","label":"S"}]` */
  predicates?: EntityRow[];
  /** Default: `true` */
  showTruthValues?: boolean;
  /** Default: `[{"id":"s1","kind":"FORALL","label":"∀x G(x)","px":"G"},{"id":"s2","kind":"EXISTS","label":"∃x S(x)","px":"S"},{"id":"s3","kind":"FORALL_EXISTS","label":"∀x(G(x) → ∃y S(y))","px":"G","py":"S"}]` */
  statements?: EntityRow[];
  /** Default: `"Quantifier Model"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-logic-quantifier descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLogicQuantifierParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLogicQuantifierEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLogicQuantifierConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LogicQuantifier.traits.LogicQuantifierSim`. */
export function stdLogicQuantifierTrait(params: StdLogicQuantifierParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LogicQuantifierSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LogicQuantifier.pages.LogicQuantifierPage`. */
export function stdLogicQuantifierPage(params: StdLogicQuantifierParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LogicQuantifierPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLogicQuantifier(params: StdLogicQuantifierParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LogicQuantifierOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLogicQuantifierTrait(params),
    ],
    pages: [
      stdLogicQuantifierPage(params),
    ],
  });
}
