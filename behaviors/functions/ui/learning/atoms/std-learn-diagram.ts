/**
 * std-learn-diagram — Generic annotated-diagram instrument. Contract: a config-declared roster of labeled parts (id, label, description, x, y, radius, color) plus optional connector links (from, to, color, label); the whole diagram renders as circles connected by lines with always-visible part labels on a biology-canvas; the learner selects a part via its choice-button and the selected part is visually emphasized with a highlight ring and enlarged radius while its description renders in a caption card alongside a "part k of n selected" readout; a Clear button returns to the no-selection overview state where the caption card shows a config-declared overview text. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-diagram.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-diagram
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-diagram';
const ALIAS = 'LearnDiagram';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnDiagramEventKey = 'CLEAR_PART' | 'INIT' | 'MODE_SELECTED' | 'PART_SELECTED' | 'SELECTION_CLEARED' | 'SELECT_PART';

/**
 * Payload shape for the `PART_SELECTED` event.
 */
export interface StdLearnDiagramPartSelectedPayload {
  partId?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnDiagramConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `"Clear"` */
  clearLabel?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `"#f59e0b"` */
  highlightColor?: string;
  /** Default: `[{"color":"#cbd5e1","from":"input","label":"","to":"processor"},{"color":"#cbd5e1","from":"processor","label":"","to":"storage"},{"color":"#cbd5e1","from":"processor","label":"","to":"output"},{"color":"#cbd5e1","from":"controller","label":"","to":"processor"}]` */
  links?: EntityRow[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Select a part below to learn what it does."` */
  overviewText?: string;
  /** Default: `[{"color":"#2563eb","description":"Receives raw data or requests from the outside world and hands them to the processor.","id":"input","label":"Input","radius":34,"x":120,"y":200},{"color":"#7c3aed","description":"The central component that transforms incoming data according to the system's rules.","id":"processor","label":"Processor","radius":46,"x":300,"y":200},{"color":"#0891b2","description":"Persists data so it can be retrieved later, even after the system restarts.","id":"storage","label":"Storage","radius":34,"x":300,"y":320},{"color":"#16a34a","description":"Delivers the processor's results back out to users or other systems.","id":"output","label":"Output","radius":34,"x":480,"y":200},{"color":"#dc2626","description":"Coordinates timing and configuration for the processor without touching the data itself.","id":"controller","label":"Controller","radius":26,"x":300,"y":80}]` */
  parts?: EntityRow[];
  /** Default: `"System Diagram"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-learn-diagram descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnDiagramParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnDiagramEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnDiagramConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnDiagram.traits.LearnDiagramSim`. */
export function stdLearnDiagramTrait(params: StdLearnDiagramParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LearnDiagramSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnDiagram.pages.LearnDiagramPage`. */
export function stdLearnDiagramPage(params: StdLearnDiagramParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnDiagramPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnDiagram(params: StdLearnDiagramParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnDiagramOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnDiagramTrait(params),
    ],
    pages: [
      stdLearnDiagramPage(params),
    ],
  });
}
