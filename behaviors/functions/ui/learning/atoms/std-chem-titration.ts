/**
 * std-chem-titration — Acid-base titration engine — the learner adds titrant in large or small increments and watches the pH curve and indicator color evolve toward (and past) the equivalence point, on a chemistry-canvas plus a pH line-chart. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-chem-titration.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-chem-titration
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-chem-titration';
const ALIAS = 'ChemTitration';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdChemTitrationEventKey = 'ADD_TITRANT_LARGE' | 'ADD_TITRANT_SMALL' | 'INIT' | 'MODE_SELECTED' | 'RESET_TITRATION' | 'TITRATION_UPDATED';

/**
 * Payload shape for the `TITRATION_UPDATED` event.
 */
export interface StdChemTitrationTitrationUpdatedPayload {
  titrantVolumeAdded?: number;
  ph?: number;
  overshoot?: boolean;
  equivalenceVolume?: number;
  statusText?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdChemTitrationConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#fafafa"` */
  backgroundColor?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Chemistry Lab"` */
  title?: string;
  /** Default: `0.1` */
  titrationAcidConcentration?: number;
  /** Default: `0.000018` */
  titrationAcidKa?: number;
  /** Default: `"strong"` */
  titrationAcidStrength?: 'strong' | 'weak';
  /** Default: `25` */
  titrationAcidVolume?: number;
  /** Default: `0.1` */
  titrationBaseConcentration?: number;
  /** Default: `1` */
  titrationIncrementLarge?: number;
  /** Default: `0.1` */
  titrationIncrementSmall?: number;
  /** Default: `"#fde68a"` */
  titrationIndicatorAcidColor?: string;
  /** Default: `"#ec4899"` */
  titrationIndicatorBaseColor?: string;
  /** Default: `8.2` */
  titrationIndicatorMidpoint?: number;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-chem-titration descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdChemTitrationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdChemTitrationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdChemTitrationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ChemTitration.traits.ChemTitrationSim`. */
export function stdChemTitrationTrait(params: StdChemTitrationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ChemTitrationSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ChemTitration.pages.ChemTitrationPage`. */
export function stdChemTitrationPage(params: StdChemTitrationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ChemTitrationPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdChemTitration(params: StdChemTitrationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ChemTitrationOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdChemTitrationTrait(params),
    ],
    pages: [
      stdChemTitrationPage(params),
    ],
  });
}
