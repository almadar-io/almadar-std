/**
 * std-earth-watercycle — Water-cycle flux simulator — the learner drives solar input and steps the cycle to watch evaporation, condensation, precipitation, and runoff fluxes rebalance across ocean, atmosphere, and land reservoirs, on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-earth-watercycle.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-earth-watercycle
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-earth-watercycle';
const ALIAS = 'EarthWatercycle';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdEarthWatercycleEventKey = 'INIT' | 'MODE_SELECTED' | 'SET_SOLAR' | 'STEP_CYCLE' | 'WATERCYCLE_UPDATED';

/**
 * Payload shape for the `WATERCYCLE_UPDATED` event.
 */
export interface StdEarthWatercycleWatercycleUpdatedPayload {
  solarInput?: number;
  cycleStep?: number;
  evaporationFlux?: number;
  condensationFlux?: number;
  precipitationFlux?: number;
  runoffFlux?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdEarthWatercycleConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f0f9ff"` */
  backgroundColor?: string;
  /** Default: `40` */
  baseEvaporationFlux?: number;
  /** Default: `0.9` */
  condensationRatio?: number;
  /** Default: `0.15` */
  fluxWidthScale?: number;
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `0.95` */
  precipitationRatio?: number;
  /** Default: `[{"color":"#0ea5e9","id":"ocean","label":"Ocean","radius":70},{"color":"#e0f2fe","id":"atmosphere","label":"Atmosphere","radius":55},{"color":"#65a30d","id":"land","label":"Land","radius":50}]` */
  reservoirs?: EntityRow[];
  /** Default: `0.3` */
  runoffRatio?: number;
  /** Default: `50` */
  solarInput?: number;
  /** Default: `15` */
  solarStepDelta?: number;
  /** Default: `"Earth Lab"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-earth-watercycle descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdEarthWatercycleParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdEarthWatercycleEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdEarthWatercycleConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `EarthWatercycle.traits.EarthWatercycleSim`. */
export function stdEarthWatercycleTrait(params: StdEarthWatercycleParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.EarthWatercycleSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `EarthWatercycle.pages.EarthWatercyclePage`. */
export function stdEarthWatercyclePage(params: StdEarthWatercycleParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.EarthWatercyclePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdEarthWatercycle(params: StdEarthWatercycleParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'EarthWatercycleOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdEarthWatercycleTrait(params),
    ],
    pages: [
      stdEarthWatercyclePage(params),
    ],
  });
}
