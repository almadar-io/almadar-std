/**
 * std-learn-trace-player — Generic auto-playing frame-sequence instrument — the timed-autoplay sibling of std-learn-stepper: a config-declared ordered roster of frames (each frame a label, caption, and a numeric value snapshot with optional highlighted indices) advances on a fixed tick while Play is engaged, Pause freezes the cursor in place, and Restart rewinds to the first frame; reaching the last frame either loops back to the start (config loop) or stops and emits a completion signal. The current frame's values render as bars on an algorithm-canvas with highlighted indices picked out by color, alongside a frame k of n readout and a caption card narrating what changed, so the same mechanism serves any ordered numeric-trace domain (sorting steps, iterative approximations, simulation snapshots, and beyond). Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-trace-player.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-trace-player
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-trace-player';
const ALIAS = 'LearnTracePlayer';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnTracePlayerEventKey = 'FRAME_ADVANCED' | 'INIT' | 'MODE_SELECTED' | 'PAUSE' | 'PLAY' | 'RESTART' | 'TRACE_DONE';

/**
 * Payload shape for the `FRAME_ADVANCED` event.
 */
export interface StdLearnTracePlayerFrameAdvancedPayload {
  frameIndex?: number;
  frameLabel?: string;
}

/**
 * Payload shape for the `TRACE_DONE` event.
 */
export interface StdLearnTracePlayerTraceDonePayload {
  totalFrames?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnTracePlayerConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `"#3b82f6"` */
  baseColor?: string;
  /** Default: `[{"caption":"Unsorted array.","highlights":[],"label":"Start","values":[5,3,8,1,9,2]},{"caption":"Compare the first two elements; 5 > 3 so swap them.","highlights":[0,1],"label":"Compare 5, 3","values":[3,5,8,1,9,2]},{"caption":"Compare the next pair; already in order.","highlights":[1,2],"label":"Compare 5, 8","values":[3,5,8,1,9,2]},{"caption":"8 > 1 so swap them.","highlights":[2,3],"label":"Compare 8, 1","values":[3,5,1,8,9,2]},{"caption":"Already in order.","highlights":[3,4],"label":"Compare 8, 9","values":[3,5,1,8,9,2]},{"caption":"9 > 2 so swap them; one pass complete.","highlights":[4,5],"label":"Compare 9, 2","values":[3,5,1,8,2,9]}]` */
  frames?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `"#f59e0b"` */
  highlightColor?: string;
  /** Default: `false` */
  loop?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `500` */
  tickMs?: number;
  /** Default: `"Trace Player"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-learn-trace-player descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnTracePlayerParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnTracePlayerEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnTracePlayerConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnTracePlayer.traits.TracePlayerSim`. */
export function stdLearnTracePlayerTrait(params: StdLearnTracePlayerParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TracePlayerSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnTracePlayer.pages.LearnTracePlayerPage`. */
export function stdLearnTracePlayerPage(params: StdLearnTracePlayerParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnTracePlayerPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnTracePlayer(params: StdLearnTracePlayerParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnTracePlayerOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnTracePlayerTrait(params),
    ],
    pages: [
      stdLearnTracePlayerPage(params),
    ],
  });
}
