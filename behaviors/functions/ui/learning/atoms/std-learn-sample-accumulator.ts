/**
 * std-learn-sample-accumulator — Generic sample-accumulator engine — a Draw button adds a batch of pseudo-random samples from a config-selected source (uniform 0..1, or a coin flip weighted by bias) into a fixed-bin histogram on a math-canvas, with running sample count, running mean, and (for the coin source) observed proportion vs bias. Subject atoms narrow the source kind and labels through config; this atom owns only the draw-and-accumulate mechanics. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-sample-accumulator.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-sample-accumulator
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-sample-accumulator';
const ALIAS = 'LearnSampleAccumulator';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnSampleAccumulatorEventKey = 'ACCUMULATOR_RESET' | 'DRAW' | 'INIT' | 'MODE_SELECTED' | 'RESET' | 'SAMPLES_DRAWN' | 'SET_BATCH_SIZE';

/**
 * Payload shape for the `SAMPLES_DRAWN` event.
 */
export interface StdLearnSampleAccumulatorSamplesDrawnPayload {
  count?: number;
  mean?: number;
}

/**
 * Payload shape for the `ACCUMULATOR_RESET` event.
 */
export interface StdLearnSampleAccumulatorAccumulatorResetPayload {
  _unit?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnSampleAccumulatorConfig {
  /** Default: `25` */
  batchSize?: number;
  /** Default: `0.5` */
  bias?: number;
  /** Default: `10` */
  binCount?: number;
  /** Default: `"#2563eb"` */
  color?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"uniform"` */
  sourceKind?: string;
  /** Default: `"Sample Accumulator"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-learn-sample-accumulator descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnSampleAccumulatorParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnSampleAccumulatorEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnSampleAccumulatorConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnSampleAccumulator.traits.SampleAccumulatorSim`. */
export function stdLearnSampleAccumulatorTrait(params: StdLearnSampleAccumulatorParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SampleAccumulatorSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnSampleAccumulator.pages.SampleAccumulatorPage`. */
export function stdLearnSampleAccumulatorPage(params: StdLearnSampleAccumulatorParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SampleAccumulatorPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnSampleAccumulator(params: StdLearnSampleAccumulatorParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SampleAccumulatorOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnSampleAccumulatorTrait(params),
    ],
    pages: [
      stdLearnSampleAccumulatorPage(params),
    ],
  });
}
