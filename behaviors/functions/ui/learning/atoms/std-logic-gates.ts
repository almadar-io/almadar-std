/**
 * std-logic-gates — Boolean circuit simulator — the learner toggles input pins, predicts the output bulb, then propagates values gate by gate (AND/OR/NOT/XOR) through the wired network to check the prediction. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-logic-gates.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-logic-gates
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-logic-gates';
const ALIAS = 'LogicGates';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLogicGatesEventKey = 'GATES_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'NEXT_CIRCUIT' | 'PREDICT_OUTPUT' | 'PROPAGATE' | 'TOGGLE_INPUT';

/**
 * Payload shape for the `GATES_UPDATED` event.
 */
export interface StdLogicGatesGatesUpdatedPayload {
  circuitIndex?: number;
  propagated?: boolean;
  prediction?: string;
  feedback?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLogicGatesConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `false` */
  challengeMode?: boolean;
  /** Default: `[{"edges":[{"from":"A","to":"g1"},{"from":"B","to":"g1"},{"from":"A","to":"g2"},{"from":"B","to":"g2"},{"from":"g1","to":"out"},{"from":"g2","to":"out"}],"gates":[{"id":"g1","kind":"AND","x":210,"y":90},{"id":"g2","kind":"OR","x":210,"y":230},{"id":"out","kind":"XOR","x":380,"y":160}],"id":"circuit-1","inputs":[{"id":"A","label":"A","value":false,"x":60,"y":90},{"id":"B","label":"B","value":false,"x":60,"y":230}],"label":"Circuit 1 — (A AND B) XOR (A OR B)","output":"out"},{"edges":[{"from":"A","to":"g1"},{"from":"B","to":"g1"},{"from":"g1","to":"out"}],"gates":[{"id":"g1","kind":"XOR","x":220,"y":160},{"id":"out","kind":"NOT","x":380,"y":160}],"id":"circuit-2","inputs":[{"id":"A","label":"A","value":true,"x":60,"y":90},{"id":"B","label":"B","value":false,"x":60,"y":230}],"label":"Circuit 2 — NOT (A XOR B)","output":"out"}]` */
  circuits?: EntityRow[];
  /** Default: `["AND","OR","NOT","XOR"]` */
  gateKinds?: string[];
  /** Default: `340` */
  height?: number;
  /** Default: `2` */
  inputCount?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `true` */
  showIntermediate?: boolean;
  /** Default: `"Boolean Circuit"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-logic-gates descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLogicGatesParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLogicGatesEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLogicGatesConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LogicGates.traits.LogicGatesSim`. */
export function stdLogicGatesTrait(params: StdLogicGatesParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LogicGatesSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LogicGates.pages.LogicGatesPage`. */
export function stdLogicGatesPage(params: StdLogicGatesParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LogicGatesPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLogicGates(params: StdLogicGatesParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LogicGatesOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLogicGatesTrait(params),
    ],
    pages: [
      stdLogicGatesPage(params),
    ],
  });
}
