/**
 * std-learn-arrangement — Generic build-an-ordering-then-grade instrument — a config-declared roster of items (each with a correct position index and an optional hint) is tapped one at a time from a remaining-items button row onto a growing sequence of ordered slots rendered on a math-canvas; a tapped item becomes unavailable, an Undo button removes the last placement, and once every slot is filled a Check button grades the arrangement with per-slot correct/wrong coloring, a score readout, and a verdict badge. Retry clears the placements and keeps the same roster. Item vocabulary is entirely config-declared, so the same mechanism serves any ordering task (process steps, chronology, procedure, ranking) standalone. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-arrangement.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-arrangement
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-arrangement';
const ALIAS = 'LearnArrangement';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnArrangementEventKey = 'ARRANGEMENT_CHECKED' | 'ARRANGEMENT_RESET' | 'CHECK' | 'INIT' | 'MODE_SELECTED' | 'PLACE' | 'RETRY' | 'UNDO';

/**
 * Payload shape for the `ARRANGEMENT_CHECKED` event.
 */
export interface StdLearnArrangementArrangementCheckedPayload {
  correctCount?: number;
  total?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnArrangementConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"Check"` */
  checkLabel?: string;
  /** Default: `"#16a34a"` */
  correctColor?: string;
  /** Default: `"#e2e8f0"` */
  emptySlotColor?: string;
  /** Default: `220` */
  height?: number;
  /** Default: `"#dc2626"` */
  incorrectColor?: string;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `[{"correctPosition":0,"hint":"What has to happen before anything can be poured?","id":"1","label":"Boil water in a kettle"},{"correctPosition":1,"hint":"The bag goes in before the water does.","id":"2","label":"Place a tea bag in the cup"},{"correctPosition":2,"hint":"Now the water and the bag meet.","id":"3","label":"Pour the hot water over the tea bag"},{"correctPosition":3,"hint":"Flavor needs time to develop.","id":"4","label":"Let it steep for a few minutes"},{"correctPosition":4,"hint":"This happens once the tea is strong enough.","id":"5","label":"Remove the tea bag and add milk or sugar to taste"}]` */
  items?: EntityRow[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#94a3b8"` */
  pendingColor?: string;
  /** Default: `"Retry"` */
  retryLabel?: string;
  /** Default: `"#334155"` */
  slotBorderColor?: string;
  /** Default: `13` */
  slotFontSize?: number;
  /** Default: `60` */
  slotHeight?: number;
  /** Default: `40` */
  slotLeft?: number;
  /** Default: `120` */
  slotSpacing?: number;
  /** Default: `"#0f172a"` */
  slotTextColor?: string;
  /** Default: `100` */
  slotWidth?: number;
  /** Default: `70` */
  slotY?: number;
  /** Default: `"Build the Order"` */
  title?: string;
  /** Default: `"Undo"` */
  undoLabel?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-learn-arrangement descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnArrangementParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnArrangementEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnArrangementConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnArrangement.traits.LearnArrangementSim`. */
export function stdLearnArrangementTrait(params: StdLearnArrangementParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LearnArrangementSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnArrangement.pages.LearnArrangementPage`. */
export function stdLearnArrangementPage(params: StdLearnArrangementParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnArrangementPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnArrangement(params: StdLearnArrangementParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnArrangementOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnArrangementTrait(params),
    ],
    pages: [
      stdLearnArrangementPage(params),
    ],
  });
}
