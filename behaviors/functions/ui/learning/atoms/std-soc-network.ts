/**
 * std-soc-network — Small-world network engine — a ring graph whose edges the learner rewires on and off: degree centrality is recomputed live, weak-tie bridges are highlighted, and a complex-contagion diffusion step spreads adoption through one strong tie or a pair of weak ties, on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-soc-network.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-soc-network
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-soc-network';
const ALIAS = 'SocNetwork';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSocNetworkEventKey = 'INIT' | 'MODE_SELECTED' | 'NETWORK_UPDATED' | 'RESET' | 'RUN_DIFFUSION' | 'TOGGLE_EDGE' | 'TOGGLE_NODE';

/**
 * Payload shape for the `NETWORK_UPDATED` event.
 */
export interface StdSocNetworkNetworkUpdatedPayload {
  nodeEnabled?: boolean[];
  edgeEnabled?: boolean[];
  adopted?: boolean[];
  adoptedCount?: number;
  hub?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSocNetworkConfig {
  /** Default: `"#dc2626"` */
  adoptedNodeColor?: string;
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f1f5f9"` */
  backgroundColor?: string;
  /** Default: `"degree"` */
  centralityMetric?: 'degree' | 'reach';
  /** Default: `"#cbd5e1"` */
  disabledNodeColor?: string;
  /** Default: `[{"from":"n0","to":"n1"},{"from":"n1","to":"n2"},{"from":"n2","to":"n3"},{"from":"n3","to":"n4"},{"from":"n4","to":"n5"},{"from":"n5","to":"n6"},{"from":"n6","to":"n7"},{"from":"n7","to":"n0"},{"from":"n0","to":"n4"},{"from":"n2","to":"n6"},{"from":"n1","to":"n5"}]` */
  edges?: EntityRow[];
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#64748b"` */
  nodeColor?: string;
  /** Default: `[{"color":"#64748b","id":"n0","label":"A","radius":16,"x":480,"y":210},{"color":"#64748b","id":"n1","label":"B","radius":16,"x":433,"y":323},{"color":"#64748b","id":"n2","label":"C","radius":16,"x":320,"y":370},{"color":"#64748b","id":"n3","label":"D","radius":16,"x":207,"y":323},{"color":"#64748b","id":"n4","label":"E","radius":16,"x":160,"y":210},{"color":"#64748b","id":"n5","label":"F","radius":16,"x":207,"y":97},{"color":"#64748b","id":"n6","label":"G","radius":16,"x":320,"y":50},{"color":"#64748b","id":"n7","label":"H","radius":16,"x":433,"y":97}]` */
  nodes?: EntityRow[];
  /** Default: `"#334155"` */
  strongTieColor?: string;
  /** Default: `[0.9,0.9,0.9,0.9,0.9,0.9,0.9,0.9,0.2,0.2,0.15]` */
  tieStrengths?: number[];
  /** Default: `"Small-World Network"` */
  title?: string;
  /** Default: `"#f59e0b"` */
  weakTieColor?: string;
  /** Default: `0.5` */
  weakTieThreshold?: number;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-soc-network descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSocNetworkParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSocNetworkEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSocNetworkConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SocNetwork.traits.SocNetSim`. */
export function stdSocNetworkTrait(params: StdSocNetworkParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SocNetSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SocNetwork.pages.SocNetPage`. */
export function stdSocNetworkPage(params: StdSocNetworkParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SocNetPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSocNetwork(params: StdSocNetworkParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SocNetOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSocNetworkTrait(params),
    ],
    pages: [
      stdSocNetworkPage(params),
    ],
  });
}
