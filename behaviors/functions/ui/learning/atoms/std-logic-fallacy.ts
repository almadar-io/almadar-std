/**
 * std-logic-fallacy — Fallacy classifier — an argument card is presented, the learner classifies it as valid or as a named fallacy (ad hominem, straw man, false dilemma, affirming the consequent), gets a verdict with explanation, and builds a running score across the corpus. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-logic-fallacy.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-logic-fallacy
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-logic-fallacy';
const ALIAS = 'LogicFallacy';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLogicFallacyEventKey = 'FALLACY_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'NEXT_ITEM' | 'PICK_LABEL' | 'REVEAL_ANSWER';

/**
 * Payload shape for the `FALLACY_UPDATED` event.
 */
export interface StdLogicFallacyFallacyUpdatedPayload {
  itemIndex?: number;
  selectedLabel?: string;
  revealed?: boolean;
  correctCount?: number;
  attemptedCount?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLogicFallacyConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"answer":"ad-hominem","explanation":"It attacks the person rather than the argument — ad hominem.","hint":"Does this engage the plan, or the person?","text":"You're only against the plan because you never liked me."},{"answer":"false-dilemma","explanation":"It presents two extremes as the only possibilities — false dilemma.","hint":"Are those really the only two options?","text":"We either ban all cars or accept choking smog forever."},{"answer":"affirming-the-consequent","explanation":"From P→Q and Q it concludes P — affirming the consequent.","hint":"Can the match be cancelled for another reason?","text":"If it rains, the match is cancelled. The match is cancelled, so it rained."},{"answer":"straw-man","explanation":"It exaggerates the position into something easier to attack — straw man.","hint":"Is that really what he argued?","text":"He wants the library closed? Next he'll want books banned."},{"answer":"valid","explanation":"The conclusion follows necessarily from the premises — a valid argument.","hint":"Does the conclusion follow from the premises?","text":"All mammals breathe air. Whales are mammals, so whales breathe air."}]` */
  corpus?: EntityRow[];
  /** Default: `"#86efac"` */
  correctColor?: string;
  /** Default: `[{"color":"#cbd5e1","label":"Valid","value":"valid"},{"color":"#fca5a5","label":"Ad Hominem","value":"ad-hominem"},{"color":"#fdba74","label":"Straw Man","value":"straw-man"},{"color":"#fde68a","label":"False Dilemma","value":"false-dilemma"},{"color":"#c4b5fd","label":"Affirming the Consequent","value":"affirming-the-consequent"}]` */
  fallacySet?: EntityRow[];
  /** Default: `160` */
  height?: number;
  /** Default: `"#ef4444"` */
  incorrectColor?: string;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `4` */
  passScore?: number;
  /** Default: `true` */
  showHint?: boolean;
  /** Default: `false` */
  shuffle?: boolean;
  /** Default: `"Fallacy Classifier"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-logic-fallacy descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLogicFallacyParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLogicFallacyEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLogicFallacyConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LogicFallacy.traits.LogicFallacySim`. */
export function stdLogicFallacyTrait(params: StdLogicFallacyParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LogicFallacySim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LogicFallacy.pages.LogicFallacyPage`. */
export function stdLogicFallacyPage(params: StdLogicFallacyParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LogicFallacyPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLogicFallacy(params: StdLogicFallacyParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LogicFallacyOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLogicFallacyTrait(params),
    ],
    pages: [
      stdLogicFallacyPage(params),
    ],
  });
}
