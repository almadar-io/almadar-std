/**
 * std-math-numberline — Integer number-line engine — a marker hops integer-by-integer along y=0 on math-canvas, sweeping -9..9 via a phase-driven step.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-math-numberline.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-math-numberline
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-math-numberline';
const ALIAS = 'MathNumberline';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMathNumberlineEventKey = 'INIT' | 'LINE_UPDATED';

/**
 * Payload shape for the `LINE_UPDATED` event.
 */
export interface StdMathNumberlineLineUpdatedPayload {
  curves?: EntityRow[];
  points?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMathNumberlineConfig {
  /** Default: `[{"color":"#94a3b8","label":"","samples":[{"x":-9,"y":0},{"x":9,"y":0}]}]` */
  curves?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `[{"color":"#94a3b8","label":"-9","radius":2,"x":-9,"y":0},{"color":"#94a3b8","label":"-8","radius":2,"x":-8,"y":0},{"color":"#94a3b8","label":"-7","radius":2,"x":-7,"y":0},{"color":"#94a3b8","label":"-6","radius":2,"x":-6,"y":0},{"color":"#94a3b8","label":"-5","radius":2,"x":-5,"y":0},{"color":"#94a3b8","label":"-4","radius":2,"x":-4,"y":0},{"color":"#94a3b8","label":"-3","radius":2,"x":-3,"y":0},{"color":"#94a3b8","label":"-2","radius":2,"x":-2,"y":0},{"color":"#94a3b8","label":"-1","radius":2,"x":-1,"y":0},{"color":"#94a3b8","label":"0","radius":2,"x":0,"y":0},{"color":"#94a3b8","label":"1","radius":2,"x":1,"y":0},{"color":"#94a3b8","label":"2","radius":2,"x":2,"y":0},{"color":"#94a3b8","label":"3","radius":2,"x":3,"y":0},{"color":"#94a3b8","label":"4","radius":2,"x":4,"y":0},{"color":"#94a3b8","label":"5","radius":2,"x":5,"y":0},{"color":"#94a3b8","label":"6","radius":2,"x":6,"y":0},{"color":"#94a3b8","label":"7","radius":2,"x":7,"y":0},{"color":"#94a3b8","label":"8","radius":2,"x":8,"y":0},{"color":"#94a3b8","label":"9","radius":2,"x":9,"y":0},{"color":"#dc2626","label":"n","radius":6,"x":-9,"y":0}]` */
  points?: EntityRow[];
  /** Default: `true` */
  running?: boolean;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-math-numberline descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMathNumberlineParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMathNumberlineEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMathNumberlineConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MathNumberline.traits.MathNumberlineSim`. */
export function stdMathNumberlineTrait(params: StdMathNumberlineParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MathNumberlineSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MathNumberline.pages.MathNumberlineEnginePage`. */
export function stdMathNumberlinePage(params: StdMathNumberlineParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MathNumberlineEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMathNumberline(params: StdMathNumberlineParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'MathNumberlineEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMathNumberlineTrait(params),
    ],
    pages: [
      stdMathNumberlinePage(params),
    ],
  });
}
