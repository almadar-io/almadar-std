/**
 * std-geo-dtm — Demographic transition model engine — stages 1-5 redraw piecewise birth/death rate curves on a math-canvas; the learner pages through stages and drives birth/death rate sliders to reproduce each stage's target rates within a tolerance. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geo-dtm.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geo-dtm
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geo-dtm';
const ALIAS = 'GeoDtm';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeoDtmEventKey = 'DTM_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'NEXT_STAGE' | 'PREV_STAGE' | 'SET_RATES';

/**
 * Payload shape for the `DTM_UPDATED` event.
 */
export interface StdGeoDtmDtmUpdatedPayload {
  stageIndex?: number;
  stageName?: string;
  birthRate?: number;
  deathRate?: number;
  matched?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeoDtmConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#94a3b8"` */
  axisColor?: string;
  /** Default: `"#2563eb"` */
  birthColor?: string;
  /** Default: `40` */
  birthRate?: number;
  /** Default: `"#dc2626"` */
  deathColor?: string;
  /** Default: `40` */
  deathRate?: number;
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `3` */
  matchTolerance?: number;
  /** Default: `50` */
  rateAxisMax?: number;
  /** Default: `5` */
  stageCount?: number;
  /** Default: `"#7c3aed"` */
  stageMarkerColor?: string;
  /** Default: `[{"birthRate":40,"deathRate":38,"description":"Pre-industrial: high birth and death rates cancel out, so population growth is near zero.","name":"High Stationary","stage":1},{"birthRate":40,"deathRate":22,"description":"Death rate falls with better food and sanitation while birth rate stays high — rapid natural increase.","name":"Early Expanding","stage":2},{"birthRate":28,"deathRate":12,"description":"Birth rate begins to fall with urbanization and contraception; growth slows.","name":"Late Expanding","stage":3},{"birthRate":12,"deathRate":11,"description":"Both rates low and roughly equal; population is large but stable.","name":"Low Stationary","stage":4},{"birthRate":8,"deathRate":11,"description":"Birth rate drops below death rate; the population ages and begins to shrink.","name":"Declining","stage":5}]` */
  stagePresets?: EntityRow[];
  /** Default: `"#1e293b"` */
  textColor?: string;
  /** Default: `"Demographic Transition Model"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-geo-dtm descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeoDtmParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeoDtmEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeoDtmConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GeoDtm.traits.GeoDtmSim`. */
export function stdGeoDtmTrait(params: StdGeoDtmParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoDtmSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GeoDtm.pages.GeoDtmPage`. */
export function stdGeoDtmPage(params: StdGeoDtmParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoDtmPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeoDtm(params: StdGeoDtmParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoDtmOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeoDtmTrait(params),
    ],
    pages: [
      stdGeoDtmPage(params),
    ],
  });
}
