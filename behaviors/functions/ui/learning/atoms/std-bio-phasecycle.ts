/**
 * std-bio-phasecycle — Staged biology phase-cycle engine — the learner steps a Next/Prev cursor through an ordered roster of stages (e.g. mitosis phases), each revealing its own node/edge/shape snapshot, label, and description, and can predict the next stage before revealing it for immediate correct/incorrect feedback. Stage vocabulary is entirely config-declared, so the same mechanism serves any staged biology process, on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-bio-phasecycle.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-bio-phasecycle
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-bio-phasecycle';
const ALIAS = 'BioPhasecycle';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBioPhasecycleEventKey = 'INIT' | 'MODE_SELECTED' | 'NEXT' | 'PHASE_CYCLE_UPDATED' | 'PREDICT' | 'PREV';

/**
 * Payload shape for the `PHASE_CYCLE_UPDATED` event.
 */
export interface StdBioPhasecyclePhaseCycleUpdatedPayload {
  currentPhase?: number;
  phaseLabel?: string;
  predictedPhase?: string;
  feedback?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBioPhasecycleConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f0fdf4"` */
  backgroundColor?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"description":"Chromatin condenses into visible chromosomes and the nuclear envelope begins breaking down.","edges":[],"label":"Prophase","nodes":[{"color":"#dcfce7","id":"membrane","kind":"cell","label":"Cell","radius":140,"x":300,"y":200},{"color":"#bfdbfe","id":"nucleus","kind":"cell","label":"Nucleus","radius":70,"x":300,"y":200},{"color":"#7c3aed","id":"chromosome-1","kind":"molecule","label":"","radius":9,"x":280,"y":185},{"color":"#7c3aed","id":"chromosome-2","kind":"molecule","label":"","radius":9,"x":320,"y":185},{"color":"#7c3aed","id":"chromosome-3","kind":"molecule","label":"","radius":9,"x":280,"y":215},{"color":"#7c3aed","id":"chromosome-4","kind":"molecule","label":"","radius":9,"x":320,"y":215}],"shapes":[]},{"description":"Chromosomes line up along the cell's equator, each attached to spindle fibers from opposite poles.","edges":[],"label":"Metaphase","nodes":[{"color":"#dcfce7","id":"membrane","kind":"cell","label":"Cell","radius":140,"x":300,"y":200},{"color":"#f59e0b","id":"pole-a","kind":"organelle","label":"Pole","radius":8,"x":150,"y":200},{"color":"#f59e0b","id":"pole-b","kind":"organelle","label":"Pole","radius":8,"x":450,"y":200},{"color":"#7c3aed","id":"chromosome-1","kind":"molecule","label":"","radius":9,"x":300,"y":150},{"color":"#7c3aed","id":"chromosome-2","kind":"molecule","label":"","radius":9,"x":300,"y":185},{"color":"#7c3aed","id":"chromosome-3","kind":"molecule","label":"","radius":9,"x":300,"y":215},{"color":"#7c3aed","id":"chromosome-4","kind":"molecule","label":"","radius":9,"x":300,"y":250}],"shapes":[{"color":"#94a3b8","lineWidth":1,"type":"line","x1":150,"x2":300,"y1":200,"y2":150},{"color":"#94a3b8","lineWidth":1,"type":"line","x1":150,"x2":300,"y1":200,"y2":250},{"color":"#94a3b8","lineWidth":1,"type":"line","x1":450,"x2":300,"y1":200,"y2":150},{"color":"#94a3b8","lineWidth":1,"type":"line","x1":450,"x2":300,"y1":200,"y2":250}]},{"description":"Sister chromatids separate and are pulled toward opposite poles by the spindle fibers.","edges":[],"label":"Anaphase","nodes":[{"color":"#dcfce7","id":"membrane","kind":"cell","label":"Cell","radius":140,"x":300,"y":200},{"color":"#f59e0b","id":"pole-a","kind":"organelle","label":"Pole","radius":8,"x":150,"y":200},{"color":"#f59e0b","id":"pole-b","kind":"organelle","label":"Pole","radius":8,"x":450,"y":200},{"color":"#7c3aed","id":"chromosome-1","kind":"molecule","label":"","radius":9,"x":210,"y":180},{"color":"#7c3aed","id":"chromosome-2","kind":"molecule","label":"","radius":9,"x":210,"y":220},{"color":"#7c3aed","id":"chromosome-3","kind":"molecule","label":"","radius":9,"x":390,"y":180},{"color":"#7c3aed","id":"chromosome-4","kind":"molecule","label":"","radius":9,"x":390,"y":220}],"shapes":[]},{"description":"Two nuclear envelopes reform around each set of chromosomes as the cytoplasm begins to divide.","edges":[],"label":"Telophase","nodes":[{"color":"#dcfce7","id":"membrane-a","kind":"cell","label":"Cell A","radius":90,"x":190,"y":200},{"color":"#dcfce7","id":"membrane-b","kind":"cell","label":"Cell B","radius":90,"x":410,"y":200},{"color":"#bfdbfe","id":"nucleus-a","kind":"cell","label":"Nucleus","radius":45,"x":190,"y":200},{"color":"#bfdbfe","id":"nucleus-b","kind":"cell","label":"Nucleus","radius":45,"x":410,"y":200}],"shapes":[{"color":"#16a34a","fill":"#16a34a","height":160,"type":"rect","width":20,"x":290,"y":120}]}]` */
  phases?: EntityRow[];
  /** Default: `true` */
  predictMode?: boolean;
  /** Default: `"Cell Division Phase Cycle"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-bio-phasecycle descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBioPhasecycleParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBioPhasecycleEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBioPhasecycleConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `BioPhasecycle.traits.BioPhaseCycleSim`. */
export function stdBioPhasecycleTrait(params: StdBioPhasecycleParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BioPhaseCycleSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `BioPhasecycle.pages.BioPhaseCyclePage`. */
export function stdBioPhasecyclePage(params: StdBioPhasecycleParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BioPhaseCyclePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBioPhasecycle(params: StdBioPhasecycleParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'BioPhaseCycleOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBioPhasecycleTrait(params),
    ],
    pages: [
      stdBioPhasecyclePage(params),
    ],
  });
}
