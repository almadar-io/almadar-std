/**
 * std-math-derivative — Slope viewer — a point rides a curve with its tangent line; an optional secant through x and x + h converges to the tangent as h shrinks, and the slope readout is f′(x). Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-math-derivative.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-math-derivative
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-math-derivative';
const ALIAS = 'MathDerivative';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMathDerivativeEventKey = 'DERIVATIVE_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'MOVE_POINT' | 'SET_FUNCTION' | 'SHRINK_H' | 'TOGGLE_SECANT';

/**
 * Payload shape for the `DERIVATIVE_UPDATED` event.
 */
export interface StdMathDerivativeDerivativeUpdatedPayload {
  x?: number;
  fx?: number;
  slope?: number;
  h?: number;
  secantSlope?: number;
  functionPreset?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMathDerivativeConfig {
  /** Default: `"#2563eb"` */
  curveColor?: string;
  /** Default: `"#9333ea"` */
  derivativeColor?: string;
  /** Default: `"parabola"` */
  functionPreset?: 'parabola' | 'cubic' | 'sine';
  /** Default: `1` */
  gridStep?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `2` */
  initialH?: number;
  /** Default: `1` */
  initialX?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#dc2626"` */
  pointColor?: string;
  /** Default: `[-6,-5.7,-5.4,-5.1,-4.8,-4.5,-4.2,-3.9,-3.6,-3.3,-3,-2.7,-2.4,-2.1,-1.8,-1.5,-1.2,-0.9,-0.6,-0.3,0,0.3,0.6,0.9,1.2,1.5,1.8,2.1,2.4,2.7,3,3.3,3.6,3.9,4.2,4.5,4.8,5.1,5.4,5.7,6]` */
  sampleXs?: number[];
  /** Default: `"#16a34a"` */
  secantColor?: string;
  /** Default: `true` */
  showAxes?: boolean;
  /** Default: `false` */
  showDerivativeCurve?: boolean;
  /** Default: `true` */
  showGrid?: boolean;
  /** Default: `true` */
  showSecant?: boolean;
  /** Default: `"#dc2626"` */
  tangentColor?: string;
  /** Default: `"Derivative — Slope Viewer"` */
  title?: string;
  /** Default: `600` */
  width?: number;
  /** Default: `6` */
  xMax?: number;
  /** Default: `-6` */
  xMin?: number;
  /** Default: `10` */
  yMax?: number;
  /** Default: `-10` */
  yMin?: number;
}

/**
 * Params for the std-math-derivative descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMathDerivativeParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMathDerivativeEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMathDerivativeConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MathDerivative.traits.MathDerivativeSim`. */
export function stdMathDerivativeTrait(params: StdMathDerivativeParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MathDerivativeSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MathDerivative.pages.MathDerivativePage`. */
export function stdMathDerivativePage(params: StdMathDerivativeParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MathDerivativePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMathDerivative(params: StdMathDerivativeParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'MathDerivativeOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMathDerivativeTrait(params),
    ],
    pages: [
      stdMathDerivativePage(params),
    ],
  });
}
