/**
 * std-learn-goal-seek — Generic goal-seek (inverse-problem) instrument — a config-declared target curve is drawn from a hidden parameter pair, and the learner tunes their own two parameters with sliders until their curve matches it, then presses Check to be graded against an RMS-distance tolerance. The curve family (power y = A·xᴮ, linear y = A + B·x, or sine y = A·sin(B·x)), the hidden target parameters, the tolerance, and every label are config-declared, so the same mechanism serves any inverse problem: fit a demand curve, match a projectile arc, tune a growth rate, recover a wave's amplitude and frequency. A live distance readout (optionally hidden for a harder variant) plays warmer/colder. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-goal-seek.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-goal-seek
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-goal-seek';
const ALIAS = 'LearnGoalSeek';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnGoalSeekEventKey = 'CHECK' | 'GOAL_CHECKED' | 'GOAL_RESET' | 'INIT' | 'MODE_SELECTED' | 'PARAMS_CHANGED' | 'RESET' | 'SET_PARAM_A' | 'SET_PARAM_B';

/**
 * Payload shape for the `PARAMS_CHANGED` event.
 */
export interface StdLearnGoalSeekParamsChangedPayload {
  paramA?: number;
  paramB?: number;
  distance?: number;
}

/**
 * Payload shape for the `GOAL_CHECKED` event.
 */
export interface StdLearnGoalSeekGoalCheckedPayload {
  matched?: boolean;
  distance?: number;
  attempts?: number;
}

/**
 * Payload shape for the `GOAL_RESET` event.
 */
export interface StdLearnGoalSeekGoalResetPayload {
  attempts?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnGoalSeekConfig {
  /** Default: `"Check"` */
  checkLabel?: string;
  /** Default: `"sine"` */
  curveKind?: string;
  /** Default: `"Tune the two sliders until your curve matches the target curve, then press Check."` */
  goalText?: string;
  /** Default: `1` */
  gridStep?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `"#3b82f6"` */
  learnerColor?: string;
  /** Default: `"Yours"` */
  learnerLabel?: string;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `1` */
  paramADefault?: number;
  /** Default: `"A"` */
  paramALabel?: string;
  /** Default: `5` */
  paramAMax?: number;
  /** Default: `-5` */
  paramAMin?: number;
  /** Default: `0.1` */
  paramAStep?: number;
  /** Default: `1` */
  paramBDefault?: number;
  /** Default: `"B"` */
  paramBLabel?: string;
  /** Default: `5` */
  paramBMax?: number;
  /** Default: `-5` */
  paramBMin?: number;
  /** Default: `0.1` */
  paramBStep?: number;
  /** Default: `true` */
  showAxes?: boolean;
  /** Default: `true` */
  showGrid?: boolean;
  /** Default: `true` */
  showLiveDistance?: boolean;
  /** Default: `2.5` */
  targetA?: number;
  /** Default: `1.5` */
  targetB?: number;
  /** Default: `"#94a3b8"` */
  targetColor?: string;
  /** Default: `"Target"` */
  targetLabel?: string;
  /** Default: `"Goal Seek"` */
  title?: string;
  /** Default: `0.35` */
  tolerance?: number;
  /** Default: `600` */
  width?: number;
  /** Default: `10` */
  xMax?: number;
  /** Default: `0` */
  xMin?: number;
}

/**
 * Params for the std-learn-goal-seek descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnGoalSeekParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnGoalSeekEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnGoalSeekConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnGoalSeek.traits.GoalSeekSim`. */
export function stdLearnGoalSeekTrait(params: StdLearnGoalSeekParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GoalSeekSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnGoalSeek.pages.GoalSeekPage`. */
export function stdLearnGoalSeekPage(params: StdLearnGoalSeekParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GoalSeekPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnGoalSeek(params: StdLearnGoalSeekParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GoalSeekOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnGoalSeekTrait(params),
    ],
    pages: [
      stdLearnGoalSeekPage(params),
    ],
  });
}
