/**
 * std-bio-processrate — Photosynthesis-style process-rate engine — the learner adjusts light, CO₂, and temperature sliders and watches the process rate and its limiting factor recompute live, with input-node radii/colors on a biology-canvas reflecting each factor's saturation. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-bio-processrate.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-bio-processrate
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-bio-processrate';
const ALIAS = 'BioProcessrate';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBioProcessrateEventKey = 'INIT' | 'MODE_SELECTED' | 'PROCESS_RATE_UPDATED' | 'SET_CO2' | 'SET_LIGHT' | 'SET_TEMP';

/**
 * Payload shape for the `PROCESS_RATE_UPDATED` event.
 */
export interface StdBioProcessrateProcessRateUpdatedPayload {
  rate?: number;
  limitingFactor?: string;
  light?: number;
  co2?: number;
  temperature?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBioProcessrateConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f0fdf4"` */
  backgroundColor?: string;
  /** Default: `50` */
  co2?: number;
  /** Default: `60` */
  co2Saturation?: number;
  /** Default: `[]` */
  edges?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `50` */
  light?: number;
  /** Default: `70` */
  lightSaturation?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `100` */
  maxRate?: number;
  /** Default: `"O₂ output"` */
  processLabel?: string;
  /** Default: `[{"color":"#16a34a","id":"leaf","kind":"organism","label":"Leaf","radius":60,"x":300,"y":200},{"color":"#facc15","id":"light-in","kind":"molecule","label":"Light","radius":12,"x":300,"y":70},{"color":"#64748b","id":"co2-in","kind":"molecule","label":"CO₂","radius":12,"x":140,"y":280},{"color":"#dc2626","id":"temp-in","kind":"molecule","label":"Temp","radius":12,"x":460,"y":280},{"color":"#0ea5e9","id":"output","kind":"molecule","label":"O₂ Output","radius":10,"x":300,"y":330}]` */
  processNodes?: EntityRow[];
  /** Default: `[]` */
  shapes?: EntityRow[];
  /** Default: `25` */
  tempOptimum?: number;
  /** Default: `15` */
  tempTolerance?: number;
  /** Default: `25` */
  temperature?: number;
  /** Default: `"Photosynthesis Rate"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-bio-processrate descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBioProcessrateParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBioProcessrateEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBioProcessrateConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `BioProcessrate.traits.BioProcessRateSim`. */
export function stdBioProcessrateTrait(params: StdBioProcessrateParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BioProcessRateSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `BioProcessrate.pages.BioProcessRatePage`. */
export function stdBioProcessratePage(params: StdBioProcessrateParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BioProcessRatePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBioProcessrate(params: StdBioProcessrateParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'BioProcessRateOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBioProcessrateTrait(params),
    ],
    pages: [
      stdBioProcessratePage(params),
    ],
  });
}
