/**
 * std-geo-population — Age-sex population pyramid engine — the learner drives birth/death/migration rate dials and steps a five-cohort cohort-component projection, reading the dependency ratio and pyramid shape classification on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geo-population.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geo-population
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geo-population';
const ALIAS = 'GeoPopulation';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeoPopulationEventKey = 'INIT' | 'MODE_SELECTED' | 'POPULATION_UPDATED' | 'SET_BIRTH_RATE' | 'SET_DEATH_RATE' | 'SET_MIGRATION' | 'STEP_DECADE';

/**
 * Payload shape for the `POPULATION_UPDATED` event.
 */
export interface StdGeoPopulationPopulationUpdatedPayload {
  birthRate?: number;
  deathRate?: number;
  migrationRate?: number;
  decadeStep?: number;
  totalPopulation?: number;
  dependencyRatio?: number;
  pyramidClassLabel?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeoPopulationConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#94a3b8"` */
  axisColor?: string;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `20` */
  birthRate?: number;
  /** Default: `0.4` */
  birthRateCoefficient?: number;
  /** Default: `[{"baseSurvivalRate":0.97,"deathSensitivity":0.15,"femaleShare":125,"id":"c0","label":"0–19","maleShare":130,"order":0},{"baseSurvivalRate":0.96,"deathSensitivity":0.2,"femaleShare":118,"id":"c1","label":"20–39","maleShare":120,"order":1},{"baseSurvivalRate":0.93,"deathSensitivity":0.3,"femaleShare":97,"id":"c2","label":"40–59","maleShare":95,"order":2},{"baseSurvivalRate":0.85,"deathSensitivity":0.5,"femaleShare":60,"id":"c3","label":"60–79","maleShare":55,"order":3},{"baseSurvivalRate":0.7,"deathSensitivity":0.6,"femaleShare":20,"id":"c4","label":"80+","maleShare":15,"order":4}]` */
  cohortSeeds?: EntityRow[];
  /** Default: `15` */
  deathRate?: number;
  /** Default: `"#db2777"` */
  femaleColor?: string;
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `"#2563eb"` */
  maleColor?: string;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `0.3` */
  migrationCoefficient?: number;
  /** Default: `0` */
  migrationRate?: number;
  /** Default: `[{"code":"expansive","description":"Wide base — youngest cohort's share of total population is at or above the expansive threshold, signalling a young, fast-growing population.","label":"Expansive","minBaseSharePercent":32},{"code":"stationary","description":"Youngest cohort's share is at or above the stationary threshold but below the expansive one, signalling a population near replacement level.","label":"Stationary","minBaseSharePercent":18},{"code":"constrictive","description":"Youngest cohort's share is below the stationary threshold, signalling a narrowing base and an aging population.","label":"Constrictive","minBaseSharePercent":0}]` */
  pyramidClassRules?: EntityRow[];
  /** Default: `40` */
  pyramidScaleMaxPercent?: number;
  /** Default: `0.512` */
  sexRatioAtBirth?: number;
  /** Default: `"#1e293b"` */
  textColor?: string;
  /** Default: `"Geography Lab"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-geo-population descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeoPopulationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeoPopulationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeoPopulationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GeoPopulation.traits.GeoPopulationSim`. */
export function stdGeoPopulationTrait(params: StdGeoPopulationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoPopulationSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GeoPopulation.pages.GeoPopulationPage`. */
export function stdGeoPopulationPage(params: StdGeoPopulationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoPopulationPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeoPopulation(params: StdGeoPopulationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoPopulationOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeoPopulationTrait(params),
    ],
    pages: [
      stdGeoPopulationPage(params),
    ],
  });
}
