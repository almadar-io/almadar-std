/**
 * std-hist-causalchain — WWI causal-chain counterfactual engine — the learner toggles structural causes and triggers off/on, a dependency cascade removes any cause whose enabling edge is gone, and the summed contribution of active causes is compared against a war threshold on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-hist-causalchain.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-hist-causalchain
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-hist-causalchain';
const ALIAS = 'HistCausalchain';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdHistCausalchainEventKey = 'CAUSAL_CHAIN_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'RESET_CAUSES' | 'TOGGLE_CAUSE';

/**
 * Payload shape for the `CAUSAL_CHAIN_UPDATED` event.
 */
export interface StdHistCausalchainCausalChainUpdatedPayload {
  activeIds?: string[];
  totalContribution?: number;
  outcomeFires?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdHistCausalchainConfig {
  /** Default: `"#92400e"` */
  activeCauseColor?: string;
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#faf5eb"` */
  backgroundColor?: string;
  /** Default: `38` */
  causeRadius?: number;
  /** Default: `[{"contribution":20,"id":"militarism","label":"Militarism","necessary":false,"x":110,"y":80},{"contribution":15,"id":"alliances","label":"Alliance System","necessary":false,"x":330,"y":50},{"contribution":15,"id":"imperialism","label":"Imperial Rivalry","necessary":false,"x":550,"y":80},{"contribution":20,"id":"nationalism","label":"Nationalism","necessary":false,"x":110,"y":260},{"contribution":30,"id":"assassination-trigger","label":"Assassination of Franz Ferdinand","necessary":true,"x":550,"y":260}]` */
  causes?: EntityRow[];
  /** Default: `"#78350f"` */
  edgeActiveColor?: string;
  /** Default: `"#d6d3d1"` */
  edgeRemovedColor?: string;
  /** Default: `[{"from":"militarism","to":"outcome"},{"from":"alliances","to":"outcome"},{"from":"imperialism","to":"outcome"},{"from":"nationalism","to":"outcome"},{"from":"assassination-trigger","to":"outcome"},{"from":"nationalism","to":"assassination-trigger"}]` */
  edges?: EntityRow[];
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#15803d"` */
  noWarColor?: string;
  /** Default: `46` */
  outcomeRadius?: number;
  /** Default: `330` */
  outcomeX?: number;
  /** Default: `360` */
  outcomeY?: number;
  /** Default: `"#a8a29e"` */
  removedCauseColor?: string;
  /** Default: `60` */
  threshold?: number;
  /** Default: `"Causal Chain"` */
  title?: string;
  /** Default: `"#b91c1c"` */
  warColor?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-hist-causalchain descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdHistCausalchainParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdHistCausalchainEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdHistCausalchainConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `HistCausalchain.traits.HistCausalChainSim`. */
export function stdHistCausalchainTrait(params: StdHistCausalchainParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.HistCausalChainSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `HistCausalchain.pages.HistCausalChainPage`. */
export function stdHistCausalchainPage(params: StdHistCausalchainParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.HistCausalChainPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdHistCausalchain(params: StdHistCausalchainParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'HistCausalChainOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdHistCausalchainTrait(params),
    ],
    pages: [
      stdHistCausalchainPage(params),
    ],
  });
}
