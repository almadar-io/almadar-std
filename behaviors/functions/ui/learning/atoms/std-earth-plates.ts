/**
 * std-earth-plates — Plate-tectonics boundary simulator — the learner picks a convergent/divergent/transform boundary and advances motion to see landforms and earthquakes accumulate, on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-earth-plates.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-earth-plates
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-earth-plates';
const ALIAS = 'EarthPlates';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdEarthPlatesEventKey = 'ADVANCE' | 'INIT' | 'MODE_SELECTED' | 'PLATES_UPDATED' | 'SELECT_BOUNDARY';

/**
 * Payload shape for the `PLATES_UPDATED` event.
 */
export interface StdEarthPlatesPlatesUpdatedPayload {
  boundaryType?: string;
  advanceSteps?: number;
  landformLabel?: string;
  quakeProfileLabel?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdEarthPlatesConfig {
  /** Default: `5` */
  advanceStepYears?: number;
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f0f9ff"` */
  backgroundColor?: string;
  /** Default: `[{"description":"Two plates move toward each other; the denser oceanic plate subducts beneath the other.","label":"Convergent","landform":"Subduction zone — volcanic arc and mountain range","quakeProfile":"Deep, inclined seismicity along the subducting slab (Wadati-Benioff zone)","type":"convergent"},{"description":"Two plates move apart; magma rises to fill the gap, forming new crust.","label":"Divergent","landform":"Rift valley / mid-ocean ridge with new oceanic crust","quakeProfile":"Shallow seismicity concentrated along the rift axis","type":"divergent"},{"description":"Two plates slide past each other horizontally; no crust is created or destroyed.","label":"Transform","landform":"Transform fault with lateral (strike-slip) offset","quakeProfile":"Shallow, strike-slip seismicity along the fault trace","type":"transform"}]` */
  boundaries?: EntityRow[];
  /** Default: `3` */
  convergeRatePixels?: number;
  /** Default: `"#f97316"` */
  crustNewColor?: string;
  /** Default: `4` */
  divergeRatePixels?: number;
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `8` */
  maxAdvanceSteps?: number;
  /** Default: `"#92400e"` */
  plateColorLeft?: string;
  /** Default: `"#78350f"` */
  plateColorRight?: string;
  /** Default: `26` */
  plateGapHalf?: number;
  /** Default: `"#1e293b"` */
  quakeColor?: string;
  /** Default: `6` */
  shearRatePixels?: number;
  /** Default: `"Earth Lab"` */
  title?: string;
  /** Default: `10` */
  transformQuakeSpacing?: number;
  /** Default: `"#dc2626"` */
  volcanoColor?: string;
  /** Default: `3` */
  volcanoThresholdSteps?: number;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-earth-plates descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdEarthPlatesParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdEarthPlatesEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdEarthPlatesConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `EarthPlates.traits.EarthPlatesSim`. */
export function stdEarthPlatesTrait(params: StdEarthPlatesParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.EarthPlatesSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `EarthPlates.pages.EarthPlatesPage`. */
export function stdEarthPlatesPage(params: StdEarthPlatesParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.EarthPlatesPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdEarthPlates(params: StdEarthPlatesParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'EarthPlatesOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdEarthPlatesTrait(params),
    ],
    pages: [
      stdEarthPlatesPage(params),
    ],
  });
}
