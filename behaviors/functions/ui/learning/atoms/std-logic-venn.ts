/**
 * std-logic-venn — Syllogism Venn tester — the learner shades regions and drops ✕ marks on a three-circle diagram for each premise of a categorical syllogism (All M are P, Some S are M), then tests whether the shaded diagram makes the conclusion valid. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-logic-venn.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-logic-venn
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-logic-venn';
const ALIAS = 'LogicVenn';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLogicVennEventKey = 'INIT' | 'MARK_X' | 'MODE_SELECTED' | 'RESET' | 'SHADE_REGION' | 'TEST_VALIDITY' | 'VENN_UPDATED';

/**
 * Payload shape for the `VENN_UPDATED` event.
 */
export interface StdLogicVennVennUpdatedPayload {
  shadedRegions?: string[];
  markedRegions?: string[];
  verdict?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLogicVennConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#2563eb","id":"S","radius":105,"x":240,"y":170},{"color":"#15803d","id":"M","radius":105,"x":360,"y":170},{"color":"#b45309","id":"P","radius":105,"x":300,"y":275}]` */
  circles?: EntityRow[];
  /** Default: `1` */
  figure?: number;
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"AII"` */
  mood?: string;
  /** Default: `["All M are P","Some S are M"]` */
  premiseTypes?: string[];
  /** Default: `[{"key":"S","label":"S"},{"key":"M","label":"M"},{"key":"P","label":"P"}]` */
  propositionLabels?: EntityRow[];
  /** Default: `[{"id":"s-only","label":"S only","x":175,"y":150},{"id":"m-only","label":"M only","x":425,"y":150},{"id":"p-only","label":"P only","x":300,"y":360},{"id":"sm-only","label":"S∩M only","x":300,"y":140},{"id":"sp-only","label":"S∩P only","x":245,"y":255},{"id":"mp-only","label":"M∩P only","x":355,"y":255},{"id":"smp","label":"S∩M∩P","x":300,"y":210}]` */
  regions?: EntityRow[];
  /** Default: `"#64748b"` */
  shadedColor?: string;
  /** Default: `true` */
  showSolution?: boolean;
  /** Default: `{"marked":["smp"],"note":"Shading all of M outside P and marking S∩M forces the ✕ into S∩M∩P — so some S are P.","shaded":["m-only","sm-only"],"valid":true}` */
  solution?: EntityRow;
  /** Default: `true` */
  strictBoolean?: boolean;
  /** Default: `"Syllogism Venn Tester"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-logic-venn descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLogicVennParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLogicVennEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLogicVennConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LogicVenn.traits.LogicVennSim`. */
export function stdLogicVennTrait(params: StdLogicVennParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LogicVennSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LogicVenn.pages.LogicVennPage`. */
export function stdLogicVennPage(params: StdLogicVennParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LogicVennPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLogicVenn(params: StdLogicVennParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LogicVennOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLogicVennTrait(params),
    ],
    pages: [
      stdLogicVennPage(params),
    ],
  });
}
