/**
 * std-civics-electoral — Electoral-college sandbox — flip swing states and watch Red/Blue electoral-vote totals race the 270 threshold, with a popular-vs-electoral split readout, on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-civics-electoral.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-civics-electoral
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-civics-electoral';
const ALIAS = 'CivicsElectoral';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCivicsElectoralEventKey = 'ELECTORAL_UPDATED' | 'INIT' | 'LOAD_SCENARIO' | 'MODE_SELECTED' | 'RESET' | 'TOGGLE_STATE';

/**
 * Payload shape for the `ELECTORAL_UPDATED` event.
 */
export interface StdCivicsElectoralElectoralUpdatedPayload {
  redEV?: number;
  blueEV?: number;
  tossupEV?: number;
  redPop?: number;
  bluePop?: number;
  splitNote?: string;
  scenarioName?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCivicsElectoralConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `[{"id":"CA","winner":"blue"},{"id":"TX","winner":"red"},{"id":"FL","winner":"red"},{"id":"NY","winner":"blue"},{"id":"PA","winner":"blue"},{"id":"OH","winner":"red"},{"id":"GA","winner":"red"},{"id":"NC","winner":"red"},{"id":"MI","winner":"blue"},{"id":"AZ","winner":"red"},{"id":"WI","winner":"blue"},{"id":"CO","winner":"blue"},{"id":"OR","winner":"red"},{"id":"OB","winner":"blue"}]` */
  baselineMap?: EntityRow[];
  /** Default: `"#2563eb"` */
  blueColor?: string;
  /** Default: `[{"ev":54,"id":"CA"},{"ev":40,"id":"TX"},{"ev":30,"id":"FL"},{"ev":28,"id":"NY"},{"ev":19,"id":"PA"},{"ev":17,"id":"OH"},{"ev":16,"id":"GA"},{"ev":16,"id":"NC"},{"ev":15,"id":"MI"},{"ev":11,"id":"AZ"},{"ev":10,"id":"WI"},{"ev":10,"id":"CO"},{"ev":136,"id":"OR"},{"ev":136,"id":"OB"}]` */
  evCounts?: EntityRow[];
  /** Default: `430` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#dc2626"` */
  redColor?: string;
  /** Default: `[{"index":0,"map":[{"id":"CA","winner":"blue"},{"id":"TX","winner":"red"},{"id":"FL","winner":"tossup"},{"id":"NY","winner":"blue"},{"id":"PA","winner":"tossup"},{"id":"OH","winner":"red"},{"id":"GA","winner":"tossup"},{"id":"NC","winner":"tossup"},{"id":"MI","winner":"tossup"},{"id":"AZ","winner":"tossup"},{"id":"WI","winner":"tossup"},{"id":"CO","winner":"blue"},{"id":"OR","winner":"red"},{"id":"OB","winner":"blue"}],"name":"Tossup reset"},{"index":1,"map":[{"id":"CA","winner":"blue"},{"id":"TX","winner":"red"},{"id":"FL","winner":"red"},{"id":"NY","winner":"blue"},{"id":"PA","winner":"red"},{"id":"OH","winner":"red"},{"id":"GA","winner":"red"},{"id":"NC","winner":"red"},{"id":"MI","winner":"red"},{"id":"AZ","winner":"red"},{"id":"WI","winner":"red"},{"id":"CO","winner":"blue"},{"id":"OR","winner":"red"},{"id":"OB","winner":"blue"}],"name":"Red sweep"}]` */
  scenarios?: EntityRow[];
  /** Default: `[{"col":0,"id":"CA","name":"California","popShare":11.7,"row":0,"swing":false},{"col":1,"id":"TX","name":"Texas","popShare":8.9,"row":0,"swing":false},{"col":2,"id":"FL","name":"Florida","popShare":7.6,"row":0,"swing":true},{"col":3,"id":"NY","name":"New York","popShare":7.8,"row":0,"swing":false},{"col":0,"id":"PA","name":"Pennsylvania","popShare":6.5,"row":1,"swing":true},{"col":1,"id":"OH","name":"Ohio","popShare":5.6,"row":1,"swing":false},{"col":2,"id":"GA","name":"Georgia","popShare":5.3,"row":1,"swing":true},{"col":3,"id":"NC","name":"North Carolina","popShare":5.4,"row":1,"swing":true},{"col":0,"id":"MI","name":"Michigan","popShare":5.4,"row":2,"swing":true},{"col":1,"id":"AZ","name":"Arizona","popShare":3.4,"row":2,"swing":true},{"col":2,"id":"WI","name":"Wisconsin","popShare":3.2,"row":2,"swing":true},{"col":3,"id":"CO","name":"Colorado","popShare":2.9,"row":2,"swing":false},{"col":0,"id":"OR","name":"Other safe Red","popShare":13,"row":3,"swing":false},{"col":1,"id":"OB","name":"Other safe Blue","popShare":13.3,"row":3,"swing":false}]` */
  states?: EntityRow[];
  /** Default: `270` */
  threshold?: number;
  /** Default: `"Civics Lab"` */
  title?: string;
  /** Default: `"#94a3b8"` */
  tossupColor?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-civics-electoral descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCivicsElectoralParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCivicsElectoralEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCivicsElectoralConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CivicsElectoral.traits.CivicsElectoralSim`. */
export function stdCivicsElectoralTrait(params: StdCivicsElectoralParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CivicsElectoralSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CivicsElectoral.pages.CivicsElectoralPage`. */
export function stdCivicsElectoralPage(params: StdCivicsElectoralParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CivicsElectoralPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCivicsElectoral(params: StdCivicsElectoralParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CivicsElectoralOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCivicsElectoralTrait(params),
    ],
    pages: [
      stdCivicsElectoralPage(params),
    ],
  });
}
