/**
 * std-geo-vonthunen — Von Thünen bid-rent engine — each crop draws a bid-rent line (rent versus distance to market) on a math-canvas; the transport-cost and market-price dials shift every line's slope and intercept, and the concentric ring boundaries (line intersections) redraw live. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geo-vonthunen.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geo-vonthunen
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geo-vonthunen';
const ALIAS = 'GeoVonthunen';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeoVonthunenEventKey = 'INIT' | 'MODE_SELECTED' | 'RESET' | 'SET_COST' | 'SET_PRICE' | 'VONTHUNEN_UPDATED';

/**
 * Payload shape for the `VONTHUNEN_UPDATED` event.
 */
export interface StdGeoVonthunenVonthunenUpdatedPayload {
  transportCost?: number;
  marketPrice?: number;
  ringBoundaries?: number[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeoVonthunenConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#94a3b8"` */
  axisColor?: string;
  /** Default: `"#7c3aed"` */
  boundaryColor?: string;
  /** Default: `[{"basePrice":60,"color":"#16a34a","fixedCost":50,"id":"horticulture","label":"Market Gardening","transportRate":40,"yieldPerHa":10},{"basePrice":50,"color":"#f59e0b","fixedCost":40,"id":"dairy","label":"Dairy","transportRate":25,"yieldPerHa":8},{"basePrice":40,"color":"#a16207","fixedCost":30,"id":"grain","label":"Grain","transportRate":10,"yieldPerHa":6},{"basePrice":30,"color":"#7c2d12","fixedCost":20,"id":"ranching","label":"Ranching","transportRate":3,"yieldPerHa":4}]` */
  crops?: EntityRow[];
  /** Default: `30` */
  distanceAxisMax?: number;
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `1` */
  marketPrices?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `600` */
  rentAxisMax?: number;
  /** Default: `"#1e293b"` */
  textColor?: string;
  /** Default: `"Von Thünen Bid-Rent Rings"` */
  title?: string;
  /** Default: `1` */
  transportCosts?: number;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-geo-vonthunen descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeoVonthunenParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeoVonthunenEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeoVonthunenConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GeoVonthunen.traits.GeoVonThunenSim`. */
export function stdGeoVonthunenTrait(params: StdGeoVonthunenParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoVonThunenSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GeoVonthunen.pages.GeoVonThunenPage`. */
export function stdGeoVonthunenPage(params: StdGeoVonthunenParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoVonThunenPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeoVonthunen(params: StdGeoVonthunenParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoVonThunenOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeoVonthunenTrait(params),
    ],
    pages: [
      stdGeoVonthunenPage(params),
    ],
  });
}
