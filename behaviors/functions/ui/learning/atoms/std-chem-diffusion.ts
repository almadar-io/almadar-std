/**
 * std-chem-diffusion — Diffusion engine — two color-coded gases random-walk toward each other, mixing to illustrate entropy.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-chem-diffusion.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-chem-diffusion
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-chem-diffusion';
const ALIAS = 'ChemDiffusion';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdChemDiffusionEventKey = 'ATOMS_UPDATED' | 'INIT';

/**
 * Payload shape for the `ATOMS_UPDATED` event.
 */
export interface StdChemDiffusionAtomsUpdatedPayload {
  atoms?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdChemDiffusionConfig {
  /** Default: `[{"color":"#dc2626","element":"A","id":"a1","radius":10,"x":60,"y":60},{"color":"#dc2626","element":"A","id":"a2","radius":10,"x":100,"y":100},{"color":"#dc2626","element":"A","id":"a3","radius":10,"x":60,"y":140},{"color":"#dc2626","element":"A","id":"a4","radius":10,"x":100,"y":180},{"color":"#dc2626","element":"A","id":"a5","radius":10,"x":60,"y":220},{"color":"#dc2626","element":"A","id":"a6","radius":10,"x":100,"y":260},{"color":"#dc2626","element":"A","id":"a7","radius":10,"x":60,"y":300},{"color":"#dc2626","element":"A","id":"a8","radius":10,"x":100,"y":340},{"color":"#2563eb","element":"B","id":"b1","radius":10,"x":540,"y":60},{"color":"#2563eb","element":"B","id":"b2","radius":10,"x":500,"y":100},{"color":"#2563eb","element":"B","id":"b3","radius":10,"x":540,"y":140},{"color":"#2563eb","element":"B","id":"b4","radius":10,"x":500,"y":180},{"color":"#2563eb","element":"B","id":"b5","radius":10,"x":540,"y":220},{"color":"#2563eb","element":"B","id":"b6","radius":10,"x":500,"y":260},{"color":"#2563eb","element":"B","id":"b7","radius":10,"x":540,"y":300},{"color":"#2563eb","element":"B","id":"b8","radius":10,"x":500,"y":340}]` */
  atoms?: EntityRow[];
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `true` */
  running?: boolean;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-chem-diffusion descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdChemDiffusionParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdChemDiffusionEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdChemDiffusionConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ChemDiffusion.traits.ChemDiffusionSim`. */
export function stdChemDiffusionTrait(params: StdChemDiffusionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ChemDiffusionSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ChemDiffusion.pages.ChemDiffusionEnginePage`. */
export function stdChemDiffusionPage(params: StdChemDiffusionParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ChemDiffusionEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdChemDiffusion(params: StdChemDiffusionParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ChemDiffusionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdChemDiffusionTrait(params),
    ],
    pages: [
      stdChemDiffusionPage(params),
    ],
  });
}
