/**
 * std-astro-stellar — Stellar life-cycle simulator — the learner picks a starting mass via slider or preset buttons and steps through the star's evolutionary stages from nebula to its predicted end state (white dwarf, neutron star, or black hole), on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-astro-stellar.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-astro-stellar
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-astro-stellar';
const ALIAS = 'AstroStellar';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAstroStellarEventKey = 'INIT' | 'MODE_SELECTED' | 'RESET_STAGE' | 'SET_MASS' | 'STELLAR_UPDATED' | 'STEP_STAGE';

/**
 * Payload shape for the `STELLAR_UPDATED` event.
 */
export interface StdAstroStellarStellarUpdatedPayload {
  initialMass?: number;
  currentStageIndex?: number;
  stageLabel?: string;
  pathEndLabel?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAstroStellarConfig {
  /** Default: `"#0f172a"` */
  backgroundColor?: string;
  /** Default: `460` */
  height?: number;
  /** Default: `1` */
  initialMass?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `[0.5,1,8,20]` */
  massPresets?: number[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"endLabel":"White Dwarf","maxMass":7.9,"stages":[{"color":"#94a3b8","description":"A vast cloud of gas and dust collapses under its own gravity, and a protostar begins to glow at its center.","label":"Nebula","radius":95},{"color":"#facc15","description":"Hydrogen fuses into helium in the core, and the star shines steadily for billions of years — this is where our Sun is now.","label":"Main Sequence","radius":42},{"color":"#f97316","description":"The core runs out of hydrogen and contracts while the outer layers swell and cool into a huge red envelope.","label":"Red Giant","radius":100},{"color":"#e0f2fe","description":"The outer layers drift away as a planetary nebula, leaving behind a hot, dense stellar remnant that slowly cools and fades.","label":"White Dwarf","radius":16}]},{"endLabel":"Neutron Star","maxMass":19.9,"stages":[{"color":"#94a3b8","description":"A vast cloud of gas and dust collapses under its own gravity, forming a protostar far more massive than the Sun.","label":"Nebula","radius":110},{"color":"#93c5fd","description":"The star burns through hydrogen far faster than a Sun-like star, shining bright blue-white.","label":"Main Sequence","radius":55},{"color":"#dc2626","description":"Heavier elements fuse in shells around an iron core, until fusion can no longer release energy.","label":"Red Supergiant","radius":130},{"color":"#fef08a","description":"The core collapses in an instant and rebounds, blasting the star's outer layers into space in a brilliant explosion.","label":"Supernova","radius":150},{"color":"#a78bfa","description":"The crushed core survives as an ultra-dense sphere of neutrons, often spinning rapidly as a pulsar.","label":"Neutron Star","radius":10}]},{"endLabel":"Black Hole","maxMass":999,"stages":[{"color":"#94a3b8","description":"A vast cloud of gas and dust collapses under its own gravity, forming an extremely massive protostar.","label":"Nebula","radius":130},{"color":"#60a5fa","description":"The star burns its fuel at a furious rate, glowing intensely blue and living only a few million years.","label":"Main Sequence","radius":65},{"color":"#3b82f6","description":"The star swells into an enormous, luminous supergiant while fusing progressively heavier elements in its core.","label":"Blue Supergiant","radius":150},{"color":"#fef08a","description":"The iron core collapses catastrophically and the star detonates, briefly outshining an entire galaxy.","label":"Supernova","radius":170},{"color":"#020617","description":"Gravity overwhelms every other force, and the collapsed core forms a black hole from which not even light can escape.","label":"Black Hole","radius":14}]}]` */
  stellarPaths?: EntityRow[];
  /** Default: `"Star Life-Cycle Simulator"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-astro-stellar descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAstroStellarParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAstroStellarEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAstroStellarConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AstroStellar.traits.AstroStellarSim`. */
export function stdAstroStellarTrait(params: StdAstroStellarParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AstroStellarSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AstroStellar.pages.AstroStellarPage`. */
export function stdAstroStellarPage(params: StdAstroStellarParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AstroStellarPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAstroStellar(params: StdAstroStellarParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'AstroStellarOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAstroStellarTrait(params),
    ],
    pages: [
      stdAstroStellarPage(params),
    ],
  });
}
