/**
 * std-chem-osmosis — Osmosis engine — water random-walks freely while trapped solute stays right of a semipermeable membrane, so water net-crosses toward the solute to equalize concentration.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-chem-osmosis.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-chem-osmosis
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-chem-osmosis';
const ALIAS = 'ChemOsmosis';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdChemOsmosisEventKey = 'ATOMS_UPDATED' | 'INIT';

/**
 * Payload shape for the `ATOMS_UPDATED` event.
 */
export interface StdChemOsmosisAtomsUpdatedPayload {
  atoms?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdChemOsmosisConfig {
  /** Default: `[{"color":"#7c3aed","element":"S","id":"s1","radius":13,"x":360,"y":80},{"color":"#7c3aed","element":"S","id":"s2","radius":13,"x":480,"y":130},{"color":"#7c3aed","element":"S","id":"s3","radius":13,"x":400,"y":200},{"color":"#7c3aed","element":"S","id":"s4","radius":13,"x":520,"y":260},{"color":"#7c3aed","element":"S","id":"s5","radius":13,"x":360,"y":320},{"color":"#7c3aed","element":"S","id":"s6","radius":13,"x":470,"y":360},{"color":"#2563eb","element":"W","id":"w1","radius":6,"x":60,"y":60},{"color":"#2563eb","element":"W","id":"w2","radius":6,"x":140,"y":120},{"color":"#2563eb","element":"W","id":"w3","radius":6,"x":90,"y":200},{"color":"#2563eb","element":"W","id":"w4","radius":6,"x":200,"y":280},{"color":"#2563eb","element":"W","id":"w5","radius":6,"x":120,"y":340},{"color":"#2563eb","element":"W","id":"w6","radius":6,"x":240,"y":80},{"color":"#2563eb","element":"W","id":"w7","radius":6,"x":260,"y":180},{"color":"#2563eb","element":"W","id":"w8","radius":6,"x":180,"y":360},{"color":"#2563eb","element":"W","id":"w9","radius":6,"x":360,"y":120},{"color":"#2563eb","element":"W","id":"w10","radius":6,"x":440,"y":300},{"color":"#2563eb","element":"W","id":"w11","radius":6,"x":520,"y":180},{"color":"#2563eb","element":"W","id":"w12","radius":6,"x":400,"y":60}]` */
  atoms?: EntityRow[];
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `true` */
  running?: boolean;
  /** Default: `[{"color":"#64748b","id":"membrane","lineWidth":4,"type":"line","x1":300,"x2":300,"y1":20,"y2":380}]` */
  shapes?: EntityRow[];
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-chem-osmosis descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdChemOsmosisParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdChemOsmosisEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdChemOsmosisConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ChemOsmosis.traits.ChemOsmosisSim`. */
export function stdChemOsmosisTrait(params: StdChemOsmosisParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ChemOsmosisSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ChemOsmosis.pages.ChemOsmosisEnginePage`. */
export function stdChemOsmosisPage(params: StdChemOsmosisParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ChemOsmosisEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdChemOsmosis(params: StdChemOsmosisParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ChemOsmosisEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdChemOsmosisTrait(params),
    ],
    pages: [
      stdChemOsmosisPage(params),
    ],
  });
}
