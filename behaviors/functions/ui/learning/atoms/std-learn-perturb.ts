/**
 * std-learn-perturb — Generic equilibrium-and-shock simulation — a single named quantity relaxes each tick toward an adjustable setpoint at an adjustable recovery rate (dV = k·(E − V)), drawn live against the setpoint line. Shock buttons knock the quantity up or down mid-run and the learner watches it recover; moving the setpoint slider mid-run makes the system chase a new equilibrium, and the recovery-rate slider shows sluggish versus snappy regulation. All names and scales are config-declared, so one mechanism serves every negative-feedback lesson: body temperature homeostasis, a thermostat, market price around equilibrium, blood glucose, reservoir level, ecological resilience. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-perturb.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-perturb
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-perturb';
const ALIAS = 'LearnPerturb';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnPerturbEventKey = 'INIT' | 'MODE_SELECTED' | 'PAUSE' | 'RATE_CHANGED' | 'RESET' | 'RUN' | 'RUN_TOGGLED' | 'SETPOINT_CHANGED' | 'SET_POINT' | 'SET_RATE' | 'SHOCK' | 'SHOCK_APPLIED' | 'SIM_RESET';

/**
 * Payload shape for the `RUN_TOGGLED` event.
 */
export interface StdLearnPerturbRunToggledPayload {
  running?: boolean;
}

/**
 * Payload shape for the `SIM_RESET` event.
 */
export interface StdLearnPerturbSimResetPayload {
  valueStart?: number;
}

/**
 * Payload shape for the `SETPOINT_CHANGED` event.
 */
export interface StdLearnPerturbSetpointChangedPayload {
  setpoint?: number;
}

/**
 * Payload shape for the `RATE_CHANGED` event.
 */
export interface StdLearnPerturbRateChangedPayload {
  rate?: number;
}

/**
 * Payload shape for the `SHOCK_APPLIED` event.
 */
export interface StdLearnPerturbShockAppliedPayload {
  delta?: number;
  value?: number;
  deviation?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnPerturbConfig {
  /** Default: `true` */
  autoRun?: boolean;
  /** Default: `20` */
  duration?: number;
  /** Default: `2` */
  gridStep?: number;
  /** Default: `360` */
  height?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `0.8` */
  rateDefault?: number;
  /** Default: `"Recovery rate"` */
  rateLabel?: string;
  /** Default: `3` */
  rateMax?: number;
  /** Default: `0.05` */
  rateMin?: number;
  /** Default: `0.05` */
  rateStep?: number;
  /** Default: `"#94a3b8"` */
  setpointColor?: string;
  /** Default: `37` */
  setpointDefault?: number;
  /** Default: `"Setpoint"` */
  setpointLabel?: string;
  /** Default: `44` */
  setpointMax?: number;
  /** Default: `30` */
  setpointMin?: number;
  /** Default: `0.5` */
  setpointStep?: number;
  /** Default: `"− Shock down"` */
  shockDownLabel?: string;
  /** Default: `4` */
  shockSize?: number;
  /** Default: `"+ Shock up"` */
  shockUpLabel?: string;
  /** Default: `"Run the system, shock it off its setpoint, and watch it recover — then move the setpoint mid-run."` */
  taskText?: string;
  /** Default: `"Equilibrium & Shock"` */
  title?: string;
  /** Default: `"#dc2626"` */
  valueColor?: string;
  /** Default: `"Temperature"` */
  valueLabel?: string;
  /** Default: `46` */
  valueMax?: number;
  /** Default: `28` */
  valueMin?: number;
  /** Default: `37` */
  valueStart?: number;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-learn-perturb descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnPerturbParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnPerturbEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnPerturbConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnPerturb.traits.PerturbSim`. */
export function stdLearnPerturbTrait(params: StdLearnPerturbParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PerturbSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnPerturb.pages.PerturbPage`. */
export function stdLearnPerturbPage(params: StdLearnPerturbParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.PerturbPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnPerturb(params: StdLearnPerturbParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'PerturbOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnPerturbTrait(params),
    ],
    pages: [
      stdLearnPerturbPage(params),
    ],
  });
}
