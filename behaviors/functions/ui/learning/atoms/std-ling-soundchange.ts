/**
 * std-ling-soundchange — Historical sound-change stepper — the learner applies ordered Great Vowel Shift rules one at a time and watches every word whose vowel matches strike all at once, rendering the before/after pronunciation on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-ling-soundchange.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-ling-soundchange
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-ling-soundchange';
const ALIAS = 'LingSoundchange';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLingSoundchangeEventKey = 'APPLY_CHANGE' | 'INIT' | 'MODE_SELECTED' | 'RESET_CHANGES' | 'SOUNDCHANGE_UPDATED';

/**
 * Payload shape for the `SOUNDCHANGE_UPDATED` event.
 */
export interface StdLingSoundchangeSoundchangeUpdatedPayload {
  ruleIndex?: number;
  totalRules?: number;
  changed?: string;
  done?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLingSoundchangeConfig {
  /** Default: `"#f97316"` */
  arrowColor?: string;
  /** Default: `270` */
  arrowX1?: number;
  /** Default: `390` */
  arrowX2?: number;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `"#dc2626"` */
  changedPronunciationColor?: string;
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `400` */
  newPronColumnX?: number;
  /** Default: `40` */
  orthColumnX?: number;
  /** Default: `"#0f172a"` */
  orthographyColor?: string;
  /** Default: `200` */
  pronColumnX?: number;
  /** Default: `"#64748b"` */
  pronunciationColor?: string;
  /** Default: `56` */
  rowHeight?: number;
  /** Default: `[{"explanation":"Middle English long high front vowel /iː/ diphthongizes to /aɪ/, as in modern \"bite\" and \"time\".","from":"iː","name":"Great Vowel Shift — /iː/ diphthongizes","to":"aɪ"},{"explanation":"Middle English long high back vowel /uː/ diphthongizes to /aʊ/, as in modern \"mouse\" and \"house\".","from":"uː","name":"Great Vowel Shift — /uː/ diphthongizes","to":"aʊ"},{"explanation":"Middle English long mid back vowel /oː/ raises to /uː/, as in modern \"goose\".","from":"oː","name":"Great Vowel Shift — /oː/ raises","to":"uː"}]` */
  rules?: EntityRow[];
  /** Default: `18` */
  soundFontSize?: number;
  /** Default: `[{"id":"w1","orthography":"bite","template":"b{V}t","vowel":"iː"},{"id":"w2","orthography":"time","template":"t{V}m","vowel":"iː"},{"id":"w3","orthography":"mouse","template":"m{V}s","vowel":"uː"},{"id":"w4","orthography":"house","template":"h{V}s","vowel":"uː"},{"id":"w5","orthography":"goose","template":"g{V}s","vowel":"oː"},{"id":"w6","orthography":"feet","template":"f{V}t","vowel":"eː"}]` */
  soundWords?: EntityRow[];
  /** Default: `"Sound Change Stepper"` */
  title?: string;
  /** Default: `40` */
  topMargin?: number;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-ling-soundchange descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLingSoundchangeParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLingSoundchangeEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLingSoundchangeConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LingSoundchange.traits.LingSoundchangeSim`. */
export function stdLingSoundchangeTrait(params: StdLingSoundchangeParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LingSoundchangeSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LingSoundchange.pages.LingSoundchangePage`. */
export function stdLingSoundchangePage(params: StdLingSoundchangeParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LingSoundchangePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLingSoundchange(params: StdLingSoundchangeParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LingSoundchangeOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLingSoundchangeTrait(params),
    ],
    pages: [
      stdLingSoundchangePage(params),
    ],
  });
}
