/**
 * std-math-quadratic — Parabola explorer — a/b/c sliders reshape y = ax² + bx + c live; the vertex, axis of symmetry, roots and discriminant readouts recompute, and the discriminant's sign drives the root display. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-math-quadratic.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-math-quadratic
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-math-quadratic';
const ALIAS = 'MathQuadratic';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMathQuadraticEventKey = 'INIT' | 'MODE_SELECTED' | 'QUADRATIC_UPDATED' | 'RESET' | 'SET_A' | 'SET_B' | 'SET_C';

/**
 * Payload shape for the `QUADRATIC_UPDATED` event.
 */
export interface StdMathQuadraticQuadraticUpdatedPayload {
  a?: number;
  b?: number;
  c?: number;
  vertexX?: number;
  vertexY?: number;
  discriminant?: number;
  rootsLabel?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMathQuadraticConfig {
  /** Default: `"#94a3b8"` */
  axisColor?: string;
  /** Default: `"#2563eb"` */
  curveColor?: string;
  /** Default: `"#9333ea"` */
  focusColor?: string;
  /** Default: `"standard"` */
  form?: 'standard' | 'vertex' | 'factored';
  /** Default: `1` */
  gridStep?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `1` */
  initialA?: number;
  /** Default: `0` */
  initialB?: number;
  /** Default: `0` */
  initialC?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#16a34a"` */
  rootColor?: string;
  /** Default: `[-10,-9.5,-9,-8.5,-8,-7.5,-7,-6.5,-6,-5.5,-5,-4.5,-4,-3.5,-3,-2.5,-2,-1.5,-1,-0.5,0,0.5,1,1.5,2,2.5,3,3.5,4,4.5,5,5.5,6,6.5,7,7.5,8,8.5,9,9.5,10]` */
  sampleXs?: number[];
  /** Default: `true` */
  showAxes?: boolean;
  /** Default: `true` */
  showDiscriminant?: boolean;
  /** Default: `false` */
  showFocus?: boolean;
  /** Default: `true` */
  showGrid?: boolean;
  /** Default: `"Quadratic Explorer"` */
  title?: string;
  /** Default: `"#dc2626"` */
  vertexColor?: string;
  /** Default: `600` */
  width?: number;
  /** Default: `10` */
  xMax?: number;
  /** Default: `-10` */
  xMin?: number;
  /** Default: `10` */
  yMax?: number;
  /** Default: `-10` */
  yMin?: number;
}

/**
 * Params for the std-math-quadratic descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMathQuadraticParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMathQuadraticEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMathQuadraticConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MathQuadratic.traits.MathQuadraticSim`. */
export function stdMathQuadraticTrait(params: StdMathQuadraticParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MathQuadraticSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MathQuadratic.pages.MathQuadraticPage`. */
export function stdMathQuadraticPage(params: StdMathQuadraticParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MathQuadraticPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMathQuadratic(params: StdMathQuadraticParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'MathQuadraticOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMathQuadraticTrait(params),
    ],
    pages: [
      stdMathQuadraticPage(params),
    ],
  });
}
