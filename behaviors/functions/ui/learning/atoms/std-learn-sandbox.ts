/**
 * std-learn-sandbox — Generic toggle-a-system→consequence instrument — a config-declared roster of elements (each with a weight and a default enabled state) sits on a biology-canvas connected by config-declared links; each element has a toggle button, and toggling recomputes a derived total (the summed weight of enabled elements) against a config-declared threshold, with a readout stating whether the system currently holds. Links to or from a disabled element render dimmed. Reset restores every element to its config-declared default. Element and link vocabulary is entirely config-declared, so the same mechanism serves any threshold-system exploration (coalition building, load balancing, ecosystem tipping points, circuit continuity) standalone. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-sandbox.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-sandbox
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-sandbox';
const ALIAS = 'LearnSandbox';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnSandboxEventKey = 'INIT' | 'MODE_SELECTED' | 'RESET' | 'SANDBOX_RESET' | 'SYSTEM_CHANGED' | 'TOGGLE';

/**
 * Payload shape for the `SYSTEM_CHANGED` event.
 */
export interface StdLearnSandboxSystemChangedPayload {
  total?: number;
  holds?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnSandboxConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `"#cbd5e1"` */
  disabledColor?: string;
  /** Default: `"#e2e8f0"` */
  disabledEdgeColor?: string;
  /** Default: `[{"enabled":true,"id":"a","label":"Module A","weight":15,"x":100,"y":80},{"enabled":true,"id":"b","label":"Module B","weight":20,"x":300,"y":60},{"enabled":false,"id":"c","label":"Module C","weight":10,"x":500,"y":80},{"enabled":true,"id":"d","label":"Module D","weight":25,"x":200,"y":220},{"enabled":true,"id":"e","label":"Module E","weight":18,"x":420,"y":220}]` */
  elements?: EntityRow[];
  /** Default: `"#2563eb"` */
  enabledColor?: string;
  /** Default: `"#1e3a8a"` */
  enabledEdgeColor?: string;
  /** Default: `320` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `[{"from":"a","to":"b"},{"from":"b","to":"c"},{"from":"a","to":"d"},{"from":"d","to":"e"}]` */
  links?: EntityRow[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `34` */
  nodeRadius?: number;
  /** Default: `"System Status"` */
  outcomeLabel?: string;
  /** Default: `"Reset"` */
  resetLabel?: string;
  /** Default: `60` */
  threshold?: number;
  /** Default: `"System Sandbox"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-learn-sandbox descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnSandboxParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnSandboxEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnSandboxConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnSandbox.traits.LearnSandboxSim`. */
export function stdLearnSandboxTrait(params: StdLearnSandboxParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LearnSandboxSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnSandbox.pages.LearnSandboxPage`. */
export function stdLearnSandboxPage(params: StdLearnSandboxParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnSandboxPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnSandbox(params: StdLearnSandboxParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnSandboxOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnSandboxTrait(params),
    ],
    pages: [
      stdLearnSandboxPage(params),
    ],
  });
}
