/**
 * std-algo-toposort — Topological sort engine (Kahn) — each tick emits the first node whose predecessors are all ordered, recolours the DAG on biology-canvas.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-algo-toposort.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-algo-toposort
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-algo-toposort';
const ALIAS = 'AlgoToposort';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAlgoToposortEventKey = 'INIT' | 'NODES_UPDATED';

/**
 * Payload shape for the `NODES_UPDATED` event.
 */
export interface StdAlgoToposortNodesUpdatedPayload {
  nodes?: EntityRow[];
  edges?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAlgoToposortConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#9ca3af","from":"A","label":"","to":"C"},{"color":"#9ca3af","from":"B","label":"","to":"C"},{"color":"#9ca3af","from":"C","label":"","to":"D"},{"color":"#9ca3af","from":"C","label":"","to":"E"},{"color":"#9ca3af","from":"D","label":"","to":"F"},{"color":"#9ca3af","from":"E","label":"","to":"F"}]` */
  edges?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `[{"color":"#cbd5e1","id":"A","kind":"cell","label":"A","radius":20,"x":150,"y":60},{"color":"#cbd5e1","id":"B","kind":"cell","label":"B","radius":20,"x":450,"y":60},{"color":"#cbd5e1","id":"C","kind":"cell","label":"C","radius":20,"x":300,"y":170},{"color":"#cbd5e1","id":"D","kind":"cell","label":"D","radius":20,"x":180,"y":270},{"color":"#cbd5e1","id":"E","kind":"cell","label":"E","radius":20,"x":420,"y":270},{"color":"#cbd5e1","id":"F","kind":"cell","label":"F","radius":20,"x":300,"y":360}]` */
  nodes?: EntityRow[];
  /** Default: `true` */
  running?: boolean;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-algo-toposort descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAlgoToposortParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAlgoToposortEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAlgoToposortConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AlgoToposort.traits.ToposortSim`. */
export function stdAlgoToposortTrait(params: StdAlgoToposortParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ToposortSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AlgoToposort.pages.ToposortEnginePage`. */
export function stdAlgoToposortPage(params: StdAlgoToposortParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ToposortEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAlgoToposort(params: StdAlgoToposortParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ToposortEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAlgoToposortTrait(params),
    ],
    pages: [
      stdAlgoToposortPage(params),
    ],
  });
}
