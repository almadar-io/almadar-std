/**
 * std-learn-coupled-sim — Generic coupled-dynamics simulation — two named quantities evolve each tick under a config-selected interaction family (predator-prey: dP = aP − bPQ, dQ = cbPQ − dQ; epidemic: dS = −aSI, dI = aSI − bI; kinetics: dP = −aP, dQ = aP − bQ), drawn live as two time-series curves while Run is engaged. The two governing rates are exposed as sliders the learner can retune mid-run, and shock buttons inject or remove a burst of the first quantity so the learner feels how the coupled system absorbs disturbance. All names, coefficients, and scales are config-declared, so one mechanism serves rabbits and foxes, infection and recovery, reactant and product, adopters and churn. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-coupled-sim.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-coupled-sim
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-coupled-sim';
const ALIAS = 'LearnCoupledSim';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnCoupledSimEventKey = 'COEFS_CHANGED' | 'INIT' | 'MODE_SELECTED' | 'PAUSE' | 'RESET' | 'RUN' | 'RUN_TOGGLED' | 'SET_A' | 'SET_B' | 'SHOCK' | 'SHOCK_APPLIED' | 'SIM_RESET';

/**
 * Payload shape for the `RUN_TOGGLED` event.
 */
export interface StdLearnCoupledSimRunToggledPayload {
  running?: boolean;
}

/**
 * Payload shape for the `SIM_RESET` event.
 */
export interface StdLearnCoupledSimSimResetPayload {
  pStart?: number;
  qStart?: number;
}

/**
 * Payload shape for the `COEFS_CHANGED` event.
 */
export interface StdLearnCoupledSimCoefsChangedPayload {
  coefA?: number;
  coefB?: number;
}

/**
 * Payload shape for the `SHOCK_APPLIED` event.
 */
export interface StdLearnCoupledSimShockAppliedPayload {
  delta?: number;
  p?: number;
  q?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnCoupledSimConfig {
  /** Default: `true` */
  autoRun?: boolean;
  /** Default: `0.6` */
  coefADefault?: number;
  /** Default: `"Prey growth rate"` */
  coefALabel?: string;
  /** Default: `2` */
  coefAMax?: number;
  /** Default: `0` */
  coefAMin?: number;
  /** Default: `0.01` */
  coefAStep?: number;
  /** Default: `0.9` */
  coefBDefault?: number;
  /** Default: `"Predation rate"` */
  coefBLabel?: string;
  /** Default: `2` */
  coefBMax?: number;
  /** Default: `0` */
  coefBMin?: number;
  /** Default: `0.01` */
  coefBStep?: number;
  /** Default: `0.5` */
  coefC?: number;
  /** Default: `0.4` */
  coefD?: number;
  /** Default: `30` */
  duration?: number;
  /** Default: `50` */
  gridStep?: number;
  /** Default: `360` */
  height?: number;
  /** Default: `"predator-prey"` */
  kind?: string;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"#16a34a"` */
  pColor?: string;
  /** Default: `"Prey"` */
  pLabel?: string;
  /** Default: `120` */
  pStart?: number;
  /** Default: `"#dc2626"` */
  qColor?: string;
  /** Default: `"Predators"` */
  qLabel?: string;
  /** Default: `40` */
  qStart?: number;
  /** Default: `"− Prey cull"` */
  shockDownLabel?: string;
  /** Default: `40` */
  shockSize?: number;
  /** Default: `"+ Prey burst"` */
  shockUpLabel?: string;
  /** Default: `"Run the system, retune the rates, and shock it — watch how the two quantities push and pull each other."` */
  taskText?: string;
  /** Default: `1` */
  timeScale?: number;
  /** Default: `"Coupled System"` */
  title?: string;
  /** Default: `300` */
  valueMax?: number;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-learn-coupled-sim descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnCoupledSimParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnCoupledSimEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnCoupledSimConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnCoupledSim.traits.CoupledSimSim`. */
export function stdLearnCoupledSimTrait(params: StdLearnCoupledSimParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CoupledSimSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnCoupledSim.pages.CoupledSimPage`. */
export function stdLearnCoupledSimPage(params: StdLearnCoupledSimParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CoupledSimPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnCoupledSim(params: StdLearnCoupledSimParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CoupledSimOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnCoupledSimTrait(params),
    ],
    pages: [
      stdLearnCoupledSimPage(params),
    ],
  });
}
