/**
 * std-algo-astargraph — A* engine — each tick expands the min-f (g+straight-line-h) node toward the goal on the weighted biology-canvas.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-algo-astargraph.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-algo-astargraph
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-algo-astargraph';
const ALIAS = 'AlgoAstargraph';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAlgoAstargraphEventKey = 'INIT' | 'NODES_UPDATED';

/**
 * Payload shape for the `NODES_UPDATED` event.
 */
export interface StdAlgoAstargraphNodesUpdatedPayload {
  nodes?: EntityRow[];
  edges?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAlgoAstargraphConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#9ca3af","from":"A","label":"186","to":"B"},{"color":"#9ca3af","from":"A","label":"142","to":"C"},{"color":"#9ca3af","from":"B","label":"143","to":"C"},{"color":"#9ca3af","from":"B","label":"163","to":"D"},{"color":"#9ca3af","from":"C","label":"162","to":"E"},{"color":"#9ca3af","from":"D","label":"163","to":"E"},{"color":"#9ca3af","from":"D","label":"163","to":"F"},{"color":"#9ca3af","from":"E","label":"200","to":"F"},{"color":"#9ca3af","from":"E","label":"182","to":"G"},{"color":"#9ca3af","from":"F","label":"148","to":"G"}]` */
  edges?: EntityRow[];
  /** Default: `"G"` */
  goal?: string;
  /** Default: `[{"d":0,"id":"A"},{"d":9999,"id":"B"},{"d":9999,"id":"C"},{"d":9999,"id":"D"},{"d":9999,"id":"E"},{"d":9999,"id":"F"},{"d":9999,"id":"G"}]` */
  gscore?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `[{"color":"#f59e0b","id":"A","kind":"cell","label":"A","radius":20,"x":60,"y":340},{"color":"#cbd5e1","id":"B","kind":"cell","label":"B","radius":20,"x":180,"y":200},{"color":"#cbd5e1","id":"C","kind":"cell","label":"C","radius":20,"x":200,"y":340},{"color":"#cbd5e1","id":"D","kind":"cell","label":"D","radius":20,"x":340,"y":180},{"color":"#cbd5e1","id":"E","kind":"cell","label":"E","radius":20,"x":360,"y":340},{"color":"#cbd5e1","id":"F","kind":"cell","label":"F","radius":20,"x":500,"y":200},{"color":"#ef4444","id":"G","kind":"cell","label":"G","radius":20,"x":540,"y":340}]` */
  nodes?: EntityRow[];
  /** Default: `true` */
  running?: boolean;
  /** Default: `"A"` */
  start?: string;
  /** Default: `[{"from":"A","to":"B","w":186},{"from":"A","to":"C","w":142},{"from":"B","to":"C","w":143},{"from":"B","to":"D","w":163},{"from":"C","to":"E","w":162},{"from":"D","to":"E","w":163},{"from":"D","to":"F","w":163},{"from":"E","to":"F","w":200},{"from":"E","to":"G","w":182},{"from":"F","to":"G","w":148}]` */
  wedges?: EntityRow[];
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-algo-astargraph descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAlgoAstargraphParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAlgoAstargraphEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAlgoAstargraphConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AlgoAstargraph.traits.AstarSim`. */
export function stdAlgoAstargraphTrait(params: StdAlgoAstargraphParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AstarSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AlgoAstargraph.pages.AstarGraphEnginePage`. */
export function stdAlgoAstargraphPage(params: StdAlgoAstargraphParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AstarGraphEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAlgoAstargraph(params: StdAlgoAstargraphParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'AstarGraphEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAlgoAstargraphTrait(params),
    ],
    pages: [
      stdAlgoAstargraphPage(params),
    ],
  });
}
