/**
 * std-civics-checks — Checks-and-balances matrix — the learner names which branch of government can check a given governmental action, drawing the check arrow actor -> checker on a three-branch diagram, on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-civics-checks.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-civics-checks
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-civics-checks';
const ALIAS = 'CivicsChecks';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCivicsChecksEventKey = 'CHECK_BY' | 'CIVICS_CHECK_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'NEXT_ACTION';

/**
 * Payload shape for the `CIVICS_CHECK_UPDATED` event.
 */
export interface StdCivicsChecksCivicsCheckUpdatedPayload {
  resultState?: string;
  score?: number;
  attempts?: number;
  checkedBranch?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCivicsChecksConfig {
  /** Default: `[{"action":"Congress passes a law.","actor":"legislative","checks":[{"by":"judicial","label":"Judicial review — Marbury v. Madison"},{"by":"executive","label":"Presidential veto"}],"hint":"Two branches can check a new law: the President can refuse to sign it, and the courts can later strike it down as unconstitutional."},{"action":"The President vetoes a bill.","actor":"executive","checks":[{"by":"legislative","label":"Congress overrides the veto with a two-thirds vote"}],"hint":"Only Congress can override a presidential veto, and only with a two-thirds supermajority in both chambers."},{"action":"The Supreme Court strikes down a law as unconstitutional.","actor":"judicial","checks":[{"by":"legislative","label":"Congress proposes a constitutional amendment"},{"by":"executive","label":"The President appoints new justices over time (with Senate confirmation)"}],"hint":"A constitutional ruling is checked over time by amending the Constitution or by future appointments — not by ordinary legislation."},{"action":"The President appoints a federal judge.","actor":"executive","checks":[{"by":"legislative","label":"Senate confirmation (advice and consent)"}],"hint":"The Senate must confirm judicial nominees — the President cannot seat a federal judge alone."},{"action":"Congress declares war and funds the military.","actor":"legislative","checks":[{"by":"executive","label":"The President, as Commander-in-Chief, directs how forces are used"}],"hint":"Congress controls the power to declare war and fund the military, but command of the forces belongs to the President."},{"action":"The President negotiates a treaty.","actor":"executive","checks":[{"by":"legislative","label":"Senate ratification (two-thirds vote)"}],"hint":"A treaty only binds the nation once two-thirds of the Senate ratifies it — the President cannot commit the nation alone."}]` */
  actions?: EntityRow[];
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#166534"` */
  arrowColor?: string;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#2563eb","key":"legislative","label":"Legislative — Congress"},{"color":"#dc2626","key":"executive","label":"Executive — President"},{"color":"#7c3aed","key":"judicial","label":"Judicial — Courts"}]` */
  branches?: EntityRow[];
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Civics Lab"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-civics-checks descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCivicsChecksParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCivicsChecksEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCivicsChecksConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CivicsChecks.traits.CivicsChecksSim`. */
export function stdCivicsChecksTrait(params: StdCivicsChecksParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CivicsChecksSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CivicsChecks.pages.CivicsChecksPage`. */
export function stdCivicsChecksPage(params: StdCivicsChecksParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CivicsChecksPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCivicsChecks(params: StdCivicsChecksParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CivicsChecksOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCivicsChecksTrait(params),
    ],
    pages: [
      stdCivicsChecksPage(params),
    ],
  });
}
