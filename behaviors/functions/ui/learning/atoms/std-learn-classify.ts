/**
 * std-learn-classify — Generic classify-with-feedback instrument — items are presented one at a time, the learner picks a category from an ordered stacked-backdrop ladder, the pick is verdicted against the item's declared correct category with feedback and a running score, and the learner advances to the next item. Category and item vocabulary is entirely config-declared, so the same mechanism serves any classification domain (needs hierarchies, checks-and-balances, sociological imagination, and beyond) on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-classify.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-classify
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-classify';
const ALIAS = 'LearnClassify';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnClassifyEventKey = 'ADVANCE' | 'CLASSIFY_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'PICK';

/**
 * Payload shape for the `CLASSIFY_UPDATED` event.
 */
export interface StdLearnClassifyClassifyUpdatedPayload {
  itemIndex?: number;
  correctCount?: number;
  attemptedCount?: number;
  selectedCategory?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnClassifyConfig {
  /** Default: `"Next"` */
  advanceLabel?: string;
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#94a3b8","label":"Category A","value":"a"},{"color":"#64748b","label":"Category B","value":"b"}]` */
  categories?: EntityRow[];
  /** Default: `"#16a34a"` */
  correctColor?: string;
  /** Default: `420` */
  height?: number;
  /** Default: `"#dc2626"` */
  incorrectColor?: string;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `[{"category":"a","text":"Example item one."},{"category":"b","text":"Example item two."}]` */
  items?: EntityRow[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Classify"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-learn-classify descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnClassifyParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnClassifyEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnClassifyConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnClassify.traits.LearnClassifySim`. */
export function stdLearnClassifyTrait(params: StdLearnClassifyParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LearnClassifySim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnClassify.pages.LearnClassifyPage`. */
export function stdLearnClassifyPage(params: StdLearnClassifyParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnClassifyPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnClassify(params: StdLearnClassifyParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnClassifyOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnClassifyTrait(params),
    ],
    pages: [
      stdLearnClassifyPage(params),
    ],
  });
}
