/**
 * std-soc-imagination — Sociological Imagination engine — C. Wright Mills' exercise linking a personal trouble to the structural public issue behind it, with a drawn trouble→issue node map, per-link feedback and a running score, on a biology-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-soc-imagination.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-soc-imagination
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-soc-imagination';
const ALIAS = 'SocImagination';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSocImaginationEventKey = 'IMAGINATION_UPDATED' | 'INIT' | 'LINK_ISSUE' | 'MODE_SELECTED' | 'NEXT_TROUBLE';

/**
 * Payload shape for the `IMAGINATION_UPDATED` event.
 */
export interface StdSocImaginationImaginationUpdatedPayload {
  troubleIndex?: number;
  score?: number;
  answeredCorrectly?: boolean;
  feedback?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSocImaginationConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f1f5f9"` */
  backgroundColor?: string;
  /** Default: `460` */
  height?: number;
  /** Default: `"#16a34a"` */
  highlightColor?: string;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `"#64748b"` */
  issueNodeColor?: string;
  /** Default: `34` */
  issueNodeRadius?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Sociological Imagination"` */
  title?: string;
  /** Default: `"#1e293b"` */
  troubleNodeColor?: string;
  /** Default: `48` */
  troubleNodeRadius?: number;
  /** Default: `[{"explanation":"C. Wright Mills: one person out of work is a personal trouble to be solved by that person's character and skills. When millions are out of work at once because factories close and whole job categories are automated away, the job market itself has broken down — that's a public issue rooted in economic structure, not individual failing.","id":"unemployment","options":[{"correct":false,"id":"unemployment-d1","label":"He just isn't trying hard enough to get hired."},{"correct":true,"id":"unemployment-issue","label":"Structural unemployment — factories closing and jobs being automated away are eliminating entire categories of work across the economy."},{"correct":false,"id":"unemployment-d2","label":"Employers today are only looking for young talent."}],"troubleText":"I can't find a job no matter how hard I try — it must be something wrong with me."},{"explanation":"When only a few marriages fail, look to the couple. When divorce rates climb across an entire society within the same few years, the institution of marriage itself is under structural strain — a public issue, not a private failing.","id":"marriage","options":[{"correct":true,"id":"marriage-issue","label":"A generation-wide collapse of the economic and social supports that used to hold marriages together."},{"correct":false,"id":"marriage-d1","label":"She should have picked a better partner."},{"correct":false,"id":"marriage-d2","label":"Marriage counseling just doesn't work anymore."}],"troubleText":"My marriage is falling apart, and it feels like it's entirely my fault."},{"explanation":"One student in debt is a private trouble. A whole generation carrying record debt loads, at the same time public funding for education has been withdrawn, is a public issue built into how college gets paid for.","id":"debt","options":[{"correct":false,"id":"debt-d1","label":"He shouldn't have gone to such an expensive school."},{"correct":false,"id":"debt-d2","label":"People today just don't budget carefully enough."},{"correct":true,"id":"debt-issue","label":"A structural shift that moved the cost of higher education from public funding onto individual borrowers, across an entire generation."}],"troubleText":"I'm drowning in student debt and can't seem to get ahead, no matter what I do."},{"explanation":"A single burned-out worker is a personal trouble. When burnout tracks a structural shift toward insecure, boundary-less work across millions of workers at once, it's a public issue in how work itself has been reorganized.","id":"burnout","options":[{"correct":false,"id":"burnout-d1","label":"She needs to manage her stress better."},{"correct":true,"id":"burnout-issue","label":"An economy-wide shift toward precarious, always-on gig and contract work that has eroded job security for millions."},{"correct":false,"id":"burnout-d2","label":"Some people just aren't cut out for a demanding career."}],"troubleText":"I feel completely burned out and anxious all the time at work, and I don't know why I can't handle it like everyone else."}]` */
  troubles?: EntityRow[];
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-soc-imagination descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSocImaginationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSocImaginationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSocImaginationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SocImagination.traits.SocImaginationSim`. */
export function stdSocImaginationTrait(params: StdSocImaginationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SocImaginationSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SocImagination.pages.SocImaginationPage`. */
export function stdSocImaginationPage(params: StdSocImaginationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SocImaginationPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSocImagination(params: StdSocImaginationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SocImaginationOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSocImaginationTrait(params),
    ],
    pages: [
      stdSocImaginationPage(params),
    ],
  });
}
