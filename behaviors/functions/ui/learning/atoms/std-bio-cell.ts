/**
 * std-bio-cell — Eukaryotic cell-structure lab — a config-declared organelle roster the learner tours with Prev/Next; the selected organelle enlarges with a highlight ring and its function is narrated, and a progress readout tracks the tour. A plantMode knob adds chloroplast, cell wall, and central vacuole. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-bio-cell.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-bio-cell
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-bio-cell';
const ALIAS = 'BioCell';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBioCellEventKey = 'CELL_SELECTED' | 'INIT' | 'MODE_SELECTED' | 'NEXT' | 'PREV' | 'RESET';

/**
 * Payload shape for the `CELL_SELECTED` event.
 */
export interface StdBioCellCellSelectedPayload {
  id?: string;
  label?: string;
  role?: string;
  tourComplete?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBioCellConfig {
  /** Default: `"#f0fdf4"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#e2e8f0","from":"mitochondrion","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"ribosomes","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"roughER","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"smoothER","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"golgi","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"lysosome","label":"","to":"nucleus"}]` */
  coreEdges?: EntityRow[];
  /** Default: `[{"color":"#475569","id":"membrane","kind":"cell","label":"Cell Membrane","radius":160,"role":"Selectively permeable barrier that controls what enters and exits the cell.","x":300,"y":200},{"color":"#7dd3fc","id":"cytoplasm","kind":"organelle","label":"Cytoplasm","radius":10,"role":"Gel-like fluid filling the cell where organelles are suspended and metabolic reactions occur.","x":240,"y":195},{"color":"#2563eb","id":"nucleus","kind":"cell","label":"Nucleus","radius":46,"role":"Houses the cell's DNA and directs protein synthesis and cell division.","x":320,"y":180},{"color":"#dc2626","id":"mitochondrion","kind":"organelle","label":"Mitochondrion","radius":20,"role":"The powerhouse of the cell — converts nutrients into ATP through cellular respiration.","x":185,"y":145},{"color":"#f59e0b","id":"ribosomes","kind":"organelle","label":"Ribosomes","radius":9,"role":"Read messenger RNA and assemble amino acids into proteins.","x":425,"y":145},{"color":"#7c3aed","id":"roughER","kind":"organelle","label":"Rough ER","radius":26,"role":"Ribosome-studded membrane network that folds and processes proteins destined for export.","x":190,"y":265},{"color":"#a855f7","id":"smoothER","kind":"organelle","label":"Smooth ER","radius":22,"role":"Synthesizes lipids and detoxifies harmful compounds; lacks ribosomes.","x":410,"y":265},{"color":"#0891b2","id":"golgi","kind":"organelle","label":"Golgi Apparatus","radius":17,"role":"Packages, modifies, and ships proteins and lipids to their destinations.","x":320,"y":318},{"color":"#eab308","id":"lysosome","kind":"organelle","label":"Lysosome","radius":11,"role":"Contains digestive enzymes that break down waste, debris, and worn-out organelles.","x":445,"y":205}]` */
  coreOrganelles?: EntityRow[];
  /** Default: `400` */
  height?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `[{"color":"#e2e8f0","from":"chloroplast","label":"","to":"nucleus"},{"color":"#e2e8f0","from":"vacuole","label":"","to":"nucleus"}]` */
  plantEdges?: EntityRow[];
  /** Default: `false` */
  plantMode?: boolean;
  /** Default: `[{"color":"#15803d","id":"cellWall","kind":"cell","label":"Cell Wall","radius":178,"role":"Rigid outer layer found in plant cells that provides structural support and protection.","x":300,"y":200},{"color":"#16a34a","id":"chloroplast","kind":"organelle","label":"Chloroplast","radius":15,"role":"Captures light energy and converts it into sugars through photosynthesis.","x":300,"y":338},{"color":"#0e7490","id":"vacuole","kind":"organelle","label":"Central Vacuole","radius":28,"role":"Large fluid-filled sac that maintains turgor pressure and stores water, nutrients, and waste.","x":250,"y":230}]` */
  plantOrganelles?: EntityRow[];
  /** Default: `"Cell Structure Explorer"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-bio-cell descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBioCellParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBioCellEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBioCellConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `BioCell.traits.BioCellSim`. */
export function stdBioCellTrait(params: StdBioCellParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BioCellSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `BioCell.pages.BioCellEnginePage`. */
export function stdBioCellPage(params: StdBioCellParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BioCellEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBioCell(params: StdBioCellParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'BioCellEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBioCellTrait(params),
    ],
    pages: [
      stdBioCellPage(params),
    ],
  });
}
