/**
 * std-ling-syntaxtree — Universal Grammar syntax-tree builder — the learner assembles a sentence's constituent structure bottom-up, attaching one labelled bracket (NP, VP, PP, S) at a time in derivation order, with evidence text on every pick and a growing bracket-tree render on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-ling-syntaxtree.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-ling-syntaxtree
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-ling-syntaxtree';
const ALIAS = 'LingSyntaxtree';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLingSyntaxtreeEventKey = 'ATTACH' | 'INIT' | 'MODE_SELECTED' | 'NEXT_SENTENCE' | 'RESET_TREE' | 'SYNTAXTREE_UPDATED';

/**
 * Payload shape for the `SYNTAXTREE_UPDATED` event.
 */
export interface StdLingSyntaxtreeSyntaxtreeUpdatedPayload {
  sentenceIndex?: number;
  stepIndex?: number;
  totalSteps?: number;
  feedback?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLingSyntaxtreeConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `60` */
  bottomMargin?: number;
  /** Default: `"#2563eb"` */
  bracketColor?: string;
  /** Default: `"#1d4ed8"` */
  bracketLabelColor?: string;
  /** Default: `[{"key":"NP","label":"NP — Noun Phrase"},{"key":"VP","label":"VP — Verb Phrase"},{"key":"PP","label":"PP — Prepositional Phrase"},{"key":"S","label":"S — Sentence"}]` */
  constituentOptions?: EntityRow[];
  /** Default: `16` */
  fontSize?: number;
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `50` */
  leftMargin?: number;
  /** Default: `60` */
  levelHeight?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `64` */
  nodeWidth?: number;
  /** Default: `"#64748b"` */
  posColor?: string;
  /** Default: `12` */
  posFontSize?: number;
  /** Default: `[{"derivation":[{"constituent":"NP","endIndex":1,"evidence":"A determiner (Det) and a noun (N) combine into a Noun Phrase — here the subject \"The cat\".","level":0,"startIndex":0},{"constituent":"NP","endIndex":4,"evidence":"A determiner (Det) and a noun (N) combine into a Noun Phrase — here the object \"the mouse\".","level":0,"startIndex":3},{"constituent":"VP","endIndex":4,"evidence":"A verb (V) and its object Noun Phrase combine into a Verb Phrase — \"chased the mouse\".","level":1,"startIndex":2},{"constituent":"S","endIndex":4,"evidence":"The subject Noun Phrase and the Verb Phrase combine into the full Sentence.","level":2,"startIndex":0}],"id":"s1","label":"The cat chased the mouse.","words":[{"pos":"Det","text":"The"},{"pos":"N","text":"cat"},{"pos":"V","text":"chased"},{"pos":"Det","text":"the"},{"pos":"N","text":"mouse"}]},{"derivation":[{"constituent":"NP","endIndex":1,"evidence":"A determiner (Det) and a noun (N) combine into a Noun Phrase — here the subject \"A dog\".","level":0,"startIndex":0},{"constituent":"NP","endIndex":5,"evidence":"A determiner (Det) and a noun (N) combine into a Noun Phrase — here the object of the preposition, \"the mat\".","level":0,"startIndex":4},{"constituent":"PP","endIndex":5,"evidence":"A preposition (P) and its Noun Phrase combine into a Prepositional Phrase — \"on the mat\".","level":1,"startIndex":3},{"constituent":"VP","endIndex":5,"evidence":"A verb (V) and its Prepositional Phrase combine into a Verb Phrase — \"sat on the mat\".","level":2,"startIndex":2},{"constituent":"S","endIndex":5,"evidence":"The subject Noun Phrase and the Verb Phrase combine into the full Sentence.","level":3,"startIndex":0}],"id":"s2","label":"A dog sat on the mat.","words":[{"pos":"Det","text":"A"},{"pos":"N","text":"dog"},{"pos":"V","text":"sat"},{"pos":"P","text":"on"},{"pos":"Det","text":"the"},{"pos":"N","text":"mat"}]},{"derivation":[{"constituent":"NP","endIndex":0,"evidence":"A bare noun (N) alone forms a Noun Phrase — the subject \"Birds\".","level":0,"startIndex":0},{"constituent":"VP","endIndex":1,"evidence":"A single intransitive verb (V) with no object still forms a Verb Phrase — \"sing\".","level":0,"startIndex":1},{"constituent":"S","endIndex":1,"evidence":"The subject Noun Phrase and the Verb Phrase combine into the full Sentence.","level":1,"startIndex":0}],"id":"s3","label":"Birds sing.","words":[{"pos":"N","text":"Birds"},{"pos":"V","text":"sing"}]}]` */
  sentences?: EntityRow[];
  /** Default: `"Syntax Tree Builder"` */
  title?: string;
  /** Default: `640` */
  width?: number;
  /** Default: `30` */
  wordLabelGap?: number;
  /** Default: `"#1e293b"` */
  wordNodeColor?: string;
  /** Default: `"#e2e8f0"` */
  wordNodeFill?: string;
  /** Default: `90` */
  wordSpacing?: number;
  /** Default: `"#0f172a"` */
  wordTextColor?: string;
}

/**
 * Params for the std-ling-syntaxtree descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLingSyntaxtreeParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLingSyntaxtreeEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLingSyntaxtreeConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LingSyntaxtree.traits.LingSyntaxtreeSim`. */
export function stdLingSyntaxtreeTrait(params: StdLingSyntaxtreeParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LingSyntaxtreeSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LingSyntaxtree.pages.LingSyntaxtreePage`. */
export function stdLingSyntaxtreePage(params: StdLingSyntaxtreeParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LingSyntaxtreePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLingSyntaxtree(params: StdLingSyntaxtreeParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LingSyntaxtreeOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLingSyntaxtreeTrait(params),
    ],
    pages: [
      stdLingSyntaxtreePage(params),
    ],
  });
}
