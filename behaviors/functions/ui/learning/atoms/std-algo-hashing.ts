/**
 * std-algo-hashing — Hash-table builder — inserts keys one at a time; the hash function maps each key to a bucket, collisions resolve by chaining or linear probing, and a load-factor readout triggers rehash; renders algorithm-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-algo-hashing.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-algo-hashing
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-algo-hashing';
const ALIAS = 'AlgoHashing';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAlgoHashingEventKey = 'DELETE_KEY' | 'HASH_UPDATED' | 'INIT' | 'INSERT_KEY' | 'MODE_SELECTED' | 'REHASH' | 'RESET';

/**
 * Payload shape for the `HASH_UPDATED` event.
 */
export interface StdAlgoHashingHashUpdatedPayload {
  buckets?: EntityRow[];
  inserted?: number;
  collisions?: number;
  size?: number;
  narration?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAlgoHashingConfig {
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0},{"color":"#e2e8f0","label":"","value":0}]` */
  buckets?: EntityRow[];
  /** Default: `"chain"` */
  collisionStrategy?: string;
  /** Default: `"mod"` */
  hashFunction?: string;
  /** Default: `400` */
  height?: number;
  /** Default: `[12,25,37,41,53,68,79,90]` */
  keys?: number[];
  /** Default: `""` */
  matchMode?: string;
  /** Default: `0.75` */
  rehashThreshold?: number;
  /** Default: `8` */
  tableSize?: number;
  /** Default: `"Hash Table"` */
  title?: string;
  /** Default: `600` */
  width?: number;
}

/**
 * Params for the std-algo-hashing descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAlgoHashingParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAlgoHashingEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAlgoHashingConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AlgoHashing.traits.HashingSim`. */
export function stdAlgoHashingTrait(params: StdAlgoHashingParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.HashingSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AlgoHashing.pages.HashingEnginePage`. */
export function stdAlgoHashingPage(params: StdAlgoHashingParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.HashingEnginePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAlgoHashing(params: StdAlgoHashingParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'HashingEngineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAlgoHashingTrait(params),
    ],
    pages: [
      stdAlgoHashingPage(params),
    ],
  });
}
