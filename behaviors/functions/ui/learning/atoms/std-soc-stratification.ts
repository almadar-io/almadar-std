/**
 * std-soc-stratification — Social stratification & mobility engine — five class strata redistribute across generations as the learner drives an intergenerational mobility index, with a live Gini-index readout, on a biology-canvas stacked bar. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-soc-stratification.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-soc-stratification
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-soc-stratification';
const ALIAS = 'SocStratification';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSocStratificationEventKey = 'INIT' | 'MODE_SELECTED' | 'SET_MOBILITY' | 'STEP_GENERATION' | 'STRATIFICATION_UPDATED';

/**
 * Payload shape for the `STRATIFICATION_UPDATED` event.
 */
export interface StdSocStratificationStratificationUpdatedPayload {
  mobility?: number;
  generation?: number;
  giniIndex?: number;
  stratumShares?: number[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSocStratificationConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f1f5f9"` */
  backgroundColor?: string;
  /** Default: `60` */
  barHeight?: number;
  /** Default: `40` */
  barMargin?: number;
  /** Default: `0.4` */
  concentrationRate?: number;
  /** Default: `460` */
  height?: number;
  /** Default: `[9,3,1.5,0.7,0.3]` */
  incomeWeights?: number[];
  /** Default: `50` */
  initialMobility?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `0.4` */
  mobilityRate?: number;
  /** Default: `[{"color":"#1e3a8a","name":"Upper Class","share":10},{"color":"#2563eb","name":"Upper-Middle Class","share":20},{"color":"#3b82f6","name":"Middle Class","share":30},{"color":"#60a5fa","name":"Working Class","share":25},{"color":"#93c5fd","name":"Lower Class","share":15}]` */
  strata?: EntityRow[];
  /** Default: `"#f8fafc"` */
  stratumTextColor?: string;
  /** Default: `"Social Stratification & Mobility"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-soc-stratification descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSocStratificationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSocStratificationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSocStratificationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SocStratification.traits.SocStratSim`. */
export function stdSocStratificationTrait(params: StdSocStratificationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SocStratSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SocStratification.pages.SocStratPage`. */
export function stdSocStratificationPage(params: StdSocStratificationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SocStratPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSocStratification(params: StdSocStratificationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SocStratOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSocStratificationTrait(params),
    ],
    pages: [
      stdSocStratificationPage(params),
    ],
  });
}
