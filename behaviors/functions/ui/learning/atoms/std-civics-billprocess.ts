/**
 * std-civics-billprocess — How a bill becomes law — a bill token travels Introduced -> House vote -> Senate vote -> President, with veto and a two-thirds congressional override, on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-civics-billprocess.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-civics-billprocess
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-civics-billprocess';
const ALIAS = 'CivicsBillprocess';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCivicsBillprocessEventKey = 'ADVANCE_BILL' | 'BILL_PROCESS_UPDATED' | 'INIT' | 'MODE_SELECTED' | 'OVERRIDE_VETO' | 'RESET_BILL' | 'SIGN_BILL' | 'VETO_BILL';

/**
 * Payload shape for the `BILL_PROCESS_UPDATED` event.
 */
export interface StdCivicsBillprocessBillProcessUpdatedPayload {
  stationLabel?: string;
  outcome?: string;
  vetoed?: boolean;
  billTitle?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCivicsBillprocessConfig {
  /** Default: `false` */
  animate?: boolean;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `"#0f172a"` */
  billColor?: string;
  /** Default: `[{"houseVotes":{"no":135,"yes":290},"overrideVotes":{"no":145,"yes":290},"senateVotes":{"no":34,"yes":64},"title":"Clean Water Infrastructure Act"},{"houseVotes":{"no":180,"yes":250},"overrideVotes":{"no":170,"yes":260},"senateVotes":{"no":47,"yes":51},"title":"National Digital Privacy Act"},{"houseVotes":{"no":200,"yes":230},"overrideVotes":{"no":200,"yes":230},"senateVotes":{"no":55,"yes":40},"title":"Border Security Modernization Act"},{"houseVotes":{"no":115,"yes":310},"overrideVotes":{"no":135,"yes":300},"senateVotes":{"no":28,"yes":70},"title":"Veterans Healthcare Expansion Act"},{"houseVotes":{"no":175,"yes":245},"overrideVotes":{"no":180,"yes":250},"senateVotes":{"no":40,"yes":58},"title":"Small Business Tax Relief Act"}]` */
  bills?: EntityRow[];
  /** Default: `[{"color":"#2563eb","key":"legislative","label":"Legislative — Congress"},{"color":"#dc2626","key":"executive","label":"Executive — President"},{"color":"#7c3aed","key":"judicial","label":"Judicial — Courts"}]` */
  branches?: EntityRow[];
  /** Default: `460` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"Civics Lab"` */
  title?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-civics-billprocess descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCivicsBillprocessParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCivicsBillprocessEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCivicsBillprocessConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CivicsBillprocess.traits.CivicsBillProcessSim`. */
export function stdCivicsBillprocessTrait(params: StdCivicsBillprocessParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CivicsBillProcessSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CivicsBillprocess.pages.CivicsBillProcessPage`. */
export function stdCivicsBillprocessPage(params: StdCivicsBillprocessParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CivicsBillProcessPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCivicsBillprocess(params: StdCivicsBillprocessParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CivicsBillProcessOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCivicsBillprocessTrait(params),
    ],
    pages: [
      stdCivicsBillprocessPage(params),
    ],
  });
}
