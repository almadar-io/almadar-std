/**
 * std-learn-compare — Generic dual-view comparator — two instances of the same config-declared curve family (power y = A·xᴮ, linear y = A + B·x, or sine y = A·sin(B·x)) render overlaid on one canvas, each side driven by its own parameter pair of sliders, with a live delta readout of the two values at the midpoint x. Changing either side recomputes immediately, so the learner sees exactly how the two settings diverge. All labels and defaults are config-declared, so the same mechanism serves any side-by-side contrast: two interest rates compounding, two spring constants, two demand elasticities, two wave frequencies. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-compare.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-compare
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-compare';
const ALIAS = 'LearnCompare';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnCompareEventKey = 'COMPARISON_CHANGED' | 'INIT' | 'MODE_SELECTED' | 'SET_PARAM';

/**
 * Payload shape for the `COMPARISON_CHANGED` event.
 */
export interface StdLearnCompareComparisonChangedPayload {
  leftA?: number;
  leftB?: number;
  rightA?: number;
  rightB?: number;
  delta?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnCompareConfig {
  /** Default: `"Tune each side's parameters and watch how the two curves diverge."` */
  compareText?: string;
  /** Default: `"power"` */
  curveKind?: string;
  /** Default: `1` */
  gridStep?: number;
  /** Default: `400` */
  height?: number;
  /** Default: `1` */
  leftADefault?: number;
  /** Default: `2` */
  leftBDefault?: number;
  /** Default: `"#3b82f6"` */
  leftColor?: string;
  /** Default: `"Left"` */
  leftLabel?: string;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `"A"` */
  paramALabel?: string;
  /** Default: `5` */
  paramAMax?: number;
  /** Default: `-5` */
  paramAMin?: number;
  /** Default: `0.1` */
  paramAStep?: number;
  /** Default: `"B"` */
  paramBLabel?: string;
  /** Default: `5` */
  paramBMax?: number;
  /** Default: `-5` */
  paramBMin?: number;
  /** Default: `0.1` */
  paramBStep?: number;
  /** Default: `2` */
  rightADefault?: number;
  /** Default: `1.5` */
  rightBDefault?: number;
  /** Default: `"#f59e0b"` */
  rightColor?: string;
  /** Default: `"Right"` */
  rightLabel?: string;
  /** Default: `true` */
  showAxes?: boolean;
  /** Default: `true` */
  showGrid?: boolean;
  /** Default: `"Compare Two Settings"` */
  title?: string;
  /** Default: `600` */
  width?: number;
  /** Default: `10` */
  xMax?: number;
  /** Default: `0` */
  xMin?: number;
}

/**
 * Params for the std-learn-compare descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnCompareParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnCompareEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnCompareConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnCompare.traits.CompareSim`. */
export function stdLearnCompareTrait(params: StdLearnCompareParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CompareSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnCompare.pages.ComparePage`. */
export function stdLearnComparePage(params: StdLearnCompareParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ComparePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnCompare(params: StdLearnCompareParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CompareOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnCompareTrait(params),
    ],
    pages: [
      stdLearnComparePage(params),
    ],
  });
}
