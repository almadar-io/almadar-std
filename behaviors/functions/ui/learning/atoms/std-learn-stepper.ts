/**
 * std-learn-stepper — Generic staged-sequence instrument — the learner moves a Next/Prev cursor across an ordered roster of stages laid out as a timeline: a legend names each category color, the cursor stage rings and grows while past/future stages stay visually distinct by reveal state, the current stage label and description render prominently in a card, a per-category running tally updates live, progress reads stage k of n, and the Prev/Next cursor clamps deliberately at either end with a narrated note rather than silently doing nothing. The category-lens filter dims non-matching stages instead of hiding them, keeping the whole timeline in view. Stage and category vocabulary is entirely config-declared, so the same mechanism serves any ordered-sequence domain (periodization timelines, phase cycles, stellar evolution, and beyond) on a math-canvas. Standalone by default; behind a lab router set matchMode and it renders only when MODE_SELECTED matches.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-learn-stepper.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-learn-stepper
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-learn-stepper';
const ALIAS = 'LearnStepper';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLearnStepperEventKey = 'FILTER_CATEGORY' | 'INIT' | 'MODE_SELECTED' | 'NEXT' | 'PREV' | 'RESET_STEPPER' | 'STEPPER_ADVANCED';

/**
 * Payload shape for the `STEPPER_ADVANCED` event.
 */
export interface StdLearnStepperStepperAdvancedPayload {
  cursorIndex?: number;
  activeFilter?: string;
  currentLabel?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLearnStepperConfig {
  /** Default: `"All"` */
  allLabel?: string;
  /** Default: `"Taken together, every stage contributes to the full sequence. No single category tells the whole story."` */
  allSummary?: string;
  /** Default: `false` */
  animate?: boolean;
  /** Default: `" — Last stage; press Reset to review from the start."` */
  atEndNote?: string;
  /** Default: `60` */
  axisMarginLeft?: number;
  /** Default: `60` */
  axisMarginRight?: number;
  /** Default: `220` */
  axisY?: number;
  /** Default: `"#f8fafc"` */
  backgroundColor?: string;
  /** Default: `[{"category":"a","color":"#7c3aed","label":"Category A"},{"category":"b","color":"#15803d","label":"Category B"}]` */
  categories?: EntityRow[];
  /** Default: `[{"category":"a","summary":"Viewed through Category A alone, only the stages tagged A are highlighted."},{"category":"b","summary":"Viewed through Category B alone, only the stages tagged B are highlighted."}]` */
  categorySummaries?: EntityRow[];
  /** Default: `420` */
  height?: number;
  /** Default: `false` */
  interactive?: boolean;
  /** Default: `14` */
  markerHighlightRadius?: number;
  /** Default: `8` */
  markerRadius?: number;
  /** Default: `""` */
  matchMode?: string;
  /** Default: `4` */
  maxPosition?: number;
  /** Default: `0` */
  minPosition?: number;
  /** Default: `[{"category":"a","description":"First stage in the sequence.","id":"stage-1","label":"Stage One","position":1},{"category":"b","description":"Second stage in the sequence.","id":"stage-2","label":"Stage Two","position":2},{"category":"a","description":"Third stage in the sequence.","id":"stage-3","label":"Stage Three","position":3}]` */
  stages?: EntityRow[];
  /** Default: `"Press Next to begin."` */
  startPrompt?: string;
  /** Default: `"Stepper"` */
  title?: string;
  /** Default: `"#d6d3d1"` */
  unreachedColor?: string;
  /** Default: `640` */
  width?: number;
}

/**
 * Params for the std-learn-stepper descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLearnStepperParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLearnStepperEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLearnStepperConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LearnStepper.traits.LearnStepperSim`. */
export function stdLearnStepperTrait(params: StdLearnStepperParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LearnStepperSim`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LearnStepper.pages.LearnStepperPage`. */
export function stdLearnStepperPage(params: StdLearnStepperParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LearnStepperPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLearnStepper(params: StdLearnStepperParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'LearnStepperOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLearnStepperTrait(params),
    ],
    pages: [
      stdLearnStepperPage(params),
    ],
  });
}
