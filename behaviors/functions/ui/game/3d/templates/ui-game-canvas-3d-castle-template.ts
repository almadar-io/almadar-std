/**
 * ui-game-canvas-3d-castle-template — GameCanvas3dCastleTemplate — UI factory (1:1 with the @almadar/ui game-canvas-3d-castle-template pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/templates/ui-game-canvas-3d-castle-template.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-game-canvas-3d-castle-template
 *
 * @level template
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-game-canvas-3d-castle-template';
const ALIAS = 'UiGameCanvas3dCastleTemplate';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiGameCanvas3dCastleTemplateEventKey = 'GameCanvas3dCastleTemplateLoaded' | 'INIT';

/**
 * Payload shape for the `GameCanvas3dCastleTemplateLoaded` event.
 */
export interface StdUiGameCanvas3dCastleTemplateGameCanvas3dCastleTemplateLoadedPayload {
  data?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiGameCanvas3dCastleTemplateConfig {
  /** Default: `{}` */
  assetManifest?: unknown;
  /** Default: `"Attack Event"` */
  attackEvent?: string;
  /** Default: `"#1e1e2e"` */
  backgroundColor?: string;
  /** Default: `"Build Event"` */
  buildEvent?: string;
  /** Default: `"Building Click Event"` */
  buildingClickEvent?: string;
  /** Default: `"isometric"` */
  cameraMode?: 'isometric' | 'perspective' | 'top-down';
  /** Default: `"Cancel Event"` */
  cancelEvent?: string;
  /** Default: `""` */
  className?: string;
  /** Default: `"End Turn Event"` */
  endTurnEvent?: string;
  /** Default: `"Exit Event"` */
  exitEvent?: string;
  /** Default: `[]` */
  features?: EntityRow[];
  /** Default: `"Game End Event"` */
  gameEndEvent?: string;
  /** Default: `"Play Again Event"` */
  playAgainEvent?: string;
  /** Default: `"Recruit Event"` */
  recruitEvent?: string;
  /** Default: `0` */
  scale?: number;
  /** Default: `false` */
  shadows?: boolean;
  /** Default: `false` */
  showGrid?: boolean;
  /** Default: `true` */
  showHeader?: boolean;
  /** Default: `[]` */
  tiles?: EntityRow[];
  /** Default: `"Unit Click Event"` */
  unitClickEvent?: string;
  /** Default: `0` */
  unitScale?: number;
}

/**
 * Params for the ui-game-canvas-3d-castle-template descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdUiGameCanvas3dCastleTemplateParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdUiGameCanvas3dCastleTemplateEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdUiGameCanvas3dCastleTemplateConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `UiGameCanvas3dCastleTemplate.traits.GameCanvas3dCastleTemplateRender`. */
export function stdUiGameCanvas3dCastleTemplateTrait(params: StdUiGameCanvas3dCastleTemplateParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GameCanvas3dCastleTemplateRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `UiGameCanvas3dCastleTemplate.pages.GameCanvas3dCastleTemplatePage`. */
export function stdUiGameCanvas3dCastleTemplatePage(params: StdUiGameCanvas3dCastleTemplateParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GameCanvas3dCastleTemplatePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdUiGameCanvas3dCastleTemplate(params: StdUiGameCanvas3dCastleTemplateParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'GameCanvas3dCastleTemplateOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdUiGameCanvas3dCastleTemplateTrait(params),
    ],
    pages: [
      stdUiGameCanvas3dCastleTemplatePage(params),
    ],
  });
}
