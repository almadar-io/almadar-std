/**
 * ui-canvas-effect — CanvasEffect — UI factory (1:1 with the @almadar/ui canvas-effect pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-canvas-effect.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-canvas-effect
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-canvas-effect';
const ALIAS = 'UiCanvasEffect';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiCanvasEffectEventKey = 'COMPLETE' | 'INIT';

/**
 * Payload shape for the `COMPLETE` event.
 */
export interface StdUiCanvasEffectCompletePayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiCanvasEffectConfig {
  /** Default: `""` */
  className?: string;
  error?: EntityRow;
  /** Default: `"melee"` */
  actionType?: 'melee' | 'ranged' | 'magic' | 'heal' | 'defend' | 'hit' | 'death' | 'buff' | 'debuff' | 'shield' | 'aoe' | 'critical';
  /** Default: `0` */
  x?: number;
  /** Default: `"Effect Sprite Url"` */
  effectSpriteUrl?: string;
  /** Default: `2000` */
  duration?: number;
  /** Default: `"Asset Base Url"` */
  assetBaseUrl?: string;
  /** Default: `400` */
  width?: number;
  /** Default: `1` */
  intensity?: number;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `300` */
  height?: number;
  assetManifest?: EntityRow;
  /** Default: `0` */
  y?: number;
}

/**
 * Tunable params for the CanvasEffectOrbital orbital.
 *
 * Canonical entity: CanvasEffectItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiCanvasEffectCanvasEffectOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CanvasEffectRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the CanvasEffectOrbital orbital with consumer params. */
export function stdUiCanvasEffectCanvasEffectOrbital(params: StdUiCanvasEffectCanvasEffectOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CanvasEffectItem';
  const built = makeOrbitalWithUses({
    name: 'CanvasEffectOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'name': 'CanvasEffectRender',
        'entityRebindable': true,
        'entityContract': {
          'requires': [],
          'provides': [],
        },
        'category': 'interaction',
        'linkedEntity': 'CanvasEffectItem',
        'emits': [
          {
            'event': 'COMPLETE',
            'description': 'Callback when the effect animation completes',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'COMPLETE',
              'name': 'Complete',
              'description': 'Callback when the effect animation completes',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'intensity': '@config.intensity',
                    'completeEvent': 'COMPLETE',
                    'type': 'canvas-effect',
                    'onComplete': 'COMPLETE',
                    'isLoading': '@config.isLoading',
                    'width': '@config.width',
                    'assetBaseUrl': '@config.assetBaseUrl',
                    'effectSpriteUrl': '@config.effectSpriteUrl',
                    'className': '@config.className',
                    'y': '@config.y',
                    'assetManifest': '@config.assetManifest',
                    'x': '@config.x',
                    'duration': '@config.duration',
                    'error': '@config.error',
                    'actionType': '@config.actionType',
                    'height': '@config.height',
                  },
                ],
              ],
            },
          ],
        },
        'config': {
          'className': {
            'type': 'string',
            'default': '',
            'label': 'Class Name',
            'description': 'Additional CSS classes',
            'tier': 'presentation',
          },
          'error': {
            'type': 'CanvasEffectError',
            'label': 'Error',
            'description': 'Error state',
            'tier': 'presentation',
            'properties': {
              'stack': {
                'name': 'stack',
                'type': 'string',
                'required': false,
              },
              'name': {
                'name': 'name',
                'type': 'string',
                'required': false,
              },
              'message': {
                'name': 'message',
                'type': 'string',
                'required': true,
              },
              'code': {
                'name': 'code',
                'type': 'string',
                'required': false,
              },
            },
          },
          'actionType': {
            'type': 'string',
            'default': 'melee',
            'label': 'Action Type',
            'description': 'The type of combat action to visualise',
            'tier': 'presentation',
            'values': [
              'melee',
              'ranged',
              'magic',
              'heal',
              'defend',
              'hit',
              'death',
              'buff',
              'debuff',
              'shield',
              'aoe',
              'critical',
            ],
          },
          'x': {
            'type': 'number',
            'default': 0,
            'label': 'X',
            'description': 'Screen-space X position (center of the effect)',
            'tier': 'presentation',
          },
          'effectSpriteUrl': {
            'type': 'string',
            'default': 'Effect Sprite Url',
            'label': 'Effect Sprite Url',
            'description': '--- Remote asset loading ---',
            'tier': 'presentation',
          },
          'duration': {
            'type': 'number',
            'default': 2000,
            'label': 'Duration',
            'description': 'Duration in ms before auto-dismiss (default 2000 for canvas, 800 for emoji)',
            'tier': 'presentation',
          },
          'assetBaseUrl': {
            'type': 'string',
            'default': 'Asset Base Url',
            'label': 'Asset Base Url',
            'description': 'Base URL for remote assets. Prepended to relative effectSpriteUrl paths.',
            'tier': 'presentation',
          },
          'width': {
            'type': 'number',
            'default': 400,
            'label': 'Width',
            'description': 'Canvas width (default 400)',
            'tier': 'presentation',
          },
          'intensity': {
            'type': 'number',
            'default': 1,
            'label': 'Intensity',
            'description': 'Optional intensity multiplier (1 = normal, 2 = double size/brightness)',
            'tier': 'presentation',
          },
          'isLoading': {
            'type': 'boolean',
            'default': false,
            'label': 'Is Loading',
            'description': 'Loading state indicator',
            'tier': 'presentation',
          },
          'height': {
            'type': 'number',
            'default': 300,
            'label': 'Height',
            'description': 'Canvas height (default 300)',
            'tier': 'presentation',
          },
          'assetManifest': {
            'type': 'CanvasEffectAssetManifest',
            'label': 'Asset Manifest',
            'description': 'Full effect asset manifest for the sprite particle engine. When provided, enables the canvas-based particle system.',
            'tier': 'presentation',
            'properties': {
              'particles': {
                'name': 'particles',
                'type': 'object',
                'required': false,
                'properties': {
                  'star': {
                    'name': 'star',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'fire': {
                    'name': 'fire',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'dirt': {
                    'name': 'dirt',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'scorch': {
                    'name': 'scorch',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'circle': {
                    'name': 'circle',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'light': {
                    'name': 'light',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'flare': {
                    'name': 'flare',
                    'type': 'string',
                    'required': false,
                  },
                  'scratch': {
                    'name': 'scratch',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'trace': {
                    'name': 'trace',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'slash': {
                    'name': 'slash',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'magic': {
                    'name': 'magic',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'twirl': {
                    'name': 'twirl',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'smoke': {
                    'name': 'smoke',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'flame': {
                    'name': 'flame',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'spark': {
                    'name': 'spark',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'symbol': {
                    'name': 'symbol',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'muzzle': {
                    'name': 'muzzle',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                },
              },
              'animations': {
                'name': 'animations',
                'type': 'object',
                'required': false,
                'properties': {
                  'explosion': {
                    'name': 'explosion',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'gasSmoke': {
                    'name': 'gasSmoke',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'smokePuff': {
                    'name': 'smokePuff',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'smokeExplosion': {
                    'name': 'smokeExplosion',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'blackSmoke': {
                    'name': 'blackSmoke',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                  'flash': {
                    'name': 'flash',
                    'type': 'array',
                    'required': false,
                    'items': {
                      'type': 'string',
                    },
                  },
                },
              },
              'baseUrl': {
                'name': 'baseUrl',
                'type': 'string',
                'required': true,
              },
            },
          },
          'y': {
            'type': 'number',
            'default': 0,
            'label': 'Y',
            'description': 'Screen-space Y position (center of the effect)',
            'tier': 'presentation',
          },
        },
        'scope': 'instance',
      } as never, 'CanvasEffectItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'CanvasEffectPage',
        'path': '/canvas-effect',
        'traits': [
          {
            'ref': 'CanvasEffectRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiCanvasEffectCanvasEffectOrbital. */
export const StdUiCanvasEffectCanvasEffectOrbitalManifest = {
  organism: 'ui-canvas-effect',
  orbitalName: 'CanvasEffectOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'CanvasEffectRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiCanvasEffectCanvasEffectOrbitalParams keys. */
export function isStdUiCanvasEffectCanvasEffectOrbitalParams(p: object): p is StdUiCanvasEffectCanvasEffectOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiCanvasEffectCanvasEffectOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiCanvasEffectCanvasEffectOrbitalManifest.traitNames,
      ...StdUiCanvasEffectCanvasEffectOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-canvas-effect — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiCanvasEffectParams {
  CanvasEffect?: StdUiCanvasEffectCanvasEffectOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiCanvasEffect(params: StdUiCanvasEffectParams = {}): OrbitalDefinition[] {
  return [
    stdUiCanvasEffectCanvasEffectOrbital(params.CanvasEffect ?? {}),
  ];
}
