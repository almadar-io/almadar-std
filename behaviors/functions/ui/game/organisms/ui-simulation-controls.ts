/**
 * ui-simulation-controls — SimulationControls — UI factory (1:1 with the @almadar/ui simulation-controls pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-simulation-controls.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-simulation-controls
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-simulation-controls';
const ALIAS = 'UiSimulationControls';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiSimulationControlsEventKey = 'INIT' | 'PARAMETER_CHANGE' | 'PAUSE' | 'PLAY' | 'RESET' | 'SPEED_CHANGE' | 'STEP';

/**
 * Payload shape for the `PLAY` event.
 */
export interface StdUiSimulationControlsPlayPayload {
  id?: string;
}

/**
 * Payload shape for the `PAUSE` event.
 */
export interface StdUiSimulationControlsPausePayload {
  id?: string;
}

/**
 * Payload shape for the `STEP` event.
 */
export interface StdUiSimulationControlsStepPayload {
  id?: string;
}

/**
 * Payload shape for the `RESET` event.
 */
export interface StdUiSimulationControlsResetPayload {
  id?: string;
}

/**
 * Payload shape for the `SPEED_CHANGE` event.
 */
export interface StdUiSimulationControlsSpeedChangePayload {
  speed?: number;
}

/**
 * Payload shape for the `PARAMETER_CHANGE` event.
 */
export interface StdUiSimulationControlsParameterChangePayload {
  name?: string;
  value?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiSimulationControlsConfig {
  /** Default: `false` */
  running?: boolean;
  /** Default: `""` */
  className?: string;
  /** Default: `{}` */
  parameters?: unknown;
  /** Default: `0` */
  speed?: number;
}

/**
 * Tunable params for the SimulationControlsOrbital orbital.
 *
 * Canonical entity: SimulationControlsItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiSimulationControlsSimulationControlsOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SimulationControlsRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the SimulationControlsOrbital orbital with consumer params. */
export function stdUiSimulationControlsSimulationControlsOrbital(params: StdUiSimulationControlsSimulationControlsOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'SimulationControlsItem';
  const built = makeOrbitalWithUses({
    name: 'SimulationControlsOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'speed',
            'type': 'number',
            'default': 0,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'name': 'SimulationControlsRender',
        'entityRebindable': true,
        'entityContract': {
          'requires': [],
          'provides': [
            'speed',
          ],
        },
        'category': 'interaction',
        'linkedEntity': 'SimulationControlsItem',
        'emits': [
          {
            'event': 'PLAY',
            'description': 'onPlay prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'PAUSE',
            'description': 'onPause prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'STEP',
            'description': 'onStep prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'RESET',
            'description': 'onReset prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'SPEED_CHANGE',
            'description': 'onSpeedChange prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'speed',
                'type': 'number',
              },
            ],
          },
          {
            'event': 'PARAMETER_CHANGE',
            'description': 'onParameterChange prop',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'name',
                'type': 'string',
              },
              {
                'name': 'value',
                'type': 'number',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'SPEED_CHANGE',
              'name': 'Speed Change',
              'description': 'onSpeedChange prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'speed',
                  'type': 'number',
                },
              ],
            },
            {
              'key': 'PLAY',
              'name': 'Play',
              'description': 'onPlay prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'PAUSE',
              'name': 'Pause',
              'description': 'onPause prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'STEP',
              'name': 'Step',
              'description': 'onStep prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'RESET',
              'name': 'Reset',
              'description': 'onReset prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'PARAMETER_CHANGE',
              'name': 'Parameter Change',
              'description': 'onParameterChange prop',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'name',
                  'type': 'string',
                },
                {
                  'name': 'value',
                  'type': 'number',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'set',
                  '@entity.speed',
                  '@config.speed',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'className': '@config.className',
                    'type': 'simulation-controls',
                    'onSpeedChange': 'SPEED_CHANGE',
                    'onPlay': 'PLAY',
                    'speed': '@entity.speed',
                    'parameters': '@config.parameters',
                    'onStep': 'STEP',
                    'onPause': 'PAUSE',
                    'running': '@config.running',
                    'onReset': 'RESET',
                    'onParameterChange': 'PARAMETER_CHANGE',
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'SPEED_CHANGE',
              'effects': [
                [
                  'set',
                  '@entity.speed',
                  '@payload.speed',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'className': '@config.className',
                    'onPlay': 'PLAY',
                    'type': 'simulation-controls',
                    'running': '@config.running',
                    'onStep': 'STEP',
                    'onReset': 'RESET',
                    'speed': '@entity.speed',
                    'onPause': 'PAUSE',
                    'parameters': '@config.parameters',
                    'onSpeedChange': 'SPEED_CHANGE',
                    'onParameterChange': 'PARAMETER_CHANGE',
                  },
                ],
              ],
            },
          ],
        },
        'config': {
          'running': {
            'type': 'boolean',
            'default': false,
            'label': 'Running',
            'description': 'running prop',
            'tier': 'presentation',
          },
          'className': {
            'type': 'string',
            'default': '',
            'label': 'Class Name',
            'description': 'className prop',
            'tier': 'presentation',
          },
          'parameters': {
            'type': 'Map<string,SimulationControlsParametersValue>',
            'default': {},
            'label': 'Parameters',
            'description': 'parameters prop',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'step': {
                  'name': 'step',
                  'type': 'number',
                  'required': true,
                },
                'max': {
                  'name': 'max',
                  'type': 'number',
                  'required': true,
                },
                'value': {
                  'name': 'value',
                  'type': 'number',
                  'required': true,
                },
                'min': {
                  'name': 'min',
                  'type': 'number',
                  'required': true,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
              },
            },
          },
          'speed': {
            'type': 'number',
            'default': 0,
            'label': 'Speed',
            'description': 'speed prop',
            'tier': 'presentation',
          },
        },
        'scope': 'instance',
      } as never, 'SimulationControlsItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'SimulationControlsPage',
        'path': '/simulation-controls',
        'traits': [
          {
            'ref': 'SimulationControlsRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiSimulationControlsSimulationControlsOrbital. */
export const StdUiSimulationControlsSimulationControlsOrbitalManifest = {
  organism: 'ui-simulation-controls',
  orbitalName: 'SimulationControlsOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'SimulationControlsRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiSimulationControlsSimulationControlsOrbitalParams keys. */
export function isStdUiSimulationControlsSimulationControlsOrbitalParams(p: object): p is StdUiSimulationControlsSimulationControlsOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiSimulationControlsSimulationControlsOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiSimulationControlsSimulationControlsOrbitalManifest.traitNames,
      ...StdUiSimulationControlsSimulationControlsOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-simulation-controls — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiSimulationControlsParams {
  SimulationControls?: StdUiSimulationControlsSimulationControlsOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiSimulationControls(params: StdUiSimulationControlsParams = {}): OrbitalDefinition[] {
  return [
    stdUiSimulationControlsSimulationControlsOrbital(params.SimulationControls ?? {}),
  ];
}
