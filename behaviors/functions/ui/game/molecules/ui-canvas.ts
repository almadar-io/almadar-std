/**
 * ui-canvas — Canvas — UI factory (1:1 with the @almadar/ui canvas pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/ui-canvas.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-canvas
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-canvas';
const ALIAS = 'UiCanvas';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiCanvasEventKey = 'FEATURE_CLICK' | 'INIT' | 'TILE_CLICK' | 'TILE_HOVER' | 'TILE_LEAVE' | 'UNIT_CLICK';

/**
 * Payload shape for the `TILE_CLICK` event.
 */
export interface StdUiCanvasTileClickPayload {
  _payload?: EntityRow;
}

/**
 * Payload shape for the `UNIT_CLICK` event.
 */
export interface StdUiCanvasUnitClickPayload {
  _payload?: EntityRow;
}

/**
 * Payload shape for the `TILE_HOVER` event.
 */
export interface StdUiCanvasTileHoverPayload {
  _payload?: EntityRow;
}

/**
 * Payload shape for the `TILE_LEAVE` event.
 */
export interface StdUiCanvasTileLeavePayload {
  id?: string;
}

/**
 * Payload shape for the `FEATURE_CLICK` event.
 */
export interface StdUiCanvasFeatureClickPayload {
  featureId: string;
  x: number;
  z: number;
  elevation?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiCanvasConfig {
  /** Default: `"Background Color"` */
  backgroundColor?: string;
  /** Default: `{"animations":["static"],"aspect":"1:1","category":"backgroundImage","dimension":"2d","name":"backgroundImage","role":"decoration","style":"pixel","thumbnailUrl":"","url":"https://almadar-kflow-assets.web.app/shared/ui-visual-novel-board/default/backgrounds/corridor.png"}` */
  backgroundImage?: unknown;
  /** Default: `{}` */
  camera?: unknown;
  /** Default: `""` */
  className?: string;
  /** Default: `[]` */
  drawables?: EntityRow[];
  /** Default: `[]` */
  fogOfWar?: unknown;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `{"item":"Item"}` */
  keyMap?: Record<string, TraitConfig>;
  /** Default: `{"item":"Item"}` */
  keyUpMap?: Record<string, TraitConfig>;
  /** Default: `"2d"` */
  mode?: '2d' | '3d';
  /** Default: `0` */
  pixelsPerUnit?: number;
  /** Default: `"isometric"` */
  projection?: 'isometric' | 'hex' | 'flat' | 'free' | 'side';
  /** Default: `false` */
  shadows?: boolean;
  /** Default: `false` */
  showCoordinates?: boolean;
  /** Default: `false` */
  showGrid?: boolean;
  /** Default: `false` */
  showMinimap?: boolean;
  /** Default: `false` */
  showTileInfo?: boolean;
  /** Default: `0` */
  unitScale?: number;
  /** Default: `0` */
  worldHeight?: number;
  /** Default: `0` */
  worldWidth?: number;
}

/**
 * Params for the ui-canvas descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdUiCanvasParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdUiCanvasEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdUiCanvasConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `UiCanvas.traits.CanvasRender`. */
export function stdUiCanvasTrait(params: StdUiCanvasParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CanvasRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `UiCanvas.pages.CanvasPage`. */
export function stdUiCanvasPage(params: StdUiCanvasParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CanvasPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdUiCanvas(params: StdUiCanvasParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'CanvasOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdUiCanvasTrait(params),
    ],
    pages: [
      stdUiCanvasPage(params),
    ],
  });
}
