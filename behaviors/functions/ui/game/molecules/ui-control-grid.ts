/**
 * ui-control-grid — ControlGrid — UI factory (1:1 with the @almadar/ui control-grid pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/ui-control-grid.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-control-grid
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-control-grid';
const ALIAS = 'UiControlGrid';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiControlGridEventKey = 'ACTION' | 'DIRECTION' | 'INIT';

/**
 * Payload shape for the `ACTION` event.
 */
export interface StdUiControlGridActionPayload {
  id?: string;
  pressed?: boolean;
}

/**
 * Payload shape for the `DIRECTION` event.
 */
export interface StdUiControlGridDirectionPayload {
  direction?: string;
  pressed?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiControlGridConfig {
  /** Default: `"ACTION"` */
  actionEvent?: string;
  /** Default: `[{"assetUrl":{"animations":["idle","walk","attack","hit","death"],"aspect":"1:1","category":"assetUrl","dimension":"2d","name":"assetUrl","role":"npc","style":"pixel","thumbnailUrl":"","url":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png"},"icon":"circle","id":"Id","label":"Label","variant":"primary"},{"assetUrl":{"animations":["idle","walk","attack","hit","death"],"aspect":"1:1","category":"assetUrl","dimension":"2d","name":"assetUrl","role":"npc","style":"pixel","thumbnailUrl":"","url":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png"},"icon":"circle","id":"Id 2","label":"Label 2","variant":"secondary"}]` */
  buttons?: EntityRow[];
  /** Default: `""` */
  className?: string;
  /** Default: `{"item":{"animations":["static"],"aspect":"1:1","category":"item","dimension":"2d","name":"item","role":"item","style":"pixel","thumbnailUrl":"","url":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/features/sword.png"}}` */
  directionAssets?: Record<string, TraitConfig>;
  /** Default: `"DIRECTION"` */
  directionEvent?: string;
  /** Default: `{"item":"Item"}` */
  directionEvents?: Record<string, TraitConfig>;
  /** Default: `{"item":"Item"}` */
  directionReleaseEvents?: Record<string, TraitConfig>;
  /** Default: `false` */
  disabled?: boolean;
  /** Default: `false` */
  includeDiagonals?: boolean;
  /** Default: `"dpad"` */
  kind?: 'dpad' | 'actions';
  /** Default: `"horizontal"` */
  layout?: 'horizontal' | 'vertical' | 'diamond';
  /** Default: `"ACTION"` */
  onAction?: string;
  /** Default: `"DIRECTION"` */
  onDirection?: string;
  /** Default: `"md"` */
  size?: 'sm' | 'md' | 'lg';
}

/**
 * Params for the ui-control-grid descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdUiControlGridParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdUiControlGridEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdUiControlGridConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `UiControlGrid.traits.ControlGridRender`. */
export function stdUiControlGridTrait(params: StdUiControlGridParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ControlGridRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `UiControlGrid.pages.ControlGridPage`. */
export function stdUiControlGridPage(params: StdUiControlGridParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ControlGridPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdUiControlGrid(params: StdUiControlGridParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ControlGridOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdUiControlGridTrait(params),
    ],
    pages: [
      stdUiControlGridPage(params),
    ],
  });
}
