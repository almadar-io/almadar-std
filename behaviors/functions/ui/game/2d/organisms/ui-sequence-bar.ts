/**
 * ui-sequence-bar — SequenceBar — UI factory (1:1 with the @almadar/ui sequence-bar pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-sequence-bar.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-sequence-bar
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-sequence-bar';
const ALIAS = 'UiSequenceBar';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiSequenceBarEventKey = 'INIT' | 'SLOT_DROP' | 'SLOT_REMOVE';

/**
 * Payload shape for the `SLOT_DROP` event.
 */
export interface StdUiSequenceBarSlotDropPayload {
  index?: number;
  item?: EntityRow;
}

/**
 * Payload shape for the `SLOT_REMOVE` event.
 */
export interface StdUiSequenceBarSlotRemovePayload {
  index?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiSequenceBarConfig {
  /** Default: `{}` */
  categoryColors?: Record<string, TraitConfig>;
  /** Default: `""` */
  className?: string;
  /** Default: `-1` */
  currentStep?: number;
  /** Default: `1` */
  maxSlots?: number;
  /** Default: `false` */
  playing?: boolean;
  /** Default: `"lg"` */
  size?: 'sm' | 'md' | 'lg';
  /** Default: `[]` */
  slotFeedback?: string[];
  /** Default: `[{"category":"Category","description":"Description","iconEmoji":"Icon Emoji","iconUrl":"https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png","id":"Id","name":"Name"},{"category":"Category 2","description":"Description 2","iconEmoji":"Icon Emoji 2","iconUrl":"https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png","id":"Id 2","name":"Name 2"}]` */
  slots?: EntityRow[];
}

/**
 * Tunable params for the SequenceBarOrbital orbital.
 *
 * Canonical entity: SequenceBarItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiSequenceBarSequenceBarOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SequenceBarRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the SequenceBarOrbital orbital with consumer params. */
export function stdUiSequenceBarSequenceBarOrbital(params: StdUiSequenceBarSequenceBarOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'SequenceBarItem';
  const built = makeOrbitalWithUses({
    name: 'SequenceBarOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'categoryColors': {
            'default': {},
            'description': 'Category → color mapping',
            'items': {
              'properties': {
                'bg': {
                  'name': 'bg',
                  'required': true,
                  'type': 'string',
                },
                'border': {
                  'name': 'border',
                  'required': true,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Category Colors',
            'tier': 'presentation',
            'type': 'Map<string,SequenceBarCategoryColorsValue>',
          },
          'className': {
            'default': '',
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'currentStep': {
            'default': -1,
            'description': 'Current step index during playback (-1 = not playing)',
            'label': 'Current Step',
            'tier': 'presentation',
            'type': 'number',
          },
          'maxSlots': {
            'default': 1,
            'description': 'Max number of slots',
            'label': 'Max Slots',
            'tier': 'presentation',
            'type': 'number',
          },
          'playing': {
            'default': false,
            'description': 'Whether the sequence is currently playing (disable interaction)',
            'label': 'Playing',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'size': {
            'default': 'lg',
            'description': 'Size variant',
            'label': 'Size',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'sm',
              'md',
              'lg',
            ],
          },
          'slotFeedback': {
            'default': [],
            'description': 'Per-slot correctness feedback shown after a failed attempt',
            'items': {
              'type': 'string',
              'values': [
                'correct',
                'wrong',
              ],
            },
            'label': 'Slot Feedback',
            'tier': 'presentation',
            'type': '[string]',
          },
          'slots': {
            'default': [
              {
                'category': 'Category',
                'description': 'Description',
                'iconEmoji': 'Icon Emoji',
                'iconUrl': 'https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png',
                'id': 'Id',
                'name': 'Name',
              },
              {
                'category': 'Category 2',
                'description': 'Description 2',
                'iconEmoji': 'Icon Emoji 2',
                'iconUrl': 'https://almadar-kflow-assets.web.app/shared/characters/archetypes/00_base_model.png',
                'id': 'Id 2',
                'name': 'Name 2',
              },
            ],
            'description': 'The current sequence (sparse — undefined means empty slot)',
            'items': {
              'properties': {
                'category': {
                  'name': 'category',
                  'required': true,
                  'type': 'string',
                },
                'description': {
                  'name': 'description',
                  'required': false,
                  'type': 'string',
                },
                'iconEmoji': {
                  'name': 'iconEmoji',
                  'required': false,
                  'type': 'string',
                },
                'iconUrl': {
                  'name': 'iconUrl',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
                'name': {
                  'name': 'name',
                  'required': true,
                  'type': 'string',
                },
                'stateMachine': {
                  'name': 'stateMachine',
                  'required': false,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Slots',
            'tier': 'presentation',
            'type': '[SequenceBarSlotsItem]',
          },
        },
        'emits': [
          {
            'description': 'Called when an item is dropped into slot at index',
            'event': 'SLOT_DROP',
            'payloadSchema': [
              {
                'name': 'index',
                'type': 'number',
              },
              {
                'name': 'item',
                'properties': [
                  {
                    'name': 'id',
                    'required': true,
                    'type': 'string',
                  },
                  {
                    'name': 'name',
                    'required': true,
                    'type': 'string',
                  },
                  {
                    'name': 'category',
                    'required': true,
                    'type': 'string',
                  },
                  {
                    'name': 'description',
                    'type': 'string',
                  },
                  {
                    'name': 'iconEmoji',
                    'type': 'string',
                  },
                  {
                    'name': 'iconUrl',
                    'type': 'asset',
                  },
                  {
                    'name': 'stateMachine',
                    'type': 'json',
                  },
                ],
                'type': 'object',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'description': 'Called when a slot is cleared',
            'event': 'SLOT_REMOVE',
            'payloadSchema': [
              {
                'name': 'index',
                'type': 'number',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'SequenceBarItem',
        'name': 'SequenceBarRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Called when an item is dropped into slot at index',
              'key': 'SLOT_DROP',
              'name': 'Slot Drop',
              'payloadSchema': [
                {
                  'name': 'index',
                  'type': 'number',
                },
                {
                  'name': 'item',
                  'properties': [
                    {
                      'name': 'id',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'name',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'category',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'description',
                      'type': 'string',
                    },
                    {
                      'name': 'iconEmoji',
                      'type': 'string',
                    },
                    {
                      'name': 'iconUrl',
                      'type': 'asset',
                    },
                    {
                      'name': 'stateMachine',
                      'type': 'json',
                    },
                  ],
                  'type': 'object',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'Called when a slot is cleared',
              'key': 'SLOT_REMOVE',
              'name': 'Slot Remove',
              'payloadSchema': [
                {
                  'name': 'index',
                  'type': 'number',
                },
              ],
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'categoryColors': '@config.categoryColors',
                    'className': '@config.className',
                    'currentStep': '@config.currentStep',
                    'maxSlots': '@config.maxSlots',
                    'onSlotDrop': 'SLOT_DROP',
                    'onSlotRemove': 'SLOT_REMOVE',
                    'playing': '@config.playing',
                    'size': '@config.size',
                    'slotFeedback': '@config.slotFeedback',
                    'slots': '@config.slots',
                    'type': 'sequence-bar',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } as never, 'SequenceBarItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'SequenceBarPage',
        'path': '/sequence-bar',
        'traits': [
          {
            'ref': 'SequenceBarRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiSequenceBarSequenceBarOrbital. */
export const StdUiSequenceBarSequenceBarOrbitalManifest = {
  organism: 'ui-sequence-bar',
  orbitalName: 'SequenceBarOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'SequenceBarRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiSequenceBarSequenceBarOrbitalParams keys. */
export function isStdUiSequenceBarSequenceBarOrbitalParams(p: object): p is StdUiSequenceBarSequenceBarOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiSequenceBarSequenceBarOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiSequenceBarSequenceBarOrbitalManifest.traitNames,
      ...StdUiSequenceBarSequenceBarOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-sequence-bar — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiSequenceBarParams {
  SequenceBar?: StdUiSequenceBarSequenceBarOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiSequenceBar(params: StdUiSequenceBarParams = {}): OrbitalDefinition[] {
  return [
    stdUiSequenceBarSequenceBarOrbital(params.SequenceBar ?? {}),
  ];
}
