/**
 * ui-dashboard-layout — DashboardLayout — UI factory (1:1 with the @almadar/ui dashboard-layout pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/templates/ui-dashboard-layout.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-dashboard-layout
 *
 * @level template
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-dashboard-layout';
const ALIAS = 'UiDashboardLayout';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiDashboardLayoutEventKey = 'INIT' | 'NOTIFICATION_CLICK' | 'SEARCH' | 'SEARCH_SUBMIT' | 'SIGN_OUT';

/**
 * Payload shape for the `SEARCH` event.
 */
export interface StdUiDashboardLayoutSearchPayload {
  value: string;
}

/**
 * Payload shape for the `SEARCH_SUBMIT` event.
 */
export interface StdUiDashboardLayoutSearchSubmitPayload {
  value?: string;
}

/**
 * Payload shape for the `NOTIFICATION_CLICK` event.
 */
export interface StdUiDashboardLayoutNotificationClickPayload {
  id?: string;
}

/**
 * Payload shape for the `SIGN_OUT` event.
 */
export interface StdUiDashboardLayoutSignOutPayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiDashboardLayoutConfig {
  /** Default: `"{{APP_TITLE}}"` */
  appName?: string;
  /** Default: `[{"content":"Sample content","type":"typography"}]` */
  children?: unknown;
  /** Default: `"Current Path"` */
  currentPath?: string;
  headerActions?: unknown;
  /** Default: `"sidebar"` */
  layoutMode?: 'sidebar' | 'topnav' | 'bottomnav' | 'minimal';
  /** Default: `[{"content":"Logo","type":"typography"}]` */
  logo?: unknown;
  /** Default: `[{"badge":"Badge","children":[],"href":"Href","icon":"circle","label":"Label"},{"badge":"Badge 2","children":[],"href":"Href 2","icon":"circle","label":"Label 2"}]` */
  navItems?: EntityRow[];
  /** Default: `"NOTIFICATION_CLICK"` */
  notificationClickEvent?: string;
  /** Default: `[{"createdAt":"Created At","description":"Description","id":"Id","message":"Message","read":false},{"createdAt":"Created At 2","description":"Description 2","id":"Id 2","message":"Message 2","read":true}]` */
  notifications?: EntityRow[];
  /** Default: `"NOTIFICATION_CLICK"` */
  onNotificationClick?: string;
  /** Default: `"SEARCH_SUBMIT"` */
  onSearchSubmit?: string;
  /** Default: `"SIGN_OUT"` */
  onSignOut?: string;
  /** Default: `"SEARCH"` */
  searchEvent?: string;
  /** Default: `false` */
  showSearch?: boolean;
  /** Default: `true` */
  showThemeToggle?: boolean;
  sidebarFooter?: unknown;
  /** Default: `[]` */
  topBarActions?: EntityRow[];
  /** Default: `{"avatar":"https://almadar-kflow-assets.web.app/shared/ui-visual-novel-board/default/portraits/guide.png","email":"Email","name":"Name"}` */
  user?: EntityRow;
}

/**
 * Params for the ui-dashboard-layout descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdUiDashboardLayoutParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdUiDashboardLayoutEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdUiDashboardLayoutConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `UiDashboardLayout.traits.DashboardLayoutRender`. */
export function stdUiDashboardLayoutTrait(params: StdUiDashboardLayoutParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DashboardLayoutRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `UiDashboardLayout.pages.DashboardLayoutPage`. */
export function stdUiDashboardLayoutPage(params: StdUiDashboardLayoutParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DashboardLayoutPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdUiDashboardLayout(params: StdUiDashboardLayoutParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'DashboardLayoutOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdUiDashboardLayoutTrait(params),
    ],
    pages: [
      stdUiDashboardLayoutPage(params),
    ],
  });
}
