/**
 * std-tree — std-tree — generic hierarchical node browser. Fetches a flat TreeNode collection and renders it through file-tree's flat `items` mode (parentId-nested at render time by the component, not the .lolo); OPEN_NODE is the external handoff for a selected node's id. Rebindable: pass `fields` at the call site to retarget it at any parent-pointer entity (categories, org units, comment threads).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-tree.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-tree
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-tree';
const ALIAS = 'Tree';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTreeEventKey = 'INIT' | 'OPEN_NODE' | 'RETRY' | 'TreeNodeLoadFailed' | 'TreeNodeLoaded';

/**
 * Payload shape for the `OPEN_NODE` event.
 */
export interface StdTreeOpenNodePayload {
  id: string;
}

/**
 * Payload shape for the `TreeNodeLoaded` event.
 */
export interface StdTreeTreeNodeLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TreeNodeLoadFailed` event.
 */
export interface StdTreeTreeNodeLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTreeConfig {
  /** Default: `"Tree"` */
  title?: string;
}

/**
 * Params for the std-tree descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTreeParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTreeEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTreeConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Tree.traits.TreeBrowse`. */
export function stdTreeTrait(params: StdTreeParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TreeBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Tree.pages.TreePage`. */
export function stdTreePage(params: StdTreeParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TreePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTree(params: StdTreeParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TreeNodeOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTreeTrait(params),
    ],
    pages: [
      stdTreePage(params),
    ],
  });
}

type _StdTreeEntityName = 'TreeNode';
type _StdTreeListenTraitName = 'TreeBrowse';

/**
 * Tunable params for the TreeNodeOrbital orbital.
 *
 * Canonical entity: TreeNode — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdTreeTreeNodeOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'TreeBrowse',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait TreeNodeOrbital's `uses[]` exports. */
type _StdTreeTreeNodeOrbitalUsesRef = never;

/** Per-orbital factory: builds the TreeNodeOrbital orbital with consumer params. */
export function stdTreeTreeNodeOrbital(params: StdTreeTreeNodeOrbitalParams = {}): OrbitalDefinition {
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'treenodes');
  const built = makeOrbitalWithUses({
    name: 'TreeNodeOrbital',
    uses: [],
    entity: {
      name: 'TreeNode',
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'description': 'The descriptive label for this node.',
            'name': 'name',
            'required': true,
            'synonyms': 'label, title, text',
            'type': 'string',
          },
          {
            'default': '',
            'description': 'Reference to the parent node in the hierarchy; empty marks a root.',
            'name': 'parentId',
            'synonyms': 'parent, superior, ancestor',
            'type': 'string',
          },
          {
            'default': '',
            'description': 'Icon name shown beside the node.',
            'name': 'icon',
            'synonyms': 'glyph, symbol, pictogram',
            'type': 'string',
          },
          {
            'name': 'createdAt',
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'title': {
            'default': 'Tree',
            'description': 'Heading shown above the tree',
            'label': 'Section title',
            'tier': 'presentation',
            'type': 'string',
          },
        },
        'emits': [
          {
            'description': 'Signals a tree node has been selected for external handoff.',
            'event': 'OPEN_NODE',
            'payloadSchema': [
              {
                'name': 'id',
                'required': true,
                'type': 'string',
              },
            ],
            'synonyms': 'select, choose, open, activate',
            'tier': 'domain',
          },
          {
            'description': 'Indicates the tree node collection has been successfully retrieved.',
            'event': 'TreeNodeLoaded',
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[TreeNode]',
              },
            ],
            'synonyms': 'loaded, fetched, retrieved, ready',
            'tier': 'domain',
          },
          {
            'description': 'Indicates a failure to load the tree node collection.',
            'event': 'TreeNodeLoadFailed',
            'payloadSchema': [
              {
                'name': 'error',
                'type': 'string',
              },
              {
                'name': 'code',
                'type': 'string',
              },
            ],
            'synonyms': 'error, failed, problem, unsuccessful',
            'tier': 'internal',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'TreeNode',
        'name': 'TreeBrowse',
        'scope': 'collection',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Indicates the tree node collection has been successfully retrieved.',
              'key': 'TreeNodeLoaded',
              'name': 'TreeNode loaded',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[TreeNode]',
                },
              ],
              'synonyms': 'loaded, fetched, retrieved, ready',
              'tier': 'domain',
            },
            {
              'description': 'Indicates a failure to load the tree node collection.',
              'key': 'TreeNodeLoadFailed',
              'name': 'TreeNode load failed',
              'payloadSchema': [
                {
                  'name': 'error',
                  'type': 'string',
                },
                {
                  'name': 'code',
                  'type': 'string',
                },
              ],
              'synonyms': 'error, failed, problem, unsuccessful',
              'tier': 'internal',
            },
            {
              'description': 'Signals a tree node has been selected for external handoff.',
              'key': 'OPEN_NODE',
              'name': 'Open Node',
              'payloadSchema': [
                {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
              ],
              'synonyms': 'select, choose, open, activate',
              'tier': 'domain',
            },
            {
              'key': 'RETRY',
              'name': 'Retry',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'loading',
            },
            {
              'name': 'browsing',
            },
            {
              'name': 'error',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'fetch',
                  ('TreeNode' satisfies _StdTreeEntityName),
                  {
                    'emit': {
                      'failure': 'TreeNodeLoadFailed',
                      'success': 'TreeNodeLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'center',
                    'children': [
                      {
                        'type': 'spinner',
                      },
                      {
                        'color': 'muted',
                        'content': 'Loading tree…',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                    ],
                    'className': 'py-12',
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'loading',
              'to': 'loading',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'align': 'center',
                        'children': [
                          {
                            'name': 'list-tree',
                            'type': 'icon',
                          },
                          {
                            'content': '@config.title',
                            'type': 'typography',
                            'variant': 'h3',
                          },
                          {
                            'label': [
                              'array/len',
                              '@payload.data',
                            ],
                            'size': 'sm',
                            'type': 'badge',
                            'variant': 'neutral',
                          },
                        ],
                        'direction': 'horizontal',
                        'gap': 'sm',
                        'type': 'stack',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'items': [
                          'array/map',
                          '@payload.data',
                          [
                            'fn',
                            'row',
                            {
                              'icon': '@row.icon',
                              'id': '@row.id',
                              'label': '@row.name',
                              'parentId': '@row.parentId',
                            },
                          ],
                        ],
                        'onNodeSelect': 'OPEN_NODE',
                        'type': 'file-tree',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'TreeNodeLoaded',
              'from': 'loading',
              'to': 'browsing',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'center',
                    'children': [
                      {
                        'color': 'error',
                        'name': 'alert-triangle',
                        'type': 'icon',
                      },
                      {
                        'content': 'Failed to load tree',
                        'type': 'typography',
                        'variant': 'body',
                      },
                      {
                        'color': 'muted',
                        'content': '@payload.error',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                      {
                        'action': 'RETRY',
                        'icon': 'rotate-ccw',
                        'label': 'Retry',
                        'type': 'button',
                        'variant': 'ghost',
                      },
                    ],
                    'className': 'py-8',
                    'direction': 'vertical',
                    'gap': 'sm',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'TreeNodeLoadFailed',
              'from': 'loading',
              'to': 'error',
            },
            {
              'effects': [
                [
                  'fetch',
                  ('TreeNode' satisfies _StdTreeEntityName),
                  {
                    'emit': {
                      'failure': 'TreeNodeLoadFailed',
                      'success': 'TreeNodeLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'center',
                    'children': [
                      {
                        'type': 'spinner',
                      },
                      {
                        'color': 'muted',
                        'content': 'Loading tree…',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                    ],
                    'className': 'py-12',
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'browsing',
              'to': 'loading',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.id',
                  '@payload.id',
                ],
              ],
              'event': 'OPEN_NODE',
              'from': 'browsing',
              'to': 'browsing',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'align': 'center',
                        'children': [
                          {
                            'name': 'list-tree',
                            'type': 'icon',
                          },
                          {
                            'content': '@config.title',
                            'type': 'typography',
                            'variant': 'h3',
                          },
                          {
                            'label': [
                              'array/len',
                              '@payload.data',
                            ],
                            'size': 'sm',
                            'type': 'badge',
                            'variant': 'neutral',
                          },
                        ],
                        'direction': 'horizontal',
                        'gap': 'sm',
                        'type': 'stack',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'items': [
                          'array/map',
                          '@payload.data',
                          [
                            'fn',
                            'row',
                            {
                              'icon': '@row.icon',
                              'id': '@row.id',
                              'label': '@row.name',
                              'parentId': '@row.parentId',
                            },
                          ],
                        ],
                        'onNodeSelect': 'OPEN_NODE',
                        'type': 'file-tree',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'TreeNodeLoaded',
              'from': 'browsing',
              'to': 'browsing',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'center',
                    'children': [
                      {
                        'color': 'error',
                        'name': 'alert-triangle',
                        'type': 'icon',
                      },
                      {
                        'content': 'Failed to load tree',
                        'type': 'typography',
                        'variant': 'body',
                      },
                      {
                        'color': 'muted',
                        'content': '@payload.error',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                      {
                        'action': 'RETRY',
                        'icon': 'rotate-ccw',
                        'label': 'Retry',
                        'type': 'button',
                        'variant': 'ghost',
                      },
                    ],
                    'className': 'py-8',
                    'direction': 'vertical',
                    'gap': 'sm',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'TreeNodeLoadFailed',
              'from': 'browsing',
              'to': 'error',
            },
            {
              'effects': [
                [
                  'fetch',
                  ('TreeNode' satisfies _StdTreeEntityName),
                  {
                    'emit': {
                      'failure': 'TreeNodeLoadFailed',
                      'success': 'TreeNodeLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'spinner',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'error',
              'to': 'loading',
            },
            {
              'effects': [
                [
                  'fetch',
                  ('TreeNode' satisfies _StdTreeEntityName),
                  {
                    'emit': {
                      'failure': 'TreeNodeLoadFailed',
                      'success': 'TreeNodeLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'spinner',
                  },
                ],
              ],
              'event': 'RETRY',
              'from': 'error',
              'to': 'loading',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'TreePage',
        'path': '/tree',
        'traits': [
          {
            'ref': 'TreeBrowse',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdTreeTreeNodeOrbital. */
export const StdTreeTreeNodeOrbitalManifest = {
  organism: 'std-tree',
  orbitalName: 'TreeNodeOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'TreeBrowse',
  ] as const,
};

/** Typed guard — runtime validates StdTreeTreeNodeOrbitalParams keys. */
export function isStdTreeTreeNodeOrbitalParams(p: object): p is StdTreeTreeNodeOrbitalParams {
  type _OverrideRecord = NonNullable<StdTreeTreeNodeOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdTreeTreeNodeOrbitalManifest.traitNames,
      ...StdTreeTreeNodeOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
