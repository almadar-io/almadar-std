/**
 * std-geosearch — std-geosearch — location-aware search variant: search-input + map-view + range-slider for radius. Owns a GeoSearchResult runtime entity; GEO_SEARCH persists @payload.searchTerm/lat/lng/radiusKm so cross-trait listeners can route them into geo-aware data atoms.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-geosearch.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-geosearch
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-geosearch';
const ALIAS = 'Geosearch';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGeosearchEventKey = 'GEO_SEARCH' | 'INIT';

/**
 * Payload shape for the `GEO_SEARCH` event.
 */
export interface StdGeosearchGeoSearchPayload {
  searchTerm: string;
  lat: number;
  lng: number;
  radiusKm: number;
  value?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGeosearchConfig {
  /** Default: `10` */
  defaultRadiusKm?: number;
  /** Default: `"GEO_SEARCH"` */
  event?: string;
  /** Default: `"Search nearby…"` */
  placeholder?: string;
}

/**
 * Params for the std-geosearch descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGeosearchParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGeosearchEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGeosearchConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Geosearch.traits.GeoSearchResultSearch`. */
export function stdGeosearchTrait(params: StdGeosearchParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GeoSearchResultSearch`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Geosearch.pages.GeoSearchResultSearchPage`. */
export function stdGeosearchPage(params: StdGeosearchParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GeoSearchResultSearchPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGeosearch(params: StdGeosearchParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GeoSearchResultOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGeosearchTrait(params),
    ],
    pages: [
      stdGeosearchPage(params),
    ],
  });
}

type _StdGeosearchEntityName = 'GeoSearchResult' | 'SearchInputItem' | 'MapViewItem' | 'RangeSliderItem' | 'StackItem';
type _StdGeosearchListenTraitName = 'GeoSearchResultSearch';

/**
 * Tunable params for the GeoSearchResultOrbital orbital.
 *
 * Canonical entity: GeoSearchResult — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdGeosearchGeoSearchResultOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'GeoSearchResultSearch',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait GeoSearchResultOrbital's `uses[]` exports. */
type _StdGeosearchGeoSearchResultOrbitalUsesRef = 'UiSearchInput.traits.SearchInputRender' | 'UiMapView.traits.MapViewRender' | 'UiRangeSlider.traits.RangeSliderRender' | 'UiStack.traits.StackRender';

/** Per-orbital factory: builds the GeoSearchResultOrbital orbital with consumer params. */
export function stdGeosearchGeoSearchResultOrbital(params: StdGeosearchGeoSearchResultOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'GeoSearchResultOrbital',
    uses: [
      {
        'as': 'UiSearchInput',
        'from': 'std/behaviors/ui-search-input',
      },
      {
        'as': 'UiMapView',
        'from': 'std/behaviors/ui-map-view',
      },
      {
        'as': 'UiRangeSlider',
        'from': 'std/behaviors/ui-range-slider',
      },
      {
        'as': 'UiStack',
        'from': 'std/behaviors/ui-stack',
      },
    ],
    entity: {
      name: 'GeoSearchResult',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'description': 'The user\'s search query string.',
            'name': 'searchTerm',
            'synonyms': 'query, text, input',
            'type': 'string',
          },
          {
            'description': 'Latitude coordinate of the search center.',
            'name': 'centerLat',
            'synonyms': 'lat, y, north',
            'type': 'number',
          },
          {
            'description': 'The longitude coordinate of the search center.',
            'name': 'centerLng',
            'synonyms': 'longitude, lng',
            'type': 'number',
          },
          {
            'default': 10,
            'description': 'The search radius in kilometers.',
            'name': 'radiusKm',
            'synonyms': 'range, distance, extent',
            'type': 'number',
          },
          {
            'description': 'Northern boundary coordinate of the search area.',
            'name': 'boundsNorth',
            'synonyms': 'north, upper, top',
            'type': 'number',
          },
          {
            'description': 'Southern boundary coordinate of the search area.',
            'name': 'boundsSouth',
            'synonyms': 'south, bottom, lowerBoundary',
            'type': 'number',
          },
          {
            'description': 'The eastern boundary coordinate of the search area.',
            'name': 'boundsEast',
            'synonyms': 'east, right, longitude',
            'type': 'number',
          },
          {
            'description': 'Western boundary coordinate of the search area.',
            'name': 'boundsWest',
            'synonyms': 'west, longitude, x',
            'type': 'number',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'defaultRadiusKm': {
            'default': 10,
            'description': 'Initial search radius around the chosen center',
            'label': 'Default radius (km)',
            'tier': 'presentation',
            'type': 'number',
          },
          'event': {
            'default': 'GEO_SEARCH',
            'description': 'Event emitted when the user runs a geo search',
            'label': 'Search event',
            'tier': 'internal',
            'type': 'event',
          },
          'placeholder': {
            'default': 'Search nearby…',
            'description': 'Hint text in the location search input',
            'label': 'Search placeholder',
            'tier': 'presentation',
            'type': 'string',
          },
        },
        'emits': [
          {
            'description': 'Indicates a search request with location parameters.',
            'event': 'GEO_SEARCH',
            'payloadSchema': [
              {
                'name': 'searchTerm',
                'required': true,
                'type': 'string',
              },
              {
                'name': 'lat',
                'required': true,
                'type': 'number',
              },
              {
                'name': 'lng',
                'required': true,
                'type': 'number',
              },
              {
                'name': 'radiusKm',
                'required': true,
                'type': 'number',
              },
              {
                'name': 'value',
                'type': 'number',
              },
            ],
            'synonyms': 'search, locate, find, query',
            'tier': 'domain',
          },
        ],
        'linkedEntity': 'GeoSearchResult',
        'name': 'GeoSearchResultSearch',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Indicates a search request with location parameters.',
              'key': 'GEO_SEARCH',
              'name': 'Geo Search',
              'payloadSchema': [
                {
                  'name': 'searchTerm',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'lat',
                  'required': true,
                  'type': 'number',
                },
                {
                  'name': 'lng',
                  'required': true,
                  'type': 'number',
                },
                {
                  'name': 'radiusKm',
                  'required': true,
                  'type': 'number',
                },
                {
                  'name': 'value',
                  'type': 'number',
                },
              ],
              'synonyms': 'search, locate, find, query',
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'event': '@config.event',
                        'placeholder': '@config.placeholder',
                        'type': 'search-input',
                      },
                      {
                        'centerLat': '@entity.centerLat',
                        'centerLng': '@entity.centerLng',
                        'height': '240px',
                        'markers': [],
                        'type': 'map-view',
                        'zoom': 12,
                      },
                      {
                        'action': 'GEO_SEARCH',
                        'max': 100,
                        'min': 1,
                        'showTooltip': true,
                        'step': 1,
                        'type': 'range-slider',
                        'value': '@entity.radiusKm',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.searchTerm',
                  '@payload.searchTerm',
                ],
                [
                  'set',
                  '@entity.centerLat',
                  '@payload.lat',
                ],
                [
                  'set',
                  '@entity.centerLng',
                  '@payload.lng',
                ],
                [
                  'set',
                  '@entity.radiusKm',
                  '@payload.radiusKm',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'event': '@config.event',
                        'placeholder': '@config.placeholder',
                        'type': 'search-input',
                      },
                      {
                        'centerLat': '@entity.centerLat',
                        'centerLng': '@entity.centerLng',
                        'height': '240px',
                        'markers': [],
                        'type': 'map-view',
                        'zoom': 12,
                      },
                      {
                        'action': 'GEO_SEARCH',
                        'max': 100,
                        'min': 1,
                        'showTooltip': true,
                        'step': 1,
                        'type': 'range-slider',
                        'value': '@entity.radiusKm',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'GEO_SEARCH',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'GeoSearchResultSearchPage',
        'path': '/geosearchresults/search',
        'traits': [
          {
            'ref': 'GeoSearchResultSearch',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdGeosearchGeoSearchResultOrbital. */
export const StdGeosearchGeoSearchResultOrbitalManifest = {
  organism: 'std-geosearch',
  orbitalName: 'GeoSearchResultOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'GeoSearchResultSearch',
  ] as const,
};

/** Typed guard — runtime validates StdGeosearchGeoSearchResultOrbitalParams keys. */
export function isStdGeosearchGeoSearchResultOrbitalParams(p: object): p is StdGeosearchGeoSearchResultOrbitalParams {
  type _OverrideRecord = NonNullable<StdGeosearchGeoSearchResultOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdGeosearchGeoSearchResultOrbitalManifest.traitNames,
      ...StdGeosearchGeoSearchResultOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
