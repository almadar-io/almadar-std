/**
 * std-gantt-schedule — std-gantt-schedule — GENERIC view-only Gantt/timeline atom: fetches a task collection on INIT and renders it as bars on a day-scale axis via the gantt pattern, with group headers, dependency arrows, and a today marker. Fully rebindable: a host overrides the linked entity and points the field-mapping configs (labelField/startField/endField/durationField/statusField/groupField) at its own columns; clicking a bar emits the config-named onBarClick event with the row id. No drag-edit, no zoom — schedule viewing only.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-gantt-schedule.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-gantt-schedule
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-gantt-schedule';
const ALIAS = 'GanttSchedule';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGanttScheduleEventKey = '' | 'INIT' | 'RETRY' | 'TaskLoadFailed' | 'TaskLoaded';

/**
 * Payload shape for the `TaskLoaded` event.
 */
export interface StdGanttScheduleTaskLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TaskLoadFailed` event.
 */
export interface StdGanttScheduleTaskLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `` event.
 */
export interface StdGanttSchedulePayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGanttScheduleConfig {
  /** Default: `28` */
  dayWidth?: number;
  /** Default: `""` */
  durationField?: string;
  /** Default: `"end"` */
  endField?: string;
  /** Default: `""` */
  groupField?: string;
  /** Default: `[]` */
  include?: string[];
  /** Default: `"title"` */
  labelField?: string;
  /** Default: `[]` */
  links?: EntityRow[];
  /** Default: `""` */
  onBarClick?: string;
  /** Default: `""` */
  rangeEnd?: string;
  /** Default: `""` */
  rangeStart?: string;
  /** Default: `true` */
  showToday?: boolean;
  /** Default: `"start"` */
  startField?: string;
  /** Default: `"status"` */
  statusField?: string;
  /** Default: `"Schedule"` */
  title?: string;
}

/**
 * Params for the std-gantt-schedule descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGanttScheduleParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGanttScheduleEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGanttScheduleConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GanttSchedule.traits.TaskGanttSchedule`. */
export function stdGanttScheduleTrait(params: StdGanttScheduleParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TaskGanttSchedule`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GanttSchedule.pages.TaskGanttSchedulePage`. */
export function stdGanttSchedulePage(params: StdGanttScheduleParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TaskGanttSchedulePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGanttSchedule(params: StdGanttScheduleParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GanttScheduleOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGanttScheduleTrait(params),
    ],
    pages: [
      stdGanttSchedulePage(params),
    ],
  });
}
