/**
 * std-event-log — std-event-log — generic chronological event feed (Stripe activity log / GitHub PR timeline / Linear event feed style). Renders a vertical `timeline` with type-coded dots, a single horizontal chip-bar of kind filters (All + per-kind), reverse-chronological sort, and an inline form for logging events that weren't auto-captured. `config.kindOptions` maps each kind → status/icon/label so consuming molecules/organisms get type-coded visuals without re-implementing topology. Topology = loading / viewing / backfilling / error.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-event-log.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-event-log
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-event-log';
const ALIAS = 'EventLog';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdEventLogEventKey = 'APPLY_FILTER' | 'CANCEL_BACKFILL' | 'EventLogLoadFailed' | 'EventLogLoaded' | 'EventLogSaveFailed' | 'EventLogSaved' | 'INIT' | 'OPEN_BACKFILL' | 'SAVE_BACKFILL';

/**
 * Payload shape for the `APPLY_FILTER` event.
 */
export interface StdEventLogApplyFilterPayload {
  tabId?: string;
}

/**
 * Payload shape for the `SAVE_BACKFILL` event.
 */
export interface StdEventLogSaveBackfillPayload {
  data?: EntityRow;
}

/**
 * Payload shape for the `EventLogLoaded` event.
 */
export interface StdEventLogEventLogLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `EventLogLoadFailed` event.
 */
export interface StdEventLogEventLogLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `EventLogSaved` event.
 */
export interface StdEventLogEventLogSavedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `EventLogSaveFailed` event.
 */
export interface StdEventLogEventLogSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdEventLogConfig {
  /** Default: `"elevated"` */
  cardLook?: 'elevated' | 'flat-bordered' | 'borderless-divider' | 'ticket' | 'invoice' | 'chip' | 'tile-image-first';
  /** Default: `["backfillTitle","backfillDescription","backfillKind","backfillDate"]` */
  formFields?: string[];
  /** Default: `[{"icon":"plus-circle","key":"created","label":"Created","status":"active"},{"icon":"edit-3","key":"updated","label":"Updated","status":"pending"},{"icon":"check-circle","key":"approved","label":"Approved","status":"complete"},{"icon":"x-circle","key":"rejected","label":"Rejected","status":"error"}]` */
  kindOptions?: EntityRow[];
  /** Default: `"vertical-spacious"` */
  timelineLook?: 'vertical-compact' | 'vertical-spacious' | 'horizontal' | 'swimlane';
  /** Default: `"Activity"` */
  title?: string;
}

/**
 * Params for the std-event-log descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdEventLogParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdEventLogEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdEventLogConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `EventLog.traits.EventLogTimeline`. */
export function stdEventLogTrait(params: StdEventLogParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.EventLogTimeline`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `EventLog.pages.EventLogPage`. */
export function stdEventLogPage(params: StdEventLogParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.EventLogPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdEventLog(params: StdEventLogParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'EventLogOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdEventLogTrait(params),
    ],
    pages: [
      stdEventLogPage(params),
    ],
  });
}
