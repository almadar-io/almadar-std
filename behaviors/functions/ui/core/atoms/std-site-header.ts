/**
 * std-site-header — std-site-header — the sticky top bar of a public website: brand (logo + name), navigation links with the current page marked, and an optional call-to-action button. On small screens the links fold into a menu opened by a toggle. Links follow `href` (in-app path, #anchor, or absolute URL); the header action and the small-screen menu links also emit NAV_CLICKED { href, label }. Pick for marketing sites, docs portals and product websites; for signed-in app chrome use std-app-layout.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-site-header.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-site-header
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-site-header';
const ALIAS = 'SiteHeader';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSiteHeaderEventKey = 'INIT' | 'MENU_TOGGLE' | 'NAV_CLICKED' | 'NAV_LINK';

/**
 * Payload shape for the `NAV_CLICKED` event.
 */
export interface StdSiteHeaderNavClickedPayload {
  href?: string;
  label?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSiteHeaderConfig {
  /** Default: `""` */
  actionHref?: string;
  /** Default: `""` */
  actionLabel?: string;
  /** Default: `"Almadar"` */
  brandName?: string;
  /** Default: `"/"` */
  currentPath?: string;
  /** Default: `{"children":[{"actions":{"action":"NAV_LINK","actionPayload":{"href":"@config.actionHref","label":"@config.actionLabel"},"className":["if",["==","@config.actionLabel",""],"hidden",""],"href":"@config.actionHref","label":"@config.actionLabel","size":"sm","type":"button","variant":"primary"},"brandName":"@config.brandName","className":"hidden md:flex","logoSrc":"@config.logoSrc","navigationItems":["array/map","@config.links",["fn","entry",{"active":["==",["object/get","@entry","href",""],"@config.currentPath"],"href":["object/get","@entry","href",""],"label":["object/get","@entry","label",""]}]],"showMenuToggle":false,"sticky":false,"type":"header","variant":"desktop"},{"brandName":"@config.brandName","className":"md:hidden","isMenuOpen":"@entity.menuOpen","logoSrc":"@config.logoSrc","onMenuToggle":"MENU_TOGGLE","showMenuToggle":true,"sticky":false,"type":"header","variant":"mobile"},{"align":"stretch","children":["array/map","@config.links",["fn","entry",{"action":"NAV_LINK","actionPayload":{"href":["object/get","@entry","href",""],"label":["object/get","@entry","label",""]},"className":"justify-start","href":["object/get","@entry","href",""],"label":["object/get","@entry","label",""],"type":"button","variant":["if",["==",["object/get","@entry","href",""],"@config.currentPath"],"secondary","ghost"]}]],"className":["if","@entity.menuOpen","md:hidden border-b border-border bg-card px-4 py-3","hidden"],"direction":"vertical","gap":"xs","type":"stack"}],"className":"sticky top-0 z-50","direction":"vertical","gap":"none","type":"stack"}` */
  headerContent?: unknown;
  /** Default: `[{"href":"/features","label":"Features"},{"href":"/pricing","label":"Pricing"},{"href":"/about","label":"About"}]` */
  links?: EntityRow[];
  /** Default: `""` */
  logoSrc?: string;
}

/**
 * Params for the std-site-header descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSiteHeaderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSiteHeaderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSiteHeaderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SiteHeader.traits.SiteHeaderRender`. */
export function stdSiteHeaderTrait(params: StdSiteHeaderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SiteHeaderRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SiteHeader.pages.SiteHeaderPage`. */
export function stdSiteHeaderPage(params: StdSiteHeaderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SiteHeaderPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSiteHeader(params: StdSiteHeaderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SiteHeaderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSiteHeaderTrait(params),
    ],
    pages: [
      stdSiteHeaderPage(params),
    ],
  });
}
