/**
 * std-export — std-export — a dropdown format menu (CSV/XLSX/PDF defaults; caller can override via config.formats). Emits EXPORT_REQUESTED { itemId, label } on the bus — itemId is the picked format id; host or sibling orbital generates the file. A format entry may carry `url`: the menu then downloads that file directly on pick (gesture-driven; the event still emits) — used for pre-baked artifacts like sprite sheets. tileId is a static per-instance config value, not echoed in the click payload.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-export.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-export
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-export';
const ALIAS = 'Export';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdExportConfig {
  /** Default: `"OPEN_EXPORT"` */
  action?: unknown;
  /** Default: `"download"` */
  icon?: unknown;
  /** Default: `"@config.buttonLabel"` */
  label?: unknown;
  /** Default: `"sm"` */
  size?: unknown;
  /** Default: `"ghost"` */
  variant?: unknown;
}

/**
 * Params for the std-export descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdExportParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map (atom key → caller key). */
  events?: Record<string, string>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdExportConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Export.traits.Button1`. */
export function stdExportButton1Trait(params: StdExportParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.Button1`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `Export.traits.Menu1`. */
export function stdExportMenu1Trait(params: StdExportParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.Menu1`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `Export.traits.ExportMenu`. */
export function stdExportExportMenuTrait(params: StdExportParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ExportMenu`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Export.pages.ExportMenuPage`. */
export function stdExportPage(params: StdExportParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ExportMenuPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdExport(params: StdExportParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ExportOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdExportButton1Trait(params),
      stdExportMenu1Trait(params),
      stdExportExportMenuTrait(params),
    ],
    pages: [
      stdExportPage(params),
    ],
  });
}

type _StdExportEntityName = 'ExportPanel' | 'ButtonItem' | 'MenuItem';
type _StdExportListenTraitName = 'Button1' | 'Menu1' | 'ExportMenu';

/**
 * Tunable params for the ExportOrbital orbital.
 *
 * Canonical entity: ExportPanel — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdExportExportOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'Button1' | 'Menu1' | 'ExportMenu',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait ExportOrbital's `uses[]` exports. */
type _StdExportExportOrbitalUsesRef = 'Button.traits.ButtonRender' | 'Menu.traits.MenuRender';

/** Per-orbital factory: builds the ExportOrbital orbital with consumer params. */
export function stdExportExportOrbital(params: StdExportExportOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'ExportOrbital',
    uses: [
      {
        'as': 'Button',
        'from': 'std/behaviors/ui-button',
      },
      {
        'as': 'Menu',
        'from': 'std/behaviors/ui-menu',
      },
    ],
    entity: {
      name: 'ExportPanel',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'config': {
          'action': {
            'default': 'OPEN_EXPORT',
            'type': 'unknown',
          },
          'icon': {
            'default': 'download',
            'type': 'unknown',
          },
          'label': {
            'default': '@config.buttonLabel',
            'type': 'unknown',
          },
          'size': {
            'default': 'sm',
            'type': 'unknown',
          },
          'variant': {
            'default': 'ghost',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'ExportPanel',
        'name': 'Button1',
        'ref': ('Button.traits.ButtonRender' satisfies _StdExportExportOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'items': {
            'default': '@config.formats',
            'type': 'unknown',
          },
          'position': {
            'default': 'bottom-start',
            'type': 'unknown',
          },
          'trigger': {
            'default': [
              '@trait.Button1',
            ],
            'type': 'unknown',
          },
        },
        'linkedEntity': 'ExportPanel',
        'name': 'Menu1',
        'ref': ('Menu.traits.MenuRender' satisfies _StdExportExportOrbitalUsesRef),
      }),
      {
        'category': 'interaction',
        'config': {
          'buttonLabel': {
            'default': 'Export',
            'description': 'Text shown on the toolbar button that opens the export popover',
            'label': 'Button label',
            'synonyms': 'the text on the export trigger button. user phrases: \'rename export button to X\' / \'set the button label to X\' / \'call the button X\' / \'change export button text to X\' -> X (e.g. \'Download CSV\', \'Download\', \'Export Data\')',
            'tier': 'presentation',
            'type': 'string',
          },
          'event': {
            'default': 'EXPORT_REQUESTED',
            'description': 'Event emitted when the user picks a format. Each entry in formats[] carries its own literal event field (no per-item templating in .lolo) — overriding this alone does not retarget the click; also set formats[].event to match.',
            'label': 'Export event',
            'synonyms': 'bus event name. internal wiring; users rarely target directly',
            'tier': 'internal',
            'type': 'string',
          },
          'formats': {
            'default': [
              {
                'event': 'EXPORT_REQUESTED',
                'format': 'csv',
                'id': 'csv',
                'label': 'CSV',
              },
              {
                'event': 'EXPORT_REQUESTED',
                'format': 'xlsx',
                'id': 'xlsx',
                'label': 'XLSX',
              },
              {
                'event': 'EXPORT_REQUESTED',
                'format': 'pdf',
                'id': 'pdf',
                'label': 'PDF',
              },
            ],
            'description': 'Export formats shown to the user in the popover',
            'items': {
              'properties': {
                'event': {
                  'name': 'event',
                  'required': false,
                  'type': 'string',
                },
                'format': {
                  'name': 'format',
                  'required': true,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
                'url': {
                  'name': 'url',
                  'required': false,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Available formats',
            'synonyms': 'list of export format entries (csv / xlsx / pdf / png / json). user phrases: \'only CSV\' -> keep only csv entry; \'add JSON\' -> append json entry; \'no PNG\' -> remove png; \'allow X and Y\' -> set the array to those two',
            'tier': 'presentation',
            'type': '[FormatOption]',
          },
          'tileId': {
            'default': '',
            'description': 'Identifier of the dashboard tile being exported',
            'label': 'Tile id',
            'synonyms': 'internal identifier — host orbital wires this; users rarely set it directly',
            'tier': 'internal',
            'type': 'string',
          },
        },
        'emits': [
          {
            'description': 'Requests export of data in the picked format. itemId is the format id (matches the FormatOption.id/format of the picked entry).',
            'event': 'EXPORT_REQUESTED',
            'payloadSchema': [
              {
                'name': 'itemId',
                'required': true,
                'type': 'string',
              },
              {
                'name': 'label',
                'required': true,
                'type': 'string',
              },
            ],
            'scope': 'external',
            'synonyms': 'export, save, download, generate',
            'tier': 'domain',
          },
        ],
        'linkedEntity': 'ExportPanel',
        'name': 'ExportMenu',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'OPEN_EXPORT',
              'name': 'Open Export',
            },
            {
              'description': 'Requests export of data in the picked format. itemId is the format id (matches the FormatOption.id/format of the picked entry).',
              'key': 'EXPORT_REQUESTED',
              'name': 'Export Requested',
              'payloadSchema': [
                {
                  'name': 'itemId',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
              ],
              'synonyms': 'export, save, download, generate',
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      '@trait.Menu1',
                    ],
                    'type': 'box',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'event': 'OPEN_EXPORT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'event': 'EXPORT_REQUESTED',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'ExportMenuPage',
        'path': '/exports/menu',
        'traits': [
          {
            'ref': 'ExportMenu',
          },
          {
            'ref': 'Button1',
          },
          {
            'ref': 'Menu1',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdExportExportOrbital. */
export const StdExportExportOrbitalManifest = {
  organism: 'std-export',
  orbitalName: 'ExportOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'Button1',
    'Menu1',
  ] as const,
  inlineTraitNames: [
    'ExportMenu',
  ] as const,
};

/** Typed guard — runtime validates StdExportExportOrbitalParams keys. */
export function isStdExportExportOrbitalParams(p: object): p is StdExportExportOrbitalParams {
  type _OverrideRecord = NonNullable<StdExportExportOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdExportExportOrbitalManifest.traitNames,
      ...StdExportExportOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
