/**
 * std-tag-strip — std-tag-strip — inline tag editing ON the document surface: the record's tags render as removable chips with a type-to-add input (a typed name that matches an existing tag attaches it; an unknown name is created in the shared taxonomy first), plus one-click chips for every existing tag not yet attached. Emits TAGS_CHANGED { id, tagIds } — the host organism routes it into its persistor's update; the document's refetch re-scopes the strip. Binds any record carrying a tagIds relation array; the tag store is the shared `tags` collection (std-tag-taxonomy's).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-tag-strip.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-tag-strip
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-tag-strip';
const ALIAS = 'TagStrip';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTagStripEventKey = 'ADD_TAG' | 'INIT' | 'REMOVE_TAG' | 'SHOW_TAG_STRIP' | 'TAGS_CHANGED' | 'TagStripAddOptions' | 'TagStripLoadFailed' | 'TagStripOptions' | 'TagStripRemoveOptions' | 'TagStripTagCreated';

/**
 * Payload shape for the `TAGS_CHANGED` event.
 */
export interface StdTagStripTagsChangedPayload {
  id?: string;
  tagIds?: string[];
}

/**
 * Payload shape for the `ADD_TAG` event.
 */
export interface StdTagStripAddTagPayload {
  tag: string;
}

/**
 * Payload shape for the `REMOVE_TAG` event.
 */
export interface StdTagStripRemoveTagPayload {
  tag?: string;
  value?: string[];
}

/**
 * Payload shape for the `TagStripOptions` event.
 */
export interface StdTagStripTagStripOptionsPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TagStripAddOptions` event.
 */
export interface StdTagStripTagStripAddOptionsPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TagStripRemoveOptions` event.
 */
export interface StdTagStripTagStripRemoveOptionsPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TagStripTagCreated` event.
 */
export interface StdTagStripTagStripTagCreatedPayload {
  id?: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `TagStripLoadFailed` event.
 */
export interface StdTagStripTagStripLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTagStripConfig {
  /** Default: `""` */
  helperText?: string;
  /** Default: `"Add a tag…"` */
  placeholder?: string;
  /** Default: `false` */
  readOnly?: boolean;
  /** Default: `"Tags"` */
  title?: string;
}

/**
 * Params for the std-tag-strip descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTagStripParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTagStripEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTagStripConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TagStrip.traits.TagStrip`. */
export function stdTagStripTrait(params: StdTagStripParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TagStrip`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TagStrip.pages.TagStripDemoPage`. */
export function stdTagStripPage(params: StdTagStripParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TagStripDemoPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTagStrip(params: StdTagStripParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TagStripOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTagStripTrait(params),
    ],
    pages: [
      stdTagStripPage(params),
    ],
  });
}
