/**
 * std-confirmation — std-confirmation as a Function
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-confirmation.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-confirmation
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-confirmation';
const ALIAS = 'Confirmation';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdConfirmationEventKey = 'CANCEL' | 'CLOSE' | 'CONFIRM' | 'ConfirmActionLoadFailed' | 'ConfirmActionLoaded' | 'INIT' | 'REQUEST';

/**
 * Payload shape for the `CONFIRM` event.
 */
export interface StdConfirmationConfirmPayload {
  id?: string;
}

/**
 * Payload shape for the `ConfirmActionLoadFailed` event.
 */
export interface StdConfirmationConfirmActionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `ConfirmActionLoaded` event.
 */
export interface StdConfirmationConfirmActionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdConfirmationConfig {
  /** Default: `"Are you sure?"` */
  alertMessage?: string;
  /** Default: `"Confirm"` */
  confirmLabel?: string;
  /** Default: `"alert-triangle"` */
  icon?: string;
  /** Default: `"Confirm Action"` */
  title?: string;
}

/**
 * Params for the std-confirmation descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdConfirmationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdConfirmationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdConfirmationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Confirmation.traits.ConfirmActionConfirmation`. */
export function stdConfirmationTrait(params: StdConfirmationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ConfirmActionConfirmation`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Confirmation.pages.ConfirmActionConfirmPage`. */
export function stdConfirmationPage(params: StdConfirmationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ConfirmActionConfirmPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdConfirmation(params: StdConfirmationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'ConfirmActionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdConfirmationTrait(params),
    ],
    pages: [
      stdConfirmationPage(params),
    ],
  });
}

type _StdConfirmationEntityName = 'ConfirmAction';
type _StdConfirmationListenTraitName = 'ConfirmActionConfirmation';

/**
 * Tunable params for the ConfirmActionOrbital orbital.
 *
 * Canonical entity: ConfirmAction — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdConfirmationConfirmActionOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ConfirmActionConfirmation',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait ConfirmActionOrbital's `uses[]` exports. */
type _StdConfirmationConfirmActionOrbitalUsesRef = never;

/** Per-orbital factory: builds the ConfirmActionOrbital orbital with consumer params. */
export function stdConfirmationConfirmActionOrbital(params: StdConfirmationConfirmActionOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'ConfirmActionOrbital',
    uses: [],
    entity: {
      name: 'ConfirmAction',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
          },
          {
            'description': 'A user-facing identifier for the confirmation action.',
            'name': 'name',
            'synonyms': 'label, title, identifier',
            'type': 'string',
          },
          {
            'description': 'A detailed explanation of the action being confirmed.',
            'name': 'description',
            'synonyms': 'details, explanation, notes, information',
            'type': 'string',
          },
          {
            'default': 'active',
            'description': 'Indicates the current operational state of the action.',
            'name': 'status',
            'synonyms': 'state, condition, enabled',
            'type': 'string',
            'values': [
              'active',
              'inactive',
              'pending',
            ],
          },
          {
            'name': 'createdAt',
            'type': 'string',
          },
          {
            'default': '',
            'description': 'Unique identifier for the pending confirmation request.',
            'name': 'pendingId',
            'synonyms': 'requestId, referenceId, confirmationId',
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'alertMessage': {
            'default': 'Are you sure?',
            'description': 'Body text explaining what the user is confirming',
            'label': 'Alert message',
            'tier': 'presentation',
            'type': 'string',
          },
          'confirmLabel': {
            'default': 'Confirm',
            'description': 'Label on the primary confirm action button',
            'label': 'Confirm button',
            'tier': 'presentation',
            'type': 'string',
          },
          'icon': {
            'default': 'alert-triangle',
            'description': 'Icon name displayed in the confirmation modal header',
            'label': 'Icon',
            'tier': 'presentation',
            'type': 'string',
          },
          'title': {
            'default': 'Confirm Action',
            'description': 'Heading shown at the top of the confirmation modal',
            'label': 'Modal title',
            'tier': 'presentation',
            'type': 'string',
          },
        },
        'emits': [
          {
            'description': 'Indicates the user has approved the action.',
            'event': 'CONFIRM',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
            'synonyms': 'approve, accept, ok',
            'tier': 'domain',
          },
          {
            'description': 'Indicates the action was cancelled by the user.',
            'event': 'CANCEL',
            'synonyms': 'dismiss, reject, undo',
            'tier': 'domain',
          },
          {
            'description': 'Indicates failure to load the confirmation action.',
            'event': 'ConfirmActionLoadFailed',
            'payloadSchema': [
              {
                'name': 'error',
                'type': 'string',
              },
              {
                'name': 'code',
                'type': 'string',
              },
            ],
            'scope': 'internal',
            'synonyms': 'error, failure, failed, problem',
            'tier': 'internal',
          },
          {
            'description': 'Indicates the confirmation action has been successfully loaded.',
            'event': 'ConfirmActionLoaded',
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[ConfirmAction]',
              },
            ],
            'scope': 'internal',
            'synonyms': 'loaded, ready, initialized',
            'tier': 'internal',
          },
        ],
        'entityContract': {
          'provides': [
            'name',
            'pendingId',
          ],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'ConfirmAction',
        'name': 'ConfirmActionConfirmation',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'A request for confirmation before proceeding.',
              'key': 'REQUEST',
              'name': 'Request',
              'payloadSchema': [
                {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'row',
                  'type': 'ConfirmAction',
                },
              ],
              'synonyms': 'prompt, ask, verify',
              'tier': 'domain',
            },
            {
              'description': 'Indicates the user has approved the action.',
              'key': 'CONFIRM',
              'name': 'Confirm',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
              'synonyms': 'approve, accept, ok',
              'tier': 'domain',
            },
            {
              'description': 'Indicates the action was cancelled by the user.',
              'key': 'CANCEL',
              'name': 'Cancel',
              'synonyms': 'dismiss, reject, undo',
              'tier': 'domain',
            },
            {
              'description': 'Indicates the user dismissed the confirmation.',
              'key': 'CLOSE',
              'name': 'Close',
              'synonyms': 'dismiss, decline, reject',
              'tier': 'domain',
            },
            {
              'description': 'Indicates failure to load the confirmation action.',
              'key': 'ConfirmActionLoadFailed',
              'name': 'ConfirmAction load failed',
              'payloadSchema': [
                {
                  'name': 'error',
                  'type': 'string',
                },
                {
                  'name': 'code',
                  'type': 'string',
                },
              ],
              'synonyms': 'error, failure, failed, problem',
              'tier': 'internal',
            },
            {
              'description': 'Indicates the confirmation action has been successfully loaded.',
              'key': 'ConfirmActionLoaded',
              'name': 'ConfirmAction loaded',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[ConfirmAction]',
                },
              ],
              'synonyms': 'loaded, ready, initialized',
              'tier': 'internal',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
            {
              'name': 'confirming',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'fetch',
                  ('ConfirmAction' satisfies _StdConfirmationEntityName),
                  {
                    'emit': {
                      'failure': 'ConfirmActionLoadFailed',
                      'success': 'ConfirmActionLoaded',
                    },
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.pendingId',
                  '@payload.id',
                ],
                [
                  'set',
                  '@entity.name',
                  '@payload.row.name',
                ],
                [
                  'render-ui',
                  'modal',
                  {
                    'children': [
                      {
                        'align': 'center',
                        'children': [
                          {
                            'name': '@config.icon',
                            'type': 'icon',
                          },
                          {
                            'content': '@config.title',
                            'type': 'typography',
                            'variant': 'h3',
                          },
                        ],
                        'direction': 'horizontal',
                        'gap': 'sm',
                        'type': 'stack',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'message': '@config.alertMessage',
                        'type': 'alert',
                        'variant': 'error',
                      },
                      {
                        'content': '@entity.name',
                        'type': 'typography',
                        'variant': 'h4',
                      },
                      {
                        'color': 'muted',
                        'content': '@entity.pendingId',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                      {
                        'children': [
                          {
                            'action': 'CANCEL',
                            'label': 'Cancel',
                            'type': 'button',
                            'variant': 'ghost',
                          },
                          {
                            'action': 'CONFIRM',
                            'actionPayload': {
                              'id': '@entity.pendingId',
                            },
                            'icon': 'check',
                            'label': '@config.confirmLabel',
                            'type': 'button',
                            'variant': 'danger',
                          },
                        ],
                        'direction': 'horizontal',
                        'gap': 'sm',
                        'justify': 'end',
                        'type': 'stack',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'REQUEST',
              'from': 'idle',
              'guard': '@payload.row',
              'to': 'confirming',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'modal',
                  null,
                ],
              ],
              'event': 'CONFIRM',
              'from': 'confirming',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'modal',
                  null,
                ],
              ],
              'event': 'CANCEL',
              'from': 'confirming',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'modal',
                  null,
                ],
              ],
              'event': 'CLOSE',
              'from': 'confirming',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'ConfirmActionConfirmPage',
        'path': '/confirmactions/confirm',
        'traits': [
          {
            'ref': 'ConfirmActionConfirmation',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdConfirmationConfirmActionOrbital. */
export const StdConfirmationConfirmActionOrbitalManifest = {
  organism: 'std-confirmation',
  orbitalName: 'ConfirmActionOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'ConfirmActionConfirmation',
  ] as const,
};

/** Typed guard — runtime validates StdConfirmationConfirmActionOrbitalParams keys. */
export function isStdConfirmationConfirmActionOrbitalParams(p: object): p is StdConfirmationConfirmActionOrbitalParams {
  type _OverrideRecord = NonNullable<StdConfirmationConfirmActionOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdConfirmationConfirmActionOrbitalManifest.traitNames,
      ...StdConfirmationConfirmActionOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
