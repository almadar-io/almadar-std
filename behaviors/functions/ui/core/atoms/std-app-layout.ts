/**
 * std-app-layout — std-app-layout — dashboard-layout chrome (sidebar nav + top bar with search + topBarActions + notifications) wrapping a single contentTrait slot. Use for list-style apps (CMS / helpdesk / CRM): the chrome stays stable while the consumer's content trait renders the data, and the consumer can add domain icons (cart / profile / help) via topBarActions. Bind once per orbital — the consumer rebinds linkedEntity via `XAppLayout = AppShell.traits.AppLayout -> XEntity` so multiple orbitals in one file each get their own chrome without colliding on a shared atom entity.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-app-layout.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-app-layout
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-app-layout';
const ALIAS = 'AppLayout';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAppLayoutEventKey = 'AppLayoutStateLoadFailed' | 'AppLayoutStateLoaded' | 'INIT' | 'NOTIFY_CLICK' | 'SEARCH';

/**
 * Payload shape for the `SEARCH` event.
 */
export interface StdAppLayoutSearchPayload {
  value?: string;
}

/**
 * Payload shape for the `NOTIFY_CLICK` event.
 */
export interface StdAppLayoutNotifyClickPayload {
  id?: string;
}

/**
 * Payload shape for the `AppLayoutStateLoaded` event.
 */
export interface StdAppLayoutAppLayoutStateLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `AppLayoutStateLoadFailed` event.
 */
export interface StdAppLayoutAppLayoutStateLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAppLayoutConfig {
  /** Default: `"App"` */
  appName?: string;
  /** Default: `"max-w-5xl mx-auto w-full"` */
  contentClassName?: string;
  contentTrait?: TraitFieldRef;
  /** Default: `""` */
  currentPath?: string;
  /** Default: `"modal"` */
  detailSlot?: unknown;
  /** Default: `"sidebar"` */
  layoutMode?: 'sidebar' | 'topnav' | 'bottomnav' | 'minimal';
  /** Default: `"dashboard-layout"` */
  layoutPattern?: unknown;
  /** Default: `[]` */
  navItems?: EntityRow[];
  /** Default: `"NOTIFY_CLICK"` */
  notificationClickEvent?: string;
  /** Default: `[]` */
  notifications?: EntityRow[];
  /** Default: `"SEARCH"` */
  searchEvent?: string;
  /** Default: `""` */
  theme?: string;
  /** Default: `[]` */
  topBarActions?: EntityRow[];
}

/**
 * Params for the std-app-layout descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAppLayoutParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAppLayoutEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAppLayoutConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AppLayout.traits.AppLayout`. */
export function stdAppLayoutTrait(params: StdAppLayoutParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AppLayout`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AppLayout.pages.AppLayoutPage`. */
export function stdAppLayoutPage(params: StdAppLayoutParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AppLayoutPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAppLayout(params: StdAppLayoutParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'AppLayoutOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAppLayoutTrait(params),
    ],
    pages: [
      stdAppLayoutPage(params),
    ],
  });
}
