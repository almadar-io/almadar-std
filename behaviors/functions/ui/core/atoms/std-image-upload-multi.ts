/**
 * std-image-upload-multi — std-image-upload-multi — multi-image upload with drop zone, gallery preview, and per-batch progress.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-image-upload-multi.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-image-upload-multi
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-image-upload-multi';
const ALIAS = 'ImageUploadMulti';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdImageUploadMultiEventKey = 'DELETE' | 'INIT' | 'UPLOAD' | 'UploadedImageCreated' | 'UploadedImageDeleteFailed' | 'UploadedImageDeleted' | 'UploadedImageLoadFailed' | 'UploadedImageLoaded' | 'UploadedImageUploadFailed';

/**
 * Payload shape for the `UPLOAD` event.
 */
export interface StdImageUploadMultiUploadPayload {
  files: EntityRow;
}

/**
 * Payload shape for the `UploadedImageLoaded` event.
 */
export interface StdImageUploadMultiUploadedImageLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `UploadedImageLoadFailed` event.
 */
export interface StdImageUploadMultiUploadedImageLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `UploadedImageCreated` event.
 */
export interface StdImageUploadMultiUploadedImageCreatedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `UploadedImageUploadFailed` event.
 */
export interface StdImageUploadMultiUploadedImageUploadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `UploadedImageDeleted` event.
 */
export interface StdImageUploadMultiUploadedImageDeletedPayload {
  id: string;
}

/**
 * Payload shape for the `UploadedImageDeleteFailed` event.
 */
export interface StdImageUploadMultiUploadedImageDeleteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdImageUploadMultiConfig {
  /** Default: `"image/*"` */
  accept?: string;
  /** Default: `10485760` */
  maxBytesPerImage?: number;
  /** Default: `10` */
  maxImages?: number;
  /** Default: `"Upload Images"` */
  title?: string;
}

/**
 * Params for the std-image-upload-multi descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdImageUploadMultiParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdImageUploadMultiEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdImageUploadMultiConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ImageUploadMulti.traits.UploadedImageUpload`. */
export function stdImageUploadMultiTrait(params: StdImageUploadMultiParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.UploadedImageUpload`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ImageUploadMulti.pages.UploadedImageUploadPage`. */
export function stdImageUploadMultiPage(params: StdImageUploadMultiParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.UploadedImageUploadPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdImageUploadMulti(params: StdImageUploadMultiParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'UploadedImageOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdImageUploadMultiTrait(params),
    ],
    pages: [
      stdImageUploadMultiPage(params),
    ],
  });
}
