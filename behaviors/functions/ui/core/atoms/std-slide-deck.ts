/**
 * std-slide-deck — std-slide-deck — generic presentation atom. An ordered list of slide nodes (`config.slides`, any render tree or @trait) shown one at a time, with a previous/next arrow on either side, a dot strip and a k / n counter underneath, and keyboard stepping through the host's KEY router (`config.nextKeys` / `config.prevKeys`, captured per `config.keymap`). The cursor clamps at either end unless `loop` is on. Each visit mounts the slide fresh, so its own entry animations replay. Carries no content or visual style: a realization supplies the slides and restyles via `bodyContent`. The default body picks the slide inside a mapped-children lambda, which expands static slides only; a deck whose slides hold stateful traits (forms, live lists) overrides `bodyContent` with one `presence` per slide gated on `@entity.slideIndex`. Every move emits SLIDE_CHANGED for speaker notes, timers or analytics.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-slide-deck.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-slide-deck
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-slide-deck';
const ALIAS = 'SlideDeck';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSlideDeckEventKey = 'GO_TO' | 'INIT' | 'SLIDE_CHANGED';

/**
 * Payload shape for the `SLIDE_CHANGED` event.
 */
export interface StdSlideDeckSlideChangedPayload {
  index: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSlideDeckConfig {
  /** Default: `{"children":[{"align":"center","children":[["array/map",[["array/len","@config.slides"]],["fn","n",["if","@config.showArrows",{"action":"GO_TO","actionPayload":{"index":["if","@config.loop",["math/wrap",["-","@entity.slideIndex",1],0,"@n"],["math/max",0,["-","@entity.slideIndex",1]]]},"disabled":["and",["==","@config.loop",false],["<=","@entity.slideIndex",0]],"icon":"@config.prevIcon","size":"lg","type":"button","variant":"ghost"},null]]],{"children":["array/map",["@entity.slideIndex"],["fn","i",{"children":[["array/nth","@config.slides","@i"]],"className":"w-full","type":"box"}]],"className":"flex-1 min-w-0","minHeight":"@config.stageMinHeight","type":"center"},["array/map",[["array/len","@config.slides"]],["fn","n",["if","@config.showArrows",{"action":"GO_TO","actionPayload":{"index":["if","@config.loop",["math/wrap",["+","@entity.slideIndex",1],0,"@n"],["math/min",["-","@n",1],["+","@entity.slideIndex",1]]]},"disabled":["and",["==","@config.loop",false],[">=","@entity.slideIndex",["-","@n",1]]],"icon":"@config.nextIcon","size":"lg","type":"button","variant":"ghost"},null]]]],"direction":"horizontal","gap":"md","type":"stack"},["if","@config.showProgress",{"align":"center","children":["array/map",[["array/len","@config.slides"]],["fn","n",{"align":"center","children":[{"count":"@n","currentIndex":"@entity.slideIndex","onDotClick":"GO_TO","size":"sm","type":"progress-dots"},{"color":"muted","content":["str/concat","",["+","@entity.slideIndex",1]," / ","@n"],"type":"typography","variant":"caption"}],"direction":"horizontal","gap":"md","type":"stack"}]],"direction":"horizontal","gap":"md","justify":"center","type":"stack"},null]],"className":"w-full","direction":"vertical","gap":"md","type":"stack"}` */
  bodyContent?: unknown;
  /** Default: `false` */
  loop?: boolean;
  /** Default: `"chevron-right"` */
  nextIcon?: unknown;
  /** Default: `"chevron-left"` */
  prevIcon?: unknown;
  /** Default: `true` */
  showArrows?: boolean;
  /** Default: `true` */
  showProgress?: boolean;
  /** Default: `[]` */
  slides?: EntityRow[];
  /** Default: `"70vh"` */
  stageMinHeight?: string;
  /** Default: `0` */
  startIndex?: number;
}

/**
 * Params for the std-slide-deck descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSlideDeckParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSlideDeckEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSlideDeckConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SlideDeck.traits.SlideDeckPresenter`. */
export function stdSlideDeckTrait(params: StdSlideDeckParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SlideDeckPresenter`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SlideDeck.pages.SlideDeckPage`. */
export function stdSlideDeckPage(params: StdSlideDeckParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SlideDeckPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSlideDeck(params: StdSlideDeckParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'SlideDeckOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSlideDeckTrait(params),
    ],
    pages: [
      stdSlideDeckPage(params),
    ],
  });
}
