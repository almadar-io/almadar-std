[
  {
    "organism": "ui-pull-to-refresh",
    "orbital": "PullToRefreshOrbital",
    "tier": "molecules",
    "factoryPath": "behaviors/functions/ui/core/molecules/ui-pull-to-refresh.ts",
    "entities": [
      {
        "name": "PullToRefreshItem",
        "fields": [],
        "persistence": "runtime"
      }
    ],
    "traits": [
      {
        "name": "PullToRefreshRender",
        "emittedEvents": [
          "REFRESH"
        ],
        "listenedEvents": [],
        "events": [
          {
            "name": "REFRESH",
            "direction": "emit",
            "description": "Event name to emit on refresh (emitted as UI:{refreshEvent})",
            "tier": "essential"
          },
          {
            "name": "INIT",
            "direction": "listen"
          }
        ],
        "overridableConfigKeys": [
          {
            "key": "children",
            "type": "node",
            "default": [
              {
                "content": "Sample content",
                "type": "typography"
              }
            ],
            "label": "Children",
            "description": "Content to wrap",
            "tier": "presentation"
          },
          {
            "key": "className",
            "type": "string",
            "default": "",
            "label": "Class Name",
            "description": "Additional CSS classes",
            "tier": "presentation"
          },
          {
            "key": "refreshEvent",
            "type": "string",
            "default": "REFRESH",
            "label": "Refresh Event",
            "description": "Event name to emit on refresh (emitted as UI:{refreshEvent})",
            "tier": "presentation"
          },
          {
            "key": "refreshPayload",
            "type": "json",
            "default": {},
            "label": "Refresh Payload",
            "description": "Payload to include with the refresh event",
            "tier": "presentation"
          },
          {
            "key": "threshold",
            "type": "number",
            "default": 60,
            "label": "Threshold",
            "description": "Pull distance threshold to trigger refresh in px (default: 60)",
            "tier": "presentation"
          }
        ],
        "capabilities": [],
        "entityRebindable": true,
        "entityContract": {
          "requires": [],
          "provides": []
        }
      }
    ],
    "pages": [
      {
        "name": "PullToRefreshPage",
        "defaultPath": "/pull-to-refresh",
        "primaryEntity": "PullToRefreshItem"
      }
    ],
    "emittedEvents": [
      "REFRESH"
    ],
    "listenedEvents": [],
    "exposure": "palette",
    "provenance": "generated",
    "sourceValid": false,
    "sourceErrors": [
      "orb validate reported invalid: [{\"code\":\"ORB_X_RENDER_UI_EVENT_LITERAL_STALE\",\"path\":\"orbitals[0 (PullToRefreshOrbital)].traits[0 (PullToRefreshRender)].stateMachine.transitions[0].effects[0]\",\"message\":\"Prop 'refreshEvent' on 'pull-to-refresh' references event 'REFRESH' but the trait's event set has no such key — literal is stale (likely missed by an `events: { OLD: NEW }` override rewrite, or an authoring rename that didn't reach this call site)\",\"suggestion\":\"Either add 'REFRESH' to the trait's `emits` / `listens` / transi"
    ]
  }
]
