/**
 * ui-repeatable-form-section — RepeatableFormSection — UI factory (1:1 with the @almadar/ui repeatable-form-section pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/ui-repeatable-form-section.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-repeatable-form-section
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-repeatable-form-section';
const ALIAS = 'UiRepeatableFormSection';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiRepeatableFormSectionEventKey = 'ADD' | 'INIT' | 'REMOVE' | 'REORDER';

/**
 * Payload shape for the `ADD` event.
 */
export interface StdUiRepeatableFormSectionAddPayload {
  id?: string;
}

/**
 * Payload shape for the `REMOVE` event.
 */
export interface StdUiRepeatableFormSectionRemovePayload {
  itemId?: string;
  index?: number;
}

/**
 * Payload shape for the `REORDER` event.
 */
export interface StdUiRepeatableFormSectionReorderPayload {
  fromIndex?: number;
  toIndex?: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiRepeatableFormSectionConfig {
  /** Default: `"Add Label"` */
  addLabel?: string;
  /** Default: `false` */
  allowReorder?: boolean;
  /** Default: `""` */
  className?: string;
  /** Default: `"Current State"` */
  currentState?: string;
  /** Default: `"Empty Message"` */
  emptyMessage?: string;
  /** Default: `[{"addedAt":"Added At","addedInState":"Added In State","id":"Id"},{"addedAt":"Added At 2","addedInState":"Added In State 2","id":"Id 2"}]` */
  items?: EntityRow[];
  /** Default: `0` */
  maxItems?: number;
  /** Default: `0` */
  minItems?: number;
  /** Default: `false` */
  readOnly?: boolean;
  /** Default: `"Section Type"` */
  sectionType?: string;
  /** Default: `false` */
  showAuditInfo?: boolean;
  /** Default: `"Title"` */
  title?: string;
  /** Default: `false` */
  trackAddedInState?: boolean;
}

/**
 * Params for the ui-repeatable-form-section descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdUiRepeatableFormSectionParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdUiRepeatableFormSectionEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdUiRepeatableFormSectionConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `UiRepeatableFormSection.traits.RepeatableFormSectionRender`. */
export function stdUiRepeatableFormSectionTrait(params: StdUiRepeatableFormSectionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.RepeatableFormSectionRender`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `UiRepeatableFormSection.pages.RepeatableFormSectionPage`. */
export function stdUiRepeatableFormSectionPage(params: StdUiRepeatableFormSectionParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.RepeatableFormSectionPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdUiRepeatableFormSection(params: StdUiRepeatableFormSectionParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'RepeatableFormSectionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdUiRepeatableFormSectionTrait(params),
    ],
    pages: [
      stdUiRepeatableFormSectionPage(params),
    ],
  });
}
