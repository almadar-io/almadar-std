/**
 * ui-subagent-trace-panel — SubagentTracePanel — UI factory (1:1 with the @almadar/ui subagent-trace-panel pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-subagent-trace-panel.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-subagent-trace-panel
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-subagent-trace-panel';
const ALIAS = 'UiSubagentTracePanel';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiSubagentTracePanelEventKey = 'CLOSE' | 'INIT' | 'OPEN';

/**
 * Payload shape for the `CLOSE` event.
 */
export interface StdUiSubagentTracePanelClosePayload {
  id?: string;
}

/**
 * Payload shape for the `OPEN` event.
 */
export interface StdUiSubagentTracePanelOpenPayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiSubagentTracePanelConfig {
  /** Default: `{}` */
  activeFilters?: unknown;
  /** Default: `""` */
  className?: string;
  /** Default: `[]` */
  coordinatorActivities?: EntityRow[];
  /** Default: `[]` */
  coordinatorMessages?: EntityRow[];
  /** Default: `1` */
  disclosureLevel?: number;
  error?: EntityRow;
  /** Default: `"Focused Orbital"` */
  focusedOrbital?: string;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `"overlay"` */
  mode?: 'overlay' | 'tab';
  /** Default: `false` */
  open?: boolean;
  /** Default: `0` */
  pageProp?: number;
  /** Default: `0` */
  pageSize?: number;
  /** Default: `"Search Value"` */
  searchValue?: string;
  /** Default: `[]` */
  selectedIds?: string[];
  /** Default: `"Sort By"` */
  sortBy?: string;
  /** Default: `"asc"` */
  sortDirection?: 'asc' | 'desc';
  /** Default: `[]` */
  subagents?: EntityRow[];
  /** Default: `0` */
  totalCount?: number;
}

/**
 * Tunable params for the SubagentTracePanelOrbital orbital.
 *
 * Canonical entity: SubagentTracePanelItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiSubagentTracePanelSubagentTracePanelOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SubagentTracePanelRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the SubagentTracePanelOrbital orbital with consumer params. */
export function stdUiSubagentTracePanelSubagentTracePanelOrbital(params: StdUiSubagentTracePanelSubagentTracePanelOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'SubagentTracePanelItem';
  const built = makeOrbitalWithUses({
    name: 'SubagentTracePanelOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'activeFilters': {
            'default': {},
            'description': 'Active filters',
            'label': 'Active Filters',
            'tier': 'presentation',
            'type': 'json',
          },
          'className': {
            'default': '',
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'coordinatorActivities': {
            'default': [],
            'description': 'Full coordinator activities (tool calls, results, messages, errors).',
            'items': {
              'type': 'string',
            },
            'label': 'Coordinator Activities',
            'tier': 'presentation',
            'type': '[json]',
          },
          'coordinatorMessages': {
            'default': [],
            'description': 'Canonical Coordinator conversation — same shape live and on reload. When present, takes precedence over `coordinatorActivities` for the Coordinator section.',
            'items': {
              'type': 'string',
            },
            'label': 'Coordinator Messages',
            'tier': 'presentation',
            'type': '[json]',
          },
          'disclosureLevel': {
            'default': 1,
            'description': 'Drives density: 1-2 → compact, 3-4 → full activity embed (when mode=\'overlay\').',
            'label': 'Disclosure Level',
            'tier': 'presentation',
            'type': 'number',
          },
          'error': {
            'description': 'Error state (UiError)',
            'label': 'Error',
            'properties': {
              'code': {
                'name': 'code',
                'required': false,
                'type': 'string',
              },
              'message': {
                'name': 'message',
                'required': true,
                'type': 'string',
              },
              'name': {
                'name': 'name',
                'required': false,
                'type': 'string',
              },
              'stack': {
                'name': 'stack',
                'required': false,
                'type': 'string',
              },
            },
            'tier': 'presentation',
            'type': 'SubagentTracePanelError',
          },
          'focusedOrbital': {
            'default': 'Focused Orbital',
            'description': 'Current canvas focus orbital — only used by overlay mode. Tab mode ignores this.',
            'label': 'Focused Orbital',
            'tier': 'presentation',
            'type': 'string',
          },
          'isLoading': {
            'default': false,
            'description': 'Loading state indicator',
            'label': 'Is Loading',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'mode': {
            'default': 'overlay',
            'description': 'Rendering mode. - `\'overlay\'` (default): floating side panel positioned absolutely over the canvas. Density follows `disclosureLevel` (compact L1/L2, rich L3+). The `open` flag actually hides the panel when false. - `\'tab\'`: full-width content for a dedicated \'trace\' supplemental tab. Always renders rich (regardless of disclosure level), always visible (the `open` flag is ignored), and includes an empty state when no subagents are present so users see the surface even before generation starts.',
            'label': 'Mode',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'overlay',
              'tab',
            ],
          },
          'open': {
            'default': false,
            'description': 'Whether the panel is visible. Typically auto-opens during generation in overlay mode.',
            'label': 'Open',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'pageProp': {
            'default': 0,
            'description': 'Current page number',
            'label': 'Page',
            'synonyms': 'page',
            'tier': 'presentation',
            'type': 'number',
          },
          'pageSize': {
            'default': 0,
            'description': 'Number of items per page',
            'label': 'Page Size',
            'tier': 'presentation',
            'type': 'number',
          },
          'searchValue': {
            'default': 'Search Value',
            'description': 'Current search query value',
            'label': 'Search Value',
            'tier': 'presentation',
            'type': 'string',
          },
          'selectedIds': {
            'default': [],
            'description': 'Currently selected item IDs',
            'items': {
              'type': 'string',
            },
            'label': 'Selected Ids',
            'tier': 'presentation',
            'type': '[string]',
          },
          'sortBy': {
            'default': 'Sort By',
            'description': 'Current sort field',
            'label': 'Sort By',
            'tier': 'presentation',
            'type': 'string',
          },
          'sortDirection': {
            'default': 'asc',
            'description': 'Current sort direction',
            'label': 'Sort Direction',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'asc',
              'desc',
            ],
          },
          'subagents': {
            'default': [],
            'description': 'All known subagents from the runner.',
            'items': {
              'type': 'string',
            },
            'label': 'Subagents',
            'tier': 'presentation',
            'type': '[json]',
          },
          'totalCount': {
            'default': 0,
            'description': 'Total number of items',
            'label': 'Total Count',
            'tier': 'presentation',
            'type': 'number',
          },
        },
        'emits': [
          {
            'description': 'Optional dismiss handler.',
            'event': 'CLOSE',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'description': 'Open the Subagent-trace-panel.',
            'event': 'OPEN',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'SubagentTracePanelItem',
        'name': 'SubagentTracePanelRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Open the Subagent-trace-panel.',
              'key': 'OPEN',
              'name': 'Open',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'Optional dismiss handler.',
              'key': 'CLOSE',
              'name': 'Close',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'closed',
            },
            {
              'name': 'open',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'action': 'OPEN',
                    'label': 'Open Subagent-trace-panel',
                    'type': 'button',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'closed',
              'to': 'closed',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'overlay',
                  {
                    'activeFilters': '@config.activeFilters',
                    'className': '@config.className',
                    'coordinatorActivities': '@config.coordinatorActivities',
                    'coordinatorMessages': '@config.coordinatorMessages',
                    'disclosureLevel': '@config.disclosureLevel',
                    'error': '@config.error',
                    'focusedOrbital': '@config.focusedOrbital',
                    'isLoading': '@config.isLoading',
                    'mode': '@config.mode',
                    'onClose': 'CLOSE',
                    'open': '@config.open',
                    'page': '@config.pageProp',
                    'pageSize': '@config.pageSize',
                    'searchValue': '@config.searchValue',
                    'selectedIds': '@config.selectedIds',
                    'sortBy': '@config.sortBy',
                    'sortDirection': '@config.sortDirection',
                    'subagents': '@config.subagents',
                    'totalCount': '@config.totalCount',
                    'type': 'subagent-trace-panel',
                  },
                ],
              ],
              'event': 'OPEN',
              'from': 'closed',
              'to': 'open',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'overlay',
                  null,
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'action': 'OPEN',
                    'label': 'Open Subagent-trace-panel',
                    'type': 'button',
                  },
                ],
              ],
              'event': 'CLOSE',
              'from': 'open',
              'to': 'closed',
            },
          ],
        },
      } as never, 'SubagentTracePanelItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'SubagentTracePanelPage',
        'path': '/subagent-trace-panel',
        'traits': [
          {
            'ref': 'SubagentTracePanelRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiSubagentTracePanelSubagentTracePanelOrbital. */
export const StdUiSubagentTracePanelSubagentTracePanelOrbitalManifest = {
  organism: 'ui-subagent-trace-panel',
  orbitalName: 'SubagentTracePanelOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'SubagentTracePanelRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiSubagentTracePanelSubagentTracePanelOrbitalParams keys. */
export function isStdUiSubagentTracePanelSubagentTracePanelOrbitalParams(p: object): p is StdUiSubagentTracePanelSubagentTracePanelOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiSubagentTracePanelSubagentTracePanelOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiSubagentTracePanelSubagentTracePanelOrbitalManifest.traitNames,
      ...StdUiSubagentTracePanelSubagentTracePanelOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-subagent-trace-panel — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiSubagentTracePanelParams {
  SubagentTracePanel?: StdUiSubagentTracePanelSubagentTracePanelOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiSubagentTracePanel(params: StdUiSubagentTracePanelParams = {}): OrbitalDefinition[] {
  return [
    stdUiSubagentTracePanelSubagentTracePanelOrbital(params.SubagentTracePanel ?? {}),
  ];
}
