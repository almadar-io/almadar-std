/**
 * ui-tabbed-container — TabbedContainer — UI factory (1:1 with the @almadar/ui tabbed-container pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-tabbed-container.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-tabbed-container
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-tabbed-container';
const ALIAS = 'UiTabbedContainer';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiTabbedContainerEventKey = 'INIT' | 'TAB_CHANGE';

/**
 * Payload shape for the `TAB_CHANGE` event.
 */
export interface StdUiTabbedContainerTabChangePayload {
  tabId?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiTabbedContainerConfig {
  /** Default: `""` */
  activeTab?: string;
  /** Default: `""` */
  className?: string;
  /** Default: `""` */
  defaultTab?: string;
  /** Default: `"TAB_CHANGE"` */
  onTabChange?: string;
  /** Default: `"top"` */
  position?: 'top' | 'left';
  /** Default: `[{"badge":"Badge","content":"Content","disabled":false,"id":"Id","label":"Label","sectionId":"Section Id"},{"badge":"Badge 2","content":"Content 2","disabled":true,"id":"Id 2","label":"Label 2","sectionId":"Section Id 2"}]` */
  tabs?: EntityRow[];
}

type _StdUiTabbedContainerEntityName = 'TabbedContainerItem';
type _StdUiTabbedContainerListenTraitName = 'TabbedContainerRender';

/**
 * Tunable params for the TabbedContainerOrbital orbital.
 *
 * Canonical entity: TabbedContainerItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiTabbedContainerTabbedContainerOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'TabbedContainerRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait TabbedContainerOrbital's `uses[]` exports. */
type _StdUiTabbedContainerTabbedContainerOrbitalUsesRef = never;

/** Per-orbital factory: builds the TabbedContainerOrbital orbital with consumer params. */
export function stdUiTabbedContainerTabbedContainerOrbital(params: StdUiTabbedContainerTabbedContainerOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'TabbedContainerOrbital',
    uses: [],
    entity: {
      name: 'TabbedContainerItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'activeTab': {
            'default': '',
            'description': 'Controlled active tab',
            'label': 'Active Tab',
            'tier': 'presentation',
            'type': 'string',
          },
          'className': {
            'default': '',
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'defaultTab': {
            'default': '',
            'description': 'Default active tab ID',
            'label': 'Default Tab',
            'tier': 'presentation',
            'type': 'string',
          },
          'onTabChange': {
            'default': 'TAB_CHANGE',
            'description': 'Callback when tab changes',
            'label': 'On Tab Change',
            'tier': 'presentation',
            'type': 'string',
          },
          'position': {
            'default': 'top',
            'description': 'Tab position',
            'label': 'Position',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'top',
              'left',
            ],
          },
          'tabs': {
            'default': [
              {
                'badge': 'Badge',
                'content': 'Content',
                'disabled': false,
                'id': 'Id',
                'label': 'Label',
                'sectionId': 'Section Id',
              },
              {
                'badge': 'Badge 2',
                'content': 'Content 2',
                'disabled': true,
                'id': 'Id 2',
                'label': 'Label 2',
                'sectionId': 'Section Id 2',
              },
            ],
            'description': 'Tab definitions',
            'items': {
              'properties': {
                'badge': {
                  'name': 'badge',
                  'required': false,
                  'type': 'string',
                },
                'content': {
                  'name': 'content',
                  'required': false,
                  'type': 'string',
                },
                'disabled': {
                  'name': 'disabled',
                  'required': false,
                  'type': 'boolean',
                },
                'id': {
                  'name': 'id',
                  'required': true,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
                'sectionId': {
                  'name': 'sectionId',
                  'required': false,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Tabs',
            'tier': 'presentation',
            'type': '[TabbedContainerTabsItem]',
          },
        },
        'emits': [
          {
            'description': 'Callback when tab changes',
            'event': '@config.onTabChange',
            'payloadSchema': [
              {
                'name': 'tabId',
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'TabbedContainerItem',
        'name': 'TabbedContainerRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Callback when tab changes',
              'key': '@config.onTabChange',
              'name': '@config.on tab change',
              'payloadSchema': [
                {
                  'name': 'tabId',
                  'type': 'string',
                },
              ],
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'activeTab': '@config.activeTab',
                    'className': '@config.className',
                    'defaultTab': '@config.defaultTab',
                    'onTabChange': '@config.onTabChange',
                    'position': '@config.position',
                    'tabs': '@config.tabs',
                    'type': 'tabbed-container',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'TabbedContainerPage',
        'path': '/tabbed-container',
        'traits': [
          {
            'ref': 'TabbedContainerRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiTabbedContainerTabbedContainerOrbital. */
export const StdUiTabbedContainerTabbedContainerOrbitalManifest = {
  organism: 'ui-tabbed-container',
  orbitalName: 'TabbedContainerOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'TabbedContainerRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiTabbedContainerTabbedContainerOrbitalParams keys. */
export function isStdUiTabbedContainerTabbedContainerOrbitalParams(p: object): p is StdUiTabbedContainerTabbedContainerOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiTabbedContainerTabbedContainerOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiTabbedContainerTabbedContainerOrbitalManifest.traitNames,
      ...StdUiTabbedContainerTabbedContainerOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-tabbed-container — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiTabbedContainerParams {
  TabbedContainer?: StdUiTabbedContainerTabbedContainerOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiTabbedContainer(params: StdUiTabbedContainerParams = {}): OrbitalDefinition[] {
  return [
    stdUiTabbedContainerTabbedContainerOrbital(params.TabbedContainer ?? {}),
  ];
}
