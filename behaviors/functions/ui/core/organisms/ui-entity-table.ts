/**
 * ui-entity-table — EntityTable — UI factory (1:1 with the @almadar/ui entity-table pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-entity-table.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-entity-table
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-entity-table';
const ALIAS = 'UiEntityTable';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiEntityTableEventKey = 'INIT' | 'VIEW';

/**
 * Payload shape for the `VIEW` event.
 */
export interface StdUiEntityTableViewPayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiEntityTableConfig {
  /** Default: `"Empty Icon"` */
  emptyIcon?: string;
  /** Default: `0` */
  totalCount?: number;
  /** Default: `"Empty Title"` */
  emptyTitle?: string;
  /** Default: `0` */
  pageSize?: number;
  /** Default: `"Empty Description"` */
  emptyDescription?: string;
  /** Default: `false` */
  searchable?: boolean;
  /** Default: `[]` */
  bulkActions?: EntityRow[];
  headerActions?: unknown;
  /** Default: `true` */
  showTotal?: boolean;
  /** Default: `"dense"` */
  look?: 'dense' | 'spacious' | 'striped' | 'borderless' | 'card-rows';
  activeFilters?: unknown;
  /** Default: `[]` */
  rowActions?: EntityRow[];
  /** Default: `"asc"` */
  sortDirection?: 'asc' | 'desc';
  /** Default: `"Sort By"` */
  sortBy?: string;
  /** Default: `[]` */
  selectedIds?: string[];
  error?: EntityRow;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `[]` */
  itemActions?: EntityRow[];
  emptyAction?: EntityRow;
  /** Default: `"Search Value"` */
  searchValue?: string;
  /** Default: `false` */
  selectable?: boolean;
  /** Default: `"Search Placeholder"` */
  searchPlaceholder?: string;
  /** Default: `0` */
  pageProp?: number;
  /** Default: `[]` */
  columns?: EntityRow[];
  /** Default: `""` */
  className?: string;
  /** Default: `[]` */
  fields?: EntityRow[];
}

/**
 * Tunable params for the EntityTableOrbital orbital.
 *
 * Canonical entity: EntityTableItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiEntityTableEntityTableOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'EntityTableRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the EntityTableOrbital orbital with consumer params. */
export function stdUiEntityTableEntityTableOrbital(params: StdUiEntityTableEntityTableOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'EntityTableItem';
  const built = makeOrbitalWithUses({
    name: 'EntityTableOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'name': 'EntityTableRender',
        'entityRebindable': true,
        'entityContract': {
          'requires': [],
          'provides': [],
        },
        'category': 'interaction',
        'linkedEntity': 'EntityTableItem',
        'emits': [
          {
            'event': 'VIEW',
            'description': 'User opened a record from the list.',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'VIEW',
              'name': 'View',
              'description': 'User opened a record from the list.',
              'tier': 'essential',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'EntityTableItem',
                  {},
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'isLoading': '@config.isLoading',
                    'emptyTitle': '@config.emptyTitle',
                    'className': '@config.className',
                    'totalCount': '@config.totalCount',
                    'activeFilters': '@config.activeFilters',
                    'emptyIcon': '@config.emptyIcon',
                    'showTotal': '@config.showTotal',
                    'sortBy': '@config.sortBy',
                    'searchValue': '@config.searchValue',
                    'rowActions': '@config.rowActions',
                    'entity': '@entity',
                    'columns': '@config.columns',
                    'sortDirection': '@config.sortDirection',
                    'fields': '@config.fields',
                    'searchable': '@config.searchable',
                    'searchPlaceholder': '@config.searchPlaceholder',
                    'bulkActions': '@config.bulkActions',
                    'headerActions': '@config.headerActions',
                    'itemActions': [
                      {
                        'label': 'View',
                        'event': 'VIEW',
                      },
                    ],
                    'selectable': '@config.selectable',
                    'look': '@config.look',
                    'page': '@config.pageProp',
                    'emptyAction': '@config.emptyAction',
                    'error': '@config.error',
                    'type': 'entity-table',
                    'pageSize': '@config.pageSize',
                    'emptyDescription': '@config.emptyDescription',
                    'selectedIds': '@config.selectedIds',
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'VIEW',
              'effects': [
                [
                  'emit',
                  'VIEW',
                  {},
                ],
              ],
            },
          ],
        },
        'config': {
          'emptyIcon': {
            'type': 'string',
            'default': 'Empty Icon',
            'label': 'Empty Icon',
            'description': 'emptyIcon prop',
            'tier': 'presentation',
          },
          'totalCount': {
            'type': 'number',
            'default': 0,
            'label': 'Total Count',
            'description': 'Total number of items',
            'tier': 'presentation',
          },
          'emptyTitle': {
            'type': 'string',
            'default': 'Empty Title',
            'label': 'Empty Title',
            'description': 'emptyTitle prop',
            'tier': 'presentation',
          },
          'pageSize': {
            'type': 'number',
            'default': 0,
            'label': 'Page Size',
            'description': 'Number of items per page',
            'tier': 'presentation',
          },
          'emptyDescription': {
            'type': 'string',
            'default': 'Empty Description',
            'label': 'Empty Description',
            'description': 'emptyDescription prop',
            'tier': 'presentation',
          },
          'searchable': {
            'type': 'boolean',
            'default': false,
            'label': 'Searchable',
            'description': 'Search',
            'tier': 'presentation',
          },
          'bulkActions': {
            'type': '[EntityTableBulkActionsItem]',
            'default': [],
            'label': 'Bulk Actions',
            'description': 'Bulk actions',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'icon': {
                  'name': 'icon',
                  'type': 'string',
                  'required': false,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
                'variant': {
                  'name': 'variant',
                  'type': 'string',
                  'required': false,
                  'values': [
                    'default',
                    'danger',
                  ],
                },
              },
            },
          },
          'headerActions': {
            'type': 'node',
            'label': 'Header Actions',
            'description': 'Header actions',
            'tier': 'presentation',
          },
          'showTotal': {
            'type': 'boolean',
            'default': true,
            'label': 'Show Total',
            'description': 'Show total count in pagination',
            'tier': 'presentation',
          },
          'look': {
            'type': 'string',
            'default': 'dense',
            'label': 'Look',
            'description': 'Layer 2 visual treatment — orthogonal to the semantic variant.',
            'tier': 'presentation',
            'values': [
              'dense',
              'spacious',
              'striped',
              'borderless',
              'card-rows',
            ],
          },
          'activeFilters': {
            'type': 'json',
            'label': 'Active Filters',
            'description': 'Active filters',
            'tier': 'presentation',
          },
          'rowActions': {
            'type': '[EntityTableRowActionsItem]',
            'default': [],
            'label': 'Row Actions',
            'description': 'Row actions',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'event': {
                  'name': 'event',
                  'type': 'string',
                  'required': false,
                },
                'icon': {
                  'name': 'icon',
                  'type': 'string',
                  'required': false,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
                'variant': {
                  'name': 'variant',
                  'type': 'string',
                  'required': false,
                  'values': [
                    'default',
                    'danger',
                  ],
                },
              },
            },
          },
          'sortDirection': {
            'type': 'string',
            'default': 'asc',
            'label': 'Sort Direction',
            'description': 'Current sort direction',
            'tier': 'presentation',
            'values': [
              'asc',
              'desc',
            ],
          },
          'sortBy': {
            'type': 'string',
            'default': 'Sort By',
            'label': 'Sort By',
            'description': 'Current sort field',
            'tier': 'presentation',
          },
          'selectedIds': {
            'type': '[string]',
            'default': [],
            'label': 'Selected Ids',
            'description': 'Currently selected item IDs',
            'tier': 'presentation',
            'items': {
              'type': 'string',
            },
          },
          'error': {
            'type': 'EntityTableError',
            'label': 'Error',
            'description': 'Error state (UiError)',
            'tier': 'presentation',
            'properties': {
              'code': {
                'name': 'code',
                'type': 'string',
                'required': false,
              },
              'message': {
                'name': 'message',
                'type': 'string',
                'required': true,
              },
              'name': {
                'name': 'name',
                'type': 'string',
                'required': false,
              },
              'stack': {
                'name': 'stack',
                'type': 'string',
                'required': false,
              },
            },
          },
          'isLoading': {
            'type': 'boolean',
            'default': false,
            'label': 'Is Loading',
            'description': 'Loading state indicator',
            'tier': 'presentation',
          },
          'itemActions': {
            'type': '[EntityTableItemActionsItem]',
            'default': [],
            'label': 'Item Actions',
            'description': 'Item actions from generated code - maps to rowActions',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'icon': {
                  'name': 'icon',
                  'type': 'string',
                  'required': false,
                },
                'variant': {
                  'name': 'variant',
                  'type': 'string',
                  'required': false,
                  'values': [
                    'default',
                    'primary',
                    'secondary',
                    'ghost',
                    'danger',
                  ],
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
                'navigatesTo': {
                  'name': 'navigatesTo',
                  'type': 'string',
                  'required': false,
                },
                'action': {
                  'name': 'action',
                  'type': 'string',
                  'required': false,
                },
                'placement': {
                  'name': 'placement',
                  'type': 'string',
                  'required': false,
                  'values': [
                    'row',
                    'bulk',
                  ],
                },
                'event': {
                  'name': 'event',
                  'type': 'string',
                  'required': false,
                },
              },
            },
          },
          'emptyAction': {
            'type': 'EntityTableEmptyAction',
            'label': 'Empty Action',
            'description': 'emptyAction prop',
            'tier': 'presentation',
            'properties': {
              'event': {
                'name': 'event',
                'type': 'string',
                'required': false,
              },
              'label': {
                'name': 'label',
                'type': 'string',
                'required': true,
              },
            },
          },
          'searchValue': {
            'type': 'string',
            'default': 'Search Value',
            'label': 'Search Value',
            'description': 'Current search query value',
            'tier': 'presentation',
          },
          'selectable': {
            'type': 'boolean',
            'default': false,
            'label': 'Selectable',
            'description': 'Selection',
            'tier': 'presentation',
          },
          'searchPlaceholder': {
            'type': 'string',
            'default': 'Search Placeholder',
            'label': 'Search Placeholder',
            'description': 'searchPlaceholder prop',
            'tier': 'presentation',
          },
          'pageProp': {
            'type': 'number',
            'default': 0,
            'label': 'Page',
            'description': 'Current page number',
            'synonyms': 'page',
            'tier': 'presentation',
          },
          'columns': {
            'type': '[EntityTableColumnsItem]',
            'default': [],
            'label': 'Columns',
            'description': 'Columns can be Column objects or simple string field names',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'header': {
                  'name': 'header',
                  'type': 'string',
                  'required': true,
                },
                'key': {
                  'name': 'key',
                  'type': 'string',
                  'required': true,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': false,
                },
                'width': {
                  'name': 'width',
                  'type': 'string',
                  'required': false,
                },
                'sortable': {
                  'name': 'sortable',
                  'type': 'boolean',
                  'required': false,
                },
                'name': {
                  'name': 'name',
                  'type': 'string',
                  'required': false,
                },
              },
            },
          },
          'className': {
            'type': 'string',
            'default': '',
            'label': 'Class Name',
            'description': 'Additional CSS classes',
            'tier': 'presentation',
          },
          'fields': {
            'type': '[EntityTableFieldsItem]',
            'default': [],
            'label': 'Fields',
            'description': 'Fields to display - accepts string[] or Column[] for unified interface. Alias for columns',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'width': {
                  'name': 'width',
                  'type': 'string',
                  'required': false,
                },
                'sortable': {
                  'name': 'sortable',
                  'type': 'boolean',
                  'required': false,
                },
                'key': {
                  'name': 'key',
                  'type': 'string',
                  'required': true,
                },
                'header': {
                  'name': 'header',
                  'type': 'string',
                  'required': true,
                },
                'name': {
                  'name': 'name',
                  'type': 'string',
                  'required': false,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': false,
                },
              },
            },
          },
        },
        'scope': 'instance',
      } as never, 'EntityTableItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'EntityTablePage',
        'path': '/entity-table',
        'traits': [
          {
            'ref': 'EntityTableRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiEntityTableEntityTableOrbital. */
export const StdUiEntityTableEntityTableOrbitalManifest = {
  organism: 'ui-entity-table',
  orbitalName: 'EntityTableOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'EntityTableRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiEntityTableEntityTableOrbitalParams keys. */
export function isStdUiEntityTableEntityTableOrbitalParams(p: object): p is StdUiEntityTableEntityTableOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiEntityTableEntityTableOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiEntityTableEntityTableOrbitalManifest.traitNames,
      ...StdUiEntityTableEntityTableOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-entity-table — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiEntityTableParams {
  EntityTable?: StdUiEntityTableEntityTableOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiEntityTable(params: StdUiEntityTableParams = {}): OrbitalDefinition[] {
  return [
    stdUiEntityTableEntityTableOrbital(params.EntityTable ?? {}),
  ];
}
