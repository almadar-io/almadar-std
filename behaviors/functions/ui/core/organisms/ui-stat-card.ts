/**
 * ui-stat-card — StatCard — UI factory (1:1 with the @almadar/ui stat-card pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-stat-card.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-stat-card
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-stat-card';
const ALIAS = 'UiStatCard';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiStatCardEventKey = 'INIT' | 'StatCardLoaded';

/**
 * Payload shape for the `StatCardLoaded` event.
 */
export interface StdUiStatCardStatCardLoadedPayload {
  data?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiStatCardConfig {
  /** Default: `{"label":"Label","navigatesTo":"Navigates To"}` */
  action?: EntityRow;
  /** Default: `{}` */
  activeFilters?: unknown;
  /** Default: `""` */
  className?: string;
  /** Default: `false` */
  compact?: boolean;
  /** Default: `0` */
  currentValue?: number;
  error?: EntityRow;
  /** Default: `"circle"` */
  icon?: unknown;
  /** Default: `"bg-muted"` */
  iconBg?: string;
  /** Default: `"text-foreground"` */
  iconColor?: string;
  /** Default: `false` */
  invertTrend?: boolean;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `""` */
  label?: string;
  /** Default: `[{"field":"Field","format":"currency","icon":"circle","label":"Label","value":"Value"},{"field":"Field 2","format":"percent","icon":"circle","label":"Label 2","value":"Value 2"}]` */
  metrics?: EntityRow[];
  /** Default: `0` */
  pageProp?: number;
  /** Default: `0` */
  pageSize?: number;
  /** Default: `0` */
  previousValue?: number;
  /** Default: `""` */
  searchValue?: string;
  /** Default: `[]` */
  selectedIds?: string[];
  /** Default: `""` */
  sortBy?: string;
  /** Default: `"asc"` */
  sortDirection?: 'asc' | 'desc';
  /** Default: `[]` */
  sparklineData?: number[];
  /** Default: `""` */
  subtitle?: string;
  /** Default: `""` */
  title?: string;
  /** Default: `0` */
  totalCount?: number;
  /** Default: `0` */
  trend?: number;
  /** Default: `"up"` */
  trendDirection?: 'up' | 'down' | 'neutral';
  /** Default: `[]` */
  value?: string[];
}

type _StdUiStatCardEntityName = 'StatCardItem';
type _StdUiStatCardListenTraitName = 'StatCardRender';

/**
 * Tunable params for the StatCardOrbital orbital.
 *
 * Canonical entity: StatCardItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiStatCardStatCardOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'StatCardRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait StatCardOrbital's `uses[]` exports. */
type _StdUiStatCardStatCardOrbitalUsesRef = never;

/** Per-orbital factory: builds the StatCardOrbital orbital with consumer params. */
export function stdUiStatCardStatCardOrbital(params: StdUiStatCardStatCardOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'StatCardOrbital',
    uses: [],
    entity: {
      name: 'StatCardItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'action': {
            'default': {
              'label': 'Label',
              'navigatesTo': 'Navigates To',
            },
            'description': 'Action button',
            'label': 'Action',
            'properties': {
              'event': {
                'name': 'event',
                'required': false,
                'type': 'string',
              },
              'label': {
                'name': 'label',
                'required': true,
                'type': 'string',
              },
              'navigatesTo': {
                'name': 'navigatesTo',
                'required': false,
                'type': 'string',
              },
            },
            'tier': 'presentation',
            'type': 'StatCardAction',
          },
          'activeFilters': {
            'default': {},
            'description': 'Active filters',
            'label': 'Active Filters',
            'tier': 'presentation',
            'type': 'json',
          },
          'className': {
            'default': '',
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'compact': {
            'default': false,
            'description': 'Compact display mode',
            'label': 'Compact',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'currentValue': {
            'default': 0,
            'description': 'Current value as number for trend calculation',
            'label': 'Current Value',
            'tier': 'presentation',
            'type': 'number',
          },
          'error': {
            'description': 'Error state (UiError)',
            'label': 'Error',
            'properties': {
              'code': {
                'name': 'code',
                'required': false,
                'type': 'string',
              },
              'message': {
                'name': 'message',
                'required': true,
                'type': 'string',
              },
              'name': {
                'name': 'name',
                'required': false,
                'type': 'string',
              },
              'stack': {
                'name': 'stack',
                'required': false,
                'type': 'string',
              },
            },
            'tier': 'presentation',
            'type': 'StatCardError',
          },
          'icon': {
            'default': 'circle',
            'description': 'Icon to display (Lucide component or icon name string)',
            'label': 'Icon',
            'tier': 'presentation',
            'type': 'icon',
          },
          'iconBg': {
            'default': 'bg-muted',
            'description': 'Icon background color',
            'label': 'Icon Bg',
            'tier': 'presentation',
            'type': 'string',
          },
          'iconColor': {
            'default': 'text-foreground',
            'description': 'Icon color',
            'label': 'Icon Color',
            'tier': 'presentation',
            'type': 'string',
          },
          'invertTrend': {
            'default': false,
            'description': 'Whether up is good (green) or bad (red)',
            'label': 'Invert Trend',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'isLoading': {
            'default': false,
            'description': 'Loading state indicator',
            'label': 'Is Loading',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'label': {
            'default': '',
            'description': 'Main label',
            'label': 'Label',
            'tier': 'presentation',
            'type': 'string',
          },
          'metrics': {
            'default': [
              {
                'field': 'Field',
                'format': 'currency',
                'icon': 'circle',
                'label': 'Label',
                'value': 'Value',
              },
              {
                'field': 'Field 2',
                'format': 'percent',
                'icon': 'circle',
                'label': 'Label 2',
                'value': 'Value 2',
              },
            ],
            'description': 'Metrics to display (schema format) - accepts readonly for compatibility with generated const arrays',
            'items': {
              'properties': {
                'field': {
                  'name': 'field',
                  'required': false,
                  'type': 'string',
                },
                'format': {
                  'name': 'format',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'currency',
                    'percent',
                    'number',
                  ],
                },
                'icon': {
                  'name': 'icon',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
                'value': {
                  'name': 'value',
                  'required': false,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Metrics',
            'tier': 'presentation',
            'type': '[StatCardMetricsItem]',
          },
          'pageProp': {
            'default': 0,
            'description': 'Current page number',
            'label': 'Page',
            'synonyms': 'page',
            'tier': 'presentation',
            'type': 'number',
          },
          'pageSize': {
            'default': 0,
            'description': 'Number of items per page',
            'label': 'Page Size',
            'tier': 'presentation',
            'type': 'number',
          },
          'previousValue': {
            'default': 0,
            'description': 'Previous value for comparison',
            'label': 'Previous Value',
            'tier': 'presentation',
            'type': 'number',
          },
          'searchValue': {
            'default': '',
            'description': 'Current search query value',
            'label': 'Search Value',
            'tier': 'presentation',
            'type': 'string',
          },
          'selectedIds': {
            'default': [],
            'description': 'Currently selected item IDs',
            'items': {
              'type': 'string',
            },
            'label': 'Selected Ids',
            'tier': 'presentation',
            'type': '[string]',
          },
          'sortBy': {
            'default': '',
            'description': 'Current sort field',
            'label': 'Sort By',
            'tier': 'presentation',
            'type': 'string',
          },
          'sortDirection': {
            'default': 'asc',
            'description': 'Current sort direction',
            'label': 'Sort Direction',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'asc',
              'desc',
            ],
          },
          'sparklineData': {
            'default': [],
            'description': 'Sparkline data points for an inline trend chart',
            'items': {
              'type': 'number',
            },
            'label': 'Sparkline Data',
            'tier': 'presentation',
            'type': '[number]',
          },
          'subtitle': {
            'default': '',
            'description': 'Subtitle or description',
            'label': 'Subtitle',
            'tier': 'presentation',
            'type': 'string',
          },
          'title': {
            'default': '',
            'description': 'Title (alias for label)',
            'label': 'Title',
            'tier': 'presentation',
            'type': 'string',
          },
          'totalCount': {
            'default': 0,
            'description': 'Total number of items',
            'label': 'Total Count',
            'tier': 'presentation',
            'type': 'number',
          },
          'trend': {
            'default': 0,
            'description': 'Manual trend percentage (overrides calculation)',
            'label': 'Trend',
            'tier': 'presentation',
            'type': 'number',
          },
          'trendDirection': {
            'default': 'up',
            'description': 'Trend direction (overrides calculation)',
            'label': 'Trend Direction',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'up',
              'down',
              'neutral',
            ],
          },
          'value': {
            'default': [],
            'description': 'Primary value - accepts array from generated code (will use first element or length)',
            'items': {
              'type': 'string',
            },
            'label': 'Value',
            'tier': 'presentation',
            'type': '[string]',
          },
        },
        'emits': [
          {
            'description': 'StatCard rows finished loading; payload.data holds the collection.',
            'event': 'StatCardLoaded',
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[StatCardItem]',
              },
            ],
            'scope': 'internal',
            'synonyms': 'loaded, fetched, retrieved',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'StatCardItem',
        'name': 'StatCardRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'StatCard rows finished loading; payload.data holds the collection.',
              'key': 'StatCardLoaded',
              'name': 'Stat card loaded',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[StatCardItem]',
                },
              ],
              'synonyms': 'loaded, fetched, retrieved',
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'fetch',
                  ('StatCardItem' satisfies _StdUiStatCardEntityName),
                  {
                    'emit': {
                      'success': 'StatCardLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'action': '@config.action',
                    'activeFilters': '@config.activeFilters',
                    'className': '@config.className',
                    'compact': '@config.compact',
                    'currentValue': '@config.currentValue',
                    'entity': '@entity',
                    'error': '@config.error',
                    'icon': '@config.icon',
                    'iconBg': '@config.iconBg',
                    'iconColor': '@config.iconColor',
                    'invertTrend': '@config.invertTrend',
                    'isLoading': '@config.isLoading',
                    'label': '@config.label',
                    'metrics': '@config.metrics',
                    'page': '@config.pageProp',
                    'pageSize': '@config.pageSize',
                    'previousValue': '@config.previousValue',
                    'searchValue': '@config.searchValue',
                    'selectedIds': '@config.selectedIds',
                    'sortBy': '@config.sortBy',
                    'sortDirection': '@config.sortDirection',
                    'sparklineData': '@config.sparklineData',
                    'subtitle': '@config.subtitle',
                    'title': '@config.title',
                    'totalCount': '@config.totalCount',
                    'trend': '@config.trend',
                    'trendDirection': '@config.trendDirection',
                    'type': 'stat-card',
                    'value': '@config.value',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'action': '@config.action',
                    'activeFilters': '@config.activeFilters',
                    'className': '@config.className',
                    'compact': '@config.compact',
                    'currentValue': '@config.currentValue',
                    'entity': '@payload.data',
                    'error': '@config.error',
                    'icon': '@config.icon',
                    'iconBg': '@config.iconBg',
                    'iconColor': '@config.iconColor',
                    'invertTrend': '@config.invertTrend',
                    'isLoading': '@config.isLoading',
                    'label': '@config.label',
                    'metrics': '@config.metrics',
                    'page': '@config.pageProp',
                    'pageSize': '@config.pageSize',
                    'previousValue': '@config.previousValue',
                    'searchValue': '@config.searchValue',
                    'selectedIds': '@config.selectedIds',
                    'sortBy': '@config.sortBy',
                    'sortDirection': '@config.sortDirection',
                    'sparklineData': '@config.sparklineData',
                    'subtitle': '@config.subtitle',
                    'title': '@config.title',
                    'totalCount': '@config.totalCount',
                    'trend': '@config.trend',
                    'trendDirection': '@config.trendDirection',
                    'type': 'stat-card',
                    'value': '@config.value',
                  },
                ],
              ],
              'event': 'StatCardLoaded',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'StatCardPage',
        'path': '/stat-card',
        'traits': [
          {
            'ref': 'StatCardRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiStatCardStatCardOrbital. */
export const StdUiStatCardStatCardOrbitalManifest = {
  organism: 'ui-stat-card',
  orbitalName: 'StatCardOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'StatCardRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiStatCardStatCardOrbitalParams keys. */
export function isStdUiStatCardStatCardOrbitalParams(p: object): p is StdUiStatCardStatCardOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiStatCardStatCardOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiStatCardStatCardOrbitalManifest.traitNames,
      ...StdUiStatCardStatCardOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-stat-card — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiStatCardParams {
  StatCard?: StdUiStatCardStatCardOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiStatCard(params: StdUiStatCardParams = {}): OrbitalDefinition[] {
  return [
    stdUiStatCardStatCardOrbital(params.StatCard ?? {}),
  ];
}
