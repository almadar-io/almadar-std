/**
 * todo-list-app — A simple todo list application with add, complete, delete, and filter capabilities.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/todo-list-app.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=todo-list-app
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/todo-list-app';
const ALIAS = 'TodoListApp';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTodoListAppEventKey = 'ADD_TODO' | 'COMPLETE_TODO' | 'DELETE_TODO' | 'INIT';

/**
 * Payload shape for the `ADD_TODO` event.
 */
export interface StdTodoListAppAddTodoPayload {
  text: string;
}

/**
 * Payload shape for the `COMPLETE_TODO` event.
 */
export interface StdTodoListAppCompleteTodoPayload {
  todoId: string;
  completed: boolean;
}

/**
 * Payload shape for the `DELETE_TODO` event.
 */
export interface StdTodoListAppDeleteTodoPayload {
  todoId: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTodoListAppConfig {
  className?: string;
  /** Default: `"ALL"` */
  filterMode?: string;
  /** Default: `"ADD_TODO"` */
  onAddTodo?: string;
  /** Default: `"COMPLETE_TODO"` */
  onCompleteTodo?: string;
  /** Default: `"DELETE_TODO"` */
  onDeleteTodo?: string;
  /** Default: `[]` */
  todos?: EntityRow[];
}

type _StdTodoListAppEntityName = 'Todo';
type _StdTodoListAppListenTraitName = 'TodoListRender';

/**
 * Tunable params for the TodoListAppOrbital orbital.
 *
 * Canonical entity: Todo — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdTodoListAppTodoListAppOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'TodoListRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait TodoListAppOrbital's `uses[]` exports. */
type _StdTodoListAppTodoListAppOrbitalUsesRef = never;

/** Per-orbital factory: builds the TodoListAppOrbital orbital with consumer params. */
export function stdTodoListAppTodoListAppOrbital(params: StdTodoListAppTodoListAppOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'TodoListAppOrbital',
    uses: [],
    entity: {
      name: 'Todo',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'text',
            'required': true,
            'type': 'string',
          },
          {
            'name': 'completed',
            'type': 'boolean',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'className': {
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'filterMode': {
            'default': 'ALL',
            'description': 'Current filter: ALL, ACTIVE, or COMPLETED',
            'label': 'Filter Mode',
            'tier': 'domain',
            'type': 'string',
          },
          'onAddTodo': {
            'default': 'ADD_TODO',
            'description': 'Handler for adding new todos',
            'label': 'On Add Todo',
            'tier': 'presentation',
            'type': 'string',
          },
          'onCompleteTodo': {
            'default': 'COMPLETE_TODO',
            'description': 'Handler for toggling todo completion',
            'label': 'On Complete Todo',
            'tier': 'presentation',
            'type': 'string',
          },
          'onDeleteTodo': {
            'default': 'DELETE_TODO',
            'description': 'Handler for deleting todos',
            'label': 'On Delete Todo',
            'tier': 'presentation',
            'type': 'string',
          },
          'todos': {
            'default': [],
            'description': 'List of todo items',
            'label': 'Todos',
            'tier': 'domain',
            'type': '[Todo]',
          },
        },
        'emits': [
          {
            'description': 'Callback when a new todo is added',
            'event': '@config.onAddTodo',
            'payloadSchema': [
              {
                'name': 'text',
                'required': true,
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
          {
            'description': 'Callback when a todo completion status changes',
            'event': '@config.onCompleteTodo',
            'payloadSchema': [
              {
                'name': 'todoId',
                'required': true,
                'type': 'string',
              },
              {
                'name': 'completed',
                'required': true,
                'type': 'boolean',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
          {
            'description': 'Callback when a todo is deleted',
            'event': '@config.onDeleteTodo',
            'payloadSchema': [
              {
                'name': 'todoId',
                'required': true,
                'type': 'string',
              },
            ],
            'scope': 'external',
            'tier': 'domain',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'Todo',
        'name': 'TodoListRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Callback when a new todo is added',
              'key': '@config.onAddTodo',
              'name': '@config.on add todo',
              'payloadSchema': [
                {
                  'name': 'text',
                  'required': true,
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
            {
              'description': 'Callback when a todo completion status changes',
              'key': '@config.onCompleteTodo',
              'name': '@config.on complete todo',
              'payloadSchema': [
                {
                  'name': 'todoId',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'completed',
                  'required': true,
                  'type': 'boolean',
                },
              ],
              'tier': 'domain',
            },
            {
              'description': 'Callback when a todo is deleted',
              'key': '@config.onDeleteTodo',
              'name': '@config.on delete todo',
              'payloadSchema': [
                {
                  'name': 'todoId',
                  'required': true,
                  'type': 'string',
                },
              ],
              'tier': 'domain',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'stretch',
                    'children': [
                      {
                        'content': 'My Todos',
                        'type': 'typography',
                        'variant': 'h1',
                      },
                      {
                        'content': 'Manage your tasks',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                    ],
                    'direction': 'vertical',
                    'gap': 'md',
                    'type': 'stack',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'TodoListPage',
        'path': '/',
        'traits': [
          {
            'ref': 'TodoListRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdTodoListAppTodoListAppOrbital. */
export const StdTodoListAppTodoListAppOrbitalManifest = {
  organism: 'todo-list-app',
  orbitalName: 'TodoListAppOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'TodoListRender',
  ] as const,
};

/** Typed guard — runtime validates StdTodoListAppTodoListAppOrbitalParams keys. */
export function isStdTodoListAppTodoListAppOrbitalParams(p: object): p is StdTodoListAppTodoListAppOrbitalParams {
  type _OverrideRecord = NonNullable<StdTodoListAppTodoListAppOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdTodoListAppTodoListAppOrbitalManifest.traitNames,
      ...StdTodoListAppTodoListAppOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for todo-list-app — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdTodoListAppParams {
  TodoListApp?: StdTodoListAppTodoListAppOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdTodoListApp(params: StdTodoListAppParams = {}): OrbitalDefinition[] {
  return [
    stdTodoListAppTodoListAppOrbital(params.TodoListApp ?? {}),
  ];
}
