/**
 * ui-dock-layout — DockLayout — UI factory (1:1 with the @almadar/ui dock-layout pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-dock-layout.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-dock-layout
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-dock-layout';
const ALIAS = 'UiDockLayout';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiDockLayoutEventKey = 'BOTTOM_PANEL_COLLAPSED_CHANGE' | 'BOTTOM_PANEL_HEIGHT_CHANGE' | 'INIT' | 'SECONDARY_SIDEBAR_COLLAPSED_CHANGE' | 'SIDEBAR_COLLAPSED_CHANGE' | 'SIDEBAR_WIDTH_CHANGE';

/**
 * Payload shape for the `SIDEBAR_COLLAPSED_CHANGE` event.
 */
export interface StdUiDockLayoutSidebarCollapsedChangePayload {
  collapsed?: boolean;
}

/**
 * Payload shape for the `SIDEBAR_WIDTH_CHANGE` event.
 */
export interface StdUiDockLayoutSidebarWidthChangePayload {
  width?: number;
}

/**
 * Payload shape for the `BOTTOM_PANEL_COLLAPSED_CHANGE` event.
 */
export interface StdUiDockLayoutBottomPanelCollapsedChangePayload {
  collapsed?: boolean;
}

/**
 * Payload shape for the `BOTTOM_PANEL_HEIGHT_CHANGE` event.
 */
export interface StdUiDockLayoutBottomPanelHeightChangePayload {
  height?: number;
}

/**
 * Payload shape for the `SECONDARY_SIDEBAR_COLLAPSED_CHANGE` event.
 */
export interface StdUiDockLayoutSecondarySidebarCollapsedChangePayload {
  collapsed?: boolean;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiDockLayoutConfig {
  bottomPanel?: PatternValue;
  bottomPanelClassName?: string;
  /** Default: `false` */
  bottomPanelCollapsed?: boolean;
  /** Default: `30` */
  bottomPanelHeight?: number;
  /** Default: `120` */
  bottomPanelMinSize?: number;
  className?: string;
  /** Default: `[]` */
  main?: PatternValue;
  mainClassName?: string;
  /** Default: `"BOTTOM_PANEL_COLLAPSED_CHANGE"` */
  onBottomPanelCollapsedChange?: string;
  /** Default: `"BOTTOM_PANEL_HEIGHT_CHANGE"` */
  onBottomPanelHeightChange?: string;
  /** Default: `"SECONDARY_SIDEBAR_COLLAPSED_CHANGE"` */
  onSecondarySidebarCollapsedChange?: string;
  /** Default: `"SIDEBAR_COLLAPSED_CHANGE"` */
  onSidebarCollapsedChange?: string;
  /** Default: `"SIDEBAR_WIDTH_CHANGE"` */
  onSidebarWidthChange?: string;
  rail?: PatternValue;
  railClassName?: string;
  /** Default: `56` */
  railWidth?: number;
  secondarySidebar?: PatternValue;
  secondarySidebarClassName?: string;
  /** Default: `false` */
  secondarySidebarCollapsed?: boolean;
  /** Default: `280` */
  secondarySidebarWidth?: number;
  sidebar?: PatternValue;
  sidebarClassName?: string;
  /** Default: `false` */
  sidebarCollapsed?: boolean;
  /** Default: `160` */
  sidebarMinSize?: number;
  /** Default: `20` */
  sidebarWidth?: number;
  statusBar?: PatternValue;
  statusBarClassName?: string;
}

type _StdUiDockLayoutEntityName = 'DockLayoutItem';
type _StdUiDockLayoutListenTraitName = 'DockLayoutRender';

/**
 * Tunable params for the DockLayoutOrbital orbital.
 *
 * Canonical entity: DockLayoutItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiDockLayoutDockLayoutOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'DockLayoutRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait DockLayoutOrbital's `uses[]` exports. */
type _StdUiDockLayoutDockLayoutOrbitalUsesRef = never;

/** Per-orbital factory: builds the DockLayoutOrbital orbital with consumer params. */
export function stdUiDockLayoutDockLayoutOrbital(params: StdUiDockLayoutDockLayoutOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'DockLayoutOrbital',
    uses: [],
    entity: {
      name: 'DockLayoutItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'bottomPanel': {
            'description': 'Collapsible, resizable-height bottom panel.',
            'label': 'Bottom Panel',
            'tier': 'presentation',
            'type': 'node',
          },
          'bottomPanelClassName': {
            'description': 'bottomPanelClassName prop',
            'label': 'Bottom Panel Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'bottomPanelCollapsed': {
            'default': false,
            'description': 'Whether `bottomPanel` is collapsed. @default false',
            'label': 'Bottom Panel Collapsed',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'bottomPanelHeight': {
            'default': 30,
            'description': 'Bottom panel size as a SplitPane ratio (0-100, percentage given to the panel). @default 30',
            'label': 'Bottom Panel Height',
            'tier': 'presentation',
            'type': 'number',
          },
          'bottomPanelMinSize': {
            'default': 120,
            'description': 'Minimum bottom panel size in pixels, forwarded to SplitPane\'s `minSize`. @default 120',
            'label': 'Bottom Panel Min Size',
            'tier': 'presentation',
            'type': 'number',
          },
          'className': {
            'description': 'Additional CSS classes on the root frame.',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'main': {
            'default': [],
            'description': 'Required center region.',
            'label': 'Main',
            'tier': 'presentation',
            'type': 'node',
          },
          'mainClassName': {
            'description': 'mainClassName prop',
            'label': 'Main Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'onBottomPanelCollapsedChange': {
            'default': 'BOTTOM_PANEL_COLLAPSED_CHANGE',
            'description': 'onBottomPanelCollapsedChange prop',
            'label': 'On Bottom Panel Collapsed Change',
            'tier': 'presentation',
            'type': 'event',
          },
          'onBottomPanelHeightChange': {
            'default': 'BOTTOM_PANEL_HEIGHT_CHANGE',
            'description': 'onBottomPanelHeightChange prop',
            'label': 'On Bottom Panel Height Change',
            'tier': 'presentation',
            'type': 'event',
          },
          'onSecondarySidebarCollapsedChange': {
            'default': 'SECONDARY_SIDEBAR_COLLAPSED_CHANGE',
            'description': 'onSecondarySidebarCollapsedChange prop',
            'label': 'On Secondary Sidebar Collapsed Change',
            'tier': 'presentation',
            'type': 'event',
          },
          'onSidebarCollapsedChange': {
            'default': 'SIDEBAR_COLLAPSED_CHANGE',
            'description': 'onSidebarCollapsedChange prop',
            'label': 'On Sidebar Collapsed Change',
            'tier': 'presentation',
            'type': 'event',
          },
          'onSidebarWidthChange': {
            'default': 'SIDEBAR_WIDTH_CHANGE',
            'description': 'onSidebarWidthChange prop',
            'label': 'On Sidebar Width Change',
            'tier': 'presentation',
            'type': 'event',
          },
          'rail': {
            'description': 'Fixed-width far-left vertical strip (e.g. an icon nav rail).',
            'label': 'Rail',
            'tier': 'presentation',
            'type': 'node',
          },
          'railClassName': {
            'description': 'railClassName prop',
            'label': 'Rail Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'railWidth': {
            'default': 56,
            'description': 'Width of `rail` in pixels. @default 56',
            'label': 'Rail Width',
            'tier': 'presentation',
            'type': 'number',
          },
          'secondarySidebar': {
            'description': 'Collapsible right sidebar.',
            'label': 'Secondary Sidebar',
            'tier': 'presentation',
            'type': 'node',
          },
          'secondarySidebarClassName': {
            'description': 'secondarySidebarClassName prop',
            'label': 'Secondary Sidebar Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'secondarySidebarCollapsed': {
            'default': false,
            'description': 'Whether `secondarySidebar` is collapsed. @default false',
            'label': 'Secondary Sidebar Collapsed',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'secondarySidebarWidth': {
            'default': 280,
            'description': 'Width of `secondarySidebar` in pixels (fixed — not resizable). @default 280',
            'label': 'Secondary Sidebar Width',
            'tier': 'presentation',
            'type': 'number',
          },
          'sidebar': {
            'description': 'Collapsible, resizable left sidebar.',
            'label': 'Sidebar',
            'tier': 'presentation',
            'type': 'node',
          },
          'sidebarClassName': {
            'description': 'sidebarClassName prop',
            'label': 'Sidebar Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'sidebarCollapsed': {
            'default': false,
            'description': 'Whether `sidebar` is collapsed. @default false',
            'label': 'Sidebar Collapsed',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'sidebarMinSize': {
            'default': 160,
            'description': 'Minimum sidebar size in pixels, forwarded to SplitPane\'s `minSize`. @default 160',
            'label': 'Sidebar Min Size',
            'tier': 'presentation',
            'type': 'number',
          },
          'sidebarWidth': {
            'default': 20,
            'description': 'Sidebar size as a SplitPane ratio (0-100, percentage of the sidebar/main split). @default 20',
            'label': 'Sidebar Width',
            'tier': 'presentation',
            'type': 'number',
          },
          'statusBar': {
            'description': 'Slim strip pinned to the bottom of the frame, below the bottom panel.',
            'label': 'Status Bar',
            'tier': 'presentation',
            'type': 'node',
          },
          'statusBarClassName': {
            'description': 'statusBarClassName prop',
            'label': 'Status Bar Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
        },
        'effectRow': [
          {
            'kind': 'render-ui',
            'resource': 'main',
          },
        ],
        'emits': [
          {
            'definerKnob': 'onSidebarCollapsedChange',
            'description': 'onSidebarCollapsedChange prop',
            'event': '@config.onSidebarCollapsedChange',
            'payloadSchema': [
              {
                'name': 'collapsed',
                'type': 'boolean',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'definerKnob': 'onSidebarWidthChange',
            'description': 'onSidebarWidthChange prop',
            'event': '@config.onSidebarWidthChange',
            'payloadSchema': [
              {
                'name': 'width',
                'type': 'number',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'definerKnob': 'onBottomPanelCollapsedChange',
            'description': 'onBottomPanelCollapsedChange prop',
            'event': '@config.onBottomPanelCollapsedChange',
            'payloadSchema': [
              {
                'name': 'collapsed',
                'type': 'boolean',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'definerKnob': 'onBottomPanelHeightChange',
            'description': 'onBottomPanelHeightChange prop',
            'event': '@config.onBottomPanelHeightChange',
            'payloadSchema': [
              {
                'name': 'height',
                'type': 'number',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'definerKnob': 'onSecondarySidebarCollapsedChange',
            'description': 'onSecondarySidebarCollapsedChange prop',
            'event': '@config.onSecondarySidebarCollapsedChange',
            'payloadSchema': [
              {
                'name': 'collapsed',
                'type': 'boolean',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'DockLayoutItem',
        'name': 'DockLayoutRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'onSidebarCollapsedChange prop',
              'key': '@config.onSidebarCollapsedChange',
              'name': '@config.on sidebar collapsed change',
              'payloadSchema': [
                {
                  'name': 'collapsed',
                  'type': 'boolean',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'onSidebarWidthChange prop',
              'key': '@config.onSidebarWidthChange',
              'name': '@config.on sidebar width change',
              'payloadSchema': [
                {
                  'name': 'width',
                  'type': 'number',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'onBottomPanelCollapsedChange prop',
              'key': '@config.onBottomPanelCollapsedChange',
              'name': '@config.on bottom panel collapsed change',
              'payloadSchema': [
                {
                  'name': 'collapsed',
                  'type': 'boolean',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'onBottomPanelHeightChange prop',
              'key': '@config.onBottomPanelHeightChange',
              'name': '@config.on bottom panel height change',
              'payloadSchema': [
                {
                  'name': 'height',
                  'type': 'number',
                },
              ],
              'tier': 'essential',
            },
            {
              'description': 'onSecondarySidebarCollapsedChange prop',
              'key': '@config.onSecondarySidebarCollapsedChange',
              'name': '@config.on secondary sidebar collapsed change',
              'payloadSchema': [
                {
                  'name': 'collapsed',
                  'type': 'boolean',
                },
              ],
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'bottomPanel': '@config.bottomPanel',
                    'bottomPanelClassName': '@config.bottomPanelClassName',
                    'bottomPanelCollapsed': '@config.bottomPanelCollapsed',
                    'bottomPanelHeight': '@config.bottomPanelHeight',
                    'bottomPanelMinSize': '@config.bottomPanelMinSize',
                    'className': '@config.className',
                    'main': '@config.main',
                    'mainClassName': '@config.mainClassName',
                    'onBottomPanelCollapsedChange': '@config.onBottomPanelCollapsedChange',
                    'onBottomPanelHeightChange': '@config.onBottomPanelHeightChange',
                    'onSecondarySidebarCollapsedChange': '@config.onSecondarySidebarCollapsedChange',
                    'onSidebarCollapsedChange': '@config.onSidebarCollapsedChange',
                    'onSidebarWidthChange': '@config.onSidebarWidthChange',
                    'rail': '@config.rail',
                    'railClassName': '@config.railClassName',
                    'railWidth': '@config.railWidth',
                    'secondarySidebar': '@config.secondarySidebar',
                    'secondarySidebarClassName': '@config.secondarySidebarClassName',
                    'secondarySidebarCollapsed': '@config.secondarySidebarCollapsed',
                    'secondarySidebarWidth': '@config.secondarySidebarWidth',
                    'sidebar': '@config.sidebar',
                    'sidebarClassName': '@config.sidebarClassName',
                    'sidebarCollapsed': '@config.sidebarCollapsed',
                    'sidebarMinSize': '@config.sidebarMinSize',
                    'sidebarWidth': '@config.sidebarWidth',
                    'statusBar': '@config.statusBar',
                    'statusBarClassName': '@config.statusBarClassName',
                    'type': 'dock-layout',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'DockLayoutPage',
        'path': '/dock-layout',
        'traits': [
          {
            'ref': 'DockLayoutRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiDockLayoutDockLayoutOrbital. */
export const StdUiDockLayoutDockLayoutOrbitalManifest = {
  organism: 'ui-dock-layout',
  orbitalName: 'DockLayoutOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'DockLayoutRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiDockLayoutDockLayoutOrbitalParams keys. */
export function isStdUiDockLayoutDockLayoutOrbitalParams(p: object): p is StdUiDockLayoutDockLayoutOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiDockLayoutDockLayoutOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiDockLayoutDockLayoutOrbitalManifest.traitNames,
      ...StdUiDockLayoutDockLayoutOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-dock-layout — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiDockLayoutParams {
  DockLayout?: StdUiDockLayoutDockLayoutOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiDockLayout(params: StdUiDockLayoutParams = {}): OrbitalDefinition[] {
  return [
    stdUiDockLayoutDockLayoutOrbital(params.DockLayout ?? {}),
  ];
}
