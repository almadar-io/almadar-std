/**
 * ui-chart — Chart — UI factory (1:1 with the @almadar/ui chart pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-chart.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-chart
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-chart';
const ALIAS = 'UiChart';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiChartEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiChartConfig {
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `[]` */
  scatterData?: EntityRow[];
  /** Default: `"bar-vertical"` */
  look?: 'bar-vertical' | 'bar-horizontal' | 'line' | 'area' | 'pie' | 'donut' | 'scatter' | 'histogram';
  /** Default: `""` */
  className?: string;
  /** Default: `""` */
  title?: string;
  /** Default: `200` */
  height?: number;
  /** Default: `false` */
  showValues?: boolean;
  /** Default: `true` */
  showLegend?: boolean;
  /** Default: `""` */
  drillEvent?: string;
  error?: unknown;
  /** Default: `[]` */
  series?: EntityRow[];
  /** Default: `"none"` */
  stack?: 'none' | 'stack' | 'normalize';
  /** Default: `false` */
  timeAxis?: boolean;
  /** Default: `"bar"` */
  chartType?: 'bar' | 'line' | 'pie' | 'area' | 'donut' | 'scatter' | 'histogram';
  /** Default: `[]` */
  data?: EntityRow[];
  /** Default: `""` */
  subtitle?: string;
  /** Default: `[]` */
  actions?: EntityRow[];
}

/**
 * Tunable params for the ChartOrbital orbital.
 *
 * Canonical entity: ChartItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiChartChartOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ChartRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the ChartOrbital orbital with consumer params. */
export function stdUiChartChartOrbital(params: StdUiChartChartOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'ChartItem';
  const built = makeOrbitalWithUses({
    name: 'ChartOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'name': 'ChartRender',
        'entityRebindable': true,
        'entityContract': {
          'requires': [],
          'provides': [],
        },
        'category': 'interaction',
        'linkedEntity': 'ChartItem',
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'ChartItem',
                  {},
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'chartType': '@config.chartType',
                    'className': '@config.className',
                    'showLegend': '@config.showLegend',
                    'height': '@config.height',
                    'data': '@config.data',
                    'drillEvent': '@config.drillEvent',
                    'actions': '@config.actions',
                    'timeAxis': '@config.timeAxis',
                    'title': '@config.title',
                    'scatterData': '@config.scatterData',
                    'entity': 'ChartItem',
                    'stack': '@config.stack',
                    'series': '@config.series',
                    'error': '@config.error',
                    'subtitle': '@config.subtitle',
                    'look': '@config.look',
                    'showValues': '@config.showValues',
                    'type': 'chart',
                    'isLoading': '@config.isLoading',
                  },
                ],
              ],
            },
          ],
        },
        'config': {
          'isLoading': {
            'type': 'boolean',
            'default': false,
            'label': 'Is Loading',
            'description': 'Loading state',
            'tier': 'presentation',
          },
          'scatterData': {
            'type': '[ChartScatterDataItem]',
            'default': [],
            'label': 'Scatter Data',
            'description': 'Raw {x,y} points for scatter',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': false,
                },
                'size': {
                  'name': 'size',
                  'type': 'number',
                  'required': false,
                },
                'x': {
                  'name': 'x',
                  'type': 'number',
                  'required': true,
                },
                'y': {
                  'name': 'y',
                  'type': 'number',
                  'required': true,
                },
                'color': {
                  'name': 'color',
                  'type': 'string',
                  'required': false,
                },
              },
            },
          },
          'look': {
            'type': 'string',
            'default': 'bar-vertical',
            'label': 'Look',
            'description': 'Layer 2 visual treatment — drives the chart sub-renderer (bar / line / pie / etc.).',
            'synonyms': 'chart style, render style, visual treatment',
            'tier': 'presentation',
            'values': [
              'bar-vertical',
              'bar-horizontal',
              'line',
              'area',
              'pie',
              'donut',
              'scatter',
              'histogram',
            ],
          },
          'className': {
            'type': 'string',
            'default': '',
            'label': 'Class Name',
            'description': 'Additional CSS classes',
            'tier': 'presentation',
          },
          'title': {
            'type': 'string',
            'default': '',
            'label': 'Title',
            'description': 'Chart title',
            'tier': 'presentation',
          },
          'height': {
            'type': 'number',
            'default': 200,
            'label': 'Height',
            'description': 'Chart height in px',
            'tier': 'presentation',
          },
          'showValues': {
            'type': 'boolean',
            'default': false,
            'label': 'Show Values',
            'description': 'Show values on chart',
            'tier': 'presentation',
          },
          'showLegend': {
            'type': 'boolean',
            'default': true,
            'label': 'Show Legend',
            'description': 'Show legend',
            'tier': 'presentation',
          },
          'drillEvent': {
            'type': 'string',
            'default': '',
            'label': 'Drill Event',
            'description': 'Event name emitted as `UI:{drillEvent}` with `{ label, value, seriesLabel? }` on data-point click',
            'tier': 'presentation',
          },
          'error': {
            'type': 'json',
            'label': 'Error',
            'description': 'Error state',
            'tier': 'presentation',
          },
          'series': {
            'type': '[ChartSeriesItem]',
            'default': [],
            'label': 'Series',
            'description': 'Multi-series data',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'color': {
                  'name': 'color',
                  'type': 'string',
                  'required': false,
                },
                'name': {
                  'name': 'name',
                  'type': 'string',
                  'required': true,
                },
                'data': {
                  'name': 'data',
                  'type': 'array',
                  'required': true,
                  'items': {
                    'type': 'object',
                    'properties': {
                      'date': {
                        'name': 'date',
                        'type': 'string',
                        'required': true,
                      },
                      'value': {
                        'name': 'value',
                        'type': 'number',
                        'required': true,
                      },
                      'label': {
                        'name': 'label',
                        'type': 'string',
                        'required': false,
                      },
                    },
                  },
                },
                'dashed': {
                  'name': 'dashed',
                  'type': 'boolean',
                  'required': false,
                },
              },
            },
          },
          'stack': {
            'type': 'string',
            'default': 'none',
            'label': 'Stack',
            'description': 'Stack mode for bar / area',
            'tier': 'presentation',
            'values': [
              'none',
              'stack',
              'normalize',
            ],
          },
          'timeAxis': {
            'type': 'boolean',
            'default': false,
            'label': 'Time Axis',
            'description': 'Format X-axis labels as time (ISO date in → \'Mar 2026\'-style label out)',
            'tier': 'presentation',
          },
          'chartType': {
            'type': 'string',
            'default': 'bar',
            'label': 'Chart Type',
            'description': 'Chart type (legacy alias for `look`).',
            'synonyms': 'chart style, graph type, bar chart, line chart, pie chart, area chart, donut chart, scatter plot, histogram',
            'tier': 'domain',
            'values': [
              'bar',
              'line',
              'pie',
              'area',
              'donut',
              'scatter',
              'histogram',
            ],
          },
          'data': {
            'type': '[ChartDataItem]',
            'default': [],
            'label': 'Data',
            'description': 'Simple single-series shorthand (bar/line/pie/area/donut/histogram)',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': false,
                },
                'date': {
                  'name': 'date',
                  'type': 'string',
                  'required': true,
                },
                'value': {
                  'name': 'value',
                  'type': 'number',
                  'required': true,
                },
              },
            },
          },
          'subtitle': {
            'type': 'string',
            'default': '',
            'label': 'Subtitle',
            'description': 'Chart subtitle / description',
            'tier': 'presentation',
          },
          'actions': {
            'type': '[ChartActionsItem]',
            'default': [],
            'label': 'Actions',
            'description': 'Top-level chart actions (export, refresh, etc.)',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'event': {
                  'name': 'event',
                  'type': 'string',
                  'required': false,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
                'navigatesTo': {
                  'name': 'navigatesTo',
                  'type': 'string',
                  'required': false,
                },
                'variant': {
                  'name': 'variant',
                  'type': 'string',
                  'required': false,
                  'values': [
                    'primary',
                    'secondary',
                    'ghost',
                  ],
                },
              },
            },
          },
        },
        'scope': 'instance',
      } as never, 'ChartItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'ChartPage',
        'path': '/chart',
        'traits': [
          {
            'ref': 'ChartRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiChartChartOrbital. */
export const StdUiChartChartOrbitalManifest = {
  organism: 'ui-chart',
  orbitalName: 'ChartOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'ChartRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiChartChartOrbitalParams keys. */
export function isStdUiChartChartOrbitalParams(p: object): p is StdUiChartChartOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiChartChartOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiChartChartOrbitalManifest.traitNames,
      ...StdUiChartChartOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-chart — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiChartParams {
  Chart?: StdUiChartChartOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiChart(params: StdUiChartParams = {}): OrbitalDefinition[] {
  return [
    stdUiChartChartOrbital(params.Chart ?? {}),
  ];
}
