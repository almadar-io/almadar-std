/**
 * ui-media-gallery — MediaGallery — UI factory (1:1 with the @almadar/ui media-gallery pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-media-gallery.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-media-gallery
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { asEntityId, asEventId, asPageId, asTraitId } from '@almadar/core/types';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-media-gallery';
const ALIAS = 'UiMediaGallery';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiMediaGalleryEventKey = 'INIT' | 'MediaGalleryLoaded' | 'SELECTION';

/**
 * Payload shape for the `SELECTION` event.
 */
export interface StdUiMediaGallerySelectionPayload {
  selection: EntityRow[];
}

/**
 * Payload shape for the `MediaGalleryLoaded` event.
 */
export interface StdUiMediaGalleryMediaGalleryLoadedPayload {
  data?: EntityRow[];
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiMediaGalleryConfig {
  /** Default: `[{"label":"Label","navigatesTo":"Navigates To","variant":"primary"},{"label":"Label 2","navigatesTo":"Navigates To 2","variant":"secondary"}]` */
  actions?: EntityRow[];
  /** Default: `{}` */
  activeFilters?: unknown;
  /** Default: `"square"` */
  aspectRatio?: 'square' | 'landscape' | 'portrait';
  /** Default: `""` */
  className?: string;
  /** Default: `3` */
  columns?: number;
  error?: EntityRow;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `[{"alt":"Alt","caption":"Caption","fileSize":"File Size","id":"Id","mediaType":"image","src":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png","thumbnail":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png"},{"alt":"Alt 2","caption":"Caption 2","fileSize":"File Size 2","id":"Id 2","mediaType":"video","src":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png","thumbnail":"https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png"}]` */
  items?: EntityRow[];
  /** Default: `0` */
  pageProp?: number;
  /** Default: `0` */
  pageSize?: number;
  /** Default: `"Search Value"` */
  searchValue?: string;
  /** Default: `false` */
  selectable?: boolean;
  /** Default: `[]` */
  selectedIds?: string[];
  /** Default: `[]` */
  selectedItems?: string[];
  /** Default: `"SELECTION"` */
  selectionEvent?: string;
  /** Default: `false` */
  showUpload?: boolean;
  /** Default: `"Sort By"` */
  sortBy?: string;
  /** Default: `"asc"` */
  sortDirection?: 'asc' | 'desc';
  /** Default: `"Title"` */
  title?: string;
  /** Default: `0` */
  totalCount?: number;
}

type _StdUiMediaGalleryEntityName = 'MediaGalleryItem';
type _StdUiMediaGalleryListenTraitName = 'MediaGalleryRender';

/**
 * Tunable params for the MediaGalleryOrbital orbital.
 *
 * Canonical entity: MediaGalleryItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiMediaGalleryMediaGalleryOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MediaGalleryRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait MediaGalleryOrbital's `uses[]` exports. */
type _StdUiMediaGalleryMediaGalleryOrbitalUsesRef = never;

/** Per-orbital factory: builds the MediaGalleryOrbital orbital with consumer params. */
export function stdUiMediaGalleryMediaGalleryOrbital(params: StdUiMediaGalleryMediaGalleryOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'MediaGalleryItem';
  const built = makeOrbitalWithUses({
    name: 'MediaGalleryOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'interaction',
        'config': {
          'actions': {
            'default': [
              {
                'label': 'Label',
                'navigatesTo': 'Navigates To',
                'variant': 'primary',
              },
              {
                'label': 'Label 2',
                'navigatesTo': 'Navigates To 2',
                'variant': 'secondary',
              },
            ],
            'description': 'Actions',
            'items': {
              'properties': {
                'event': {
                  'name': 'event',
                  'required': false,
                  'type': 'string',
                },
                'label': {
                  'name': 'label',
                  'required': true,
                  'type': 'string',
                },
                'navigatesTo': {
                  'name': 'navigatesTo',
                  'required': false,
                  'type': 'string',
                },
                'variant': {
                  'name': 'variant',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'primary',
                    'secondary',
                    'ghost',
                  ],
                },
              },
              'type': 'object',
            },
            'label': 'Actions',
            'tier': 'presentation',
            'type': '[MediaGalleryActionsItem]',
          },
          'activeFilters': {
            'default': {},
            'description': 'Active filters',
            'label': 'Active Filters',
            'tier': 'presentation',
            'type': 'json',
          },
          'aspectRatio': {
            'default': 'square',
            'description': 'Aspect ratio for thumbnails',
            'label': 'Aspect Ratio',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'square',
              'landscape',
              'portrait',
            ],
          },
          'className': {
            'default': '',
            'description': 'Additional CSS classes',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'columns': {
            'default': 3,
            'description': 'Column count',
            'label': 'Columns',
            'tier': 'presentation',
            'type': 'number',
          },
          'error': {
            'description': 'Error state (UiError)',
            'label': 'Error',
            'properties': {
              'code': {
                'name': 'code',
                'required': false,
                'type': 'string',
              },
              'message': {
                'name': 'message',
                'required': true,
                'type': 'string',
              },
              'name': {
                'name': 'name',
                'required': false,
                'type': 'string',
              },
              'stack': {
                'name': 'stack',
                'required': false,
                'type': 'string',
              },
            },
            'tier': 'presentation',
            'type': 'MediaGalleryError',
          },
          'isLoading': {
            'default': false,
            'description': 'Loading state indicator',
            'label': 'Is Loading',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'items': {
            'default': [
              {
                'alt': 'Alt',
                'caption': 'Caption',
                'fileSize': 'File Size',
                'id': 'Id',
                'mediaType': 'image',
                'src': 'https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png',
                'thumbnail': 'https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png',
              },
              {
                'alt': 'Alt 2',
                'caption': 'Caption 2',
                'fileSize': 'File Size 2',
                'id': 'Id 2',
                'mediaType': 'video',
                'src': 'https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png',
                'thumbnail': 'https://almadar-kflow-assets.web.app/shared/ui-roguelike-board/default/units/player.png',
              },
            ],
            'description': 'Media items',
            'items': {
              'properties': {
                'alt': {
                  'name': 'alt',
                  'required': false,
                  'type': 'string',
                },
                'caption': {
                  'name': 'caption',
                  'required': false,
                  'type': 'string',
                },
                'fileSize': {
                  'name': 'fileSize',
                  'required': false,
                  'type': 'string',
                },
                'id': {
                  'name': 'id',
                  'required': false,
                  'type': 'string',
                },
                'mediaType': {
                  'name': 'mediaType',
                  'required': false,
                  'type': 'string',
                  'values': [
                    'image',
                    'video',
                  ],
                },
                'src': {
                  'name': 'src',
                  'required': false,
                  'type': 'string',
                },
                'thumbnail': {
                  'name': 'thumbnail',
                  'required': false,
                  'type': 'string',
                },
              },
              'type': 'object',
            },
            'label': 'Items',
            'tier': 'presentation',
            'type': '[MediaGalleryItemsItem]',
          },
          'pageProp': {
            'default': 0,
            'description': 'Current page number',
            'label': 'Page',
            'synonyms': 'page',
            'tier': 'presentation',
            'type': 'number',
          },
          'pageSize': {
            'default': 0,
            'description': 'Number of items per page',
            'label': 'Page Size',
            'tier': 'presentation',
            'type': 'number',
          },
          'searchValue': {
            'default': 'Search Value',
            'description': 'Current search query value',
            'label': 'Search Value',
            'tier': 'presentation',
            'type': 'string',
          },
          'selectable': {
            'default': false,
            'description': 'Enable item selection',
            'label': 'Selectable',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'selectedIds': {
            'default': [],
            'description': 'Currently selected item IDs',
            'items': {
              'type': 'string',
            },
            'label': 'Selected Ids',
            'tier': 'presentation',
            'type': '[string]',
          },
          'selectedItems': {
            'default': [],
            'description': 'Selected item IDs',
            'items': {
              'type': 'string',
            },
            'label': 'Selected Items',
            'tier': 'presentation',
            'type': '[string]',
          },
          'selectionEvent': {
            'default': 'SELECTION',
            'description': 'Event name emitted when selection changes (emitted as UI:{selectionEvent})',
            'label': 'Selection Event',
            'tier': 'presentation',
            'type': 'string',
          },
          'showUpload': {
            'default': false,
            'description': 'Show upload button',
            'label': 'Show Upload',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'sortBy': {
            'default': 'Sort By',
            'description': 'Current sort field',
            'label': 'Sort By',
            'tier': 'presentation',
            'type': 'string',
          },
          'sortDirection': {
            'default': 'asc',
            'description': 'Current sort direction',
            'label': 'Sort Direction',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'asc',
              'desc',
            ],
          },
          'title': {
            'default': 'Title',
            'description': 'Gallery title',
            'label': 'Title',
            'tier': 'presentation',
            'type': 'string',
          },
          'totalCount': {
            'default': 0,
            'description': 'Total number of items',
            'label': 'Total Count',
            'tier': 'presentation',
            'type': 'number',
          },
        },
        'emits': [
          {
            'description': 'Event name emitted when selection changes (emitted as UI:{selectionEvent})',
            'event': '@config.selectionEvent',
            'eventId': asEventId('evt_01KXG04STWF6EJB6RZPKZEB7ZS'),
            'payloadSchema': [
              {
                'name': 'selection',
                'required': true,
                'type': '[object]',
              },
            ],
            'scope': 'external',
            'tier': 'essential',
          },
          {
            'description': 'MediaGallery rows finished loading; payload.data holds the collection.',
            'event': 'MediaGalleryLoaded',
            'eventId': asEventId('evt_01KXG04STWA91DKSP7J3RGWY7C'),
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[MediaGalleryItem]',
              },
            ],
            'scope': 'internal',
            'synonyms': 'loaded, fetched, retrieved',
            'tier': 'essential',
          },
        ],
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'id': asTraitId('trt_01KXG04STSKR7RB9HG6V6H19XZ'),
        'linkedEntity': 'MediaGalleryItem',
        'linkedEntityId': asEntityId('ent_01KXG04STSMPAV51YZFEJ09NCJ'),
        'name': 'MediaGalleryRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'MediaGallery rows finished loading; payload.data holds the collection.',
              'key': 'MediaGalleryLoaded',
              'name': 'Media gallery loaded',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[MediaGalleryItem]',
                },
              ],
              'synonyms': 'loaded, fetched, retrieved',
              'tier': 'essential',
            },
            {
              'description': 'Event name emitted when selection changes (emitted as UI:{selectionEvent})',
              'key': '@config.selectionEvent',
              'name': '@config.selection event',
              'payloadSchema': [
                {
                  'name': 'selection',
                  'required': true,
                  'type': '[object]',
                },
              ],
              'tier': 'essential',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'fetch',
                  ('MediaGalleryItem' satisfies _StdUiMediaGalleryEntityName),
                  {
                    'emit': {
                      'success': 'MediaGalleryLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'actions': '@config.actions',
                    'activeFilters': '@config.activeFilters',
                    'aspectRatio': '@config.aspectRatio',
                    'className': '@config.className',
                    'columns': '@config.columns',
                    'entity': '@entity',
                    'error': '@config.error',
                    'isLoading': '@config.isLoading',
                    'items': '@config.items',
                    'page': '@config.pageProp',
                    'pageSize': '@config.pageSize',
                    'searchValue': '@config.searchValue',
                    'selectable': '@config.selectable',
                    'selectedIds': '@config.selectedIds',
                    'selectedItems': '@config.selectedItems',
                    'selectionEvent': '@config.selectionEvent',
                    'showUpload': '@config.showUpload',
                    'sortBy': '@config.sortBy',
                    'sortDirection': '@config.sortDirection',
                    'title': '@config.title',
                    'totalCount': '@config.totalCount',
                    'type': 'media-gallery',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'actions': '@config.actions',
                    'activeFilters': '@config.activeFilters',
                    'aspectRatio': '@config.aspectRatio',
                    'className': '@config.className',
                    'columns': '@config.columns',
                    'entity': '@payload.data',
                    'error': '@config.error',
                    'isLoading': '@config.isLoading',
                    'items': '@config.items',
                    'page': '@config.pageProp',
                    'pageSize': '@config.pageSize',
                    'searchValue': '@config.searchValue',
                    'selectable': '@config.selectable',
                    'selectedIds': '@config.selectedIds',
                    'selectedItems': '@config.selectedItems',
                    'selectionEvent': '@config.selectionEvent',
                    'showUpload': '@config.showUpload',
                    'sortBy': '@config.sortBy',
                    'sortDirection': '@config.sortDirection',
                    'title': '@config.title',
                    'totalCount': '@config.totalCount',
                    'type': 'media-gallery',
                  },
                ],
              ],
              'event': 'MediaGalleryLoaded',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait, 'MediaGalleryItem', canonicalName),
    ],
    pages: [
      {
        'id': asPageId('pag_01KXG04STWNYYKB6JEWP4GT8Q5'),
        'name': 'MediaGalleryPage',
        'path': '/media-gallery',
        'traits': [
          {
            'ref': 'MediaGalleryRender',
            'refId': asTraitId('trt_01KXG04STSKR7RB9HG6V6H19XZ'),
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiMediaGalleryMediaGalleryOrbital. */
export const StdUiMediaGalleryMediaGalleryOrbitalManifest = {
  organism: 'ui-media-gallery',
  orbitalName: 'MediaGalleryOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'MediaGalleryRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiMediaGalleryMediaGalleryOrbitalParams keys. */
export function isStdUiMediaGalleryMediaGalleryOrbitalParams(p: object): p is StdUiMediaGalleryMediaGalleryOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiMediaGalleryMediaGalleryOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiMediaGalleryMediaGalleryOrbitalManifest.traitNames,
      ...StdUiMediaGalleryMediaGalleryOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-media-gallery — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiMediaGalleryParams {
  MediaGallery?: StdUiMediaGalleryMediaGalleryOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiMediaGallery(params: StdUiMediaGalleryParams = {}): OrbitalDefinition[] {
  return [
    stdUiMediaGalleryMediaGalleryOrbital(params.MediaGallery ?? {}),
  ];
}
