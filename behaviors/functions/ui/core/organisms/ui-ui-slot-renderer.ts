/**
 * ui-ui-slot-renderer — UiSlotRenderer — UI factory (1:1 with the @almadar/ui ui-slot-renderer pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-ui-slot-renderer.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-ui-slot-renderer
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-ui-slot-renderer';
const ALIAS = 'UiUiSlotRenderer';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiUiSlotRendererEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiUiSlotRendererConfig {
  /** Default: `""` */
  className?: string;
  error?: EntityRow;
  /** Default: `"fixed"` */
  hudMode?: 'fixed' | 'inline';
  /** Default: `false` */
  includeFloating?: boolean;
  /** Default: `false` */
  includeHud?: boolean;
  /** Default: `false` */
  isLoading?: boolean;
  /** Default: `{"enabled":false}` */
  suspense?: EntityRow;
}

type _StdUiUiSlotRendererEntityName = 'UiSlotRendererItem';
type _StdUiUiSlotRendererListenTraitName = 'UiSlotRendererRender';

/**
 * Tunable params for the UiSlotRendererOrbital orbital.
 *
 * Canonical entity: UiSlotRendererItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiUiSlotRendererUiSlotRendererOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'UiSlotRendererRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait UiSlotRendererOrbital's `uses[]` exports. */
type _StdUiUiSlotRendererUiSlotRendererOrbitalUsesRef = never;

/** Per-orbital factory: builds the UiSlotRendererOrbital orbital with consumer params. */
export function stdUiUiSlotRendererUiSlotRendererOrbital(params: StdUiUiSlotRendererUiSlotRendererOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'UiSlotRendererOrbital',
    uses: [],
    entity: {
      name: 'UiSlotRendererItem',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'required': true,
            'type': 'string',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'interaction',
        'config': {
          'className': {
            'default': '',
            'description': 'Additional class name for the container',
            'label': 'Class Name',
            'tier': 'presentation',
            'type': 'string',
          },
          'error': {
            'description': 'Error state',
            'label': 'Error',
            'properties': {
              'code': {
                'name': 'code',
                'required': false,
                'type': 'string',
              },
              'message': {
                'name': 'message',
                'required': true,
                'type': 'string',
              },
              'name': {
                'name': 'name',
                'required': false,
                'type': 'string',
              },
              'stack': {
                'name': 'stack',
                'required': false,
                'type': 'string',
              },
            },
            'tier': 'presentation',
            'type': 'UiSlotRendererError',
          },
          'hudMode': {
            'default': 'fixed',
            'description': 'HUD positioning mode: \'fixed\' (default, viewport-relative) or \'inline\' (container-relative, uses sticky)',
            'label': 'Hud Mode',
            'tier': 'presentation',
            'type': 'string',
            'values': [
              'fixed',
              'inline',
            ],
          },
          'includeFloating': {
            'default': false,
            'description': 'Include floating slot',
            'label': 'Include Floating',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'includeHud': {
            'default': false,
            'description': 'Include HUD slots',
            'label': 'Include Hud',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'isLoading': {
            'default': false,
            'description': 'Loading state indicator',
            'label': 'Is Loading',
            'tier': 'presentation',
            'type': 'boolean',
          },
          'suspense': {
            'default': {
              'enabled': false,
            },
            'description': 'Enable Suspense boundaries around each slot. When true, each inline slot is wrapped in `<ErrorBoundary><Suspense>` with Skeleton fallbacks. Opt-in — existing isLoading prop pattern still works.',
            'label': 'Suspense',
            'properties': {
              'enabled': {
                'name': 'enabled',
                'required': true,
                'type': 'boolean',
              },
              'slotFallbacks': {
                'items': {
                  'type': 'string',
                },
                'name': 'slotFallbacks',
                'required': false,
                'type': 'object',
              },
            },
            'tier': 'presentation',
            'type': 'UiSlotRendererSuspense',
          },
        },
        'entityContract': {
          'provides': [],
          'requires': [],
        },
        'entityRebindable': true,
        'linkedEntity': 'UiSlotRendererItem',
        'name': 'UiSlotRendererRender',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'fetch',
                  ('UiSlotRendererItem' satisfies _StdUiUiSlotRendererEntityName),
                  {},
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'className': '@config.className',
                    'entity': 'UiSlotRendererItem',
                    'error': '@config.error',
                    'hudMode': '@config.hudMode',
                    'includeFloating': '@config.includeFloating',
                    'includeHud': '@config.includeHud',
                    'isLoading': '@config.isLoading',
                    'suspense': '@config.suspense',
                    'type': 'ui-slot-renderer',
                  },
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'UiSlotRendererPage',
        'path': '/ui-slot-renderer',
        'traits': [
          {
            'ref': 'UiSlotRendererRender',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiUiSlotRendererUiSlotRendererOrbital. */
export const StdUiUiSlotRendererUiSlotRendererOrbitalManifest = {
  organism: 'ui-ui-slot-renderer',
  orbitalName: 'UiSlotRendererOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'UiSlotRendererRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiUiSlotRendererUiSlotRendererOrbitalParams keys. */
export function isStdUiUiSlotRendererUiSlotRendererOrbitalParams(p: object): p is StdUiUiSlotRendererUiSlotRendererOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiUiSlotRendererUiSlotRendererOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiUiSlotRendererUiSlotRendererOrbitalManifest.traitNames,
      ...StdUiUiSlotRendererUiSlotRendererOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-ui-slot-renderer — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiUiSlotRendererParams {
  UiSlotRenderer?: StdUiUiSlotRendererUiSlotRendererOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiUiSlotRenderer(params: StdUiUiSlotRendererParams = {}): OrbitalDefinition[] {
  return [
    stdUiUiSlotRendererUiSlotRendererOrbital(params.UiSlotRenderer ?? {}),
  ];
}
