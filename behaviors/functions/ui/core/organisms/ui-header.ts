/**
 * ui-header — Header — UI factory (1:1 with the @almadar/ui header pattern).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/ui-header.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=ui-header
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/ui-header';
const ALIAS = 'UiHeader';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdUiHeaderEventKey = 'INIT' | 'LOGO_CLICK' | 'MENU_TOGGLE' | 'SEARCH' | 'USER_CLICK';

/**
 * Payload shape for the `MENU_TOGGLE` event.
 */
export interface StdUiHeaderMenuTogglePayload {
  id?: string;
}

/**
 * Payload shape for the `SEARCH` event.
 */
export interface StdUiHeaderSearchPayload {
  value?: string;
}

/**
 * Payload shape for the `USER_CLICK` event.
 */
export interface StdUiHeaderUserClickPayload {
  id?: string;
}

/**
 * Payload shape for the `LOGO_CLICK` event.
 */
export interface StdUiHeaderLogoClickPayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdUiHeaderConfig {
  /** Default: `"KFlow"` */
  brandName?: string;
  /** Default: `false` */
  isMenuOpen?: boolean;
  /** Default: `""` */
  className?: string;
  /** Default: `"mobile"` */
  variant?: 'mobile' | 'desktop';
  /** Default: `""` */
  logoSrc?: string;
  /** Default: `[]` */
  navigationItems?: EntityRow[];
  /** Default: `""` */
  searchPlaceholder?: string;
  actions?: unknown;
  error?: EntityRow;
  /** Default: `"compact-bar"` */
  look?: 'hero' | 'compact-bar' | 'breadcrumb' | 'contextual' | 'editorial-banner';
  /** Default: `true` */
  showMenuToggle?: boolean;
  /** Default: `""` */
  userName?: string;
  logo?: unknown;
  /** Default: `true` */
  sticky?: boolean;
  /** Default: `false` */
  showSearch?: boolean;
  /** Default: `false` */
  isLoading?: boolean;
  userAvatar?: EntityRow;
}

/**
 * Tunable params for the HeaderOrbital orbital.
 *
 * Canonical entity: HeaderItem — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdUiHeaderHeaderOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'HeaderRender',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the HeaderOrbital orbital with consumer params. */
export function stdUiHeaderHeaderOrbital(params: StdUiHeaderHeaderOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'HeaderItem';
  const built = makeOrbitalWithUses({
    name: 'HeaderOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'name': 'HeaderRender',
        'entityRebindable': true,
        'entityContract': {
          'requires': [],
          'provides': [],
        },
        'category': 'interaction',
        'linkedEntity': 'HeaderItem',
        'emits': [
          {
            'event': 'MENU_TOGGLE',
            'description': 'Menu toggle callback',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'SEARCH',
            'description': 'Search callback',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'value',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'USER_CLICK',
            'description': 'Callback when user avatar is clicked',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'LOGO_CLICK',
            'description': 'Callback when logo/brand is clicked',
            'tier': 'essential',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'MENU_TOGGLE',
              'name': 'Menu Toggle',
              'description': 'Menu toggle callback',
              'tier': 'essential',
            },
            {
              'key': 'SEARCH',
              'name': 'Search',
              'description': 'Search callback',
              'tier': 'essential',
            },
            {
              'key': 'USER_CLICK',
              'name': 'User Click',
              'description': 'Callback when user avatar is clicked',
              'tier': 'essential',
            },
            {
              'key': 'LOGO_CLICK',
              'name': 'Logo Click',
              'description': 'Callback when logo/brand is clicked',
              'tier': 'essential',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'HeaderItem',
                  {},
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'brandName': '@config.brandName',
                    'navigationItems': '@config.navigationItems',
                    'look': '@config.look',
                    'isLoading': '@config.isLoading',
                    'logo': '@config.logo',
                    'searchPlaceholder': '@config.searchPlaceholder',
                    'userName': '@config.userName',
                    'showMenuToggle': '@config.showMenuToggle',
                    'isMenuOpen': '@config.isMenuOpen',
                    'error': '@config.error',
                    'logoSrc': '@config.logoSrc',
                    'showSearch': '@config.showSearch',
                    'variant': '@config.variant',
                    'onLogoClick': 'LOGO_CLICK',
                    'actions': '@config.actions',
                    'userAvatar': '@config.userAvatar',
                    'onUserClick': 'USER_CLICK',
                    'type': 'header',
                    'onMenuToggle': 'MENU_TOGGLE',
                    'entity': 'HeaderItem',
                    'sticky': '@config.sticky',
                    'className': '@config.className',
                    'onSearch': 'SEARCH',
                  },
                ],
              ],
            },
          ],
        },
        'config': {
          'brandName': {
            'type': 'string',
            'default': 'KFlow',
            'label': 'Brand Name',
            'description': 'Brand/App name',
            'tier': 'presentation',
          },
          'isMenuOpen': {
            'type': 'boolean',
            'default': false,
            'label': 'Is Menu Open',
            'description': 'Is menu open (for toggle icon)',
            'tier': 'presentation',
          },
          'className': {
            'type': 'string',
            'default': '',
            'label': 'Class Name',
            'description': 'Additional CSS classes',
            'tier': 'presentation',
          },
          'variant': {
            'type': 'string',
            'default': 'mobile',
            'label': 'Variant',
            'description': 'Variant - mobile shows menu toggle, desktop shows full nav',
            'tier': 'presentation',
            'values': [
              'mobile',
              'desktop',
            ],
          },
          'logoSrc': {
            'type': 'string',
            'default': '',
            'label': 'Logo Src',
            'description': 'Logo image source',
            'tier': 'presentation',
          },
          'navigationItems': {
            'type': '[HeaderNavigationItemsItem]',
            'default': [],
            'label': 'Navigation Items',
            'description': 'Navigation items (for desktop header variant)',
            'tier': 'presentation',
            'items': {
              'type': 'object',
              'properties': {
                'href': {
                  'name': 'href',
                  'type': 'string',
                  'required': false,
                },
                'badge': {
                  'name': 'badge',
                  'type': 'string',
                  'required': false,
                },
                'label': {
                  'name': 'label',
                  'type': 'string',
                  'required': true,
                },
                'active': {
                  'name': 'active',
                  'type': 'boolean',
                  'required': false,
                },
                'icon': {
                  'name': 'icon',
                  'type': 'string',
                  'required': false,
                },
              },
            },
          },
          'searchPlaceholder': {
            'type': 'string',
            'default': '',
            'label': 'Search Placeholder',
            'description': 'Search placeholder',
            'tier': 'presentation',
          },
          'actions': {
            'type': 'node',
            'label': 'Actions',
            'description': 'Action buttons (right side)',
            'tier': 'presentation',
          },
          'error': {
            'type': 'HeaderError',
            'label': 'Error',
            'description': 'Error state (closed circuit)',
            'tier': 'presentation',
            'properties': {
              'code': {
                'name': 'code',
                'type': 'string',
                'required': false,
              },
              'name': {
                'name': 'name',
                'type': 'string',
                'required': false,
              },
              'message': {
                'name': 'message',
                'type': 'string',
                'required': true,
              },
              'stack': {
                'name': 'stack',
                'type': 'string',
                'required': false,
              },
            },
          },
          'look': {
            'type': 'string',
            'default': 'compact-bar',
            'label': 'Look',
            'description': 'Layer 2 visual treatment — orthogonal to the structural variant (which currently flips mobile vs desktop).',
            'tier': 'presentation',
            'values': [
              'hero',
              'compact-bar',
              'breadcrumb',
              'contextual',
              'editorial-banner',
            ],
          },
          'showMenuToggle': {
            'type': 'boolean',
            'default': true,
            'label': 'Show Menu Toggle',
            'description': 'Show menu toggle button',
            'tier': 'presentation',
          },
          'userName': {
            'type': 'string',
            'default': '',
            'label': 'User Name',
            'description': 'User name (display name or email)',
            'tier': 'presentation',
          },
          'logo': {
            'type': 'node',
            'label': 'Logo',
            'description': 'Logo/Brand content',
            'tier': 'presentation',
          },
          'sticky': {
            'type': 'boolean',
            'default': true,
            'label': 'Sticky',
            'description': 'Sticky header',
            'tier': 'presentation',
          },
          'showSearch': {
            'type': 'boolean',
            'default': false,
            'label': 'Show Search',
            'description': 'Show search input',
            'tier': 'presentation',
          },
          'isLoading': {
            'type': 'boolean',
            'default': false,
            'label': 'Is Loading',
            'description': 'Loading state indicator (closed circuit)',
            'tier': 'presentation',
          },
          'userAvatar': {
            'type': 'HeaderUserAvatar',
            'label': 'User Avatar',
            'description': 'User avatar configuration',
            'tier': 'presentation',
            'properties': {
              'alt': {
                'name': 'alt',
                'type': 'string',
                'required': false,
              },
              'src': {
                'name': 'src',
                'type': 'string',
                'required': false,
              },
              'initials': {
                'name': 'initials',
                'type': 'string',
                'required': false,
              },
            },
          },
        },
        'scope': 'instance',
      } as never, 'HeaderItem', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'HeaderPage',
        'path': '/header',
        'traits': [
          {
            'ref': 'HeaderRender',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdUiHeaderHeaderOrbital. */
export const StdUiHeaderHeaderOrbitalManifest = {
  organism: 'ui-header',
  orbitalName: 'HeaderOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'HeaderRender',
  ] as const,
};

/** Typed guard — runtime validates StdUiHeaderHeaderOrbitalParams keys. */
export function isStdUiHeaderHeaderOrbitalParams(p: object): p is StdUiHeaderHeaderOrbitalParams {
  type _OverrideRecord = NonNullable<StdUiHeaderHeaderOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdUiHeaderHeaderOrbitalManifest.traitNames,
      ...StdUiHeaderHeaderOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for ui-header — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdUiHeaderParams {
  Header?: StdUiHeaderHeaderOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdUiHeader(params: StdUiHeaderParams = {}): OrbitalDefinition[] {
  return [
    stdUiHeaderHeaderOrbital(params.Header ?? {}),
  ];
}
