/**
 * std-step-flow — std-step-flow — generic N-step sequential decision chain (Stripe Connect / TurboTax / Vercel deploy style). `config.steps` is an array of step definitions `{ key, label, description, icon, allowedRoles?: [string] }`; the trait projects the current step into a prominent body card and a `wizard-progress` stepper bar at the top. When a step declares `allowedRoles`, only users whose `@user.role` is in that list can advance through it. One primary button ADVANCEs along the chain; at the last step the same button finalizes (label/icon/variant swap via entity binding, state routed via guard). REJECT/ESCALATE terminate. Topology = loading / running / approved / rejected / escalated / error.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-step-flow.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-step-flow
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-step-flow';
const ALIAS = 'StepFlow';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdStepFlowEventKey = 'ADVANCE' | 'BACK' | 'ESCALATE' | 'INIT' | 'REJECT' | 'RESTART' | 'StepItemsLoadFailed' | 'StepItemsLoaded' | 'StepItemsSaveFailed' | 'StepItemsSaved';

/**
 * Payload shape for the `ADVANCE` event.
 */
export interface StdStepFlowAdvancePayload {
  id?: string;
  stepKey?: string;
}

/**
 * Payload shape for the `BACK` event.
 */
export interface StdStepFlowBackPayload {
  id?: string;
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdStepFlowRejectPayload {
  id?: string;
  reason?: string;
}

/**
 * Payload shape for the `ESCALATE` event.
 */
export interface StdStepFlowEscalatePayload {
  id?: string;
  reason?: string;
}

/**
 * Payload shape for the `RESTART` event.
 */
export interface StdStepFlowRestartPayload {
  id?: string;
}

/**
 * Payload shape for the `StepItemsLoaded` event.
 */
export interface StdStepFlowStepItemsLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `StepItemsLoadFailed` event.
 */
export interface StdStepFlowStepItemsLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdStepFlowConfig {
  /** Default: `{"children":[{"align":"center","children":[{"name":"shield-check","type":"icon"},{"content":"@config.title","type":"typography","variant":"h3"}],"direction":"horizontal","gap":"sm","type":"stack"},{"allowNavigation":false,"currentStep":"@entity.currentStepIndex","steps":"@entity.wizardSteps","type":"wizard-progress"},{"children":[{"children":[{"align":"center","children":[{"name":"@entity.currentStepIcon","size":"lg","type":"icon"},{"children":[{"content":"@entity.currentStepLabel","type":"typography","variant":"h2"},{"color":"muted","content":"@entity.currentStepDescription","type":"typography","variant":"body"}],"direction":"vertical","gap":"xs","type":"stack"}],"direction":"horizontal","gap":"sm","type":"stack"}],"direction":"vertical","gap":"md","type":"stack"}],"look":"@config.cardLook","type":"card"},{"align":"center","children":[{"action":"BACK","actionPayload":{"id":"@entity.id"},"disabled":"@entity.isFirstStep","icon":"chevron-left","label":"Back","type":"button","variant":"ghost"},{"action":"REJECT","actionPayload":{"id":"@entity.id"},"icon":"x","label":"Reject","type":"button","variant":"ghost"},{"action":"ESCALATE","actionPayload":{"id":"@entity.id"},"icon":"alert-triangle","label":"Escalate","type":"button","variant":"ghost"},{"action":"ADVANCE","actionPayload":{"id":"@entity.id"},"icon":"@entity.primaryActionIcon","label":"@entity.primaryActionLabel","type":"button","variant":"primary"}],"direction":"horizontal","gap":"sm","type":"stack"}],"direction":"vertical","gap":"lg","type":"stack"}` */
  bodyContent?: unknown;
  /** Default: `"elevated"` */
  cardLook?: 'elevated' | 'flat-bordered' | 'borderless-divider' | 'ticket' | 'invoice' | 'chip' | 'tile-image-first';
  /** Default: `[{"description":"Initial review by direct manager","icon":"user","key":"manager","label":"Manager Review"},{"description":"Department director sign-off","icon":"users","key":"director","label":"Director Approval"},{"description":"Final executive approval","icon":"shield","key":"executive","label":"Executive Sign-off"}]` */
  steps?: EntityRow[];
  /** Default: `"Review"` */
  title?: string;
}

/**
 * Params for the std-step-flow descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdStepFlowParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdStepFlowEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdStepFlowConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `StepFlow.traits.StepFlowReview`. */
export function stdStepFlowTrait(params: StdStepFlowParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StepFlowReview`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `StepFlow.pages.StepFlowPage`. */
export function stdStepFlowPage(params: StdStepFlowParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.StepFlowPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdStepFlow(params: StdStepFlowParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'StepFlowOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdStepFlowTrait(params),
    ],
    pages: [
      stdStepFlowPage(params),
    ],
  });
}
