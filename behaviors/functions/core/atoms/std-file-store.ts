/**
 * std-file-store — std-file-store — the generic file/page tree primitive. A file-tree + config-driven data table over a hierarchical collection; opens a row in `viewing_single` with config-driven detail actions. Rebindable: pass `fields`/`itemActions`/`detailActions`/`tree` to retarget it at any hierarchical entity (files, wiki pages, note trees). Folds in the former std-page-tree via `reorderable` + a page field/action set.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-file-store.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-file-store
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-file-store';
const ALIAS = 'FileStore';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdFileStoreEventKey = 'ARCHIVE_FILE' | 'CLOSE_VIEW' | 'DOWNLOAD_FILE' | 'INIT' | 'OPEN_FILE' | 'REORDER' | 'StoredFileLoadFailed' | 'StoredFileLoaded';

/**
 * Payload shape for the `OPEN_FILE` event.
 */
export interface StdFileStoreOpenFilePayload {
  id: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `DOWNLOAD_FILE` event.
 */
export interface StdFileStoreDownloadFilePayload {
  id: string;
}

/**
 * Payload shape for the `ARCHIVE_FILE` event.
 */
export interface StdFileStoreArchiveFilePayload {
  id: string;
}

/**
 * Payload shape for the `REORDER` event.
 */
export interface StdFileStoreReorderPayload {
  id: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `StoredFileLoaded` event.
 */
export interface StdFileStoreStoredFileLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `StoredFileLoadFailed` event.
 */
export interface StdFileStoreStoredFileLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdFileStoreConfig {
  /** Default: `[{"event":"DOWNLOAD_FILE","label":"Download","variant":"primary"},{"event":"ARCHIVE_FILE","label":"Archive","variant":"danger"}]` */
  detailActions?: EntityRow[];
  /** Default: `[{"label":"Name","name":"name","variant":"caption"},{"label":"Folder","name":"folder","variant":"caption"},{"label":"Type","name":"mimeType","variant":"badge"},{"label":"Size","name":"sizeBytes","variant":"caption"},{"label":"Uploaded","name":"uploadedAt","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `"folder"` */
  headerIcon?: string;
  /** Default: `[{"event":"OPEN_FILE","icon":"arrow-right","label":"Open","variant":"primary"},{"event":"ARCHIVE_FILE","label":"Archive","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `false` */
  reorderable?: boolean;
  /** Default: `"dense"` */
  tableLook?: 'dense' | 'spacious' | 'striped' | 'borderless' | 'card-rows';
  /** Default: `"Files"` */
  title?: string;
  /** Default: `[{"children":[{"name":"Active.pdf","path":"Active.pdf","type":"file"},{"name":"Archived.pdf","path":"Archived.pdf","type":"file"}],"name":"Contracts","path":"Contracts","type":"dir"},{"children":[{"name":"HR.docx","path":"Policies/HR.docx","type":"file"},{"name":"Security.md","path":"Policies/Security.md","type":"file"}],"name":"Policies","path":"Policies","type":"dir"},{"children":[{"name":"Invoice.docx","path":"Templates/Invoice.docx","type":"file"},{"name":"NDA.pdf","path":"Templates/NDA.pdf","type":"file"}],"name":"Templates","path":"Templates","type":"dir"}]` */
  tree?: EntityRow[];
}

/**
 * Params for the std-file-store descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdFileStoreParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdFileStoreEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdFileStoreConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `FileStore.traits.StoredFileStore`. */
export function stdFileStoreTrait(params: StdFileStoreParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StoredFileStore`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `FileStore.pages.StoredFilePage`. */
export function stdFileStorePage(params: StdFileStoreParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.StoredFilePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdFileStore(params: StdFileStoreParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'StoredFileOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdFileStoreTrait(params),
    ],
    pages: [
      stdFileStorePage(params),
    ],
  });
}
