/**
 * std-browse — std-browse as a Function — single primitive: data-grid. Owns the fetch + REFETCH (query/filter/page) state machine. Consumers configure fields/imageField/itemActions/cols/gap/variant; the atom renders the grid. Each `fields[]` entry accepts `{ name, label, variant }` plus optional `referenceEntity` + `displayField` to render a foreign-key column as the joined entity's display value (data-grid resolves @list[referenceEntity] at render time when both keys are present).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-browse.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-browse
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-browse';
const ALIAS = 'Browse';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBrowseEventKey = 'BrowseItemLoadFailed' | 'BrowseItemLoaded' | 'DELETE' | 'EDIT' | 'INIT' | 'REFETCH_FILTER' | 'REFETCH_PAGE' | 'REFETCH_QUERY' | 'VIEW';

/**
 * Payload shape for the `BrowseItemLoaded` event.
 */
export interface StdBrowseBrowseItemLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `BrowseItemLoadFailed` event.
 */
export interface StdBrowseBrowseItemLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `VIEW` event.
 */
export interface StdBrowseViewPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `EDIT` event.
 */
export interface StdBrowseEditPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `DELETE` event.
 */
export interface StdBrowseDeletePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBrowseConfig {
  /** Default: `"@item.name"` */
  titleBinding?: string;
  /** Default: `"@item.status"` */
  badgeBinding?: string;
  /** Default: `[{"weight":"medium","key":"name","field":"name","header":"Name","align":"left"},{"align":"left","key":"description","header":"Detail","field":"description"},{"key":"status","format":"badge","align":"center","field":"status","header":"Status"}]` */
  columns?: EntityRow[];
  /** Default: `[]` */
  itemActions?: EntityRow[];
  /** Default: `10` */
  pageSize?: number;
  /** Default: `"@item.priority"` */
  priorityBinding?: string;
  /** Default: `"@item.category"` */
  categoryBinding?: string;
  /** Default: `"@item.price"` */
  priceBinding?: string;
  /** Default: `[]` */
  filters?: EntityRow[];
  /** Default: `99` */
  maxInlineActions?: number;
  /** Default: `1` */
  cols?: number;
  /** Default: `10` */
  displayPageSize?: number;
  /** Default: `"data-grid"` */
  viewPattern?: unknown;
  /** Default: `"Search…"` */
  searchPlaceholder?: string;
  /** Default: `"@item.createdAt"` */
  metaBinding?: string;
  /** Default: `"md"` */
  gap?: string;
  /** Default: `{"type":"stack","direction":"vertical","children":[{"imageField":"@config.imageField","entity":"@payload.data","fields":"@config.fields","itemActions":"@config.itemActions","gap":"@config.gap","pageSize":"@config.displayPageSize","type":"data-grid","cols":"@config.cols"}]}` */
  bodyContent?: unknown;
  /** Default: `"@item.thumbnailLabel"` */
  imageLabelBinding?: string;
  /** Default: `"@item.assignee"` */
  assigneeBinding?: string;
  /** Default: `"@item.description"` */
  descriptionBinding?: string;
  /** Default: `[{"variant":"h4","label":"Name","name":"name"},{"name":"description","label":"Description","variant":"caption"},{"variant":"badge","label":"Status","name":"status"}]` */
  fields?: EntityRow[];
  /** Default: `""` */
  imageField?: string;
}

/**
 * Params for the std-browse descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBrowseParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBrowseEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBrowseConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Browse.traits.BrowseItemBrowse`. */
export function stdBrowseTrait(params: StdBrowseParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BrowseItemBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Browse.pages.BrowseItemPage`. */
export function stdBrowsePage(params: StdBrowseParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BrowseItemPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBrowse(params: StdBrowseParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'BrowseItemOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBrowseTrait(params),
    ],
    pages: [
      stdBrowsePage(params),
    ],
  });
}
