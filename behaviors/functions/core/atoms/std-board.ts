/**
 * std-board — std-board — generic N-column board primitive. `config.columns` is an array of column definitions ({ key, label, icon, variant }); the trait `array/map`s them on `BoardItemsLoaded` into `@entity.boards` where each board carries its filtered items + a count. The render is a `data-grid` over `@entity.boards` (one card per column) whose `renderItem` lambda nests a `data-list` over `@col.items` (the cards in that stage). N is config-driven — pass any number of columns; the outer `data-grid`'s `cols` is bound to `@config.gridCols` so the grid wraps if you exceed it. Topology = loading / viewing_board / viewing_card / adding / error — distinct from the cookie-cutter `loading/browsing/viewing_single/error` CRUD shape. App-level atoms (e.g. std-pipeline) compose std-board via `uses` and add a sibling persistor + extended source entity.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-board.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-board
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-board';
const ALIAS = 'Board';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBoardEventKey = 'ADD_CARD' | 'BoardItemsLoadFailed' | 'BoardItemsLoaded' | 'BoardItemsSaveFailed' | 'BoardItemsSaved' | 'CANCEL_ADD' | 'CLOSE_CARD' | 'DELETE_CARD' | 'INIT' | 'MOVE_CARD' | 'OPEN_CARD' | 'REORDER_CARD' | 'REORDER_POSITION' | 'SAVE_CARD';

/**
 * Payload shape for the `OPEN_CARD` event.
 */
export interface StdBoardOpenCardPayload {
  id: string;
  row: EntityRow;
}

/**
 * Payload shape for the `SAVE_CARD` event.
 */
export interface StdBoardSaveCardPayload {
  data: EntityRow;
}

/**
 * Payload shape for the `MOVE_CARD` event.
 */
export interface StdBoardMoveCardPayload {
  id: string;
  sourceGroup?: string;
  targetGroup: string;
  newIndex?: number;
}

/**
 * Payload shape for the `REORDER_CARD` event.
 */
export interface StdBoardReorderCardPayload {
  id: string;
  oldIndex?: number;
  newIndex?: number;
}

/**
 * Payload shape for the `DELETE_CARD` event.
 */
export interface StdBoardDeleteCardPayload {
  id: string;
}

/**
 * Payload shape for the `BoardItemsLoaded` event.
 */
export interface StdBoardBoardItemsLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `BoardItemsLoadFailed` event.
 */
export interface StdBoardBoardItemsLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `BoardItemsSaved` event.
 */
export interface StdBoardBoardItemsSavedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `BoardItemsSaveFailed` event.
 */
export interface StdBoardBoardItemsSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBoardConfig {
  /** Default: `"elevated"` */
  cardLook?: 'elevated' | 'flat-bordered' | 'borderless-divider' | 'ticket' | 'invoice' | 'chip' | 'tile-image-first';
  /** Default: `"Board"` */
  title?: string;
  /** Default: `[{"icon":"circle","variant":"default","key":"todo","label":"To Do"},{"key":"doing","icon":"circle-dot","label":"In Progress","variant":"primary"},{"key":"done","label":"Done","variant":"success","icon":"check-circle"}]` */
  columns?: EntityRow[];
  /** Default: `3` */
  gridCols?: number;
  /** Default: `["title","description","stage","notes"]` */
  formFields?: string[];
  /** Default: `{"gap":"md","type":"stack","direction":"vertical","children":[{"direction":"horizontal","type":"stack","gap":"sm","children":[{"name":"kanban-square","type":"icon"},{"type":"typography","content":"@config.title","variant":"h3"}],"align":"center"},{"type":"divider"},{"type":"data-grid","cols":"@config.gridCols","renderItem":["fn","col",{"look":"@config.cardLook","type":"card","children":[{"type":"stack","direction":"vertical","children":[{"children":[{"type":"icon","name":"@col.icon"},{"type":"typography","content":"@col.label","variant":"h4"},{"variant":"@col.variant","type":"badge","label":"@col.count"}],"gap":"xs","direction":"horizontal","type":"stack","align":"center"},{"sortable":true,"fields":[],"reorderEvent":"REORDER_CARD","positionEvent":"REORDER_POSITION","type":"data-list","entity":"@col.items","dropEvent":"MOVE_CARD","dragGroup":"@col.key","gap":"sm","accepts":"*","renderItem":["fn","item",{"children":[{"gap":"xs","direction":"vertical","type":"stack","children":[{"content":"@item.title","type":"typography","variant":"h4"},{"color":"muted","type":"typography","variant":"caption","content":"@item.description"},{"direction":"horizontal","gap":"xs","align":"center","children":[{"actionPayload":{"row":"@item","id":"@item.id"},"variant":"ghost","icon":"arrow-right","label":"Open","action":"OPEN_CARD","type":"button"}],"type":"stack"}]}],"look":"@config.cardLook","type":"card"}]}],"gap":"sm"}]}],"dndRoot":true,"gap":"md","entity":"@entity.boards","fields":[]},{"label":"Add item","icon":"plus","variant":"primary","type":"floating-action-button","action":"ADD_CARD"}]}` */
  bodyContent?: unknown;
}

/**
 * Params for the std-board descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBoardParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBoardEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBoardConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Board.traits.BoardItemBoard`. */
export function stdBoardTrait(params: StdBoardParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BoardItemBoard`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Board.pages.BoardPage`. */
export function stdBoardPage(params: StdBoardParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BoardPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBoard(params: StdBoardParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'BoardOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBoardTrait(params),
    ],
    pages: [
      stdBoardPage(params),
    ],
  });
}
