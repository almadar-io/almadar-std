/**
 * std-thread — std-thread — forum-style nested replies with collapse + flat-mode toggle. Owns ThreadPost (persistent). flat:true covers 1:1 linear inquiry threads; flat:false renders forum-style nested. INIT fetches; REPLY persists a child post under @payload.parentId. Layer 3 variants override `bodyContent` to deliver chat-bubble, email-threaded, or linear-comments UX while inheriting the same trait, state machine, emits, and listens.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-thread.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-thread
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-thread';
const ALIAS = 'Thread';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdThreadEventKey = 'CANCEL_REPLY' | 'EDIT_REPLY' | 'INIT' | 'REPLY' | 'SELECT' | 'SUBMIT_REPLY' | 'ThreadPostCreateFailed' | 'ThreadPostCreated' | 'ThreadPostLoadFailed' | 'ThreadPostLoaded';

/**
 * Payload shape for the `SELECT` event.
 */
export interface StdThreadSelectPayload {
  id?: string;
  row: EntityRow;
}

/**
 * Payload shape for the `REPLY` event.
 */
export interface StdThreadReplyPayload {
  parentId?: string;
  parentNodeId?: string;
  content?: string;
}

/**
 * Payload shape for the `EDIT_REPLY` event.
 */
export interface StdThreadEditReplyPayload {
  value: string;
}

/**
 * Payload shape for the `SUBMIT_REPLY` event.
 */
export interface StdThreadSubmitReplyPayload {
  data: EntityRow;
}

/**
 * Payload shape for the `ThreadPostLoaded` event.
 */
export interface StdThreadThreadPostLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ThreadPostLoadFailed` event.
 */
export interface StdThreadThreadPostLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `ThreadPostCreated` event.
 */
export interface StdThreadThreadPostCreatedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `ThreadPostCreateFailed` event.
 */
export interface StdThreadThreadPostCreateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdThreadConfig {
  /** Default: `{"children":[{"entity":"@payload.data","fields":[{"label":"Author","name":"authorName","variant":"h4"},{"label":"","name":"content","variant":"body"},{"label":"Votes","name":"voteCount","variant":"caption"}],"gap":"sm","itemActions":[{"event":"REPLY","icon":"message-square","label":"Reply"}],"look":"@config.tableLook","type":"data-list","variant":"card"}],"direction":"vertical","gap":"md","type":"stack"}` */
  bodyContent?: unknown;
  /** Default: `"@entity.focusedRow.authorName"` */
  detailAuthorBinding?: string;
  /** Default: `"@entity.focusedRow.content"` */
  detailContentBinding?: string;
  /** Default: `"@entity.focusedRow.createdAt"` */
  detailDateBinding?: string;
  /** Default: `"@entity.focusedRow.fromAddress"` */
  detailFromBinding?: string;
  /** Default: `"@entity.focusedRow.subject"` */
  detailSubjectBinding?: string;
  /** Default: `"@entity.focusedRow.toAddress"` */
  detailToBinding?: string;
  /** Default: `"@item.authorName"` */
  emailAuthorBinding?: string;
  /** Default: `"@item.preview"` */
  emailPreviewBinding?: string;
  /** Default: `"@item.subject"` */
  emailSubjectBinding?: string;
  /** Default: `false` */
  flat?: boolean;
  /** Default: `"Conversation"` */
  recipientName?: string;
  /** Default: `"@reply.authorName"` */
  replyAuthorBinding?: string;
  /** Default: `"@reply.content"` */
  replyContentBinding?: string;
  /** Default: `"@reply.createdAt"` */
  replyDateBinding?: string;
  /** Default: `"@reply.fromAddress"` */
  replyFromBinding?: string;
  /** Default: `"Discussion"` */
  sectionTitle?: string;
  /** Default: `"dense"` */
  tableLook?: 'dense' | 'spacious' | 'striped' | 'borderless' | 'card-rows';
  /** Default: `""` */
  threadRootId?: string;
}

/**
 * Params for the std-thread descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdThreadParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdThreadEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdThreadConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Thread.traits.ThreadPostBrowse`. */
export function stdThreadTrait(params: StdThreadParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ThreadPostBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Thread.pages.ThreadPostBrowsePage`. */
export function stdThreadPage(params: StdThreadParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ThreadPostBrowsePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdThread(params: StdThreadParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ThreadPostOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdThreadTrait(params),
    ],
    pages: [
      stdThreadPage(params),
    ],
  });
}
