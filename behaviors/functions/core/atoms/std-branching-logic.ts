/**
 * std-branching-logic — std-branching-logic — survey skip-logic rules. Owns a BranchingRule entity and a manage trait that fetches rules on INIT, lists them via the branching-logic-builder pattern, and persists ADD_RULE / UPDATE_RULE / DELETE_RULE.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-branching-logic.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-branching-logic
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-branching-logic';
const ALIAS = 'BranchingLogic';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBranchingLogicEventKey = 'ADD_RULE' | 'BranchingRuleAdded' | 'BranchingRuleDeleted' | 'BranchingRuleLoadFailed' | 'BranchingRuleLoaded' | 'BranchingRuleUpdated' | 'CANCEL' | 'DELETE_RULE' | 'EDIT' | 'EDIT_OPERATOR' | 'EDIT_SOURCE' | 'EDIT_TARGET' | 'EDIT_VALUE' | 'INIT' | 'UPDATE_RULE';

/**
 * Payload shape for the `ADD_RULE` event.
 */
export interface StdBranchingLogicAddRulePayload {
  data: EntityRow;
}

/**
 * Payload shape for the `UPDATE_RULE` event.
 */
export interface StdBranchingLogicUpdateRulePayload {
  ruleId: string;
  row?: {
    id: string;
    surveyId: string;
    sourceQuestionId: string;
    operator?: string;
    value?: string;
    targetQuestionId: string;
    priority?: number;
  };
  data: EntityRow;
}

/**
 * Payload shape for the `DELETE_RULE` event.
 */
export interface StdBranchingLogicDeleteRulePayload {
  ruleId: string;
  row?: {
    id: string;
    surveyId: string;
    sourceQuestionId: string;
    operator?: string;
    value?: string;
    targetQuestionId: string;
    priority?: number;
  };
}

/**
 * Payload shape for the `EDIT` event.
 */
export interface StdBranchingLogicEditPayload {
  id: string;
}

/**
 * Payload shape for the `EDIT_SOURCE` event.
 */
export interface StdBranchingLogicEditSourcePayload {
  value: string;
}

/**
 * Payload shape for the `EDIT_OPERATOR` event.
 */
export interface StdBranchingLogicEditOperatorPayload {
  value: string;
}

/**
 * Payload shape for the `EDIT_VALUE` event.
 */
export interface StdBranchingLogicEditValuePayload {
  value: string;
}

/**
 * Payload shape for the `EDIT_TARGET` event.
 */
export interface StdBranchingLogicEditTargetPayload {
  value: string;
}

/**
 * Payload shape for the `BranchingRuleLoaded` event.
 */
export interface StdBranchingLogicBranchingRuleLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `BranchingRuleLoadFailed` event.
 */
export interface StdBranchingLogicBranchingRuleLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `BranchingRuleAdded` event.
 */
export interface StdBranchingLogicBranchingRuleAddedPayload {
  row?: {
    id: string;
    surveyId: string;
    sourceQuestionId: string;
    operator?: string;
    value?: string;
    targetQuestionId: string;
    priority?: number;
  };
}

/**
 * Payload shape for the `BranchingRuleUpdated` event.
 */
export interface StdBranchingLogicBranchingRuleUpdatedPayload {
  row?: {
    id: string;
    surveyId: string;
    sourceQuestionId: string;
    operator?: string;
    value?: string;
    targetQuestionId: string;
    priority?: number;
  };
}

/**
 * Payload shape for the `BranchingRuleDeleted` event.
 */
export interface StdBranchingLogicBranchingRuleDeletedPayload {
  id: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdBranchingLogicConfig {
  /** Default: `"Branching Rules"` */
  title?: string;
}

/**
 * Params for the std-branching-logic descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBranchingLogicParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBranchingLogicEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdBranchingLogicConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `BranchingLogic.traits.BranchingRuleManage`. */
export function stdBranchingLogicTrait(params: StdBranchingLogicParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BranchingRuleManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `BranchingLogic.pages.BranchingRuleManagePage`. */
export function stdBranchingLogicPage(params: StdBranchingLogicParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BranchingRuleManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBranchingLogic(params: StdBranchingLogicParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'BranchingLogicOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBranchingLogicTrait(params),
    ],
    pages: [
      stdBranchingLogicPage(params),
    ],
  });
}
