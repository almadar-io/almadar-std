/**
 * std-recurrence — std-recurrence — recurring-rule definition + upcoming runs (Stripe Subscriptions / Cron Hub style). The viewing state shows a plain-English rule sentence and the next N dated runs as an agenda timeline. Per-run exception modal supports skip / reschedule. Topology = loading / defining_rule / viewing / awaiting_exception_decision / cancelled / error.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-recurrence.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-recurrence
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-recurrence';
const ALIAS = 'Recurrence';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdRecurrenceEventKey = 'CANCEL_RULE' | 'CANCEL_SCHEDULE' | 'CLOSE_EXCEPTION' | 'EDIT_RULE' | 'INIT' | 'OPEN_EXCEPTION' | 'RESCHEDULE_OCCURRENCE' | 'RESTART' | 'RecurrenceLoadFailed' | 'RecurrenceLoaded' | 'RecurrenceSaveFailed' | 'RecurrenceSaved' | 'SAVE_RULE' | 'SKIP_OCCURRENCE';

/**
 * Payload shape for the `SAVE_RULE` event.
 */
export interface StdRecurrenceSaveRulePayload {
  data?: EntityRow;
}

/**
 * Payload shape for the `OPEN_EXCEPTION` event.
 */
export interface StdRecurrenceOpenExceptionPayload {
  row: EntityRow;
}

/**
 * Payload shape for the `SKIP_OCCURRENCE` event.
 */
export interface StdRecurrenceSkipOccurrencePayload {
  id?: string;
}

/**
 * Payload shape for the `RESCHEDULE_OCCURRENCE` event.
 */
export interface StdRecurrenceRescheduleOccurrencePayload {
  id?: string;
  newDate?: string;
}

/**
 * Payload shape for the `RecurrenceLoaded` event.
 */
export interface StdRecurrenceRecurrenceLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `RecurrenceLoadFailed` event.
 */
export interface StdRecurrenceRecurrenceLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `RecurrenceSaved` event.
 */
export interface StdRecurrenceRecurrenceSavedPayload {
  row?: EntityRow;
}

/**
 * Payload shape for the `RecurrenceSaveFailed` event.
 */
export interface StdRecurrenceRecurrenceSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRecurrenceConfig {
  /** Default: `"Schedule"` */
  title?: string;
  /** Default: `["frequency","interval","startDate","endDate","endAfterCount"]` */
  formFields?: string[];
}

/**
 * Params for the std-recurrence descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdRecurrenceParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdRecurrenceEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdRecurrenceConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Recurrence.traits.RecurrenceEditor`. */
export function stdRecurrenceTrait(params: StdRecurrenceParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.RecurrenceEditor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Recurrence.pages.RecurrencePage`. */
export function stdRecurrencePage(params: StdRecurrenceParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.RecurrencePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdRecurrence(params: StdRecurrenceParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'RecurrenceOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdRecurrenceTrait(params),
    ],
    pages: [
      stdRecurrencePage(params),
    ],
  });
}
