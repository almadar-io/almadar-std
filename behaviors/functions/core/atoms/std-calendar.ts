/**
 * std-calendar — std-calendar as a Function — owns the fetch + SELECT_DAY state machine; default bodyContent renders the month-grid + upcoming-events tree. Layer 3 variants override bodyContent to deliver agenda, week-timeline, etc. while inheriting the same trait, state machine, emits, and listens.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-calendar.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-calendar
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-calendar';
const ALIAS = 'Calendar';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCalendarEventKey = 'CalendarEventLoadFailed' | 'CalendarEventLoaded' | 'INIT' | 'SELECT_DAY' | 'SELECT_SLOT';

/**
 * Payload shape for the `CalendarEventLoaded` event.
 */
export interface StdCalendarCalendarEventLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `CalendarEventLoadFailed` event.
 */
export interface StdCalendarCalendarEventLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCalendarConfig {
  /** Default: `"@item.name"` */
  titleBinding?: string;
  /** Default: `"dense"` */
  tableLook?: 'dense' | 'spacious' | 'striped' | 'borderless' | 'card-rows';
  /** Default: `"@item.status"` */
  badgeBinding?: string;
  /** Default: `{"gap":"lg","direction":"vertical","type":"stack","children":[{"align":"center","direction":"horizontal","type":"stack","children":[{"align":"center","type":"stack","direction":"horizontal","gap":"sm","children":[{"name":"calendar","type":"icon"},{"type":"typography","content":"CalendarEvents Calendar","variant":"h2"}]},{"onSelect":"SELECT_DAY","type":"date-range-selector","selected":"@entity.selectedRange"}],"gap":"md","justify":"between"},{"type":"divider"},{"type":"calendar-grid"},{"type":"divider"},{"type":"typography","content":"Upcoming Events","variant":"h4"},{"look":"@config.tableLook","type":"data-list","itemActions":[{"event":"SELECT_DAY","icon":"eye","label":"View"}],"entity":"@payload.data","fields":[{"icon":"calendar","label":"Event","variant":"h4","name":"name"},{"label":"Time","variant":"badge","name":"time"},{"name":"date","variant":"caption","label":"Date","format":"date"},{"label":"Status","variant":"badge","name":"status"}]}]}` */
  bodyContent?: unknown;
  /** Default: `"@item.time"` */
  timeBinding?: string;
  /** Default: `"@item.description"` */
  descriptionBinding?: string;
}

/**
 * Params for the std-calendar descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCalendarParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCalendarEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCalendarConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Calendar.traits.CalendarEventCalendar`. */
export function stdCalendarTrait(params: StdCalendarParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CalendarEventCalendar`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Calendar.pages.CalendarEventCalendarPage`. */
export function stdCalendarPage(params: StdCalendarParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CalendarEventCalendarPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCalendar(params: StdCalendarParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CalendarEventOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCalendarTrait(params),
    ],
    pages: [
      stdCalendarPage(params),
    ],
  });
}
