/**
 * std-version-history — std-version-history — wiki/CMS revision history with diff + rollback. Browses revisions; opens detail in `viewing_single`; compares two revisions in `comparing` with intrinsic version-diff; persists rollback.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-version-history.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-version-history
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-version-history';
const ALIAS = 'VersionHistory';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdVersionHistoryEventKey = 'CLOSE_VIEW' | 'COMPARE' | 'INIT' | 'OPEN_REVISION' | 'ROLLBACK' | 'RevisionLoadFailed' | 'RevisionLoaded' | 'RevisionRollbackFailed' | 'RevisionRolledBack';

/**
 * Payload shape for the `OPEN_REVISION` event.
 */
export interface StdVersionHistoryOpenRevisionPayload {
  id: string;
  row?: {
    id: string;
    documentId: string;
    documentType: string;
    versionNumber: number;
    authorId?: string;
    authorName?: string;
    content?: string;
    summary?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `COMPARE` event.
 */
export interface StdVersionHistoryComparePayload {
  id: string;
  beforeId: string;
  afterId: string;
  revisions?: EntityRow[];
}

/**
 * Payload shape for the `ROLLBACK` event.
 */
export interface StdVersionHistoryRollbackPayload {
  id: string;
}

/**
 * Payload shape for the `RevisionLoaded` event.
 */
export interface StdVersionHistoryRevisionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `RevisionLoadFailed` event.
 */
export interface StdVersionHistoryRevisionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `RevisionRolledBack` event.
 */
export interface StdVersionHistoryRevisionRolledBackPayload {
  row?: {
    id: string;
    documentId: string;
    documentType: string;
    versionNumber: number;
    authorId?: string;
    authorName?: string;
    content?: string;
    summary?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `RevisionRollbackFailed` event.
 */
export interface StdVersionHistoryRevisionRollbackFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdVersionHistoryConfig {
  /** Default: `"dense"` */
  tableLook?: 'dense' | 'spacious' | 'striped' | 'borderless' | 'card-rows';
  /** Default: `"elevated"` */
  statLook?: 'elevated' | 'flat' | 'progress-backed' | 'gauge' | 'sparkline';
  /** Default: `"Version History"` */
  title?: string;
}

/**
 * Params for the std-version-history descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdVersionHistoryParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdVersionHistoryEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdVersionHistoryConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `VersionHistory.traits.RevisionManage`. */
export function stdVersionHistoryTrait(params: StdVersionHistoryParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.RevisionManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `VersionHistory.pages.RevisionManagePage`. */
export function stdVersionHistoryPage(params: StdVersionHistoryParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.RevisionManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdVersionHistory(params: StdVersionHistoryParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'RevisionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdVersionHistoryTrait(params),
    ],
    pages: [
      stdVersionHistoryPage(params),
    ],
  });
}
