/**
 * std-form-advanced — std-form-advanced — full-typed form-section primitive. Hosts the `editing` state with a configurable `fields` array (each entry mirrors @almadar/ui's `form-section` field schema: `{ name, label, type?, values?, relation?, required? }`). Field-level constraints live in `config.fieldValidation` as `[{ field, format?: 'email'|'url'|'phone', regex?, minLength?, maxLength?, message? }]` — form-section surfaces inline errors when a submit violates a declared constraint. Agents drive shape by overriding `config.fields` + `config.fieldValidation` per-call-site.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-form-advanced.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-form-advanced
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-form-advanced';
const ALIAS = 'FormAdvanced';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdFormAdvancedEventKey = 'FormEntryLoadFailed' | 'FormEntryLoaded' | 'FormEntrySaveFailed' | 'FormEntrySaved' | 'INIT' | 'RESET' | 'SUBMIT';

/**
 * Payload shape for the `FormEntryLoaded` event.
 */
export interface StdFormAdvancedFormEntryLoadedPayload {
  id?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
  notes?: string;
  age?: number;
  isActive?: boolean;
  birthDate?: string;
  priority?: string;
  categoryId?: string;
}

/**
 * Payload shape for the `FormEntryLoadFailed` event.
 */
export interface StdFormAdvancedFormEntryLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `FormEntrySaved` event.
 */
export interface StdFormAdvancedFormEntrySavedPayload {
  id?: string;
}

/**
 * Payload shape for the `FormEntrySaveFailed` event.
 */
export interface StdFormAdvancedFormEntrySaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdFormAdvancedConfig {
  /** Default: `[{"name":"name","label":"Name"},{"name":"description","label":"Description"},{"name":"status","values":["active","inactive","pending"],"label":"Status","type":"enum"},{"label":"Notes","name":"notes","type":"textarea"},{"type":"number","name":"age","label":"Age"},{"label":"Is Active","type":"boolean","name":"isActive"},{"type":"date","name":"birthDate","label":"Birth Date"},{"values":["low","medium","high","critical"],"name":"priority","label":"Priority","type":"enum"},{"relation":{"entity":"FormEntry","cardinality":"one"},"label":"Category ID","type":"relation","name":"categoryId"}]` */
  fields?: TraitConfig[];
  /** Default: `[]` */
  fieldValidation?: TraitConfig[];
}

/**
 * Params for the std-form-advanced descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdFormAdvancedParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdFormAdvancedEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdFormAdvancedConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `FormAdvanced.traits.FormEntryFormAdvanced`. */
export function stdFormAdvancedTrait(params: StdFormAdvancedParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.FormEntryFormAdvanced`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `FormAdvanced.pages.FormEntryFormPage`. */
export function stdFormAdvancedPage(params: StdFormAdvancedParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.FormEntryFormPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdFormAdvanced(params: StdFormAdvancedParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'FormEntryOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdFormAdvancedTrait(params),
    ],
    pages: [
      stdFormAdvancedPage(params),
    ],
  });
}
