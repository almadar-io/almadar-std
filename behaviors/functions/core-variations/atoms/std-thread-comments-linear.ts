/**
 * std-thread-comments-linear — Nested threaded comments — Disqus/Hacker News style: indented reply tree, inline reply composer on every comment card, and a visible replies group per parent. For forum discussions, blog comments, document review threads, project-manager issue comments.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-thread-comments-linear.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-thread-comments-linear
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-thread-comments-linear';
const ALIAS = 'ThreadCommentsLinear';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdThreadCommentsLinearConfig {
  /** Default: `{"children":[{"align":"center","children":[{"name":"message-square","size":"md","type":"icon"},{"content":"@config.sectionTitle","type":"typography","variant":"h3"}],"direction":"horizontal","gap":"sm","type":"stack"},{"children":{"children":[{"content":"Add a comment","type":"typography","variant":"label"},{"action":"EDIT_REPLY","inputType":"textarea","placeholder":"Share your thoughts…","rows":3,"type":"input"},{"children":[{"action":"CANCEL_REPLY","label":"Cancel","size":"sm","type":"button","variant":"ghost"},{"action":"SUBMIT_REPLY","icon":"send","label":"Comment","size":"sm","type":"button","variant":"primary"}],"direction":"horizontal","gap":"sm","justify":"end","type":"stack"}],"direction":"vertical","gap":"sm","type":"stack"},"look":"flat-bordered","padding":"md","type":"card"},{"nodes":"@payload.data","replyEvent":"REPLY","showActions":true,"type":"reply-tree"}],"className":"w-full px-card-md py-section","direction":"vertical","gap":"lg","type":"stack"}` */
  bodyContent?: unknown;
}

/**
 * Params for the std-thread-comments-linear descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdThreadCommentsLinearParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map (atom key → caller key). */
  events?: Record<string, string>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdThreadCommentsLinearConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ThreadCommentsLinear.traits.CommentsLinear`. */
export function stdThreadCommentsLinearTrait(params: StdThreadCommentsLinearParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CommentsLinear`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ThreadCommentsLinear.pages.CommentsLinearPage`. */
export function stdThreadCommentsLinearPage(params: StdThreadCommentsLinearParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CommentsLinearPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdThreadCommentsLinear(params: StdThreadCommentsLinearParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CommentsLinearOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdThreadCommentsLinearTrait(params),
    ],
    pages: [
      stdThreadCommentsLinearPage(params),
    ],
  });
}
