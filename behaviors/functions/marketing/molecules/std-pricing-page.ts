/**
 * std-pricing-page — Pricing page molecule for marketing sites. Composes hero, pricing grid, FAQ accordion, and CTA sections as a vertical stack of individual marketing patterns. Pick for SaaS pricing pages and plan comparisons.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/std-pricing-page.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-pricing-page
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-pricing-page';
const ALIAS = 'PricingPage';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdPricingPageEventKey = 'INIT';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdPricingPageConfig {
  /** Default: `"subtle"` */
  heroBackground?: string;
  /** Default: `"/contact"` */
  heroSecondaryActionHref?: string;
  /** Default: `""` */
  heroTitleAccent?: string;
  /** Default: `"/signup"` */
  heroPrimaryActionHref?: string;
  /** Default: `"Contact Sales"` */
  heroSecondaryActionLabel?: string;
  /** Default: `[{"name":"Starter","actionLabel":"Get Started","actionHref":"#","description":"For individuals and small projects.","features":["1 project","Community support","Basic analytics"],"price":"$0","id":"starter","highlighted":false,"badge":""},{"id":"pro","highlighted":true,"actionHref":"#","price":"$49","actionLabel":"Start Trial","description":"For growing teams that need more power.","features":["Unlimited projects","Priority support","Advanced analytics","Custom domains"],"badge":"Most Popular","name":"Pro"},{"badge":"","id":"enterprise","description":"For organizations with advanced needs.","actionLabel":"Contact Sales","features":["Dedicated infra","SLA guarantee","SSO & SAML","Audit logs","Custom contracts"],"highlighted":false,"actionHref":"#","price":"Custom","name":"Enterprise"}]` */
  plans?: EntityRow[];
  /** Default: `"center"` */
  heroAlign?: string;
  /** Default: `"Simple, transparent pricing"` */
  heroTitle?: string;
  /** Default: `{"label":"Contact Sales","href":"/contact"}` */
  ctaAction?: EntityRow;
  /** Default: `"Get Started"` */
  heroPrimaryActionLabel?: string;
  /** Default: `"Start free, scale as you grow."` */
  heroSubtitle?: string;
  /** Default: `[{"items":[{"label":"Features","href":"#"},{"label":"Pricing","href":"#"},{"label":"Changelog","href":"#"}],"title":"Product"},{"title":"Company","items":[{"label":"About","href":"#"},{"label":"Careers","href":"#"},{"label":"Blog","href":"#"}]},{"items":[{"href":"#","label":"Docs"},{"href":"#","label":"Community"},{"href":"#","label":"Support"}],"title":"Resources"}]` */
  footerColumns?: EntityRow[];
  /** Default: `"© 2026 Almadar. All rights reserved."` */
  footerCopyright?: string;
  /** Default: `"Still have questions?"` */
  ctaTitle?: string;
  /** Default: `""` */
  heroTag?: string;
  /** Default: `[{"question":"Can I change plans later?","answer":"Yes, you can upgrade or downgrade at any time."},{"answer":"Yes, every paid plan includes a 14-day free trial.","question":"Is there a free trial?"},{"question":"What payment methods do you accept?","answer":"We accept all major credit cards and wire transfers for Enterprise."}]` */
  faq?: EntityRow[];
  /** Default: `"Our team is here to help you find the right plan."` */
  ctaSubtitle?: string;
}

/**
 * Params for the std-pricing-page descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdPricingPageParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdPricingPageEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdPricingPageConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `PricingPage.traits.PricingPageItemLayout`. */
export function stdPricingPageTrait(params: StdPricingPageParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PricingPageItemLayout`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `PricingPage.pages.PricingPage`. */
export function stdPricingPagePage(params: StdPricingPageParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.PricingPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdPricingPage(params: StdPricingPageParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'PricingPageItemOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdPricingPageTrait(params),
    ],
    pages: [
      stdPricingPagePage(params),
    ],
  });
}
