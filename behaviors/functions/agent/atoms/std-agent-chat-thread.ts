/**
 * std-agent-chat-thread — std-agent-chat-thread as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-chat-thread.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-agent-chat-thread
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-chat-thread';
const ALIAS = 'AgentChatThread';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentChatThreadEventKey = 'AgentChatThreadDeleteFailed' | 'AgentChatThreadDeleted' | 'AgentChatThreadLoadFailed' | 'AgentChatThreadLoaded' | 'AgentChatThreadSaveFailed' | 'AgentChatThreadSaved' | 'CANCEL_COMPOSE' | 'CLEAR' | 'COMPOSE' | 'INIT' | 'SEND';

/**
 * Payload shape for the `AgentChatThreadLoaded` event.
 */
export interface StdAgentChatThreadAgentChatThreadLoadedPayload {
  data?: Array<Record<string, unknown>>;
}

/**
 * Payload shape for the `AgentChatThreadLoadFailed` event.
 */
export interface StdAgentChatThreadAgentChatThreadLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentChatThreadSaved` event.
 */
export interface StdAgentChatThreadAgentChatThreadSavedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentChatThreadSaveFailed` event.
 */
export interface StdAgentChatThreadAgentChatThreadSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentChatThreadDeleted` event.
 */
export interface StdAgentChatThreadAgentChatThreadDeletedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentChatThreadDeleteFailed` event.
 */
export interface StdAgentChatThreadAgentChatThreadDeleteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-agent-chat-thread descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentChatThreadParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentChatThreadEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentChatThread.traits.AgentChatThreadThread`. */
export function stdAgentChatThreadTrait(params: StdAgentChatThreadParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentChatThreadThread`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentChatThread.pages.AgentChatThreadPage`. */
export function stdAgentChatThreadPage(params: StdAgentChatThreadParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentChatThreadPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentChatThread(params: StdAgentChatThreadParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AgentChatThreadOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentChatThreadTrait(params),
    ],
    pages: [
      stdAgentChatThreadPage(params),
    ],
  });
}
