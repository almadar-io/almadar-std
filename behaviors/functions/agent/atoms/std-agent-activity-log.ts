/**
 * std-agent-activity-log — std-agent-activity-log as a Function
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-activity-log.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-agent-activity-log
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-activity-log';
const ALIAS = 'AgentActivityLog';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentActivityLogEventKey = 'AgentActivityLogDeleteFailed' | 'AgentActivityLogDeleted' | 'AgentActivityLogLoadFailed' | 'AgentActivityLogLoaded' | 'AgentActivityLogSaveFailed' | 'AgentActivityLogSaved' | 'CLEAR' | 'INIT' | 'LOG_ENTRY';

/**
 * Payload shape for the `AgentActivityLogLoaded` event.
 */
export interface StdAgentActivityLogAgentActivityLogLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `AgentActivityLogLoadFailed` event.
 */
export interface StdAgentActivityLogAgentActivityLogLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentActivityLogSaved` event.
 */
export interface StdAgentActivityLogAgentActivityLogSavedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentActivityLogSaveFailed` event.
 */
export interface StdAgentActivityLogAgentActivityLogSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentActivityLogDeleted` event.
 */
export interface StdAgentActivityLogAgentActivityLogDeletedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentActivityLogDeleteFailed` event.
 */
export interface StdAgentActivityLogAgentActivityLogDeleteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-agent-activity-log descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentActivityLogParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentActivityLogEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentActivityLog.traits.AgentActivityLogLog`. */
export function stdAgentActivityLogTrait(params: StdAgentActivityLogParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentActivityLogLog`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentActivityLog.pages.AgentActivityLogPage`. */
export function stdAgentActivityLogPage(params: StdAgentActivityLogParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentActivityLogPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentActivityLog(params: StdAgentActivityLogParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AgentActivityLogOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentActivityLogTrait(params),
    ],
    pages: [
      stdAgentActivityLogPage(params),
    ],
  });
}
