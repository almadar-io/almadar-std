/**
 * std-agent-memory — std-agent-memory as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-memory.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-agent-memory
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-memory';
const ALIAS = 'AgentMemory';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentMemoryEventKey = 'AgentMemoryLoadFailed' | 'AgentMemoryLoaded' | 'DECAY' | 'FORGET' | 'INIT' | 'MEMORIZE' | 'PIN' | 'RECALL' | 'REINFORCE';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdAgentMemoryListenKey = 'MEMORIZED' | 'MEMORY_CREATED' | 'PINNED' | 'FORGOT' | 'REINFORCED' | 'DECAYED';

/**
 * Payload shape for the `PIN` event.
 */
export interface StdAgentMemoryPinPayload {
  id: string;
  row?: {
    id: string;
    name?: string;
    description?: string;
    status?: string;
    createdAt?: string;
    content?: string;
    category?: string;
    strength?: number;
    pinned?: boolean;
    scope?: string;
    lastAccessedAt?: string;
  };
}

/**
 * Payload shape for the `REINFORCE` event.
 */
export interface StdAgentMemoryReinforcePayload {
  id: string;
  row?: {
    id: string;
    name?: string;
    description?: string;
    status?: string;
    createdAt?: string;
    content?: string;
    category?: string;
    strength?: number;
    pinned?: boolean;
    scope?: string;
    lastAccessedAt?: string;
  };
}

/**
 * Payload shape for the `FORGET` event.
 */
export interface StdAgentMemoryForgetPayload {
  id: string;
  row?: {
    id: string;
    name?: string;
    description?: string;
    status?: string;
    createdAt?: string;
    content?: string;
    category?: string;
    strength?: number;
    pinned?: boolean;
    scope?: string;
    lastAccessedAt?: string;
  };
}

/**
 * Payload shape for the `AgentMemoryLoaded` event.
 */
export interface StdAgentMemoryAgentMemoryLoadedPayload {
  data?: Array<Record<string, unknown>>;
}

/**
 * Payload shape for the `AgentMemoryLoadFailed` event.
 */
export interface StdAgentMemoryAgentMemoryLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-agent-memory descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentMemoryParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentMemoryEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentMemory.traits.AgentMemoryBrowse`. */
export function stdAgentMemoryAgentMemoryBrowseTrait(params: StdAgentMemoryParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentMemoryBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentMemory.traits.AgentMemoryCreate`. */
export function stdAgentMemoryAgentMemoryCreateTrait(params: StdAgentMemoryParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentMemoryCreate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentMemory.traits.AgentMemoryAgent`. */
export function stdAgentMemoryAgentMemoryAgentTrait(params: StdAgentMemoryParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentMemoryAgent`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentMemory.pages.AgentMemoryPage`. */
export function stdAgentMemoryPage(params: StdAgentMemoryParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentMemoryPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentMemory(params: StdAgentMemoryParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AgentMemoryOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentMemoryAgentMemoryBrowseTrait(params),
      stdAgentMemoryAgentMemoryCreateTrait(params),
      stdAgentMemoryAgentMemoryAgentTrait(params),
    ],
    pages: [
      stdAgentMemoryPage(params),
    ],
  });
}
