/**
 * std-agent-tool-call — Wraps llm/call-tools — takes messages + tools, returns tool-call result.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-tool-call.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-agent-tool-call
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page, PatternValue } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-tool-call';
const ALIAS = 'AgentToolCall';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentToolCallEventKey = 'CALL_TOOLS' | 'INIT' | 'RESET' | 'TOOLS_CALLED' | 'TOOLS_FAILED';

/**
 * Payload shape for the `TOOLS_CALLED` event.
 */
export interface StdAgentToolCallToolsCalledPayload {
  result: string;
}

/**
 * Params for the std-agent-tool-call descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentToolCallParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentToolCallEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentToolCall.traits.ToolCallExecution`. */
export function stdAgentToolCallToolCallExecutionTrait(params: StdAgentToolCallParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ToolCallExecution`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentToolCall.traits.ToolCallReady`. */
export function stdAgentToolCallToolCallReadyTrait(params: StdAgentToolCallParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ToolCallReady`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentToolCall.pages.AgentToolCallPage`. */
export function stdAgentToolCallPage(params: StdAgentToolCallParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentToolCallPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentToolCall(params: StdAgentToolCallParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'AgentToolCallOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentToolCallToolCallExecutionTrait(params),
      stdAgentToolCallToolCallReadyTrait(params),
    ],
    pages: [
      stdAgentToolCallPage(params),
    ],
  });
}

type _StdAgentToolCallEntityName = 'ToolCall';
type _StdAgentToolCallListenTraitName = 'ToolCallExecution' | 'ToolCallReady';

/**
 * Tunable params for the AgentToolCallOrbital orbital.
 *
 * Canonical entity: ToolCall — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdAgentToolCallAgentToolCallOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ToolCallExecution' | 'ToolCallReady',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait AgentToolCallOrbital's `uses[]` exports. */
type _StdAgentToolCallAgentToolCallOrbitalUsesRef = never;

/** Per-orbital factory: builds the AgentToolCallOrbital orbital with consumer params. */
export function stdAgentToolCallAgentToolCallOrbital(params: StdAgentToolCallAgentToolCallOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'AgentToolCallOrbital',
    uses: [],
    entity: {
      name: 'ToolCall',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'default': [],
            'items': {
              'properties': {
                'content': {
                  'name': 'content',
                  'required': true,
                  'type': 'string',
                },
                'role': {
                  'name': 'role',
                  'required': true,
                  'type': 'string',
                },
                'toolCallId': {
                  'name': 'toolCallId',
                  'required': false,
                  'type': 'string',
                },
                'toolCalls': {
                  'items': {
                    'type': 'opaque',
                  },
                  'name': 'toolCalls',
                  'required': false,
                  'type': 'array',
                },
              },
              'type': 'object',
            },
            'name': 'messages',
            'type': 'array',
          },
          {
            'default': [],
            'items': {
              'properties': {
                'description': {
                  'name': 'description',
                  'required': true,
                  'type': 'string',
                },
                'name': {
                  'name': 'name',
                  'required': true,
                  'type': 'string',
                },
                'schema': {
                  'name': 'schema',
                  'required': false,
                  'type': 'opaque',
                },
              },
              'type': 'object',
            },
            'name': 'tools',
            'type': 'array',
          },
          {
            'default': '',
            'name': 'response',
            'type': 'string',
          },
          {
            'default': 'idle',
            'name': 'status',
            'type': 'string',
            'values': [
              'idle',
              'done',
            ],
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'lifecycle',
        'emits': [
          {
            'event': 'TOOLS_CALLED',
            'payloadSchema': [
              {
                'name': 'result',
                'required': true,
                'type': 'string',
              },
            ],
          },
        ],
        'linkedEntity': 'ToolCall',
        'name': 'ToolCallExecution',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'CALL_TOOLS',
              'name': 'Call Tools',
              'payloadSchema': [
                {
                  'name': 'messages',
                  'properties': [
                    {
                      'name': 'role',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'content',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'toolCalls',
                      'type': '[opaque]',
                    },
                    {
                      'name': 'toolCallId',
                      'type': 'string',
                    },
                  ],
                  'required': true,
                  'type': '[object]',
                },
                {
                  'name': 'tools',
                  'properties': [
                    {
                      'name': 'name',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'description',
                      'required': true,
                      'type': 'string',
                    },
                    {
                      'name': 'schema',
                      'type': 'opaque',
                    },
                  ],
                  'required': true,
                  'type': '[object]',
                },
              ],
            },
            {
              'key': 'TOOLS_CALLED',
              'name': 'Tools Called',
              'payloadSchema': [
                {
                  'name': 'result',
                  'required': true,
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'TOOLS_FAILED',
              'name': 'Tools Failed',
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
            {
              'name': 'calling',
            },
            {
              'name': 'done',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.messages',
                  [],
                ],
                [
                  'set',
                  '@entity.tools',
                  [],
                ],
                [
                  'set',
                  '@entity.response',
                  '',
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.messages',
                  '@payload.messages',
                ],
                [
                  'set',
                  '@entity.tools',
                  '@payload.tools',
                ],
                [
                  'llm/call-tools',
                  '@entity.messages',
                  '@entity.tools',
                  {
                    'emit': {
                      'failure': 'TOOLS_FAILED',
                      'success': 'TOOLS_CALLED',
                    },
                  },
                ],
              ],
              'event': 'CALL_TOOLS',
              'from': 'idle',
              'to': 'calling',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.response',
                  '@payload.result',
                ],
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'TOOLS_CALLED',
              'from': 'calling',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.response',
                  '',
                ],
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'TOOLS_FAILED',
              'from': 'calling',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'INIT',
              'from': 'done',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.response',
                  '',
                ],
                [
                  'set',
                  '@entity.messages',
                  [],
                ],
                [
                  'set',
                  '@entity.tools',
                  [],
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'RESET',
              'from': 'done',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      {
        'category': 'lifecycle',
        'linkedEntity': 'ToolCall',
        'listens': [
          {
            'event': 'TOOLS_CALLED',
            'source': {
              'kind': 'trait',
              'trait': ('ToolCallExecution' satisfies _StdAgentToolCallListenTraitName),
            },
            'triggers': 'TOOLS_CALLED',
          },
        ],
        'name': 'ToolCallReady',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'TOOLS_CALLED',
              'name': 'Tools Called',
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'waiting',
            },
            {
              'name': 'ready',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'INIT',
              'from': 'waiting',
              'to': 'waiting',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'TOOLS_CALLED',
              'from': 'waiting',
              'to': 'ready',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'INIT',
              'from': 'ready',
              'to': 'ready',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'RESET',
              'from': 'ready',
              'to': 'waiting',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'AgentToolCallPage',
        'path': '/agent-tool-call',
        'traits': [
          {
            'ref': 'ToolCallExecution',
          },
          {
            'ref': 'ToolCallReady',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdAgentToolCallAgentToolCallOrbital. */
export const StdAgentToolCallAgentToolCallOrbitalManifest = {
  organism: 'std-agent-tool-call',
  orbitalName: 'AgentToolCallOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'ToolCallExecution',
    'ToolCallReady',
  ] as const,
};

/** Typed guard — runtime validates StdAgentToolCallAgentToolCallOrbitalParams keys. */
export function isStdAgentToolCallAgentToolCallOrbitalParams(p: object): p is StdAgentToolCallAgentToolCallOrbitalParams {
  type _OverrideRecord = NonNullable<StdAgentToolCallAgentToolCallOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdAgentToolCallAgentToolCallOrbitalManifest.traitNames,
      ...StdAgentToolCallAgentToolCallOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
