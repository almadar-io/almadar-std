/**
 * std-agent-tool-call — std-agent-tool-call as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-tool-call.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-agent-tool-call
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-tool-call';
const ALIAS = 'AgentToolCall';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentToolCallEventKey = 'AgentToolCallLoadFailed' | 'AgentToolCallLoaded' | 'AgentToolCallSaveFailed' | 'AgentToolCallSaved' | 'CLOSE' | 'INIT' | 'INVOKE' | 'INVOKED' | 'SAVE';

/**
 * Payload shape for the `INVOKED` event.
 */
export interface StdAgentToolCallInvokedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentToolCallLoaded` event.
 */
export interface StdAgentToolCallAgentToolCallLoadedPayload {
  id?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
  toolName?: string;
  args?: string;
  result?: string;
  error?: string;
  duration?: number;
  action?: string;
  detail?: string;
  timestamp?: string;
}

/**
 * Payload shape for the `AgentToolCallLoadFailed` event.
 */
export interface StdAgentToolCallAgentToolCallLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `AgentToolCallSaved` event.
 */
export interface StdAgentToolCallAgentToolCallSavedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentToolCallSaveFailed` event.
 */
export interface StdAgentToolCallAgentToolCallSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-agent-tool-call descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentToolCallParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentToolCallEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentToolCall.traits.AgentToolCallModal`. */
export function stdAgentToolCallAgentToolCallModalTrait(params: StdAgentToolCallParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentToolCallModal`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentToolCall.traits.AgentToolCallAgent`. */
export function stdAgentToolCallAgentToolCallAgentTrait(params: StdAgentToolCallParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentToolCallAgent`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentToolCall.pages.AgentToolCallPage`. */
export function stdAgentToolCallPage(params: StdAgentToolCallParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentToolCallPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentToolCall(params: StdAgentToolCallParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AgentToolCallOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentToolCallAgentToolCallModalTrait(params),
      stdAgentToolCallAgentToolCallAgentTrait(params),
    ],
    pages: [
      stdAgentToolCallPage(params),
    ],
  });
}
