/**
 * std-agent-session — std-agent-session as a Function
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-agent-session.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-agent-session
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-session';
const ALIAS = 'AgentSession';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentSessionEventKey = 'AgentSessionLoadFailed' | 'AgentSessionLoaded' | 'AgentSessionSaveFailed' | 'AgentSessionSaved' | 'AgentSessionUpdateFailed' | 'AgentSessionUpdated' | 'END' | 'FORK' | 'INIT' | 'LABEL';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdAgentSessionListenKey = 'FORKED' | 'LABELED' | 'ENDED';

/**
 * Payload shape for the `FORK` event.
 */
export interface StdAgentSessionForkPayload {
  id: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `LABEL` event.
 */
export interface StdAgentSessionLabelPayload {
  id: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `END` event.
 */
export interface StdAgentSessionEndPayload {
  id: string;
  row?: EntityRow;
}

/**
 * Payload shape for the `AgentSessionLoaded` event.
 */
export interface StdAgentSessionAgentSessionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `AgentSessionLoadFailed` event.
 */
export interface StdAgentSessionAgentSessionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentSessionUpdated` event.
 */
export interface StdAgentSessionAgentSessionUpdatedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentSessionUpdateFailed` event.
 */
export interface StdAgentSessionAgentSessionUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AgentSessionSaved` event.
 */
export interface StdAgentSessionAgentSessionSavedPayload {
  id?: string;
}

/**
 * Payload shape for the `AgentSessionSaveFailed` event.
 */
export interface StdAgentSessionAgentSessionSaveFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-agent-session descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAgentSessionParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAgentSessionEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AgentSession.traits.AgentSessionBrowse`. */
export function stdAgentSessionAgentSessionBrowseTrait(params: StdAgentSessionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentSessionBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentSession.traits.AgentSessionLabel`. */
export function stdAgentSessionAgentSessionLabelTrait(params: StdAgentSessionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentSessionLabel`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `AgentSession.traits.AgentSessionAgent`. */
export function stdAgentSessionAgentSessionAgentTrait(params: StdAgentSessionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AgentSessionAgent`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AgentSession.pages.AgentSessionPage`. */
export function stdAgentSessionPage(params: StdAgentSessionParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AgentSessionPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAgentSession(params: StdAgentSessionParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AgentSessionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAgentSessionAgentSessionBrowseTrait(params),
      stdAgentSessionAgentSessionLabelTrait(params),
      stdAgentSessionAgentSessionAgentTrait(params),
    ],
    pages: [
      stdAgentSessionPage(params),
    ],
  });
}
