/**
 * std-agent-builder — Agent builder organism — plan, build, validate, fix a single orbital end-to-end.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/std-agent-builder.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-agent-builder
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { applyTraitRenames, rebindInlineTraitEntity, mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-agent-builder';
const ALIAS = 'AgentBuilder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAgentBuilderEventKey = 'BUILD_COMPLETE' | 'INIT' | 'PLANNED' | 'PLAN_FAILED' | 'RESET' | 'RETRY' | 'START';

/**
 * Payload shape for the `BUILD_COMPLETE` event.
 */
export interface StdAgentBuilderBuildCompletePayload {
  orbitalName: string;
  plan: string;
}

/**
 * Payload shape for the `PLANNED` event.
 */
export interface StdAgentBuilderPlannedPayload {
  result: string;
}

type _StdAgentBuilderEntityName = 'BuilderProcess';
type _StdAgentBuilderListenTraitName = 'BuilderPipeline' | 'BuilderListener';

/**
 * Tunable params for the AgentBuilderOrbital orbital.
 *
 * Canonical entity: BuilderProcess — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdAgentBuilderAgentBuilderOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'BuilderPipeline' | 'BuilderListener',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait AgentBuilderOrbital's `uses[]` exports. */
type _StdAgentBuilderAgentBuilderOrbitalUsesRef = never;

/** Per-orbital factory: builds the AgentBuilderOrbital orbital with consumer params. */
export function stdAgentBuilderAgentBuilderOrbital(params: StdAgentBuilderAgentBuilderOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'BuilderProcess';
  const built = makeOrbitalWithUses({
    name: 'AgentBuilderOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'default': '',
            'name': 'request',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'plan',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'orbitalName',
            'type': 'string',
          },
          {
            'default': {},
            'name': 'spec',
            'properties': {
              'deltaPrompt': {
                'name': 'deltaPrompt',
                'required': true,
                'type': 'string',
              },
              'method': {
                'name': 'method',
                'required': true,
                'type': 'string',
              },
              'orbitalName': {
                'name': 'orbitalName',
                'required': true,
                'type': 'string',
              },
              'organism': {
                'name': 'organism',
                'required': true,
                'type': 'string',
              },
            },
            'type': 'object',
          },
          {
            'default': '',
            'name': 'validationResult',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'fixPrompt',
            'type': 'string',
          },
          {
            'default': 'idle',
            'name': 'status',
            'type': 'string',
            'values': [
              'idle',
              'done',
            ],
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      rebindInlineTraitEntity({
        'category': 'lifecycle',
        'emits': [
          {
            'event': 'BUILD_COMPLETE',
            'payloadSchema': [
              {
                'name': 'orbitalName',
                'required': true,
                'type': 'string',
              },
              {
                'name': 'plan',
                'required': true,
                'type': 'string',
              },
            ],
          },
          {
            'event': 'PLANNED',
            'payloadSchema': [
              {
                'name': 'result',
                'required': true,
                'type': 'string',
              },
            ],
          },
        ],
        'linkedEntity': 'BuilderProcess',
        'name': 'BuilderPipeline',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'START',
              'name': 'Start',
              'payloadSchema': [
                {
                  'name': 'request',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'orbitalName',
                  'required': true,
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'PLANNED',
              'name': 'Planned',
              'payloadSchema': [
                {
                  'name': 'result',
                  'required': true,
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'PLAN_FAILED',
              'name': 'Plan Failed',
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
            {
              'key': 'BUILD_COMPLETE',
              'name': 'Build Complete',
              'payloadSchema': [
                {
                  'name': 'orbitalName',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'plan',
                  'required': true,
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'RETRY',
              'name': 'Retry',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
            {
              'name': 'planning',
            },
            {
              'name': 'done',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.request',
                  '',
                ],
                [
                  'set',
                  '@entity.plan',
                  '',
                ],
                [
                  'set',
                  '@entity.orbitalName',
                  '',
                ],
                [
                  'set',
                  '@entity.validationResult',
                  '',
                ],
                [
                  'set',
                  '@entity.fixPrompt',
                  '',
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.request',
                  '@payload.request',
                ],
                [
                  'set',
                  '@entity.orbitalName',
                  '@payload.orbitalName',
                ],
                [
                  'llm/generate',
                  '@entity.request',
                  {
                    'emit': {
                      'failure': 'PLAN_FAILED',
                      'success': 'PLANNED',
                    },
                  },
                ],
              ],
              'event': 'START',
              'from': 'idle',
              'to': 'planning',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.plan',
                  '@payload.result',
                ],
                [
                  'behavior/instantiate',
                  '@entity.orbitalName',
                  '@entity.plan',
                ],
                [
                  'validate/validate',
                  '@entity.orbitalName',
                ],
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
                [
                  'emit',
                  'BUILD_COMPLETE',
                  {
                    'orbitalName': '@entity.orbitalName',
                    'plan': '@entity.plan',
                  },
                ],
              ],
              'event': 'PLANNED',
              'from': 'planning',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.plan',
                  '',
                ],
                [
                  'behavior/instantiate',
                  '@entity.orbitalName',
                  '@entity.plan',
                ],
                [
                  'validate/validate',
                  '@entity.orbitalName',
                ],
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
                [
                  'emit',
                  'BUILD_COMPLETE',
                  {
                    'orbitalName': '@entity.orbitalName',
                    'plan': '@entity.plan',
                  },
                ],
              ],
              'event': 'PLAN_FAILED',
              'from': 'planning',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'INIT',
              'from': 'done',
              'to': 'done',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.request',
                  '',
                ],
                [
                  'set',
                  '@entity.plan',
                  '',
                ],
                [
                  'set',
                  '@entity.orbitalName',
                  '',
                ],
                [
                  'set',
                  '@entity.validationResult',
                  '',
                ],
                [
                  'set',
                  '@entity.fixPrompt',
                  '',
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'RESET',
              'from': 'done',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait, 'BuilderProcess', canonicalName),
      rebindInlineTraitEntity({
        'category': 'lifecycle',
        'linkedEntity': 'BuilderProcess',
        'listens': [
          {
            'event': 'BUILD_COMPLETE',
            'source': {
              'kind': 'trait',
              'trait': ('BuilderPipeline' satisfies _StdAgentBuilderListenTraitName),
            },
            'triggers': 'BUILD_COMPLETE',
          },
        ],
        'name': 'BuilderListener',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'BUILD_COMPLETE',
              'name': 'Build Complete',
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'waiting',
            },
            {
              'name': 'resolved',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'INIT',
              'from': 'waiting',
              'to': 'waiting',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'BUILD_COMPLETE',
              'from': 'waiting',
              'to': 'resolved',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'done',
                ],
              ],
              'event': 'INIT',
              'from': 'resolved',
              'to': 'resolved',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'RESET',
              'from': 'resolved',
              'to': 'waiting',
            },
          ],
        },
      } satisfies Trait, 'BuilderProcess', canonicalName),
    ],
    pages: [
      {
        'name': 'AgentBuilderPage',
        'path': '/agent-builder',
        'traits': [
          {
            'ref': 'BuilderPipeline',
          },
          {
            'ref': 'BuilderListener',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  const traitRenames = new Map<string, string>();
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined && override.name !== tr.name) {
        // A rename must also rewrite page trait refs + @trait.<old> config
        // tokens (applyTraitRenames below) or the built orbital dangles.
        traitRenames.set(tr.name, override.name);
        merged.name = override.name;
      }
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (traitRenames.size > 0) {
    const renamed = applyTraitRenames(built, traitRenames);
    built.traits = renamed.traits;
    built.pages = renamed.pages;
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdAgentBuilderAgentBuilderOrbital. */
export const StdAgentBuilderAgentBuilderOrbitalManifest = {
  organism: 'std-agent-builder',
  orbitalName: 'AgentBuilderOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'BuilderPipeline',
    'BuilderListener',
  ] as const,
};

/** Typed guard — runtime validates StdAgentBuilderAgentBuilderOrbitalParams keys. */
export function isStdAgentBuilderAgentBuilderOrbitalParams(p: object): p is StdAgentBuilderAgentBuilderOrbitalParams {
  type _OverrideRecord = NonNullable<StdAgentBuilderAgentBuilderOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdAgentBuilderAgentBuilderOrbitalManifest.traitNames,
      ...StdAgentBuilderAgentBuilderOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-agent-builder — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdAgentBuilderParams {
  AgentBuilder?: StdAgentBuilderAgentBuilderOrbitalParams;
}

/** Whole-organism descriptor (1 orbitals). Composes per-orbital factories. */
export function stdAgentBuilder(params: StdAgentBuilderParams = {}): OrbitalDefinition[] {
  return [
    stdAgentBuilderAgentBuilderOrbital(params.AgentBuilder ?? {}),
  ];
}
