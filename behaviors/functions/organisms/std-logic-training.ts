/**
 * std-logic-training — Logic training organism. Composes: stdDebuggerGame(DebugChallenge) + stdNegotiatorGame(NegotiateChallenge) + stdScoreBoard(TrainingScore)
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-logic-training.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-logic-training
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/organisms/std-logic-training';
const ALIAS = 'LogicTraining';

/**
 * Params for the std-logic-training descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLogicTrainingParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map (atom key → caller key). */
  events?: Record<string, string>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: Record<string, Record<string, unknown>>;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LogicTraining.traits.DebugChallengeDebuggerGame`. */
export function stdLogicTrainingTrait(params: StdLogicTrainingParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DebugChallengeDebuggerGame`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config } : {}),
  });
}

/** Page descriptor: `LogicTraining.pages.DebuggerPage`. */
export function stdLogicTrainingPage(params: StdLogicTrainingParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DebuggerPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor (3 orbitals). */
export function stdLogicTraining(params: StdLogicTrainingParams): OrbitalDefinition[] {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
  };
  return [
    makeOrbitalWithUses({
      name: 'DebugChallengeOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.DebugChallengeDebuggerGame`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.DebuggerPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'NegotiateChallengeOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.NegotiateChallengeNegotiatorGame`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.NegotiatorPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'TrainingScoreOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.TrainingScoreScoreBoard`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.ScoresPage`, linkedEntity: params.entityName }),
      ],
    }),
  ];
}
