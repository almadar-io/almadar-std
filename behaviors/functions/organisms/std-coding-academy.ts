/**
 * std-coding-academy — Coding academy organism. Composes: stdSequencerGame(SeqChallenge) + stdBuilderGame(BuildChallenge) + stdEventHandlerGame(EventChallenge) + stdDisplay(StudentProgress)
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-coding-academy.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-coding-academy
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/organisms/std-coding-academy';
const ALIAS = 'CodingAcademy';

/**
 * Params for the std-coding-academy descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCodingAcademyParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map (atom key → caller key). */
  events?: Record<string, string>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: Record<string, Record<string, unknown>>;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CodingAcademy.traits.SeqChallengeSequencerGame`. */
export function stdCodingAcademyTrait(params: StdCodingAcademyParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SeqChallengeSequencerGame`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config } : {}),
  });
}

/** Page descriptor: `CodingAcademy.pages.SequencerPage`. */
export function stdCodingAcademyPage(params: StdCodingAcademyParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SequencerPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor (4 orbitals). */
export function stdCodingAcademy(params: StdCodingAcademyParams): OrbitalDefinition[] {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
  };
  return [
    makeOrbitalWithUses({
      name: 'SeqChallengeOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.SeqChallengeSequencerGame`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.SequencerPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'BuildChallengeOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.BuildChallengeBuilderGame`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.BuilderPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'EventChallengeOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.EventChallengeEventHandlerGame`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.EventsPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'StudentProgressOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.StudentProgressDisplay`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.ProgressPage`, linkedEntity: params.entityName }),
      ],
    }),
  ];
}
