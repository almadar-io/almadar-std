/**
 * std-rpg-game — RPG game organism. Composes: stdTurnBasedBattle(BattleState) + stdOverworld(WorldZone) + stdInventory(RpgItem) + stdQuest(Mission)
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-rpg-game.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-rpg-game
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/organisms/std-rpg-game';
const ALIAS = 'RpgGame';

/**
 * Params for the std-rpg-game descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdRpgGameParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map (atom key → caller key). */
  events?: Record<string, string>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: Record<string, Record<string, unknown>>;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `RpgGame.traits.BattleStateBattleFlow`. */
export function stdRpgGameBattleStateBattleFlowTrait(params: StdRpgGameParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BattleStateBattleFlow`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config } : {}),
  });
}

/** Trait descriptor: `RpgGame.traits.BattleStateCombatLog`. */
export function stdRpgGameBattleStateCombatLogTrait(params: StdRpgGameParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BattleStateCombatLog`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config } : {}),
  });
}

/** Page descriptor: `RpgGame.pages.BattlePage`. */
export function stdRpgGamePage(params: StdRpgGameParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BattlePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor (4 orbitals). */
export function stdRpgGame(params: StdRpgGameParams): OrbitalDefinition[] {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
  };
  return [
    makeOrbitalWithUses({
      name: 'BattleStateOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.BattleStateBattleFlow`, linkedEntity: params.entityName }),
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.BattleStateCombatLog`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.BattlePage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'WorldZoneOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.WorldZoneNavigation`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.WorldPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'RpgItemOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.RpgItemBrowse`, linkedEntity: params.entityName }),
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.RpgItemAdd`, linkedEntity: params.entityName }),
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.RpgItemUse`, linkedEntity: params.entityName }),
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.RpgItemDrop`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.InventoryPage`, linkedEntity: params.entityName }),
      ],
    }),
    makeOrbitalWithUses({
      name: 'MissionOrbital',
      uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
      entity,
      traits: [
        makeTraitRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.traits.MissionTracking`, linkedEntity: params.entityName }),
      ],
      pages: [
        makePageRef({ from: BEHAVIOR_PATH, ref: `${ALIAS}.pages.QuestsPage`, linkedEntity: params.entityName }),
      ],
    }),
  ];
}
