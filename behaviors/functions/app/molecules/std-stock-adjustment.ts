/**
 * std-stock-adjustment — Tracks stock-level adjustments as deltas to onHand per sku/warehouse
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/molecules/std-stock-adjustment.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-stock-adjustment
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-stock-adjustment';
const ALIAS = 'StockAdjustment';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdStockAdjustmentEventKey = 'CREATE' | 'INIT' | 'REQUEST_DELETE' | 'StockAdjustmentLoadFailed' | 'StockAdjustmentLoaded';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdStockAdjustmentListenKey = 'ADJUSTMENT_CREATED' | 'ADJUSTMENT_DELETED';

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdStockAdjustmentRequestDeletePayload {
  id: string;
  row?: {
    id: string;
    stockLevelId: string;
    sku?: string;
    warehouseId?: string;
    warehouseName?: string;
    quantityDelta?: number;
    reason?: string;
    adjustedBy?: string;
    adjustedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `StockAdjustmentLoaded` event.
 */
export interface StdStockAdjustmentStockAdjustmentLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `StockAdjustmentLoadFailed` event.
 */
export interface StdStockAdjustmentStockAdjustmentLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-stock-adjustment descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdStockAdjustmentParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdStockAdjustmentEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `StockAdjustment.traits.StockAdjustmentBrowse`. */
export function stdStockAdjustmentStockAdjustmentBrowseTrait(params: StdStockAdjustmentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StockAdjustmentBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `StockAdjustment.traits.StockAdjustmentCreate`. */
export function stdStockAdjustmentStockAdjustmentCreateTrait(params: StdStockAdjustmentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StockAdjustmentCreate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `StockAdjustment.traits.StockAdjustmentDelete`. */
export function stdStockAdjustmentStockAdjustmentDeleteTrait(params: StdStockAdjustmentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StockAdjustmentDelete`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `StockAdjustment.traits.StockAdjustmentPersistor`. */
export function stdStockAdjustmentStockAdjustmentPersistorTrait(params: StdStockAdjustmentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.StockAdjustmentPersistor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `StockAdjustment.pages.StockAdjustmentPage`. */
export function stdStockAdjustmentPage(params: StdStockAdjustmentParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.StockAdjustmentPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdStockAdjustment(params: StdStockAdjustmentParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'StockAdjustmentOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdStockAdjustmentStockAdjustmentBrowseTrait(params),
      stdStockAdjustmentStockAdjustmentCreateTrait(params),
      stdStockAdjustmentStockAdjustmentDeleteTrait(params),
      stdStockAdjustmentStockAdjustmentPersistorTrait(params),
    ],
    pages: [
      stdStockAdjustmentPage(params),
    ],
  });
}
