/**
 * std-broadcast-builder — std-broadcast-builder composes EmailBroadcast + Segment + TemplateEditor + Modal + Confirmation into a unified broadcast-building UI
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/std-broadcast-builder.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-broadcast-builder
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-broadcast-builder';
const ALIAS = 'BroadcastBuilder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdBroadcastBuilderEventKey = 'BroadcastDraftLoadFailed' | 'BroadcastDraftLoaded' | 'CREATE' | 'INIT' | 'REQUEST_DELETE';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdBroadcastBuilderListenKey = 'DRAFT_CREATED' | 'DRAFT_DELETED';

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdBroadcastBuilderRequestDeletePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    segmentId?: string;
    segmentName?: string;
    templateId?: string;
    templateName?: string;
    subject?: string;
    scheduledAt?: string;
    status?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `BroadcastDraftLoaded` event.
 */
export interface StdBroadcastBuilderBroadcastDraftLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `BroadcastDraftLoadFailed` event.
 */
export interface StdBroadcastBuilderBroadcastDraftLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-broadcast-builder descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdBroadcastBuilderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdBroadcastBuilderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `BroadcastBuilder.traits.BroadcastDraftBrowse`. */
export function stdBroadcastBuilderBroadcastDraftBrowseTrait(params: StdBroadcastBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BroadcastDraftBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `BroadcastBuilder.traits.BroadcastDraftCreate`. */
export function stdBroadcastBuilderBroadcastDraftCreateTrait(params: StdBroadcastBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BroadcastDraftCreate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `BroadcastBuilder.traits.BroadcastDraftDelete`. */
export function stdBroadcastBuilderBroadcastDraftDeleteTrait(params: StdBroadcastBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BroadcastDraftDelete`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `BroadcastBuilder.traits.BroadcastDraftPersistor`. */
export function stdBroadcastBuilderBroadcastDraftPersistorTrait(params: StdBroadcastBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.BroadcastDraftPersistor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `BroadcastBuilder.pages.BroadcastBuilderPage`. */
export function stdBroadcastBuilderPage(params: StdBroadcastBuilderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.BroadcastBuilderPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdBroadcastBuilder(params: StdBroadcastBuilderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'BroadcastBuilderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdBroadcastBuilderBroadcastDraftBrowseTrait(params),
      stdBroadcastBuilderBroadcastDraftCreateTrait(params),
      stdBroadcastBuilderBroadcastDraftDeleteTrait(params),
      stdBroadcastBuilderBroadcastDraftPersistorTrait(params),
    ],
    pages: [
      stdBroadcastBuilderPage(params),
    ],
  });
}
