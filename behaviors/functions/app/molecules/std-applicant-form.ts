/**
 * std-applicant-form — std-applicant-form — unified applicant intake: capture applicant + first interview slot atomically.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/std-applicant-form.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-applicant-form
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-applicant-form';
const ALIAS = 'ApplicantForm';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdApplicantFormEventKey = 'ApplicationIntakeLoadFailed' | 'ApplicationIntakeLoaded' | 'CREATE' | 'INIT' | 'REQUEST_DELETE' | 'REQUEST_EDIT';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdApplicantFormListenKey = 'INTAKE_CREATED' | 'INTAKE_UPDATED' | 'INTAKE_DELETED';

/**
 * Payload shape for the `REQUEST_EDIT` event.
 */
export interface StdApplicantFormRequestEditPayload {
  id: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail?: string;
    applicantPhone?: string;
    jobId?: string;
    resumeUrl?: string;
    firstInterviewAt?: string;
    interviewerName?: string;
    interviewFormat?: string;
    status?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdApplicantFormRequestDeletePayload {
  id: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail?: string;
    applicantPhone?: string;
    jobId?: string;
    resumeUrl?: string;
    firstInterviewAt?: string;
    interviewerName?: string;
    interviewFormat?: string;
    status?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `ApplicationIntakeLoaded` event.
 */
export interface StdApplicantFormApplicationIntakeLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ApplicationIntakeLoadFailed` event.
 */
export interface StdApplicantFormApplicationIntakeLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-applicant-form descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdApplicantFormParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdApplicantFormEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ApplicantForm.traits.ApplicationIntakeBrowse`. */
export function stdApplicantFormApplicationIntakeBrowseTrait(params: StdApplicantFormParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationIntakeBrowse`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `ApplicantForm.traits.ApplicationIntakeCreate`. */
export function stdApplicantFormApplicationIntakeCreateTrait(params: StdApplicantFormParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationIntakeCreate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `ApplicantForm.traits.ApplicationIntakeEdit`. */
export function stdApplicantFormApplicationIntakeEditTrait(params: StdApplicantFormParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationIntakeEdit`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `ApplicantForm.traits.ApplicationIntakeDelete`. */
export function stdApplicantFormApplicationIntakeDeleteTrait(params: StdApplicantFormParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationIntakeDelete`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `ApplicantForm.traits.ApplicationIntakePersistor`. */
export function stdApplicantFormApplicationIntakePersistorTrait(params: StdApplicantFormParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationIntakePersistor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ApplicantForm.pages.ApplicationIntakePage`. */
export function stdApplicantFormPage(params: StdApplicantFormParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ApplicationIntakePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdApplicantForm(params: StdApplicantFormParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ApplicationIntakeOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdApplicantFormApplicationIntakeBrowseTrait(params),
      stdApplicantFormApplicationIntakeCreateTrait(params),
      stdApplicantFormApplicationIntakeEditTrait(params),
      stdApplicantFormApplicationIntakeDeleteTrait(params),
      stdApplicantFormApplicationIntakePersistorTrait(params),
    ],
    pages: [
      stdApplicantFormPage(params),
    ],
  });
}
