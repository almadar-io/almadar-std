/**
 * std-retail-pos — Retail POS: sales ledger, checkout terminal, receipts, and customer accounts. Pick for general non-restaurant retail.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/std-retail-pos.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-retail-pos
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-retail-pos';
const ALIAS = 'RetailPos';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRetailPosConfig {
  navItems?: TraitConfig;
  notifications?: TraitConfig;
}

/**
 * Tunable params for the SaleOrbital orbital.
 *
 * Canonical entity: Sale — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdRetailPosSaleOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'SaleAppLayout' | 'SaleSearch' | 'SaleFilter' | 'SaleStats' | 'SaleGraphs' | 'SaleBrowseList' | 'SaleCreate' | 'SaleEdit' | 'SaleView' | 'SaleDelete' | 'SaleCatalog' | 'SalePersistor',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the SaleOrbital orbital with consumer params. */
export function stdRetailPosSaleOrbital(params: StdRetailPosSaleOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'Sale';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'sales');
  const built = makeOrbitalWithUses({
    name: 'SaleOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-confirmation',
        'as': 'Confirmation',
      },
      {
        'from': 'std/behaviors/std-search',
        'as': 'Search',
      },
      {
        'from': 'std/behaviors/std-filter',
        'as': 'Filter',
      },
      {
        'from': 'std/behaviors/std-stats',
        'as': 'Stats',
      },
      {
        'from': 'std/behaviors/std-graphs',
        'as': 'Graphs',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'total',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'in-progress',
            'values': [
              'in-progress',
              'completed',
              'refunded',
              'voided',
            ],
          },
          {
            'name': 'paymentMethod',
            'type': 'string',
            'default': 'card',
            'values': [
              'cash',
              'card',
              'check',
              'digital',
            ],
          },
          {
            'name': 'customerId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'cashierId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'completedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'pendingId',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'SaleAppLayout',
        'config': {
          'navItems': [
            {
              'icon': 'shopping-bag',
              'label': 'Sales',
              'href': '/sales',
            },
            {
              'icon': 'credit-card',
              'href': '/checkout',
              'label': 'Checkout',
            },
            {
              'icon': 'receipt',
              'label': 'Receipts',
              'href': '/receipts',
            },
            {
              'label': 'Customers',
              'href': '/customers',
              'icon': 'users',
            },
          ],
          'notifications': [],
          'notificationClickEvent': 'SALE_NOTIFICATIONS_OPEN',
          'contentTrait': '@trait.SaleCatalog',
          'appName': 'Retail POS',
          'searchEvent': 'SALE_SEARCH',
        },
        'events': {
          'NOTIFY_CLICK': 'SALE_NOTIFICATIONS_OPEN',
          'SEARCH': 'SALE_SEARCH',
        },
      }),
      rebindInlineTraitEntity({
        'name': 'SaleCatalog',
        'category': 'interaction',
        'emits': [
          {
            'event': 'CREATE',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'source',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SALE_SEARCH',
            'triggers': 'SALE_SEARCH',
            'source': {
              'kind': 'trait',
              'trait': 'SaleAppLayout',
            },
          },
          {
            'event': 'SALE_NOTIFICATIONS_OPEN',
            'triggers': 'SALE_NOTIFICATIONS_OPEN',
            'source': {
              'kind': 'trait',
              'trait': 'SaleAppLayout',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'SALE_SEARCH',
              'name': 'Sale Search',
              'payloadSchema': [
                {
                  'name': 'value',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'SALE_NOTIFICATIONS_OPEN',
              'name': 'Sale Notifications Open',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'CREATE',
              'name': 'Create',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'type': 'stack',
                        'children': [
                          {
                            'type': 'stack',
                            'direction': 'horizontal',
                            'gap': 'sm',
                            'children': [
                              {
                                'name': 'shopping-bag',
                                'type': 'icon',
                              },
                              {
                                'type': 'typography',
                                'content': 'Sales',
                                'variant': 'h2',
                              },
                            ],
                            'align': 'center',
                          },
                          {
                            'gap': 'sm',
                            'type': 'stack',
                            'children': [
                              {
                                'variant': 'primary',
                                'type': 'button',
                                'icon': 'plus',
                                'label': 'New Sale',
                                'action': 'CREATE',
                              },
                            ],
                            'direction': 'horizontal',
                          },
                        ],
                        'justify': 'between',
                        'gap': 'md',
                        'direction': 'horizontal',
                        'align': 'center',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'gap': 'md',
                        'direction': 'horizontal',
                        'align': 'center',
                        'type': 'stack',
                        'children': [
                          '@trait.SaleSearch',
                          '@trait.SaleFilter',
                        ],
                      },
                      '@trait.SaleStats',
                      '@trait.SaleGraphs',
                      {
                        'type': 'divider',
                      },
                      '@trait.SaleBrowseList',
                    ],
                    'gap': 'lg',
                    'direction': 'vertical',
                    'type': 'stack',
                  },
                ],
              ],
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'SALE_SEARCH',
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'SALE_NOTIFICATIONS_OPEN',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'type': 'icon',
                        'name': 'bell',
                      },
                      {
                        'variant': 'h3',
                        'content': 'No notifications',
                        'type': 'typography',
                      },
                      {
                        'content': 'You\'re all caught up.',
                        'type': 'typography',
                        'variant': 'caption',
                        'color': 'muted',
                      },
                      {
                        'type': 'button',
                        'action': 'INIT',
                        'label': 'Back to sales',
                        'variant': 'ghost',
                      },
                    ],
                    'gap': 'md',
                    'direction': 'vertical',
                    'className': 'py-8',
                    'type': 'stack',
                    'align': 'center',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'Sale', canonicalName) as never,
      makeTraitRef({
        'ref': 'Search.traits.SearchResultSearch',
        'name': 'SaleSearch',
        'config': {
          'event': 'SALE_SEARCH',
          'placeholder': 'Search sales…',
        },
      }),
      makeTraitRef({
        'ref': 'Filter.traits.FilterTargetFilter',
        'name': 'SaleFilter',
        'config': {
          'filters': [
            {
              'options': [
                'in-progress',
                'completed',
                'refunded',
                'voided',
              ],
              'field': 'status',
              'label': 'Status',
              'filterType': 'select',
            },
            {
              'options': [
                'cash',
                'card',
                'check',
                'digital',
              ],
              'label': 'Payment',
              'filterType': 'select',
              'field': 'paymentMethod',
            },
          ],
          'event': 'SALE_FILTER',
        },
      }),
      makeTraitRef({
        'ref': 'Stats.traits.StatsItemStats',
        'name': 'SaleStats',
        'config': {
          'title': 'Sales',
          'metrics': [
            {
              'aggregation': 'count',
              'variant': 'primary',
              'label': 'Total',
              'icon': 'shopping-bag',
              'format': 'number',
            },
            {
              'aggregation': 'count',
              'label': 'Completed',
              'icon': 'check-circle',
              'variant': 'success',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'completed',
                ],
              ],
              'format': 'number',
            },
            {
              'icon': 'dollar-sign',
              'label': 'Revenue',
              'aggregation': 'sum',
              'format': 'currency',
              'variant': 'info',
              'field': 'total',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'completed',
                ],
              ],
            },
          ],
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'SaleBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Graphs.traits.GraphItemGraph',
        'name': 'SaleGraphs',
        'config': {
          'categoryField': 'paymentMethod',
          'showLegend': false,
          'title': 'Sales by Payment Method',
          'aggregation': 'count',
          'height': 240,
          'chartType': 'bar',
          'subtitle': 'Volume across payment types',
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'SaleBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'SaleBrowseList',
        'linkedEntity': canonicalName,
        'config': {
          'fields': [
            {
              'variant': 'h4',
              'name': 'id',
              'icon': 'shopping-bag',
            },
            {
              'name': 'status',
              'variant': 'badge',
            },
            {
              'variant': 'badge',
              'name': 'paymentMethod',
            },
            {
              'variant': 'caption',
              'name': 'total',
            },
            {
              'name': 'completedAt',
              'variant': 'caption',
            },
          ],
          'itemActions': [
            {
              'label': 'View',
              'event': 'VIEW',
              'variant': 'ghost',
            },
            {
              'variant': 'ghost',
              'event': 'EDIT',
              'label': 'Edit',
            },
            {
              'label': 'Delete',
              'event': 'DELETE',
              'variant': 'danger',
            },
          ],
          'cols': 1,
          'gap': 'sm',
        },
        'listens': [
          {
            'event': 'SEARCH',
            'triggers': 'REFETCH_QUERY',
            'source': {
              'kind': 'trait',
              'trait': 'SaleSearch',
            },
          },
          {
            'event': 'FILTER',
            'triggers': 'REFETCH_FILTER',
            'source': {
              'kind': 'trait',
              'trait': 'SaleFilter',
            },
          },
          {
            'event': 'SALE_CREATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'SalePersistor',
            },
          },
          {
            'event': 'SALE_UPDATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'SalePersistor',
            },
          },
          {
            'event': 'SALE_DELETED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'SalePersistor',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'SaleCreate',
        'linkedEntity': canonicalName,
        'config': {
          'mode': 'create',
          'fields': [
            'total',
            'status',
            'paymentMethod',
            'customerId',
            'cashierId',
          ],
          'title': 'New Sale',
          'icon': 'plus-circle',
        },
        'events': {
          'OPEN': 'CREATE',
        },
        'listens': [
          {
            'event': 'CREATE',
            'triggers': 'CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'SaleCatalog',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'SaleEdit',
        'linkedEntity': canonicalName,
        'config': {
          'mode': 'edit',
          'icon': 'edit',
          'fields': [
            'total',
            'status',
            'paymentMethod',
            'customerId',
            'cashierId',
            'completedAt',
          ],
          'title': 'Edit Sale',
        },
        'events': {
          'OPEN': 'EDIT',
        },
        'listens': [
          {
            'event': 'EDIT',
            'triggers': 'EDIT',
            'source': {
              'kind': 'trait',
              'trait': 'SaleBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'SaleView',
        'linkedEntity': canonicalName,
        'config': {
          'fields': [
            'total',
            'status',
            'paymentMethod',
            'customerId',
            'cashierId',
            'completedAt',
          ],
          'icon': 'eye',
          'title': 'View Sale',
          'mode': 'edit',
        },
        'events': {
          'OPEN': 'VIEW',
        },
        'listens': [
          {
            'event': 'VIEW',
            'triggers': 'VIEW',
            'source': {
              'kind': 'trait',
              'trait': 'SaleBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Confirmation.traits.ConfirmActionConfirmation',
        'name': 'SaleDelete',
        'linkedEntity': canonicalName,
        'config': {
          'confirmLabel': 'Delete',
          'icon': 'alert-triangle',
          'title': 'Delete Sale',
          'alertMessage': 'This action cannot be undone.',
        },
        'events': {
          'CONFIRM': 'CONFIRM_DELETE',
          'REQUEST': 'DELETE',
        },
        'listens': [
          {
            'event': 'DELETE',
            'triggers': 'DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'SaleBrowseList',
            },
          },
        ],
      }),
      rebindInlineTraitEntity({
        'name': 'SalePersistor',
        'category': 'lifecycle',
        'linkedEntity': 'Sale',
        'emits': [
          {
            'event': 'SALE_CREATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'SALE_UPDATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'SALE_DELETED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SAVE',
            'triggers': 'DO_CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'SaleCreate',
            },
          },
          {
            'event': 'SAVE',
            'triggers': 'DO_UPDATE',
            'source': {
              'kind': 'trait',
              'trait': 'SaleEdit',
            },
          },
          {
            'event': 'CONFIRM_DELETE',
            'triggers': 'DO_DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'SaleDelete',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'DO_CREATE',
              'name': 'Do Create',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_UPDATE',
              'name': 'Do Update',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_DELETE',
              'name': 'Do Delete',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'SALE_CREATED',
              'name': 'Sale Created',
            },
            {
              'key': 'SALE_UPDATED',
              'name': 'Sale Updated',
            },
            {
              'key': 'SALE_DELETED',
              'name': 'Sale Deleted',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_CREATE',
              'effects': [
                [
                  'persist',
                  'create',
                  'Sale',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'SALE_CREATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_UPDATE',
              'effects': [
                [
                  'persist',
                  'update',
                  'Sale',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'SALE_UPDATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_DELETE',
              'effects': [
                [
                  'persist',
                  'delete',
                  'Sale',
                  '@payload.id',
                  {
                    'emit': {
                      'success': 'SALE_DELETED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'Sale', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'SalesPage',
        'path': '/sales',
        'traits': [
          {
            'ref': 'SaleAppLayout',
          },
          {
            'ref': 'SaleCatalog',
          },
          {
            'ref': 'SaleSearch',
          },
          {
            'ref': 'SaleFilter',
          },
          {
            'ref': 'SaleStats',
          },
          {
            'ref': 'SaleGraphs',
          },
          {
            'ref': 'SaleBrowseList',
          },
          {
            'ref': 'SaleCreate',
          },
          {
            'ref': 'SaleEdit',
          },
          {
            'ref': 'SaleView',
          },
          {
            'ref': 'SaleDelete',
          },
          {
            'ref': 'SalePersistor',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdRetailPosSaleOrbital. */
export const StdRetailPosSaleOrbitalManifest = {
  organism: 'std-retail-pos',
  orbitalName: 'SaleOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'SaleAppLayout',
    'SaleSearch',
    'SaleFilter',
    'SaleStats',
    'SaleGraphs',
    'SaleBrowseList',
    'SaleCreate',
    'SaleEdit',
    'SaleView',
    'SaleDelete',
  ] as const,
  inlineTraitNames: [
    'SaleCatalog',
    'SalePersistor',
  ] as const,
};

/** Typed guard — runtime validates StdRetailPosSaleOrbitalParams keys. */
export function isStdRetailPosSaleOrbitalParams(p: object): p is StdRetailPosSaleOrbitalParams {
  type _OverrideRecord = NonNullable<StdRetailPosSaleOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdRetailPosSaleOrbitalManifest.traitNames,
      ...StdRetailPosSaleOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the CheckoutOrbital orbital.
 *
 * Canonical entity: CheckoutSession — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdRetailPosCheckoutOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CheckoutAppLayout' | 'CheckoutTerminalBoard',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the CheckoutOrbital orbital with consumer params. */
export function stdRetailPosCheckoutOrbital(params: StdRetailPosCheckoutOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CheckoutSession';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'checkoutsessions');
  const built = makeOrbitalWithUses({
    name: 'CheckoutOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-checkout-terminal',
        'as': 'CheckoutTerminal',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'terminalId',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'cashierId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'cashierName',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'cartItemsJson',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'subtotal',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'tax',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'total',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'paymentMethod',
            'type': 'string',
            'default': 'card',
            'values': [
              'cash',
              'card',
              'gift_card',
              'split',
            ],
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'open',
            'values': [
              'open',
              'paying',
              'completed',
              'voided',
            ],
          },
          {
            'name': 'openedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'completedAt',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'CheckoutAppLayout',
        'config': {
          'appName': 'Retail POS',
          'notifications': [],
          'notificationClickEvent': 'CHECKOUT_NOTIFICATIONS_OPEN',
          'searchEvent': 'CHECKOUT_SEARCH',
          'contentTrait': '@trait.CheckoutTerminalBoard',
          'navItems': [
            {
              'href': '/sales',
              'icon': 'shopping-bag',
              'label': 'Sales',
            },
            {
              'icon': 'credit-card',
              'label': 'Checkout',
              'href': '/checkout',
            },
            {
              'icon': 'receipt',
              'label': 'Receipts',
              'href': '/receipts',
            },
            {
              'label': 'Customers',
              'href': '/customers',
              'icon': 'users',
            },
          ],
        },
        'events': {
          'SEARCH': 'CHECKOUT_SEARCH',
          'NOTIFY_CLICK': 'CHECKOUT_NOTIFICATIONS_OPEN',
        },
      }),
      makeTraitRef({
        'ref': 'CheckoutTerminal.traits.CheckoutSessionTerminal',
        'name': 'CheckoutTerminalBoard',
        'config': {
          'title': 'Open Terminals',
        },
      }),
    ],
    pages: [
      {
        'name': 'CheckoutPage',
        'path': '/checkout',
        'traits': [
          {
            'ref': 'CheckoutAppLayout',
          },
          {
            'ref': 'CheckoutTerminalBoard',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdRetailPosCheckoutOrbital. */
export const StdRetailPosCheckoutOrbitalManifest = {
  organism: 'std-retail-pos',
  orbitalName: 'CheckoutOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'CheckoutAppLayout',
    'CheckoutTerminalBoard',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdRetailPosCheckoutOrbitalParams keys. */
export function isStdRetailPosCheckoutOrbitalParams(p: object): p is StdRetailPosCheckoutOrbitalParams {
  type _OverrideRecord = NonNullable<StdRetailPosCheckoutOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdRetailPosCheckoutOrbitalManifest.traitNames,
      ...StdRetailPosCheckoutOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the ReceiptOrbital orbital.
 *
 * Canonical entity: Receipt — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdRetailPosReceiptOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ReceiptAppLayout' | 'ReceiptLedger',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the ReceiptOrbital orbital with consumer params. */
export function stdRetailPosReceiptOrbital(params: StdRetailPosReceiptOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'Receipt';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'receipts');
  const built = makeOrbitalWithUses({
    name: 'ReceiptOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-receipt',
        'as': 'Receipt',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'receiptNumber',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'recipientName',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'recipientEmail',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'transactionId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'transactionType',
            'type': 'string',
            'default': 'purchase',
            'values': [
              'purchase',
              'refund',
              'donation',
              'service',
            ],
          },
          {
            'name': 'subtotal',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'tax',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'total',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'lineItemsJson',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'issuedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'isEmailed',
            'type': 'boolean',
            'default': false,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'ReceiptAppLayout',
        'config': {
          'searchEvent': 'RECEIPT_SEARCH',
          'navItems': [
            {
              'href': '/sales',
              'icon': 'shopping-bag',
              'label': 'Sales',
            },
            {
              'label': 'Checkout',
              'icon': 'credit-card',
              'href': '/checkout',
            },
            {
              'icon': 'receipt',
              'label': 'Receipts',
              'href': '/receipts',
            },
            {
              'label': 'Customers',
              'href': '/customers',
              'icon': 'users',
            },
          ],
          'contentTrait': '@trait.ReceiptLedger',
          'appName': 'Retail POS',
          'notificationClickEvent': 'RECEIPT_NOTIFICATIONS_OPEN',
          'notifications': [],
        },
        'events': {
          'SEARCH': 'RECEIPT_SEARCH',
          'NOTIFY_CLICK': 'RECEIPT_NOTIFICATIONS_OPEN',
        },
      }),
      makeTraitRef({
        'ref': 'Receipt.traits.ReceiptIssuer',
        'name': 'ReceiptLedger',
        'config': {
          'title': 'Receipts',
        },
      }),
    ],
    pages: [
      {
        'name': 'ReceiptsPage',
        'path': '/receipts',
        'traits': [
          {
            'ref': 'ReceiptAppLayout',
          },
          {
            'ref': 'ReceiptLedger',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdRetailPosReceiptOrbital. */
export const StdRetailPosReceiptOrbitalManifest = {
  organism: 'std-retail-pos',
  orbitalName: 'ReceiptOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'ReceiptAppLayout',
    'ReceiptLedger',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdRetailPosReceiptOrbitalParams keys. */
export function isStdRetailPosReceiptOrbitalParams(p: object): p is StdRetailPosReceiptOrbitalParams {
  type _OverrideRecord = NonNullable<StdRetailPosReceiptOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdRetailPosReceiptOrbitalManifest.traitNames,
      ...StdRetailPosReceiptOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the CustomerOrbital orbital.
 *
 * Canonical entity: CustomerAccount — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdRetailPosCustomerOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CustomerAppLayout' | 'CustomerDirectory',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the CustomerOrbital orbital with consumer params. */
export function stdRetailPosCustomerOrbital(params: StdRetailPosCustomerOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CustomerAccount';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'customeraccounts');
  const built = makeOrbitalWithUses({
    name: 'CustomerOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-customer-account',
        'as': 'CustomerAccount',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'name',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'email',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'phone',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'loyaltyPoints',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'storeCreditAmount',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'totalSpent',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'joinedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'lastVisitAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'active',
            'values': [
              'active',
              'inactive',
              'banned',
            ],
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'CustomerAppLayout',
        'config': {
          'contentTrait': '@trait.CustomerDirectory',
          'appName': 'Retail POS',
          'searchEvent': 'CUSTOMER_SEARCH',
          'notifications': [],
          'notificationClickEvent': 'CUSTOMER_NOTIFICATIONS_OPEN',
          'navItems': [
            {
              'href': '/sales',
              'icon': 'shopping-bag',
              'label': 'Sales',
            },
            {
              'href': '/checkout',
              'label': 'Checkout',
              'icon': 'credit-card',
            },
            {
              'icon': 'receipt',
              'label': 'Receipts',
              'href': '/receipts',
            },
            {
              'label': 'Customers',
              'href': '/customers',
              'icon': 'users',
            },
          ],
        },
        'events': {
          'SEARCH': 'CUSTOMER_SEARCH',
          'NOTIFY_CLICK': 'CUSTOMER_NOTIFICATIONS_OPEN',
        },
      }),
      makeTraitRef({
        'ref': 'CustomerAccount.traits.CustomerAccountDirectory',
        'name': 'CustomerDirectory',
        'config': {
          'title': 'Customers',
        },
      }),
    ],
    pages: [
      {
        'name': 'CustomersPage',
        'path': '/customers',
        'traits': [
          {
            'ref': 'CustomerAppLayout',
          },
          {
            'ref': 'CustomerDirectory',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdRetailPosCustomerOrbital. */
export const StdRetailPosCustomerOrbitalManifest = {
  organism: 'std-retail-pos',
  orbitalName: 'CustomerOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'CustomerAppLayout',
    'CustomerDirectory',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Typed guard — runtime validates StdRetailPosCustomerOrbitalParams keys. */
export function isStdRetailPosCustomerOrbitalParams(p: object): p is StdRetailPosCustomerOrbitalParams {
  type _OverrideRecord = NonNullable<StdRetailPosCustomerOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdRetailPosCustomerOrbitalManifest.traitNames,
      ...StdRetailPosCustomerOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-retail-pos — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdRetailPosParams {
  Sale?: StdRetailPosSaleOrbitalParams;
  Checkout?: StdRetailPosCheckoutOrbitalParams;
  Receipt?: StdRetailPosReceiptOrbitalParams;
  Customer?: StdRetailPosCustomerOrbitalParams;
}

/** Whole-organism descriptor (4 orbitals). Composes per-orbital factories. */
export function stdRetailPos(params: StdRetailPosParams = {}): OrbitalDefinition[] {
  return [
    stdRetailPosSaleOrbital(params.Sale ?? {}),
    stdRetailPosCheckoutOrbital(params.Checkout ?? {}),
    stdRetailPosReceiptOrbital(params.Receipt ?? {}),
    stdRetailPosCustomerOrbital(params.Customer ?? {}),
  ];
}
