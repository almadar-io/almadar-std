/**
 * std-fitness-studio — Fitness studio: member directory, recurring group-class scheduling, tiered memberships, and class rosters. Pick for gym/yoga/dance studio apps (Mindbody pattern).
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-fitness-studio.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-fitness-studio
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-fitness-studio';
const ALIAS = 'FitnessStudio';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdFitnessStudioConfig {
  navItems?: TraitConfig;
  notifications?: TraitConfig;
}

/**
 * Tunable params for the MemberOrbital orbital.
 *
 * Canonical entity: Member — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdFitnessStudioMemberOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MemberAppLayout' | 'MemberSearch' | 'MemberFilter' | 'MemberStats' | 'MemberGraphs' | 'MemberBrowseList' | 'MemberCreate' | 'MemberEdit' | 'MemberView' | 'MemberDelete',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the MemberOrbital orbital with consumer params. */
export function stdFitnessStudioMemberOrbital(params: StdFitnessStudioMemberOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'Member';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'members');
  const built = makeOrbitalWithUses({
    name: 'MemberOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-confirmation',
        'as': 'Confirmation',
      },
      {
        'from': 'std/behaviors/std-search',
        'as': 'Search',
      },
      {
        'from': 'std/behaviors/std-filter',
        'as': 'Filter',
      },
      {
        'from': 'std/behaviors/std-stats',
        'as': 'Stats',
      },
      {
        'from': 'std/behaviors/std-graphs',
        'as': 'Graphs',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'name',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'email',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'joinedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'membershipTier',
            'type': 'string',
            'default': 'basic',
            'values': [
              'basic',
              'premium',
              'unlimited',
            ],
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'active',
            'values': [
              'active',
              'paused',
              'cancelled',
            ],
          },
          {
            'name': 'pendingId',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'MemberAppLayout',
        'config': {
          'notificationClickEvent': 'MEMBER_NOTIFICATIONS_OPEN',
          'navItems': [
            {
              'icon': 'users',
              'label': 'Members',
              'href': '/members',
            },
            {
              'icon': 'calendar',
              'href': '/classes',
              'label': 'Classes',
            },
            {
              'icon': 'credit-card',
              'label': 'Memberships',
              'href': '/memberships',
            },
            {
              'icon': 'clipboard-list',
              'label': 'Rosters',
              'href': '/rosters',
            },
          ],
          'notifications': [],
          'searchEvent': 'MEMBER_SEARCH',
          'contentTrait': '@trait.MemberCatalog',
          'appName': 'Fitness Studio',
        },
        'events': {
          'NOTIFY_CLICK': 'MEMBER_NOTIFICATIONS_OPEN',
          'SEARCH': 'MEMBER_SEARCH',
        },
      }),
      {
        'name': 'MemberCatalog',
        'category': 'interaction',
        'emits': [
          {
            'event': 'CREATE',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'source',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'MEMBER_SEARCH',
            'triggers': 'MEMBER_SEARCH',
            'source': {
              'kind': 'trait',
              'trait': 'MemberAppLayout',
            },
          },
          {
            'event': 'MEMBER_NOTIFICATIONS_OPEN',
            'triggers': 'MEMBER_NOTIFICATIONS_OPEN',
            'source': {
              'kind': 'trait',
              'trait': 'MemberAppLayout',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'MEMBER_SEARCH',
              'name': 'Member Search',
              'payloadSchema': [
                {
                  'name': 'value',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MEMBER_NOTIFICATIONS_OPEN',
              'name': 'Member Notifications Open',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'CREATE',
              'name': 'Create',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'direction': 'vertical',
                    'type': 'stack',
                    'children': [
                      {
                        'align': 'center',
                        'direction': 'horizontal',
                        'justify': 'between',
                        'gap': 'md',
                        'children': [
                          {
                            'gap': 'sm',
                            'direction': 'horizontal',
                            'type': 'stack',
                            'children': [
                              {
                                'name': 'users',
                                'type': 'icon',
                              },
                              {
                                'type': 'typography',
                                'variant': 'h2',
                                'content': 'Members',
                              },
                            ],
                            'align': 'center',
                          },
                          {
                            'type': 'stack',
                            'direction': 'horizontal',
                            'gap': 'sm',
                            'children': [
                              {
                                'action': 'CREATE',
                                'type': 'button',
                                'variant': 'primary',
                                'label': 'New Member',
                                'icon': 'user-plus',
                              },
                            ],
                          },
                        ],
                        'type': 'stack',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'children': [
                          '@trait.MemberSearch',
                          '@trait.MemberFilter',
                        ],
                        'align': 'center',
                        'type': 'stack',
                        'direction': 'horizontal',
                        'gap': 'md',
                      },
                      '@trait.MemberStats',
                      '@trait.MemberGraphs',
                      {
                        'type': 'divider',
                      },
                      '@trait.MemberBrowseList',
                    ],
                    'gap': 'lg',
                  },
                ],
              ],
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MEMBER_SEARCH',
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MEMBER_NOTIFICATIONS_OPEN',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'align': 'center',
                    'children': [
                      {
                        'name': 'bell',
                        'type': 'icon',
                      },
                      {
                        'type': 'typography',
                        'variant': 'h3',
                        'content': 'No notifications',
                      },
                      {
                        'variant': 'caption',
                        'type': 'typography',
                        'content': 'You\'re all caught up.',
                        'color': 'muted',
                      },
                      {
                        'label': 'Back to members',
                        'variant': 'ghost',
                        'type': 'button',
                        'action': 'INIT',
                      },
                    ],
                    'className': 'py-8',
                    'gap': 'md',
                    'type': 'stack',
                    'direction': 'vertical',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
      makeTraitRef({
        'ref': 'Search.traits.SearchResultSearch',
        'name': 'MemberSearch',
        'config': {
          'placeholder': 'Search members…',
          'event': 'MEMBER_SEARCH',
        },
      }),
      makeTraitRef({
        'ref': 'Filter.traits.FilterTargetFilter',
        'name': 'MemberFilter',
        'config': {
          'filters': [
            {
              'field': 'membershipTier',
              'options': [
                'basic',
                'premium',
                'unlimited',
              ],
              'filterType': 'select',
              'label': 'Tier',
            },
            {
              'label': 'Status',
              'options': [
                'active',
                'paused',
                'cancelled',
              ],
              'filterType': 'select',
              'field': 'status',
            },
          ],
          'event': 'MEMBER_FILTER',
        },
      }),
      makeTraitRef({
        'ref': 'Stats.traits.StatsItemStats',
        'name': 'MemberStats',
        'config': {
          'metrics': [
            {
              'variant': 'primary',
              'format': 'number',
              'aggregation': 'count',
              'label': 'Total',
              'icon': 'users',
            },
            {
              'icon': 'user-check',
              'aggregation': 'count',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'active',
                ],
              ],
              'format': 'number',
              'variant': 'success',
              'label': 'Active',
            },
            {
              'aggregation': 'count',
              'variant': 'warning',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'paused',
                ],
              ],
              'format': 'number',
              'label': 'Paused',
              'icon': 'pause-circle',
            },
            {
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'cancelled',
                ],
              ],
              'icon': 'user-x',
              'aggregation': 'count',
              'label': 'Cancelled',
              'variant': 'danger',
              'format': 'number',
            },
          ],
          'title': 'Members',
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'MemberBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Graphs.traits.GraphItemGraph',
        'name': 'MemberGraphs',
        'config': {
          'title': 'Members by Tier',
          'chartType': 'bar',
          'height': 240,
          'showLegend': false,
          'subtitle': 'Distribution across membership tiers',
          'aggregation': 'count',
          'categoryField': 'membershipTier',
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'MemberBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'MemberBrowseList',
        'linkedEntity': canonicalName,
        'config': {
          'fields': [
            {
              'name': 'name',
              'icon': 'user',
              'variant': 'h4',
            },
            {
              'name': 'email',
              'variant': 'body',
            },
            {
              'name': 'membershipTier',
              'variant': 'badge',
            },
            {
              'name': 'status',
              'variant': 'badge',
            },
            {
              'variant': 'caption',
              'name': 'joinedAt',
            },
          ],
          'cols': 1,
          'gap': 'sm',
          'itemActions': [
            {
              'event': 'VIEW',
              'variant': 'ghost',
              'label': 'View',
            },
            {
              'label': 'Edit',
              'event': 'EDIT',
              'variant': 'ghost',
            },
            {
              'label': 'Delete',
              'event': 'DELETE',
              'variant': 'danger',
            },
          ],
        },
        'listens': [
          {
            'event': 'SEARCH',
            'triggers': 'REFETCH_QUERY',
            'source': {
              'kind': 'trait',
              'trait': 'MemberSearch',
            },
          },
          {
            'event': 'FILTER',
            'triggers': 'REFETCH_FILTER',
            'source': {
              'kind': 'trait',
              'trait': 'MemberFilter',
            },
          },
          {
            'event': 'MEMBER_CREATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MemberPersistor',
            },
          },
          {
            'event': 'MEMBER_UPDATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MemberPersistor',
            },
          },
          {
            'event': 'MEMBER_DELETED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MemberPersistor',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MemberCreate',
        'linkedEntity': canonicalName,
        'config': {
          'fields': [
            'name',
            'email',
            'membershipTier',
            'status',
            'joinedAt',
          ],
          'icon': 'user-plus',
          'title': 'New Member',
          'mode': 'create',
        },
        'events': {
          'OPEN': 'CREATE',
        },
        'listens': [
          {
            'event': 'CREATE',
            'triggers': 'CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MemberCatalog',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MemberEdit',
        'linkedEntity': canonicalName,
        'config': {
          'mode': 'edit',
          'fields': [
            'name',
            'email',
            'membershipTier',
            'status',
            'joinedAt',
          ],
          'icon': 'edit',
          'title': 'Edit Member',
        },
        'events': {
          'OPEN': 'EDIT',
        },
        'listens': [
          {
            'event': 'EDIT',
            'triggers': 'EDIT',
            'source': {
              'kind': 'trait',
              'trait': 'MemberBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MemberView',
        'linkedEntity': canonicalName,
        'config': {
          'title': 'View Member',
          'icon': 'eye',
          'mode': 'edit',
          'fields': [
            'name',
            'email',
            'membershipTier',
            'status',
            'joinedAt',
          ],
        },
        'events': {
          'OPEN': 'VIEW',
        },
        'listens': [
          {
            'event': 'VIEW',
            'triggers': 'VIEW',
            'source': {
              'kind': 'trait',
              'trait': 'MemberBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Confirmation.traits.ConfirmActionConfirmation',
        'name': 'MemberDelete',
        'linkedEntity': canonicalName,
        'config': {
          'icon': 'alert-triangle',
          'title': 'Cancel Membership',
          'alertMessage': 'This action cannot be undone.',
          'confirmLabel': 'Delete',
        },
        'events': {
          'REQUEST': 'DELETE',
          'CONFIRM': 'CONFIRM_DELETE',
        },
        'listens': [
          {
            'event': 'DELETE',
            'triggers': 'DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MemberBrowseList',
            },
          },
        ],
      }),
      {
        'name': 'MemberPersistor',
        'category': 'lifecycle',
        'linkedEntity': 'Member',
        'emits': [
          {
            'event': 'MEMBER_CREATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MEMBER_UPDATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MEMBER_DELETED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SAVE',
            'triggers': 'DO_CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MemberCreate',
            },
          },
          {
            'event': 'SAVE',
            'triggers': 'DO_UPDATE',
            'source': {
              'kind': 'trait',
              'trait': 'MemberEdit',
            },
          },
          {
            'event': 'CONFIRM_DELETE',
            'triggers': 'DO_DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MemberDelete',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'DO_CREATE',
              'name': 'Do Create',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_UPDATE',
              'name': 'Do Update',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_DELETE',
              'name': 'Do Delete',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MEMBER_CREATED',
              'name': 'Member Created',
            },
            {
              'key': 'MEMBER_UPDATED',
              'name': 'Member Updated',
            },
            {
              'key': 'MEMBER_DELETED',
              'name': 'Member Deleted',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_CREATE',
              'effects': [
                [
                  'persist',
                  'create',
                  'Member',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MEMBER_CREATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_UPDATE',
              'effects': [
                [
                  'persist',
                  'update',
                  'Member',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MEMBER_UPDATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_DELETE',
              'effects': [
                [
                  'persist',
                  'delete',
                  'Member',
                  '@payload.id',
                  {
                    'emit': {
                      'success': 'MEMBER_DELETED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'MembersPage',
        'path': '/members',
        'traits': [
          {
            'ref': 'MemberAppLayout',
          },
          {
            'ref': 'MemberCatalog',
          },
          {
            'ref': 'MemberSearch',
          },
          {
            'ref': 'MemberFilter',
          },
          {
            'ref': 'MemberStats',
          },
          {
            'ref': 'MemberGraphs',
          },
          {
            'ref': 'MemberBrowseList',
          },
          {
            'ref': 'MemberCreate',
          },
          {
            'ref': 'MemberEdit',
          },
          {
            'ref': 'MemberView',
          },
          {
            'ref': 'MemberDelete',
          },
          {
            'ref': 'MemberPersistor',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdFitnessStudioMemberOrbital. */
export const StdFitnessStudioMemberOrbitalManifest = {
  organism: 'std-fitness-studio',
  orbitalName: 'MemberOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MemberAppLayout',
    'MemberSearch',
    'MemberFilter',
    'MemberStats',
    'MemberGraphs',
    'MemberBrowseList',
    'MemberCreate',
    'MemberEdit',
    'MemberView',
    'MemberDelete',
  ] as const,
  inlineTraitNames: [
    'MemberCatalog',
    'MemberPersistor',
  ] as const,
};

/** Typed guard — runtime validates StdFitnessStudioMemberOrbitalParams keys. */
export function isStdFitnessStudioMemberOrbitalParams(p: object): p is StdFitnessStudioMemberOrbitalParams {
  type _OverrideRecord = NonNullable<StdFitnessStudioMemberOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdFitnessStudioMemberOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the ClassSessionOrbital orbital.
 *
 * Canonical entity: ClassSessionRow — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdFitnessStudioClassSessionOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ClassSessionAppLayout' | 'ClassSessionSchedule',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the ClassSessionOrbital orbital with consumer params. */
export function stdFitnessStudioClassSessionOrbital(params: StdFitnessStudioClassSessionOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'ClassSessionRow';
  const built = makeOrbitalWithUses({
    name: 'ClassSessionOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-class-session',
        'as': 'ClassSession',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'ClassSessionAppLayout',
        'config': {
          'contentTrait': '@trait.ClassSessionDisplay',
          'navItems': [
            {
              'label': 'Members',
              'href': '/members',
              'icon': 'users',
            },
            {
              'href': '/classes',
              'icon': 'calendar',
              'label': 'Classes',
            },
            {
              'icon': 'credit-card',
              'href': '/memberships',
              'label': 'Memberships',
            },
            {
              'label': 'Rosters',
              'href': '/rosters',
              'icon': 'clipboard-list',
            },
          ],
          'notificationClickEvent': 'CLASS_SESSION_NOTIFICATIONS_OPEN',
          'appName': 'Fitness Studio',
          'notifications': [],
          'searchEvent': 'CLASS_SESSION_SEARCH',
        },
        'events': {
          'NOTIFY_CLICK': 'CLASS_SESSION_NOTIFICATIONS_OPEN',
          'SEARCH': 'CLASS_SESSION_SEARCH',
        },
      }),
      {
        'name': 'ClassSessionDisplay',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'children': [
                      {
                        'align': 'center',
                        'type': 'stack',
                        'gap': 'sm',
                        'children': [
                          {
                            'name': 'calendar',
                            'type': 'icon',
                          },
                          {
                            'type': 'typography',
                            'content': 'Class Schedule',
                            'variant': 'h2',
                          },
                        ],
                        'direction': 'horizontal',
                      },
                      {
                        'color': 'muted',
                        'variant': 'caption',
                        'content': 'Group classes for the week. Start a class to open its roster; cancel to notify enrolled members.',
                        'type': 'typography',
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.ClassSessionSchedule',
                    ],
                    'direction': 'vertical',
                    'gap': 'lg',
                    'type': 'stack',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
      makeTraitRef({
        'ref': 'ClassSession.traits.ClassSessionSchedule',
        'name': 'ClassSessionSchedule',
        'config': {
          'title': 'Upcoming Classes',
        },
      }),
    ],
    pages: [
      {
        'name': 'ClassSessionsPage',
        'path': '/classes',
        'traits': [
          {
            'ref': 'ClassSessionAppLayout',
          },
          {
            'ref': 'ClassSessionDisplay',
          },
          {
            'ref': 'ClassSessionSchedule',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdFitnessStudioClassSessionOrbital. */
export const StdFitnessStudioClassSessionOrbitalManifest = {
  organism: 'std-fitness-studio',
  orbitalName: 'ClassSessionOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'ClassSessionAppLayout',
    'ClassSessionSchedule',
  ] as const,
  inlineTraitNames: [
    'ClassSessionDisplay',
  ] as const,
};

/** Typed guard — runtime validates StdFitnessStudioClassSessionOrbitalParams keys. */
export function isStdFitnessStudioClassSessionOrbitalParams(p: object): p is StdFitnessStudioClassSessionOrbitalParams {
  type _OverrideRecord = NonNullable<StdFitnessStudioClassSessionOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdFitnessStudioClassSessionOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the MembershipOrbital orbital.
 *
 * Canonical entity: MembershipRow — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdFitnessStudioMembershipOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MembershipAppLayout' | 'MembershipDirectory',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the MembershipOrbital orbital with consumer params. */
export function stdFitnessStudioMembershipOrbital(params: StdFitnessStudioMembershipOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'MembershipRow';
  const built = makeOrbitalWithUses({
    name: 'MembershipOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-membership',
        'as': 'Membership',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'MembershipAppLayout',
        'config': {
          'notifications': [],
          'searchEvent': 'MEMBERSHIP_SEARCH',
          'appName': 'Fitness Studio',
          'notificationClickEvent': 'MEMBERSHIP_NOTIFICATIONS_OPEN',
          'contentTrait': '@trait.MembershipDisplay',
          'navItems': [
            {
              'icon': 'users',
              'href': '/members',
              'label': 'Members',
            },
            {
              'label': 'Classes',
              'href': '/classes',
              'icon': 'calendar',
            },
            {
              'href': '/memberships',
              'label': 'Memberships',
              'icon': 'credit-card',
            },
            {
              'href': '/rosters',
              'label': 'Rosters',
              'icon': 'clipboard-list',
            },
          ],
        },
        'events': {
          'NOTIFY_CLICK': 'MEMBERSHIP_NOTIFICATIONS_OPEN',
          'SEARCH': 'MEMBERSHIP_SEARCH',
        },
      }),
      {
        'name': 'MembershipDisplay',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'stack',
                    'gap': 'lg',
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'children': [
                      {
                        'direction': 'horizontal',
                        'align': 'center',
                        'children': [
                          {
                            'type': 'icon',
                            'name': 'id-card',
                          },
                          {
                            'content': 'Memberships',
                            'type': 'typography',
                            'variant': 'h2',
                          },
                        ],
                        'type': 'stack',
                        'gap': 'sm',
                      },
                      {
                        'type': 'typography',
                        'variant': 'caption',
                        'color': 'muted',
                        'content': 'Recurring subscriptions per member. Upgrade tiers, freeze, or cancel from the row actions.',
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.MembershipDirectory',
                    ],
                    'direction': 'vertical',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
      makeTraitRef({
        'ref': 'Membership.traits.MembershipDirectory',
        'name': 'MembershipDirectory',
        'config': {
          'title': 'Active Memberships',
        },
      }),
    ],
    pages: [
      {
        'name': 'MembershipsPage',
        'path': '/memberships',
        'traits': [
          {
            'ref': 'MembershipAppLayout',
          },
          {
            'ref': 'MembershipDisplay',
          },
          {
            'ref': 'MembershipDirectory',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdFitnessStudioMembershipOrbital. */
export const StdFitnessStudioMembershipOrbitalManifest = {
  organism: 'std-fitness-studio',
  orbitalName: 'MembershipOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MembershipAppLayout',
    'MembershipDirectory',
  ] as const,
  inlineTraitNames: [
    'MembershipDisplay',
  ] as const,
};

/** Typed guard — runtime validates StdFitnessStudioMembershipOrbitalParams keys. */
export function isStdFitnessStudioMembershipOrbitalParams(p: object): p is StdFitnessStudioMembershipOrbitalParams {
  type _OverrideRecord = NonNullable<StdFitnessStudioMembershipOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdFitnessStudioMembershipOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the ClassRosterOrbital orbital.
 *
 * Canonical entity: ClassRosterRow — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdFitnessStudioClassRosterOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'ClassRosterAppLayout' | 'ClassRosterEntries',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the ClassRosterOrbital orbital with consumer params. */
export function stdFitnessStudioClassRosterOrbital(params: StdFitnessStudioClassRosterOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'ClassRosterRow';
  const built = makeOrbitalWithUses({
    name: 'ClassRosterOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-class-roster',
        'as': 'ClassRoster',
      },
    ],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'ClassRosterAppLayout',
        'config': {
          'contentTrait': '@trait.ClassRosterDisplay',
          'appName': 'Fitness Studio',
          'navItems': [
            {
              'label': 'Members',
              'icon': 'users',
              'href': '/members',
            },
            {
              'label': 'Classes',
              'href': '/classes',
              'icon': 'calendar',
            },
            {
              'label': 'Memberships',
              'href': '/memberships',
              'icon': 'credit-card',
            },
            {
              'label': 'Rosters',
              'href': '/rosters',
              'icon': 'clipboard-list',
            },
          ],
          'searchEvent': 'CLASS_ROSTER_SEARCH',
          'notificationClickEvent': 'CLASS_ROSTER_NOTIFICATIONS_OPEN',
          'notifications': [],
        },
        'events': {
          'SEARCH': 'CLASS_ROSTER_SEARCH',
          'NOTIFY_CLICK': 'CLASS_ROSTER_NOTIFICATIONS_OPEN',
        },
      }),
      {
        'name': 'ClassRosterDisplay',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'type': 'stack',
                        'align': 'center',
                        'gap': 'sm',
                        'children': [
                          {
                            'name': 'clipboard-list',
                            'type': 'icon',
                          },
                          {
                            'content': 'Class Rosters',
                            'type': 'typography',
                            'variant': 'h2',
                          },
                        ],
                        'direction': 'horizontal',
                      },
                      {
                        'content': 'Enrolled members per session. Check in arrivals or mark no-shows from the row actions.',
                        'variant': 'caption',
                        'color': 'muted',
                        'type': 'typography',
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.ClassRosterEntries',
                    ],
                    'type': 'stack',
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'direction': 'vertical',
                    'gap': 'lg',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
      makeTraitRef({
        'ref': 'ClassRoster.traits.RosterEntryRoster',
        'name': 'ClassRosterEntries',
        'config': {
          'title': 'Roster Entries',
        },
      }),
    ],
    pages: [
      {
        'name': 'ClassRostersPage',
        'path': '/rosters',
        'traits': [
          {
            'ref': 'ClassRosterAppLayout',
          },
          {
            'ref': 'ClassRosterDisplay',
          },
          {
            'ref': 'ClassRosterEntries',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdFitnessStudioClassRosterOrbital. */
export const StdFitnessStudioClassRosterOrbitalManifest = {
  organism: 'std-fitness-studio',
  orbitalName: 'ClassRosterOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'ClassRosterAppLayout',
    'ClassRosterEntries',
  ] as const,
  inlineTraitNames: [
    'ClassRosterDisplay',
  ] as const,
};

/** Typed guard — runtime validates StdFitnessStudioClassRosterOrbitalParams keys. */
export function isStdFitnessStudioClassRosterOrbitalParams(p: object): p is StdFitnessStudioClassRosterOrbitalParams {
  type _OverrideRecord = NonNullable<StdFitnessStudioClassRosterOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdFitnessStudioClassRosterOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-fitness-studio — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdFitnessStudioParams {
  Member?: StdFitnessStudioMemberOrbitalParams;
  ClassSession?: StdFitnessStudioClassSessionOrbitalParams;
  Membership?: StdFitnessStudioMembershipOrbitalParams;
  ClassRoster?: StdFitnessStudioClassRosterOrbitalParams;
}

/** Whole-organism descriptor (4 orbitals). Composes per-orbital factories. */
export function stdFitnessStudio(params: StdFitnessStudioParams = {}): OrbitalDefinition[] {
  return [
    stdFitnessStudioMemberOrbital(params.Member ?? {}),
    stdFitnessStudioClassSessionOrbital(params.ClassSession ?? {}),
    stdFitnessStudioMembershipOrbital(params.Membership ?? {}),
    stdFitnessStudioClassRosterOrbital(params.ClassRoster ?? {}),
  ];
}
