/**
 * std-restaurant-pos — Restaurant POS: floor plan with tables, order at table, kitchen display, tip split at close. Pick for table-service restaurants.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-restaurant-pos.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-restaurant-pos
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-restaurant-pos';
const ALIAS = 'RestaurantPos';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRestaurantPosConfig {
  notifications?: TraitConfig;
  navItems?: TraitConfig;
}

/**
 * Tunable params for the MenuOrbital orbital.
 *
 * Canonical entity: MenuRecord (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdRestaurantPosMenuOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    'MenuAppLayout' | 'MenuSearch' | 'MenuFilter' | 'MenuCategoryTree' | 'MenuBrowseList' | 'MenuModifierGroups' | 'MenuImageUpload' | 'MenuCreate' | 'MenuEdit' | 'MenuDelete',
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'MenuCatalog' | 'MenuPersistor',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the MenuOrbital orbital with consumer params. */
export function stdRestaurantPosMenuOrbital(params: StdRestaurantPosMenuOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'MenuRecord';
  const built = makeOrbitalWithUses({
    name: 'MenuOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-confirmation',
        'as': 'Confirmation',
      },
      {
        'from': 'std/behaviors/std-menu',
        'as': 'MenuAtom',
      },
      {
        'from': 'std/behaviors/std-modifier-group',
        'as': 'ModifierAtom',
      },
      {
        'from': 'std/behaviors/std-image-upload-multi',
        'as': 'ImageUpload',
      },
      {
        'from': 'std/behaviors/std-search',
        'as': 'Search',
      },
      {
        'from': 'std/behaviors/std-filter',
        'as': 'Filter',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
      {
        'from': 'std/behaviors/std-tag-taxonomy',
        'as': 'TagTaxonomy',
      },
    ],
    entity: {
      name: canonicalName,
      collection: 'menurecords',
      persistence: params.persistence ?? 'persistent',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'name',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'description',
          'type': 'string',
          'default': '',
        },
        {
          'name': 'price',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'category',
          'type': 'string',
          'default': '',
        },
        {
          'name': 'available',
          'type': 'boolean',
          'default': true,
        },
        {
          'name': 'pendingId',
          'type': 'string',
          'default': '',
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'MenuAppLayout',
        'config': {
          'notifications': [],
          'contentTrait': '@trait.MenuCatalog',
          'appName': 'Restaurant POS',
          'navItems': [
            {
              'label': 'Menu',
              'icon': 'utensils',
              'href': '/menu',
            },
            {
              'href': '/floor',
              'icon': 'layout-grid',
              'label': 'Floor',
            },
            {
              'icon': 'receipt',
              'href': '/orders',
              'label': 'Orders',
            },
            {
              'icon': 'chef-hat',
              'href': '/kitchen',
              'label': 'Kitchen',
            },
          ],
          'searchEvent': 'MENU_SEARCH',
          'notificationClickEvent': 'MENU_NOTIFICATIONS_OPEN',
        },
        'events': {
          'NOTIFY_CLICK': 'MENU_NOTIFICATIONS_OPEN',
          'SEARCH': 'MENU_SEARCH',
        },
      }),
      {
        'name': 'MenuCatalog',
        'category': 'interaction',
        'emits': [
          {
            'event': 'CREATE',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'source',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'MENU_SEARCH',
            'triggers': 'MENU_SEARCH',
            'source': {
              'kind': 'trait',
              'trait': 'MenuAppLayout',
            },
          },
          {
            'event': 'MENU_NOTIFICATIONS_OPEN',
            'triggers': 'MENU_NOTIFICATIONS_OPEN',
            'source': {
              'kind': 'trait',
              'trait': 'MenuAppLayout',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'MENU_SEARCH',
              'name': 'Menu Search',
              'payloadSchema': [
                {
                  'name': 'value',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MENU_NOTIFICATIONS_OPEN',
              'name': 'Menu Notifications Open',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'CREATE',
              'name': 'Create',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'children': [
                          {
                            'gap': 'sm',
                            'direction': 'horizontal',
                            'align': 'center',
                            'type': 'stack',
                            'children': [
                              {
                                'type': 'icon',
                                'name': 'utensils',
                              },
                              {
                                'type': 'typography',
                                'content': 'Menu',
                                'variant': 'h2',
                              },
                            ],
                          },
                          {
                            'gap': 'sm',
                            'type': 'stack',
                            'direction': 'horizontal',
                            'children': [
                              {
                                'type': 'button',
                                'action': 'CREATE',
                                'label': 'New Item',
                                'variant': 'primary',
                                'icon': 'plus',
                              },
                            ],
                          },
                        ],
                        'gap': 'md',
                        'type': 'stack',
                        'justify': 'between',
                        'align': 'center',
                        'direction': 'horizontal',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'children': [
                          '@trait.MenuSearch',
                          '@trait.MenuFilter',
                        ],
                        'direction': 'horizontal',
                        'gap': 'md',
                        'type': 'stack',
                        'align': 'center',
                      },
                      '@trait.MenuCategoryTree',
                      {
                        'type': 'divider',
                      },
                      '@trait.MenuBrowseList',
                      {
                        'type': 'divider',
                      },
                      {
                        'content': 'Modifier groups',
                        'type': 'typography',
                        'variant': 'h3',
                      },
                      '@trait.MenuModifierGroups',
                      {
                        'type': 'divider',
                      },
                      {
                        'type': 'typography',
                        'variant': 'h3',
                        'content': 'Item images',
                      },
                      '@trait.MenuImageUpload',
                    ],
                    'direction': 'vertical',
                    'type': 'stack',
                    'gap': 'lg',
                  },
                ],
              ],
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MENU_SEARCH',
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MENU_NOTIFICATIONS_OPEN',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'gap': 'md',
                    'type': 'stack',
                    'direction': 'vertical',
                    'align': 'center',
                    'className': 'py-8',
                    'children': [
                      {
                        'type': 'icon',
                        'name': 'bell',
                      },
                      {
                        'variant': 'h3',
                        'content': 'No notifications',
                        'type': 'typography',
                      },
                      {
                        'variant': 'ghost',
                        'label': 'Back to menu',
                        'type': 'button',
                        'action': 'INIT',
                      },
                    ],
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
      makeTraitRef({
        'ref': 'Search.traits.SearchResultSearch',
        'name': 'MenuSearch',
        'config': {
          'placeholder': 'Search menu…',
          'event': 'MENU_SEARCH',
        },
      }),
      makeTraitRef({
        'ref': 'Filter.traits.FilterTargetFilter',
        'name': 'MenuFilter',
        'config': {
          'filters': [
            {
              'filterType': 'select',
              'options': [
                'appetizer',
                'entree',
                'dessert',
                'drink',
              ],
              'field': 'category',
              'label': 'Category',
            },
            {
              'options': [
                'true',
                'false',
              ],
              'field': 'available',
              'label': 'Available',
              'filterType': 'select',
            },
          ],
          'event': 'MENU_FILTER',
        },
      }),
      makeTraitRef({
        'ref': 'TagTaxonomy.traits.TagBrowse',
        'name': 'MenuCategoryTree',
        'config': {
          'allowEdit': false,
          'title': 'Categories',
        },
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'MenuBrowseList',
        'linkedEntity': 'MenuRecord',
        'config': {
          'gap': 'sm',
          'fields': [
            {
              'icon': 'utensils',
              'name': 'name',
              'variant': 'h4',
            },
            {
              'name': 'category',
              'variant': 'badge',
            },
            {
              'name': 'price',
              'variant': 'caption',
            },
            {
              'name': 'available',
              'variant': 'badge',
            },
            {
              'name': 'description',
              'variant': 'caption',
            },
          ],
          'cols': 1,
          'itemActions': [
            {
              'event': 'EDIT',
              'variant': 'ghost',
              'label': 'Edit',
            },
            {
              'event': 'DELETE',
              'label': 'Delete',
              'variant': 'danger',
            },
          ],
        },
        'listens': [
          {
            'event': 'SEARCH',
            'triggers': 'REFETCH_QUERY',
            'source': {
              'kind': 'trait',
              'trait': 'MenuSearch',
            },
          },
          {
            'event': 'FILTER',
            'triggers': 'REFETCH_FILTER',
            'source': {
              'kind': 'trait',
              'trait': 'MenuFilter',
            },
          },
          {
            'event': 'MENU_CREATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MenuPersistor',
            },
          },
          {
            'event': 'MENU_UPDATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MenuPersistor',
            },
          },
          {
            'event': 'MENU_DELETED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MenuPersistor',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'ModifierAtom.traits.ModifierGroupManage',
        'name': 'MenuModifierGroups',
        'config': {
          'title': 'Modifier Groups',
        },
      }),
      makeTraitRef({
        'ref': 'ImageUpload.traits.UploadedImageUpload',
        'name': 'MenuImageUpload',
        'config': {
          'title': 'Item Images',
          'maxBytesPerImage': 5242880,
          'maxImages': 12,
          'accept': 'image/*',
        },
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MenuCreate',
        'linkedEntity': 'MenuRecord',
        'config': {
          'title': 'New Menu Item',
          'mode': 'create',
          'icon': 'plus-circle',
          'fields': [
            'name',
            'description',
            'price',
            'category',
            'available',
          ],
        },
        'events': {
          'OPEN': 'CREATE',
        },
        'listens': [
          {
            'event': 'CREATE',
            'triggers': 'CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MenuCatalog',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MenuEdit',
        'linkedEntity': 'MenuRecord',
        'config': {
          'icon': 'edit',
          'mode': 'edit',
          'title': 'Edit Menu Item',
          'fields': [
            'name',
            'description',
            'price',
            'category',
            'available',
          ],
        },
        'events': {
          'OPEN': 'EDIT',
        },
        'listens': [
          {
            'event': 'EDIT',
            'triggers': 'EDIT',
            'source': {
              'kind': 'trait',
              'trait': 'MenuBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Confirmation.traits.ConfirmActionConfirmation',
        'name': 'MenuDelete',
        'linkedEntity': 'MenuRecord',
        'config': {
          'icon': 'alert-triangle',
          'confirmLabel': 'Delete',
          'title': 'Delete Menu Item',
          'alertMessage': 'This action cannot be undone.',
        },
        'events': {
          'CONFIRM': 'CONFIRM_DELETE',
          'REQUEST': 'DELETE',
        },
        'listens': [
          {
            'event': 'DELETE',
            'triggers': 'DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MenuBrowseList',
            },
          },
        ],
      }),
      {
        'name': 'MenuPersistor',
        'category': 'lifecycle',
        'linkedEntity': 'MenuRecord',
        'emits': [
          {
            'event': 'MENU_CREATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MENU_UPDATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MENU_DELETED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SAVE',
            'triggers': 'DO_CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MenuCreate',
            },
          },
          {
            'event': 'SAVE',
            'triggers': 'DO_UPDATE',
            'source': {
              'kind': 'trait',
              'trait': 'MenuEdit',
            },
          },
          {
            'event': 'CONFIRM_DELETE',
            'triggers': 'DO_DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MenuDelete',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'DO_CREATE',
              'name': 'Do Create',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_UPDATE',
              'name': 'Do Update',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_DELETE',
              'name': 'Do Delete',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MENU_CREATED',
              'name': 'Menu Created',
            },
            {
              'key': 'MENU_UPDATED',
              'name': 'Menu Updated',
            },
            {
              'key': 'MENU_DELETED',
              'name': 'Menu Deleted',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_CREATE',
              'effects': [
                [
                  'persist',
                  'create',
                  'MenuRecord',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MENU_CREATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_UPDATE',
              'effects': [
                [
                  'persist',
                  'update',
                  'MenuRecord',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MENU_UPDATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_DELETE',
              'effects': [
                [
                  'persist',
                  'delete',
                  'MenuRecord',
                  '@payload.id',
                  {
                    'emit': {
                      'success': 'MENU_DELETED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'MenuPage',
        'path': '/menu',
        'traits': [
          {
            'ref': 'MenuAppLayout',
          },
          {
            'ref': 'MenuCatalog',
          },
          {
            'ref': 'MenuSearch',
          },
          {
            'ref': 'MenuFilter',
          },
          {
            'ref': 'MenuCategoryTree',
          },
          {
            'ref': 'MenuBrowseList',
          },
          {
            'ref': 'MenuModifierGroups',
          },
          {
            'ref': 'MenuImageUpload',
          },
          {
            'ref': 'MenuCreate',
          },
          {
            'ref': 'MenuEdit',
          },
          {
            'ref': 'MenuDelete',
          },
          {
            'ref': 'MenuPersistor',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdRestaurantPosMenuOrbital. */
export const StdRestaurantPosMenuOrbitalManifest = {
  organism: 'std-restaurant-pos',
  orbitalName: 'MenuOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
    'MenuAppLayout',
    'MenuSearch',
    'MenuFilter',
    'MenuCategoryTree',
    'MenuBrowseList',
    'MenuModifierGroups',
    'MenuImageUpload',
    'MenuCreate',
    'MenuEdit',
    'MenuDelete',
  ] as const,
  inlineTraitNames: [
    'MenuCatalog',
    'MenuPersistor',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdRestaurantPosMenuOrbitalParams keys. */
export function isStdRestaurantPosMenuOrbitalParams(p: object): p is StdRestaurantPosMenuOrbitalParams {
  type _OverrideRecord = NonNullable<StdRestaurantPosMenuOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdRestaurantPosMenuOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdRestaurantPosMenuOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdRestaurantPosMenuOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the TableOrbital orbital.
 *
 * Canonical entity: TableRecord (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdRestaurantPosTableOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    'TableFloorPlan' | 'TableRoster' | 'TableEdit',
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'TablePersistor',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the TableOrbital orbital with consumer params. */
export function stdRestaurantPosTableOrbital(params: StdRestaurantPosTableOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'TableRecord';
  const built = makeOrbitalWithUses({
    name: 'TableOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-table-map',
        'as': 'TableMap',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
    ],
    entity: {
      name: canonicalName,
      collection: 'tablerecords',
      persistence: params.persistence ?? 'persistent',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'label',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'capacity',
          'type': 'number',
          'default': 4,
        },
        {
          'name': 'section',
          'type': 'string',
          'default': '',
        },
        {
          'name': 'pendingId',
          'type': 'string',
          'default': '',
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'TableMap.traits.TableManage',
        'name': 'TableFloorPlan',
        'config': {
          'title': 'Floor Plan',
          'canvasHeight': 640,
          'canvasWidth': 960,
        },
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'TableRoster',
        'linkedEntity': 'TableRecord',
        'config': {
          'cols': 1,
          'gap': 'sm',
          'fields': [
            {
              'variant': 'h4',
              'name': 'label',
              'icon': 'layout-grid',
            },
            {
              'variant': 'badge',
              'name': 'capacity',
            },
            {
              'name': 'section',
              'variant': 'caption',
            },
          ],
          'itemActions': [
            {
              'event': 'EDIT',
              'variant': 'ghost',
              'label': 'Edit',
            },
          ],
        },
        'listens': [
          {
            'event': 'TABLE_UPDATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'TablePersistor',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'TableEdit',
        'linkedEntity': 'TableRecord',
        'config': {
          'title': 'Edit Table',
          'icon': 'edit',
          'mode': 'edit',
          'fields': [
            'label',
            'capacity',
            'section',
          ],
        },
        'events': {
          'OPEN': 'EDIT',
        },
        'listens': [
          {
            'event': 'EDIT',
            'triggers': 'EDIT',
            'source': {
              'kind': 'trait',
              'trait': 'TableRoster',
            },
          },
        ],
      }),
      {
        'name': 'TablePersistor',
        'category': 'lifecycle',
        'linkedEntity': 'TableRecord',
        'emits': [
          {
            'event': 'TABLE_UPDATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SAVE',
            'triggers': 'DO_UPDATE',
            'source': {
              'kind': 'trait',
              'trait': 'TableEdit',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'DO_UPDATE',
              'name': 'Do Update',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'TABLE_UPDATED',
              'name': 'Table Updated',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_UPDATE',
              'effects': [
                [
                  'persist',
                  'update',
                  'TableRecord',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'TABLE_UPDATED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'FloorPage',
        'path': '/floor',
        'traits': [
          {
            'ref': 'TableFloorPlan',
          },
          {
            'ref': 'TableRoster',
          },
          {
            'ref': 'TableEdit',
          },
          {
            'ref': 'TablePersistor',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdRestaurantPosTableOrbital. */
export const StdRestaurantPosTableOrbitalManifest = {
  organism: 'std-restaurant-pos',
  orbitalName: 'TableOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
    'TableFloorPlan',
    'TableRoster',
    'TableEdit',
  ] as const,
  inlineTraitNames: [
    'TablePersistor',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdRestaurantPosTableOrbitalParams keys. */
export function isStdRestaurantPosTableOrbitalParams(p: object): p is StdRestaurantPosTableOrbitalParams {
  type _OverrideRecord = NonNullable<StdRestaurantPosTableOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdRestaurantPosTableOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdRestaurantPosTableOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdRestaurantPosTableOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the OrderOrbital orbital.
 *
 * Canonical entity: OrderTicket (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdRestaurantPosOrderOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    'OrderCart' | 'OrderCartAddItem' | 'OrderCartRemoveConfirm' | 'OrderCartPersistor',
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'OrderClose',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the OrderOrbital orbital with consumer params. */
export function stdRestaurantPosOrderOrbital(params: StdRestaurantPosOrderOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'OrderTicket';
  const built = makeOrbitalWithUses({
    name: 'OrderOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-cart',
        'as': 'Cart',
      },
      {
        'from': 'std/behaviors/std-confirmation',
        'as': 'Confirmation',
      },
    ],
    entity: {
      name: canonicalName,
      collection: 'orderTickets',
      persistence: params.persistence ?? 'persistent',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'tableId',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'serverName',
          'type': 'string',
          'default': '',
        },
        {
          'name': 'subtotal',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'taxAmount',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'tipAmount',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'total',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'status',
          'type': 'string',
          'default': 'open',
          'values': [
            'open',
            'submitted',
            'closed',
          ],
        },
        {
          'name': 'pendingId',
          'type': 'string',
          'default': '',
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'Cart.traits.CartItemCartBrowse',
        'name': 'OrderCart',
        'config': {
          'keyedOn': 'tableId',
        },
      }),
      makeTraitRef({
        'ref': 'Cart.traits.CartItemAddItem',
        'name': 'OrderCartAddItem',
        'config': {
          'fields': [
            'name',
            'description',
            'status',
          ],
          'mode': 'create',
          'openButtonLabel': 'Add Item',
          'icon': 'plus-circle',
          'title': 'Add to Order',
        },
      }),
      makeTraitRef({
        'ref': 'Cart.traits.CartItemRemoveConfirm',
        'name': 'OrderCartRemoveConfirm',
        'config': {
          'title': 'Remove From Order',
          'confirmLabel': 'Remove',
          'alertMessage': 'Remove this item from the order?',
          'icon': 'alert-triangle',
        },
      }),
      makeTraitRef({
        'ref': 'Cart.traits.CartItemPersistor',
        'name': 'OrderCartPersistor',
      }),
      {
        'name': 'OrderClose',
        'category': 'interaction',
        'linkedEntity': 'OrderTicket',
        'emits': [
          {
            'event': 'SUBMIT',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'source',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'CLOSE_TICKET',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'tipAmount',
                'type': 'number',
                'required': true,
              },
              {
                'name': 'total',
                'type': 'number',
                'required': true,
              },
            ],
          },
          {
            'event': 'ORDER_SUBMITTED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'ORDER_CLOSED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'OrderTicketLoaded',
            'description': 'Fired when the order ticket collection finishes loading',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'data',
                'type': '[OrderTicket]',
              },
            ],
          },
          {
            'event': 'OrderTicketLoadFailed',
            'description': 'Fired when the order ticket collection fails to load',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'error',
                'type': 'string',
              },
              {
                'name': 'code',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'loading',
              'isInitial': true,
            },
            {
              'name': 'ready',
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'OrderTicketLoaded',
              'name': 'OrderTicket loaded',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': '[OrderTicket]',
                },
              ],
            },
            {
              'key': 'OrderTicketLoadFailed',
              'name': 'OrderTicket load failed',
              'payloadSchema': [
                {
                  'name': 'error',
                  'type': 'string',
                },
                {
                  'name': 'code',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'SUBMIT',
              'name': 'Submit',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'source',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'CLOSE_TICKET',
              'name': 'Close Ticket',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'tipAmount',
                  'type': 'number',
                  'required': true,
                },
                {
                  'name': 'total',
                  'type': 'number',
                  'required': true,
                },
              ],
            },
            {
              'key': 'ORDER_SUBMITTED',
              'name': 'Order Submitted',
            },
            {
              'key': 'ORDER_CLOSED',
              'name': 'Order Closed',
            },
          ],
          'transitions': [
            {
              'from': 'loading',
              'to': 'loading',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'OrderTicket',
                  {
                    'emit': {
                      'success': 'OrderTicketLoaded',
                      'failure': 'OrderTicketLoadFailed',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'gap': 'md',
                    'align': 'center',
                    'className': 'py-12',
                    'type': 'stack',
                    'children': [
                      {
                        'type': 'spinner',
                      },
                      {
                        'color': 'muted',
                        'content': 'Loading order…',
                        'type': 'typography',
                        'variant': 'caption',
                      },
                    ],
                    'direction': 'vertical',
                  },
                ],
              ],
            },
            {
              'from': 'loading',
              'to': 'ready',
              'event': 'OrderTicketLoaded',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'gap': 'md',
                    'type': 'stack',
                    'direction': 'vertical',
                    'children': [
                      {
                        'align': 'center',
                        'gap': 'sm',
                        'type': 'stack',
                        'children': [
                          {
                            'name': 'receipt',
                            'type': 'icon',
                          },
                          {
                            'variant': 'h3',
                            'content': 'Close & Tip Split',
                            'type': 'typography',
                          },
                        ],
                        'direction': 'horizontal',
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'type': 'data-grid',
                        'entity': '@payload.data',
                        'gap': 'sm',
                        'cols': 1,
                        'fields': [
                          {
                            'name': 'tableId',
                            'variant': 'label',
                          },
                          {
                            'variant': 'caption',
                            'name': 'subtotal',
                          },
                          {
                            'variant': 'caption',
                            'name': 'taxAmount',
                          },
                          {
                            'variant': 'caption',
                            'name': 'tipAmount',
                          },
                          {
                            'name': 'total',
                            'variant': 'h4',
                          },
                          {
                            'variant': 'badge',
                            'name': 'status',
                          },
                        ],
                      },
                      {
                        'gap': 'sm',
                        'direction': 'horizontal',
                        'justify': 'end',
                        'children': [
                          {
                            'type': 'button',
                            'icon': 'send',
                            'variant': 'secondary',
                            'label': 'Submit to Kitchen',
                            'action': 'SUBMIT',
                          },
                          {
                            'variant': 'primary',
                            'label': 'Close & Split Tip',
                            'action': 'CLOSE_TICKET',
                            'icon': 'split',
                            'type': 'button',
                          },
                        ],
                        'type': 'stack',
                      },
                    ],
                  },
                ],
              ],
            },
            {
              'from': 'loading',
              'to': 'ready',
              'event': 'OrderTicketLoadFailed',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'alert',
                    'message': '@payload.error',
                    'variant': 'error',
                  },
                ],
              ],
            },
            {
              'from': 'ready',
              'to': 'loading',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'OrderTicket',
                  {
                    'emit': {
                      'success': 'OrderTicketLoaded',
                      'failure': 'OrderTicketLoadFailed',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'size': 'sm',
                    'type': 'spinner',
                  },
                ],
              ],
            },
            {
              'from': 'ready',
              'to': 'ready',
              'event': 'SUBMIT',
              'effects': [
                [
                  'set',
                  '@entity.id',
                  '@payload.id',
                ],
                [
                  'set',
                  '@entity.status',
                  'submitted',
                ],
                [
                  'persist',
                  'update',
                  'OrderTicket',
                  '@entity',
                  {
                    'emit': {
                      'success': 'ORDER_SUBMITTED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'ready',
              'to': 'ready',
              'event': 'CLOSE_TICKET',
              'effects': [
                [
                  'set',
                  '@entity.id',
                  '@payload.id',
                ],
                [
                  'set',
                  '@entity.tipAmount',
                  '@payload.tipAmount',
                ],
                [
                  'set',
                  '@entity.total',
                  '@payload.total',
                ],
                [
                  'set',
                  '@entity.status',
                  'closed',
                ],
                [
                  'persist',
                  'update',
                  'OrderTicket',
                  '@entity',
                  {
                    'emit': {
                      'success': 'ORDER_CLOSED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'collection',
      } as never,
    ],
    pages: [
      {
        'name': 'OrderPage',
        'path': '/orders',
        'traits': [
          {
            'ref': 'OrderCart',
          },
          {
            'ref': 'OrderCartAddItem',
          },
          {
            'ref': 'OrderCartRemoveConfirm',
          },
          {
            'ref': 'OrderCartPersistor',
          },
          {
            'ref': 'OrderClose',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdRestaurantPosOrderOrbital. */
export const StdRestaurantPosOrderOrbitalManifest = {
  organism: 'std-restaurant-pos',
  orbitalName: 'OrderOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
    'OrderCart',
    'OrderCartAddItem',
    'OrderCartRemoveConfirm',
    'OrderCartPersistor',
  ] as const,
  inlineTraitNames: [
    'OrderClose',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdRestaurantPosOrderOrbitalParams keys. */
export function isStdRestaurantPosOrderOrbitalParams(p: object): p is StdRestaurantPosOrderOrbitalParams {
  type _OverrideRecord = NonNullable<StdRestaurantPosOrderOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdRestaurantPosOrderOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdRestaurantPosOrderOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdRestaurantPosOrderOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the KitchenOrbital orbital.
 *
 * Canonical entity: KitchenLog (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdRestaurantPosKitchenOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    'KitchenBoard' | 'KitchenLogList',
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    never,
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the KitchenOrbital orbital with consumer params. */
export function stdRestaurantPosKitchenOrbital(params: StdRestaurantPosKitchenOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'KitchenLog';
  const built = makeOrbitalWithUses({
    name: 'KitchenOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-kitchen-display',
        'as': 'Kitchen',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
    ],
    entity: {
      name: canonicalName,
      collection: 'kitchenlogs',
      persistence: params.persistence ?? 'persistent',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'orderId',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'note',
          'type': 'string',
          'default': '',
        },
        {
          'name': 'createdAt',
          'type': 'string',
          'default': '',
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'Kitchen.traits.KitchenTicketManage',
        'name': 'KitchenBoard',
        'config': {
          'overdueMinutes': 18,
          'title': 'Kitchen Display',
        },
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'KitchenLogList',
        'linkedEntity': 'KitchenLog',
        'config': {
          'fields': [
            {
              'name': 'orderId',
              'variant': 'label',
            },
            {
              'name': 'note',
              'variant': 'caption',
            },
            {
              'variant': 'caption',
              'name': 'createdAt',
            },
          ],
          'cols': 1,
          'gap': 'sm',
        },
      }),
    ],
    pages: [
      {
        'name': 'KitchenPage',
        'path': '/kitchen',
        'traits': [
          {
            'ref': 'KitchenBoard',
          },
          {
            'ref': 'KitchenLogList',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdRestaurantPosKitchenOrbital. */
export const StdRestaurantPosKitchenOrbitalManifest = {
  organism: 'std-restaurant-pos',
  orbitalName: 'KitchenOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
    'KitchenBoard',
    'KitchenLogList',
  ] as const,
  inlineTraitNames: [
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdRestaurantPosKitchenOrbitalParams keys. */
export function isStdRestaurantPosKitchenOrbitalParams(p: object): p is StdRestaurantPosKitchenOrbitalParams {
  type _OverrideRecord = NonNullable<StdRestaurantPosKitchenOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdRestaurantPosKitchenOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdRestaurantPosKitchenOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdRestaurantPosKitchenOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-restaurant-pos — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdRestaurantPosParams {
  Menu?: StdRestaurantPosMenuOrbitalParams;
  Table?: StdRestaurantPosTableOrbitalParams;
  Order?: StdRestaurantPosOrderOrbitalParams;
  Kitchen?: StdRestaurantPosKitchenOrbitalParams;
}

/** Whole-organism descriptor (4 orbitals). Composes per-orbital factories. */
export function stdRestaurantPos(params: StdRestaurantPosParams = {}): OrbitalDefinition[] {
  return [
    stdRestaurantPosMenuOrbital(params.Menu ?? {}),
    stdRestaurantPosTableOrbital(params.Table ?? {}),
    stdRestaurantPosOrderOrbital(params.Order ?? {}),
    stdRestaurantPosKitchenOrbital(params.Kitchen ?? {}),
  ];
}
