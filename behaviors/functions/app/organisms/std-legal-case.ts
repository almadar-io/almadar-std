/**
 * std-legal-case — Legal Case Management: matters, billable hours, and court deadlines for law firms and in-house counsel. Pick for litigation, transactional, and regulatory practice.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/organisms/std-legal-case.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-legal-case
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-legal-case';
const ALIAS = 'LegalCase';

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLegalCaseConfig {
  notifications?: TraitConfig;
  navItems?: TraitConfig;
}

/**
 * Tunable params for the MatterOrbital orbital.
 *
 * Canonical entity: Matter — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLegalCaseMatterOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MatterAppLayout' | 'MatterSearch' | 'MatterFilter' | 'MatterStats' | 'MatterGraphs' | 'MatterBrowseList' | 'MatterCreate' | 'MatterEdit' | 'MatterView' | 'MatterDelete' | 'MatterCatalog' | 'MatterPersistor',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the MatterOrbital orbital with consumer params. */
export function stdLegalCaseMatterOrbital(params: StdLegalCaseMatterOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'Matter';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'matters');
  const built = makeOrbitalWithUses({
    name: 'MatterOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-modal',
        'as': 'Modal',
      },
      {
        'from': 'std/behaviors/std-confirmation',
        'as': 'Confirmation',
      },
      {
        'from': 'std/behaviors/std-search',
        'as': 'Search',
      },
      {
        'from': 'std/behaviors/std-filter',
        'as': 'Filter',
      },
      {
        'from': 'std/behaviors/std-stats',
        'as': 'Stats',
      },
      {
        'from': 'std/behaviors/std-graphs',
        'as': 'Graphs',
      },
      {
        'from': 'std/behaviors/std-browse',
        'as': 'Browse',
      },
      {
        'from': 'std/behaviors/std-matter',
        'as': 'Matter',
      },
      {
        'from': 'std/behaviors/std-billable-hour',
        'as': 'BillableHour',
      },
      {
        'from': 'std/behaviors/std-court-deadline',
        'as': 'CourtDeadline',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'matterNumber',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'title',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'clientName',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'practiceArea',
            'type': 'string',
            'default': 'other',
            'values': [
              'litigation',
              'transactional',
              'regulatory',
              'ip',
              'immigration',
              'family',
              'other',
            ],
          },
          {
            'name': 'leadAttorney',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'open',
            'values': [
              'open',
              'active',
              'on_hold',
              'closed',
              'archived',
            ],
          },
          {
            'name': 'openedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'closedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'notes',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'pendingId',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'MatterAppLayout',
        'config': {
          'notifications': [],
          'searchEvent': 'MATTER_SEARCH',
          'appName': 'Legal Case',
          'notificationClickEvent': 'MATTER_NOTIFICATIONS_OPEN',
          'contentTrait': '@trait.MatterCatalog',
          'navItems': [
            {
              'label': 'Matters',
              'href': '/matters',
              'icon': 'scale',
            },
            {
              'icon': 'folder',
              'label': 'Cases',
              'href': '/cases',
            },
            {
              'icon': 'clock',
              'label': 'Time',
              'href': '/timesheet',
            },
            {
              'icon': 'calendar',
              'label': 'Deadlines',
              'href': '/deadlines',
            },
          ],
        },
        'events': {
          'NOTIFY_CLICK': 'MATTER_NOTIFICATIONS_OPEN',
          'SEARCH': 'MATTER_SEARCH',
        },
      }),
      rebindInlineTraitEntity({
        'name': 'MatterCatalog',
        'category': 'interaction',
        'emits': [
          {
            'event': 'CREATE',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'source',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'MATTER_SEARCH',
            'triggers': 'MATTER_SEARCH',
            'source': {
              'kind': 'trait',
              'trait': 'MatterAppLayout',
            },
          },
          {
            'event': 'MATTER_NOTIFICATIONS_OPEN',
            'triggers': 'MATTER_NOTIFICATIONS_OPEN',
            'source': {
              'kind': 'trait',
              'trait': 'MatterAppLayout',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'MATTER_SEARCH',
              'name': 'Matter Search',
              'payloadSchema': [
                {
                  'name': 'value',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MATTER_NOTIFICATIONS_OPEN',
              'name': 'Matter Notifications Open',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'CREATE',
              'name': 'Create',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'direction': 'vertical',
                    'gap': 'lg',
                    'children': [
                      {
                        'direction': 'horizontal',
                        'gap': 'md',
                        'type': 'stack',
                        'justify': 'between',
                        'align': 'center',
                        'children': [
                          {
                            'children': [
                              {
                                'name': 'scale',
                                'type': 'icon',
                              },
                              {
                                'type': 'typography',
                                'content': 'Matters',
                                'variant': 'h2',
                              },
                            ],
                            'direction': 'horizontal',
                            'type': 'stack',
                            'align': 'center',
                            'gap': 'sm',
                          },
                          {
                            'gap': 'sm',
                            'direction': 'horizontal',
                            'type': 'stack',
                            'children': [
                              {
                                'label': 'New Matter',
                                'variant': 'primary',
                                'action': 'CREATE',
                                'type': 'button',
                                'icon': 'plus',
                              },
                            ],
                          },
                        ],
                      },
                      {
                        'type': 'divider',
                      },
                      {
                        'direction': 'horizontal',
                        'align': 'center',
                        'type': 'stack',
                        'children': [
                          '@trait.MatterSearch',
                          '@trait.MatterFilter',
                        ],
                        'gap': 'md',
                      },
                      '@trait.MatterStats',
                      '@trait.MatterGraphs',
                      {
                        'type': 'divider',
                      },
                      '@trait.MatterBrowseList',
                    ],
                    'type': 'stack',
                  },
                ],
              ],
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MATTER_SEARCH',
            },
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'MATTER_NOTIFICATIONS_OPEN',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'direction': 'vertical',
                    'className': 'py-8',
                    'type': 'stack',
                    'children': [
                      {
                        'name': 'bell',
                        'type': 'icon',
                      },
                      {
                        'variant': 'h3',
                        'content': 'No notifications',
                        'type': 'typography',
                      },
                      {
                        'content': 'You\'re all caught up.',
                        'variant': 'caption',
                        'type': 'typography',
                        'color': 'muted',
                      },
                      {
                        'label': 'Back to matters',
                        'action': 'INIT',
                        'variant': 'ghost',
                        'type': 'button',
                      },
                    ],
                    'align': 'center',
                    'gap': 'md',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'Matter', canonicalName) as never,
      makeTraitRef({
        'ref': 'Search.traits.SearchResultSearch',
        'name': 'MatterSearch',
        'config': {
          'placeholder': 'Search matters…',
          'event': 'MATTER_SEARCH',
        },
      }),
      makeTraitRef({
        'ref': 'Filter.traits.FilterTargetFilter',
        'name': 'MatterFilter',
        'config': {
          'event': 'MATTER_FILTER',
          'filters': [
            {
              'label': 'Practice',
              'options': [
                'litigation',
                'transactional',
                'regulatory',
                'ip',
                'immigration',
                'family',
                'other',
              ],
              'filterType': 'select',
              'field': 'practiceArea',
            },
            {
              'filterType': 'select',
              'options': [
                'open',
                'active',
                'on_hold',
                'closed',
                'archived',
              ],
              'label': 'Status',
              'field': 'status',
            },
          ],
        },
      }),
      makeTraitRef({
        'ref': 'Stats.traits.StatsItemStats',
        'name': 'MatterStats',
        'config': {
          'title': 'Matters',
          'metrics': [
            {
              'label': 'Total',
              'variant': 'primary',
              'format': 'number',
              'aggregation': 'count',
              'icon': 'scale',
            },
            {
              'aggregation': 'count',
              'label': 'Open',
              'variant': 'warning',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'open',
                ],
              ],
              'format': 'number',
              'icon': 'circle',
            },
            {
              'format': 'number',
              'icon': 'check-circle',
              'aggregation': 'count',
              'label': 'Active',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'active',
                ],
              ],
              'variant': 'success',
            },
            {
              'icon': 'archive',
              'aggregation': 'count',
              'variant': 'info',
              'label': 'Closed',
              'filter': [
                'fn',
                'row',
                [
                  '=',
                  '@row.status',
                  'closed',
                ],
              ],
              'format': 'number',
            },
          ],
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'MatterBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Graphs.traits.GraphItemGraph',
        'name': 'MatterGraphs',
        'config': {
          'chartType': 'bar',
          'height': 240,
          'subtitle': 'Caseload composition across practice areas',
          'title': 'Matters by Practice Area',
          'categoryField': 'practiceArea',
          'showLegend': false,
          'aggregation': 'count',
        },
        'listens': [
          {
            'event': 'BrowseItemLoaded',
            'triggers': 'ITEMS_LOADED',
            'source': {
              'kind': 'trait',
              'trait': 'MatterBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Browse.traits.BrowseItemBrowse',
        'name': 'MatterBrowseList',
        'linkedEntity': canonicalName,
        'config': {
          'cols': 1,
          'gap': 'sm',
          'fields': [
            {
              'variant': 'caption',
              'name': 'matterNumber',
              'icon': 'hash',
            },
            {
              'variant': 'h3',
              'name': 'title',
            },
            {
              'variant': 'body',
              'name': 'clientName',
            },
            {
              'name': 'practiceArea',
              'variant': 'badge',
            },
            {
              'variant': 'caption',
              'name': 'leadAttorney',
            },
            {
              'name': 'status',
              'variant': 'badge',
            },
          ],
          'itemActions': [
            {
              'variant': 'ghost',
              'label': 'View',
              'event': 'VIEW',
            },
            {
              'variant': 'ghost',
              'label': 'Edit',
              'event': 'EDIT',
            },
            {
              'variant': 'danger',
              'label': 'Delete',
              'event': 'DELETE',
            },
          ],
        },
        'listens': [
          {
            'event': 'SEARCH',
            'triggers': 'REFETCH_QUERY',
            'source': {
              'kind': 'trait',
              'trait': 'MatterSearch',
            },
          },
          {
            'event': 'FILTER',
            'triggers': 'REFETCH_FILTER',
            'source': {
              'kind': 'trait',
              'trait': 'MatterFilter',
            },
          },
          {
            'event': 'MATTER_CREATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MatterPersistor',
            },
          },
          {
            'event': 'MATTER_UPDATED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MatterPersistor',
            },
          },
          {
            'event': 'MATTER_DELETED',
            'triggers': 'INIT',
            'source': {
              'kind': 'trait',
              'trait': 'MatterPersistor',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MatterCreate',
        'linkedEntity': canonicalName,
        'config': {
          'title': 'New Matter',
          'mode': 'create',
          'icon': 'plus-circle',
          'fields': [
            'matterNumber',
            'title',
            'clientName',
            'practiceArea',
            'leadAttorney',
            'status',
            'openedAt',
            'notes',
          ],
        },
        'events': {
          'OPEN': 'CREATE',
        },
        'listens': [
          {
            'event': 'CREATE',
            'triggers': 'CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MatterCatalog',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MatterEdit',
        'linkedEntity': canonicalName,
        'config': {
          'title': 'Edit Matter',
          'mode': 'edit',
          'icon': 'edit',
          'fields': [
            'matterNumber',
            'title',
            'clientName',
            'practiceArea',
            'leadAttorney',
            'status',
            'openedAt',
            'closedAt',
            'notes',
          ],
        },
        'events': {
          'OPEN': 'EDIT',
        },
        'listens': [
          {
            'event': 'EDIT',
            'triggers': 'EDIT',
            'source': {
              'kind': 'trait',
              'trait': 'MatterBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Modal.traits.ModalRecordModal',
        'name': 'MatterView',
        'linkedEntity': canonicalName,
        'config': {
          'title': 'View Matter',
          'icon': 'eye',
          'fields': [
            'matterNumber',
            'title',
            'clientName',
            'practiceArea',
            'leadAttorney',
            'status',
            'openedAt',
            'closedAt',
            'notes',
          ],
          'mode': 'edit',
        },
        'events': {
          'OPEN': 'VIEW',
        },
        'listens': [
          {
            'event': 'VIEW',
            'triggers': 'VIEW',
            'source': {
              'kind': 'trait',
              'trait': 'MatterBrowseList',
            },
          },
        ],
      }),
      makeTraitRef({
        'ref': 'Confirmation.traits.ConfirmActionConfirmation',
        'name': 'MatterDelete',
        'linkedEntity': canonicalName,
        'config': {
          'confirmLabel': 'Delete',
          'alertMessage': 'This action cannot be undone.',
          'title': 'Delete Matter',
          'icon': 'alert-triangle',
        },
        'events': {
          'CONFIRM': 'CONFIRM_DELETE',
          'REQUEST': 'DELETE',
        },
        'listens': [
          {
            'event': 'DELETE',
            'triggers': 'DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MatterBrowseList',
            },
          },
        ],
      }),
      rebindInlineTraitEntity({
        'name': 'MatterPersistor',
        'category': 'lifecycle',
        'linkedEntity': 'Matter',
        'emits': [
          {
            'event': 'MATTER_CREATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MATTER_UPDATED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
          {
            'event': 'MATTER_DELETED',
            'scope': 'external',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
              },
            ],
          },
        ],
        'listens': [
          {
            'event': 'SAVE',
            'triggers': 'DO_CREATE',
            'source': {
              'kind': 'trait',
              'trait': 'MatterCreate',
            },
          },
          {
            'event': 'SAVE',
            'triggers': 'DO_UPDATE',
            'source': {
              'kind': 'trait',
              'trait': 'MatterEdit',
            },
          },
          {
            'event': 'CONFIRM_DELETE',
            'triggers': 'DO_DELETE',
            'source': {
              'kind': 'trait',
              'trait': 'MatterDelete',
            },
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'DO_CREATE',
              'name': 'Do Create',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_UPDATE',
              'name': 'Do Update',
              'payloadSchema': [
                {
                  'name': 'data',
                  'type': 'object',
                  'required': true,
                },
              ],
            },
            {
              'key': 'DO_DELETE',
              'name': 'Do Delete',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'MATTER_CREATED',
              'name': 'Matter Created',
            },
            {
              'key': 'MATTER_UPDATED',
              'name': 'Matter Updated',
            },
            {
              'key': 'MATTER_DELETED',
              'name': 'Matter Deleted',
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_CREATE',
              'effects': [
                [
                  'persist',
                  'create',
                  'Matter',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MATTER_CREATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_UPDATE',
              'effects': [
                [
                  'persist',
                  'update',
                  'Matter',
                  '@payload.data',
                  {
                    'emit': {
                      'success': 'MATTER_UPDATED',
                    },
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'DO_DELETE',
              'effects': [
                [
                  'persist',
                  'delete',
                  'Matter',
                  '@payload.id',
                  {
                    'emit': {
                      'success': 'MATTER_DELETED',
                    },
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'Matter', canonicalName) as never,
    ],
    pages: [
      {
        'name': 'MattersPage',
        'path': '/matters',
        'traits': [
          {
            'ref': 'MatterAppLayout',
          },
          {
            'ref': 'MatterCatalog',
          },
          {
            'ref': 'MatterSearch',
          },
          {
            'ref': 'MatterFilter',
          },
          {
            'ref': 'MatterStats',
          },
          {
            'ref': 'MatterGraphs',
          },
          {
            'ref': 'MatterBrowseList',
          },
          {
            'ref': 'MatterCreate',
          },
          {
            'ref': 'MatterEdit',
          },
          {
            'ref': 'MatterView',
          },
          {
            'ref': 'MatterDelete',
          },
          {
            'ref': 'MatterPersistor',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLegalCaseMatterOrbital. */
export const StdLegalCaseMatterOrbitalManifest = {
  organism: 'std-legal-case',
  orbitalName: 'MatterOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MatterAppLayout',
    'MatterSearch',
    'MatterFilter',
    'MatterStats',
    'MatterGraphs',
    'MatterBrowseList',
    'MatterCreate',
    'MatterEdit',
    'MatterView',
    'MatterDelete',
  ] as const,
  inlineTraitNames: [
    'MatterCatalog',
    'MatterPersistor',
  ] as const,
};

/** Typed guard — runtime validates StdLegalCaseMatterOrbitalParams keys. */
export function isStdLegalCaseMatterOrbitalParams(p: object): p is StdLegalCaseMatterOrbitalParams {
  type _OverrideRecord = NonNullable<StdLegalCaseMatterOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLegalCaseMatterOrbitalManifest.traitNames,
      ...StdLegalCaseMatterOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the BillableHourPanelOrbital orbital.
 *
 * Canonical entity: BillableHour — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLegalCaseBillableHourPanelOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'BillableHourAppLayout' | 'BillableHourTimesheetView' | 'BillableHourPanel',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the BillableHourPanelOrbital orbital with consumer params. */
export function stdLegalCaseBillableHourPanelOrbital(params: StdLegalCaseBillableHourPanelOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'BillableHour';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'billablehours');
  const built = makeOrbitalWithUses({
    name: 'BillableHourPanelOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-billable-hour',
        'as': 'BillableHour',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'workerId',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'workerName',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'matterId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'projectId',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'workDate',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'hours',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'rate',
            'type': 'number',
            'default': 0,
          },
          {
            'name': 'description',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'isBillable',
            'type': 'boolean',
            'default': true,
          },
          {
            'name': 'invoiceStatus',
            'type': 'string',
            'default': 'unbilled',
            'values': [
              'unbilled',
              'invoiced',
              'paid',
              'written_off',
            ],
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'BillableHourAppLayout',
        'config': {
          'notificationClickEvent': 'BILLABLE_HOUR_NOTIFICATIONS_OPEN',
          'contentTrait': '@trait.BillableHourPanel',
          'navItems': [
            {
              'label': 'Matters',
              'href': '/matters',
              'icon': 'scale',
            },
            {
              'icon': 'folder',
              'href': '/cases',
              'label': 'Cases',
            },
            {
              'href': '/timesheet',
              'icon': 'clock',
              'label': 'Time',
            },
            {
              'label': 'Deadlines',
              'icon': 'calendar',
              'href': '/deadlines',
            },
          ],
          'searchEvent': 'BILLABLE_HOUR_SEARCH',
          'notifications': [],
          'appName': 'Legal Case',
        },
        'events': {
          'NOTIFY_CLICK': 'BILLABLE_HOUR_NOTIFICATIONS_OPEN',
          'SEARCH': 'BILLABLE_HOUR_SEARCH',
        },
      }),
      rebindInlineTraitEntity({
        'name': 'BillableHourPanel',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'direction': 'vertical',
                    'gap': 'lg',
                    'children': [
                      {
                        'children': [
                          {
                            'name': 'clock',
                            'type': 'icon',
                          },
                          {
                            'content': 'Time Entries',
                            'type': 'typography',
                            'variant': 'h2',
                          },
                        ],
                        'gap': 'sm',
                        'type': 'stack',
                        'align': 'center',
                        'direction': 'horizontal',
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.BillableHourTimesheetView',
                    ],
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'type': 'stack',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'BillableHour', canonicalName) as never,
      makeTraitRef({
        'ref': 'BillableHour.traits.BillableHourTimesheet',
        'name': 'BillableHourTimesheetView',
        'config': {
          'title': 'Billable Hours',
        },
      }),
    ],
    pages: [
      {
        'name': 'TimesheetPage',
        'path': '/timesheet',
        'traits': [
          {
            'ref': 'BillableHourAppLayout',
          },
          {
            'ref': 'BillableHourPanel',
          },
          {
            'ref': 'BillableHourTimesheetView',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLegalCaseBillableHourPanelOrbital. */
export const StdLegalCaseBillableHourPanelOrbitalManifest = {
  organism: 'std-legal-case',
  orbitalName: 'BillableHourPanelOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'BillableHourAppLayout',
    'BillableHourTimesheetView',
  ] as const,
  inlineTraitNames: [
    'BillableHourPanel',
  ] as const,
};

/** Typed guard — runtime validates StdLegalCaseBillableHourPanelOrbitalParams keys. */
export function isStdLegalCaseBillableHourPanelOrbitalParams(p: object): p is StdLegalCaseBillableHourPanelOrbitalParams {
  type _OverrideRecord = NonNullable<StdLegalCaseBillableHourPanelOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLegalCaseBillableHourPanelOrbitalManifest.traitNames,
      ...StdLegalCaseBillableHourPanelOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the CourtDeadlinePanelOrbital orbital.
 *
 * Canonical entity: CourtDeadline — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLegalCaseCourtDeadlinePanelOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'CourtDeadlineAppLayout' | 'CourtDeadlineDocketView' | 'CourtDeadlinePanel',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the CourtDeadlinePanelOrbital orbital with consumer params. */
export function stdLegalCaseCourtDeadlinePanelOrbital(params: StdLegalCaseCourtDeadlinePanelOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'CourtDeadline';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'courtdeadlines');
  const built = makeOrbitalWithUses({
    name: 'CourtDeadlinePanelOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-court-deadline',
        'as': 'CourtDeadline',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'matterId',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'title',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'deadlineType',
            'type': 'string',
            'default': 'filing',
            'values': [
              'filing',
              'response',
              'discovery',
              'hearing',
              'trial',
              'other',
            ],
          },
          {
            'name': 'dueAt',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'jurisdiction',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'assignedTo',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'upcoming',
            'values': [
              'upcoming',
              'completed',
              'missed',
              'extended',
            ],
          },
          {
            'name': 'reminderSent',
            'type': 'boolean',
            'default': false,
          },
          {
            'name': 'notes',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'CourtDeadlineAppLayout',
        'config': {
          'appName': 'Legal Case',
          'navItems': [
            {
              'href': '/matters',
              'icon': 'scale',
              'label': 'Matters',
            },
            {
              'label': 'Cases',
              'icon': 'folder',
              'href': '/cases',
            },
            {
              'label': 'Time',
              'icon': 'clock',
              'href': '/timesheet',
            },
            {
              'icon': 'calendar',
              'label': 'Deadlines',
              'href': '/deadlines',
            },
          ],
          'searchEvent': 'COURT_DEADLINE_SEARCH',
          'notifications': [],
          'contentTrait': '@trait.CourtDeadlinePanel',
          'notificationClickEvent': 'COURT_DEADLINE_NOTIFICATIONS_OPEN',
        },
        'events': {
          'SEARCH': 'COURT_DEADLINE_SEARCH',
          'NOTIFY_CLICK': 'COURT_DEADLINE_NOTIFICATIONS_OPEN',
        },
      }),
      rebindInlineTraitEntity({
        'name': 'CourtDeadlinePanel',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'type': 'stack',
                    'direction': 'vertical',
                    'gap': 'lg',
                    'children': [
                      {
                        'direction': 'horizontal',
                        'gap': 'sm',
                        'align': 'center',
                        'children': [
                          {
                            'type': 'icon',
                            'name': 'calendar',
                          },
                          {
                            'content': 'Court Deadlines',
                            'variant': 'h2',
                            'type': 'typography',
                          },
                        ],
                        'type': 'stack',
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.CourtDeadlineDocketView',
                    ],
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'CourtDeadline', canonicalName) as never,
      makeTraitRef({
        'ref': 'CourtDeadline.traits.CourtDeadlineDocket',
        'name': 'CourtDeadlineDocketView',
        'config': {
          'title': 'Filing Deadlines',
        },
      }),
    ],
    pages: [
      {
        'name': 'DeadlinesPage',
        'path': '/deadlines',
        'traits': [
          {
            'ref': 'CourtDeadlineAppLayout',
          },
          {
            'ref': 'CourtDeadlinePanel',
          },
          {
            'ref': 'CourtDeadlineDocketView',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLegalCaseCourtDeadlinePanelOrbital. */
export const StdLegalCaseCourtDeadlinePanelOrbitalManifest = {
  organism: 'std-legal-case',
  orbitalName: 'CourtDeadlinePanelOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'CourtDeadlineAppLayout',
    'CourtDeadlineDocketView',
  ] as const,
  inlineTraitNames: [
    'CourtDeadlinePanel',
  ] as const,
};

/** Typed guard — runtime validates StdLegalCaseCourtDeadlinePanelOrbitalParams keys. */
export function isStdLegalCaseCourtDeadlinePanelOrbitalParams(p: object): p is StdLegalCaseCourtDeadlinePanelOrbitalParams {
  type _OverrideRecord = NonNullable<StdLegalCaseCourtDeadlinePanelOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLegalCaseCourtDeadlinePanelOrbitalManifest.traitNames,
      ...StdLegalCaseCourtDeadlinePanelOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the MatterPanelOrbital orbital.
 *
 * Canonical entity: MatterCase — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   persistence    — entity persistence mode
 *   entityName     — rename the canonical entity
 *   collection     — override the derived collection key
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdLegalCaseMatterPanelOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /** Override derived collection key (defaults to plural(entityName).toLowerCase()). */
  collection?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'MatterCaseAppLayout' | 'MatterCaseDocketView' | 'MatterCasePanel',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** Per-orbital factory: builds the MatterPanelOrbital orbital with consumer params. */
export function stdLegalCaseMatterPanelOrbital(params: StdLegalCaseMatterPanelOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = params.entityName ?? 'MatterCase';
  const collectionName = params.collection
    ?? (params.entityName ? `${params.entityName.toLowerCase()}s` : 'matters');
  const built = makeOrbitalWithUses({
    name: 'MatterPanelOrbital',
    uses: [
      {
        'from': 'std/behaviors/std-app-layout',
        'as': 'AppShell',
      },
      {
        'from': 'std/behaviors/std-matter',
        'as': 'Matter',
      },
    ],
    entity: {
      name: canonicalName,
      collection: collectionName,
      persistence: params.persistence ?? 'persistent',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'name': 'id',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'matterNumber',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'title',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'clientName',
            'type': 'string',
            'required': true,
          },
          {
            'name': 'practiceArea',
            'type': 'string',
            'default': 'other',
            'values': [
              'litigation',
              'transactional',
              'regulatory',
              'ip',
              'immigration',
              'family',
              'other',
            ],
          },
          {
            'name': 'leadAttorney',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'status',
            'type': 'string',
            'default': 'open',
            'values': [
              'open',
              'active',
              'on_hold',
              'closed',
              'archived',
            ],
          },
          {
            'name': 'openedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'closedAt',
            'type': 'string',
            'default': '',
          },
          {
            'name': 'notes',
            'type': 'string',
            'default': '',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      makeTraitRef({
        'ref': 'AppShell.traits.AppLayout',
        'name': 'MatterCaseAppLayout',
        'config': {
          'navItems': [
            {
              'icon': 'scale',
              'label': 'Matters',
              'href': '/matters',
            },
            {
              'label': 'Cases',
              'href': '/cases',
              'icon': 'folder',
            },
            {
              'label': 'Time',
              'icon': 'clock',
              'href': '/timesheet',
            },
            {
              'label': 'Deadlines',
              'href': '/deadlines',
              'icon': 'calendar',
            },
          ],
          'notifications': [],
          'contentTrait': '@trait.MatterCasePanel',
          'searchEvent': 'MATTER_CASE_SEARCH',
          'notificationClickEvent': 'MATTER_CASE_NOTIFICATIONS_OPEN',
          'appName': 'Legal Case',
        },
        'events': {
          'NOTIFY_CLICK': 'MATTER_CASE_NOTIFICATIONS_OPEN',
          'SEARCH': 'MATTER_CASE_SEARCH',
        },
      }),
      rebindInlineTraitEntity({
        'name': 'MatterCasePanel',
        'category': 'interaction',
        'stateMachine': {
          'states': [
            {
              'name': 'composing',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
          ],
          'transitions': [
            {
              'from': 'composing',
              'to': 'composing',
              'event': 'INIT',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'direction': 'vertical',
                    'type': 'stack',
                    'className': 'max-w-6xl mx-auto w-full p-4',
                    'children': [
                      {
                        'direction': 'horizontal',
                        'align': 'center',
                        'type': 'stack',
                        'gap': 'sm',
                        'children': [
                          {
                            'type': 'icon',
                            'name': 'folder',
                          },
                          {
                            'variant': 'h2',
                            'content': 'Cases',
                            'type': 'typography',
                          },
                        ],
                      },
                      {
                        'type': 'divider',
                      },
                      '@trait.MatterCaseDocketView',
                    ],
                    'gap': 'lg',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never, 'MatterCase', canonicalName) as never,
      makeTraitRef({
        'ref': 'Matter.traits.MatterDocket',
        'name': 'MatterCaseDocketView',
        'linkedEntity': canonicalName,
        'config': {
          'title': 'Case Files',
        },
      }),
    ],
    pages: [
      {
        'name': 'CasesPage',
        'path': '/cases',
        'traits': [
          {
            'ref': 'MatterCaseAppLayout',
          },
          {
            'ref': 'MatterCasePanel',
          },
          {
            'ref': 'MatterCaseDocketView',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.name !== undefined) merged.name = override.name;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdLegalCaseMatterPanelOrbital. */
export const StdLegalCaseMatterPanelOrbitalManifest = {
  organism: 'std-legal-case',
  orbitalName: 'MatterPanelOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'collection', type: 'string', description: 'Override derived collection key. Defaults to plural(entityName).toLowerCase().' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'MatterCaseAppLayout',
    'MatterCaseDocketView',
  ] as const,
  inlineTraitNames: [
    'MatterCasePanel',
  ] as const,
};

/** Typed guard — runtime validates StdLegalCaseMatterPanelOrbitalParams keys. */
export function isStdLegalCaseMatterPanelOrbitalParams(p: object): p is StdLegalCaseMatterPanelOrbitalParams {
  type _OverrideRecord = NonNullable<StdLegalCaseMatterPanelOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdLegalCaseMatterPanelOrbitalManifest.traitNames,
      ...StdLegalCaseMatterPanelOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-legal-case — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLegalCaseParams {
  Matter?: StdLegalCaseMatterOrbitalParams;
  BillableHourPanel?: StdLegalCaseBillableHourPanelOrbitalParams;
  CourtDeadlinePanel?: StdLegalCaseCourtDeadlinePanelOrbitalParams;
  MatterPanel?: StdLegalCaseMatterPanelOrbitalParams;
}

/** Whole-organism descriptor (4 orbitals). Composes per-orbital factories. */
export function stdLegalCase(params: StdLegalCaseParams = {}): OrbitalDefinition[] {
  return [
    stdLegalCaseMatterOrbital(params.Matter ?? {}),
    stdLegalCaseBillableHourPanelOrbital(params.BillableHourPanel ?? {}),
    stdLegalCaseCourtDeadlinePanelOrbital(params.CourtDeadlinePanel ?? {}),
    stdLegalCaseMatterPanelOrbital(params.MatterPanel ?? {}),
  ];
}
