/**
 * std-margin-account — std-margin-account — margin account leverage and call thresholds for trading apps.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-margin-account.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-margin-account
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-margin-account';
const ALIAS = 'MarginAccount';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMarginAccountEventKey = 'CLOSE_VIEW' | 'INIT' | 'ISSUE_CALL' | 'LIQUIDATE' | 'MarginAccountLoadFailed' | 'MarginAccountLoaded' | 'OPEN_ACCOUNT';

/**
 * Payload shape for the `OPEN_ACCOUNT` event.
 */
export interface StdMarginAccountOpenAccountPayload {
  id: string;
  row?: {
    id: string;
    accountHolderId: string;
    equityUSD?: number;
    marginUsedUSD?: number;
    marginAvailableUSD?: number;
    status?: string;
    lastCallAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ISSUE_CALL` event.
 */
export interface StdMarginAccountIssueCallPayload {
  id: string;
  row?: {
    id: string;
    accountHolderId: string;
    equityUSD?: number;
    marginUsedUSD?: number;
    marginAvailableUSD?: number;
    status?: string;
    lastCallAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `LIQUIDATE` event.
 */
export interface StdMarginAccountLiquidatePayload {
  id: string;
  row?: {
    id: string;
    accountHolderId: string;
    equityUSD?: number;
    marginUsedUSD?: number;
    marginAvailableUSD?: number;
    status?: string;
    lastCallAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `MarginAccountLoaded` event.
 */
export interface StdMarginAccountMarginAccountLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `MarginAccountLoadFailed` event.
 */
export interface StdMarginAccountMarginAccountLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMarginAccountConfig {
  /** Default: `"2x"` */
  marginLeverage?: '2x' | '4x' | '10x' | '20x';
  /** Default: `[{"icon":"arrow-right","event":"OPEN_ACCOUNT","label":"Open","variant":"primary"},{"label":"Issue Call","event":"ISSUE_CALL","icon":"alert-triangle","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"Margin Accounts"` */
  title?: string;
  /** Default: `30` */
  marginCallThresholdPct?: number;
  /** Default: `[{"name":"accountHolderId","variant":"caption","label":"Holder"},{"variant":"caption","name":"equityUSD","label":"Equity"},{"variant":"caption","name":"marginUsedUSD","label":"Used"},{"label":"Available","variant":"caption","name":"marginAvailableUSD"},{"label":"Status","name":"status","variant":"badge"},{"variant":"caption","label":"Last Call","name":"lastCallAt"}]` */
  fields?: EntityRow[];
  /** Default: `20` */
  forcedLiquidationThresholdPct?: number;
}

/**
 * Params for the std-margin-account descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMarginAccountParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMarginAccountEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMarginAccountConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MarginAccount.traits.MarginAccountManage`. */
export function stdMarginAccountTrait(params: StdMarginAccountParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MarginAccountManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MarginAccount.pages.MarginAccountManagePage`. */
export function stdMarginAccountPage(params: StdMarginAccountParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MarginAccountManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMarginAccount(params: StdMarginAccountParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'MarginAccountOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMarginAccountTrait(params),
    ],
    pages: [
      stdMarginAccountPage(params),
    ],
  });
}
