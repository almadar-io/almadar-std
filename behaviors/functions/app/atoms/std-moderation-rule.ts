/**
 * std-moderation-rule — std-moderation-rule — content moderation policy layer over std-mod-queue with thresholds, auto-actions, appeal + escalation.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-moderation-rule.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-moderation-rule
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-moderation-rule';
const ALIAS = 'ModerationRule';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdModerationRuleEventKey = 'ARCHIVE_RULE' | 'CLOSE_VIEW' | 'INIT' | 'ModerationRuleLoadFailed' | 'ModerationRuleLoaded' | 'OPEN_RULE' | 'TEST_RULE';

/**
 * Payload shape for the `OPEN_RULE` event.
 */
export interface StdModerationRuleOpenRulePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    appliesTo?: string;
    priority?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `TEST_RULE` event.
 */
export interface StdModerationRuleTestRulePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    appliesTo?: string;
    priority?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARCHIVE_RULE` event.
 */
export interface StdModerationRuleArchiveRulePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    appliesTo?: string;
    priority?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ModerationRuleLoaded` event.
 */
export interface StdModerationRuleModerationRuleLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ModerationRuleLoadFailed` event.
 */
export interface StdModerationRuleModerationRuleLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdModerationRuleConfig {
  /** Default: `48` */
  appealWindowHours?: number;
  /** Default: `[{"variant":"primary","event":"OPEN_RULE","icon":"arrow-right","label":"Open"},{"label":"Archive","variant":"danger","event":"ARCHIVE_RULE"}]` */
  itemActions?: EntityRow[];
  /** Default: `0.6` */
  humanReviewThreshold?: number;
  /** Default: `"flag"` */
  autoAction?: 'flag' | 'hide' | 'shadow-ban' | 'delete' | 'ban-user';
  /** Default: `0.85` */
  toxicityThreshold?: number;
  /** Default: `"Moderation Rules"` */
  title?: string;
  /** Default: `"tempban"` */
  repeatOffenderEscalation?: 'warn' | 'tempban' | 'ban';
  /** Default: `[{"label":"Rule","name":"name","variant":"caption"},{"label":"Applies to","variant":"caption","name":"appliesTo"},{"variant":"caption","name":"priority","label":"Priority"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-moderation-rule descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdModerationRuleParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdModerationRuleEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdModerationRuleConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ModerationRule.traits.ModerationRuleManage`. */
export function stdModerationRuleTrait(params: StdModerationRuleParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ModerationRuleManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ModerationRule.pages.ModerationRulePage`. */
export function stdModerationRulePage(params: StdModerationRuleParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ModerationRulePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdModerationRule(params: StdModerationRuleParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ModerationRuleOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdModerationRuleTrait(params),
    ],
    pages: [
      stdModerationRulePage(params),
    ],
  });
}
