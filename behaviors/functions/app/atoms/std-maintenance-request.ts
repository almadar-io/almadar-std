/**
 * std-maintenance-request — std-maintenance-request — tenant request lifecycle. Listens for SET_STATUS so std-lifecycle can auto-escalate. Exposes Scheduling + Service knobs (bookingWindowDays, responseTimeMinutes, resolutionSLAHours, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-maintenance-request.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-maintenance-request
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-maintenance-request';
const ALIAS = 'MaintenanceRequest';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMaintenanceRequestEventKey = 'CANCEL_REQUEST' | 'CLOSE_VIEW' | 'COMPLETE_REQUEST' | 'INIT' | 'MaintenanceRequestLoadFailed' | 'MaintenanceRequestLoaded' | 'MaintenanceRequestUpdateFailed' | 'MaintenanceRequestUpdated' | 'OPEN_REQUEST' | 'SCHEDULE_REQUEST' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_REQUEST` event.
 */
export interface StdMaintenanceRequestOpenRequestPayload {
  id: string;
  row?: {
    id: string;
    unitId: string;
    tenantId?: string;
    requesterName?: string;
    description: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    requestedAt?: string;
    scheduledAt?: string;
    completedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `SCHEDULE_REQUEST` event.
 */
export interface StdMaintenanceRequestScheduleRequestPayload {
  id: string;
  scheduledAt: string;
  row?: {
    id: string;
    unitId: string;
    tenantId?: string;
    requesterName?: string;
    description: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    requestedAt?: string;
    scheduledAt?: string;
    completedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `COMPLETE_REQUEST` event.
 */
export interface StdMaintenanceRequestCompleteRequestPayload {
  id: string;
  completedAt?: string;
  row?: {
    id: string;
    unitId: string;
    tenantId?: string;
    requesterName?: string;
    description: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    requestedAt?: string;
    scheduledAt?: string;
    completedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `CANCEL_REQUEST` event.
 */
export interface StdMaintenanceRequestCancelRequestPayload {
  id: string;
  notes?: string;
  row?: {
    id: string;
    unitId: string;
    tenantId?: string;
    requesterName?: string;
    description: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    requestedAt?: string;
    scheduledAt?: string;
    completedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `MaintenanceRequestLoaded` event.
 */
export interface StdMaintenanceRequestMaintenanceRequestLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `MaintenanceRequestLoadFailed` event.
 */
export interface StdMaintenanceRequestMaintenanceRequestLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `MaintenanceRequestUpdated` event.
 */
export interface StdMaintenanceRequestMaintenanceRequestUpdatedPayload {
  row?: {
    id: string;
    unitId: string;
    tenantId?: string;
    requesterName?: string;
    description: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    requestedAt?: string;
    scheduledAt?: string;
    completedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `MaintenanceRequestUpdateFailed` event.
 */
export interface StdMaintenanceRequestMaintenanceRequestUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMaintenanceRequestConfig {
  /** Default: `12` */
  rescheduleWindowHours?: number;
  /** Default: `240` */
  responseTimeMinutes?: number;
  /** Default: `[]` */
  blackoutDates?: string[];
  /** Default: `30` */
  noShowGracePeriodMinutes?: number;
  /** Default: `48` */
  resolutionSLAHours?: number;
  /** Default: `72` */
  escalationThresholdHours?: number;
  /** Default: `[{"variant":"caption","name":"unitId","label":"Unit"},{"name":"requesterName","label":"Requester","variant":"caption"},{"label":"Description","variant":"caption","name":"description"},{"name":"priority","label":"Priority","variant":"badge"},{"variant":"badge","name":"status","label":"Status"},{"name":"requestedAt","label":"Requested","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `"Maintenance Requests"` */
  title?: string;
  /** Default: `false` */
  requirePhotoOnComplete?: boolean;
  /** Default: `[]` */
  skillsRequired?: string[];
  /** Default: `false` */
  requireSignatureOnComplete?: boolean;
  /** Default: `"free"` */
  cancellationPolicy?: 'free' | 'partial-refund' | 'no-refund';
  /** Default: `["scheduled","in_progress","completed"]` */
  customerVisibleStatuses?: string[];
  /** Default: `"nearest"` */
  dispatchStrategy?: 'nearest' | 'round-robin' | 'manual';
  /** Default: `[{"label":"Open","variant":"primary","event":"OPEN_REQUEST","icon":"arrow-right"},{"variant":"danger","event":"CANCEL_REQUEST","icon":"x","label":"Cancel"}]` */
  itemActions?: EntityRow[];
  /** Default: `[]` */
  serviceZones?: string[];
  /** Default: `120` */
  leadTimeMinutes?: number;
  /** Default: `30` */
  bookingWindowDays?: number;
  /** Default: `2` */
  maxReschedules?: number;
  /** Default: `25` */
  serviceAreaRadius?: number;
  /** Default: `"08:00-18:00"` */
  bookingHours?: string;
}

/**
 * Params for the std-maintenance-request descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMaintenanceRequestParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMaintenanceRequestEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMaintenanceRequestConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MaintenanceRequest.traits.MaintenanceRequestQueue`. */
export function stdMaintenanceRequestTrait(params: StdMaintenanceRequestParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MaintenanceRequestQueue`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MaintenanceRequest.pages.MaintenanceRequestPage`. */
export function stdMaintenanceRequestPage(params: StdMaintenanceRequestParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MaintenanceRequestPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMaintenanceRequest(params: StdMaintenanceRequestParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'MaintenanceRequestOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMaintenanceRequestTrait(params),
    ],
    pages: [
      stdMaintenanceRequestPage(params),
    ],
  });
}
