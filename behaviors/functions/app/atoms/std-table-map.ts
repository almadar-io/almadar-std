/**
 * std-table-map — std-table-map — restaurant POS interactive floor plan: TableNode entities rendered as draggable nodes (round/rectangle/square) with status (empty/seated/ordered/awaiting-bill/cleaning). Owns a TableNode entity and a single manage trait that loads tables, renders them on a TableFloorPlan canvas, and persists select / move / seat / clear actions back to the row.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-table-map.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-table-map
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-table-map';
const ALIAS = 'TableMap';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTableMapEventKey = 'CLEAR_TABLE' | 'INIT' | 'MOVE_TABLE' | 'SEAT' | 'SELECT_TABLE' | 'TableLoadFailed' | 'TableLoaded' | 'TableSelected' | 'TableUpdated' | 'TableWriteFailed';

/**
 * Payload shape for the `SELECT_TABLE` event.
 */
export interface StdTableMapSelectTablePayload {
  id: string;
}

/**
 * Payload shape for the `MOVE_TABLE` event.
 */
export interface StdTableMapMoveTablePayload {
  id: string;
  x: number;
  y: number;
}

/**
 * Payload shape for the `SEAT` event.
 */
export interface StdTableMapSeatPayload {
  id: string;
  partySize?: number;
  serverName?: string;
}

/**
 * Payload shape for the `CLEAR_TABLE` event.
 */
export interface StdTableMapClearTablePayload {
  id: string;
}

/**
 * Payload shape for the `TableLoaded` event.
 */
export interface StdTableMapTableLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TableLoadFailed` event.
 */
export interface StdTableMapTableLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `TableSelected` event.
 */
export interface StdTableMapTableSelectedPayload {
  id: string;
}

/**
 * Payload shape for the `TableUpdated` event.
 */
export interface StdTableMapTableUpdatedPayload {
  row?: {
    id: string;
    label: string;
    x?: number;
    y?: number;
    shape?: string;
    capacity: number;
    status?: string;
    partySize?: number;
    serverName?: string;
    currentOrderId?: string;
  };
}

/**
 * Payload shape for the `TableWriteFailed` event.
 */
export interface StdTableMapTableWriteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTableMapConfig {
  /** Default: `800` */
  canvasWidth?: number;
  /** Default: `600` */
  canvasHeight?: number;
  /** Default: `"Floor Plan"` */
  title?: string;
}

/**
 * Params for the std-table-map descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTableMapParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTableMapEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTableMapConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TableMap.traits.TableManage`. */
export function stdTableMapTrait(params: StdTableMapParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TableManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TableMap.pages.TableManagePage`. */
export function stdTableMapPage(params: StdTableMapParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TableManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTableMap(params: StdTableMapParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TableMapOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTableMapTrait(params),
    ],
    pages: [
      stdTableMapPage(params),
    ],
  });
}
