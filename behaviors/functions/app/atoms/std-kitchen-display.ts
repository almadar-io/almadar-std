/**
 * std-kitchen-display — std-kitchen-display — restaurant POS kitchen display system. Owns a KitchenTicket entity (one row per kitchen-bound order) and a single management trait that loads pending tickets, renders them in a data-grid with Mark Preparing / Ready / Served actions, and persists status + aging back to the row. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-kitchen-display.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-kitchen-display
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-kitchen-display';
const ALIAS = 'KitchenDisplay';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdKitchenDisplayEventKey = 'CLOSE_VIEW' | 'INIT' | 'KitchenTicketLoadFailed' | 'KitchenTicketLoaded' | 'KitchenTicketUpdateFailed' | 'KitchenTicketUpdated' | 'MARK_PREPARING' | 'MARK_READY' | 'MARK_SERVED' | 'OPEN_TICKET';

/**
 * Payload shape for the `OPEN_TICKET` event.
 */
export interface StdKitchenDisplayOpenTicketPayload {
  id: string;
  row?: {
    id: string;
    orderId: string;
    tableLabel?: string;
    serverName?: string;
    itemsJson?: string;
    status?: string;
    aging?: string;
    receivedAt?: string;
    readyAt?: string;
    servedAt?: string;
  };
}

/**
 * Payload shape for the `MARK_PREPARING` event.
 */
export interface StdKitchenDisplayMarkPreparingPayload {
  ticketId: string;
  row?: {
    id: string;
    orderId: string;
    tableLabel?: string;
    serverName?: string;
    itemsJson?: string;
    status?: string;
    aging?: string;
    receivedAt?: string;
    readyAt?: string;
    servedAt?: string;
  };
}

/**
 * Payload shape for the `MARK_READY` event.
 */
export interface StdKitchenDisplayMarkReadyPayload {
  ticketId: string;
  readyAt: string;
  row?: {
    id: string;
    orderId: string;
    tableLabel?: string;
    serverName?: string;
    itemsJson?: string;
    status?: string;
    aging?: string;
    receivedAt?: string;
    readyAt?: string;
    servedAt?: string;
  };
}

/**
 * Payload shape for the `MARK_SERVED` event.
 */
export interface StdKitchenDisplayMarkServedPayload {
  ticketId: string;
  servedAt: string;
  row?: {
    id: string;
    orderId: string;
    tableLabel?: string;
    serverName?: string;
    itemsJson?: string;
    status?: string;
    aging?: string;
    receivedAt?: string;
    readyAt?: string;
    servedAt?: string;
  };
}

/**
 * Payload shape for the `KitchenTicketLoaded` event.
 */
export interface StdKitchenDisplayKitchenTicketLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `KitchenTicketLoadFailed` event.
 */
export interface StdKitchenDisplayKitchenTicketLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `KitchenTicketUpdated` event.
 */
export interface StdKitchenDisplayKitchenTicketUpdatedPayload {
  row?: {
    id: string;
    orderId: string;
    tableLabel?: string;
    serverName?: string;
    itemsJson?: string;
    status?: string;
    aging?: string;
    receivedAt?: string;
    readyAt?: string;
    servedAt?: string;
  };
}

/**
 * Payload shape for the `KitchenTicketUpdateFailed` event.
 */
export interface StdKitchenDisplayKitchenTicketUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdKitchenDisplayConfig {
  /** Default: `[{"variant":"primary","icon":"arrow-right","event":"OPEN_TICKET","label":"Open"},{"label":"Mark Served","variant":"danger","icon":"utensils","event":"MARK_SERVED"}]` */
  itemActions?: TraitConfig[];
  /** Default: `"Kitchen Display"` */
  title?: string;
  /** Default: `20` */
  overdueMinutes?: number;
  /** Default: `[{"name":"orderId","label":"Order","variant":"caption"},{"label":"Table","variant":"caption","name":"tableLabel"},{"name":"serverName","variant":"caption","label":"Server"},{"label":"Status","name":"status","variant":"badge"},{"variant":"badge","name":"aging","label":"Aging"},{"label":"Received","name":"receivedAt","variant":"caption"}]` */
  fields?: TraitConfig[];
}

/**
 * Params for the std-kitchen-display descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdKitchenDisplayParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdKitchenDisplayEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdKitchenDisplayConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `KitchenDisplay.traits.KitchenTicketManage`. */
export function stdKitchenDisplayTrait(params: StdKitchenDisplayParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.KitchenTicketManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `KitchenDisplay.pages.KitchenTicketManagePage`. */
export function stdKitchenDisplayPage(params: StdKitchenDisplayParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.KitchenTicketManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdKitchenDisplay(params: StdKitchenDisplayParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'KitchenTicketOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdKitchenDisplayTrait(params),
    ],
    pages: [
      stdKitchenDisplayPage(params),
    ],
  });
}
