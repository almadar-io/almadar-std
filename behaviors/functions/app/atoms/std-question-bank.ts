/**
 * std-question-bank — std-question-bank — typed question library for surveys. Lists questions; detail offers Update / Delete. Exposes Content/Knowledge knobs (visibilityScope, enabledLocales, keepVersions, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-question-bank.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-question-bank
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-question-bank';
const ALIAS = 'QuestionBank';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdQuestionBankEventKey = 'CLOSE_VIEW' | 'DELETE_QUESTION' | 'INIT' | 'OPEN_QUESTION' | 'QuestionLoadFailed' | 'QuestionLoaded' | 'UPDATE_QUESTION';

/**
 * Payload shape for the `OPEN_QUESTION` event.
 */
export interface StdQuestionBankOpenQuestionPayload {
  id: string;
  row?: {
    id: string;
    label: string;
    kind?: string;
    optionsJson?: string;
    category?: string;
    surveyId?: string;
    order?: number;
  };
}

/**
 * Payload shape for the `UPDATE_QUESTION` event.
 */
export interface StdQuestionBankUpdateQuestionPayload {
  id: string;
}

/**
 * Payload shape for the `DELETE_QUESTION` event.
 */
export interface StdQuestionBankDeleteQuestionPayload {
  id: string;
}

/**
 * Payload shape for the `QuestionLoaded` event.
 */
export interface StdQuestionBankQuestionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `QuestionLoadFailed` event.
 */
export interface StdQuestionBankQuestionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdQuestionBankConfig {
  /** Default: `["en"]` */
  enabledLocales?: string[];
  /** Default: `"en"` */
  defaultLocale?: string;
  /** Default: `[{"label":"Question","name":"label","variant":"caption"},{"name":"kind","label":"Type","variant":"badge"},{"name":"category","label":"Category","variant":"caption"},{"label":"Survey","name":"surveyId","variant":"caption"},{"label":"Order","name":"order","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `30` */
  draftAutosaveSeconds?: number;
  /** Default: `"internal"` */
  visibilityScope?: 'public' | 'internal' | 'customer';
  /** Default: `true` */
  allowRollback?: boolean;
  /** Default: `false` */
  scheduledPublishEnabled?: boolean;
  /** Default: `5` */
  keepVersions?: number;
  /** Default: `"Question Bank"` */
  title?: string;
  /** Default: `[{"event":"OPEN_QUESTION","variant":"primary","label":"Open","icon":"arrow-right"},{"event":"DELETE_QUESTION","variant":"danger","label":"Delete"}]` */
  itemActions?: EntityRow[];
  /** Default: `false` */
  seoEnabled?: boolean;
}

/**
 * Params for the std-question-bank descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdQuestionBankParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdQuestionBankEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdQuestionBankConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `QuestionBank.traits.QuestionManage`. */
export function stdQuestionBankTrait(params: StdQuestionBankParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.QuestionManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `QuestionBank.pages.QuestionManagePage`. */
export function stdQuestionBankPage(params: StdQuestionBankParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.QuestionManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdQuestionBank(params: StdQuestionBankParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'QuestionBankOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdQuestionBankTrait(params),
    ],
    pages: [
      stdQuestionBankPage(params),
    ],
  });
}
