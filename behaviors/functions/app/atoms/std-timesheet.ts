/**
 * std-timesheet — std-timesheet — weekly timesheet primitive. Listens for SET_STATUS so std-lifecycle can auto-close periods. Exposes HR knobs (backgroundCheckRequired, anonymizeOnRejection, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-timesheet.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-timesheet
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-timesheet';
const ALIAS = 'Timesheet';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTimesheetEventKey = 'APPROVE' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_TIMESHEET' | 'REJECT' | 'REQUEST_DELETE' | 'REQUEST_REVISION' | 'SET_STATUS' | 'SUBMIT' | 'TimesheetLoadFailed' | 'TimesheetLoaded';

/**
 * Payload shape for the `OPEN_TIMESHEET` event.
 */
export interface StdTimesheetOpenTimesheetPayload {
  id: string;
  row?: {
    id: string;
    employeeId: string;
    employeeName?: string;
    periodStart: string;
    periodEnd: string;
    totalHours?: number;
    billableHours?: number;
    status?: string;
    submittedAt?: string;
    approvedBy?: string;
    approvedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `SUBMIT` event.
 */
export interface StdTimesheetSubmitPayload {
  id: string;
  row?: {
    id: string;
    employeeId: string;
    employeeName?: string;
    periodStart: string;
    periodEnd: string;
    totalHours?: number;
    billableHours?: number;
    status?: string;
    submittedAt?: string;
    approvedBy?: string;
    approvedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `APPROVE` event.
 */
export interface StdTimesheetApprovePayload {
  id: string;
  row?: {
    id: string;
    employeeId: string;
    employeeName?: string;
    periodStart: string;
    periodEnd: string;
    totalHours?: number;
    billableHours?: number;
    status?: string;
    submittedAt?: string;
    approvedBy?: string;
    approvedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdTimesheetRejectPayload {
  id: string;
  row?: {
    id: string;
    employeeId: string;
    employeeName?: string;
    periodStart: string;
    periodEnd: string;
    totalHours?: number;
    billableHours?: number;
    status?: string;
    submittedAt?: string;
    approvedBy?: string;
    approvedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REQUEST_REVISION` event.
 */
export interface StdTimesheetRequestRevisionPayload {
  id: string;
  row?: {
    id: string;
    employeeId: string;
    employeeName?: string;
    periodStart: string;
    periodEnd: string;
    totalHours?: number;
    billableHours?: number;
    status?: string;
    submittedAt?: string;
    approvedBy?: string;
    approvedAt?: string;
    notes?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdTimesheetRequestDeletePayload {
  id: string;
}

/**
 * Payload shape for the `TimesheetLoaded` event.
 */
export interface StdTimesheetTimesheetLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `TimesheetLoadFailed` event.
 */
export interface StdTimesheetTimesheetLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTimesheetConfig {
  /** Default: `false` */
  eVerifyRequired?: boolean;
  /** Default: `false` */
  i9DocumentsRequired?: boolean;
  /** Default: `"Timesheets"` */
  title?: string;
  /** Default: `[{"label":"Employee","variant":"caption","name":"employeeName"},{"name":"periodStart","label":"Period Start","variant":"caption"},{"label":"Period End","variant":"caption","name":"periodEnd"},{"label":"Total Hours","variant":"caption","name":"totalHours"},{"name":"billableHours","variant":"caption","label":"Billable"},{"label":"Status","variant":"badge","name":"status"},{"variant":"caption","name":"approvedBy","label":"Approved By"}]` */
  fields?: EntityRow[];
  /** Default: `1095` */
  retentionAfterRejectionDays?: number;
  /** Default: `false` */
  backgroundCheckRequired?: boolean;
  /** Default: `[{"icon":"arrow-right","event":"OPEN_TIMESHEET","variant":"primary","label":"Open"},{"label":"Delete","variant":"danger","event":"REQUEST_DELETE"}]` */
  itemActions?: EntityRow[];
  /** Default: `false` */
  anonymizeOnRejection?: boolean;
  /** Default: `false` */
  equalOpportunityTrackingEnabled?: boolean;
}

/**
 * Params for the std-timesheet descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTimesheetParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTimesheetEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTimesheetConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Timesheet.traits.TimesheetLedger`. */
export function stdTimesheetTrait(params: StdTimesheetParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TimesheetLedger`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Timesheet.pages.TimesheetPage`. */
export function stdTimesheetPage(params: StdTimesheetParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TimesheetPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTimesheet(params: StdTimesheetParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TimesheetOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTimesheetTrait(params),
    ],
    pages: [
      stdTimesheetPage(params),
    ],
  });
}
