/**
 * std-incident — std-incident — devops/oncall incident lifecycle (open → acknowledged → investigating → mitigated → resolved). Listens for SET_STATUS so siblings can drive automated transitions. Exposes Incident-pattern knobs (severity, runbookId, pagedRoles, escalation, postmortem threshold, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-incident.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-incident
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-incident';
const ALIAS = 'Incident';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdIncidentEventKey = 'ACK' | 'CLOSE_VIEW' | 'INCIDENT_OPEN' | 'INIT' | 'IncidentLoadFailed' | 'IncidentLoaded' | 'MITIGATE' | 'RESOLVE' | 'SET_STATUS';

/**
 * Payload shape for the `INCIDENT_OPEN` event.
 */
export interface StdIncidentIncidentOpenPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description: string;
    severity?: string;
    status?: string;
    pagedTo?: string;
    runbookId?: string;
    acknowledgedAt?: string;
    mitigatedAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ACK` event.
 */
export interface StdIncidentAckPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description: string;
    severity?: string;
    status?: string;
    pagedTo?: string;
    runbookId?: string;
    acknowledgedAt?: string;
    mitigatedAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `MITIGATE` event.
 */
export interface StdIncidentMitigatePayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description: string;
    severity?: string;
    status?: string;
    pagedTo?: string;
    runbookId?: string;
    acknowledgedAt?: string;
    mitigatedAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `RESOLVE` event.
 */
export interface StdIncidentResolvePayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description: string;
    severity?: string;
    status?: string;
    pagedTo?: string;
    runbookId?: string;
    acknowledgedAt?: string;
    mitigatedAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `IncidentLoaded` event.
 */
export interface StdIncidentIncidentLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `IncidentLoadFailed` event.
 */
export interface StdIncidentIncidentLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdIncidentConfig {
  /** Default: `"sev1-2"` */
  customerCommsRequired?: 'always' | 'sev1-only' | 'sev1-2' | 'never';
  /** Default: `[{"icon":"arrow-right","label":"Open","variant":"primary","event":"INCIDENT_OPEN"},{"label":"Resolve","variant":"primary","icon":"check","event":"RESOLVE"}]` */
  itemActions?: EntityRow[];
  /** Default: `"sev2"` */
  postmortemRequiredAbove?: 'sev1' | 'sev2' | 'sev3' | 'sev4' | 'sev5';
  /** Default: `"Incidents"` */
  title?: string;
  /** Default: `["oncall-primary"]` */
  pagedRoles?: string[];
  /** Default: `true` */
  autoCloseAfterResolution?: boolean;
  /** Default: `"sev3"` */
  severity?: 'sev1' | 'sev2' | 'sev3' | 'sev4' | 'sev5';
  /** Default: `""` */
  runbookId?: string;
  /** Default: `15` */
  pageEscalationMinutes?: number;
  /** Default: `[{"variant":"caption","name":"title","label":"Title"},{"variant":"badge","label":"Severity","name":"severity"},{"name":"status","variant":"badge","label":"Status"},{"label":"Paged","name":"pagedTo","variant":"caption"},{"label":"Runbook","variant":"caption","name":"runbookId"}]` */
  fields?: EntityRow[];
  /** Default: `5` */
  acknowledgementDeadlineMinutes?: number;
}

/**
 * Params for the std-incident descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdIncidentParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdIncidentEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdIncidentConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Incident.traits.IncidentManage`. */
export function stdIncidentTrait(params: StdIncidentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.IncidentManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Incident.pages.IncidentManagePage`. */
export function stdIncidentPage(params: StdIncidentParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.IncidentManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdIncident(params: StdIncidentParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'IncidentOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdIncidentTrait(params),
    ],
    pages: [
      stdIncidentPage(params),
    ],
  });
}
