/**
 * std-work-order — std-work-order — field-service work order lifecycle (created → assigned → en-route → on-site → completed). Lists work orders in `browsing`; opens detail in `viewing_single` with Assign / Travel / Arrive / Complete / Cancel actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-work-order.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-work-order
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-work-order';
const ALIAS = 'WorkOrder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdWorkOrderEventKey = 'ARRIVE' | 'ASSIGN' | 'CANCEL' | 'CLOSE_VIEW' | 'COMPLETE' | 'INIT' | 'OPEN_ORDER' | 'START_TRAVEL' | 'WorkOrderLoadFailed' | 'WorkOrderLoaded';

/**
 * Payload shape for the `OPEN_ORDER` event.
 */
export interface StdWorkOrderOpenOrderPayload {
  id: string;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ASSIGN` event.
 */
export interface StdWorkOrderAssignPayload {
  id: string;
  assignedTo: string;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `START_TRAVEL` event.
 */
export interface StdWorkOrderStartTravelPayload {
  id: string;
  etaMinutes: number;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARRIVE` event.
 */
export interface StdWorkOrderArrivePayload {
  id: string;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `COMPLETE` event.
 */
export interface StdWorkOrderCompletePayload {
  id: string;
  notes?: string;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `CANCEL` event.
 */
export interface StdWorkOrderCancelPayload {
  id: string;
  notes?: string;
  row?: {
    id: string;
    customerName: string;
    serviceType: string;
    address: string;
    priority?: string;
    status?: string;
    assignedTo?: string;
    scheduledAt?: string;
    etaMinutes?: number;
    notes?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `WorkOrderLoaded` event.
 */
export interface StdWorkOrderWorkOrderLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `WorkOrderLoadFailed` event.
 */
export interface StdWorkOrderWorkOrderLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdWorkOrderConfig {
  /** Default: `[{"event":"OPEN_ORDER","icon":"arrow-right","label":"Open","variant":"primary"},{"variant":"danger","label":"Cancel","event":"CANCEL","icon":"x"}]` */
  itemActions?: EntityRow[];
  /** Default: `[{"variant":"caption","label":"Customer","name":"customerName"},{"variant":"caption","label":"Service","name":"serviceType"},{"variant":"caption","label":"Address","name":"address"},{"name":"priority","variant":"badge","label":"Priority"},{"label":"Status","name":"status","variant":"badge"},{"name":"assignedTo","variant":"caption","label":"Assigned"},{"variant":"caption","label":"ETA (min)","name":"etaMinutes"}]` */
  fields?: EntityRow[];
  /** Default: `"Work Orders"` */
  title?: string;
}

/**
 * Params for the std-work-order descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdWorkOrderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdWorkOrderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdWorkOrderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `WorkOrder.traits.WorkOrderManage`. */
export function stdWorkOrderTrait(params: StdWorkOrderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.WorkOrderManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `WorkOrder.pages.WorkOrderManagePage`. */
export function stdWorkOrderPage(params: StdWorkOrderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.WorkOrderManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdWorkOrder(params: StdWorkOrderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'WorkOrderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdWorkOrderTrait(params),
    ],
    pages: [
      stdWorkOrderPage(params),
    ],
  });
}
