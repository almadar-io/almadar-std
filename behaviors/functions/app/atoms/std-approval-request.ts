/**
 * std-approval-request — std-approval-request — approver inbox / triage queue. Browses many pending approval requests in a list (data-grid), opens one at a time for detail with Approve / Reject / Request Info / Withdraw. Pick this when the user is an approver working through a queue. For single-item N-step sequential approval (manager → finance → exec on ONE item), use std-approval-chain instead. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-approval-request.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-approval-request
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-approval-request';
const ALIAS = 'ApprovalRequest';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdApprovalRequestEventKey = 'APPROVE' | 'ApprovalRequestLoadFailed' | 'ApprovalRequestLoaded' | 'CANCEL_REJECTION' | 'CLOSE_REQUEST' | 'EDIT_COMMENT' | 'INIT' | 'OPEN_REQUEST' | 'REJECT' | 'REQUEST_INFO' | 'SUBMIT_REJECTION' | 'WITHDRAW';

/**
 * Payload shape for the `OPEN_REQUEST` event.
 */
export interface StdApprovalRequestOpenRequestPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    targetType?: string;
    targetId: string;
    requestedBy: string;
    requestedAt?: string;
    assignedApprover?: string;
    approverDecision?: string;
    decidedAt?: string;
    comment?: string;
    status?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `APPROVE` event.
 */
export interface StdApprovalRequestApprovePayload {
  id: string;
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdApprovalRequestRejectPayload {
  id: string;
}

/**
 * Payload shape for the `REQUEST_INFO` event.
 */
export interface StdApprovalRequestRequestInfoPayload {
  id: string;
}

/**
 * Payload shape for the `WITHDRAW` event.
 */
export interface StdApprovalRequestWithdrawPayload {
  id: string;
}

/**
 * Payload shape for the `EDIT_COMMENT` event.
 */
export interface StdApprovalRequestEditCommentPayload {
  comment?: string;
}

/**
 * Payload shape for the `SUBMIT_REJECTION` event.
 */
export interface StdApprovalRequestSubmitRejectionPayload {
  id: string;
  comment?: string;
}

/**
 * Payload shape for the `ApprovalRequestLoaded` event.
 */
export interface StdApprovalRequestApprovalRequestLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `ApprovalRequestLoadFailed` event.
 */
export interface StdApprovalRequestApprovalRequestLoadFailedPayload {
  message?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdApprovalRequestConfig {
  /** Default: `"Approval Requests"` */
  title?: string;
  /** Default: `[{"label":"Open","variant":"primary","icon":"arrow-right","event":"OPEN_REQUEST"},{"event":"WITHDRAW","variant":"danger","label":"Withdraw"}]` */
  itemActions?: EntityRow[];
  /** Default: `false` */
  requireCommentOnReject?: boolean;
  /** Default: `[{"label":"Title","variant":"caption","name":"title"},{"variant":"badge","name":"targetType","label":"Target"},{"name":"requestedBy","label":"Requested By","variant":"caption"},{"variant":"caption","name":"assignedApprover","label":"Approver"},{"name":"approverDecision","label":"Decision","variant":"badge"},{"name":"status","label":"Status","variant":"badge"},{"variant":"caption","name":"decidedAt","label":"Decided"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-approval-request descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdApprovalRequestParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdApprovalRequestEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdApprovalRequestConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ApprovalRequest.traits.ApprovalRequestPipeline`. */
export function stdApprovalRequestTrait(params: StdApprovalRequestParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApprovalRequestPipeline`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ApprovalRequest.pages.ApprovalRequestPage`. */
export function stdApprovalRequestPage(params: StdApprovalRequestParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ApprovalRequestPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdApprovalRequest(params: StdApprovalRequestParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ApprovalRequestOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdApprovalRequestTrait(params),
    ],
    pages: [
      stdApprovalRequestPage(params),
    ],
  });
}
