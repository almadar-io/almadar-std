/**
 * std-chart-of-accounts — std-chart-of-accounts — accounts catalog (asset/liability/equity/revenue/expense) with Activate / Deactivate actions. Exposes Financial-pattern knobs (currency, jurisdiction, retention).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-chart-of-accounts.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-chart-of-accounts
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-chart-of-accounts';
const ALIAS = 'ChartOfAccounts';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdChartOfAccountsEventKey = 'ACTIVATE_ACCOUNT' | 'AccountLoadFailed' | 'AccountLoaded' | 'CLOSE_VIEW' | 'DEACTIVATE_ACCOUNT' | 'INIT' | 'OPEN_ACCOUNT';

/**
 * Payload shape for the `OPEN_ACCOUNT` event.
 */
export interface StdChartOfAccountsOpenAccountPayload {
  id: string;
  row?: {
    id: string;
    code: string;
    name: string;
    parentCode?: string;
    isActive?: boolean;
    balance?: number;
  };
}

/**
 * Payload shape for the `ACTIVATE_ACCOUNT` event.
 */
export interface StdChartOfAccountsActivateAccountPayload {
  id: string;
}

/**
 * Payload shape for the `DEACTIVATE_ACCOUNT` event.
 */
export interface StdChartOfAccountsDeactivateAccountPayload {
  id: string;
}

/**
 * Payload shape for the `AccountLoaded` event.
 */
export interface StdChartOfAccountsAccountLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `AccountLoadFailed` event.
 */
export interface StdChartOfAccountsAccountLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdChartOfAccountsConfig {
  /** Default: `"none"` */
  taxStrategy?: 'inclusive' | 'exclusive' | 'none';
  /** Default: `"none"` */
  lateFeePolicy?: 'none' | 'flat' | 'percent';
  /** Default: `0` */
  lateFeeAmount?: number;
  /** Default: `""` */
  complianceJurisdiction?: string;
  /** Default: `[{"icon":"arrow-right","label":"Open","variant":"primary","event":"OPEN_ACCOUNT"},{"event":"DEACTIVATE_ACCOUNT","label":"Deactivate","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"bankers"` */
  roundingMode?: 'bankers' | 'half-up' | 'down';
  /** Default: `"USD"` */
  currency?: string;
  /** Default: `7` */
  retentionYears?: number;
  /** Default: `0` */
  gracePeriodDays?: number;
  /** Default: `[{"name":"code","label":"Code","variant":"caption"},{"variant":"caption","name":"name","label":"Name"},{"label":"Type","name":"type","variant":"badge"},{"name":"parentCode","label":"Parent","variant":"caption"},{"name":"balance","label":"Balance","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `"Chart of Accounts"` */
  title?: string;
}

/**
 * Params for the std-chart-of-accounts descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdChartOfAccountsParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdChartOfAccountsEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdChartOfAccountsConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ChartOfAccounts.traits.AccountChart`. */
export function stdChartOfAccountsTrait(params: StdChartOfAccountsParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AccountChart`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ChartOfAccounts.pages.AccountPage`. */
export function stdChartOfAccountsPage(params: StdChartOfAccountsParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AccountPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdChartOfAccounts(params: StdChartOfAccountsParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ChartOfAccountsOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdChartOfAccountsTrait(params),
    ],
    pages: [
      stdChartOfAccountsPage(params),
    ],
  });
}
