/**
 * std-conversation-policy — std-conversation-policy — chat/realtime governance: retention, e2ee, moderation mode, edit/delete windows.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-conversation-policy.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-conversation-policy
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-conversation-policy';
const ALIAS = 'ConversationPolicy';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdConversationPolicyEventKey = 'ARCHIVE_POLICY' | 'CLOSE_VIEW' | 'ConversationPolicyLoadFailed' | 'ConversationPolicyLoaded' | 'INIT' | 'OPEN_POLICY';

/**
 * Payload shape for the `OPEN_POLICY` event.
 */
export interface StdConversationPolicyOpenPolicyPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    appliesTo?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARCHIVE_POLICY` event.
 */
export interface StdConversationPolicyArchivePolicyPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    appliesTo?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ConversationPolicyLoaded` event.
 */
export interface StdConversationPolicyConversationPolicyLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ConversationPolicyLoadFailed` event.
 */
export interface StdConversationPolicyConversationPolicyLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdConversationPolicyConfig {
  /** Default: `false` */
  e2eeRequired?: boolean;
  /** Default: `"Conversation Policies"` */
  title?: string;
  /** Default: `15` */
  editWindowMinutes?: number;
  /** Default: `[{"variant":"primary","label":"Open","event":"OPEN_POLICY","icon":"arrow-right"},{"event":"ARCHIVE_POLICY","label":"Archive","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `365` */
  retentionDays?: number;
  /** Default: `"post-hoc"` */
  moderationMode?: 'off' | 'post-hoc' | 'pre-publish' | 'human-only';
  /** Default: `1440` */
  deleteWindowMinutes?: number;
  /** Default: `[{"label":"Policy","variant":"caption","name":"name"},{"name":"appliesTo","label":"Applies to","variant":"caption"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-conversation-policy descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdConversationPolicyParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdConversationPolicyEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdConversationPolicyConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ConversationPolicy.traits.ConversationPolicyManage`. */
export function stdConversationPolicyTrait(params: StdConversationPolicyParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ConversationPolicyManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ConversationPolicy.pages.ConversationPolicyPage`. */
export function stdConversationPolicyPage(params: StdConversationPolicyParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ConversationPolicyPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdConversationPolicy(params: StdConversationPolicyParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ConversationPolicyOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdConversationPolicyTrait(params),
    ],
    pages: [
      stdConversationPolicyPage(params),
    ],
  });
}
