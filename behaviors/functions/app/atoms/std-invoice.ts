/**
 * std-invoice — std-invoice — invoice generation atom. Owns an Invoice entity (line-items + tax + total + refund tiers via config). Browses invoices in a data-grid with Send / Mark-Paid / Void / Refund actions; viewing state renders the canonical invoice pattern detail. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-invoice.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-invoice
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-invoice';
const ALIAS = 'Invoice';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdInvoiceEventKey = 'CLOSE_VIEW' | 'INIT' | 'InvoiceLoadFailed' | 'InvoiceLoaded' | 'InvoiceRefundFailed' | 'InvoiceRefunded' | 'InvoiceUpdateFailed' | 'InvoiceUpdated' | 'MARK_PAID' | 'OPEN_INVOICE' | 'REFUND' | 'SEND' | 'VOID';

/**
 * Payload shape for the `OPEN_INVOICE` event.
 */
export interface StdInvoiceOpenInvoicePayload {
  id: string;
  row?: {
    id: string;
    invoiceNumber: string;
    customerId: string;
    subscriptionId?: string;
    status?: string;
    currency?: string;
    subtotal?: number;
    tax?: number;
    total?: number;
    issuedAt?: string;
    dueAt?: string;
    paidAt?: string;
    lineItemsJson?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `SEND` event.
 */
export interface StdInvoiceSendPayload {
  invoiceId: string;
}

/**
 * Payload shape for the `MARK_PAID` event.
 */
export interface StdInvoiceMarkPaidPayload {
  invoiceId: string;
}

/**
 * Payload shape for the `VOID` event.
 */
export interface StdInvoiceVoidPayload {
  invoiceId: string;
}

/**
 * Payload shape for the `REFUND` event.
 */
export interface StdInvoiceRefundPayload {
  invoiceId: string;
  amount: number;
}

/**
 * Payload shape for the `InvoiceLoaded` event.
 */
export interface StdInvoiceInvoiceLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `InvoiceLoadFailed` event.
 */
export interface StdInvoiceInvoiceLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `InvoiceUpdated` event.
 */
export interface StdInvoiceInvoiceUpdatedPayload {
  row?: {
    id: string;
    invoiceNumber: string;
    customerId: string;
    subscriptionId?: string;
    status?: string;
    currency?: string;
    subtotal?: number;
    tax?: number;
    total?: number;
    issuedAt?: string;
    dueAt?: string;
    paidAt?: string;
    lineItemsJson?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `InvoiceUpdateFailed` event.
 */
export interface StdInvoiceInvoiceUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `InvoiceRefunded` event.
 */
export interface StdInvoiceInvoiceRefundedPayload {
  row?: {
    id: string;
    invoiceNumber: string;
    customerId: string;
    subscriptionId?: string;
    status?: string;
    currency?: string;
    subtotal?: number;
    tax?: number;
    total?: number;
    issuedAt?: string;
    dueAt?: string;
    paidAt?: string;
    lineItemsJson?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `InvoiceRefundFailed` event.
 */
export interface StdInvoiceInvoiceRefundFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdInvoiceConfig {
  /** Default: `[{"label":"Invoice #","variant":"caption","name":"invoiceNumber"},{"name":"customerId","label":"Customer","variant":"caption"},{"name":"status","label":"Status","variant":"badge"},{"label":"Total","variant":"caption","name":"total"},{"name":"currency","label":"Currency","variant":"badge"},{"variant":"caption","label":"Due","name":"dueAt"}]` */
  fields?: EntityRow[];
  /** Default: `[{"event":"OPEN_INVOICE","icon":"arrow-right","label":"Open","variant":"primary"},{"variant":"danger","icon":"x","label":"Void","event":"VOID"}]` */
  itemActions?: EntityRow[];
  /** Default: `"[]"` */
  refundTiersJson?: string;
  /** Default: `"Invoices"` */
  title?: string;
}

/**
 * Params for the std-invoice descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdInvoiceParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdInvoiceEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdInvoiceConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Invoice.traits.InvoiceManage`. */
export function stdInvoiceTrait(params: StdInvoiceParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.InvoiceManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Invoice.pages.InvoiceManagePage`. */
export function stdInvoicePage(params: StdInvoiceParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.InvoiceManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdInvoice(params: StdInvoiceParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'InvoiceOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdInvoiceTrait(params),
    ],
    pages: [
      stdInvoicePage(params),
    ],
  });
}
