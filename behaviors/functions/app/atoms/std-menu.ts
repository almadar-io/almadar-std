/**
 * std-menu — std-menu — restaurant POS menu items grouped by category with availability windows. Owns a MenuItem entity (price, category, available, prep time, modifier groups) and a single manage trait that loads items, browses them in a data-grid with Create / Update / Toggle-Available / Delete actions, and persists each verdict back to the row. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-menu.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-menu
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-menu';
const ALIAS = 'Menu';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMenuEventKey = 'CLOSE_VIEW' | 'CREATE' | 'DELETE' | 'INIT' | 'MenuItemCreated' | 'MenuItemDeleted' | 'MenuItemLoadFailed' | 'MenuItemLoaded' | 'MenuItemUpdated' | 'MenuItemWriteFailed' | 'OPEN_ITEM' | 'TOGGLE_AVAILABLE' | 'UPDATE';

/**
 * Payload shape for the `OPEN_ITEM` event.
 */
export interface StdMenuOpenItemPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `CREATE` event.
 */
export interface StdMenuCreatePayload {
  data: EntityRow;
}

/**
 * Payload shape for the `UPDATE` event.
 */
export interface StdMenuUpdatePayload {
  itemId: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
  data: EntityRow;
}

/**
 * Payload shape for the `TOGGLE_AVAILABLE` event.
 */
export interface StdMenuToggleAvailablePayload {
  itemId: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `DELETE` event.
 */
export interface StdMenuDeletePayload {
  itemId: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `MenuItemLoaded` event.
 */
export interface StdMenuMenuItemLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `MenuItemLoadFailed` event.
 */
export interface StdMenuMenuItemLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `MenuItemCreated` event.
 */
export interface StdMenuMenuItemCreatedPayload {
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `MenuItemUpdated` event.
 */
export interface StdMenuMenuItemUpdatedPayload {
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `MenuItemDeleted` event.
 */
export interface StdMenuMenuItemDeletedPayload {
  row?: {
    id: string;
    name: string;
    description?: string;
    price: number;
    currency?: string;
    category: string;
    imageUrl?: string;
    available?: boolean;
    availableFrom?: string;
    availableUntil?: string;
    tags?: string[];
    modifierGroupIds?: string[];
    prepTimeMinutes?: number;
    order?: number;
  };
}

/**
 * Payload shape for the `MenuItemWriteFailed` event.
 */
export interface StdMenuMenuItemWriteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMenuConfig {
  /** Default: `[{"label":"Item","variant":"caption","name":"name"},{"label":"Category","name":"category","variant":"badge"},{"name":"price","variant":"caption","label":"Price"},{"name":"currency","label":"Currency","variant":"badge"},{"name":"prepTimeMinutes","variant":"caption","label":"Prep (min)"},{"variant":"badge","name":"available","label":"Available"}]` */
  fields?: EntityRow[];
  /** Default: `"Menu"` */
  title?: string;
  /** Default: `[{"event":"OPEN_ITEM","icon":"arrow-right","variant":"primary","label":"Open"},{"label":"Delete","event":"DELETE","variant":"danger","icon":"trash"}]` */
  itemActions?: EntityRow[];
}

/**
 * Params for the std-menu descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMenuParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMenuEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMenuConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Menu.traits.MenuItemManage`. */
export function stdMenuTrait(params: StdMenuParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MenuItemManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Menu.pages.MenuItemManagePage`. */
export function stdMenuPage(params: StdMenuParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MenuItemManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMenu(params: StdMenuParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'MenuOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMenuTrait(params),
    ],
    pages: [
      stdMenuPage(params),
    ],
  });
}
