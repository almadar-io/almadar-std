/**
 * std-legal-hold — std-legal-hold — legal hold trigger, duration, and release approval; companion to std-matter and std-court-deadline.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-legal-hold.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-legal-hold
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-legal-hold';
const ALIAS = 'LegalHold';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLegalHoldEventKey = 'CLOSE_VIEW' | 'EXTEND_HOLD' | 'INIT' | 'LegalHoldLoadFailed' | 'LegalHoldLoaded' | 'OPEN_HOLD' | 'RELEASE_HOLD';

/**
 * Payload shape for the `OPEN_HOLD` event.
 */
export interface StdLegalHoldOpenHoldPayload {
  id: string;
  row?: {
    id: string;
    matterId?: string;
    triggeredBy?: string;
    triggeredAt?: string;
    expectedReleaseAt?: string;
    status?: string;
    affectedEntities?: string[];
    description?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `EXTEND_HOLD` event.
 */
export interface StdLegalHoldExtendHoldPayload {
  id: string;
  row?: {
    id: string;
    matterId?: string;
    triggeredBy?: string;
    triggeredAt?: string;
    expectedReleaseAt?: string;
    status?: string;
    affectedEntities?: string[];
    description?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `RELEASE_HOLD` event.
 */
export interface StdLegalHoldReleaseHoldPayload {
  id: string;
  row?: {
    id: string;
    matterId?: string;
    triggeredBy?: string;
    triggeredAt?: string;
    expectedReleaseAt?: string;
    status?: string;
    affectedEntities?: string[];
    description?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `LegalHoldLoaded` event.
 */
export interface StdLegalHoldLegalHoldLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `LegalHoldLoadFailed` event.
 */
export interface StdLegalHoldLegalHoldLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdLegalHoldConfig {
  /** Default: `[{"variant":"caption","label":"Matter","name":"matterId"},{"name":"triggeredBy","variant":"caption","label":"Trigger"},{"label":"Status","variant":"badge","name":"status"},{"label":"Started","name":"triggeredAt","variant":"caption"},{"name":"expectedReleaseAt","variant":"caption","label":"Releases"}]` */
  fields?: EntityRow[];
  /** Default: `"Legal Holds"` */
  title?: string;
  /** Default: `[{"icon":"arrow-right","event":"OPEN_HOLD","variant":"primary","label":"Open"},{"event":"RELEASE_HOLD","variant":"danger","label":"Release"}]` */
  itemActions?: EntityRow[];
  /** Default: `"litigation"` */
  triggeredBy?: 'litigation' | 'regulatory-inquiry' | 'internal-investigation' | 'audit';
  /** Default: `365` */
  holdDurationDays?: number;
  /** Default: `["general-counsel","compliance-officer"]` */
  releaseApprovalRoles?: string[];
  /** Default: `["records-manager","it-admin"]` */
  notifyRolesOnHold?: string[];
}

/**
 * Params for the std-legal-hold descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdLegalHoldParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdLegalHoldEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdLegalHoldConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `LegalHold.traits.LegalHoldPipeline`. */
export function stdLegalHoldTrait(params: StdLegalHoldParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.LegalHoldPipeline`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `LegalHold.pages.LegalHoldPage`. */
export function stdLegalHoldPage(params: StdLegalHoldParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.LegalHoldPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdLegalHold(params: StdLegalHoldParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'LegalHoldOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdLegalHoldTrait(params),
    ],
    pages: [
      stdLegalHoldPage(params),
    ],
  });
}
