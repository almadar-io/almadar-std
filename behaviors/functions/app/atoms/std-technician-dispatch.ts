/**
 * std-technician-dispatch — std-technician-dispatch — dispatch board for field technicians. Lists technicians in `browsing`; opens detail in `viewing_single` with Assign / Accept / Reject / Toggle Availability actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-technician-dispatch.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-technician-dispatch
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-technician-dispatch';
const ALIAS = 'TechnicianDispatch';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTechnicianDispatchEventKey = 'ACCEPT' | 'ASSIGN' | 'AssignmentAccepted' | 'AssignmentRejected' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_TECHNICIAN' | 'REJECT' | 'TOGGLE_AVAILABLE' | 'TechnicianLoadFailed' | 'TechnicianLoaded' | 'TechnicianUpdateFailed' | 'TechnicianUpdated';

/**
 * Payload shape for the `OPEN_TECHNICIAN` event.
 */
export interface StdTechnicianDispatchOpenTechnicianPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    phone?: string;
    skills?: string[];
    available?: boolean;
    currentOrderId?: string;
    lat?: number;
    lng?: number;
    lastSeenAt?: string;
  };
}

/**
 * Payload shape for the `ASSIGN` event.
 */
export interface StdTechnicianDispatchAssignPayload {
  technicianId: string;
  orderId: string;
}

/**
 * Payload shape for the `ACCEPT` event.
 */
export interface StdTechnicianDispatchAcceptPayload {
  technicianId: string;
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdTechnicianDispatchRejectPayload {
  technicianId: string;
}

/**
 * Payload shape for the `TOGGLE_AVAILABLE` event.
 */
export interface StdTechnicianDispatchToggleAvailablePayload {
  technicianId: string;
  available: boolean;
}

/**
 * Payload shape for the `TechnicianLoaded` event.
 */
export interface StdTechnicianDispatchTechnicianLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TechnicianLoadFailed` event.
 */
export interface StdTechnicianDispatchTechnicianLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `TechnicianUpdated` event.
 */
export interface StdTechnicianDispatchTechnicianUpdatedPayload {
  row?: {
    id: string;
    name: string;
    phone?: string;
    skills?: string[];
    available?: boolean;
    currentOrderId?: string;
    lat?: number;
    lng?: number;
    lastSeenAt?: string;
  };
}

/**
 * Payload shape for the `TechnicianUpdateFailed` event.
 */
export interface StdTechnicianDispatchTechnicianUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `AssignmentAccepted` event.
 */
export interface StdTechnicianDispatchAssignmentAcceptedPayload {
  technicianId: string;
}

/**
 * Payload shape for the `AssignmentRejected` event.
 */
export interface StdTechnicianDispatchAssignmentRejectedPayload {
  technicianId: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTechnicianDispatchConfig {
  /** Default: `[{"icon":"arrow-right","variant":"primary","event":"OPEN_TECHNICIAN","label":"Open"},{"event":"ASSIGN","label":"Assign","variant":"danger"}]` */
  itemActions?: TraitConfig[];
  /** Default: `"Dispatch Board"` */
  title?: string;
  /** Default: `[{"label":"Technician","variant":"body","name":"name"},{"name":"skills","label":"Skills","variant":"caption"},{"label":"Available","variant":"badge","name":"available"},{"variant":"caption","label":"Current Order","name":"currentOrderId"},{"label":"Last Seen","variant":"caption","name":"lastSeenAt"}]` */
  fields?: TraitConfig[];
}

/**
 * Params for the std-technician-dispatch descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTechnicianDispatchParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTechnicianDispatchEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTechnicianDispatchConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TechnicianDispatch.traits.TechnicianDispatch`. */
export function stdTechnicianDispatchTrait(params: StdTechnicianDispatchParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TechnicianDispatch`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TechnicianDispatch.pages.TechnicianDispatchPage`. */
export function stdTechnicianDispatchPage(params: StdTechnicianDispatchParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TechnicianDispatchPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTechnicianDispatch(params: StdTechnicianDispatchParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TechnicianDispatchOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTechnicianDispatchTrait(params),
    ],
    pages: [
      stdTechnicianDispatchPage(params),
    ],
  });
}
