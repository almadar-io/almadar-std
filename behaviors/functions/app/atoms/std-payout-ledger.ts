/**
 * std-payout-ledger — std-payout-ledger — marketplace vendor payout ledger with Withdraw / Mark Paid actions. Listens for SET_STATUS so std-lifecycle can drive automated transitions. Exposes Financial-pattern knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-payout-ledger.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-payout-ledger
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-payout-ledger';
const ALIAS = 'PayoutLedger';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdPayoutLedgerEventKey = 'CLOSE_VIEW' | 'INIT' | 'MARK_PAID' | 'OPEN_PAYOUT' | 'PayoutLoadFailed' | 'PayoutLoaded' | 'PayoutUpdateFailed' | 'PayoutUpdated' | 'REQUEST_WITHDRAW' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_PAYOUT` event.
 */
export interface StdPayoutLedgerOpenPayoutPayload {
  id: string;
  row?: {
    id: string;
    vendorId: string;
    amount: number;
    currency?: string;
    status?: string;
    bankAccountMasked?: string;
    requestedAt?: string;
    paidAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `REQUEST_WITHDRAW` event.
 */
export interface StdPayoutLedgerRequestWithdrawPayload {
  id: string;
}

/**
 * Payload shape for the `MARK_PAID` event.
 */
export interface StdPayoutLedgerMarkPaidPayload {
  id: string;
}

/**
 * Payload shape for the `PayoutLoaded` event.
 */
export interface StdPayoutLedgerPayoutLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `PayoutLoadFailed` event.
 */
export interface StdPayoutLedgerPayoutLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `PayoutUpdated` event.
 */
export interface StdPayoutLedgerPayoutUpdatedPayload {
  row?: {
    id: string;
    vendorId: string;
    amount: number;
    currency?: string;
    status?: string;
    bankAccountMasked?: string;
    requestedAt?: string;
    paidAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `PayoutUpdateFailed` event.
 */
export interface StdPayoutLedgerPayoutUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdPayoutLedgerConfig {
  /** Default: `0` */
  lateFeeAmount?: number;
  /** Default: `"USD"` */
  currency?: string;
  /** Default: `"none"` */
  lateFeePolicy?: 'none' | 'flat' | 'percent';
  /** Default: `"none"` */
  taxStrategy?: 'inclusive' | 'exclusive' | 'none';
  /** Default: `7` */
  retentionYears?: number;
  /** Default: `3` */
  gracePeriodDays?: number;
  /** Default: `[{"variant":"caption","name":"vendorId","label":"Vendor"},{"name":"amount","variant":"caption","label":"Amount"},{"label":"Currency","name":"currency","variant":"badge"},{"label":"Status","name":"status","variant":"badge"},{"variant":"caption","label":"Bank","name":"bankAccountMasked"},{"name":"requestedAt","variant":"caption","label":"Requested"}]` */
  fields?: EntityRow[];
  /** Default: `""` */
  complianceJurisdiction?: string;
  /** Default: `"half-up"` */
  roundingMode?: 'bankers' | 'half-up' | 'down';
  /** Default: `50` */
  minimumWithdrawal?: number;
  /** Default: `"Payouts"` */
  title?: string;
  /** Default: `[{"label":"Open","event":"OPEN_PAYOUT","variant":"primary","icon":"arrow-right"},{"label":"Withdraw","variant":"danger","event":"REQUEST_WITHDRAW"}]` */
  itemActions?: EntityRow[];
}

/**
 * Params for the std-payout-ledger descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdPayoutLedgerParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdPayoutLedgerEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdPayoutLedgerConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `PayoutLedger.traits.PayoutLedger`. */
export function stdPayoutLedgerTrait(params: StdPayoutLedgerParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PayoutLedger`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `PayoutLedger.pages.PayoutLedgerPage`. */
export function stdPayoutLedgerPage(params: StdPayoutLedgerParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.PayoutLedgerPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdPayoutLedger(params: StdPayoutLedgerParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'PayoutOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdPayoutLedgerTrait(params),
    ],
    pages: [
      stdPayoutLedgerPage(params),
    ],
  });
}
