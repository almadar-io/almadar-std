/**
 * std-portfolio — std-portfolio — portfolio rebalancing, tax-lot accounting, and drift policies for trading apps.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-portfolio.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-portfolio
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-portfolio';
const ALIAS = 'Portfolio';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdPortfolioEventKey = 'ARCHIVE' | 'CLOSE_VIEW' | 'FREEZE' | 'INIT' | 'OPEN_PORTFOLIO' | 'PortfolioLoadFailed' | 'PortfolioLoaded' | 'REBALANCE';

/**
 * Payload shape for the `OPEN_PORTFOLIO` event.
 */
export interface StdPortfolioOpenPortfolioPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    ownerAccountId: string;
    currentValueUSD?: number;
    targetAllocationJson?: string;
    driftBps?: number;
    status?: string;
    lastRebalancedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `REBALANCE` event.
 */
export interface StdPortfolioRebalancePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    ownerAccountId: string;
    currentValueUSD?: number;
    targetAllocationJson?: string;
    driftBps?: number;
    status?: string;
    lastRebalancedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `FREEZE` event.
 */
export interface StdPortfolioFreezePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    ownerAccountId: string;
    currentValueUSD?: number;
    targetAllocationJson?: string;
    driftBps?: number;
    status?: string;
    lastRebalancedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARCHIVE` event.
 */
export interface StdPortfolioArchivePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    ownerAccountId: string;
    currentValueUSD?: number;
    targetAllocationJson?: string;
    driftBps?: number;
    status?: string;
    lastRebalancedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `PortfolioLoaded` event.
 */
export interface StdPortfolioPortfolioLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `PortfolioLoadFailed` event.
 */
export interface StdPortfolioPortfolioLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdPortfolioConfig {
  /** Default: `90` */
  rebalancingFrequencyDays?: number;
  /** Default: `[{"name":"name","variant":"caption","label":"Name"},{"variant":"caption","name":"ownerAccountId","label":"Owner"},{"name":"currentValueUSD","variant":"caption","label":"Value (USD)"},{"label":"Drift (bps)","variant":"caption","name":"driftBps"},{"variant":"badge","name":"status","label":"Status"},{"name":"lastRebalancedAt","label":"Last Rebalanced","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `500` */
  driftThresholdBps?: number;
  /** Default: `5` */
  cashBufferPct?: number;
  /** Default: `"FIFO"` */
  taxLotMethod?: 'FIFO' | 'LIFO' | 'HIFO' | 'specific-lot';
  /** Default: `"Portfolios"` */
  title?: string;
  /** Default: `"auto"` */
  dividendReinvestPolicy?: 'auto' | 'cash' | 'specified-fund';
  /** Default: `[{"icon":"arrow-right","event":"OPEN_PORTFOLIO","label":"Open","variant":"primary"},{"variant":"primary","icon":"refresh-cw","event":"REBALANCE","label":"Rebalance"}]` */
  itemActions?: EntityRow[];
}

/**
 * Params for the std-portfolio descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdPortfolioParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdPortfolioEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdPortfolioConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Portfolio.traits.PortfolioManage`. */
export function stdPortfolioTrait(params: StdPortfolioParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PortfolioManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Portfolio.pages.PortfolioManagePage`. */
export function stdPortfolioPage(params: StdPortfolioParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.PortfolioManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdPortfolio(params: StdPortfolioParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'PortfolioOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdPortfolioTrait(params),
    ],
    pages: [
      stdPortfolioPage(params),
    ],
  });
}
