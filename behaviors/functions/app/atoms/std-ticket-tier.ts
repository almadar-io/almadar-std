/**
 * std-ticket-tier — std-ticket-tier — event ticket pricing tiers (VIP, general, early-bird) with quantity tracking. Owns a TicketTier entity (price, quantityTotal, quantitySold, status) and a single manage trait that loads tiers, browses them in a data-grid with Create / Update / Sell / Close actions, and persists each verdict back to the row.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-ticket-tier.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-ticket-tier
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-ticket-tier';
const ALIAS = 'TicketTier';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTicketTierEventKey = 'CLOSE' | 'CREATE' | 'INIT' | 'SELL' | 'TicketTierClosed' | 'TicketTierCreated' | 'TicketTierLoadFailed' | 'TicketTierLoaded' | 'TicketTierSold' | 'TicketTierUpdated' | 'TicketTierWriteFailed' | 'UPDATE';

/**
 * Payload shape for the `CREATE` event.
 */
export interface StdTicketTierCreatePayload {
  data: EntityRow;
}

/**
 * Payload shape for the `UPDATE` event.
 */
export interface StdTicketTierUpdatePayload {
  tierId: string;
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
  data: EntityRow;
}

/**
 * Payload shape for the `SELL` event.
 */
export interface StdTicketTierSellPayload {
  tierId: string;
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
  qty: number;
}

/**
 * Payload shape for the `CLOSE` event.
 */
export interface StdTicketTierClosePayload {
  tierId: string;
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `TicketTierLoaded` event.
 */
export interface StdTicketTierTicketTierLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TicketTierLoadFailed` event.
 */
export interface StdTicketTierTicketTierLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `TicketTierCreated` event.
 */
export interface StdTicketTierTicketTierCreatedPayload {
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `TicketTierUpdated` event.
 */
export interface StdTicketTierTicketTierUpdatedPayload {
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `TicketTierSold` event.
 */
export interface StdTicketTierTicketTierSoldPayload {
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `TicketTierClosed` event.
 */
export interface StdTicketTierTicketTierClosedPayload {
  row?: {
    id: string;
    eventId: string;
    name: string;
    price: number;
    currency?: string;
    quantityTotal: number;
    quantitySold: number;
    availableFrom?: string;
    availableUntil?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `TicketTierWriteFailed` event.
 */
export interface StdTicketTierTicketTierWriteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTicketTierConfig {
  /** Default: `"Ticket Tiers"` */
  title?: string;
}

/**
 * Params for the std-ticket-tier descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTicketTierParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTicketTierEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTicketTierConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TicketTier.traits.TicketTierManage`. */
export function stdTicketTierTrait(params: StdTicketTierParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TicketTierManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TicketTier.pages.TicketTierManagePage`. */
export function stdTicketTierPage(params: StdTicketTierParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TicketTierManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTicketTier(params: StdTicketTierParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TicketTierOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTicketTierTrait(params),
    ],
    pages: [
      stdTicketTierPage(params),
    ],
  });
}
