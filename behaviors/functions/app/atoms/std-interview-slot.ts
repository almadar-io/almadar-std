/**
 * std-interview-slot — std-interview-slot — scheduled interview between an applicant and one or more interviewers, with date/time, format, and status. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-interview-slot.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-interview-slot
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-interview-slot';
const ALIAS = 'InterviewSlot';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdInterviewSlotEventKey = 'CANCEL_INTERVIEW' | 'CLOSE_VIEW' | 'COMPLETE_INTERVIEW' | 'INIT' | 'InterviewSlotLoadFailed' | 'InterviewSlotLoaded' | 'OPEN_INTERVIEW';

/**
 * Payload shape for the `OPEN_INTERVIEW` event.
 */
export interface StdInterviewSlotOpenInterviewPayload {
  id: string;
  row?: {
    id: string;
    applicantId: string;
    applicantName?: string;
    interviewerName?: string;
    scheduledAt: string;
    durationMinutes?: number;
    format?: string;
    status?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `COMPLETE_INTERVIEW` event.
 */
export interface StdInterviewSlotCompleteInterviewPayload {
  id: string;
}

/**
 * Payload shape for the `CANCEL_INTERVIEW` event.
 */
export interface StdInterviewSlotCancelInterviewPayload {
  id: string;
}

/**
 * Payload shape for the `InterviewSlotLoaded` event.
 */
export interface StdInterviewSlotInterviewSlotLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `InterviewSlotLoadFailed` event.
 */
export interface StdInterviewSlotInterviewSlotLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdInterviewSlotConfig {
  /** Default: `"Interviews"` */
  title?: string;
  /** Default: `[{"name":"applicantName","label":"Applicant","variant":"caption"},{"variant":"caption","label":"Interviewer","name":"interviewerName"},{"label":"Scheduled","variant":"caption","name":"scheduledAt"},{"name":"format","variant":"badge","label":"Format"},{"label":"Status","name":"status","variant":"badge"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"event":"OPEN_INTERVIEW","variant":"primary","label":"Open","icon":"arrow-right"},{"event":"CANCEL_INTERVIEW","variant":"danger","label":"Cancel"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-interview-slot descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdInterviewSlotParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdInterviewSlotEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdInterviewSlotConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `InterviewSlot.traits.InterviewSlotSchedule`. */
export function stdInterviewSlotTrait(params: StdInterviewSlotParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.InterviewSlotSchedule`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `InterviewSlot.pages.InterviewSlotPage`. */
export function stdInterviewSlotPage(params: StdInterviewSlotParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.InterviewSlotPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdInterviewSlot(params: StdInterviewSlotParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'InterviewSlotOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdInterviewSlotTrait(params),
    ],
    pages: [
      stdInterviewSlotPage(params),
    ],
  });
}
