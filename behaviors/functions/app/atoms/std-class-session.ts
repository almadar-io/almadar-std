/**
 * std-class-session — std-class-session — scheduled fitness/yoga/dance class session. Lists sessions in `browsing`; opens session detail in `viewing_single` with Start / Cancel actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-class-session.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-class-session
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-class-session';
const ALIAS = 'ClassSession';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdClassSessionEventKey = 'CANCEL_CLASS' | 'CLOSE_VIEW' | 'ClassSessionLoadFailed' | 'ClassSessionLoaded' | 'INIT' | 'OPEN_SESSION' | 'START_CLASS';

/**
 * Payload shape for the `OPEN_SESSION` event.
 */
export interface StdClassSessionOpenSessionPayload {
  id: string;
  row?: {
    id: string;
    className: string;
    instructorName?: string;
    startTime: string;
    durationMinutes?: number;
    capacity?: number;
    bookedCount?: number;
    location?: string;
    status?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `START_CLASS` event.
 */
export interface StdClassSessionStartClassPayload {
  id: string;
}

/**
 * Payload shape for the `CANCEL_CLASS` event.
 */
export interface StdClassSessionCancelClassPayload {
  id: string;
}

/**
 * Payload shape for the `ClassSessionLoaded` event.
 */
export interface StdClassSessionClassSessionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ClassSessionLoadFailed` event.
 */
export interface StdClassSessionClassSessionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdClassSessionConfig {
  /** Default: `"Class Sessions"` */
  title?: string;
  /** Default: `[{"variant":"caption","label":"Class","name":"className"},{"label":"Instructor","variant":"caption","name":"instructorName"},{"label":"Start","name":"startTime","variant":"caption"},{"variant":"badge","name":"capacity","label":"Capacity"},{"variant":"badge","name":"bookedCount","label":"Booked"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"label":"Open","variant":"primary","event":"OPEN_SESSION","icon":"arrow-right"},{"event":"CANCEL_CLASS","variant":"danger","label":"Cancel"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-class-session descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdClassSessionParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdClassSessionEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdClassSessionConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ClassSession.traits.ClassSessionSchedule`. */
export function stdClassSessionTrait(params: StdClassSessionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ClassSessionSchedule`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ClassSession.pages.ClassSessionPage`. */
export function stdClassSessionPage(params: StdClassSessionParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ClassSessionPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdClassSession(params: StdClassSessionParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ClassSessionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdClassSessionTrait(params),
    ],
    pages: [
      stdClassSessionPage(params),
    ],
  });
}
