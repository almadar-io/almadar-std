/**
 * std-help-article — std-help-article — Public help center article primitive with title, body, category, view count, and published/draft status. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-help-article.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-help-article
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-help-article';
const ALIAS = 'HelpArticle';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdHelpArticleEventKey = 'ARCHIVE' | 'CLOSE_VIEW' | 'HelpArticleLoadFailed' | 'HelpArticleLoaded' | 'INIT' | 'OPEN_ARTICLE' | 'PUBLISH';

/**
 * Payload shape for the `OPEN_ARTICLE` event.
 */
export interface StdHelpArticleOpenArticlePayload {
  id: string;
  row?: {
    id: string;
    title: string;
    slug: string;
    body?: string;
    summary?: string;
    categoryId?: string;
    categoryName?: string;
    author?: string;
    status?: string;
    viewCount?: number;
    helpfulCount?: number;
    notHelpfulCount?: number;
    publishedAt?: string;
    updatedAt?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `PUBLISH` event.
 */
export interface StdHelpArticlePublishPayload {
  id: string;
}

/**
 * Payload shape for the `ARCHIVE` event.
 */
export interface StdHelpArticleArchivePayload {
  id: string;
}

/**
 * Payload shape for the `HelpArticleLoaded` event.
 */
export interface StdHelpArticleHelpArticleLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `HelpArticleLoadFailed` event.
 */
export interface StdHelpArticleHelpArticleLoadFailedPayload {
  message?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdHelpArticleConfig {
  /** Default: `"Help Articles"` */
  title?: string;
  /** Default: `[{"variant":"caption","name":"title","label":"Title"},{"name":"slug","label":"Slug","variant":"caption"},{"name":"categoryName","label":"Category","variant":"caption"},{"label":"Author","name":"author","variant":"caption"},{"name":"status","label":"Status","variant":"badge"},{"label":"Views","variant":"caption","name":"viewCount"},{"variant":"caption","label":"Published","name":"publishedAt"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"variant":"primary","event":"OPEN_ARTICLE","label":"Open","icon":"arrow-right"},{"variant":"danger","label":"Archive","event":"ARCHIVE"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-help-article descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdHelpArticleParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdHelpArticleEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdHelpArticleConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `HelpArticle.traits.HelpArticleManage`. */
export function stdHelpArticleTrait(params: StdHelpArticleParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.HelpArticleManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `HelpArticle.pages.HelpArticlePage`. */
export function stdHelpArticlePage(params: StdHelpArticleParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.HelpArticlePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdHelpArticle(params: StdHelpArticleParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'HelpArticleOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdHelpArticleTrait(params),
    ],
    pages: [
      stdHelpArticlePage(params),
    ],
  });
}
