/**
 * std-dunning — std-dunning — failed-payment escalation ladder. Lists / escalates / resolves / suspends dunning cases. Listens for SET_STATUS so std-lifecycle can drive automated transitions. Exposes Financial-pattern knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-dunning.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-dunning
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-dunning';
const ALIAS = 'Dunning';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdDunningEventKey = 'CLOSE_VIEW' | 'DunningCaseLoadFailed' | 'DunningCaseLoaded' | 'DunningCaseUpdateFailed' | 'DunningCaseUpdated' | 'ESCALATE' | 'INIT' | 'OPEN_CASE' | 'RESOLVE' | 'SET_STATUS' | 'SUSPEND';

/**
 * Payload shape for the `OPEN_CASE` event.
 */
export interface StdDunningOpenCasePayload {
  id: string;
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    severity?: string;
    attemptNumber?: number;
    attemptsTotal?: number;
    failedAt?: string;
    nextRetryAt?: string;
    amountDue?: number;
    currency?: string;
    resolvedAt?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `ESCALATE` event.
 */
export interface StdDunningEscalatePayload {
  id: string;
}

/**
 * Payload shape for the `RESOLVE` event.
 */
export interface StdDunningResolvePayload {
  id: string;
}

/**
 * Payload shape for the `SUSPEND` event.
 */
export interface StdDunningSuspendPayload {
  id: string;
}

/**
 * Payload shape for the `DunningCaseLoaded` event.
 */
export interface StdDunningDunningCaseLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `DunningCaseLoadFailed` event.
 */
export interface StdDunningDunningCaseLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `DunningCaseUpdated` event.
 */
export interface StdDunningDunningCaseUpdatedPayload {
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    severity?: string;
    attemptNumber?: number;
    attemptsTotal?: number;
    failedAt?: string;
    nextRetryAt?: string;
    amountDue?: number;
    currency?: string;
    resolvedAt?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `DunningCaseUpdateFailed` event.
 */
export interface StdDunningDunningCaseUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdDunningConfig {
  /** Default: `7` */
  gracePeriodDays?: number;
  /** Default: `""` */
  complianceJurisdiction?: string;
  /** Default: `"USD"` */
  currency?: string;
  /** Default: `"none"` */
  taxStrategy?: 'inclusive' | 'exclusive' | 'none';
  /** Default: `"half-up"` */
  roundingMode?: 'bankers' | 'half-up' | 'down';
  /** Default: `0` */
  lateFeeAmount?: number;
  /** Default: `"Dunning Cases"` */
  title?: string;
  /** Default: `7` */
  retentionYears?: number;
  /** Default: `[{"event":"OPEN_CASE","variant":"primary","label":"Open","icon":"arrow-right"},{"icon":"ban","event":"SUSPEND","label":"Suspend","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `[{"name":"subscriptionId","label":"Subscription","variant":"caption"},{"name":"customerId","label":"Customer","variant":"caption"},{"name":"severity","label":"Severity","variant":"badge"},{"label":"Attempt","variant":"caption","name":"attemptNumber"},{"variant":"caption","label":"Amount Due","name":"amountDue"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: EntityRow[];
  /** Default: `"none"` */
  lateFeePolicy?: 'none' | 'flat' | 'percent';
}

/**
 * Params for the std-dunning descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdDunningParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdDunningEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdDunningConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Dunning.traits.DunningEscalate`. */
export function stdDunningTrait(params: StdDunningParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DunningEscalate`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Dunning.pages.DunningEscalatePage`. */
export function stdDunningPage(params: StdDunningParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DunningEscalatePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdDunning(params: StdDunningParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'DunningCaseOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdDunningTrait(params),
    ],
    pages: [
      stdDunningPage(params),
    ],
  });
}
