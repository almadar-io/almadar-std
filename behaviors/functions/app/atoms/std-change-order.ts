/**
 * std-change-order — std-change-order — construction change-order primitive tracking contract scope/cost/schedule modifications with owner approval. Lists orders in `browsing`; opens detail in `viewing_single` with Approve / Reject / Execute / Delete actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-change-order.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-change-order
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-change-order';
const ALIAS = 'ChangeOrder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdChangeOrderEventKey = 'APPROVE' | 'CLOSE_VIEW' | 'ChangeOrderLoadFailed' | 'ChangeOrderLoaded' | 'EXECUTE' | 'INIT' | 'OPEN_CHANGE_ORDER' | 'REJECT' | 'REQUEST_DELETE' | 'SUBMIT_FOR_APPROVAL';

/**
 * Payload shape for the `OPEN_CHANGE_ORDER` event.
 */
export interface StdChangeOrderOpenChangeOrderPayload {
  id: string;
  row?: {
    id: string;
    coNumber: string;
    description: string;
    reason?: string;
    costImpact?: number;
    scheduleImpactDays?: number;
    status?: string;
    requestedBy?: string;
    approvedBy?: string;
    requestedAt?: string;
    approvedAt?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdChangeOrderRequestDeletePayload {
  id: string;
  row?: {
    id: string;
    coNumber: string;
    description: string;
    reason?: string;
    costImpact?: number;
    scheduleImpactDays?: number;
    status?: string;
    requestedBy?: string;
    approvedBy?: string;
    requestedAt?: string;
    approvedAt?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `SUBMIT_FOR_APPROVAL` event.
 */
export interface StdChangeOrderSubmitForApprovalPayload {
  id: string;
}

/**
 * Payload shape for the `APPROVE` event.
 */
export interface StdChangeOrderApprovePayload {
  id: string;
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdChangeOrderRejectPayload {
  id: string;
}

/**
 * Payload shape for the `EXECUTE` event.
 */
export interface StdChangeOrderExecutePayload {
  id: string;
}

/**
 * Payload shape for the `ChangeOrderLoaded` event.
 */
export interface StdChangeOrderChangeOrderLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `ChangeOrderLoadFailed` event.
 */
export interface StdChangeOrderChangeOrderLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdChangeOrderConfig {
  /** Default: `[{"label":"CO #","variant":"caption","name":"coNumber"},{"name":"description","variant":"caption","label":"Description"},{"variant":"caption","name":"costImpact","label":"Cost Impact"},{"variant":"caption","label":"Days","name":"scheduleImpactDays"},{"label":"Status","variant":"badge","name":"status"},{"label":"Requested By","name":"requestedBy","variant":"caption"}]` */
  fields?: TraitConfig[];
  /** Default: `"Change Orders"` */
  title?: string;
  /** Default: `[{"event":"OPEN_CHANGE_ORDER","icon":"arrow-right","label":"Open","variant":"primary"},{"variant":"danger","label":"Delete","event":"REQUEST_DELETE"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-change-order descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdChangeOrderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdChangeOrderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdChangeOrderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ChangeOrder.traits.ChangeOrderLedger`. */
export function stdChangeOrderTrait(params: StdChangeOrderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ChangeOrderLedger`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ChangeOrder.pages.ChangeOrderPage`. */
export function stdChangeOrderPage(params: StdChangeOrderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ChangeOrderPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdChangeOrder(params: StdChangeOrderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ChangeOrderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdChangeOrderTrait(params),
    ],
    pages: [
      stdChangeOrderPage(params),
    ],
  });
}
