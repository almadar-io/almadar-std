/**
 * std-telematics-event — std-telematics-event — read-only per-vehicle telemetry stream from GPS/OBD-II devices. Lists events in `browsing`; opens event detail in `viewing_single` with Acknowledge action. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-telematics-event.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-telematics-event
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-telematics-event';
const ALIAS = 'TelematicsEvent';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTelematicsEventEventKey = 'ACKNOWLEDGE' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_EVENT' | 'TelematicsEventLoadFailed' | 'TelematicsEventLoaded';

/**
 * Payload shape for the `OPEN_EVENT` event.
 */
export interface StdTelematicsEventOpenEventPayload {
  id: string;
  row?: {
    id: string;
    vehicleId: string;
    eventType?: string;
    severity?: string;
    speed?: number;
    locationLat?: number;
    locationLng?: number;
    occurredAt?: string;
    acknowledged?: boolean;
    acknowledgedBy?: string;
    acknowledgedAt?: string;
  };
}

/**
 * Payload shape for the `ACKNOWLEDGE` event.
 */
export interface StdTelematicsEventAcknowledgePayload {
  id: string;
}

/**
 * Payload shape for the `TelematicsEventLoaded` event.
 */
export interface StdTelematicsEventTelematicsEventLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `TelematicsEventLoadFailed` event.
 */
export interface StdTelematicsEventTelematicsEventLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTelematicsEventConfig {
  /** Default: `[{"label":"Vehicle","variant":"caption","name":"vehicleId"},{"variant":"badge","label":"Type","name":"eventType"},{"variant":"badge","name":"severity","label":"Severity"},{"name":"speed","variant":"caption","label":"Speed"},{"name":"occurredAt","label":"Occurred","variant":"caption"},{"label":"Ack","name":"acknowledged","variant":"badge"}]` */
  fields?: TraitConfig[];
  /** Default: `"Telematics Events"` */
  title?: string;
  /** Default: `[{"variant":"primary","icon":"arrow-right","event":"OPEN_EVENT","label":"Open"},{"event":"ACKNOWLEDGE","label":"Acknowledge","variant":"secondary"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-telematics-event descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTelematicsEventParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTelematicsEventEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTelematicsEventConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TelematicsEvent.traits.TelematicsEventStream`. */
export function stdTelematicsEventTrait(params: StdTelematicsEventParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TelematicsEventStream`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TelematicsEvent.pages.TelematicsEventPage`. */
export function stdTelematicsEventPage(params: StdTelematicsEventParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TelematicsEventPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTelematicsEvent(params: StdTelematicsEventParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TelematicsEventOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTelematicsEventTrait(params),
    ],
    pages: [
      stdTelematicsEventPage(params),
    ],
  });
}
