/**
 * std-trade-order — std-trade-order — trade-order lifecycle (pending → working → filled) with position limits, VaR ceilings, and pre-trade risk-check knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-trade-order.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-trade-order
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-trade-order';
const ALIAS = 'TradeOrder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTradeOrderEventKey = 'CANCEL' | 'CLOSE_VIEW' | 'FILL' | 'INIT' | 'OPEN_ORDER' | 'SET_STATUS' | 'TradeOrderLoadFailed' | 'TradeOrderLoaded' | 'TradeOrderUpdateFailed' | 'TradeOrderUpdated';

/**
 * Payload shape for the `OPEN_ORDER` event.
 */
export interface StdTradeOrderOpenOrderPayload {
  id: string;
  row?: {
    id: string;
    instrument: string;
    side?: string;
    orderType?: string;
    quantity?: number;
    price?: number;
    limitPrice?: number;
    stopPrice?: number;
    status?: string;
    venue?: string;
    filledAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `FILL` event.
 */
export interface StdTradeOrderFillPayload {
  id: string;
}

/**
 * Payload shape for the `CANCEL` event.
 */
export interface StdTradeOrderCancelPayload {
  id: string;
}

/**
 * Payload shape for the `TradeOrderLoaded` event.
 */
export interface StdTradeOrderTradeOrderLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TradeOrderLoadFailed` event.
 */
export interface StdTradeOrderTradeOrderLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `TradeOrderUpdated` event.
 */
export interface StdTradeOrderTradeOrderUpdatedPayload {
  row?: {
    id: string;
    instrument: string;
    side?: string;
    orderType?: string;
    quantity?: number;
    price?: number;
    limitPrice?: number;
    stopPrice?: number;
    status?: string;
    venue?: string;
    filledAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `TradeOrderUpdateFailed` event.
 */
export interface StdTradeOrderTradeOrderUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTradeOrderConfig {
  /** Default: `"Trade Orders"` */
  title?: string;
  /** Default: `"hard"` */
  clientSideRiskCheck?: 'soft' | 'hard' | 'off';
  /** Default: `30` */
  washTradePreventionMinutes?: number;
  /** Default: `"best-execution"` */
  executionVenuePolicy?: 'best-execution' | 'primary-only' | 'dark-pool-preferred' | 'lit-only';
  /** Default: `[{"label":"Open","event":"OPEN_ORDER","icon":"arrow-right","variant":"primary"},{"event":"CANCEL","label":"Cancel","icon":"x","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"market"` */
  orderType?: 'market' | 'limit' | 'stop' | 'stop-limit' | 'trailing';
  /** Default: `[{"variant":"caption","name":"instrument","label":"Instrument"},{"label":"Side","variant":"badge","name":"side"},{"label":"Type","name":"orderType","variant":"badge"},{"name":"quantity","variant":"caption","label":"Qty"},{"variant":"caption","name":"price","label":"Price"},{"variant":"badge","name":"status","label":"Status"},{"variant":"caption","name":"venue","label":"Venue"}]` */
  fields?: EntityRow[];
  /** Default: `1000000` */
  positionLimitUSD?: number;
  /** Default: `250000` */
  dailyVarLimitUSD?: number;
  /** Default: `500` */
  priceCollarsBps?: number;
  /** Default: `true` */
  partialFillsAllowed?: boolean;
}

/**
 * Params for the std-trade-order descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTradeOrderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTradeOrderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTradeOrderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TradeOrder.traits.TradeOrderManage`. */
export function stdTradeOrderTrait(params: StdTradeOrderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TradeOrderManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TradeOrder.pages.TradeOrderManagePage`. */
export function stdTradeOrderPage(params: StdTradeOrderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TradeOrderManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTradeOrder(params: StdTradeOrderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TradeOrderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTradeOrderTrait(params),
    ],
    pages: [
      stdTradeOrderPage(params),
    ],
  });
}
