/**
 * std-court-deadline — std-court-deadline — statutory filing deadline tied to a legal matter. Lists deadlines in `browsing`; opens detail in `viewing_single` with Complete / Extend actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-court-deadline.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-court-deadline
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-court-deadline';
const ALIAS = 'CourtDeadline';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCourtDeadlineEventKey = 'CLOSE_VIEW' | 'CourtDeadlineLoadFailed' | 'CourtDeadlineLoaded' | 'EXTEND_DEADLINE' | 'INIT' | 'MARK_COMPLETED' | 'OPEN_DEADLINE';

/**
 * Payload shape for the `OPEN_DEADLINE` event.
 */
export interface StdCourtDeadlineOpenDeadlinePayload {
  id: string;
  row?: {
    id: string;
    matterId: string;
    title: string;
    deadlineType?: string;
    dueAt: string;
    jurisdiction?: string;
    assignedTo?: string;
    status?: string;
    reminderSent?: boolean;
    notes?: string;
  };
}

/**
 * Payload shape for the `MARK_COMPLETED` event.
 */
export interface StdCourtDeadlineMarkCompletedPayload {
  id: string;
}

/**
 * Payload shape for the `EXTEND_DEADLINE` event.
 */
export interface StdCourtDeadlineExtendDeadlinePayload {
  id: string;
}

/**
 * Payload shape for the `CourtDeadlineLoaded` event.
 */
export interface StdCourtDeadlineCourtDeadlineLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `CourtDeadlineLoadFailed` event.
 */
export interface StdCourtDeadlineCourtDeadlineLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCourtDeadlineConfig {
  /** Default: `"Deadlines"` */
  title?: string;
  /** Default: `[{"name":"title","label":"Title","variant":"caption"},{"variant":"badge","label":"Type","name":"deadlineType"},{"name":"dueAt","label":"Due","variant":"caption"},{"variant":"caption","label":"Jurisdiction","name":"jurisdiction"},{"label":"Assigned","variant":"caption","name":"assignedTo"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"label":"Open","event":"OPEN_DEADLINE","icon":"arrow-right","variant":"primary"},{"event":"MARK_COMPLETED","variant":"danger","label":"Complete"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-court-deadline descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCourtDeadlineParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCourtDeadlineEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCourtDeadlineConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CourtDeadline.traits.CourtDeadlineDocket`. */
export function stdCourtDeadlineTrait(params: StdCourtDeadlineParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CourtDeadlineDocket`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CourtDeadline.pages.CourtDeadlinePage`. */
export function stdCourtDeadlinePage(params: StdCourtDeadlineParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CourtDeadlinePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCourtDeadline(params: StdCourtDeadlineParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CourtDeadlineOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCourtDeadlineTrait(params),
    ],
    pages: [
      stdCourtDeadlinePage(params),
    ],
  });
}
