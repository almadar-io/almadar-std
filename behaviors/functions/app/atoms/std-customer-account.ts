/**
 * std-customer-account — std-customer-account — retail customer profile with loyalty points, store credit and Grant Points / Issue Credit / Ban actions. Listens for SET_STATUS so std-lifecycle can drive automated transitions. Exposes CRM-pattern knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-customer-account.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-customer-account
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-customer-account';
const ALIAS = 'CustomerAccount';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCustomerAccountEventKey = 'BAN_CUSTOMER' | 'CLOSE_VIEW' | 'CustomerAccountLoadFailed' | 'CustomerAccountLoaded' | 'CustomerAccountUpdateFailed' | 'CustomerAccountUpdated' | 'GRANT_CREDIT' | 'GRANT_POINTS' | 'INIT' | 'OPEN_CUSTOMER' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_CUSTOMER` event.
 */
export interface StdCustomerAccountOpenCustomerPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    email?: string;
    phone?: string;
    loyaltyPoints?: number;
    storeCreditAmount?: number;
    totalSpent?: number;
    joinedAt?: string;
    lastVisitAt?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `GRANT_POINTS` event.
 */
export interface StdCustomerAccountGrantPointsPayload {
  id: string;
}

/**
 * Payload shape for the `GRANT_CREDIT` event.
 */
export interface StdCustomerAccountGrantCreditPayload {
  id: string;
}

/**
 * Payload shape for the `BAN_CUSTOMER` event.
 */
export interface StdCustomerAccountBanCustomerPayload {
  id: string;
}

/**
 * Payload shape for the `CustomerAccountLoaded` event.
 */
export interface StdCustomerAccountCustomerAccountLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `CustomerAccountLoadFailed` event.
 */
export interface StdCustomerAccountCustomerAccountLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `CustomerAccountUpdated` event.
 */
export interface StdCustomerAccountCustomerAccountUpdatedPayload {
  row?: {
    id: string;
    name: string;
    email?: string;
    phone?: string;
    loyaltyPoints?: number;
    storeCreditAmount?: number;
    totalSpent?: number;
    joinedAt?: string;
    lastVisitAt?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `CustomerAccountUpdateFailed` event.
 */
export interface StdCustomerAccountCustomerAccountUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCustomerAccountConfig {
  /** Default: `[{"variant":"primary","event":"OPEN_CUSTOMER","icon":"arrow-right","label":"Open"},{"variant":"danger","label":"Ban","event":"BAN_CUSTOMER"}]` */
  itemActions?: EntityRow[];
  /** Default: `[{"variant":"caption","label":"Name","name":"name"},{"label":"Email","variant":"caption","name":"email"},{"name":"phone","label":"Phone","variant":"caption"},{"name":"loyaltyPoints","label":"Points","variant":"caption"},{"name":"totalSpent","label":"Total Spent","variant":"caption"},{"name":"status","variant":"badge","label":"Status"}]` */
  fields?: EntityRow[];
  /** Default: `5` */
  contactFrequencyCapPerWeek?: number;
  /** Default: `"linear"` */
  lifecycleStagePolicy?: 'linear' | 'branching' | 'none';
  /** Default: `7` */
  defaultRetentionYears?: number;
  /** Default: `50` */
  healthScoreThresholdAlert?: number;
  /** Default: `"Customers"` */
  title?: string;
  /** Default: `[30,7,1]` */
  renewalReminderDaysBefore?: number[];
  /** Default: `true` */
  gdprConsentRequired?: boolean;
  /** Default: `true` */
  consentTrackingEnabled?: boolean;
  /** Default: `365` */
  archiveInactiveAfterDays?: number;
}

/**
 * Params for the std-customer-account descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCustomerAccountParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCustomerAccountEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCustomerAccountConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CustomerAccount.traits.CustomerAccountDirectory`. */
export function stdCustomerAccountTrait(params: StdCustomerAccountParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CustomerAccountDirectory`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CustomerAccount.pages.CustomerAccountPage`. */
export function stdCustomerAccountPage(params: StdCustomerAccountParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CustomerAccountPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCustomerAccount(params: StdCustomerAccountParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CustomerAccountOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCustomerAccountTrait(params),
    ],
    pages: [
      stdCustomerAccountPage(params),
    ],
  });
}
