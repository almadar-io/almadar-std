/**
 * std-campaign-goal — std-campaign-goal — nonprofit fundraising campaign with Launch / End actions. Listens for SET_STATUS so std-lifecycle can drive automated transitions. Exposes CRM-pattern knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-campaign-goal.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-campaign-goal
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-campaign-goal';
const ALIAS = 'CampaignGoal';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCampaignGoalEventKey = 'CLOSE_VIEW' | 'CampaignGoalLoadFailed' | 'CampaignGoalLoaded' | 'CampaignGoalUpdateFailed' | 'CampaignGoalUpdated' | 'END_CAMPAIGN' | 'INIT' | 'LAUNCH_CAMPAIGN' | 'OPEN_CAMPAIGN' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_CAMPAIGN` event.
 */
export interface StdCampaignGoalOpenCampaignPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    targetAmount?: number;
    raisedAmount?: number;
    donorCount?: number;
    startDate?: string;
    endDate?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `LAUNCH_CAMPAIGN` event.
 */
export interface StdCampaignGoalLaunchCampaignPayload {
  id: string;
}

/**
 * Payload shape for the `END_CAMPAIGN` event.
 */
export interface StdCampaignGoalEndCampaignPayload {
  id: string;
}

/**
 * Payload shape for the `CampaignGoalLoaded` event.
 */
export interface StdCampaignGoalCampaignGoalLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `CampaignGoalLoadFailed` event.
 */
export interface StdCampaignGoalCampaignGoalLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `CampaignGoalUpdated` event.
 */
export interface StdCampaignGoalCampaignGoalUpdatedPayload {
  row?: {
    id: string;
    name: string;
    description?: string;
    targetAmount?: number;
    raisedAmount?: number;
    donorCount?: number;
    startDate?: string;
    endDate?: string;
    status?: string;
  };
}

/**
 * Payload shape for the `CampaignGoalUpdateFailed` event.
 */
export interface StdCampaignGoalCampaignGoalUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCampaignGoalConfig {
  /** Default: `25` */
  healthScoreThresholdAlert?: number;
  /** Default: `[30,7,1]` */
  renewalReminderDaysBefore?: number[];
  /** Default: `true` */
  consentTrackingEnabled?: boolean;
  /** Default: `[{"event":"OPEN_CAMPAIGN","label":"Open","variant":"primary","icon":"arrow-right"},{"label":"End","variant":"danger","event":"END_CAMPAIGN"}]` */
  itemActions?: EntityRow[];
  /** Default: `"Campaigns"` */
  title?: string;
  /** Default: `180` */
  archiveInactiveAfterDays?: number;
  /** Default: `[{"name":"name","variant":"caption","label":"Name"},{"name":"targetAmount","label":"Target","variant":"caption"},{"name":"raisedAmount","label":"Raised","variant":"caption"},{"name":"donorCount","variant":"caption","label":"Donors"},{"variant":"badge","label":"Status","name":"status"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-campaign-goal descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCampaignGoalParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCampaignGoalEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCampaignGoalConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `CampaignGoal.traits.CampaignGoalTracker`. */
export function stdCampaignGoalTrait(params: StdCampaignGoalParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CampaignGoalTracker`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `CampaignGoal.pages.CampaignGoalPage`. */
export function stdCampaignGoalPage(params: StdCampaignGoalParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CampaignGoalPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCampaignGoal(params: StdCampaignGoalParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CampaignGoalOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCampaignGoalTrait(params),
    ],
    pages: [
      stdCampaignGoalPage(params),
    ],
  });
}
