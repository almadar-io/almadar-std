/**
 * std-insurance-claim — std-insurance-claim — insurance claim lifecycle (draft → eligible → submitted → accepted → denied → paid) with EDI submission knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-insurance-claim.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-insurance-claim
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-insurance-claim';
const ALIAS = 'InsuranceClaim';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdInsuranceClaimEventKey = 'APPEAL' | 'CLOSE_VIEW' | 'INIT' | 'InsuranceClaimLoadFailed' | 'InsuranceClaimLoaded' | 'InsuranceClaimUpdateFailed' | 'InsuranceClaimUpdated' | 'MARK_PAID' | 'OPEN_CLAIM' | 'SET_STATUS' | 'SUBMIT';

/**
 * Payload shape for the `OPEN_CLAIM` event.
 */
export interface StdInsuranceClaimOpenClaimPayload {
  id: string;
  row?: {
    id: string;
    patientId: string;
    payerId: string;
    serviceDate?: string;
    submittedAt?: string;
    status?: string;
    claimAmount?: number;
    paidAmount?: number;
    denialReason?: string;
    priorAuthNumber?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SUBMIT` event.
 */
export interface StdInsuranceClaimSubmitPayload {
  id: string;
  row?: {
    id: string;
    patientId: string;
    payerId: string;
    serviceDate?: string;
    submittedAt?: string;
    status?: string;
    claimAmount?: number;
    paidAmount?: number;
    denialReason?: string;
    priorAuthNumber?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `MARK_PAID` event.
 */
export interface StdInsuranceClaimMarkPaidPayload {
  id: string;
  row?: {
    id: string;
    patientId: string;
    payerId: string;
    serviceDate?: string;
    submittedAt?: string;
    status?: string;
    claimAmount?: number;
    paidAmount?: number;
    denialReason?: string;
    priorAuthNumber?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `APPEAL` event.
 */
export interface StdInsuranceClaimAppealPayload {
  id: string;
  row?: {
    id: string;
    patientId: string;
    payerId: string;
    serviceDate?: string;
    submittedAt?: string;
    status?: string;
    claimAmount?: number;
    paidAmount?: number;
    denialReason?: string;
    priorAuthNumber?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `InsuranceClaimLoaded` event.
 */
export interface StdInsuranceClaimInsuranceClaimLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `InsuranceClaimLoadFailed` event.
 */
export interface StdInsuranceClaimInsuranceClaimLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `InsuranceClaimUpdated` event.
 */
export interface StdInsuranceClaimInsuranceClaimUpdatedPayload {
  row?: {
    id: string;
    patientId: string;
    payerId: string;
    serviceDate?: string;
    submittedAt?: string;
    status?: string;
    claimAmount?: number;
    paidAmount?: number;
    denialReason?: string;
    priorAuthNumber?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `InsuranceClaimUpdateFailed` event.
 */
export interface StdInsuranceClaimInsuranceClaimUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdInsuranceClaimConfig {
  /** Default: `"EDI-837P"` */
  claimSubmissionFormat?: 'CMS-1500' | 'UB-04' | 'EDI-837P' | 'EDI-837I';
  /** Default: `30` */
  denialAppealWindowDays?: number;
  /** Default: `"Insurance Claims"` */
  title?: string;
  /** Default: `[{"variant":"caption","label":"Patient","name":"patientId"},{"name":"payerId","variant":"caption","label":"Payer"},{"variant":"badge","name":"status","label":"Status"},{"name":"claimAmount","variant":"caption","label":"Amount"},{"variant":"caption","label":"Service","name":"serviceDate"}]` */
  fields?: EntityRow[];
  /** Default: `false` */
  autoResubmitOnDenial?: boolean;
  /** Default: `[{"event":"OPEN_CLAIM","label":"Open","icon":"arrow-right","variant":"primary"},{"variant":"primary","icon":"send","label":"Submit","event":"SUBMIT"}]` */
  itemActions?: EntityRow[];
  /** Default: `""` */
  payerId?: string;
  /** Default: `24` */
  eligibilityCheckCadenceHours?: number;
  /** Default: `5` */
  priorAuthLeadTimeDays?: number;
}

/**
 * Params for the std-insurance-claim descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdInsuranceClaimParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdInsuranceClaimEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdInsuranceClaimConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `InsuranceClaim.traits.InsuranceClaimManage`. */
export function stdInsuranceClaimTrait(params: StdInsuranceClaimParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.InsuranceClaimManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `InsuranceClaim.pages.InsuranceClaimManagePage`. */
export function stdInsuranceClaimPage(params: StdInsuranceClaimParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.InsuranceClaimManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdInsuranceClaim(params: StdInsuranceClaimParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'InsuranceClaimOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdInsuranceClaimTrait(params),
    ],
    pages: [
      stdInsuranceClaimPage(params),
    ],
  });
}
