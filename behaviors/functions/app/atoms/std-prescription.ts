/**
 * std-prescription — std-prescription — prescription lifecycle (draft → signed → sent → dispensed) with controlled-substance, refill, and pharmacist-consult knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-prescription.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-prescription
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-prescription';
const ALIAS = 'Prescription';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdPrescriptionEventKey = 'CLOSE_VIEW' | 'DISPENSE' | 'INIT' | 'OPEN_RX' | 'PrescriptionLoadFailed' | 'PrescriptionLoaded' | 'PrescriptionUpdateFailed' | 'PrescriptionUpdated' | 'SEND_RX' | 'SET_STATUS' | 'SIGN_RX' | 'VOID_RX';

/**
 * Payload shape for the `OPEN_RX` event.
 */
export interface StdPrescriptionOpenRxPayload {
  id: string;
  row?: {
    id: string;
    patientId: string;
    medication: string;
    dosage?: string;
    quantity?: number;
    refillsRemaining?: number;
    controlled?: boolean;
    status?: string;
    prescribedBy?: string;
    pharmacy?: string;
    signedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SIGN_RX` event.
 */
export interface StdPrescriptionSignRxPayload {
  id: string;
}

/**
 * Payload shape for the `SEND_RX` event.
 */
export interface StdPrescriptionSendRxPayload {
  id: string;
}

/**
 * Payload shape for the `DISPENSE` event.
 */
export interface StdPrescriptionDispensePayload {
  id: string;
}

/**
 * Payload shape for the `VOID_RX` event.
 */
export interface StdPrescriptionVoidRxPayload {
  id: string;
}

/**
 * Payload shape for the `PrescriptionLoaded` event.
 */
export interface StdPrescriptionPrescriptionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `PrescriptionLoadFailed` event.
 */
export interface StdPrescriptionPrescriptionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `PrescriptionUpdated` event.
 */
export interface StdPrescriptionPrescriptionUpdatedPayload {
  row?: {
    id: string;
    patientId: string;
    medication: string;
    dosage?: string;
    quantity?: number;
    refillsRemaining?: number;
    controlled?: boolean;
    status?: string;
    prescribedBy?: string;
    pharmacy?: string;
    signedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `PrescriptionUpdateFailed` event.
 */
export interface StdPrescriptionPrescriptionUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdPrescriptionConfig {
  /** Default: `false` */
  epaRequired?: boolean;
  /** Default: `"none"` */
  controlledSubstanceSchedule?: 'none' | 'II' | 'III' | 'IV' | 'V';
  /** Default: `"patient-consent"` */
  genericSubstitutionAllowed?: 'always' | 'patient-consent' | 'prescriber-only' | 'never';
  /** Default: `[]` */
  priorAuthRequiredFor?: string[];
  /** Default: `5` */
  refillsAllowedPerYear?: number;
  /** Default: `"controlled-only"` */
  pharmacistConsultationRequired?: 'always' | 'controlled-only' | 'high-risk' | 'never';
  /** Default: `[{"icon":"arrow-right","event":"OPEN_RX","variant":"primary","label":"Open"},{"event":"VOID_RX","label":"Void","icon":"x","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"Prescriptions"` */
  title?: string;
  /** Default: `90` */
  dispenseQuantityLimit?: number;
  /** Default: `false` */
  dawCodeRequired?: boolean;
  /** Default: `[{"name":"medication","label":"Medication","variant":"caption"},{"label":"Patient","variant":"caption","name":"patientId"},{"label":"Dosage","variant":"caption","name":"dosage"},{"name":"status","label":"Status","variant":"badge"},{"label":"Pharmacy","name":"pharmacy","variant":"caption"},{"variant":"caption","name":"prescribedBy","label":"Prescriber"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-prescription descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdPrescriptionParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdPrescriptionEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdPrescriptionConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Prescription.traits.PrescriptionManage`. */
export function stdPrescriptionTrait(params: StdPrescriptionParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PrescriptionManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Prescription.pages.PrescriptionManagePage`. */
export function stdPrescriptionPage(params: StdPrescriptionParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.PrescriptionManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdPrescription(params: StdPrescriptionParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'PrescriptionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdPrescriptionTrait(params),
    ],
    pages: [
      stdPrescriptionPage(params),
    ],
  });
}
