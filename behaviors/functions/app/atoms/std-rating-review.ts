/**
 * std-rating-review — std-rating-review — capture a 1..N star rating with optional comment + photos. Owns a RatingReview entity; SUBMIT persists @payload.data (rating, comment, photos, targetId) and emits RatingReviewSubmitted so cross-trait listeners can react (e.g. recompute aggregate).
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-rating-review.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-rating-review
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-rating-review';
const ALIAS = 'RatingReview';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdRatingReviewEventKey = 'EDIT_COMMENT' | 'INIT' | 'RATE' | 'RatingReviewLoadFailed' | 'RatingReviewLoaded' | 'RatingReviewSubmitFailed' | 'RatingReviewSubmitted' | 'SUBMIT';

/**
 * Payload shape for the `SUBMIT` event.
 */
export interface StdRatingReviewSubmitPayload {
  data: EntityRow;
}

/**
 * Payload shape for the `RATE` event.
 */
export interface StdRatingReviewRatePayload {
  rating: number;
}

/**
 * Payload shape for the `EDIT_COMMENT` event.
 */
export interface StdRatingReviewEditCommentPayload {
  value: string;
}

/**
 * Payload shape for the `RatingReviewLoaded` event.
 */
export interface StdRatingReviewRatingReviewLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `RatingReviewLoadFailed` event.
 */
export interface StdRatingReviewRatingReviewLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `RatingReviewSubmitted` event.
 */
export interface StdRatingReviewRatingReviewSubmittedPayload {
  row?: {
    id: string;
    rating: number;
    comment?: string;
    photos?: string[];
    authorId?: string;
    targetId: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `RatingReviewSubmitFailed` event.
 */
export interface StdRatingReviewRatingReviewSubmitFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRatingReviewConfig {
  /** Default: `"SUBMIT_REVIEW"` */
  submitEvent?: string;
  /** Default: `5` */
  maxStars?: number;
  /** Default: `false` */
  allowPhotos?: boolean;
  /** Default: `true` */
  allowComment?: boolean;
  /** Default: `"Rate this"` */
  title?: string;
}

/**
 * Params for the std-rating-review descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdRatingReviewParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdRatingReviewEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdRatingReviewConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `RatingReview.traits.RatingReviewSubmit`. */
export function stdRatingReviewTrait(params: StdRatingReviewParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.RatingReviewSubmit`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `RatingReview.pages.RatingReviewSubmitPage`. */
export function stdRatingReviewPage(params: StdRatingReviewParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.RatingReviewSubmitPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdRatingReview(params: StdRatingReviewParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'RatingReviewOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdRatingReviewTrait(params),
    ],
    pages: [
      stdRatingReviewPage(params),
    ],
  });
}
