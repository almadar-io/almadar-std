/**
 * std-runbook — std-runbook — operational runbook lifecycle (draft → active → deprecated) with auto-execute policy + approval gates + rollback.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-runbook.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-runbook
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-runbook';
const ALIAS = 'Runbook';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdRunbookEventKey = 'ACTIVATE_RUNBOOK' | 'CLOSE_VIEW' | 'DEPRECATE_RUNBOOK' | 'EXECUTE_RUNBOOK' | 'INIT' | 'OPEN_RUNBOOK' | 'RunbookLoadFailed' | 'RunbookLoaded' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_RUNBOOK` event.
 */
export interface StdRunbookOpenRunbookPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    triggerEvent?: string;
    status?: string;
    stepsJson?: string;
    lastExecutedAt?: string;
    executionCount?: number;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `EXECUTE_RUNBOOK` event.
 */
export interface StdRunbookExecuteRunbookPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    triggerEvent?: string;
    status?: string;
    stepsJson?: string;
    lastExecutedAt?: string;
    executionCount?: number;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ACTIVATE_RUNBOOK` event.
 */
export interface StdRunbookActivateRunbookPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    triggerEvent?: string;
    status?: string;
    stepsJson?: string;
    lastExecutedAt?: string;
    executionCount?: number;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `DEPRECATE_RUNBOOK` event.
 */
export interface StdRunbookDeprecateRunbookPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    triggerEvent?: string;
    status?: string;
    stepsJson?: string;
    lastExecutedAt?: string;
    executionCount?: number;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `RunbookLoaded` event.
 */
export interface StdRunbookRunbookLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `RunbookLoadFailed` event.
 */
export interface StdRunbookRunbookLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRunbookConfig {
  /** Default: `[{"variant":"caption","name":"title","label":"Title"},{"label":"Trigger","name":"triggerEvent","variant":"caption"},{"variant":"badge","name":"status","label":"Status"},{"variant":"caption","label":"Runs","name":"executionCount"},{"label":"Last run","name":"lastExecutedAt","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `[{"label":"Open","variant":"primary","event":"OPEN_RUNBOOK","icon":"arrow-right"},{"variant":"secondary","event":"EXECUTE_RUNBOOK","label":"Execute","icon":"play"}]` */
  itemActions?: EntityRow[];
  /** Default: `[]` */
  triggersOn?: string[];
  /** Default: `"Runbooks"` */
  title?: string;
  /** Default: `"never"` */
  autoExecutePolicy?: 'never' | 'dry-run-first' | 'approve-then-execute' | 'always';
  /** Default: `"medium"` */
  approvalRequiredAbove?: 'low' | 'medium' | 'high' | 'critical';
  /** Default: `true` */
  rollbackOnFailure?: boolean;
}

/**
 * Params for the std-runbook descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdRunbookParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdRunbookEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdRunbookConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Runbook.traits.RunbookManage`. */
export function stdRunbookTrait(params: StdRunbookParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.RunbookManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Runbook.pages.RunbookPage`. */
export function stdRunbookPage(params: StdRunbookParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.RunbookPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdRunbook(params: StdRunbookParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'RunbookOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdRunbookTrait(params),
    ],
    pages: [
      stdRunbookPage(params),
    ],
  });
}
