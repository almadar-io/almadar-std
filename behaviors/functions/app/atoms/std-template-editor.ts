/**
 * std-template-editor — std-template-editor — message template authoring primitive. Lists templates in `browsing`; opens detail in `viewing_single` with Edit / Save / Delete actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-template-editor.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-template-editor
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-template-editor';
const ALIAS = 'TemplateEditor';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTemplateEditorEventKey = 'CLOSE_VIEW' | 'DELETE_TEMPLATE' | 'EDIT_TEMPLATE' | 'INIT' | 'MessageTemplateLoadFailed' | 'MessageTemplateLoaded' | 'OPEN_TEMPLATE' | 'SAVE_TEMPLATE';

/**
 * Payload shape for the `OPEN_TEMPLATE` event.
 */
export interface StdTemplateEditorOpenTemplatePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    channel?: string;
    subject?: string;
    body?: string;
    variables?: string;
    lastEditedAt?: string;
  };
}

/**
 * Payload shape for the `EDIT_TEMPLATE` event.
 */
export interface StdTemplateEditorEditTemplatePayload {
  id: string;
}

/**
 * Payload shape for the `SAVE_TEMPLATE` event.
 */
export interface StdTemplateEditorSaveTemplatePayload {
  id: string;
}

/**
 * Payload shape for the `DELETE_TEMPLATE` event.
 */
export interface StdTemplateEditorDeleteTemplatePayload {
  id: string;
}

/**
 * Payload shape for the `MessageTemplateLoaded` event.
 */
export interface StdTemplateEditorMessageTemplateLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `MessageTemplateLoadFailed` event.
 */
export interface StdTemplateEditorMessageTemplateLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTemplateEditorConfig {
  /** Default: `[{"name":"name","variant":"caption","label":"Name"},{"name":"channel","label":"Channel","variant":"badge"},{"variant":"caption","name":"subject","label":"Subject"},{"name":"lastEditedAt","label":"Updated","variant":"caption"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"icon":"arrow-right","label":"Open","event":"OPEN_TEMPLATE","variant":"primary"},{"variant":"danger","label":"Delete","event":"DELETE_TEMPLATE"}]` */
  itemActions?: TraitConfig[];
  /** Default: `"Templates"` */
  title?: string;
}

/**
 * Params for the std-template-editor descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTemplateEditorParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTemplateEditorEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTemplateEditorConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `TemplateEditor.traits.MessageTemplateEditor`. */
export function stdTemplateEditorTrait(params: StdTemplateEditorParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.MessageTemplateEditor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `TemplateEditor.pages.MessageTemplatePage`. */
export function stdTemplateEditorPage(params: StdTemplateEditorParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MessageTemplatePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTemplateEditor(params: StdTemplateEditorParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'MessageTemplateOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTemplateEditorTrait(params),
    ],
    pages: [
      stdTemplateEditorPage(params),
    ],
  });
}
