/**
 * std-device-fleet — std-device-fleet — IoT device fleet provisioning + OTA firmware + certificate rotation. Listens for SET_STATUS so std-lifecycle can drive transitions.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-device-fleet.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-device-fleet
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-device-fleet';
const ALIAS = 'DeviceFleet';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdDeviceFleetEventKey = 'CLOSE_VIEW' | 'DeviceLoadFailed' | 'DeviceLoaded' | 'DeviceUpdateFailed' | 'DeviceUpdated' | 'INIT' | 'OPEN_DEVICE' | 'PUSH_FIRMWARE' | 'REPROVISION' | 'RETIRE' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_DEVICE` event.
 */
export interface StdDeviceFleetOpenDevicePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    deviceType: string;
    firmwareVersion?: string;
    lastSeenAt?: string;
    certExpiresAt?: string;
    status?: string;
    provisionedBy?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `REPROVISION` event.
 */
export interface StdDeviceFleetReprovisionPayload {
  id: string;
}

/**
 * Payload shape for the `PUSH_FIRMWARE` event.
 */
export interface StdDeviceFleetPushFirmwarePayload {
  id: string;
}

/**
 * Payload shape for the `RETIRE` event.
 */
export interface StdDeviceFleetRetirePayload {
  id: string;
}

/**
 * Payload shape for the `DeviceLoaded` event.
 */
export interface StdDeviceFleetDeviceLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `DeviceLoadFailed` event.
 */
export interface StdDeviceFleetDeviceLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `DeviceUpdated` event.
 */
export interface StdDeviceFleetDeviceUpdatedPayload {
  row?: {
    id: string;
    name: string;
    deviceType: string;
    firmwareVersion?: string;
    lastSeenAt?: string;
    certExpiresAt?: string;
    status?: string;
    provisionedBy?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `DeviceUpdateFailed` event.
 */
export interface StdDeviceFleetDeviceUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdDeviceFleetConfig {
  /** Default: `[{"label":"Name","variant":"caption","name":"name"},{"label":"Type","variant":"caption","name":"deviceType"},{"variant":"caption","name":"firmwareVersion","label":"Firmware"},{"variant":"badge","name":"status","label":"Status"},{"name":"lastSeenAt","variant":"caption","label":"Last seen"},{"label":"Cert expires","name":"certExpiresAt","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `"Devices"` */
  title?: string;
  /** Default: `"zero-touch"` */
  provisioningModel?: 'zero-touch' | 'claim-then-bind' | 'manual';
  /** Default: `[{"event":"OPEN_DEVICE","variant":"primary","icon":"arrow-right","label":"Open"},{"event":"RETIRE","icon":"x","label":"Retire","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"queue"` */
  offlineActionPolicy?: 'queue' | 'drop' | 'fail-fast';
  /** Default: `30` */
  otaUpdateCadenceDays?: number;
  /** Default: `90` */
  dataRetentionDays?: number;
  /** Default: `90` */
  certificateRotationDays?: number;
}

/**
 * Params for the std-device-fleet descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdDeviceFleetParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdDeviceFleetEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdDeviceFleetConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `DeviceFleet.traits.DeviceFleetManage`. */
export function stdDeviceFleetTrait(params: StdDeviceFleetParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DeviceFleetManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `DeviceFleet.pages.DeviceFleetManagePage`. */
export function stdDeviceFleetPage(params: StdDeviceFleetParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DeviceFleetManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdDeviceFleet(params: StdDeviceFleetParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'DeviceFleetOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdDeviceFleetTrait(params),
    ],
    pages: [
      stdDeviceFleetPage(params),
    ],
  });
}
