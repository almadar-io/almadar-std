/**
 * std-checkin — std-checkin — arrival check-in atom listing recorded check-ins (qr or geo). Lists check-ins in `browsing`; opens detail in `viewing_single` with Verify / Revoke actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-checkin.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-checkin
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-checkin';
const ALIAS = 'Checkin';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdCheckinEventKey = 'CLOSE_VIEW' | 'CheckinLoadFailed' | 'CheckinLoaded' | 'INIT' | 'OPEN_CHECKIN' | 'REVOKE_CHECKIN' | 'VERIFY_CHECKIN';

/**
 * Payload shape for the `OPEN_CHECKIN` event.
 */
export interface StdCheckinOpenCheckinPayload {
  id: string;
  row?: {
    id: string;
    mode?: string;
    targetId: string;
    targetType: string;
    userId?: string;
    lat?: number;
    lng?: number;
    qrCode?: string;
    verified?: boolean;
    checkedInAt?: string;
  };
}

/**
 * Payload shape for the `VERIFY_CHECKIN` event.
 */
export interface StdCheckinVerifyCheckinPayload {
  id: string;
}

/**
 * Payload shape for the `REVOKE_CHECKIN` event.
 */
export interface StdCheckinRevokeCheckinPayload {
  id: string;
}

/**
 * Payload shape for the `CheckinLoaded` event.
 */
export interface StdCheckinCheckinLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `CheckinLoadFailed` event.
 */
export interface StdCheckinCheckinLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdCheckinConfig {
  /** Default: `"Check-ins"` */
  title?: string;
  /** Default: `[{"name":"targetType","label":"Type","variant":"badge"},{"label":"Target","name":"targetId","variant":"caption"},{"name":"userId","label":"User","variant":"caption"},{"name":"mode","label":"Mode","variant":"badge"},{"label":"When","variant":"caption","name":"checkedInAt"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"event":"OPEN_CHECKIN","label":"Open","icon":"arrow-right","variant":"primary"},{"variant":"danger","label":"Revoke","event":"REVOKE_CHECKIN"}]` */
  itemActions?: TraitConfig[];
  /** Default: `"geo"` */
  mode?: string;
}

/**
 * Params for the std-checkin descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdCheckinParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdCheckinEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdCheckinConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Checkin.traits.CheckinScan`. */
export function stdCheckinTrait(params: StdCheckinParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.CheckinScan`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Checkin.pages.CheckinScanPage`. */
export function stdCheckinPage(params: StdCheckinParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.CheckinScanPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdCheckin(params: StdCheckinParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'CheckinOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdCheckinTrait(params),
    ],
    pages: [
      stdCheckinPage(params),
    ],
  });
}
