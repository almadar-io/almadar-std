/**
 * std-appointment-policy — std-appointment-policy — shared reschedule/cancellation/no-show policy referenced by std-interview-slot, std-class-session, std-event, std-court-deadline via a policyId knob. Author one policy + reuse across appointment types. Exposes Scheduling-pattern knobs.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-appointment-policy.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-appointment-policy
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-appointment-policy';
const ALIAS = 'AppointmentPolicy';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdAppointmentPolicyEventKey = 'ARCHIVE_POLICY' | 'AppointmentPolicyLoadFailed' | 'AppointmentPolicyLoaded' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_POLICY' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_POLICY` event.
 */
export interface StdAppointmentPolicyOpenPolicyPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    rescheduleWindowHours?: number;
    maxReschedules?: number;
    cancellationPolicy?: string;
    noShowGracePeriodMinutes?: number;
    noShowFeeAmount?: number;
    lateArrivalGraceMinutes?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARCHIVE_POLICY` event.
 */
export interface StdAppointmentPolicyArchivePolicyPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    description?: string;
    rescheduleWindowHours?: number;
    maxReschedules?: number;
    cancellationPolicy?: string;
    noShowGracePeriodMinutes?: number;
    noShowFeeAmount?: number;
    lateArrivalGraceMinutes?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `AppointmentPolicyLoaded` event.
 */
export interface StdAppointmentPolicyAppointmentPolicyLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `AppointmentPolicyLoadFailed` event.
 */
export interface StdAppointmentPolicyAppointmentPolicyLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdAppointmentPolicyConfig {
  /** Default: `15` */
  noShowGracePeriodMinutes?: number;
  /** Default: `2` */
  maxReschedules?: number;
  /** Default: `0` */
  noShowFeeAmount?: number;
  /** Default: `10` */
  lateArrivalGraceMinutes?: number;
  /** Default: `[{"variant":"primary","icon":"arrow-right","label":"Open","event":"OPEN_POLICY"},{"event":"ARCHIVE_POLICY","label":"Archive","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `[{"label":"Name","variant":"caption","name":"name"},{"name":"rescheduleWindowHours","label":"Reschedule (h)","variant":"caption"},{"label":"Max reschedules","variant":"caption","name":"maxReschedules"},{"variant":"badge","name":"cancellationPolicy","label":"Cancellation"},{"name":"noShowGracePeriodMinutes","label":"No-show grace","variant":"caption"},{"variant":"caption","label":"No-show fee","name":"noShowFeeAmount"},{"name":"status","label":"Status","variant":"badge"}]` */
  fields?: EntityRow[];
  /** Default: `24` */
  rescheduleWindowHours?: number;
  /** Default: `"Appointment Policies"` */
  title?: string;
  /** Default: `"free"` */
  cancellationPolicy?: 'free' | 'partial-refund' | 'no-refund';
}

/**
 * Params for the std-appointment-policy descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdAppointmentPolicyParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdAppointmentPolicyEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdAppointmentPolicyConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `AppointmentPolicy.traits.AppointmentPolicyManage`. */
export function stdAppointmentPolicyTrait(params: StdAppointmentPolicyParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.AppointmentPolicyManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `AppointmentPolicy.pages.AppointmentPolicyPage`. */
export function stdAppointmentPolicyPage(params: StdAppointmentPolicyParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.AppointmentPolicyPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdAppointmentPolicy(params: StdAppointmentPolicyParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'AppointmentPolicyOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdAppointmentPolicyTrait(params),
    ],
    pages: [
      stdAppointmentPolicyPage(params),
    ],
  });
}
