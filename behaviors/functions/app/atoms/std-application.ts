/**
 * std-application — std-application — job-style application bundle: form + attachments + status flow (draft/submitted/under-review/accepted/rejected). Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-application.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-application
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-application';
const ALIAS = 'Application';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdApplicationEventKey = 'ACCEPT' | 'ApplicationAccepted' | 'ApplicationCreated' | 'ApplicationLoadFailed' | 'ApplicationLoaded' | 'ApplicationRejected' | 'ApplicationSubmitted' | 'ApplicationUpdated' | 'CANCEL_EDIT' | 'CREATE' | 'CREATE_SUBMIT' | 'EDIT' | 'INIT' | 'REJECT' | 'SUBMIT' | 'UPDATE_SUBMIT';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdApplicationListenKey = 'ITEM_CREATED' | 'ITEM_UPDATED';

/**
 * Payload shape for the `EDIT` event.
 */
export interface StdApplicationEditPayload {
  id: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `SUBMIT` event.
 */
export interface StdApplicationSubmitPayload {
  applicationId: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `ACCEPT` event.
 */
export interface StdApplicationAcceptPayload {
  applicationId: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `REJECT` event.
 */
export interface StdApplicationRejectPayload {
  applicationId: string;
  row?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
  notes?: string;
}

/**
 * Payload shape for the `CREATE_SUBMIT` event.
 */
export interface StdApplicationCreateSubmitPayload {
  data?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `UPDATE_SUBMIT` event.
 */
export interface StdApplicationUpdateSubmitPayload {
  data?: {
    id: string;
    applicantName: string;
    applicantEmail: string;
    targetId: string;
    targetType: string;
    coverLetter?: string;
    attachmentsJson?: string;
    status?: string;
    submittedAt?: string;
    reviewedAt?: string;
    notes?: string;
  };
}

/**
 * Payload shape for the `ApplicationLoaded` event.
 */
export interface StdApplicationApplicationLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ApplicationLoadFailed` event.
 */
export interface StdApplicationApplicationLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `ApplicationCreated` event.
 */
export interface StdApplicationApplicationCreatedPayload {
  id?: string;
}

/**
 * Payload shape for the `ApplicationUpdated` event.
 */
export interface StdApplicationApplicationUpdatedPayload {
  id?: string;
}

/**
 * Payload shape for the `ApplicationSubmitted` event.
 */
export interface StdApplicationApplicationSubmittedPayload {
  id?: string;
}

/**
 * Payload shape for the `ApplicationAccepted` event.
 */
export interface StdApplicationApplicationAcceptedPayload {
  id?: string;
}

/**
 * Payload shape for the `ApplicationRejected` event.
 */
export interface StdApplicationApplicationRejectedPayload {
  id?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdApplicationConfig {
  /** Default: `"Applications"` */
  title?: string;
  /** Default: `[{"icon":"user","name":"applicantName","label":"Applicant","variant":"caption"},{"variant":"caption","name":"applicantEmail","label":"Email"},{"name":"targetType","label":"Target","variant":"caption"},{"name":"status","variant":"badge","label":"Status"},{"name":"submittedAt","label":"Submitted","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `[{"variant":"ghost","event":"EDIT","label":"Edit"},{"label":"Submit","event":"SUBMIT","variant":"primary"},{"label":"Accept","event":"ACCEPT","variant":"primary"},{"event":"REJECT","variant":"danger","label":"Reject"}]` */
  itemActions?: EntityRow[];
}

/**
 * Params for the std-application descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdApplicationParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdApplicationEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdApplicationConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Application.traits.ApplicationManage`. */
export function stdApplicationApplicationManageTrait(params: StdApplicationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `Application.traits.ApplicationPersistor`. */
export function stdApplicationApplicationPersistorTrait(params: StdApplicationParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicationPersistor`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Application.pages.ApplicationPage`. */
export function stdApplicationPage(params: StdApplicationParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ApplicationPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdApplication(params: StdApplicationParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ApplicationOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdApplicationApplicationManageTrait(params),
      stdApplicationApplicationPersistorTrait(params),
    ],
    pages: [
      stdApplicationPage(params),
    ],
  });
}
