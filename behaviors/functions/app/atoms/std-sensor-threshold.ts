/**
 * std-sensor-threshold — std-sensor-threshold — sensor alert bands with dwell/hysteresis; sibling to std-telematics-event for IoT monitoring.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-sensor-threshold.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-sensor-threshold
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-sensor-threshold';
const ALIAS = 'SensorThreshold';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdSensorThresholdEventKey = 'ARCHIVE_THRESHOLD' | 'CLOSE_VIEW' | 'INIT' | 'MUTE_THRESHOLD' | 'OPEN_THRESHOLD' | 'SensorThresholdLoadFailed' | 'SensorThresholdLoaded';

/**
 * Payload shape for the `OPEN_THRESHOLD` event.
 */
export interface StdSensorThresholdOpenThresholdPayload {
  id: string;
  row?: {
    id: string;
    sensorId: string;
    metric?: string;
    warningLow?: number;
    warningHigh?: number;
    criticalLow?: number;
    criticalHigh?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `MUTE_THRESHOLD` event.
 */
export interface StdSensorThresholdMuteThresholdPayload {
  id: string;
  row?: {
    id: string;
    sensorId: string;
    metric?: string;
    warningLow?: number;
    warningHigh?: number;
    criticalLow?: number;
    criticalHigh?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ARCHIVE_THRESHOLD` event.
 */
export interface StdSensorThresholdArchiveThresholdPayload {
  id: string;
  row?: {
    id: string;
    sensorId: string;
    metric?: string;
    warningLow?: number;
    warningHigh?: number;
    criticalLow?: number;
    criticalHigh?: number;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SensorThresholdLoaded` event.
 */
export interface StdSensorThresholdSensorThresholdLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `SensorThresholdLoadFailed` event.
 */
export interface StdSensorThresholdSensorThresholdLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdSensorThresholdConfig {
  /** Default: `5` */
  hysteresisPct?: number;
  /** Default: `30` */
  dwellTimeSeconds?: number;
  /** Default: `["oncall-l1","oncall-l2"]` */
  escalationRoles?: string[];
  /** Default: `[{"variant":"caption","name":"sensorId","label":"Sensor"},{"name":"metric","label":"Metric","variant":"caption"},{"variant":"caption","name":"warningHigh","label":"Warn"},{"variant":"caption","name":"criticalHigh","label":"Crit"},{"label":"Status","variant":"badge","name":"status"}]` */
  fields?: EntityRow[];
  /** Default: `[{"event":"OPEN_THRESHOLD","icon":"arrow-right","label":"Open","variant":"primary"},{"event":"MUTE_THRESHOLD","variant":"secondary","label":"Mute"}]` */
  itemActions?: EntityRow[];
  /** Default: `[{"low":0,"high":70,"band":"warning"},{"high":100,"low":70,"band":"critical"}]` */
  thresholdBands?: EntityRow[];
  /** Default: `"Sensor Thresholds"` */
  title?: string;
}

/**
 * Params for the std-sensor-threshold descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdSensorThresholdParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdSensorThresholdEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdSensorThresholdConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `SensorThreshold.traits.SensorThresholdManage`. */
export function stdSensorThresholdTrait(params: StdSensorThresholdParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SensorThresholdManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `SensorThreshold.pages.SensorThresholdPage`. */
export function stdSensorThresholdPage(params: StdSensorThresholdParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SensorThresholdPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdSensorThreshold(params: StdSensorThresholdParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'SensorThresholdOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdSensorThresholdTrait(params),
    ],
    pages: [
      stdSensorThresholdPage(params),
    ],
  });
}
