/**
 * std-helpdesk-ticket — std-helpdesk-ticket — support ticket lifecycle (new → open → pending → resolved → closed). Listens for SET_STATUS so siblings can drive automated transitions. Exposes Helpdesk-pattern knobs (slaTier, severity, response/resolution SLA, escalationMatrix, routingStrategy, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-helpdesk-ticket.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-helpdesk-ticket
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-helpdesk-ticket';
const ALIAS = 'HelpdeskTicket';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdHelpdeskTicketEventKey = 'ASSIGN' | 'CLOSE' | 'CLOSE_VIEW' | 'ESCALATE' | 'INIT' | 'OPEN_TICKET' | 'REPLY' | 'SET_STATUS' | 'TicketLoadFailed' | 'TicketLoaded';

/**
 * Payload shape for the `OPEN_TICKET` event.
 */
export interface StdHelpdeskTicketOpenTicketPayload {
  id: string;
  row?: {
    id: string;
    subject: string;
    description: string;
    requester: string;
    assignedTo?: string;
    priority?: string;
    status?: string;
    category?: string;
    firstReplyAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `REPLY` event.
 */
export interface StdHelpdeskTicketReplyPayload {
  id: string;
  row?: {
    id: string;
    subject: string;
    description: string;
    requester: string;
    assignedTo?: string;
    priority?: string;
    status?: string;
    category?: string;
    firstReplyAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ASSIGN` event.
 */
export interface StdHelpdeskTicketAssignPayload {
  id: string;
  assignedTo: string;
  row?: {
    id: string;
    subject: string;
    description: string;
    requester: string;
    assignedTo?: string;
    priority?: string;
    status?: string;
    category?: string;
    firstReplyAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `ESCALATE` event.
 */
export interface StdHelpdeskTicketEscalatePayload {
  id: string;
  row?: {
    id: string;
    subject: string;
    description: string;
    requester: string;
    assignedTo?: string;
    priority?: string;
    status?: string;
    category?: string;
    firstReplyAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `CLOSE` event.
 */
export interface StdHelpdeskTicketClosePayload {
  id: string;
  row?: {
    id: string;
    subject: string;
    description: string;
    requester: string;
    assignedTo?: string;
    priority?: string;
    status?: string;
    category?: string;
    firstReplyAt?: string;
    resolvedAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `TicketLoaded` event.
 */
export interface StdHelpdeskTicketTicketLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TicketLoadFailed` event.
 */
export interface StdHelpdeskTicketTicketLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdHelpdeskTicketConfig {
  /** Default: `["L1","L2","manager"]` */
  escalationMatrix?: string[];
  /** Default: `"medium"` */
  severity?: 'low' | 'medium' | 'high' | 'critical';
  /** Default: `"Tickets"` */
  title?: string;
  /** Default: `60` */
  firstReplyDeadline?: number;
  /** Default: `60` */
  responseTimeMinutes?: number;
  /** Default: `[{"label":"Open","icon":"arrow-right","variant":"primary","event":"OPEN_TICKET"},{"icon":"x","label":"Close","variant":"danger","event":"CLOSE"}]` */
  itemActions?: EntityRow[];
  /** Default: `"silver"` */
  slaTier?: 'bronze' | 'silver' | 'gold' | 'platinum';
  /** Default: `["billing","technical","account","general"]` */
  categories?: string[];
  /** Default: `"skills"` */
  routingStrategy?: 'round-robin' | 'skills' | 'manual' | 'load-balanced';
  /** Default: `[{"variant":"caption","name":"subject","label":"Subject"},{"variant":"caption","name":"requester","label":"Requester"},{"variant":"badge","name":"priority","label":"Priority"},{"variant":"badge","name":"status","label":"Status"},{"label":"Assigned","variant":"caption","name":"assignedTo"},{"name":"category","label":"Category","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `7` */
  autoCloseInactiveDays?: number;
  /** Default: `1` */
  csatSurveySentAfterDays?: number;
  /** Default: `24` */
  resolutionSLAHours?: number;
}

/**
 * Params for the std-helpdesk-ticket descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdHelpdeskTicketParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdHelpdeskTicketEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdHelpdeskTicketConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `HelpdeskTicket.traits.TicketManage`. */
export function stdHelpdeskTicketTrait(params: StdHelpdeskTicketParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TicketManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `HelpdeskTicket.pages.TicketManagePage`. */
export function stdHelpdeskTicketPage(params: StdHelpdeskTicketParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TicketManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdHelpdeskTicket(params: StdHelpdeskTicketParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TicketOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdHelpdeskTicketTrait(params),
    ],
    pages: [
      stdHelpdeskTicketPage(params),
    ],
  });
}
