/**
 * std-waitlist — std-waitlist — sorted queue with auto-promote-on-cancel. Listens for SET_STATUS so std-lifecycle can auto-expire. Exposes Scheduling/Booking knobs (bookingWindowDays, leadTimeMinutes, cancellationPolicy, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-waitlist.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-waitlist
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-waitlist';
const ALIAS = 'Waitlist';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdWaitlistEventKey = 'CANCEL' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_ENTRY' | 'PROMOTE' | 'SET_STATUS' | 'WaitlistEntryLoadFailed' | 'WaitlistEntryLoaded';

/**
 * Payload shape for the `OPEN_ENTRY` event.
 */
export interface StdWaitlistOpenEntryPayload {
  id: string;
  row?: {
    id: string;
    targetId: string;
    targetType: string;
    userId: string;
    position: number;
    status?: string;
    joinedAt?: string;
    promotedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `PROMOTE` event.
 */
export interface StdWaitlistPromotePayload {
  entryId: string;
  row?: {
    id: string;
    targetId: string;
    targetType: string;
    userId: string;
    position: number;
    status?: string;
    joinedAt?: string;
    promotedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `CANCEL` event.
 */
export interface StdWaitlistCancelPayload {
  entryId: string;
  row?: {
    id: string;
    targetId: string;
    targetType: string;
    userId: string;
    position: number;
    status?: string;
    joinedAt?: string;
    promotedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `WaitlistEntryLoaded` event.
 */
export interface StdWaitlistWaitlistEntryLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `WaitlistEntryLoadFailed` event.
 */
export interface StdWaitlistWaitlistEntryLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdWaitlistConfig {
  /** Default: `30` */
  bookingWindowDays?: number;
  /** Default: `[{"icon":"arrow-right","event":"OPEN_ENTRY","label":"Open","variant":"primary"},{"event":"CANCEL","label":"Cancel","variant":"danger","icon":"x"}]` */
  itemActions?: EntityRow[];
  /** Default: `[]` */
  blackoutDates?: string[];
  /** Default: `24` */
  rescheduleWindowHours?: number;
  /** Default: `"Waitlist"` */
  title?: string;
  /** Default: `"00:00-23:59"` */
  bookingHours?: string;
  /** Default: `60` */
  noShowGracePeriodMinutes?: number;
  /** Default: `1` */
  maxReschedules?: number;
  /** Default: `[{"name":"position","label":"#","variant":"badge"},{"label":"User","variant":"caption","name":"userId"},{"name":"targetType","label":"Type","variant":"badge"},{"name":"status","label":"Status","variant":"badge"},{"name":"joinedAt","variant":"caption","label":"Joined"}]` */
  fields?: EntityRow[];
  /** Default: `0` */
  leadTimeMinutes?: number;
  /** Default: `"free"` */
  cancellationPolicy?: 'free' | 'partial-refund' | 'no-refund';
}

/**
 * Params for the std-waitlist descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdWaitlistParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdWaitlistEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdWaitlistConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Waitlist.traits.WaitlistManage`. */
export function stdWaitlistTrait(params: StdWaitlistParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.WaitlistManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Waitlist.pages.WaitlistManagePage`. */
export function stdWaitlistPage(params: StdWaitlistParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.WaitlistManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdWaitlist(params: StdWaitlistParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'WaitlistOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdWaitlistTrait(params),
    ],
    pages: [
      stdWaitlistPage(params),
    ],
  });
}
