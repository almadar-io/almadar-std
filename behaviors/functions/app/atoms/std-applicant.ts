/**
 * std-applicant — std-applicant — job applicant primitive tracking a person who applied to a job, their pipeline stage, and basic info. Lists applicants in `browsing`; opens applicant detail in `viewing_single` with Advance / Reject actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-applicant.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-applicant
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-applicant';
const ALIAS = 'Applicant';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdApplicantEventKey = 'ADVANCE_STAGE' | 'ApplicantLoadFailed' | 'ApplicantLoaded' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_APPLICANT' | 'REJECT_APPLICANT';

/**
 * Payload shape for the `OPEN_APPLICANT` event.
 */
export interface StdApplicantOpenApplicantPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    email?: string;
    phone?: string;
    jobId?: string;
    resumeUrl?: string;
    stage?: string;
    notes?: string;
    appliedAt?: string;
  };
}

/**
 * Payload shape for the `ADVANCE_STAGE` event.
 */
export interface StdApplicantAdvanceStagePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    email?: string;
    phone?: string;
    jobId?: string;
    resumeUrl?: string;
    stage?: string;
    notes?: string;
    appliedAt?: string;
  };
}

/**
 * Payload shape for the `REJECT_APPLICANT` event.
 */
export interface StdApplicantRejectApplicantPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    email?: string;
    phone?: string;
    jobId?: string;
    resumeUrl?: string;
    stage?: string;
    notes?: string;
    appliedAt?: string;
  };
}

/**
 * Payload shape for the `ApplicantLoaded` event.
 */
export interface StdApplicantApplicantLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ApplicantLoadFailed` event.
 */
export interface StdApplicantApplicantLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdApplicantConfig {
  /** Default: `[{"label":"Open","icon":"arrow-right","variant":"primary","event":"OPEN_APPLICANT"},{"event":"REJECT_APPLICANT","label":"Reject","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `"Applicants"` */
  title?: string;
  /** Default: `[{"variant":"caption","name":"name","label":"Name"},{"label":"Email","variant":"caption","name":"email"},{"name":"jobId","variant":"caption","label":"Job"},{"name":"stage","variant":"badge","label":"Stage"},{"name":"appliedAt","variant":"caption","label":"Applied"}]` */
  fields?: EntityRow[];
}

/**
 * Params for the std-applicant descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdApplicantParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdApplicantEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdApplicantConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Applicant.traits.ApplicantPipeline`. */
export function stdApplicantTrait(params: StdApplicantParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ApplicantPipeline`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Applicant.pages.ApplicantPage`. */
export function stdApplicantPage(params: StdApplicantParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ApplicantPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdApplicant(params: StdApplicantParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ApplicantOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdApplicantTrait(params),
    ],
    pages: [
      stdApplicantPage(params),
    ],
  });
}
