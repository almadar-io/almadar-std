/**
 * std-driver — std-driver — fleet driver primitive tracking license info, hire date, status, and currently-assigned vehicle.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-driver.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-driver
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-driver';
const ALIAS = 'Driver';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdDriverEventKey = 'ASSIGN_VEHICLE' | 'CREATE' | 'DriverLoadFailed' | 'DriverLoaded' | 'EDIT' | 'FILTER_STATUS' | 'INIT' | 'REFRESH' | 'REINSTATE' | 'REQUEST_DELETE' | 'SEARCH' | 'SUSPEND' | 'TERMINATE' | 'VIEW';

/**
 * Payload shape for the `VIEW` event.
 */
export interface StdDriverViewPayload {
  id: string;
  source?: string;
}

/**
 * Payload shape for the `EDIT` event.
 */
export interface StdDriverEditPayload {
  id: string;
  source?: string;
}

/**
 * Payload shape for the `CREATE` event.
 */
export interface StdDriverCreatePayload {
  source?: string;
}

/**
 * Payload shape for the `REQUEST_DELETE` event.
 */
export interface StdDriverRequestDeletePayload {
  id: string;
  source?: string;
}

/**
 * Payload shape for the `SUSPEND` event.
 */
export interface StdDriverSuspendPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    licenseNumber: string;
    licenseClass?: string;
    licenseExpiresAt?: string;
    hiredAt?: string;
    phone?: string;
    email?: string;
    status?: string;
    assignedVehicleId?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `REINSTATE` event.
 */
export interface StdDriverReinstatePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    licenseNumber: string;
    licenseClass?: string;
    licenseExpiresAt?: string;
    hiredAt?: string;
    phone?: string;
    email?: string;
    status?: string;
    assignedVehicleId?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `TERMINATE` event.
 */
export interface StdDriverTerminatePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    licenseNumber: string;
    licenseClass?: string;
    licenseExpiresAt?: string;
    hiredAt?: string;
    phone?: string;
    email?: string;
    status?: string;
    assignedVehicleId?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `ASSIGN_VEHICLE` event.
 */
export interface StdDriverAssignVehiclePayload {
  id: string;
  row?: {
    id: string;
    name: string;
    licenseNumber: string;
    licenseClass?: string;
    licenseExpiresAt?: string;
    hiredAt?: string;
    phone?: string;
    email?: string;
    status?: string;
    assignedVehicleId?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `DriverLoaded` event.
 */
export interface StdDriverDriverLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `DriverLoadFailed` event.
 */
export interface StdDriverDriverLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdDriverConfig {
  /** Default: `"Drivers"` */
  title?: string;
}

/**
 * Params for the std-driver descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdDriverParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdDriverEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdDriverConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Driver.traits.DriverRoster`. */
export function stdDriverTrait(params: StdDriverParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DriverRoster`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Driver.pages.DriverPage`. */
export function stdDriverPage(params: StdDriverParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DriverPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdDriver(params: StdDriverParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'DriverOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdDriverTrait(params),
    ],
    pages: [
      stdDriverPage(params),
    ],
  });
}
