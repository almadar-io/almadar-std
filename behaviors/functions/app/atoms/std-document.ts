/**
 * std-document — std-document — document lifecycle with classification, retention, and lock. Listens for SET_STATUS so siblings can drive automated transitions. Composes naturally with std-file-store / std-version-history / std-esign-request at the organism level.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-document.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-document
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-document';
const ALIAS = 'Document';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdDocumentEventKey = 'CLOSE_VIEW' | 'DOWNLOAD' | 'DocumentLoadFailed' | 'DocumentLoaded' | 'INIT' | 'LOCK' | 'OPEN_DOCUMENT' | 'REVOKE' | 'SET_STATUS' | 'UNLOCK';

/**
 * Payload shape for the `OPEN_DOCUMENT` event.
 */
export interface StdDocumentOpenDocumentPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    mimeType?: string;
    classification?: string;
    lockedBy?: string;
    lockedAt?: string;
    retentionUntil?: string;
    legalHold?: boolean;
    createdAt?: string;
    updatedAt?: string;
  };
}

/**
 * Payload shape for the `LOCK` event.
 */
export interface StdDocumentLockPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    mimeType?: string;
    classification?: string;
    lockedBy?: string;
    lockedAt?: string;
    retentionUntil?: string;
    legalHold?: boolean;
    createdAt?: string;
    updatedAt?: string;
  };
}

/**
 * Payload shape for the `UNLOCK` event.
 */
export interface StdDocumentUnlockPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    mimeType?: string;
    classification?: string;
    lockedBy?: string;
    lockedAt?: string;
    retentionUntil?: string;
    legalHold?: boolean;
    createdAt?: string;
    updatedAt?: string;
  };
}

/**
 * Payload shape for the `DOWNLOAD` event.
 */
export interface StdDocumentDownloadPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    mimeType?: string;
    classification?: string;
    lockedBy?: string;
    lockedAt?: string;
    retentionUntil?: string;
    legalHold?: boolean;
    createdAt?: string;
    updatedAt?: string;
  };
}

/**
 * Payload shape for the `REVOKE` event.
 */
export interface StdDocumentRevokePayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    mimeType?: string;
    classification?: string;
    lockedBy?: string;
    lockedAt?: string;
    retentionUntil?: string;
    legalHold?: boolean;
    createdAt?: string;
    updatedAt?: string;
  };
}

/**
 * Payload shape for the `DocumentLoaded` event.
 */
export interface StdDocumentDocumentLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `DocumentLoadFailed` event.
 */
export interface StdDocumentDocumentLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdDocumentConfig {
  /** Default: `[{"name":"title","label":"Title","variant":"caption"},{"label":"Type","name":"mimeType","variant":"caption"},{"variant":"badge","label":"Classification","name":"classification"},{"variant":"caption","label":"Locked by","name":"lockedBy"},{"label":"Retention","name":"retentionUntil","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `7` */
  defaultRetentionYears?: number;
  /** Default: `["public","internal","confidential","restricted"]` */
  classificationLevels?: string[];
  /** Default: `"Documents"` */
  title?: string;
  /** Default: `["application/pdf","application/msword","image/png","image/jpeg"]` */
  allowedMimeTypes?: string[];
  /** Default: `"soft"` */
  lockedStatus?: 'none' | 'soft' | 'hard';
  /** Default: `false` */
  requireReasonOnDownload?: boolean;
  /** Default: `[{"label":"Open","variant":"primary","icon":"arrow-right","event":"OPEN_DOCUMENT"},{"event":"DOWNLOAD","variant":"secondary","icon":"download","label":"Download"}]` */
  itemActions?: EntityRow[];
  /** Default: `10` */
  versionRetentionCount?: number;
  /** Default: `"none"` */
  watermarkPolicy?: 'none' | 'viewer-id' | 'viewer-id-timestamp';
  /** Default: `true` */
  legalHoldEnabled?: boolean;
}

/**
 * Params for the std-document descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdDocumentParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdDocumentEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdDocumentConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Document.traits.DocumentManage`. */
export function stdDocumentTrait(params: StdDocumentParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DocumentManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Document.pages.DocumentManagePage`. */
export function stdDocumentPage(params: StdDocumentParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DocumentManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdDocument(params: StdDocumentParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'DocumentOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdDocumentTrait(params),
    ],
    pages: [
      stdDocumentPage(params),
    ],
  });
}
