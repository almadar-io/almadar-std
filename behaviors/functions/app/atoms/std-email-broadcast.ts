/**
 * std-email-broadcast — std-email-broadcast — send an email blast to a segment of contacts using a chosen template. Lists broadcasts; detail offers Send / Cancel. Listens for SET_STATUS so std-lifecycle can drive transitions. Exposes Communication knobs (sendsPerHour, gdprConsentRequired, unsubscribe link, …).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-email-broadcast.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-email-broadcast
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-email-broadcast';
const ALIAS = 'EmailBroadcast';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdEmailBroadcastEventKey = 'CANCEL_BROADCAST' | 'CLOSE_VIEW' | 'EmailBroadcastLoadFailed' | 'EmailBroadcastLoaded' | 'EmailBroadcastUpdateFailed' | 'EmailBroadcastUpdated' | 'INIT' | 'OPEN_BROADCAST' | 'SEND' | 'SET_STATUS';

/**
 * Payload shape for the `OPEN_BROADCAST` event.
 */
export interface StdEmailBroadcastOpenBroadcastPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    subject?: string;
    segmentId?: string;
    templateId?: string;
    status?: string;
    scheduledAt?: string;
    recipientCount?: number;
    sentCount?: number;
  };
}

/**
 * Payload shape for the `SEND` event.
 */
export interface StdEmailBroadcastSendPayload {
  broadcastId: string;
}

/**
 * Payload shape for the `CANCEL_BROADCAST` event.
 */
export interface StdEmailBroadcastCancelBroadcastPayload {
  id: string;
}

/**
 * Payload shape for the `EmailBroadcastLoaded` event.
 */
export interface StdEmailBroadcastEmailBroadcastLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `EmailBroadcastLoadFailed` event.
 */
export interface StdEmailBroadcastEmailBroadcastLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `EmailBroadcastUpdated` event.
 */
export interface StdEmailBroadcastEmailBroadcastUpdatedPayload {
  row?: {
    id: string;
    name: string;
    subject?: string;
    segmentId?: string;
    templateId?: string;
    status?: string;
    scheduledAt?: string;
    recipientCount?: number;
    sentCount?: number;
  };
}

/**
 * Payload shape for the `EmailBroadcastUpdateFailed` event.
 */
export interface StdEmailBroadcastEmailBroadcastUpdateFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdEmailBroadcastConfig {
  /** Default: `"Email Broadcasts"` */
  title?: string;
  /** Default: `[{"variant":"caption","label":"Name","name":"name"},{"name":"subject","label":"Subject","variant":"caption"},{"name":"status","variant":"badge","label":"Status"},{"variant":"caption","label":"Recipients","name":"recipientCount"},{"variant":"caption","name":"sentCount","label":"Sent"},{"name":"scheduledAt","label":"Scheduled","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `[{"variant":"primary","event":"OPEN_BROADCAST","icon":"arrow-right","label":"Open"},{"event":"CANCEL_BROADCAST","label":"Cancel","variant":"danger"}]` */
  itemActions?: EntityRow[];
  /** Default: `1000` */
  sendsPerHour?: number;
  /** Default: `true` */
  suppressionListEnabled?: boolean;
  /** Default: `true` */
  trackClicks?: boolean;
  /** Default: `true` */
  gdprConsentRequired?: boolean;
  /** Default: `true` */
  includeUnsubscribeLink?: boolean;
  /** Default: `""` */
  physicalAddressFooter?: string;
  /** Default: `true` */
  respectQuietHours?: boolean;
  /** Default: `5` */
  frequencyCapPerWeek?: number;
  /** Default: `true` */
  trackOpens?: boolean;
}

/**
 * Params for the std-email-broadcast descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdEmailBroadcastParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdEmailBroadcastEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdEmailBroadcastConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `EmailBroadcast.traits.EmailBroadcastBroadcast`. */
export function stdEmailBroadcastTrait(params: StdEmailBroadcastParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.EmailBroadcastBroadcast`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `EmailBroadcast.pages.EmailBroadcastPage`. */
export function stdEmailBroadcastPage(params: StdEmailBroadcastParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.EmailBroadcastPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdEmailBroadcast(params: StdEmailBroadcastParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'EmailBroadcastOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdEmailBroadcastTrait(params),
    ],
    pages: [
      stdEmailBroadcastPage(params),
    ],
  });
}
