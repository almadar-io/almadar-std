/**
 * std-response-collector — std-response-collector — survey per-respondent answer accumulator. Lists responses; detail offers Resubmit / Delete. Exposes Communication knobs (suppressionListEnabled, gdprConsentRequired, …) for the questionnaire.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-response-collector.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-response-collector
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-response-collector';
const ALIAS = 'ResponseCollector';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdResponseCollectorEventKey = 'CLOSE_VIEW' | 'DELETE_RESPONSE' | 'INIT' | 'OPEN_RESPONSE' | 'RESUBMIT' | 'ResponseLoadFailed' | 'ResponseLoaded';

/**
 * Payload shape for the `OPEN_RESPONSE` event.
 */
export interface StdResponseCollectorOpenResponsePayload {
  id: string;
  row?: {
    id: string;
    surveyId: string;
    respondentId: string;
    questionId: string;
    answerJson?: string;
    submittedAt?: string;
  };
}

/**
 * Payload shape for the `RESUBMIT` event.
 */
export interface StdResponseCollectorResubmitPayload {
  id: string;
}

/**
 * Payload shape for the `DELETE_RESPONSE` event.
 */
export interface StdResponseCollectorDeleteResponsePayload {
  id: string;
}

/**
 * Payload shape for the `ResponseLoaded` event.
 */
export interface StdResponseCollectorResponseLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ResponseLoadFailed` event.
 */
export interface StdResponseCollectorResponseLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdResponseCollectorConfig {
  /** Default: `false` */
  trackClicks?: boolean;
  /** Default: `true` */
  respectQuietHours?: boolean;
  /** Default: `false` */
  trackOpens?: boolean;
  /** Default: `true` */
  includeUnsubscribeLink?: boolean;
  /** Default: `[{"variant":"caption","name":"questionId","label":"Question"},{"name":"respondentId","label":"Respondent","variant":"caption"},{"variant":"caption","name":"surveyId","label":"Survey"},{"name":"answerJson","variant":"caption","label":"Answer"},{"label":"Submitted","variant":"caption","name":"submittedAt"}]` */
  fields?: EntityRow[];
  /** Default: `2` */
  frequencyCapPerWeek?: number;
  /** Default: `[{"event":"OPEN_RESPONSE","variant":"primary","icon":"arrow-right","label":"Open"},{"variant":"danger","event":"DELETE_RESPONSE","label":"Delete"}]` */
  itemActions?: EntityRow[];
  /** Default: `true` */
  suppressionListEnabled?: boolean;
  /** Default: `500` */
  sendsPerHour?: number;
  /** Default: `""` */
  physicalAddressFooter?: string;
  /** Default: `true` */
  gdprConsentRequired?: boolean;
  /** Default: `"Responses"` */
  title?: string;
}

/**
 * Params for the std-response-collector descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdResponseCollectorParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdResponseCollectorEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdResponseCollectorConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ResponseCollector.traits.ResponseCollect`. */
export function stdResponseCollectorTrait(params: StdResponseCollectorParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ResponseCollect`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ResponseCollector.pages.ResponseCollectPage`. */
export function stdResponseCollectorPage(params: StdResponseCollectorParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ResponseCollectPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdResponseCollector(params: StdResponseCollectorParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ResponseCollectorOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdResponseCollectorTrait(params),
    ],
    pages: [
      stdResponseCollectorPage(params),
    ],
  });
}
