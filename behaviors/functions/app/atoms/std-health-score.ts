/**
 * std-health-score — std-health-score — customer success health score primitive surfacing churn risk via composite usage/support/engagement signals. Lists health scores in `browsing`; opens detail in `viewing_single` with Recalculate / Assign CSM actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-health-score.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-health-score
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-health-score';
const ALIAS = 'HealthScore';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdHealthScoreEventKey = 'ASSIGN_CSM' | 'CLOSE_VIEW' | 'HealthScoreLoadFailed' | 'HealthScoreLoaded' | 'INIT' | 'OPEN_SCORE' | 'RECALCULATE';

/**
 * Payload shape for the `OPEN_SCORE` event.
 */
export interface StdHealthScoreOpenScorePayload {
  id: string;
  row?: {
    id: string;
    customerId: string;
    customerName?: string;
    score?: number;
    tier?: string;
    usageScore?: number;
    supportScore?: number;
    engagementScore?: number;
    trend?: string;
    lastCalculatedAt?: string;
    assignedCsm?: string;
    pendingId?: string;
  };
}

/**
 * Payload shape for the `RECALCULATE` event.
 */
export interface StdHealthScoreRecalculatePayload {
  id: string;
}

/**
 * Payload shape for the `ASSIGN_CSM` event.
 */
export interface StdHealthScoreAssignCsmPayload {
  id: string;
}

/**
 * Payload shape for the `HealthScoreLoaded` event.
 */
export interface StdHealthScoreHealthScoreLoadedPayload {
  data?: EntityRow[];
  totalCount?: number;
}

/**
 * Payload shape for the `HealthScoreLoadFailed` event.
 */
export interface StdHealthScoreHealthScoreLoadFailedPayload {
  message?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdHealthScoreConfig {
  /** Default: `"Customer Health Scores"` */
  title?: string;
  /** Default: `[{"variant":"primary","event":"OPEN_SCORE","icon":"arrow-right","label":"Open"},{"event":"RECALCULATE","variant":"secondary","label":"Recalculate"}]` */
  itemActions?: TraitConfig[];
  /** Default: `[{"name":"customerName","label":"Customer","variant":"caption"},{"label":"Score","variant":"badge","name":"score"},{"name":"tier","label":"Tier","variant":"badge"},{"label":"Usage","name":"usageScore","variant":"caption"},{"label":"Support","name":"supportScore","variant":"caption"},{"label":"Trend","variant":"badge","name":"trend"},{"variant":"caption","name":"assignedCsm","label":"CSM"}]` */
  fields?: TraitConfig[];
}

/**
 * Params for the std-health-score descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdHealthScoreParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdHealthScoreEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdHealthScoreConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `HealthScore.traits.HealthScoreDashboard`. */
export function stdHealthScoreTrait(params: StdHealthScoreParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.HealthScoreDashboard`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `HealthScore.pages.HealthScorePage`. */
export function stdHealthScorePage(params: StdHealthScoreParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.HealthScorePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdHealthScore(params: StdHealthScoreParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'HealthScoreOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdHealthScoreTrait(params),
    ],
    pages: [
      stdHealthScorePage(params),
    ],
  });
}
