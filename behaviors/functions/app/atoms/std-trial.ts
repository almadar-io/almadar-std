/**
 * std-trial — std-trial — subscription trial countdown with auto-convert at expiry. Lists trials in `browsing`; opens detail in `viewing_single` with Convert / Expire / Cancel actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-trial.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-trial
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-trial';
const ALIAS = 'Trial';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdTrialEventKey = 'CANCEL' | 'CLOSE_VIEW' | 'CONVERT' | 'EXPIRE' | 'INIT' | 'OPEN_TRIAL' | 'TrialLoadFailed' | 'TrialLoaded';

/**
 * Payload shape for the `OPEN_TRIAL` event.
 */
export interface StdTrialOpenTrialPayload {
  id: string;
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    planId: string;
    startedAt?: string;
    expiresAt: string;
    status?: string;
    convertedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `CONVERT` event.
 */
export interface StdTrialConvertPayload {
  trialId: string;
  convertedAt: string;
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    planId: string;
    startedAt?: string;
    expiresAt: string;
    status?: string;
    convertedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `EXPIRE` event.
 */
export interface StdTrialExpirePayload {
  trialId: string;
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    planId: string;
    startedAt?: string;
    expiresAt: string;
    status?: string;
    convertedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `CANCEL` event.
 */
export interface StdTrialCancelPayload {
  trialId: string;
  row?: {
    id: string;
    subscriptionId: string;
    customerId: string;
    planId: string;
    startedAt?: string;
    expiresAt: string;
    status?: string;
    convertedAt?: string;
    notifiedAt?: string;
  };
}

/**
 * Payload shape for the `TrialLoaded` event.
 */
export interface StdTrialTrialLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `TrialLoadFailed` event.
 */
export interface StdTrialTrialLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdTrialConfig {
  /** Default: `[{"name":"customerId","label":"Customer","variant":"caption"},{"label":"Plan","variant":"caption","name":"planId"},{"label":"Status","variant":"badge","name":"status"},{"name":"startedAt","variant":"caption","label":"Started"},{"variant":"caption","label":"Expires","name":"expiresAt"},{"label":"Converted","name":"convertedAt","variant":"caption"},{"label":"Notified","name":"notifiedAt","variant":"caption"}]` */
  fields?: TraitConfig[];
  /** Default: `"Trials"` */
  title?: string;
  /** Default: `[{"label":"Open","icon":"arrow-right","event":"OPEN_TRIAL","variant":"primary"},{"icon":"x","variant":"danger","label":"Cancel","event":"CANCEL"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-trial descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdTrialParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdTrialEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdTrialConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `Trial.traits.TrialManage`. */
export function stdTrialTrait(params: StdTrialParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.TrialManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `Trial.pages.TrialManagePage`. */
export function stdTrialPage(params: StdTrialParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.TrialManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdTrial(params: StdTrialParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'TrialOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdTrialTrait(params),
    ],
    pages: [
      stdTrialPage(params),
    ],
  });
}
