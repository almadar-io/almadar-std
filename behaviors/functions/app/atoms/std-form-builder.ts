/**
 * std-form-builder — std-form-builder — survey form composer with list/detail browse + sidebar question and rules editors. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-form-builder.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-form-builder
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-form-builder';
const ALIAS = 'FormBuilder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdFormBuilderEventKey = 'CLOSE_VIEW' | 'CREATE' | 'INIT' | 'OPEN_SURVEY' | 'PUBLISH' | 'SurveyCreated' | 'SurveyLoadFailed' | 'SurveyLoaded' | 'SurveyPublished' | 'SurveyUpdated';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdFormBuilderListenKey = 'QuestionsChanged' | 'RulesChanged';

/**
 * Payload shape for the `OPEN_SURVEY` event.
 */
export interface StdFormBuilderOpenSurveyPayload {
  id: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    questionsJson?: string;
    rulesJson?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `CREATE` event.
 */
export interface StdFormBuilderCreatePayload {
  id: string;
  data: EntityRow;
}

/**
 * Payload shape for the `PUBLISH` event.
 */
export interface StdFormBuilderPublishPayload {
  id: string;
  surveyId: string;
  row?: {
    id: string;
    title: string;
    description?: string;
    questionsJson?: string;
    rulesJson?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SurveyLoaded` event.
 */
export interface StdFormBuilderSurveyLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `SurveyLoadFailed` event.
 */
export interface StdFormBuilderSurveyLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `SurveyCreated` event.
 */
export interface StdFormBuilderSurveyCreatedPayload {
  row?: {
    id: string;
    title: string;
    description?: string;
    questionsJson?: string;
    rulesJson?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SurveyUpdated` event.
 */
export interface StdFormBuilderSurveyUpdatedPayload {
  row?: {
    id: string;
    title: string;
    description?: string;
    questionsJson?: string;
    rulesJson?: string;
    status?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `SurveyPublished` event.
 */
export interface StdFormBuilderSurveyPublishedPayload {
  id: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdFormBuilderConfig {
  /** Default: `[{"label":"Title","name":"title","variant":"caption"},{"name":"description","variant":"caption","label":"Description"},{"name":"status","variant":"badge","label":"Status"},{"name":"createdAt","label":"Created","variant":"caption"}]` */
  fields?: EntityRow[];
  /** Default: `[{"icon":"arrow-right","variant":"primary","label":"Open","event":"OPEN_SURVEY"},{"label":"Publish","icon":"send","event":"PUBLISH","variant":"secondary"}]` */
  itemActions?: EntityRow[];
  /** Default: `"Survey Builder"` */
  title?: string;
}

/**
 * Params for the std-form-builder descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdFormBuilderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdFormBuilderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdFormBuilderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `FormBuilder.traits.SurveyManage`. */
export function stdFormBuilderSurveyManageTrait(params: StdFormBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SurveyManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `FormBuilder.traits.SurveyQuestions`. */
export function stdFormBuilderSurveyQuestionsTrait(params: StdFormBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SurveyQuestions`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `FormBuilder.traits.SurveyBranching`. */
export function stdFormBuilderSurveyBranchingTrait(params: StdFormBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SurveyBranching`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `FormBuilder.pages.FormBuilderPage`. */
export function stdFormBuilderPage(params: StdFormBuilderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.FormBuilderPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdFormBuilder(params: StdFormBuilderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'FormBuilderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdFormBuilderSurveyManageTrait(params),
      stdFormBuilderSurveyQuestionsTrait(params),
      stdFormBuilderSurveyBranchingTrait(params),
    ],
    pages: [
      stdFormBuilderPage(params),
    ],
  });
}
