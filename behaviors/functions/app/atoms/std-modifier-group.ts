/**
 * std-modifier-group — std-modifier-group — restaurant menu item customization groups with required/optional/multi-select constraints. Owns a ModifierGroupRecord entity (constraint type, min/max selections, serialized options) and a single manage trait that loads groups, browses them in a data-grid with Create / Update / Delete actions, and persists each verdict back to the row. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-modifier-group.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-modifier-group
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { rebindInlineTraitEntity } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-modifier-group';
const ALIAS = 'ModifierGroup';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdModifierGroupEventKey = 'CLOSE_VIEW' | 'CREATE' | 'DELETE' | 'INIT' | 'ModifierGroupCreated' | 'ModifierGroupDeleted' | 'ModifierGroupLoadFailed' | 'ModifierGroupLoaded' | 'ModifierGroupUpdated' | 'ModifierGroupWriteFailed' | 'OPEN_GROUP' | 'UPDATE';

/**
 * Payload shape for the `OPEN_GROUP` event.
 */
export interface StdModifierGroupOpenGroupPayload {
  id: string;
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
}

/**
 * Payload shape for the `CREATE` event.
 */
export interface StdModifierGroupCreatePayload {
  data: EntityRow;
}

/**
 * Payload shape for the `UPDATE` event.
 */
export interface StdModifierGroupUpdatePayload {
  groupId: string;
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
  data: EntityRow;
}

/**
 * Payload shape for the `DELETE` event.
 */
export interface StdModifierGroupDeletePayload {
  groupId: string;
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
}

/**
 * Payload shape for the `ModifierGroupLoaded` event.
 */
export interface StdModifierGroupModifierGroupLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `ModifierGroupLoadFailed` event.
 */
export interface StdModifierGroupModifierGroupLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Payload shape for the `ModifierGroupCreated` event.
 */
export interface StdModifierGroupModifierGroupCreatedPayload {
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
}

/**
 * Payload shape for the `ModifierGroupUpdated` event.
 */
export interface StdModifierGroupModifierGroupUpdatedPayload {
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
}

/**
 * Payload shape for the `ModifierGroupDeleted` event.
 */
export interface StdModifierGroupModifierGroupDeletedPayload {
  row?: {
    id: string;
    name: string;
    constraintType?: string;
    minSelect?: number;
    maxSelect?: number;
    optionsJson?: string;
  };
}

/**
 * Payload shape for the `ModifierGroupWriteFailed` event.
 */
export interface StdModifierGroupModifierGroupWriteFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdModifierGroupConfig {
  /** Default: `"Modifier Groups"` */
  title?: string;
  /** Default: `[{"label":"Open","variant":"primary","event":"OPEN_GROUP","icon":"arrow-right"},{"icon":"trash","variant":"danger","label":"Delete","event":"DELETE"}]` */
  itemActions?: TraitConfig[];
  /** Default: `[{"name":"name","label":"Group","variant":"caption"},{"label":"Constraint","name":"constraintType","variant":"badge"},{"name":"required","label":"Required","variant":"badge"},{"label":"Min","name":"minSelect","variant":"caption"},{"label":"Max","variant":"caption","name":"maxSelect"}]` */
  fields?: TraitConfig[];
}

/**
 * Params for the std-modifier-group descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdModifierGroupParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdModifierGroupEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdModifierGroupConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ModifierGroup.traits.ModifierGroupManage`. */
export function stdModifierGroupTrait(params: StdModifierGroupParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ModifierGroupManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ModifierGroup.pages.ModifierGroupManagePage`. */
export function stdModifierGroupPage(params: StdModifierGroupParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ModifierGroupManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdModifierGroup(params: StdModifierGroupParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ModifierGroupOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdModifierGroupTrait(params),
    ],
    pages: [
      stdModifierGroupPage(params),
    ],
  });
}
