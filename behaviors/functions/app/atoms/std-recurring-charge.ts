/**
 * std-recurring-charge — std-recurring-charge — subscription cycle clock. Lists subscriptions in `browsing`; opens detail in `viewing_single` with Charge / Cancel actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-recurring-charge.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-recurring-charge
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-recurring-charge';
const ALIAS = 'RecurringCharge';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdRecurringChargeEventKey = 'CANCEL' | 'CHARGE' | 'CLOSE_VIEW' | 'INIT' | 'OPEN_SUBSCRIPTION' | 'SubscriptionLoadFailed' | 'SubscriptionLoaded';

/**
 * Payload shape for the `OPEN_SUBSCRIPTION` event.
 */
export interface StdRecurringChargeOpenSubscriptionPayload {
  id: string;
  row?: {
    id: string;
    customerId: string;
    planId: string;
    status?: string;
    amount?: number;
    currency?: string;
    interval?: string;
    currentPeriodStart?: string;
    currentPeriodEnd?: string;
    nextBillingAt?: string;
    attemptCount?: number;
    lastChargeAt?: string;
    cancelledAt?: string;
    createdAt?: string;
  };
}

/**
 * Payload shape for the `CHARGE` event.
 */
export interface StdRecurringChargeChargePayload {
  id: string;
}

/**
 * Payload shape for the `CANCEL` event.
 */
export interface StdRecurringChargeCancelPayload {
  id: string;
}

/**
 * Payload shape for the `SubscriptionLoaded` event.
 */
export interface StdRecurringChargeSubscriptionLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `SubscriptionLoadFailed` event.
 */
export interface StdRecurringChargeSubscriptionLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdRecurringChargeConfig {
  /** Default: `"Subscriptions"` */
  title?: string;
  /** Default: `4` */
  maxRetries?: number;
  /** Default: `[{"label":"Customer","variant":"caption","name":"customerId"},{"name":"planId","variant":"caption","label":"Plan"},{"variant":"badge","label":"Status","name":"status"},{"name":"amount","label":"Amount","variant":"caption"},{"label":"Interval","name":"interval","variant":"badge"},{"name":"nextBillingAt","label":"Next Bill","variant":"caption"},{"name":"attemptCount","label":"Retries","variant":"caption"}]` */
  fields?: TraitConfig[];
  /** Default: `[{"event":"OPEN_SUBSCRIPTION","label":"Open","icon":"arrow-right","variant":"primary"},{"variant":"danger","label":"Cancel","event":"CANCEL"}]` */
  itemActions?: TraitConfig[];
}

/**
 * Params for the std-recurring-charge descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdRecurringChargeParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdRecurringChargeEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdRecurringChargeConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `RecurringCharge.traits.SubscriptionManage`. */
export function stdRecurringChargeTrait(params: StdRecurringChargeParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.SubscriptionManage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `RecurringCharge.pages.SubscriptionManagePage`. */
export function stdRecurringChargePage(params: StdRecurringChargeParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.SubscriptionManagePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdRecurringCharge(params: StdRecurringChargeParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'SubscriptionOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdRecurringChargeTrait(params),
    ],
    pages: [
      stdRecurringChargePage(params),
    ],
  });
}
