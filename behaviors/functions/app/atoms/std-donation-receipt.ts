/**
 * std-donation-receipt — std-donation-receipt — tax-deductible donation receipt with IRS-compliance fields (taxYear, taxDeductible, nonprofitEin). Lists receipts in `browsing`; opens detail in `viewing_single` with Email / Void actions. Agents tune the list shape via `config.fields` + `config.itemActions`.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-donation-receipt.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-donation-receipt
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-donation-receipt';
const ALIAS = 'DonationReceipt';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdDonationReceiptEventKey = 'CLOSE_VIEW' | 'DonationReceiptLoadFailed' | 'DonationReceiptLoaded' | 'EMAIL_RECEIPT' | 'INIT' | 'OPEN_RECEIPT' | 'VOID_RECEIPT';

/**
 * Payload shape for the `OPEN_RECEIPT` event.
 */
export interface StdDonationReceiptOpenReceiptPayload {
  id: string;
  row?: {
    id: string;
    receiptNumber: string;
    donorId: string;
    donorName?: string;
    campaignId?: string;
    amount?: number;
    donationDate: string;
    taxYear?: number;
    taxDeductible?: boolean;
    nonprofitEin?: string;
    isEmailed?: boolean;
  };
}

/**
 * Payload shape for the `EMAIL_RECEIPT` event.
 */
export interface StdDonationReceiptEmailReceiptPayload {
  id: string;
}

/**
 * Payload shape for the `VOID_RECEIPT` event.
 */
export interface StdDonationReceiptVoidReceiptPayload {
  id: string;
}

/**
 * Payload shape for the `DonationReceiptLoaded` event.
 */
export interface StdDonationReceiptDonationReceiptLoadedPayload {
  data?: EntityRow[];
}

/**
 * Payload shape for the `DonationReceiptLoadFailed` event.
 */
export interface StdDonationReceiptDonationReceiptLoadFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdDonationReceiptConfig {
  /** Default: `[{"event":"OPEN_RECEIPT","variant":"primary","icon":"arrow-right","label":"Open"},{"event":"VOID_RECEIPT","label":"Void","variant":"danger"}]` */
  itemActions?: TraitConfig[];
  /** Default: `[{"variant":"caption","label":"Receipt #","name":"receiptNumber"},{"label":"Donor","name":"donorName","variant":"caption"},{"label":"Amount","variant":"caption","name":"amount"},{"variant":"caption","label":"Date","name":"donationDate"},{"name":"taxYear","variant":"caption","label":"Tax Year"},{"variant":"badge","label":"Emailed","name":"isEmailed"}]` */
  fields?: TraitConfig[];
  /** Default: `"Donation Receipts"` */
  title?: string;
}

/**
 * Params for the std-donation-receipt descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdDonationReceiptParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdDonationReceiptEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdDonationReceiptConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `DonationReceipt.traits.DonationReceiptIssuer`. */
export function stdDonationReceiptTrait(params: StdDonationReceiptParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.DonationReceiptIssuer`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `DonationReceipt.pages.DonationReceiptPage`. */
export function stdDonationReceiptPage(params: StdDonationReceiptParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.DonationReceiptPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdDonationReceipt(params: StdDonationReceiptParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'DonationReceiptOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdDonationReceiptTrait(params),
    ],
    pages: [
      stdDonationReceiptPage(params),
    ],
  });
}
