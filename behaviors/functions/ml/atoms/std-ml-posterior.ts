/**
 * std-ml-posterior — std-ml-posterior — the statistical rung (R2) of the intelligence ladder. Holds a Beta(alpha, beta) belief over an unknown success rate and updates it from boolean observations (Bayesian updating, knowledge tracing, Thompson-sampling prior). After each observation it draws samples from the posterior, computes a credible interval, and decides: MASTERY_REACHED when the mean clears the floor with a tight interval, REMEDIATION_NEEDED when the mean sits at or below the ceiling with the same confidence, or EVIDENCE_INSUFFICIENT — the abstain — when the interval is still too wide to trust. Use when a hand-tuned score needs to become a belief that can say 'not enough evidence yet' (mastery, skill, risk, preference, difficulty).
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-ml-posterior.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-ml-posterior
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-ml-posterior';
const ALIAS = 'MlPosterior';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdMlPosteriorEventKey = 'EVIDENCE_INSUFFICIENT' | 'INIT' | 'MASTERY_REACHED' | 'OBSERVE' | 'REMEDIATION_NEEDED';

/**
 * Payload shape for the `MASTERY_REACHED` event.
 */
export interface StdMlPosteriorMasteryReachedPayload {
  mean: number;
  intervalLow: number;
  intervalHigh: number;
}

/**
 * Payload shape for the `REMEDIATION_NEEDED` event.
 */
export interface StdMlPosteriorRemediationNeededPayload {
  mean: number;
  intervalLow: number;
  intervalHigh: number;
}

/**
 * Payload shape for the `EVIDENCE_INSUFFICIENT` event.
 */
export interface StdMlPosteriorEvidenceInsufficientPayload {
  mean: number;
  intervalLow: number;
  intervalHigh: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdMlPosteriorConfig {
  /** Default: `0.3` */
  intervalWidthMax?: number;
  /** Default: `0.8` */
  masteryFloor?: number;
  /** Default: `1` */
  priorAlpha?: number;
  /** Default: `1` */
  priorBeta?: number;
  /** Default: `0.4` */
  remediationCeiling?: number;
  /** Default: `200` */
  sampleCount?: number;
}

/**
 * Params for the std-ml-posterior descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdMlPosteriorParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdMlPosteriorEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdMlPosteriorConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `MlPosterior.traits.PosteriorBelief`. */
export function stdMlPosteriorTrait(params: StdMlPosteriorParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PosteriorBelief`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `MlPosterior.pages.MlPosteriorPage`. */
export function stdMlPosteriorPage(params: StdMlPosteriorParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.MlPosteriorPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdMlPosterior(params: StdMlPosteriorParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'MlPosteriorOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdMlPosteriorTrait(params),
    ],
    pages: [
      stdMlPosteriorPage(params),
    ],
  });
}
