/**
 * std-graph-builder — Graph construction atom — builds node/edge structures from an entity collection for downstream GNN pipelines; pure array/object transform, no ML compute.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/atoms/std-graph-builder.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-graph-builder
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-graph-builder';
const ALIAS = 'GraphBuilder';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdGraphBuilderEventKey = 'BUILD_GRAPH' | 'GRAPH_READY' | 'INIT' | 'RESET';

/**
 * Payload shape for the `GRAPH_READY` event.
 */
export interface StdGraphBuilderGraphReadyPayload {
  nodes: EntityRow[];
  edges: EntityRow[];
  nodeCount: number;
  edgeCount: number;
}

/**
 * Typed call-site config block for this trait — every
 * field maps to a `config { ... }` entry in the source
 * .lolo. The agent fills these to specialise the trait
 * without modifying its state-machine topology.
 */
export interface StdGraphBuilderConfig {
  /** Default: `false` */
  directed?: boolean;
  /** Default: `""` */
  edgeField?: string;
  /** Default: `[]` */
  nodeFeatures?: string[];
}

/**
 * Params for the std-graph-builder descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdGraphBuilderParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdGraphBuilderEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Typed call-site config block — see the per-field interface. */
  config?: StdGraphBuilderConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `GraphBuilder.traits.GraphBuilderRun`. */
export function stdGraphBuilderTrait(params: StdGraphBuilderParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.GraphBuilderRun`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `GraphBuilder.pages.GraphBuilderPage`. */
export function stdGraphBuilderPage(params: StdGraphBuilderParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.GraphBuilderPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdGraphBuilder(params: StdGraphBuilderParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'GraphBuilderOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdGraphBuilderTrait(params),
    ],
    pages: [
      stdGraphBuilderPage(params),
    ],
  });
}

type _StdGraphBuilderEntityName = 'GraphStructure';
type _StdGraphBuilderListenTraitName = 'GraphBuilderRun';

/**
 * Tunable params for the GraphBuilderOrbital orbital.
 *
 * Canonical entity: GraphStructure — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdGraphBuilderGraphBuilderOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'GraphBuilderRun',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait GraphBuilderOrbital's `uses[]` exports. */
type _StdGraphBuilderGraphBuilderOrbitalUsesRef = never;

/** Per-orbital factory: builds the GraphBuilderOrbital orbital with consumer params. */
export function stdGraphBuilderGraphBuilderOrbital(params: StdGraphBuilderGraphBuilderOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'GraphBuilderOrbital',
    uses: [],
    entity: {
      name: 'GraphStructure',
      persistence: 'runtime',
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'default': [],
            'items': {
              'type': 'object',
            },
            'name': 'nodes',
            'type': 'array',
          },
          {
            'default': [],
            'items': {
              'type': 'object',
            },
            'name': 'edges',
            'type': 'array',
          },
          {
            'default': 0,
            'name': 'nodeCount',
            'type': 'number',
          },
          {
            'default': 0,
            'name': 'edgeCount',
            'type': 'number',
          },
          {
            'default': 'idle',
            'name': 'status',
            'type': 'string',
            'values': [
              'idle',
              'built',
            ],
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'lifecycle',
        'config': {
          'directed': {
            'default': false,
            'description': 'Carried through for downstream consumers; edges are emitted once per adjacency entry, so an undirected reader should treat each edge as bidirectional',
            'label': 'Directed',
            'tier': 'policy',
            'type': 'boolean',
          },
          'edgeField': {
            'default': '',
            'description': 'Field on each source entity holding its adjacency — an array of target ids',
            'label': 'Edge field',
            'tier': 'domain',
            'type': 'string',
          },
          'nodeFeatures': {
            'default': [],
            'description': 'Entity fields carried onto each node alongside id',
            'items': {
              'type': 'string',
            },
            'label': 'Node features',
            'tier': 'domain',
            'type': '[string]',
          },
        },
        'emits': [
          {
            'description': 'Fired when node/edge structures have been built from the source entity collection',
            'event': 'GRAPH_READY',
            'payloadSchema': [
              {
                'name': 'nodes',
                'required': true,
                'type': '[object]',
              },
              {
                'name': 'edges',
                'required': true,
                'type': '[object]',
              },
              {
                'name': 'nodeCount',
                'required': true,
                'type': 'int',
              },
              {
                'name': 'edgeCount',
                'required': true,
                'type': 'int',
              },
            ],
            'scope': 'external',
            'tier': 'primary',
          },
        ],
        'linkedEntity': 'GraphStructure',
        'name': 'GraphBuilderRun',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'BUILD_GRAPH',
              'name': 'Build Graph',
              'payloadSchema': [
                {
                  'name': 'entities',
                  'required': true,
                  'type': '[object]',
                },
              ],
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
            {
              'description': 'Fired when node/edge structures have been built from the source entity collection',
              'key': 'GRAPH_READY',
              'name': 'Graph Ready',
              'payloadSchema': [
                {
                  'name': 'nodes',
                  'required': true,
                  'type': '[object]',
                },
                {
                  'name': 'edges',
                  'required': true,
                  'type': '[object]',
                },
                {
                  'name': 'nodeCount',
                  'required': true,
                  'type': 'int',
                },
                {
                  'name': 'edgeCount',
                  'required': true,
                  'type': 'int',
                },
              ],
              'tier': 'primary',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
            {
              'name': 'built',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.nodes',
                  [],
                ],
                [
                  'set',
                  '@entity.edges',
                  [],
                ],
                [
                  'set',
                  '@entity.nodeCount',
                  0,
                ],
                [
                  'set',
                  '@entity.edgeCount',
                  0,
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'let',
                  [
                    [
                      'nodeFields',
                      [
                        'array/unique',
                        [
                          'array/append',
                          '@config.nodeFeatures',
                          'id',
                        ],
                      ],
                    ],
                    [
                      'nodes',
                      [
                        'array/map',
                        '@payload.entities',
                        [
                          'fn',
                          'e',
                          [
                            'object/pick',
                            '@e',
                            '@nodeFields',
                          ],
                        ],
                      ],
                    ],
                    [
                      'edges',
                      [
                        'array/flatten',
                        [
                          'array/map',
                          '@payload.entities',
                          [
                            'fn',
                            'e',
                            [
                              'array/map',
                              [
                                'object/get',
                                '@e',
                                '@config.edgeField',
                                [],
                              ],
                              [
                                'fn',
                                'target',
                                {
                                  'source': [
                                    'object/get',
                                    '@e',
                                    'id',
                                  ],
                                  'target': '@target',
                                },
                              ],
                            ],
                          ],
                        ],
                      ],
                    ],
                  ],
                  [
                    'do',
                    [
                      'set',
                      '@entity.nodes',
                      '@nodes',
                    ],
                    [
                      'set',
                      '@entity.edges',
                      '@edges',
                    ],
                    [
                      'set',
                      '@entity.nodeCount',
                      [
                        'array/len',
                        '@nodes',
                      ],
                    ],
                    [
                      'set',
                      '@entity.edgeCount',
                      [
                        'array/len',
                        '@edges',
                      ],
                    ],
                    [
                      'set',
                      '@entity.status',
                      'built',
                    ],
                    [
                      'emit',
                      'GRAPH_READY',
                      {
                        'edgeCount': [
                          'array/len',
                          '@edges',
                        ],
                        'edges': '@edges',
                        'nodeCount': [
                          'array/len',
                          '@nodes',
                        ],
                        'nodes': '@nodes',
                      },
                    ],
                  ],
                ],
              ],
              'event': 'BUILD_GRAPH',
              'from': 'idle',
              'to': 'built',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.status',
                  'built',
                ],
              ],
              'event': 'INIT',
              'from': 'built',
              'to': 'built',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.nodes',
                  [],
                ],
                [
                  'set',
                  '@entity.edges',
                  [],
                ],
                [
                  'set',
                  '@entity.nodeCount',
                  0,
                ],
                [
                  'set',
                  '@entity.edgeCount',
                  0,
                ],
                [
                  'set',
                  '@entity.status',
                  'idle',
                ],
              ],
              'event': 'RESET',
              'from': 'built',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
    ],
    pages: [
      {
        'name': 'GraphBuilderPage',
        'path': '/graph-builder',
        'traits': [
          {
            'ref': 'GraphBuilderRun',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdGraphBuilderGraphBuilderOrbital. */
export const StdGraphBuilderGraphBuilderOrbitalManifest = {
  organism: 'std-graph-builder',
  orbitalName: 'GraphBuilderOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'GraphBuilderRun',
  ] as const,
};

/** Typed guard — runtime validates StdGraphBuilderGraphBuilderOrbitalParams keys. */
export function isStdGraphBuilderGraphBuilderOrbitalParams(p: object): p is StdGraphBuilderGraphBuilderOrbitalParams {
  type _OverrideRecord = NonNullable<StdGraphBuilderGraphBuilderOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdGraphBuilderGraphBuilderOrbitalManifest.traitNames,
      ...StdGraphBuilderGraphBuilderOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
