/**
 * std-knowledge-tracing — std-knowledge-tracing — turns one true/false observation ('did the learner get this right?') into a tracked belief about mastery, and captures the same observation as a labeled training example in the same motion. Composes std-ml-posterior (the R2 statistical rung — a Beta-Bernoulli belief over an unknown success rate) with std-ml-label-capture: every posterior verdict — MASTERY_REACHED, REMEDIATION_NEEDED, or the abstain EVIDENCE_INSUFFICIENT — becomes a persisted labeled example (the input skill, the posterior's own verdict, and the ground-truth outcome that produced it), so a cheaper rung can later be trained to reproduce the same call without running the statistics. Domain-agnostic: it knows about skills and observations, not students, quizzes, or courses. One instance tracks one belief (e.g. one learner × one skill); the host composes as many instances as it has beliefs to hold.
 *
 * Autogenerated by almadar-sync std-ts from
 * packages/almadar-std/behaviors/registry/molecules/std-knowledge-tracing.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   npx almadar-sync std-ts --only=std-knowledge-tracing
 *
 * @level molecule
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityRef, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine, Page } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';
import { mergeCallSiteConfigOverrides } from '../../../../factory-runtime/apply-params-to-orb.js';

const BEHAVIOR_PATH = 'std/behaviors/std-knowledge-tracing';
const ALIAS = 'KnowledgeTracing';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdKnowledgeTracingEventKey = 'CAPTURE_LABEL' | 'INIT' | 'OBSERVATION_MADE' | 'OBSERVE_SKILL' | 'VERDICT_REACHED';

/**
 * Closed set of event keys this trait listens for —
 * derived from the .orb's `listens[]` block.
 */
export type StdKnowledgeTracingListenKey = 'MASTERY_REACHED' | 'REMEDIATION_NEEDED' | 'EVIDENCE_INSUFFICIENT';

/**
 * Payload shape for the `OBSERVATION_MADE` event.
 */
export interface StdKnowledgeTracingObservationMadePayload {
  outcome: boolean;
}

/**
 * Payload shape for the `CAPTURE_LABEL` event.
 */
export interface StdKnowledgeTracingCaptureLabelPayload {
  input: unknown;
  verdict: unknown;
  source: string;
  outcome?: unknown;
}

/**
 * Params for the std-knowledge-tracing descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdKnowledgeTracingParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdKnowledgeTracingEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `KnowledgeTracing.traits.ObservationRelay`. */
export function stdKnowledgeTracingObservationRelayTrait(params: StdKnowledgeTracingParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ObservationRelay`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `KnowledgeTracing.traits.PosteriorTracker`. */
export function stdKnowledgeTracingPosteriorTrackerTrait(params: StdKnowledgeTracingParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.PosteriorTracker`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Trait descriptor: `KnowledgeTracing.traits.Labels`. */
export function stdKnowledgeTracingLabelsTrait(params: StdKnowledgeTracingParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.Labels`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `KnowledgeTracing.pages.KnowledgeTracingPage`. */
export function stdKnowledgeTracingPage(params: StdKnowledgeTracingParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.KnowledgeTracingPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdKnowledgeTracing(params: StdKnowledgeTracingParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    persistence: 'runtime',
  };
  return makeOrbitalWithUses({
    name: 'KnowledgeTracingOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdKnowledgeTracingObservationRelayTrait(params),
      stdKnowledgeTracingPosteriorTrackerTrait(params),
      stdKnowledgeTracingLabelsTrait(params),
    ],
    pages: [
      stdKnowledgeTracingPage(params),
    ],
  });
}

type _StdKnowledgeTracingEntityName = 'KnowledgeState' | 'Posterior' | 'LabeledExample';
type _StdKnowledgeTracingListenTraitName = 'ObservationRelay' | 'PosteriorTracker' | 'Labels';

/**
 * Tunable params for the KnowledgeTracingOrbital orbital.
 *
 * Canonical entity: KnowledgeState — overridable via
 * `entityName`. The factory threads the effective name through every
 * trait's `linkedEntity` binding; the `.orb` compiler's inline phase
 * auto-rewrites every `@Entity.x`, `["ref",X]`, `["fetch",X,…]`,
 * `["persist",…,X,…]` and payload type string accordingly.
 *
 * Override surface (mirrors `.lolo`'s native overrides 1:1):
 *   fields         — extra entity fields (appended)
 *   pagePath       — first-page URL override
 *   entityName     — rename the canonical entity
 *   traitOverrides — per-imported-trait `config`, `linkedEntity`,
 *                    `events`, `name`, `emitsScope`, `listens`.
 *                    `effects` is NOT exposed — `.lolo` removed it
 *                    in Phase 9.5.H. Use `listens` via a sibling
 *                    trait to react to atom events.
 */
export interface StdKnowledgeTracingKnowledgeTracingOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Rename the canonical entity (PascalCase singular, ≤32 chars). */
  entityName?: string;
  /**
   * Per-imported-trait override surface keyed on each imported
   * trait's canonical `name`. Accepts every override `.lolo`
   * natively supports: `config`, `linkedEntity`, `events`,
   * `name`, `emitsScope`, `listens`. `effects` is excluded —
   * atom-owned (use `listens` via a sibling trait instead).
   */
  traitOverrides?: Partial<Record<
    'PosteriorTracker' | 'Labels' | 'ObservationRelay',
    Pick<MakeTraitRefOpts, 'config' | 'linkedEntity' | 'events' | 'name' | 'emitsScope' | 'listens'>
  >>;
}

/** `'Alias.traits.TraitName'` literal union of every trait KnowledgeTracingOrbital's `uses[]` exports. */
type _StdKnowledgeTracingKnowledgeTracingOrbitalUsesRef = 'Posterior.traits.PosteriorBelief' | 'LabelCap.traits.LabelCaptureListener';

/** Per-orbital factory: builds the KnowledgeTracingOrbital orbital with consumer params. */
export function stdKnowledgeTracingKnowledgeTracingOrbital(params: StdKnowledgeTracingKnowledgeTracingOrbitalParams = {}): OrbitalDefinition {
  const built = makeOrbitalWithUses({
    name: 'KnowledgeTracingOrbital',
    uses: [
      {
        'as': 'Posterior',
        'from': 'std/behaviors/std-ml-posterior',
      },
      {
        'as': 'LabelCap',
        'from': 'std/behaviors/std-ml-label-capture',
      },
    ],
    entity: {
      name: 'KnowledgeState',
      persistence: 'runtime',
      shared: true,
      fields: ((): EntityField[] => {
        const canonical: EntityField[] = [
          {
            'default': '',
            'name': 'skillId',
            'type': 'string',
          },
          {
            'default': false,
            'name': 'lastOutcome',
            'type': 'boolean',
          },
          {
            'default': 1,
            'name': 'alpha',
            'type': 'number',
          },
          {
            'default': 1,
            'name': 'beta',
            'type': 'number',
          },
          {
            'default': 0,
            'name': 'mean',
            'type': 'number',
          },
          {
            'default': 0,
            'name': 'intervalLow',
            'type': 'number',
          },
          {
            'default': 0,
            'name': 'intervalHigh',
            'type': 'number',
          },
          {
            'default': 0,
            'name': 'observations',
            'type': 'number',
          },
          {
            'default': 'idle',
            'name': 'status',
            'type': 'string',
            'values': [
              'idle',
              'insufficient',
              'mastered',
              'remediate',
            ],
          },
          {
            'name': 'input',
            'type': 'string',
          },
          {
            'name': 'verdict',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'source',
            'type': 'string',
          },
          {
            'name': 'outcome',
            'type': 'string',
          },
          {
            'default': '',
            'name': 'sink',
            'type': 'string',
          },
          {
            'default': 0,
            'name': 'recordedAt',
            'type': 'number',
          },
        ];
        const extras = params.fields ?? [];
        if (extras.length === 0) return canonical;
        const extraNames = new Set(extras.map((f) => f.name));
        return [...canonical.filter((f) => !extraNames.has(f.name)), ...extras];
      })(),
    } as Entity,
    traits: [
      {
        'category': 'lifecycle',
        'emits': [
          {
            'description': 'Forwards the raw outcome to PosteriorTracker once skillId is recorded on the shared entity.',
            'event': 'OBSERVATION_MADE',
            'payloadSchema': [
              {
                'name': 'outcome',
                'required': true,
                'type': 'boolean',
              },
            ],
            'tier': 'secondary',
          },
          {
            'description': 'The posterior\'s verdict reshaped into a labeled example for LabelCaptureListener.',
            'event': 'CAPTURE_LABEL',
            'payloadSchema': [
              {
                'name': 'input',
                'required': true,
                'type': 'any',
              },
              {
                'name': 'verdict',
                'required': true,
                'type': 'any',
              },
              {
                'name': 'source',
                'required': true,
                'type': 'string',
              },
              {
                'name': 'outcome',
                'type': 'any',
              },
            ],
            'tier': 'secondary',
          },
        ],
        'linkedEntity': 'KnowledgeState',
        'listens': [
          {
            'event': 'MASTERY_REACHED',
            'payloadMapping': {
              'intervalHigh': '@payload.intervalHigh',
              'intervalLow': '@payload.intervalLow',
              'mean': '@payload.mean',
              'status': 'mastered',
            },
            'source': {
              'kind': 'trait',
              'trait': ('PosteriorTracker' satisfies _StdKnowledgeTracingListenTraitName),
            },
            'triggers': 'VERDICT_REACHED',
          },
          {
            'event': 'REMEDIATION_NEEDED',
            'payloadMapping': {
              'intervalHigh': '@payload.intervalHigh',
              'intervalLow': '@payload.intervalLow',
              'mean': '@payload.mean',
              'status': 'remediate',
            },
            'source': {
              'kind': 'trait',
              'trait': ('PosteriorTracker' satisfies _StdKnowledgeTracingListenTraitName),
            },
            'triggers': 'VERDICT_REACHED',
          },
          {
            'event': 'EVIDENCE_INSUFFICIENT',
            'payloadMapping': {
              'intervalHigh': '@payload.intervalHigh',
              'intervalLow': '@payload.intervalLow',
              'mean': '@payload.mean',
              'status': 'insufficient',
            },
            'source': {
              'kind': 'trait',
              'trait': ('PosteriorTracker' satisfies _StdKnowledgeTracingListenTraitName),
            },
            'triggers': 'VERDICT_REACHED',
          },
        ],
        'name': 'ObservationRelay',
        'scope': 'instance',
        'stateMachine': {
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'description': 'Public entry point — one true/false observation of a learner against one skill.',
              'key': 'OBSERVE_SKILL',
              'name': 'Observe Skill',
              'payloadSchema': [
                {
                  'name': 'skillId',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'outcome',
                  'required': true,
                  'type': 'boolean',
                },
              ],
              'tier': 'primary',
            },
            {
              'description': 'Any posterior verdict, tagged with which one it was.',
              'key': 'VERDICT_REACHED',
              'name': 'Verdict Reached',
              'payloadSchema': [
                {
                  'name': 'status',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'mean',
                  'required': true,
                  'type': 'float',
                },
                {
                  'name': 'intervalLow',
                  'required': true,
                  'type': 'float',
                },
                {
                  'name': 'intervalHigh',
                  'required': true,
                  'type': 'float',
                },
              ],
              'tier': 'secondary',
            },
            {
              'description': 'Forwards the raw outcome to PosteriorTracker once skillId is recorded on the shared entity.',
              'key': 'OBSERVATION_MADE',
              'name': 'Observation Made',
              'payloadSchema': [
                {
                  'name': 'outcome',
                  'required': true,
                  'type': 'boolean',
                },
              ],
              'tier': 'secondary',
            },
            {
              'description': 'The posterior\'s verdict reshaped into a labeled example for LabelCaptureListener.',
              'key': 'CAPTURE_LABEL',
              'name': 'Capture Label',
              'payloadSchema': [
                {
                  'name': 'input',
                  'required': true,
                  'type': 'any',
                },
                {
                  'name': 'verdict',
                  'required': true,
                  'type': 'any',
                },
                {
                  'name': 'source',
                  'required': true,
                  'type': 'string',
                },
                {
                  'name': 'outcome',
                  'type': 'any',
                },
              ],
              'tier': 'secondary',
            },
          ],
          'states': [
            {
              'isInitial': true,
              'name': 'idle',
            },
          ],
          'transitions': [
            {
              'effects': [
                [
                  'set',
                  '@entity.skillId',
                  '',
                ],
                [
                  'set',
                  '@entity.lastOutcome',
                  false,
                ],
              ],
              'event': 'INIT',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'set',
                  '@entity.skillId',
                  '@payload.skillId',
                ],
                [
                  'set',
                  '@entity.lastOutcome',
                  '@payload.outcome',
                ],
                [
                  'emit',
                  'OBSERVATION_MADE',
                  {
                    'outcome': '@payload.outcome',
                  },
                ],
              ],
              'event': 'OBSERVE_SKILL',
              'from': 'idle',
              'to': 'idle',
            },
            {
              'effects': [
                [
                  'emit',
                  'CAPTURE_LABEL',
                  {
                    'input': {
                      'skillId': '@entity.skillId',
                    },
                    'outcome': '@entity.lastOutcome',
                    'source': 'posterior',
                    'verdict': {
                      'intervalHigh': '@payload.intervalHigh',
                      'intervalLow': '@payload.intervalLow',
                      'mean': '@payload.mean',
                      'status': '@payload.status',
                    },
                  },
                ],
              ],
              'event': 'VERDICT_REACHED',
              'from': 'idle',
              'to': 'idle',
            },
          ],
        },
      } satisfies Trait,
      makeTraitRef({
        'config': {
          'intervalWidthMax': {
            'default': 0.25,
            'type': 'unknown',
          },
          'masteryFloor': {
            'default': 0.85,
            'type': 'unknown',
          },
          'priorAlpha': {
            'default': 1,
            'type': 'unknown',
          },
          'priorBeta': {
            'default': 1,
            'type': 'unknown',
          },
          'remediationCeiling': {
            'default': 0.35,
            'type': 'unknown',
          },
          'sampleCount': {
            'default': 200,
            'type': 'unknown',
          },
        },
        'linkedEntity': 'KnowledgeState',
        'listens': [
          {
            'event': 'OBSERVATION_MADE',
            'source': {
              'kind': 'trait',
              'trait': 'ObservationRelay',
            },
            'triggers': 'OBSERVE',
          },
        ],
        'name': 'PosteriorTracker',
        'ref': ('Posterior.traits.PosteriorBelief' satisfies _StdKnowledgeTracingKnowledgeTracingOrbitalUsesRef),
      }),
      makeTraitRef({
        'config': {
          'sampleRate': {
            'default': 1,
            'type': 'unknown',
          },
          'sink': {
            'default': 'knowledge-tracing',
            'type': 'unknown',
          },
        },
        'linkedEntity': 'KnowledgeState',
        'listens': [
          {
            'event': 'CAPTURE_LABEL',
            'source': {
              'kind': 'trait',
              'trait': 'ObservationRelay',
            },
            'triggers': 'CAPTURE',
          },
        ],
        'name': 'Labels',
        'ref': ('LabelCap.traits.LabelCaptureListener' satisfies _StdKnowledgeTracingKnowledgeTracingOrbitalUsesRef),
      }),
    ],
    pages: [
      {
        'name': 'KnowledgeTracingPage',
        'path': '/knowledge-tracing',
        'traits': [
          {
            'ref': 'ObservationRelay',
          },
          {
            'ref': 'PosteriorTracker',
          },
          {
            'ref': 'Labels',
          },
        ],
      } satisfies Page,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "linkedEntity" | "events" | "name" | "emitsScope" | "listens">;
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference & { name?: string };
      // Match by name so inline traits (no `ref`) and
      // reference traits (with `ref`) both pick up the
      // override surface keyed on the trait's `name`.
      if (typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) {
        merged.config = mergeCallSiteConfigOverrides(tr.config ?? {}, override.config);
      }
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.listens !== undefined) merged.listens = override.listens;
      return merged;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Manifest — describes the params surface of stdKnowledgeTracingKnowledgeTracingOrbital. */
export const StdKnowledgeTracingKnowledgeTracingOrbitalManifest = {
  organism: 'std-knowledge-tracing',
  orbitalName: 'KnowledgeTracingOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'entityName', type: 'string', description: 'Rename the canonical entity. PascalCase singular, ≤32 chars. Threads through every trait\'s linkedEntity binding; compiler rewrites @Entity.x refs.' },
    { name: 'traitOverrides', type: "Partial<Record<TraitName, { config?, linkedEntity?, events?, name?, emitsScope?, listens? }>>", description: 'Per-imported-trait overrides — mirrors .lolo\'s native trait-composition surface 1:1. effects is excluded (atom-owned; use listens via a sibling trait).' },
  ] as const,
  traitNames: [
    'PosteriorTracker',
    'Labels',
  ] as const,
  inlineTraitNames: [
    'ObservationRelay',
  ] as const,
};

/** Typed guard — runtime validates StdKnowledgeTracingKnowledgeTracingOrbitalParams keys. */
export function isStdKnowledgeTracingKnowledgeTracingOrbitalParams(p: object): p is StdKnowledgeTracingKnowledgeTracingOrbitalParams {
  type _OverrideRecord = NonNullable<StdKnowledgeTracingKnowledgeTracingOrbitalParams['traitOverrides']>;
  const obj = p as { traitOverrides?: _OverrideRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = [
      ...StdKnowledgeTracingKnowledgeTracingOrbitalManifest.traitNames,
      ...StdKnowledgeTracingKnowledgeTracingOrbitalManifest.inlineTraitNames,
    ];
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}
