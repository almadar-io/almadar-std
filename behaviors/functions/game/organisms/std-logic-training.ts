/**
 * std-logic-training — std-logic-training as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/organisms/std-logic-training.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-logic-training
 *
 * @level organism
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener, Trait, StateMachine } from '@almadar/core/types';
import type { MakeTraitRefOpts } from '@almadar/core/builders';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-logic-training';
const ALIAS = 'LogicTraining';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdLogicTrainingEventKey = 'COMPLETE' | 'DebugChallengeLoadFailed' | 'DebugChallengeLoaded' | 'INIT' | 'NAVIGATE' | 'RESTART' | 'START';

/**
 * Payload shape for the `DebugChallengeLoaded` event.
 */
export interface StdLogicTrainingDebugChallengeLoadedPayload {
  id: string;
  title: string;
  bugType?: string;
  score?: number;
  solved?: boolean;
  level?: number;
}

/**
 * Payload shape for the `DebugChallengeLoadFailed` event.
 */
export interface StdLogicTrainingDebugChallengeLoadFailedPayload {
  message?: string;
}

/**
 * Tunable params for the DebugChallengeOrbital orbital.
 *
 * Canonical entity: DebugChallenge (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdLogicTrainingDebugChallengeOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    never,
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'DebugChallengeDebuggerGame',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the DebugChallengeOrbital orbital with consumer params. */
export function stdLogicTrainingDebugChallengeOrbital(params: StdLogicTrainingDebugChallengeOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'DebugChallenge';
  const built = makeOrbitalWithUses({
    name: 'DebugChallengeOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'title',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'bugType',
          'type': 'string',
        },
        {
          'name': 'score',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'solved',
          'type': 'boolean',
          'default': false,
        },
        {
          'name': 'level',
          'type': 'number',
          'default': 1,
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      {
        'name': 'DebugChallengeDebuggerGame',
        'category': 'interaction',
        'linkedEntity': 'DebugChallenge',
        'emits': [
          {
            'event': 'DebugChallengeLoaded',
            'description': 'Fired when DebugChallenge finishes loading',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'title',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'bugType',
                'type': 'string',
              },
              {
                'name': 'score',
                'type': 'number',
              },
              {
                'name': 'solved',
                'type': 'boolean',
              },
              {
                'name': 'level',
                'type': 'number',
              },
            ],
          },
          {
            'event': 'DebugChallengeLoadFailed',
            'description': 'Fired when DebugChallenge fails to load',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'message',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'menu',
              'isInitial': true,
            },
            {
              'name': 'playing',
            },
            {
              'name': 'complete',
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'START',
              'name': 'Start',
            },
            {
              'key': 'NAVIGATE',
              'name': 'Navigate',
            },
            {
              'key': 'COMPLETE',
              'name': 'Complete',
            },
            {
              'key': 'RESTART',
              'name': 'Restart',
            },
            {
              'key': 'DebugChallengeLoaded',
              'name': 'DebugChallenge loaded',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'title',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'bugType',
                  'type': 'string',
                },
                {
                  'name': 'score',
                  'type': 'number',
                },
                {
                  'name': 'solved',
                  'type': 'boolean',
                },
                {
                  'name': 'level',
                  'type': 'number',
                },
              ],
            },
            {
              'key': 'DebugChallengeLoadFailed',
              'name': 'DebugChallenge load failed',
              'payloadSchema': [
                {
                  'name': 'message',
                  'type': 'string',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'menu',
              'to': 'menu',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'DebugChallenge',
                  {
                    'emit': {
                      'success': 'DebugChallengeLoaded',
                      'failure': 'DebugChallengeLoadFailed',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'showTopBar': true,
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'children': [
                      {
                        'title': 'Debug Challenge',
                        'menuItems': [
                          {
                            'event': 'START',
                            'variant': 'primary',
                            'label': 'Start',
                          },
                        ],
                        'type': 'game-menu',
                      },
                    ],
                  },
                ],
              ],
            },
            {
              'from': 'menu',
              'to': 'playing',
              'event': 'START',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'showTopBar': true,
                    'children': [
                      {
                        'type': 'stack',
                        'gap': 'md',
                        'direction': 'vertical',
                        'children': [
                          {
                            'stats': [
                              {
                                'label': 'Score',
                                'value': '@entity.score',
                              },
                              {
                                'label': 'Level',
                                'value': '@entity.level',
                              },
                            ],
                            'type': 'game-hud',
                          },
                          {
                            'type': 'debugger-board',
                            'completeEvent': 'COMPLETE',
                            'entity': 'DebugChallenge',
                          },
                        ],
                      },
                    ],
                  },
                ],
              ],
            },
            {
              'from': 'menu',
              'to': 'menu',
              'event': 'NAVIGATE',
            },
            {
              'from': 'playing',
              'to': 'complete',
              'event': 'COMPLETE',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'appName': 'Logic Training',
                    'type': 'game-shell',
                    'children': [
                      {
                        'type': 'game-over-screen',
                        'title': 'Well Done!',
                        'menuItems': [
                          {
                            'label': 'Play Again',
                            'variant': 'primary',
                            'event': 'RESTART',
                          },
                        ],
                      },
                    ],
                    'showTopBar': true,
                  },
                ],
              ],
            },
            {
              'from': 'complete',
              'to': 'menu',
              'event': 'RESTART',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'appName': 'Logic Training',
                    'showTopBar': true,
                    'children': [
                      {
                        'menuItems': [
                          {
                            'event': 'START',
                            'variant': 'primary',
                            'label': 'Start',
                          },
                        ],
                        'title': 'Debug Challenge',
                        'type': 'game-menu',
                      },
                    ],
                    'type': 'game-shell',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'DebuggerPage',
        'path': '/debugger',
        'traits': [
          {
            'ref': 'DebugChallengeDebuggerGame',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdLogicTrainingDebugChallengeOrbital. */
export const StdLogicTrainingDebugChallengeOrbitalManifest = {
  organism: 'std-logic-training',
  orbitalName: 'DebugChallengeOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'DebugChallengeDebuggerGame',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdLogicTrainingDebugChallengeOrbitalParams keys. */
export function isStdLogicTrainingDebugChallengeOrbitalParams(p: object): p is StdLogicTrainingDebugChallengeOrbitalParams {
  type _OverrideRecord = NonNullable<StdLogicTrainingDebugChallengeOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdLogicTrainingDebugChallengeOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdLogicTrainingDebugChallengeOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdLogicTrainingDebugChallengeOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the NegotiateChallengeOrbital orbital.
 *
 * Canonical entity: NegotiateChallenge (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdLogicTrainingNegotiateChallengeOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    never,
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'NegotiateChallengeNegotiatorGame',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the NegotiateChallengeOrbital orbital with consumer params. */
export function stdLogicTrainingNegotiateChallengeOrbital(params: StdLogicTrainingNegotiateChallengeOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'NegotiateChallenge';
  const built = makeOrbitalWithUses({
    name: 'NegotiateChallengeOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'title',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'scenario',
          'type': 'string',
        },
        {
          'name': 'score',
          'type': 'number',
          'default': 0,
        },
        {
          'name': 'outcome',
          'type': 'string',
        },
        {
          'name': 'level',
          'type': 'number',
          'default': 1,
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      {
        'name': 'NegotiateChallengeNegotiatorGame',
        'category': 'interaction',
        'linkedEntity': 'NegotiateChallenge',
        'emits': [
          {
            'event': 'NegotiateChallengeLoaded',
            'description': 'Fired when NegotiateChallenge finishes loading',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'title',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'scenario',
                'type': 'string',
              },
              {
                'name': 'score',
                'type': 'number',
              },
              {
                'name': 'outcome',
                'type': 'string',
              },
              {
                'name': 'level',
                'type': 'number',
              },
            ],
          },
          {
            'event': 'NegotiateChallengeLoadFailed',
            'description': 'Fired when NegotiateChallenge fails to load',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'message',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'menu',
              'isInitial': true,
            },
            {
              'name': 'playing',
            },
            {
              'name': 'complete',
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'START',
              'name': 'Start',
            },
            {
              'key': 'NAVIGATE',
              'name': 'Navigate',
            },
            {
              'key': 'COMPLETE',
              'name': 'Complete',
            },
            {
              'key': 'RESTART',
              'name': 'Restart',
            },
            {
              'key': 'NegotiateChallengeLoaded',
              'name': 'NegotiateChallenge loaded',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'title',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'scenario',
                  'type': 'string',
                },
                {
                  'name': 'score',
                  'type': 'number',
                },
                {
                  'name': 'outcome',
                  'type': 'string',
                },
                {
                  'name': 'level',
                  'type': 'number',
                },
              ],
            },
            {
              'key': 'NegotiateChallengeLoadFailed',
              'name': 'NegotiateChallenge load failed',
              'payloadSchema': [
                {
                  'name': 'message',
                  'type': 'string',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'menu',
              'to': 'menu',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'NegotiateChallenge',
                  {
                    'emit': {
                      'failure': 'NegotiateChallengeLoadFailed',
                      'success': 'NegotiateChallengeLoaded',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'title': 'Negotiate Challenge',
                        'menuItems': [
                          {
                            'variant': 'primary',
                            'label': 'Start',
                            'event': 'START',
                          },
                        ],
                        'type': 'game-menu',
                      },
                    ],
                    'showTopBar': true,
                    'appName': 'Logic Training',
                    'type': 'game-shell',
                  },
                ],
              ],
            },
            {
              'from': 'menu',
              'to': 'playing',
              'event': 'START',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'showTopBar': true,
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'children': [
                      {
                        'type': 'stack',
                        'direction': 'vertical',
                        'children': [
                          {
                            'type': 'game-hud',
                            'stats': [
                              {
                                'label': 'Score',
                                'value': '@entity.score',
                              },
                              {
                                'label': 'Level',
                                'value': '@entity.level',
                              },
                            ],
                          },
                          {
                            'completeEvent': 'COMPLETE',
                            'type': 'negotiator-board',
                            'entity': 'NegotiateChallenge',
                          },
                        ],
                        'gap': 'md',
                      },
                    ],
                  },
                ],
              ],
            },
            {
              'from': 'menu',
              'to': 'menu',
              'event': 'NAVIGATE',
            },
            {
              'from': 'playing',
              'to': 'complete',
              'event': 'COMPLETE',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'showTopBar': true,
                    'children': [
                      {
                        'type': 'game-over-screen',
                        'title': 'Well Done!',
                        'menuItems': [
                          {
                            'event': 'RESTART',
                            'label': 'Play Again',
                            'variant': 'primary',
                          },
                        ],
                      },
                    ],
                  },
                ],
              ],
            },
            {
              'from': 'complete',
              'to': 'menu',
              'event': 'RESTART',
              'effects': [
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'type': 'game-menu',
                        'title': 'Negotiate Challenge',
                        'menuItems': [
                          {
                            'event': 'START',
                            'label': 'Start',
                            'variant': 'primary',
                          },
                        ],
                      },
                    ],
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'showTopBar': true,
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'Negotiator',
        'path': '/negotiator',
        'traits': [
          {
            'ref': 'NegotiateChallengeNegotiatorGame',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdLogicTrainingNegotiateChallengeOrbital. */
export const StdLogicTrainingNegotiateChallengeOrbitalManifest = {
  organism: 'std-logic-training',
  orbitalName: 'NegotiateChallengeOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'NegotiateChallengeNegotiatorGame',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdLogicTrainingNegotiateChallengeOrbitalParams keys. */
export function isStdLogicTrainingNegotiateChallengeOrbitalParams(p: object): p is StdLogicTrainingNegotiateChallengeOrbitalParams {
  type _OverrideRecord = NonNullable<StdLogicTrainingNegotiateChallengeOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdLogicTrainingNegotiateChallengeOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdLogicTrainingNegotiateChallengeOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdLogicTrainingNegotiateChallengeOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Tunable params for the TrainingScoreOrbital orbital.
 *
 * Canonical entity: TrainingScore (locked — not overridable).
 * The factory hardcodes `linkedEntity` to the canonical entity on
 * every trait/page; renaming the entity would desync those references.
 *
 * Override surface (Phase 9):
 *   fields            — extra entity fields (appended)
 *   pagePath          — first-page URL override
 *   persistence       — entity persistence mode
 *   traitOverrides    — Zone 1: per-imported-trait override surface
 *                       (config / events / effects / listens / emitsScope /
 *                       linkedEntity). Atom owns topology.
 *   interactionStates — Zone 2: inline-trait state-machine splice
 *                       (states / events / transitions). Factory
 *                       owns topology.
 */
export interface StdLogicTrainingTrainingScoreOrbitalParams {
  /** Extra fields appended to the canonical entity. */
  fields?: EntityField[];
  /** URL path override for the orbital's first page. */
  pagePath?: string;
  /** Override the canonical entity persistence mode. */
  persistence?: EntityPersistence;
  /**
   * Zone 1 — per-imported-trait override surface. Keyed on each
   * imported trait's local `name`. The atom owns state-machine
   * topology (states/events/transitions); these fields rewrite the
   * caller-side surface only.
   */
  traitOverrides?: Partial<Record<
    never,
    Pick<MakeTraitRefOpts, 'config' | 'events' | 'effects' | 'listens' | 'emitsScope' | 'linkedEntity'>
  >>;
  /**
   * Zone 2 — inline-trait state-machine splice. Keyed on each inline
   * (factory-authored, no `ref:`) trait's `name`. Merges into the
   * matching inline trait's `stateMachine` block. Factory owns topology;
   * splice inserts caller-named states/events/transitions.
   */
  interactionStates?: Partial<Record<
    'TrainingScoreScoreBoard',
    Partial<Pick<NonNullable<Trait['stateMachine']>, 'states' | 'events' | 'transitions'>>
  >>;
}

/** Per-orbital factory: builds the TrainingScoreOrbital orbital with consumer params. */
export function stdLogicTrainingTrainingScoreOrbital(params: StdLogicTrainingTrainingScoreOrbitalParams = {}): OrbitalDefinition {
  const canonicalName = 'TrainingScore';
  const built = makeOrbitalWithUses({
    name: 'TrainingScoreOrbital',
    uses: [],
    entity: {
      name: canonicalName,
      persistence: params.persistence ?? 'runtime',
      fields: [
        {
          'name': 'id',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'playerName',
          'type': 'string',
          'required': true,
        },
        {
          'name': 'score',
          'type': 'number',
          'required': true,
          'default': 0,
        },
        {
          'name': 'category',
          'type': 'string',
        },
        {
          'name': 'completedAt',
          'type': 'string',
        },
        {
          'name': 'highScore',
          'type': 'number',
        },
        {
          'name': 'combo',
          'type': 'number',
        },
        {
          'name': 'multiplier',
          'type': 'number',
        },
        {
          'name': 'level',
          'type': 'number',
          'default': 1,
        },
        ...(params.fields ?? []),
      ],
    } as Entity,
    traits: [
      {
        'name': 'TrainingScoreScoreBoard',
        'category': 'interaction',
        'linkedEntity': 'TrainingScore',
        'emits': [
          {
            'event': 'TrainingScoreLoaded',
            'description': 'Fired when TrainingScore finishes loading',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'id',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'playerName',
                'type': 'string',
                'required': true,
              },
              {
                'name': 'score',
                'type': 'number',
                'required': true,
              },
              {
                'name': 'category',
                'type': 'string',
              },
              {
                'name': 'completedAt',
                'type': 'string',
              },
              {
                'name': 'highScore',
                'type': 'number',
              },
              {
                'name': 'combo',
                'type': 'number',
              },
              {
                'name': 'multiplier',
                'type': 'number',
              },
              {
                'name': 'level',
                'type': 'number',
              },
            ],
          },
          {
            'event': 'TrainingScoreLoadFailed',
            'description': 'Fired when TrainingScore fails to load',
            'scope': 'internal',
            'payloadSchema': [
              {
                'name': 'message',
                'type': 'string',
              },
            ],
          },
        ],
        'stateMachine': {
          'states': [
            {
              'name': 'idle',
              'isInitial': true,
            },
          ],
          'events': [
            {
              'key': 'INIT',
              'name': 'Initialize',
            },
            {
              'key': 'ADD_SCORE',
              'name': 'Add Score',
              'payloadSchema': [
                {
                  'name': 'points',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'COMBO',
              'name': 'Combo',
              'payloadSchema': [
                {
                  'name': 'multiplier',
                  'type': 'string',
                },
              ],
            },
            {
              'key': 'RESET',
              'name': 'Reset',
            },
            {
              'key': 'TrainingScoreLoaded',
              'name': 'TrainingScore loaded',
              'payloadSchema': [
                {
                  'name': 'id',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'playerName',
                  'type': 'string',
                  'required': true,
                },
                {
                  'name': 'score',
                  'type': 'number',
                  'required': true,
                },
                {
                  'name': 'category',
                  'type': 'string',
                },
                {
                  'name': 'completedAt',
                  'type': 'string',
                },
                {
                  'name': 'highScore',
                  'type': 'number',
                },
                {
                  'name': 'combo',
                  'type': 'number',
                },
                {
                  'name': 'multiplier',
                  'type': 'number',
                },
                {
                  'name': 'level',
                  'type': 'number',
                },
              ],
            },
            {
              'key': 'TrainingScoreLoadFailed',
              'name': 'TrainingScore load failed',
              'payloadSchema': [
                {
                  'name': 'message',
                  'type': 'string',
                },
              ],
            },
          ],
          'transitions': [
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'INIT',
              'effects': [
                [
                  'fetch',
                  'TrainingScore',
                  {
                    'emit': {
                      'success': 'TrainingScoreLoaded',
                      'failure': 'TrainingScoreLoadFailed',
                    },
                  },
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'children': [
                      {
                        'combo': '@entity.combo',
                        'level': '@entity.level',
                        'highScore': '@entity.highScore',
                        'score': '@entity.score',
                        'multiplier': '@entity.multiplier',
                        'type': 'score-board',
                      },
                    ],
                    'showTopBar': true,
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'ADD_SCORE',
              'effects': [
                [
                  'set',
                  '@entity.score',
                  [
                    '+',
                    '@entity.score',
                    '@payload.points',
                  ],
                ],
                [
                  'set',
                  '@entity.combo',
                  [
                    '+',
                    '@entity.combo',
                    1,
                  ],
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'appName': 'Logic Training',
                    'children': [
                      {
                        'multiplier': '@entity.multiplier',
                        'level': '@entity.level',
                        'score': '@entity.score',
                        'highScore': '@entity.highScore',
                        'type': 'score-board',
                        'combo': '@entity.combo',
                      },
                    ],
                    'type': 'game-shell',
                    'showTopBar': true,
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'COMBO',
              'effects': [
                [
                  'set',
                  '@entity.multiplier',
                  '@payload.multiplier',
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'type': 'score-board',
                        'highScore': '@entity.highScore',
                        'score': '@entity.score',
                        'combo': '@entity.combo',
                        'multiplier': '@entity.multiplier',
                        'level': '@entity.level',
                      },
                    ],
                    'type': 'game-shell',
                    'appName': 'Logic Training',
                    'showTopBar': true,
                  },
                ],
              ],
            },
            {
              'from': 'idle',
              'to': 'idle',
              'event': 'RESET',
              'effects': [
                [
                  'set',
                  '@entity.score',
                  0,
                ],
                [
                  'set',
                  '@entity.combo',
                  0,
                ],
                [
                  'set',
                  '@entity.multiplier',
                  1,
                ],
                [
                  'render-ui',
                  'main',
                  {
                    'children': [
                      {
                        'combo': '@entity.combo',
                        'highScore': '@entity.highScore',
                        'score': '@entity.score',
                        'multiplier': '@entity.multiplier',
                        'level': '@entity.level',
                        'type': 'score-board',
                      },
                    ],
                    'showTopBar': true,
                    'appName': 'Logic Training',
                    'type': 'game-shell',
                  },
                ],
              ],
            },
          ],
        },
        'scope': 'instance',
      } as never,
    ],
    pages: [
      {
        'name': 'Scores',
        'path': '/scores',
        'traits': [
          {
            'ref': 'TrainingScoreScoreBoard',
          },
        ],
      } as never,
    ],
  });
  type _OrbTrait = OrbitalDefinition["traits"][number];
  type _OrbPage = NonNullable<OrbitalDefinition["pages"]>[number];
  type _RefOverride = Pick<MakeTraitRefOpts, "config" | "events" | "effects" | "listens" | "emitsScope" | "linkedEntity">;
  type _InlineSplice = Partial<Pick<NonNullable<Trait["stateMachine"]>, "states" | "events" | "transitions">>;
  // Zone 1: per-imported-trait override surface, keyed by trait `name`.
  // After the `ref:`-string filter, the trait variant is TraitReference,
  // whose surface matches the override surface 1:1.
  if (built.traits && params.traitOverrides !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as TraitReference;
      if (typeof tr.ref !== "string" || typeof tr.name !== "string") return t;
      const overrides = params.traitOverrides as Record<string, _RefOverride | undefined> | undefined;
      const override = overrides?.[tr.name];
      if (!override) return t;
      const merged: TraitReference = { ...tr };
      if (override.config !== undefined) merged.config = { ...(tr.config ?? {}), ...override.config };
      if (override.events !== undefined) merged.events = { ...(tr.events ?? {}), ...override.events };
      if (override.effects !== undefined) merged.effects = { ...(tr.effects ?? {}), ...override.effects };
      if (override.listens !== undefined) merged.listens = override.listens;
      if (override.emitsScope !== undefined) merged.emitsScope = override.emitsScope;
      if (override.linkedEntity !== undefined) merged.linkedEntity = override.linkedEntity;
      return merged;
    });
  }
  // Zone 2: inline-trait state-machine splice, keyed by inline trait `name`.
  // After the `ref:`-not-set filter, the trait variant is Trait, whose
  // stateMachine block is what we splice into.
  if (built.traits && params.interactionStates !== undefined) {
    built.traits = (built.traits as _OrbTrait[]).map((t): _OrbTrait => {
      if (!t || typeof t !== "object") return t;
      const tr = t as Trait & { ref?: string };
      if (typeof tr.ref === "string" || typeof tr.name !== "string") return t;
      const splices = params.interactionStates as Record<string, _InlineSplice | undefined> | undefined;
      const splice = splices?.[tr.name];
      if (!splice || !tr.stateMachine) return t;
      const sm = tr.stateMachine;
      const mergedSm: StateMachine = {
        ...sm,
        ...(splice.states !== undefined ? { states: [...(sm.states ?? []), ...splice.states] } : {}),
        ...(splice.events !== undefined ? { events: [...(sm.events ?? []), ...splice.events] } : {}),
        ...(splice.transitions !== undefined ? { transitions: [...(sm.transitions ?? []), ...splice.transitions] } : {}),
      };
      const mergedTrait: Trait = { ...tr, stateMachine: mergedSm };
      return mergedTrait;
    });
  }
  if (built.pages && params.pagePath !== undefined) {
    built.pages = (built.pages as _OrbPage[]).map((p, idx) => {
      if (!p || typeof p !== "object") return p;
      if (idx !== 0) return p;
      const out = { ...p } as _OrbPage & { path?: string };
      out.path = params.pagePath;
      return out;
    });
  }
  return built;
}

/** Phase 9 manifest — describes the params surface of stdLogicTrainingTrainingScoreOrbital. */
export const StdLogicTrainingTrainingScoreOrbitalManifest = {
  organism: 'std-logic-training',
  orbitalName: 'TrainingScoreOrbital',
  paramFields: [
    { name: 'fields', type: 'EntityField[]', description: 'Extra fields appended to the canonical entity.' },
    { name: 'pagePath', type: 'string', description: 'URL override for the orbital first page.' },
    { name: 'persistence', type: "'persistent' | 'runtime' | 'singleton' | 'instance' | 'local'", description: 'Override the canonical entity persistence mode.' },
    { name: 'traitOverrides', type: 'Partial<Record<TraitName, MakeTraitRefOpts>>', description: 'Zone 1 — per-imported-trait override surface (config / events / effects / listens / emitsScope / linkedEntity). Atom owns topology.' },
    { name: 'interactionStates', type: 'Partial<Record<InlineTraitName, StateMachineSplice>>', description: 'Zone 2 — inline-trait state-machine splice (states / events / transitions). Factory owns topology.' },
  ] as const,
  traitNames: [
  ] as const,
  inlineTraitNames: [
    'TrainingScoreScoreBoard',
  ] as const,
};

/** Phase 9 typed guard — runtime validates StdLogicTrainingTrainingScoreOrbitalParams keys. */
export function isStdLogicTrainingTrainingScoreOrbitalParams(p: object): p is StdLogicTrainingTrainingScoreOrbitalParams {
  type _OverrideRecord = NonNullable<StdLogicTrainingTrainingScoreOrbitalParams['traitOverrides']>;
  type _SpliceRecord = NonNullable<StdLogicTrainingTrainingScoreOrbitalParams['interactionStates']>;
  const obj = p as { traitOverrides?: _OverrideRecord; interactionStates?: _SpliceRecord };
  if (obj.traitOverrides !== undefined) {
    if (typeof obj.traitOverrides !== "object" || obj.traitOverrides === null) return false;
    const allowed: readonly string[] = StdLogicTrainingTrainingScoreOrbitalManifest.traitNames;
    for (const k of Object.keys(obj.traitOverrides)) {
      if (!allowed.includes(k)) return false;
    }
  }
  if (obj.interactionStates !== undefined) {
    if (typeof obj.interactionStates !== "object" || obj.interactionStates === null) return false;
    const allowed: readonly string[] = StdLogicTrainingTrainingScoreOrbitalManifest.inlineTraitNames;
    for (const k of Object.keys(obj.interactionStates)) {
      if (!allowed.includes(k)) return false;
    }
  }
  return true;
}

/**
 * Bundled params for std-logic-training — one optional entry per orbital.
 * Each entry maps to its per-orbital factory above.
 */
export interface StdLogicTrainingParams {
  DebugChallenge?: StdLogicTrainingDebugChallengeOrbitalParams;
  NegotiateChallenge?: StdLogicTrainingNegotiateChallengeOrbitalParams;
  TrainingScore?: StdLogicTrainingTrainingScoreOrbitalParams;
}

/** Whole-organism descriptor (3 orbitals). Composes per-orbital factories. */
export function stdLogicTraining(params: StdLogicTrainingParams = {}): OrbitalDefinition[] {
  return [
    stdLogicTrainingDebugChallengeOrbital(params.DebugChallenge ?? {}),
    stdLogicTrainingNegotiateChallengeOrbital(params.NegotiateChallenge ?? {}),
    stdLogicTrainingTrainingScoreOrbital(params.TrainingScore ?? {}),
  ];
}
