/**
 * std-service-redis — std-service-redis as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-service-redis.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-service-redis
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, EntityRow, SExpr, TraitEventListener } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-service-redis';
const ALIAS = 'ServiceRedis';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdServiceRedisEventKey = 'DELETE_KEY' | 'EXECUTED' | 'FAILED' | 'GET_KEY' | 'INIT' | 'RESET' | 'SET_KEY' | 'ServiceRedisLoadFailed' | 'ServiceRedisLoaded' | 'ServiceRedisRedisCompleted' | 'ServiceRedisRedisFailed';

/**
 * Payload shape for the `ServiceRedisLoaded` event.
 */
export interface StdServiceRedisServiceRedisLoadedPayload {
  id?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
  key?: string;
  value?: string;
  ttl?: number;
  result?: string;
  redisStatus?: string;
  error?: string;
}

/**
 * Payload shape for the `ServiceRedisLoadFailed` event.
 */
export interface StdServiceRedisServiceRedisLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `ServiceRedisRedisCompleted` event.
 */
export interface StdServiceRedisServiceRedisRedisCompletedPayload {
  result?: EntityRow;
}

/**
 * Payload shape for the `ServiceRedisRedisFailed` event.
 */
export interface StdServiceRedisServiceRedisRedisFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-service-redis descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdServiceRedisParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdServiceRedisEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ServiceRedis.traits.ServiceRedisRedis`. */
export function stdServiceRedisTrait(params: StdServiceRedisParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ServiceRedisRedis`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ServiceRedis.pages.ServiceRedisPage`. */
export function stdServiceRedisPage(params: StdServiceRedisParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ServiceRedisPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdServiceRedis(params: StdServiceRedisParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ServiceRedisOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdServiceRedisTrait(params),
    ],
    pages: [
      stdServiceRedisPage(params),
    ],
  });
}
