/**
 * std-service-oauth — std-service-oauth as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-service-oauth.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-service-oauth
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-service-oauth';
const ALIAS = 'ServiceOauth';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdServiceOauthEventKey = 'AUTH_URL_RECEIVED' | 'CALLBACK' | 'FAILED' | 'INIT' | 'LOGIN' | 'LOGOUT' | 'REFRESH' | 'RETRY' | 'ServiceOauthLoadFailed' | 'ServiceOauthLoaded' | 'ServiceOauthOauthCompleted' | 'ServiceOauthOauthFailed' | 'TOKEN_RECEIVED' | 'TOKEN_REFRESHED';

/**
 * Payload shape for the `ServiceOauthLoaded` event.
 */
export interface StdServiceOauthServiceOauthLoadedPayload {
  id: string;
  provider?: string;
  authUrl?: string;
  accessToken?: string;
  refreshToken?: string;
  authStatus?: string;
  error?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
}

/**
 * Payload shape for the `ServiceOauthLoadFailed` event.
 */
export interface StdServiceOauthServiceOauthLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `ServiceOauthOauthCompleted` event.
 */
export interface StdServiceOauthServiceOauthOauthCompletedPayload {
  result?: Record<string, unknown>;
}

/**
 * Payload shape for the `ServiceOauthOauthFailed` event.
 */
export interface StdServiceOauthServiceOauthOauthFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-service-oauth descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdServiceOauthParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdServiceOauthEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ServiceOauth.traits.ServiceOauthOauth`. */
export function stdServiceOauthTrait(params: StdServiceOauthParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ServiceOauthOauth`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ServiceOauth.pages.ServiceOauthPage`. */
export function stdServiceOauthPage(params: StdServiceOauthParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ServiceOauthPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdServiceOauth(params: StdServiceOauthParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ServiceOauthOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdServiceOauthTrait(params),
    ],
    pages: [
      stdServiceOauthPage(params),
    ],
  });
}
