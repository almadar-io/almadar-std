/**
 * std-service-twilio — std-service-twilio as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-service-twilio.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-service-twilio
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig, TraitFieldRef, EntityRow, SExpr, TraitEventListener } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-service-twilio';
const ALIAS = 'ServiceTwilio';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdServiceTwilioEventKey = 'FAILED' | 'INIT' | 'RESET' | 'RETRY' | 'SEND_SMS' | 'SEND_WHATSAPP' | 'SENT' | 'ServiceTwilioLoadFailed' | 'ServiceTwilioLoaded' | 'ServiceTwilioTwilioCompleted' | 'ServiceTwilioTwilioFailed';

/**
 * Payload shape for the `ServiceTwilioLoaded` event.
 */
export interface StdServiceTwilioServiceTwilioLoadedPayload {
  id?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
  to?: string;
  messageBody?: string;
  channel?: string;
  messageSid?: string;
  sendStatus?: string;
  error?: string;
}

/**
 * Payload shape for the `ServiceTwilioLoadFailed` event.
 */
export interface StdServiceTwilioServiceTwilioLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `ServiceTwilioTwilioCompleted` event.
 */
export interface StdServiceTwilioServiceTwilioTwilioCompletedPayload {
  result?: EntityRow;
}

/**
 * Payload shape for the `ServiceTwilioTwilioFailed` event.
 */
export interface StdServiceTwilioServiceTwilioTwilioFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-service-twilio descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdServiceTwilioParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdServiceTwilioEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, SExpr[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: TraitEventListener[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ServiceTwilio.traits.ServiceTwilioTwilio`. */
export function stdServiceTwilioTrait(params: StdServiceTwilioParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ServiceTwilioTwilio`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects } : {}),
    ...(params.listens !== undefined ? { listens: params.listens } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ServiceTwilio.pages.ServiceTwilioPage`. */
export function stdServiceTwilioPage(params: StdServiceTwilioParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ServiceTwilioPage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdServiceTwilio(params: StdServiceTwilioParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ServiceTwilioOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdServiceTwilioTrait(params),
    ],
    pages: [
      stdServiceTwilioPage(params),
    ],
  });
}
