/**
 * std-service-storage — std-service-storage as a Function
 *
 * Autogenerated by scripts/regenerate-std-ts.mjs from
 * packages/almadar-std/behaviors/registry/atoms/std-service-storage.orb.
 *
 * This is a thin descriptor layer over the canonical .orb. The exported
 * functions return TraitReference / PageRefObject / OrbitalDefinition
 * payloads that reference the sibling .orb via `uses:` + `ref:`. The
 * compiler's inline phase materialises the real trait at build time.
 *
 * Do not hand-edit — regenerate via
 *   node scripts/regenerate-std-ts.mjs --only=std-service-storage
 *
 * @level atom
 * @packageDocumentation
 */

import type { TraitReference, PageRefObject, OrbitalDefinition, Entity, EntityField, EntityPersistence, TraitConfig } from '@almadar/core/types';
import { makeTraitRef, makePageRef, makeOrbitalWithUses } from '@almadar/core/builders';

const BEHAVIOR_PATH = 'std/behaviors/std-service-storage';
const ALIAS = 'ServiceStorage';

/**
 * Closed set of event keys this trait recognises —
 * derived from the .orb's `stateMachine.events[]` block
 * (transition triggers + emit names). Use as the key type
 * when passing an `events:` rename map at the call site.
 */
export type StdServiceStorageEventKey = 'DELETE_FILE' | 'DOWNLOAD_FILE' | 'EXECUTED' | 'FAILED' | 'INIT' | 'LIST_FILES' | 'RESET' | 'ServiceStorageLoadFailed' | 'ServiceStorageLoaded' | 'ServiceStorageStorageCompleted' | 'ServiceStorageStorageFailed' | 'UPLOAD_FILE';

/**
 * Payload shape for the `ServiceStorageLoaded` event.
 */
export interface StdServiceStorageServiceStorageLoadedPayload {
  id?: string;
  name?: string;
  description?: string;
  status?: string;
  createdAt?: string;
  bucket?: string;
  fileKey?: string;
  prefix?: string;
  content?: string;
  storageStatus?: string;
  result?: string;
  error?: string;
}

/**
 * Payload shape for the `ServiceStorageLoadFailed` event.
 */
export interface StdServiceStorageServiceStorageLoadFailedPayload {
  message?: string;
}

/**
 * Payload shape for the `ServiceStorageStorageCompleted` event.
 */
export interface StdServiceStorageServiceStorageStorageCompletedPayload {
  result?: Record<string, unknown>;
}

/**
 * Payload shape for the `ServiceStorageStorageFailed` event.
 */
export interface StdServiceStorageServiceStorageStorageFailedPayload {
  error?: string;
  code?: string;
}

/**
 * Params for the std-service-storage descriptor helpers.
 *
 * `entityName` binds every trait/page reference's `linkedEntity`.
 * The optional override fields mirror TraitReference / PageRefObject
 * fields and are forwarded to `makeTraitRef` / `makePageRef`.
 */
export interface StdServiceStorageParams {
  entityName: string;
  /** Extra fields to add to the orbital-scoped entity clone. */
  fields?: EntityField[];
  /** Entity persistence mode. Defaults to `persistent` when omitted.
   *  See @almadar/core EntityPersistence: persistent | runtime | singleton | instance | local. */
  persistence?: EntityPersistence;
  /** Rename the inlined trait at the call site. */
  traitName?: string;
  /** Per-key event rename map. Keys narrow to the trait's declared emit names. */
  events?: Partial<Record<StdServiceStorageEventKey, string>>;
  /** Per-event effect replacement (keys are POST-rename event names). */
  effects?: Record<string, unknown[]>;
  /** Replace the imported trait's `listens` array entirely. */
  listens?: unknown[];
  /** Set every emit's scope. */
  emitsScope?: 'internal' | 'external';
  /** Nested config override (outer key = config field name). */
  config?: TraitConfig;
  /** URL path override for the (first) page. */
  pagePath?: string;
}

/** Trait descriptor: `ServiceStorage.traits.ServiceStorageStorage`. */
export function stdServiceStorageTrait(params: StdServiceStorageParams): TraitReference {
  return makeTraitRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.traits.ServiceStorageStorage`,
    linkedEntity: params.entityName,
    ...(params.traitName !== undefined ? { name: params.traitName } : {}),
    ...(params.events !== undefined ? { events: params.events as Record<string, string> } : {}),
    ...(params.effects !== undefined ? { effects: params.effects as Record<string, never> } : {}),
    ...(params.listens !== undefined ? { listens: params.listens as never } : {}),
    ...(params.emitsScope !== undefined ? { emitsScope: params.emitsScope } : {}),
    ...(params.config !== undefined ? { config: params.config as TraitConfig } : {}),
  });
}

/** Page descriptor: `ServiceStorage.pages.ServiceStoragePage`. */
export function stdServiceStoragePage(params: StdServiceStorageParams): PageRefObject {
  return makePageRef({
    from: BEHAVIOR_PATH,
    ref: `${ALIAS}.pages.ServiceStoragePage`,
    ...(params.pagePath !== undefined ? { path: params.pagePath } : {}),
    linkedEntity: params.entityName,
  });
}

/** Whole-orbital descriptor. */
export function stdServiceStorage(params: StdServiceStorageParams): OrbitalDefinition {
  const entity: Entity = {
    name: params.entityName,
    fields: params.fields ?? [],
    ...(params.persistence !== undefined ? { persistence: params.persistence } : {}),
  };
  return makeOrbitalWithUses({
    name: 'ServiceStorageOrbital',
    uses: [{ from: BEHAVIOR_PATH, as: ALIAS }],
    entity,
    traits: [
      stdServiceStorageTrait(params),
    ],
    pages: [
      stdServiceStoragePage(params),
    ],
  });
}
