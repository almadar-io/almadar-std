<!-- Gap ledger for this repo: the source of truth for its open gaps. Managed with scripts/gaps-ledger.mjs in the Almadar monorepo. -->
# @almadar/std — open gaps

Every open gap this repo owns lives here. This file is the source of truth; the monorepo's `docs/Almadar_Gaps.md` only rolls it up.

- **One entry per gap:** `- **<code>** — <what is wrong and where>. <owning package> [mechanical|architectural] — <evidence, prevention rung>`. `[mechanical]` = small and well-scoped; `[architectural]` = needs design judgment.
- **Codes:** new gaps use this repo's prefix `G-STD-`. Take the "Next code" below, then bump it in the same edit. Codes are never reused or renamed.
- **Close by deleting.** Remove the entry in the same commit as the fix. There is no "closed" section; git history is the record.
- **Cross-repo gaps don't go here.** If fixing it needs another repo, describe it in your report or PR body; the monorepo coordinator files it.

Next code: `G-STD-033`

## Open gaps

### Std tier (`@almadar/std`)

- **G-STD-005** — NARROWED 2026-09-25: all 31 candidates now route every `searchEvent` they declare through a `listens` route (0 unrouted). Residual: a per-site typed-query probe (functional check only verified on project-friday). RE-AUDITED 2026-09-21, narrowed but still open: `searchEvent` is a `render-ui` PATTERN PROP (`DashboardLayout.tsx` does `eventBus.emit(\`UI:${searchEvent}\`, {value})` on Enter), the same dynamic-emit shape as `G-STD-002` (invalidated) — `audit_listens` genuinely cannot see it, which is why the ledger's own earlier spot-check read `std-hr-portal.lolo` as "wired". Ran all 48 files referencing `searchEvent` through `audit_listens` (confirms it's blind to the whole class, as expected) and a text-based heuristic (does the same file's search-results pattern carry a `listens={{ "Trait.<CUSTOM_SEARCH_EVENT>": ... }}` prop anywhere): **17/48 show a same-file listens reference, 29/48 show none.** The 29 are real candidates for "search box renders but typing does nothing" — a genuine UI-level defect, not an audit-tool artifact — but the heuristic has false-negative risk (a consumer could listen from a different composed file, or a search-results pattern could use a differently-shaped prop), so each of the 29 needs an actual `play_scene`/interactive probe (type a query, confirm the list filters) before being called broken, not just a text scan. Candidate list: `std-retail-pos, std-document-mgmt, std-lms, std-construction-pm, std-project-manager, std-realtime-chat, std-social-feed, std-property-mgmt, std-survey, std-booking-system, std-restaurant-pos, std-ats-recruiting, std-accounting, std-builder, std-task-manager, std-time-tracking, std-trading-dashboard, std-marketing-campaign, std-notes, std-coding-academy, std-api-gateway, std-cicd-pipeline, std-subscription-billing, std-devops-dashboard, std-marketplace, std-cms, std-wiki, std-public-help-center, std-blaz-klemenc, std-crm, std-nonprofit-donations`. `[needs live play_scene verification per site before a mechanical fix pass — not a static-lint task]` — orig: `Almadar_Std_Gaps.md` → S2 residual
- **G-STD-006** — Game combat/progression behaviors (`std-trait-wars` ×8, `std-iram` ×5) trip `ORB_BINDING_PERSIST_ROW_ID_NEVER_SET` — writes land at row `""`. Owner has deferred games. `[medium, confirm before assigning]` — orig: `Almadar_Std_Gaps.md` → item 4
- **G-STD-008** — Biology-lab `phase-cycle` renders disabled-not-absent guess buttons as a workaround; a second workaround queued pending runtime probes. `[medium, flagged compromise]` — orig: `Almadar_Kflow_Sdk_Gaps.md` → 6f
- **G-STD-009** — LIKELY STALE (2026-09-21 sweep): dated from the earliest investigation pass, before `ui/learning` was wholesale rewritten (28 new instrument atoms) in the later Wave-1–4 atomization campaign. No specific file named; needs fresh visual verification against the current architecture before assignment. `[needs visual verification]` — orig: `Almadar_Kflow_Sdk_Gaps.md` → 0b
- **G-STD-010** — PARTIALLY ADDRESSED (2026-09-21 sweep): its named example (physics labs) already got explicit Start/Pause/Reset controls in the Wave-4 atomization; the general class may still apply to other instrument atoms — needs a fresh sweep across all of them, not just physics, before assignment. `[medium, design-adjacent]` — orig: `Almadar_Kflow_Sdk_Gaps.md` → 0d
- **G-STD-011** — `std-pickup` feature idea: optional `active` flag for gated pickups + `FEATURES_ACTIVE`/`ADD_FEATURES` events (new work, not a defect). `[mechanical-medium, new .lolo authoring]` — orig: `Almadar_Riya_Gaps.md`
- **G-STD-012** — Riya sprite/game atom families ship stale embeddings until a keyed rebuild runs. `[ops task, not code]` — orig: `Almadar_Riya_Gaps.md`
- **G-STD-013** — Headless-Chromium platformer runs use atom-default gravity instead of config gravity; headed browser unaffected. `[high, needs live browser debugging]` — orig: `Almadar_Std_Game_Gaps.md`
- **G-STD-014** — `play_scene`'s filmstrip is the only instrument catching camera-pose instability; no automated temporal-stability assertion exists. `[medium-high, verify-tool design]` — orig: `Almadar_Std_Game_Gaps.md`
- **G-STD-019** — `std-tabs` declares `TabSpec.content : trait` and documents per-tab content traits, but the atom's `INIT`/`TAB_CHANGED` arms `(set @entity.items @config.tabs)`, so any `content: @trait.X` a call site passes trips `ORB_BINDING_TRAIT_OUTSIDE_RENDER_UI`; and the `tabs` pattern's `items[].content` is not a node slot, so inlining it in `bodyContent` trips `ORB_BINDING_TRAIT_INVALID_POSITION`. The advertised per-tab content surface is unusable. Workaround in `std-almadar-pitch`: label-only `tabs` + `presence` children gated on `@entity.activeTab` in `bodyContent`. Prevention rung: `orb validate` caught it; the defect is the atom contract (drop `content` from `TabSpec`, or make tab content a node slot end-to-end). `[architectural, std atom + pattern contract]`
